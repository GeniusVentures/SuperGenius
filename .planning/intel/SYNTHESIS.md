# Synthesis Summary

Entry point for gsd-roadmapper. Generated by gsd-doc-synthesizer, 2026-08-26 (merge mode).

## Doc counts by type

- Total ingested: 1
- DOC: 1 (.planning/notes/ELM-bridging-gaps.md — medium confidence, manifest override; carries owner-directed scope corrections for GitHub issue #369)
- ADR / SPEC / PRD: 0

## Cycle detection

- Ref graph: 1 node; cross_refs ("issue #369", "SGElmProcessing.proto") point outside the ingest set → no intra-set edges.
- Result: acyclic, depth 1 (cap 50). Synthesis proceeded on the full set.

## Decisions

- Total: 8 (intel/decisions.md, D-ELM-01..08) — **0 locked**. All non-locked at DOC precedence; D-ELM-07/D-ELM-08 are owner/orchestrator directives.
- Highlights: SuperGenius IS the scheduler, GCS only creates/funds/publishes (D-ELM-01); ELM array in existing `Task.json_data`, no main-proto change (D-ELM-02, corroborated against `src/processing/proto/SGProcessing.proto`); local content-addressed model cache keyed by manifest hash, no advertising, cache-neutral eligibility (D-ELM-03); model download is billable job work at the fixed rate (D-ELM-04); real execution work belongs in SGProcessingManager (D-ELM-05); optional separate `SGElmProcessing.proto` for events/streaming only, not needed for first non-streaming proof (D-ELM-06); v1.0 product pre-ship placement (D-ELM-07); SGProcessingManager work-split with different assignee (D-ELM-08).

## Requirements

- Total: 11 provisional IDs (intel/requirements.md): REQ-ELM-JOB-MODEL, REQ-ELM-SCOPE-REMOVAL, REQ-ELM-SCHEDULER, REQ-ELM-MANIFEST, REQ-ELM-CACHE, REQ-ELM-RUNTIME, REQ-ELM-RESULTS, REQ-ELM-FUNDING, REQ-ELM-PROTO-OPTIONAL, REQ-ELM-GCS-API, REQ-ELM-V1-SHIP.
- Routing note: REQ-ELM-RUNTIME (SGProcessingManager execution) is the split candidate per D-ELM-08.

## Constraints

- Total: 8 (intel/constraints.md) — protocol: 1 (C-ELM-01 GCS must not schedule); api-contract: 2 (C-ELM-02 no main-proto change + forbidden messages, C-ELM-07 worker nodes expose no /v1); schema: 2 (C-ELM-03 ELM job JSON, C-ELM-04 model manifest); nfr: 3 (C-ELM-05 cache-neutral eligibility, C-ELM-06 fixed pricing no negotiation — rate units flagged, C-ELM-08 v1.0 placement).

## Context topics

- Total: 6 (intel/context.md): corrected architecture, superseded design (do-not-synthesize list), Task/SubTask mapping, model fetch/cache flow, repository facts grounding the split, milestone placement facts.

## Conflicts

- Blockers: 0 — no LOCKED decisions in ingest or existing context touching ELM/#369/advertising/bidding (verified across EXISTING_CONTEXT).
- Competing variants (WARNING): 2 — (1) "v1.0 milestone track" placement: owner's product-v1.0 pre-ship directive vs the already-shipped internal "v1.0 GeniusNode Construction Refactor" milestone (current is v1.1); (2) fixed-rate units: "$.0003 cents/hour" vs "$0.0003/hour" (1000x) in the same source.
- Auto-resolved (INFO): 4 — doc's own retraction honored (superseded design excluded); pre-existing planning drift recorded (MILESTONES vs ROADMAP vs STATE vs PROJECT on v1.1 status, stray "v2.0" reference); DOC-with-directives handling per manifest override (nothing overridden, all marked non-locked); SGProcessingManager work-split preserved rather than merged.

Detail: .planning/INGEST-CONFLICTS.md

## Pointers

- Decisions: .planning/intel/decisions.md
- Requirements: .planning/intel/requirements.md
- Constraints: .planning/intel/constraints.md
- Context: .planning/intel/context.md
- Classification input: .planning/intel/classifications/ELM-bridging-gaps-2c574375.json

## Status

AWAITING USER — 2 competing variants need resolution before routing: (a) define/confirm the product-v1.0 pre-ship milestone track for REQ-ELM-*; (b) confirm the canonical fixed rate ($0.0003/hour presumed). No blockers; roadmapping may otherwise proceed.
