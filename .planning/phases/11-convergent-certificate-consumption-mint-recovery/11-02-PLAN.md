---
phase: 11-convergent-certificate-consumption-mint-recovery
plan: "02"
type: tdd
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/account/TransactionManager.hpp
  - src/account/TransactionManager.cpp
  - test/src/account/transaction_manager_certificate_fallback_test.cpp
autonomous: true
requirements:
  - CERT-05
  - MINT-01
user_setup: []
must_haves:
  truths:
    - "Certificate-first processing finds the exact winning transaction from CRDT before using the exact embedded transaction as fallback."
    - "Transaction-first, certificate-first, and later transaction delivery all enter the same confirmation lifecycle."
    - "A same-slot losing Mint is never confirmed or applied from the winner's certificate."
  artifacts:
    - path: src/account/TransactionManager.hpp
      provides: "Private exact-hash CRDT transaction recovery helper declaration"
    - path: src/account/TransactionManager.cpp
      provides: "Exact-bound certificate-first candidate selection before ChangeTransactionState(CONFIRMED)"
      contains: "OnConsensusCertificate"
    - path: test/src/account/transaction_manager_certificate_fallback_test.cpp
      provides: "CRDT-first, embedded-fallback, and competing-Mint certificate ingress regressions"
      contains: "CertificateFallbackTest"
  key_links:
    - from: "TransactionManager::OnConsensusCertificate"
      to: "TransactionManager::FetchTransaction"
      via: "monitored-network exact-hash CRDT recovery before embedded fallback"
      pattern: "OnConsensusCertificate[\\s\\S]*FetchTransaction"
    - from: "TransactionManager::OnConsensusCertificate"
      to: "TransactionManager::CertificateMatchesTransaction"
      via: "mandatory guard before ChangeTransactionState(CONFIRMED)"
      pattern: "CertificateMatchesTransaction"
---

<objective>
Make certificate-first Mint consumption select only the exact certified winner and route it through the established transaction lifecycle, regardless of whether transaction or certificate data arrives first (D-02, D-03, D-04, D-05, D-06).

Purpose: eliminate divergent certificate-first processing and prevent a same-slot contender from substituting for the certified Mint.
Output: exact-hash CRDT recovery helper, guarded certificate-first handler, and focused account regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-CONTEXT.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-RESEARCH.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-VALIDATION.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-PATTERNS.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify exact certificate-first winner selection and convergence</name>
  <files>test/src/account/transaction_manager_certificate_fallback_test.cpp</files>
  <read_first>
    - test/src/account/transaction_manager_certificate_fallback_test.cpp: CertificateFallbackTest fixture, CertificateFallbackTestAccess, malformed embedded-certificate cases, and SharedMintSlotConfirmsOnlyTheCertifiedTransaction.
    - src/account/TransactionManager.cpp: TransactionManager::FetchAndProcessTransaction, CertificateMatchesTransaction, OnConsensusCertificate, and TransactionManager::New handler registration lambda.
    - src/account/TransactionManager.hpp: FetchTransaction/GetMonitoredNetworkIDs declarations and CertificateMatchesTransaction contract.
  </read_first>
  <behavior>
    - With no tx_processed_m entry but an exact winning serialized transaction at its normal monitored-network CRDT path, OnConsensusCertificate confirms that CRDT transaction rather than relying on embedded fallback.
    - With a CRDT miss, only a CheckHash- and subject-hash-valid exact embedded winner follows the same ChangeTransactionState(CONFIRMED) path; malformed or mismatched data remains unapplied.
    - A winner and loser sharing one canonical slot prove that the accepted winner certificate confirms/applies only the winner; the loser gets neither terminal confirmation nor Mint output.
    - A later normal transaction ingestion sees the same slot certificate and remains convergent with certificate-first processing.
  </behavior>
  <action>Extend the existing CRDT-backed CertificateFallbackTest fixture and friend accessor; do not create a mock consensus or a separate completion route. Persist the winner with GlobalDB::Buffer at TransactionManager::GetTransactionPath, deliberately keep it out of tx_processed_m, then invoke the certificate handler and inspect the same tracking/UTXO/marker state used by current tests. Add a CRDT-miss embedded-fallback case and route the existing MakeCompetingMintV2 winner/loser construction through OnConsensusCertificate. Retain the existing exact hash, same-slot, and malformed-certificate assertions so test failures reject substitution rather than merely slot mismatch (D-03, D-04, D-05).</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target transaction_manager_certificate_fallback_test --parallel 4 && ctest --test-dir build/OSX/Release -R '^transaction_manager_certificate_fallback_test$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The tests fail before CRDT-first selection and exact binding are enforced, then prove the selected winner is the only transaction that can reach the shared confirmation/application lifecycle.</acceptance_criteria>
  <done>The focused account target covers exact CRDT-first recovery, embedded-only fallback, malformed/mismatched refusal, same-slot loser refusal, and arrival-order convergence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement guarded CRDT-first certificate consumption through the existing lifecycle</name>
  <files>src/account/TransactionManager.hpp, src/account/TransactionManager.cpp</files>
  <read_first>
    - src/account/TransactionManager.hpp: private FetchTransaction and GetMonitoredNetworkIDs declarations, CertificateMatchesTransaction declaration/comment, and test-friend placement.
    - src/account/TransactionManager.cpp: FetchTransaction/globaldb conventions, GetMonitoredNetworkIDs, FetchAndProcessTransaction state selection, OnConsensusCertificate, and TransactionManager::New certificate handler lambda.
    - src/blockchain/Consensus.cpp: ProcessCommittedCertificate outcome handling introduced by 11-01, so local errors remain retryable journal work.
    - test/src/account/transaction_manager_certificate_fallback_test.cpp: the Task 1 behavioral contract.
  </read_first>
  <behavior>
    - Candidate priority is tracked GetTransactionByHash(tx_hash), then exact-hash CRDT lookup across monitored networks, then validated embedded certificate transaction only when CRDT has no exact candidate.
    - Every candidate passes CertificateMatchesTransaction(certificate, *transaction) before ChangeTransactionState(CONFIRMED).
    - A decoded CRDT value whose GetHash() differs from tx_hash is ignored; no lookup constructs or reads /cert/<tx-hash>.
    - Empty/non-nonce certificate compatibility returns remain unchanged, while retryable local failures propagate through outcome::result<ConsensusManager::Check>.
  </behavior>
  <action>Add a private helper adjacent to FetchTransaction/GetMonitoredNetworkIDs that searches monitored network transaction paths for tx_hash and returns a decoded transaction only when GetHash() exactly equals tx_hash. Update TransactionManager::OnConsensusCertificate to select tracked transaction, helper result, then the existing validated embedded deserialization fallback in that order; retain the fallback's CheckHash and nonce subject-hash gates. Before every certificate-first ChangeTransactionState(CONFIRMED), require the established CertificateMatchesTransaction proof binding account, nonce, subject tx hash, embedded hash, and canonical slot. Reuse ChangeTransactionState rather than adding another certificate acceptance or Mint application path (D-01, D-02, D-03, D-04, D-05). Return outcome errors for local retryability so the Phase 11-01 journal remains Stalled instead of converting a valid certificate into terminal failure (D-06).</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target transaction_manager_certificate_fallback_test --parallel 4 && ctest --test-dir build/OSX/Release -R '^transaction_manager_certificate_fallback_test$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Certificate-first handling cannot confirm a same-slot non-winner, prioritizes exact CRDT evidence, and uses the exact embedded winner only as the constrained fallback into the existing lifecycle.</acceptance_criteria>
  <done>All accepted certificate and transaction arrival orders converge on one exact-bound TransactionManager confirmation path, with no subject-hash authority or alternative finality state added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Accepted certificate → transaction candidate selection | Certificate contents and replicated transaction data must not substitute a same-slot losing Mint. |
| CRDT transaction payload → TransactionManager lifecycle | Replicated bytes are untrusted until deserialized, exact-hash checked, and certificate-bound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | `TransactionManager::OnConsensusCertificate` | mitigate | Require tracked/CRDT/fallback candidate ordering, exact CRDT `GetHash()` equality, and `CertificateMatchesTransaction` before confirmation; test winner versus same-slot loser. |
| T-11-02b | Elevation of privilege | certificate lookup | mitigate | Do not derive authority from subject hash or query `/cert/<tx-hash>`; authority remains the Phase 10 canonical slot record. |
</threat_model>

<verification>
Build `transaction_manager_certificate_fallback_test`, then run the focused account CTest command after each task. Ensure the production path uses real GlobalDB fixture persistence and the existing TransactionManager handler rather than a direct local-author helper.
</verification>

<success_criteria>
1. Certificate-first delivery consults exact CRDT transaction evidence before embedded fallback.
2. The certified winner alone can confirm or apply for a contested canonical slot.
3. Later transaction delivery and startup scanning remain on the same transaction lifecycle.
</success_criteria>

<output>
Create `.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-02-SUMMARY.md` when done.
</output>
