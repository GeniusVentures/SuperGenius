---
phase: 11-convergent-certificate-consumption-mint-recovery
plan: "03"
type: tdd
wave: 3
depends_on:
  - 11-02
files_modified:
  - src/account/TransactionManager.hpp
  - src/account/TransactionManager.cpp
  - src/blockchain/Blockchain.hpp
  - src/blockchain/Consensus.hpp
  - test/src/account/transaction_manager_certificate_fallback_test.cpp
autonomous: true
requirements:
  - MINT-01
  - MINT-02
user_setup: []
must_haves:
  truths:
    - "A certified Mint V2 persists idempotent UTXO effects before its existing bridge-executed marker and becomes terminally confirmed only after both are durable."
    - "Duplicate certificate delivery and recovery after durable outputs with an absent marker produce one Mint effect and repair completion."
    - "UTXO or marker persistence failure leaves certified work retryable and nonterminal instead of silently completing or rolling back durable effects."
  artifacts:
    - path: src/account/TransactionManager.hpp
      provides: "Named marker-only write-failure seam and persistence helper declaration accessible only through CertificateFallbackTestAccess"
      contains: "SetBridgeExecutedMarkerWriteFailureForTest"
    - path: src/account/TransactionManager.cpp
      provides: "Mint V2 error propagation plus marker-only write failure and post-effect marker/terminal-confirmation ordering"
      contains: "PersistBridgeExecutedMarker"
    - path: src/blockchain/Blockchain.hpp
      provides: "Friend access for CertificateFallbackTestAccess to the fixture's registered ConsensusManager"
      contains: "CertificateFallbackTestAccess"
    - path: src/blockchain/Consensus.hpp
      provides: "Friend access for CertificateFallbackTestAccess to callback, durable recovery, and certificate-work journal state"
      contains: "CertificateFallbackTestAccess"
    - path: test/src/account/transaction_manager_certificate_fallback_test.cpp
      provides: "Named marker-failure recovery regression through ConsensusManager callback, durable readback, and registered TransactionManager handler"
      contains: "CertificateCallbackMarkerWriteFailureStallsThenRecoversExactlyOnce"
  key_links:
    - from: "TransactionManager::ParseMintTransaction"
      to: "UTXOManager::PutUTXO"
      via: "propagated outcome for Mint V2 output persistence"
      pattern: "ParseMintTransaction[\\s\\S]*PutUTXO"
    - from: "TransactionManager::ChangeTransactionState(CONFIRMED)"
      to: "/bridge/executed/<chain>:<source>"
      via: "marker persisted after ParseTransaction and before terminal map state"
      pattern: "ParseTransaction[\\s\\S]*bridge/executed"
    - from: "ConsensusManager::RecoverPendingCertificateWork"
      to: "TransactionManager::OnConsensusCertificate"
      via: "registered nonce certificate handler returns marker-write error so certificate work remains Stalled"
      pattern: "RecoverPendingCertificateWork[\\s\\S]*MarkStalled"
---

<objective>
Establish the existing UTXO set plus bridge-executed marker as the durable Mint completion boundary: apply idempotent effects, persist the marker, then transition terminally, while preserving recovery through the shared certificate work journal (D-01, D-02, D-06, D-07, D-08).

Purpose: make a successful certificate handler mean all exact Mint effects are durable and make crash-equivalent partial completion safely replayable.
Output: corrected Mint V2 persistence/error propagation and deterministic duplicate/recovery regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-CONTEXT.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-RESEARCH.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-VALIDATION.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-PATTERNS.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify marker-only failure recovery through the real certificate ingress path</name>
  <files>test/src/account/transaction_manager_certificate_fallback_test.cpp</files>
  <read_first>
    - test/src/account/transaction_manager_certificate_fallback_test.cpp: CertificateFallbackTest fixture, current certificate-handler cases, competing Mint test, UTXO loading, and friend accessor.
    - src/blockchain/Blockchain.hpp and src/blockchain/Consensus.hpp: private consensus-manager ownership plus existing `ConsensusPendingLifecycleTestAccess` callback/recovery/journal access pattern.
    - src/blockchain/Consensus.cpp: CertificateReceived, RecoverPendingCertificateWork, ProcessCommittedCertificate, and registered certificate-handler outcome handling from 11-01.
    - src/account/TransactionManager.hpp and src/account/TransactionManager.cpp: CertificateFallbackTestAccess, SetBridgeExecutedMarkerWriteFailureForTest, PersistBridgeExecutedMarker, ParseMintTransaction(MintTransactionV2), ChangeTransactionState(CONFIRMED), and OnConsensusCertificate.
    - src/account/UTXOManager.cpp: PutUTXO outpoint idempotence and persistence result behavior.
    - .planning/phases/11-convergent-certificate-consumption-mint-recovery/11-VALIDATION.md: Task 11-03-01 contract.
  </read_first>
  <behavior>
    - Process the same accepted winning certificate twice; exactly one Mint output/outpoint and balance effect exist and the marker exists once.
    - `CertificateCallbackMarkerWriteFailureStallsThenRecoversExactlyOnce` drives the winning canonical certificate through `ConsensusManager::CertificateReceived` before CRDT commit, persists the exact `/cert/<slot>` record, then calls `RecoverPendingCertificateWork` on the fixture's actual ConsensusManager whose nonce handler was installed by `TransactionManager::New`.
    - With `TransactionManager::SetBridgeExecutedMarkerWriteFailureForTest(true)`, the named seam fails only `PersistBridgeExecutedMarker`: winning Mint UTXOs/outpoint and balance persist first, the marker is absent, the shared certificate-work journal is Stalled, and the transaction remains nonterminal.
    - After `SetBridgeExecutedMarkerWriteFailureForTest(false)`, a second `RecoverPendingCertificateWork` replays the same exact certificate through the registered handler, adds no UTXO/balance effect, persists the existing marker, transitions the transaction to CONFIRMED, and marks journal work Done.
  </behavior>
  <action>Extend `sgns::CertificateFallbackTestAccess` with the narrow friend-only methods needed to obtain `Blockchain::consensus_manager_`, invoke `ConsensusManager::CertificateReceived` and `RecoverPendingCertificateWork`, inspect `certificate_work_journal_`, read the tracked transaction, inspect the marker, and toggle `SetBridgeExecutedMarkerWriteFailureForTest`. Add `CertificateCallbackMarkerWriteFailureStallsThenRecoversExactlyOnce` to CertificateFallbackTest: do not invoke `OnConsensusCertificate` directly for this end-to-end assertion. Instead, retain the pre-commit callback-to-stalled transition, durably write the exact canonical certificate, then prove the registered TransactionManager handler returns the named marker-only failure through ProcessCommittedCertificate. Assert persisted winning UTXOs before the failed marker, nonterminal tracking and Stalled journal; restore writes and invoke durable recovery again, then assert marker-before-CONFIRMED/journal-Done plus exactly one output/outpoint/balance effect. Keep direct `OnConsensusCertificate` tests only for the Plan 11-02 candidate-selection contract, not this recovery proof (D-02, D-06, D-07, D-08).</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target transaction_manager_certificate_fallback_test consensus_pending_lifecycle_test --parallel 4 && ctest --test-dir build/OSX/Release -R '^(transaction_manager_certificate_fallback_test|consensus_pending_lifecycle_test)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>The regression suite fails with pre-application marker persistence, swallowed marker/UTXO errors, or a premature CONFIRMED map entry, and it proves no duplicate Mint effect during recovery.</acceptance_criteria>
  <done>The named marker-only failure regression proves the callback → durable readback → registered TransactionManager handler path stalls until writes recover, then completes exactly once without a direct handler-call substitute.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Mint V2 terminal confirmation follow durable effects and marker</name>
  <files>src/account/TransactionManager.hpp, src/account/TransactionManager.cpp, src/blockchain/Blockchain.hpp, src/blockchain/Consensus.hpp</files>
  <read_first>
    - src/account/TransactionManager.hpp: CertificateFallbackTestAccess friendship, bridge marker constants, and private member/function declarations.
    - src/account/TransactionManager.cpp: ParseMintTransaction overloads around the existing Mint V2 output and bridge-input operations.
    - src/account/TransactionManager.cpp: ChangeTransactionState's CONFIRMED branch, tx_processed_m state updates, metrics/nonces/missing-set effects, and bridge-executed datastore write.
    - src/blockchain/Blockchain.hpp and src/blockchain/Consensus.hpp: add only test-access friendship for the existing `sgns::CertificateFallbackTestAccess`; do not add a production getter or public API.
    - src/account/UTXOManager.cpp: PutUTXO and ConsumeUTXOs outcome/result contract and outpoint replay behavior.
    - src/blockchain/Consensus.cpp: ProcessCommittedCertificate handling of handler outcome errors and Check::Stalled from 11-01.
    - test/src/account/transaction_manager_certificate_fallback_test.cpp: Task 1 regressions.
  </read_first>
  <behavior>
    - Mint V2 PutUTXO and bridge ConsumeUTXOs storage errors propagate through ParseMintTransaction/ChangeTransactionState to OnConsensusCertificate.
    - `PersistBridgeExecutedMarker` returns `std::errc::io_error` only while the named `SetBridgeExecutedMarkerWriteFailureForTest(true)` seam is enabled; all other production marker writes retain the existing datastore behavior.
    - For certified Mint V2, the lifecycle remains retryable while ParseTransaction persists idempotent outputs; then the existing marker write must succeed; only then is tx_processed_m terminally CONFIRMED and later duplicate processing a no-op.
    - If an output is already present from interrupted work, replay treats that outpoint as safe progress, does not roll it back, writes the missing marker, and completes.
  </behavior>
  <action>In TransactionManager.hpp, add the private named test seam `SetBridgeExecutedMarkerWriteFailureForTest(bool fail)` and its boolean state, plus private `PersistBridgeExecutedMarker(const MintTransactionV2 &mint_tx)` declaration; expose neither as a production public API, only through the existing `sgns::CertificateFallbackTestAccess` friend. In TransactionManager.cpp, implement `PersistBridgeExecutedMarker` as the single current marker-write path: when the seam is enabled return `outcome::failure(std::errc::io_error)` before touching storage; otherwise build the existing `/bridge/executed/<chain>:<source>` key and propagate the datastore `put` result. Use BOOST_OUTCOME_TRY or the established outcome propagation pattern for MintTransactionV2 output PutUTXO calls and bridge-input ConsumeUTXOs calls; retain idempotent-existing-output behavior and do not roll back partial effects. Narrowly refactor ChangeTransactionState's certified Mint V2 CONFIRMED path so it retains/reuses retryable VERIFYING state, calls ParseTransaction first, calls PersistBridgeExecutedMarker second, and only then applies terminal tx_processed_m, metrics, nonce, and missing-set updates. Add `CertificateFallbackTestAccess` friendship only in Blockchain.hpp and Consensus.hpp to reach the fixture-owned manager and existing private recovery hooks; do not add a production getter, callback shortcut, second journal, /bridge/certified record, or broad state-machine change (D-01, D-02, D-06, D-07, D-08).</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target transaction_manager_certificate_fallback_test consensus_pending_lifecycle_test --parallel 4 && ctest --test-dir build/OSX/Release -R '^(transaction_manager_certificate_fallback_test|consensus_pending_lifecycle_test)$' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>A certified Mint cannot reach terminal CONFIRMED with missing durable effects or marker; every local persistence failure is observable to the existing retry mechanism and a replay causes no duplicate Mint effect.</acceptance_criteria>
  <done>Mint V2 completion is ordered as idempotent durable UTXOs, durable existing marker, then terminal confirmation, with all duplicate and partial-recovery tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Accepted certificate handler → local UTXO/marker persistence | A valid remote certificate must not be acknowledged complete while local Mint effects are incomplete. |
| Durable UTXO effects → completion marker/retry | A crash or write failure can leave partial local state and must replay safely without double minting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering / denial of service | `TransactionManager::ChangeTransactionState` Mint V2 completion | mitigate | Persist idempotent UTXOs, propagate all storage errors, persist existing marker, then set terminal CONFIRMED; certificate-work journal retries errors. |
| T-11-03b | Repudiation | `/bridge/executed/<chain>:<source>` completion evidence | mitigate | Treat missing marker after durable effects as recoverable evidence; replay restores it before terminal state, with focused duplicate/restart tests. |
</threat_model>

<verification>
Build both focused targets, then run the combined focused CTest command after each task. Confirm `CertificateCallbackMarkerWriteFailureStallsThenRecoversExactlyOnce` uses callback → durable readback → registered handler rather than a direct `OnConsensusCertificate` call, uses no sleeps, and run the full suite when the build host permits as required by VALIDATION.md.
</verification>

<success_criteria>
1. The exact certified Mint produces no more than one local UTXO/balance effect under duplicate ingress or replay.
2. A crash-equivalent marker gap completes safely on replay without rolling back durable effects.
3. Storage failure keeps certificate work retryable and prevents terminal confirmation until all effects and marker are durable.
</success_criteria>

<output>
Create `.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-03-SUMMARY.md` when done.
</output>
