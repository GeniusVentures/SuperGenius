---
phase: 10-durable-vote-lock-and-finalization-state-machine
plan: 03
type: execute
wave: 2
depends_on:
  - 10-02
files_modified:
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - src/blockchain/Blockchain.hpp
  - src/blockchain/impl/Blockchain.cpp
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/account/network_config_precedence_test.cpp
  - test/src/blockchain/consensus_vote_journal_test.cpp
autonomous: true
requirements:
  - VOTE-03
  - VOTE-04
must_haves:
  truths:
    - "D-01: ConsensusManager validates and restores all local vote/process/conflict state before subscribe, filter/listener registration, timer start, proposal handling, or publication."
    - "D-03: after transport is ready, startup republishes each active unexpired lock through a raw-byte helper using only the stored envelope bytes; it never reconstructs or re-signs the vote."
    - The selection window configuration is resolved before Blockchain and ConsensusManager factories create consensus side effects.
    - Missing certificate handlers are valid pending dependencies and never cause a processing record to become Complete.
  artifacts:
    - path: src/blockchain/Consensus.hpp
      provides: Immutable ConsensusConfig and restored state/store ownership
    - path: src/blockchain/Consensus.cpp
      provides: Fail-closed pre-side-effect startup scan, raw PublishSerialized helper, and executable post-transport stored-envelope replay
    - path: src/account/GeniusNode.cpp
      provides: Bounded consensus_vote_selection_window_ms parsing and propagation
  key_links:
    - from: GeniusNode::InitNetwork
      to: ConsensusManager::New
      via: ConsensusConfig value passed through Blockchain::New before startup
    - from: ConsensusStateStore::Scan
      to: pubsub Subscribe/RegisterCertificateFilter/StartRoundTimer
      via: successful restore is a mandatory predecessor
---

<objective>
Wire strict local-state restoration and vote-window configuration into consensus startup before any observable consensus action.

Purpose: A corrupt or contradictory vote journal must stop the node before it can subscribe, process, sign, or replay.
Output: Value-based configuration propagation, pre-side-effect startup validation, restored locks/safety states, and exact raw-envelope startup replay.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
`src/account/GeniusNode.cpp` is already user-modified. Preserve its existing changes and edit only the configuration parsing/Blockchain factory call needed here.
</execution_note>

<context>
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-CONTEXT.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-PATTERNS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-16-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Propagate config and restore local safety state before startup side effects</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, src/blockchain/Blockchain.hpp, src/blockchain/impl/Blockchain.cpp, src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/account/network_config_precedence_test.cpp, test/src/blockchain/consensus_vote_journal_test.cpp</files>
  <read_first>
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - src/blockchain/Blockchain.hpp
    - src/blockchain/impl/Blockchain.cpp
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - test/src/account/network_config_precedence_test.cpp
    - test/src/blockchain/consensus_vote_journal_test.cpp
    - src/blockchain/ConsensusStateStore.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/crdt/globaldb/globaldb.hpp
    - test/src/blockchain/consensus_certificate_store_test.cpp
    - test/src/startup/startup_wiring_test.cpp
  </read_first>
  <action>
Add `ConsensusConfig` with immutable `vote_selection_window` defaulting to 500ms and a documented positive upper bound. Parse `consensus_vote_selection_window_ms` in `GeniusNode::InitNetwork`: accept only an integer within bounds; missing, zero, negative, non-integer, or excessive values retain the compiled default and log a warning. Store the resolved value and pass it by value through `Blockchain::New` into `ConsensusManager::New`; preserve default parameters for direct test factories, and do not add a post-construction setter.

In `ConsensusManager::New`, acquire `db_->GetDataStore`, create `ConsensusStateStore`, perform the existing `/cert/` compatibility scan, strictly scan all local records, enumerate/cross-check authoritative slot certificates against processing and safety records, restore active vote locks, Pending/Processing/Complete and SafetyViolation slot states, and synthesize only the safe recoverable gap of an authoritative certificate missing its Pending marker. Fail by returning `nullptr` on any other unreadable/inconsistent relationship before `Subscribe`, `RegisterCertificateFilter`, callback/listener registration, `StartRoundTimer`, proposal processing, or publication.

After successful validation, establish transport/filter/timer lifetime in explicit order. In this plan, implement the private `PublishSerialized(std::string_view envelope_bytes)` publication helper and the complete restored-envelope replay loop. For every active unexpired lock, pass the stored outbound `ConsensusMessage` bytes directly to `PublishSerialized`; do not parse-and-reserialize, call `CreateVote`, or invoke the signer. Suppress replay when restored authoritative finality or SafetyViolation exists for the slot, and retain the active record plus publication metadata for retry when raw publication fails. Restore stale Processing as Pending. If a certificate subject handler is absent, retain Pending and make `RegisterCertificateHandler` wake the pending processor after registration instead of marking work done. Add private friend-only startup-event, raw-publication-byte, signer-count, and config readers in the test translation units; do not expose a production mutation seam.

Test valid/default/invalid config propagation and startup tables for unreadable query, malformed record, wrong validator, bad signature/envelope, contradictory active records, marker without/mismatched certificate, and restored safety state. Assert every failure has zero subscribe/filter/timer/publish counters. Add same-database restart cases proving the lock exists before the raw replay hook, replay bytes equal the stored envelope byte-for-byte, signer count remains zero, final/safety-stopped slots do not replay, and publication failure leaves the exact active record retryable.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_vote_journal_test network_config_precedence_test -j2 &amp;&amp; build/OSX/Release/test_bin/consensus_vote_journal_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/network_config_precedence_test --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - `consensus_vote_selection_window_ms` has one 500ms default, bounded validation, and value propagation before manager construction.
    - `ConsensusManager::New` calls local-state validation/restoration before every subscribe/filter/listener/timer/publish call in source order.
    - All listed corrupt/inconsistent startup cases return null with every side-effect counter equal to zero.
    - Valid restored locks exist in the manager before exact-envelope replay is attempted.
    - `PublishSerialized` and restored active-lock replay are implemented in this plan; restart publishes byte-identical stored envelope bytes with zero signer calls, and a raw publish failure preserves the same retryable record.
    - Stale Processing becomes Pending, and an absent handler remains Pending until registration wakes processing.
    - Existing network config precedence tests remain green and user changes in GeniusNode.cpp are preserved.
  </acceptance_criteria>
  <done>Consensus starts only from validated durable local state and receives its fixed selection configuration before any consensus side effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| RocksDB startup state → live consensus | A bad lock could be ignored after the node has already subscribed or signed |
| Operator JSON → candidate deadline | Invalid timing can eliminate or indefinitely extend candidate selection |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-10-03-01 | High | Elevation of Privilege / Tampering | Strict restoration completes before every consensus side effect; any inconsistency aborts factory creation |
| T-10-03-02 | High | Repudiation | Startup event counters prove locks restore before replay and failures emit no side effects |
| T-10-03-03 | Medium | Denial of Service | Bounded positive config with safe fallback prevents zero or excessive timer values |
| T-10-03-04 | High | Tampering | Missing handlers preserve Pending state and cannot convert an unapplied winner to Complete |
</threat_model>

<verification>
Run the journal and config suites, then the 10-VALIDATION focused CTest regex for this wave and `git diff --check`.
</verification>

<success_criteria>
- Startup is fail-closed before all consensus side effects.
- Configuration reaches the manager as an immutable pre-start value.
- Valid locks/safety state restore and pending work is never lost for a missing handler.
</success_criteria>

<output>
Create `.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-03-SUMMARY.md` when done.
</output>
