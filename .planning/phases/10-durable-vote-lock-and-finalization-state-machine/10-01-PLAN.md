---
phase: 10-durable-vote-lock-and-finalization-state-machine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/src/blockchain/CMakeLists.txt
  - test/src/blockchain/consensus_vote_journal_test.cpp
  - test/src/blockchain/consensus_finalization_test.cpp
autonomous: true
requirements:
  - CERT-05
  - CERT-06
  - CERT-07
  - VOTE-01
  - VOTE-02
  - VOTE-03
  - VOTE-06
  - VOTE-07
must_haves:
  truths:
    - Wave 0 provides buildable deterministic RocksDB/CRDT fixtures before any Phase 10 behavior plan depends on them.
    - Test utilities use explicit clocks, barriers, and friend-only access patterns rather than sleeps or public production fault APIs.
    - The finalization fixture can observe durable certificate, processing, cleanup, handler, conflict, and publication ordering independently.
  artifacts:
    - path: test/src/blockchain/consensus_vote_journal_test.cpp
      provides: Persistent-database restart fixture and exact-byte/startup-order assertion helpers
    - path: test/src/blockchain/consensus_finalization_test.cpp
      provides: Multi-ingress finalization fixture with deterministic handler barriers and source counters
    - path: test/src/blockchain/CMakeLists.txt
      provides: Active focused targets required by 10-VALIDATION.md
  key_links:
    - from: test/src/blockchain/CMakeLists.txt
      to: consensus_vote_journal_test and consensus_finalization_test
      via: repository addtest and blockchain_genesis/base_crdt_test linkage
---

<objective>
Create the Wave 0 test infrastructure required to implement and sample durable vote and finalization behavior.

Purpose: Make every later persistence and concurrency task testable with deterministic clocks/barriers and focused binaries before production behavior changes.
Output: Two active GoogleTest targets with reusable persistent CRDT/RocksDB fixtures and baseline harness tests.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
This is Wave 0 infrastructure only. Do not implement vote-lock, finalization, conflict, Phase 11 burn-reservation, or Phase 12 11-node race behavior here.
Preserve all user-owned dirty files, especially `src/account/GeniusNode.cpp`, `ProofSystem`, `.claude/`, logs, and generated test directories.
</execution_note>

<context>
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-CONTEXT.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-RESEARCH.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-PATTERNS.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register deterministic vote-journal and finalization test harnesses</name>
  <files>test/src/blockchain/CMakeLists.txt, test/src/blockchain/consensus_vote_journal_test.cpp, test/src/blockchain/consensus_finalization_test.cpp</files>
  <read_first>
    - test/src/blockchain/CMakeLists.txt
    - test/src/blockchain/consensus_vote_journal_test.cpp
    - test/src/blockchain/consensus_finalization_test.cpp
    - test/src/blockchain/consensus_certificate_store_test.cpp
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
    - test/src/account/utxo_manager_test.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - src/crdt/globaldb/globaldb.hpp
    - src/storage/rocksdb/rocksdb.hpp
    - .planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-VALIDATION.md
  </read_first>
  <action>
Add active `addtest(consensus_vote_journal_test ...)` and `addtest(consensus_finalization_test ...)` targets using the same `blockchain_genesis`, `genius_node_test`, `rapidjson`, and `base_crdt_test` dependencies as the active certificate/pending fixtures. Build reusable fixture helpers that allocate unique temporary database paths, construct the existing real GlobalDB/registry/pubsub dependencies, close every manager, and reopen the same RocksDB path. Add RAII reset helpers for future friend-only hooks, explicit system/steady clock values, promises/condition variables for handler barriers, and counters for signer, raw publish, handler, cleanup, subscription, timer, and certificate-filter events. Do not use `sleep_for`, detached threads, public test setters, or placeholder disabled tests.

Keep Wave 0 buildable against current production APIs: add baseline tests named `ConsensusVoteJournalHarness.PersistentDatabaseReopensCleanly` and `ConsensusFinalizationHarness.ConcurrentBarrierJoinsCleanly` that prove the fixture can reopen storage and deterministically release/join a blocked worker. Leave clearly named helper methods for later plans to add behavioral assertions, but do not assert the pre-Phase-10 unsafe semantics.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_vote_journal_test consensus_finalization_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_vote_journal_test --gtest_list_tests | grep -Ec 'PersistentDatabaseReopensCleanly')" -eq 1 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_finalization_test --gtest_list_tests | grep -Ec 'ConcurrentBarrierJoinsCleanly')" -eq 1 &amp;&amp; build/OSX/Release/test_bin/consensus_vote_journal_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/consensus_finalization_test --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - Both focused binaries are active CMake targets and both named harness tests are listed and pass.
    - The journal fixture reopens the same real RocksDB path after closing its first owner.
    - The finalization fixture blocks/releases/joins with promises or condition variables and has no wall-clock sleep.
    - RAII helpers restore all injected callbacks/counters after each test.
    - No production source file or public testing API is modified in Wave 0.
  </acceptance_criteria>
  <done>The two focused Phase 10 binaries build quickly and expose deterministic fixtures used by every later behavioral plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| Test harness → production concurrency | A timing-only harness can miss equivocation and duplicate application races |
| Persistent fixture → restart assertions | Accidental fresh databases can falsely claim recovery coverage |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-10-01-01 | High | Tampering | Real same-path RocksDB reopen helpers and nonzero named-test guards prevent fake persistence coverage |
| T-10-01-02 | High | Repudiation | Explicit barrier/counter traces prove event ordering without timing inference |
| T-10-01-03 | Medium | Denial of Service | RAII release/join/reset ensures a failed assertion cannot strand workers or hooks |
</threat_model>

<verification>
Run both focused binaries, inspect `--gtest_list_tests` for the named baseline cases, and run `git diff --check`. This Wave 0 plan intentionally does not run the full behavioral regex because those behaviors do not exist yet.
</verification>

<success_criteria>
- Wave 0 test targets compile and run independently.
- Deterministic persistent-storage and concurrency fixtures are available before Wave 1.
- No Phase 10 behavior or deferred Phase 11/12 scope is implemented.
</success_criteria>

<output>
Create `.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-01-SUMMARY.md` when done.
</output>
