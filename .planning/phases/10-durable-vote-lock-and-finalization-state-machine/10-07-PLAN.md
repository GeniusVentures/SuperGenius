---
phase: 10-durable-vote-lock-and-finalization-state-machine
plan: 07
type: execute
wave: 6
depends_on:
  - 10-06
files_modified:
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - test/src/blockchain/consensus_vote_journal_test.cpp
  - test/src/blockchain/consensus_finalization_test.cpp
  - test/src/blockchain/consensus_pending_lifecycle_test.cpp
  - test/src/blockchain/consensus_certificate_store_test.cpp
  - test/src/account/network_config_precedence_test.cpp
  - test/src/account/transaction_manager_pending_lifecycle_test.cpp
  - test/src/account/utxo_manager_test.cpp
autonomous: true
requirements:
  - CERT-05
  - CERT-06
  - CERT-07
  - VOTE-01
  - VOTE-02
  - VOTE-03
  - VOTE-04
  - VOTE-05
  - VOTE-06
  - VOTE-07
must_haves:
  truths:
    - Shutdown joins the owned timer and pending-processing activity with open selection windows and blocked handlers; no callback runs after destruction.
    - Deterministic friend-only seams cover every persistence/publication/application boundary without widening production APIs.
    - Existing Phase 9 slot/index, nonce-chain, producer-certificate, transaction pending, and UTXO idempotency behavior remains compatible.
    - Phase 10 performs no bridge burn reservation/consumption redesign and no Phase 12 11-node race execution.
  artifacts:
    - path: test/src/blockchain/consensus_vote_journal_test.cpp
      provides: Final restart/non-equivocation and store failure regression matrix
    - path: test/src/blockchain/consensus_finalization_test.cpp
      provides: Shutdown, ingress race, cleanup, and safety-stop closure matrix
    - path: src/blockchain/Consensus.cpp
      provides: Audited lifetime/lock ordering and complete deterministic wake/join behavior
  key_links:
    - from: ConsensusManager::Close
      to: timer/recovery/handler workers
      via: stop signal, wake, bounded weak ownership, and join before destruction
    - from: Phase 10 focused tests
      to: Phase 9 compatibility suites
      via: final CTest regression gate
---

<objective>
Close lifecycle, concurrency, and compatibility gaps after the durable vote/finality state machine is assembled.

Purpose: Prove the complete Phase 10 safety boundary survives shutdown, restart, ingress races, configuration defaults, and existing certificate/application consumers.
Output: Lifetime fixes where needed, complete deterministic scenario matrix, and full focused regression gate.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
This is Phase 10 closure, not a conditional production-repair plan, Phase 11 burn reservation, or Phase 12 multi-node race execution. Production edits are limited to the named Consensus files for lock/lifetime closure. Preserve user-owned dirty files.
</execution_note>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-CONTEXT.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-VALIDATION.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove shutdown, concurrency, configuration, and compatibility closure</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, test/src/blockchain/consensus_vote_journal_test.cpp, test/src/blockchain/consensus_finalization_test.cpp, test/src/blockchain/consensus_pending_lifecycle_test.cpp, test/src/blockchain/consensus_certificate_store_test.cpp, test/src/account/network_config_precedence_test.cpp, test/src/account/transaction_manager_pending_lifecycle_test.cpp, test/src/account/utxo_manager_test.cpp</files>
  <read_first>
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - test/src/blockchain/consensus_vote_journal_test.cpp
    - test/src/blockchain/consensus_finalization_test.cpp
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp
    - test/src/blockchain/consensus_certificate_store_test.cpp
    - test/src/account/network_config_precedence_test.cpp
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
    - test/src/account/utxo_manager_test.cpp
    - test/src/blockchain/certificate_compatibility_test.cpp
    - src/account/TransactionManager.cpp
    - src/account/UTXOManager.cpp
    - .planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-VALIDATION.md
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-VERIFICATION.md
  </read_first>
  <action>
Audit and document the manager lock order across slot state, finalization/process gate, local state store, timer mutex, and handler registry. Ensure no arbitrary signer, CRDT write, pubsub publish, or transaction handler runs while holding the broad slot mutex; all such operations reserve/recheck a generation or lease. Extend `Close()` so it sets the stop flag, wakes candidate-deadline and pending-processing waits, joins the existing owned timer/worker activity, and prevents weak-pointer callbacks after destruction. Do not create detached per-slot threads or strong-reference cycles.

Complete the exact Phase 10 scenario matrix across the focused tests: write-before-publish; crash before publish; restart competitor suppression; fixed deadline/comparator/tie; deadline/finalization barrier race; live-horizon boundaries and durable retirement; local/pubsub/CRDT/recovery single handler; missing handler pending/wakeup; handler failure and effect-before-Complete recovery; winner overriding local vote; conflict source/dedup/no-rebroadcast/safety restart; shutdown with open selection, raw replay queued, blocked handler, and stale Processing. Use friend-only clocks, fault stages, publish observers, and barriers; remove any timing sleeps introduced during implementation.

Run the existing Phase 9 certificate store/index, `certificate_compatibility_test`, transaction pending lifecycle, and UTXO idempotency paths as verification-only compatibility gates. Confirm normal transaction nonce-chain/producer lookup continues through `GetCertificateBySubjectHash`, stored authoritative certificates remain timeless, and the Plan 10-05 TransactionManager mapping treats existing mint `AlreadyApplied` as handler success. Do not modify `src/account/TransactionManager.hpp/.cpp` or `src/account/UTXOManager.hpp/.cpp` in this closure plan. If a newly discovered defect requires production changes outside `src/blockchain/Consensus.hpp` or `src/blockchain/Consensus.cpp`, stop this plan with the failing command and exact file/call-path evidence, then route it through `$gsd-plan-phase 10 --gaps` as a new dependency-ordered gap plan before Phase 10 verification; do not make an unspecified conditional fix here. Do not add reservation ownership or multi-node race fixtures.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_vote_journal_test consensus_finalization_test consensus_pending_lifecycle_test consensus_certificate_store_test certificate_compatibility_test network_config_precedence_test transaction_manager_pending_lifecycle_test utxo_manager_test -j2 &amp;&amp; ctest --test-dir build/OSX/Release -R '(consensus_vote_journal|consensus_finalization|consensus_pending_lifecycle|consensus_certificate_store|certificate_compatibility|network_config_precedence|transaction_manager_pending_lifecycle|utxo_manager)' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Source documents one lock order and contains no external signer/CRDT/pubsub/handler invocation under the broad slot mutex.
    - Close with an open window, queued replay, blocked handler, or pending recovery deterministically wakes and joins; post-destruction counters remain zero.
    - The focused tests contain the complete listed matrix and use explicit clocks/barriers, with no `sleep_for` timing assertions.
    - The full named CTest regex passes, including certificate compatibility, transaction pending lifecycle, and UTXO idempotency.
    - Existing slot/index lookup and normal transaction certificate consumers remain unchanged in semantics.
    - This plan modifies no TransactionManager or UTXOManager production file; any newly discovered out-of-scope production defect is documented and routed to a later dependency-ordered gap plan rather than repaired conditionally.
    - No files or assertions implement Phase 11 burn reservations or execute the Phase 12 11-node race.
  </acceptance_criteria>
  <done>The assembled Phase 10 state machine is lifetime-safe, deterministically tested, and compatible with all existing certificate/application consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| Manager lifetime → timer/handler callbacks | Use-after-free or leaked ownership can publish/apply after shutdown |
| Multiple locks → external operations | Lock inversion or reentrancy can deadlock safety-critical transitions |
| New finality path → existing consumers | Compatibility regressions can bypass slot/index or duplicate application |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-10-07-01 | High | Denial of Service | Documented lock order and no external calls under broad state lock |
| T-10-07-02 | High | Elevation of Privilege | Stop/wake/join plus weak ownership prevents post-close signing, publishing, or applying |
| T-10-07-03 | High | Tampering | Full compatibility suite proves slot/index authority and exact-winner idempotency remain intact |
| T-10-07-04 | Medium | Repudiation | Deterministic complete scenario matrix and nonzero focused targets preserve reproducible evidence |
</threat_model>

<verification>
Run the exact build and CTest command, audit test sources for sleeps/detached threads, audit production call sites for lock-held external operations, and run `git diff --check`.
</verification>

<success_criteria>
- Shutdown and callbacks have a deterministic owned lifetime.
- All Phase 10 requirements and D-01 through D-17 are exercised by focused tests.
- Existing certificate, transaction, and UTXO compatibility suites pass.
</success_criteria>

<output>
Create `.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-07-SUMMARY.md` when done.
</output>
