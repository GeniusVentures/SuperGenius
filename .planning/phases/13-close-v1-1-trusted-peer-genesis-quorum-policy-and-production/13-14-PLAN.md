---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 14
type: execute
wave: 2
depends_on: [13-13]
files_modified:
  - src/account/TrustStartupController.hpp
  - src/account/TrustStartupController.cpp
  - src/trustedpeer/genesis_tool/LocalTrustAdmin.cpp
  - test/src/startup/trust_first_boot_e2e_test.cpp
  - test/src/trustedpeer/trust_genesis_tool_test.cpp
autonomous: true
gap_closure: true
requirements: [BOOT-04, BURN-03, TEST-01]
must_haves:
  truths:
    - "D-13: WaitingForInitialBurn permits the explicit burn-v1 approvals needed for progress but never policy-successor proposal or approval."
    - "Local and callback activation failures that are not an expected under-quorum pending state are observable rather than silently discarded (bounded WR-01 repair)."
    - "A policy-v2 attempt before burn-v1 confirmation leaves the controller recoverably WaitingForInitialBurn and the normal burn-v1 path can still reach ConfirmedReady."
  artifacts:
    - path: "src/account/TrustStartupController.cpp"
      provides: "Runtime successor gate and structured activation-failure reporting"
    - path: "src/trustedpeer/genesis_tool/LocalTrustAdmin.cpp"
      provides: "Local policy preflight plus non-pending activation error propagation"
    - path: "test/src/startup/trust_first_boot_e2e_test.cpp"
      provides: "Production-path no-stranding state-machine regression"
  key_links:
    - from: "TrustStartupController::CanApproveSuccessors"
      to: "TrustStartupController::State::ConfirmedReady"
      via: "only fully ready state permits later policy operations"
      pattern: "CanApproveSuccessors.*ConfirmedReady"
    - from: "LocalTrustAdmin::ProposePolicy"
      to: "BurnConfig::IsEconomicallyReady"
      via: "local preflight mirrors durable D-13 guard"
      pattern: "IsEconomicallyReady"
---

<objective>
Close the CR-01 runtime/admin half and the directly touched WR-01 failure-visibility warning.

Purpose: Prevent honest local workflows from attempting forbidden policy transitions, keep burn-v1 progress available, and surface real activation failures without misclassifying ordinary under-quorum candidates.
Output: Controller/admin gates, structured failure handling, and production/local-tool regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-13-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate policy operations while preserving burn-v1 recovery</name>
  <files>src/account/TrustStartupController.hpp, src/account/TrustStartupController.cpp, src/trustedpeer/genesis_tool/LocalTrustAdmin.cpp, test/src/startup/trust_first_boot_e2e_test.cpp, test/src/trustedpeer/trust_genesis_tool_test.cpp</files>
  <read_first>
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - src/account/BurnConfig.hpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/genesis_tool/LocalTrustAdmin.hpp
    - src/trustedpeer/genesis_tool/LocalTrustAdmin.cpp
    - test/src/startup/trust_first_boot_e2e_test.cpp
    - test/src/trustedpeer/trust_genesis_tool_test.cpp
  </read_first>
  <behavior>
    - CanApproveSuccessors is false in FreshWaitingForGenesis, WaitingForInitialBurn, and FatalMismatch; it is true only in ConfirmedReady.
    - LocalTrustAdmin rejects policy proposal/approval with operation_not_permitted while BurnConfig is not economically ready, while burn-v1 proposal/approval remains callable.
    - Expected below-threshold activation leaves a candidate pending and returns its ID; COMMIT_FAILED, corruption, wrong-head, and other non-pending activation failures return an error locally and emit TRUST_ACTIVATION_FAILED from asynchronous startup callbacks.
    - In a real first-boot flow, an attempted policy-v2 transition before burn confirmation changes neither durable policy head nor readiness; completing burn-v1 quorum transitions to ConfirmedReady and enables the policy operation.
  </behavior>
  <action>RED: add `PolicyV2BeforeInitialBurnCannotStrandStartup` to the first-boot suite and focused local-admin cases to `trust_genesis_tool_test`; commit as `test(13-14): add runtime initial-burn ordering regressions`. GREEN: change `CanApproveSuccessors()` to return true only for `State::ConfirmedReady`. In `LocalTrustAdmin::ProposePolicy` and the policy branch of `Approve`, require `burn_config_->IsEconomicallyReady()` before adding a signature; do not apply this gate to burn operations. After an explicit proposal/approval, inspect `TryActivatePolicyCandidate`/`TryActivateBurnCandidate`: retain candidate-ID success for a genuinely pending below-quorum candidate, but return durable commit/corruption/link failures instead of casting them to void. In startup candidate callbacks, add `EventCode::TRUST_ACTIVATION_FAILED` and emit it for non-pending activation failures before Refresh; do not auto-sign and do not turn a single valid approval into a critical event. Preserve networking and candidate receipt in WaitingForInitialBurn. GREEN commit: `fix(13-14): align runtime policy gates with durable burn readiness`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trust_first_boot_e2e_test trust_genesis_tool_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='*PolicyV2BeforeInitialBurnCannotStrandStartup*' &amp;&amp; build/OSX/Release/test_bin/trust_genesis_tool_test --gtest_filter='*Admin*InitialBurn*:*Admin*ActivationFailure*'</automated>
  </verify>
  <acceptance_criteria>
    - The E2E observes WaitingForInitialBurn, a rejected policy-v2 operation, unchanged durable heads, successful burn-v1 quorum, ConfirmedReady, and then a permitted policy operation.
    - The local-admin test proves burn approval is not blocked by the policy gate.
    - A fault-injected durable activation error is returned/emitted; an under-quorum candidate remains a successful pending operation.
    - Receive/list paths remain signer-free and the automated command exits 0.
  </acceptance_criteria>
  <done>All honest control surfaces mirror the durable initial-burn guard, retain the only recovery path, and expose real activation failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local operator action to policy candidate | A trusted operator still cannot violate required durable sequencing. |
| Remote candidate callback to activation | Untrusted arrival can trigger validation, never signing, and real failures must be observable. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G03 | Elevation of Privilege | HIGH | controller/local policy operations | mitigate | ConfirmedReady/economic-ready preflight plus Plan 13 durable guard. | `trust_first_boot_e2e_test --gtest_filter='*PolicyV2BeforeInitialBurnCannotStrandStartup*'` |
| T13-G04 | Repudiation | HIGH | activation callbacks/admin | mitigate | Propagate local non-pending failures and emit structured asynchronous activation failures. | `trust_genesis_tool_test --gtest_filter='*Admin*ActivationFailure*'` |
| T-13-SC | Tampering | LOW | package supply chain | accept | No package install occurs. | Build graph inspection |
</threat_model>

<verification>
Run both focused binaries, then `ctest --test-dir build/OSX/Release --output-on-failure -R 'trust_first_boot_e2e_test|trust_genesis_tool_test|operator_approval_test'`.
</verification>

<success_criteria>
- Runtime/admin policy authorization is false until peer-confirmed initial burn.
- Burn-v1 progress remains available and demonstrably reaches readiness.
- Directly touched activation errors are no longer silently swallowed.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-14-SUMMARY.md` when done.
</output>
