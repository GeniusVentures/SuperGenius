---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 26
type: execute
wave: 3
depends_on: [13-23, 13-24, 13-25]
files_modified: []
autonomous: true
gap_closure: true
requirements: [BOOT-04, POLICY-01, TEST-01, TPR-02, BURN-03]
must_haves:
  truths:
    - "All twelve earlier focused closure counterexamples, including TrustFirstBootE2ETest.PolicyV2BeforeInitialBurnCannotStrandStartup, and the three CR-08/CR-09/CR-10 counterexamples execute from freshly linked binaries with an exact 15-name set, exact source-declaration equality, and zero failure/error/skip/not-run/disabled result."
    - "The unchanged Phase 13 regex enumerates exactly the explicit expected 25-name CTest target set, and JUnit reports exactly that same 25-name set with 25 passes and zero failure, skip, or disabled result; target substitution or registration drift cannot masquerade as success."
    - "The corrected passive burn lifetime case passes five additional consecutive focused runs after the broad gate."
    - "Every ASan or TSan tree/kind reported as configured proves that both exact lifecycle targets carry that sanitizer in their own effective compile commands and have target-specific link proof through either their final link command or built executable's sanitizer-runtime imports, then passes both lifecycle counterexamples with machine-checked target-scoped evidence; unrelated cache, toolchain, tree, or fuzz-target flags are ignored, and absence is recorded only as sanitizer_configured=0 and sanitizer_status=NOT_RUN, never PASS."
  artifacts:
    - path: ".planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-26-SUMMARY.md"
      provides: "Exact 15-focused, explicit 25-target-name, sanitizer tree/kind/per-case, five-repeat, and HIGH-threat closure evidence"
  key_links:
    - from: "three new counterexamples"
      to: "all prior closure counterexamples"
      via: "one exact-name XML accounting gate before broad CTest execution"
      pattern: "PassiveBurnSuccessor|RetainedPolicyQuorum|PersistedHistoricalTrust"
    - from: "freshly linked selected targets"
      to: "CTest JUnit and passive lifetime repetition evidence"
      via: "fail-fast enumeration, execution accounting, then five labeled focused runs"
      pattern: "ctest_selected_names_exact=PASS|junit_names_exact=PASS|focused_executed=15|passive_lifetime_repeat_5=PASS"
---

<objective>
Prove the third Phase 13 gap closure without losing any earlier security, production-path, exact-count, or lifetime gate.

Purpose: Turn CR-08, CR-09, CR-10, and WR-07 into named machine evidence while rejecting stale binaries, renamed/disabled tests, CTest target drift, and unavailable-path substitutions.
Output: Fresh-link exact 15-case XML/source accounting, exact explicit 25-target enumeration/JUnit name accounting, sanitizer tree/kind/per-case results or NOT_RUN, five passive-lifetime repetitions, and complete HIGH-threat dispositions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-18-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-22-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-23-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-24-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-25-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove prior and new counterexample fixture shape</name>
  <files>None; read-only structural verification of the focused C++ test sources.</files>
  <read_first>
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-22-PLAN.md
    - test/src/startup/trust_first_boot_e2e_test.cpp
    - test/src/multiaccount/policy_lifetime_multi_account_test.cpp
    - test/src/multiaccount/multi_account_sync.cpp
  </read_first>
  <action>Run the Plan 13-22 global direct-hook rejection and its comment/string-aware, brace-depth parser for `PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall` unchanged. In the same fail-fast structural gate, brace-extract each new exact test body. Require one `operator_a_admin.ProposeBurn` and one `operator_b_admin.Approve`, then inspect only the marked passive-burn window and reject every admin receiver, approval/proposal/submission/direct activation, callback injection, or raw GlobalDB write while requiring C durable `LoadAndVerify` and real `PayEscrow` observations. Inspect the marked reconstruction window and reject the same mutation/nudge vocabulary while requiring durable `LoadAndVerify`. For the historical restart body require `historical_base_path` to flow through the fixture's `existingBasePath` branch and reject fresh-storage deletion/replacement. This task is a blocking predecessor to Task 2: do not run the five lifetime repetitions when any structural assertion fails.</action>
  <verify>
    <automated>set -euo pipefail; ! rg --no-line-number 'OnTrustedPeerGenesisConfirmed\(|TryActivate(Burn|Policy)Candidate\(' test/src/startup/trust_first_boot_e2e_test.cpp | rg -v '^[[:space:]]*//'; python3 -c 'exec("""import re,sys
def extract(path,suite,name):
    source=open(path,encoding=\"utf-8\").read()
    macro=re.compile(r\"TEST(?:_F)?\\s*\\(\\s*\"+re.escape(suite)+r\"\\s*,\\s*\"+re.escape(name)+r\"\\s*\\)\")
    matches=list(macro.finditer(source))
    assert len(matches)==1,(suite,name,len(matches))
    start=source.find(\"{\",matches[0].end())
    assert start != -1
    tokens=re.compile(r\"//[^\\n]*|/\\*.*?\\*/|\\\"(?:\\\\.|[^\\\"\\\\])*\\\"|\\x27(?:\\\\.|[^\\x27\\\\])*\\x27|[{}]\",re.S)
    depth=0
    for token in tokens.finditer(source,start):
        if token.group()==\"{\": depth+=1
        elif token.group()==\"}\":
            depth-=1
            if depth==0: return source[start:token.end()]
    raise AssertionError((suite,name,\"unbalanced body\"))
noise=re.compile(r\"//[^\\n]*|/\\*.*?\\*/|\\\"(?:\\\\.|[^\\\"\\\\])*\\\"|\\x27(?:\\\\.|[^\\x27\\\\])*\\x27\",re.S)
clean=lambda raw:noise.sub(lambda x:\"\\n\"*x.group().count(\"\\n\"),raw)
comments_only=lambda raw:noise.sub(lambda x:\"\\n\"*x.group().count(\"\\n\") if x.group().startswith((\"//\",\"/*\")) else x.group(),raw)
def marked(raw,begin,end):
    assert raw.count(begin)==1 and raw.count(end)==1
    left,tail=raw.split(begin,1)
    middle,right=tail.split(end,1)
    assert raw.find(begin) &lt; raw.find(end)
    return clean(middle)
first=extract(sys.argv[1],\"TrustFirstBootE2ETest\",\"PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall\")
first_code=clean(first)
approvals=list(re.finditer(r\"(?P&lt;receiver&gt;[A-Za-z_]\\w*)\\s*(?:\\.|-&gt;|::)\\s*Approve\\s*\\(\",first_code))
assert len(re.findall(r\"\\bApprove\\s*\\(\",first_code))==1 and len(approvals)==1 and approvals[0].group(\"receiver\")==\"operator_b_admin\"
tail_start=approvals[0].end(); wait_start=first_code.find(\"sgns::test::assertWaitForCondition\",tail_start); paren=first_code.find(\"(\",wait_start); assert wait_start != -1 and paren != -1
depth=0; wait_end=None
for index in range(paren,len(first_code)):
    if first_code[index]==\"(\": depth+=1
    elif first_code[index]==\")\":
        depth-=1
        if depth==0: wait_end=index+1; break
assert wait_end is not None
prior_window=first_code[tail_start:wait_end]
assert not re.findall(r\"(?:\\b[A-Za-z_]\\w*admin\\w*\\s*(?:\\.|-&gt;)\\s*[A-Za-z_]\\w*\\s*\\(|\\b(?:Approve|Propose\\w*|SubmitCandidateApproval|TryActivate\\w*|OnTrustedPeerGenesisConfirmed)\\s*\\()\",prior_window,re.I)
assert \"passive_store\" in prior_window and \"LoadAndVerify\" in prior_window
burn=extract(sys.argv[2],\"PolicyLifetimeMultiAccountTest\",\"PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin\")
burn_code=clean(burn)
assert len(re.findall(r\"operator_a_admin\\s*(?:\\.|-&gt;)\\s*ProposeBurn\\s*\\(\",burn_code))==1
assert len(re.findall(r\"operator_b_admin\\s*(?:\\.|-&gt;)\\s*Approve\\s*\\(\",burn_code))==1
burn_window=marked(burn,\"PASSIVE_BURN_NO_RECEIVER_ACTION_BEGIN\",\"PASSIVE_BURN_NO_RECEIVER_ACTION_END\")
forbidden=r\"(?:\\b[A-Za-z_]\\w*admin\\w*\\s*(?:\\.|-&gt;)\\s*[A-Za-z_]\\w*\\s*\\(|\\b(?:Approve|Propose\\w*|SubmitCandidateApproval|TryActivate\\w*|OnTrustedPeerGenesisConfirmed|SetCandidateCallback|Put)\\s*\\()\"
assert not re.findall(forbidden,burn_window,re.I)
assert \"LoadAndVerify\" in burn_window and \"PayEscrow\" in burn_window
reconstruction=extract(sys.argv[1],\"TrustFirstBootE2ETest\",\"RetainedPolicyQuorumRetriesAfterControllerReconstructionWithoutNewWrite\")
reconstruction_window=marked(reconstruction,\"RECONSTRUCTION_NO_WRITE_WINDOW_BEGIN\",\"RECONSTRUCTION_NO_WRITE_WINDOW_END\")
assert not re.findall(forbidden,reconstruction_window,re.I)
assert \"LoadAndVerify\" in reconstruction_window
historical_raw=extract(sys.argv[3],\"MultiAccountTest\",\"PersistedHistoricalTrustAndTransactionsRestartWithSingleManagerOwnership\")
historical=comments_only(historical_raw)
assert historical.count(\"historical_base_path\") &gt;= 3
same_account_calls=re.findall(r\"\\bCreateNode\\s*\\(\\s*\\\"persisted_historical_restart\\\"\\s*,(.*?)\\)\",historical,re.S)
assert len(same_account_calls)==2 and \"historical_base_path\" not in same_account_calls[0] and \"historical_base_path\" in same_account_calls[1]
assert len(re.findall(r\"historical_base_path\\s*=\\s*GetBaseWritePath\\s*\\(\\s*historical_node\\s*\\)\",historical))==1
assert len(re.findall(r\"GetBaseWritePath\\s*\\(\\s*restarted_node\\s*\\)\\s*,\\s*historical_base_path\",historical))==1
assert len(re.findall(r\"GetTransactionManagerConstructionCount\\s*\\(\\s*restarted_node\\s*\\)\\s*,\\s*1U\",historical))==2
assert len(re.findall(r\"GetTransactionManagerStartCount\\s*\\(\\s*restarted_node\\s*\\)\\s*,\\s*1U\",historical))==2
assert len(re.findall(r\"GetTransactionManagerOwnerGeneration\\s*\\(\\s*restarted_node\\s*\\)\",historical))==1
assert len(re.findall(r\"owner_generation\\s*,\\s*1U\",historical))==1
assert len(re.findall(r\"GetAccountTransactionCallbackOwnerGeneration\\s*\\(\\s*restarted_node\\s*\\)\\s*,\\s*owner_generation\",historical))==2
assert len(re.findall(r\"GetBlockchainSlotHashOwnerGeneration\\s*\\(\\s*restarted_node\\s*\\)\\s*,\\s*owner_generation\",historical))==2
assert not re.findall(r\"(?:remove_all|RemoveAll|DeleteStorage|freshBasePath)\\s*\\(\",historical,re.I)
fixture_source=open(sys.argv[3],encoding=\"utf-8\").read()
helper_matches=list(re.finditer(r\"std::shared_ptr\\s*&lt;\\s*sgns::GeniusNode\\s*&gt;\\s+CreateNode\\s*\\((.*?)\\)\\s*\\{\",fixture_source,re.S))
assert len(helper_matches)==1
helper_signature=clean(helper_matches[0].group(0))
assert re.search(r\"std::string\\s+existingBasePath\\s*=\\s*\\{\\s*\\}\",helper_signature)
helper_start=helper_matches[0].end()-1
tokens=re.compile(r\"//[^\\n]*|/\\*.*?\\*/|\\\"(?:\\\\.|[^\\\"\\\\])*\\\"|\\x27(?:\\\\.|[^\\x27\\\\])*\\x27|[{}]\",re.S)
depth=0; helper_end=None
for token in tokens.finditer(fixture_source,helper_start):
    if token.group()==\"{\": depth+=1
    elif token.group()==\"}\":
        depth-=1
        if depth==0: helper_end=token.end(); break
assert helper_end is not None
helper=clean(fixture_source[helper_start:helper_end])
assert re.search(r\"reuseStorage\\s*=\\s*!\\s*existingBasePath\\.empty\\s*\\(\\s*\\)\",helper)
assert re.search(r\"outPathStr\\s*=\\s*std::move\\s*\\(\\s*existingBasePath\\s*\\)\",helper)
assert re.search(r\"if\\s*\\(\\s*outPathStr\\.empty\\s*\\(\\s*\\)\\s*\\)\",helper) and \"nodeCounter.fetch_add\" in helper and \"FILE_PREFIX\" in helper
assert re.search(r\"GeniusNodeConfig\\s+devConfig\\s*=\\s*\\{[^;]*outPathStr\\s*\\}\",helper,re.S)
assert len(re.findall(r\"if\\s*\\(\\s*!\\s*reuseStorage\\s*\\)\",helper))&gt;=2
assert \"removeAllWithRetry\" in helper and \"GeniusNode::New\" in helper
print(\"prior_passive_guard=PASS passive_burn_guard=PASS reconstruction_no_write_guard=PASS historical_existing_path_guard=PASS\")
""")' test/src/startup/trust_first_boot_e2e_test.cpp test/src/multiaccount/policy_lifetime_multi_account_test.cpp test/src/multiaccount/multi_account_sync.cpp</automated>
  </verify>
  <acceptance_criteria>
    - The unchanged prior direct-hook and passive-policy guards pass.
    - The passive-burn marked window reports zero receiver action/nudge and contains durable plus PayEscrow observation after exactly one A proposal and one B approval.
    - The reconstruction marked window reports zero new write/admin/direct activation/callback nudge and contains durable convergence observation.
    - The historical body proves the same named base path enters `existingBasePath` without storage deletion/replacement.
    - The automated command exits 0 before Task 2 begins.
  </acceptance_criteria>
  <done>Fixture source shape mechanically distinguishes passive discovery/restart behavior from local nudges, fresh storage, or callback injection.</done>
</task>

<task type="auto">
  <name>Task 2: Run the cumulative exact closure, sanitizer, and passive-lifetime gate</name>
  <files>None; verification-only task. Create only the required 13-26-SUMMARY.md output with exact commands, paths, counts, sanitizer discovery, repetitions, and threat dispositions.</files>
  <read_first>
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-18-PLAN.md
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-18-SUMMARY.md
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-22-PLAN.md
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-22-SUMMARY.md
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-23-SUMMARY.md
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-24-SUMMARY.md
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-25-SUMMARY.md
    - test/src/trustedpeer/trust_state_store_test.cpp
    - test/src/startup/trust_first_boot_e2e_test.cpp
    - test/src/startup/trust_restart_test.cpp
    - test/src/account/burnconfig_policy_e2e_test.cpp
    - test/src/securecrdt/securecrdt_quorum_gate_test.cpp
    - test/src/multiaccount/policy_lifetime_multi_account_test.cpp
    - test/src/multiaccount/multi_account_sync.cpp
  </read_first>
  <action>Run one fail-fast Release chain. First explicitly build the exact 25 targets selected by the unchanged Phase 13 regex from Plan 13-22. Produce separate GoogleTest XML for the twelve earlier cases—four TrustStateStore cases (`PolicySuccessorRejectedUntilInitialBurnPeerConfirmed`, `BurnV2RejectedUntilInitialBurnPeerConfirmed`, `LoadAndVerifySerializesPolicyAndBurnTransitionWithoutTornSnapshot`, `CommitPathsUseUnlockedVerifierWithoutSelfDeadlock`); `TrustFirstBootE2ETest.PolicyV2BeforeInitialBurnCannotStrandStartup` per CR-01 and Plan 13-18; `HistoricalBurnProofRemainsReadyAfterCurrentThresholdIncrease`; `PayEscrowUsesExactOverflowSafeBurnForUint64Maximum`; both outsider/retention cases; and the three Plan 13-22 first-boot cases—plus the three new cases `RetainedPolicyQuorumRetriesAfterControllerReconstructionWithoutNewWrite`, `PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin`, and `PersistedHistoricalTrustAndTransactionsRestartWithSingleManagerOwnership`. Parse XML and comment/string-aware C++ test declarations to require one unique declaration for every member of the exact 15 fully qualified expected-name set, XML name-set equality, executed=15, passed=15, and failure/error/skip/not-run/disabled/`DISABLED_` counterpart=0. Re-run the Plan 13-22 direct-hook and brace-aware passive-policy structural guards unchanged. Add body guards proving the passive-burn case has exactly A's proposal and B's approval before C's durable/PayEscrow convergence and no C admin/submission/direct activation, the retained-policy reconstruction window has no write/admin/direct activation, and the historical restart calls the fixture's existing-base-path branch rather than fresh-node creation. Enumerate the unchanged regex with `ctest -N -V`; parse the enumerated target names and require exact set equality with the explicit 25-name target list, uniqueness, one `Total Tests: 25`, and no disabled target. Run the same regex with `--output-junit`; require the JUnit testcase-name set to equal that same explicit 25-name set with executed=25, passed=25, failed=0, skipped=0, disabled=0. Determine sanitizer eligibility only from each exact target's own generated effective command data: select compile records whose output belongs to `CMakeFiles/policy_lifetime_multi_account_test.dir/` or `CMakeFiles/multi_account_test.dir/`, then build candidate pairs for which every selected compile command for both targets contains the same `-fsanitize=address` or `-fsanitize=thread` flag. After the build, prove each exact target's link instrumentation either from that target's Make `link.txt` or Ninja `-t commands` final link invocation carrying the same flag, or from the built executable artifact importing/linking the matching ASan/TSan runtime; this allows honest inherited link-runtime proof but never substitutes linked dependencies for instrumentation of either target's own translation units. Do not use CMake cache, toolchain, directory-wide, unrelated-target, or fuzz-target flags as eligibility evidence. For every fully proven tree/kind pair, run both exact lifecycle cases and print mechanically verified `sanitizer_tree`, `sanitizer_kind`, `policy_lifetime_multi_account_test_compile=PASS`, `policy_lifetime_multi_account_test_link=PASS`, `multi_account_test_compile=PASS`, `multi_account_test_link=PASS`, `passive_case=PASS`, and `historical_case=PASS`. If no fully proven pair exists, print exactly `sanitizer_configured=0 sanitizer_status=NOT_RUN`; absence must never be labeled PASS or substitute for Release. Only after all applicable gates pass, run the freshly linked Release passive-burn lifetime case five consecutive times with its exact filter and emit `passive_lifetime_repeat_1=PASS` through `_5=PASS`. Record all prior T13-G01..G16 and new T13-G17..G25 named mitigations, with no unresolved HIGH, in `13-26-SUMMARY.md`. Do not edit product/test sources, existing plans/summaries, STATE, ROADMAP, REQUIREMENTS, REVIEW, or VERIFICATION.</action>
  <verify>
    <automated>set -euo pipefail; phase13_regex='genesis_manifest_test|quorum_policy_test|trust_state_store_test|operator_approval_test|trust_genesis_tool_test|trust_first_boot_e2e_test|trust_restart_test|trust_tamper_e2e_test|securecrdt|trustedpeer|burnconfig|account_management|node_startup|startup|multi_account'; expected_ctest='account_management_test,burnconfig_test,burnconfig_policy_e2e_test,securecrdt_interface_test,securecrdt_registry_test,securecrdt_candidate_test,securecrdt_candidate_race_test,securecrdt_quorum_gate_test,securecrdt_propose_sign_quorum_test,securecrdt_quorum_contract_e2e_test,genesis_manifest_test,trust_genesis_tool_test,quorum_policy_test,trust_state_store_test,operator_approval_test,trustedpeerregistry_genesis_test,trustedpeerregistry_quorum_test,trustedpeerregistry_threshold_floor_test,startup_wiring_test,trust_first_boot_e2e_test,trust_restart_test,trust_tamper_e2e_test,node_startup_test,multi_account_test,policy_lifetime_multi_account_test'; expected_focused='TrustStateStoreTest.PolicySuccessorRejectedUntilInitialBurnPeerConfirmed,TrustStateStoreTest.BurnV2RejectedUntilInitialBurnPeerConfirmed,TrustStateStoreTest.LoadAndVerifySerializesPolicyAndBurnTransitionWithoutTornSnapshot,TrustStateStoreTest.CommitPathsUseUnlockedVerifierWithoutSelfDeadlock,TrustFirstBootE2ETest.PolicyV2BeforeInitialBurnCannotStrandStartup,TrustRestartTest.HistoricalBurnProofRemainsReadyAfterCurrentThresholdIncrease,BurnConfigPolicyE2ETest.PayEscrowUsesExactOverflowSafeBurnForUint64Maximum,SecureCrdtQuorumGateTest.LocalOutsiderSignatureNeverPersists,SecureCrdtQuorumGateTest.RemoteOutsiderSignatureNeverReplicatesAndRetentionBoundTracksAuthorizedSet,TrustFirstBootE2ETest.ProductionControllerInitiatesBurnAndRestartRecoversPersistedApprovals,TrustFirstBootE2ETest.PreloadedRefreshActivationFailuresAreReturnedAndEmitted,TrustFirstBootE2ETest.PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall,TrustFirstBootE2ETest.RetainedPolicyQuorumRetriesAfterControllerReconstructionWithoutNewWrite,PolicyLifetimeMultiAccountTest.PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin,MultiAccountTest.PersistedHistoricalTrustAndTransactionsRestartWithSingleManagerOwnership'; evidence_dir="$(mktemp -d)"; cmake --build build/OSX/Release --target account_management_test burnconfig_test burnconfig_policy_e2e_test securecrdt_interface_test securecrdt_registry_test securecrdt_candidate_test securecrdt_candidate_race_test securecrdt_quorum_gate_test securecrdt_propose_sign_quorum_test securecrdt_quorum_contract_e2e_test genesis_manifest_test trust_genesis_tool_test quorum_policy_test trust_state_store_test operator_approval_test trustedpeerregistry_genesis_test trustedpeerregistry_quorum_test trustedpeerregistry_threshold_floor_test startup_wiring_test trust_first_boot_e2e_test trust_restart_test trust_tamper_e2e_test node_startup_test multi_account_test policy_lifetime_multi_account_test -j8; build/OSX/Release/test_bin/trust_state_store_test --gtest_filter='TrustStateStoreTest.PolicySuccessorRejectedUntilInitialBurnPeerConfirmed:TrustStateStoreTest.BurnV2RejectedUntilInitialBurnPeerConfirmed:TrustStateStoreTest.LoadAndVerifySerializesPolicyAndBurnTransitionWithoutTornSnapshot:TrustStateStoreTest.CommitPathsUseUnlockedVerifierWithoutSelfDeadlock' --gtest_output="xml:$evidence_dir/store.xml"; build/OSX/Release/test_bin/trust_restart_test --gtest_filter='TrustRestartTest.HistoricalBurnProofRemainsReadyAfterCurrentThresholdIncrease' --gtest_output="xml:$evidence_dir/restart.xml"; build/OSX/Release/test_bin/burnconfig_policy_e2e_test --gtest_filter='BurnConfigPolicyE2ETest.PayEscrowUsesExactOverflowSafeBurnForUint64Maximum' --gtest_output="xml:$evidence_dir/burn.xml"; build/OSX/Release/test_bin/securecrdt_quorum_gate_test --gtest_filter='SecureCrdtQuorumGateTest.LocalOutsiderSignatureNeverPersists:SecureCrdtQuorumGateTest.RemoteOutsiderSignatureNeverReplicatesAndRetentionBoundTracksAuthorizedSet' --gtest_output="xml:$evidence_dir/securecrdt.xml"; build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='TrustFirstBootE2ETest.PolicyV2BeforeInitialBurnCannotStrandStartup:TrustFirstBootE2ETest.ProductionControllerInitiatesBurnAndRestartRecoversPersistedApprovals:TrustFirstBootE2ETest.PreloadedRefreshActivationFailuresAreReturnedAndEmitted:TrustFirstBootE2ETest.PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall:TrustFirstBootE2ETest.RetainedPolicyQuorumRetriesAfterControllerReconstructionWithoutNewWrite' --gtest_output="xml:$evidence_dir/first-boot.xml"; build/OSX/Release/test_bin/policy_lifetime_multi_account_test --gtest_filter='PolicyLifetimeMultiAccountTest.PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin' --gtest_output="xml:$evidence_dir/passive-burn.xml"; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.PersistedHistoricalTrustAndTransactionsRestartWithSingleManagerOwnership' --gtest_output="xml:$evidence_dir/historical.xml"; python3 -c 'import glob,re,sys,xml.etree.ElementTree as E
expected_list=sys.argv[1].split(","); expected=set(expected_list); assert len(expected_list)==len(expected)==15
cases=[]
for path in glob.glob(sys.argv[2]+"/*.xml"):
    cases.extend(x for x in E.parse(path).getroot().iter() if x.tag.rsplit("}",1)[-1]=="testcase")
names=[x.get("classname")+"."+x.get("name") for x in cases]
bad=[x for x in cases if any(y.tag.rsplit("}",1)[-1] in ("failure","error","skipped") for y in x) or x.get("status","").lower() in ("disabled","notrun","skipped")]
noise=re.compile(r"//[^\n]*|/\*.*?\*/|\"(?:\\.|[^\"\\])*\"|\x27(?:\\.|[^\x27\\])*\x27",re.S)
decls=[]
for path in sys.argv[3:]:
    clean=noise.sub(lambda m:"\n"*m.group().count("\n"),open(path,encoding="utf-8").read())
    decls.extend(a+"."+b for a,b in re.findall(r"TEST(?:_F)?\s*\(\s*([A-Za-z_]\w*)\s*,\s*([A-Za-z_]\w*)\s*\)",clean,re.S))
focused_decls=[name for name in decls if name in expected]
disabled=[name for name in decls if (name.split(".",1)[0].startswith("DISABLED_") or name.split(".",1)[1].startswith("DISABLED_")) and ".".join(part.removeprefix("DISABLED_") for part in name.split(".",1)) in expected]
assert len(names)==15 and len(set(names))==15 and set(names)==expected
assert len(focused_decls)==15 and len(set(focused_decls))==15 and set(focused_decls)==expected
assert not bad and not disabled
print("focused_names_exact=PASS focused_declarations_exact=PASS focused_executed=15 focused_passed=15 focused_failed_or_skipped=0 focused_disabled=0")' "$expected_focused" "$evidence_dir" test/src/trustedpeer/trust_state_store_test.cpp test/src/startup/trust_restart_test.cpp test/src/account/burnconfig_policy_e2e_test.cpp test/src/securecrdt/securecrdt_quorum_gate_test.cpp test/src/startup/trust_first_boot_e2e_test.cpp test/src/multiaccount/policy_lifetime_multi_account_test.cpp test/src/multiaccount/multi_account_sync.cpp; ctest --test-dir build/OSX/Release -N -V -R "$phase13_regex" | tee "$evidence_dir/selection.txt"; python3 -c 'import re,sys
expected_list=sys.argv[1].split(","); expected=set(expected_list); assert len(expected_list)==len(expected)==25
text=open(sys.argv[2],encoding="utf-8").read()
names=re.findall(r"^\s*Test\s+#\d+:\s+(\S+?)(?:\s+\(Disabled\))?\s*$",text,re.M)
totals=re.findall(r"^Total Tests:\s*(\d+)\s*$",text,re.M)
assert len(totals)==1 and int(totals[0])==25
assert len(names)==25 and len(set(names))==25 and set(names)==expected
assert "(Disabled)" not in text
print("ctest_selected_names_exact=PASS selected=25 disabled=0")' "$expected_ctest" "$evidence_dir/selection.txt"; ctest --test-dir build/OSX/Release --output-on-failure --output-junit "$evidence_dir/results.xml" -R "$phase13_regex"; python3 -c 'import sys,xml.etree.ElementTree as E
expected_list=sys.argv[1].split(","); expected=set(expected_list); assert len(expected_list)==len(expected)==25
root=E.parse(sys.argv[2]).getroot(); tag=lambda x:x.tag.rsplit("}",1)[-1]
cases=[x for x in root.iter() if tag(x)=="testcase"]; names=[x.get("name") for x in cases]
failed=[x for x in cases if any(tag(y) in ("failure","error") for y in x)]
skipped=[x for x in cases if any(tag(y)=="skipped" for y in x) or x.get("status","").lower() in ("disabled","notrun","skipped")]
bad_attrs=[(tag(x),a,x.get(a)) for x in root.iter() if tag(x) in ("testsuites","testsuite") for a in ("failures","errors","skipped","disabled") if int(x.get(a,"0"))]
assert len(cases)==25 and len(set(names))==25 and set(names)==expected
assert not failed and not skipped and not bad_attrs
print("junit_names_exact=PASS executed=25 passed=25 failed=0 skipped=0 disabled=0")' "$expected_ctest" "$evidence_dir/results.xml"; : &gt; "$evidence_dir/sanitizer-eligible.tsv"; : &gt; "$evidence_dir/sanitizers.txt"; while IFS= read -r cache; do sanitizer_build="${cache%/CMakeCache.txt}"; python3 -c 'exec("""import json,pathlib,re,shlex,subprocess,sys
tree=pathlib.Path(sys.argv[1])
targets=("policy_lifetime_multi_account_test","multi_account_test")
kinds=("address","thread")
def has_kind(command,kind):
    for token in shlex.split(command):
        token=token.strip(chr(34)+chr(39))
        for prefix in ("-fsanitize=","/fsanitize=","/fsanitize:"):
            if token.startswith(prefix) and kind in token[len(prefix):].split(","):
                return True
    return False
def compile_commands(target):
    path=tree/"compile_commands.json"
    if not path.is_file(): return []
    entries=json.loads(path.read_text(encoding="utf-8"))
    marker="CMakeFiles/"+target+".dir/"
    return [entry.get("command") or " ".join(entry.get("arguments",[])) for entry in entries if marker in str(entry.get("output","")) or marker in (entry.get("command") or " ".join(entry.get("arguments",[])))]
def link_commands(target):
    links=list(tree.glob("**/CMakeFiles/"+target+".dir/link.txt"))
    if links:
        assert len(links)==1,(tree,target,"ambiguous link.txt",[str(x) for x in links])
        return [links[0].read_text(encoding="utf-8")]
    if (tree/"build.ninja").is_file():
        run=subprocess.run(["ninja","-C",str(tree),"-t","commands",target],text=True,capture_output=True,check=True)
        marker="CMakeFiles/"+target+".dir/"
        output=re.compile(r"(?:^|\\s)-o\\s+\\S*test_bin/"+re.escape(target)+r"(?:\\s|$)")
        return [line for line in run.stdout.splitlines() if marker in line and output.search(line)]
    return []
proof={}
for target in targets:
    compiles=compile_commands(target)
    proof[target]={kind:(bool(compiles) and all(has_kind(command,kind) for command in compiles)) for kind in kinds}
for kind in kinds:
    if all(proof[target][kind] for target in targets):
        print(str(tree)+"|"+kind)
""")' "$sanitizer_build" &gt;&gt; "$evidence_dir/sanitizer-eligible.tsv"; done &lt; &lt;(find build -name CMakeCache.txt -type f | sort); sort -u "$evidence_dir/sanitizer-eligible.tsv" -o "$evidence_dir/sanitizer-eligible.tsv"; while IFS='|' read -r sanitizer_build sanitizer_kind; do if test -z "$sanitizer_build"; then continue; fi; cmake --build "$sanitizer_build" --target policy_lifetime_multi_account_test multi_account_test -j8; python3 -c 'exec("""import json,pathlib,re,shlex,shutil,subprocess,sys
tree=pathlib.Path(sys.argv[1]); kind=sys.argv[2]
targets=("policy_lifetime_multi_account_test","multi_account_test")
def has_kind(command):
    for token in shlex.split(command):
        token=token.strip(chr(34)+chr(39))
        for prefix in ("-fsanitize=","/fsanitize=","/fsanitize:"):
            if token.startswith(prefix) and kind in token[len(prefix):].split(","):
                return True
    return False
def compile_commands(target):
    entries=json.loads((tree/"compile_commands.json").read_text(encoding="utf-8"))
    marker="CMakeFiles/"+target+".dir/"
    return [entry.get("command") or " ".join(entry.get("arguments",[])) for entry in entries if marker in str(entry.get("output","")) or marker in (entry.get("command") or " ".join(entry.get("arguments",[])))]
def link_commands(target):
    links=list(tree.glob("**/CMakeFiles/"+target+".dir/link.txt"))
    if links:
        assert len(links)==1,(tree,target,"ambiguous link.txt",[str(x) for x in links])
        return [links[0].read_text(encoding="utf-8")]
    if (tree/"build.ninja").is_file():
        run=subprocess.run(["ninja","-C",str(tree),"-t","commands",target],text=True,capture_output=True,check=True)
        marker="CMakeFiles/"+target+".dir/"
        output=re.compile(r"(?:^|\\s)-o\\s+\\S*test_bin/"+re.escape(target)+r"(?:\\s|$)")
        return [line for line in run.stdout.splitlines() if marker in line and output.search(line)]
    return []
def runtime_evidence(target):
    binary=tree/"test_bin"/target
    assert binary.is_file(),(tree,target,"binary missing")
    output=[]
    for argv in (("otool","-L",str(binary)),("ldd",str(binary)),("readelf","-d",str(binary)),("nm","-u",str(binary)),("strings",str(binary))):
        if shutil.which(argv[0]):
            run=subprocess.run(argv,text=True,capture_output=True)
            output.append(run.stdout); output.append(run.stderr)
    return "\\n".join(output)
runtime_pattern={"address":r"(?:lib(?:clang_rt\\.)?asan|__asan_)","thread":r"(?:lib(?:clang_rt\\.)?tsan|__tsan_)"}[kind]
for target in targets:
    compiles=compile_commands(target); links=link_commands(target); artifact=runtime_evidence(target)
    assert compiles and all(has_kind(command) for command in compiles),(tree,kind,target,"compile proof missing")
    assert (links and any(has_kind(command) for command in links)) or re.search(runtime_pattern,artifact,re.I),(tree,kind,target,"link/runtime proof missing")
print("sanitizer_target_proof=PASS")
""")' "$sanitizer_build" "$sanitizer_kind"; "$sanitizer_build/test_bin/policy_lifetime_multi_account_test" --gtest_filter='PolicyLifetimeMultiAccountTest.PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin'; "$sanitizer_build/test_bin/multi_account_test" --gtest_filter='MultiAccountTest.PersistedHistoricalTrustAndTransactionsRestartWithSingleManagerOwnership'; printf 'sanitizer_tree=%s sanitizer_kind=%s policy_lifetime_multi_account_test_compile=PASS policy_lifetime_multi_account_test_link=PASS multi_account_test_compile=PASS multi_account_test_link=PASS passive_case=PASS historical_case=PASS\n' "$sanitizer_build" "$sanitizer_kind" | tee -a "$evidence_dir/sanitizers.txt"; done &lt; "$evidence_dir/sanitizer-eligible.tsv"; sanitizer_configured="$(wc -l &lt; "$evidence_dir/sanitizers.txt" | tr -d '[:space:]')"; if test "$sanitizer_configured" -eq 0; then printf 'sanitizer_configured=0 sanitizer_status=NOT_RUN\n'; else python3 -c 'import re,sys
lines=[x.strip() for x in open(sys.argv[1],encoding="utf-8") if x.strip()]
count=int(sys.argv[2]); assert len(lines)==count and count&gt;0
rx=re.compile(r"^sanitizer_tree=(\S+) sanitizer_kind=(address|thread) policy_lifetime_multi_account_test_compile=PASS policy_lifetime_multi_account_test_link=PASS multi_account_test_compile=PASS multi_account_test_link=PASS passive_case=PASS historical_case=PASS$")
records=[rx.fullmatch(x) for x in lines]; assert all(records)
pairs={(m.group(1),m.group(2)) for m in records}; assert len(pairs)==count
print(f"sanitizer_configured={count} sanitizer_status=PASS sanitizer_records_exact=PASS")' "$evidence_dir/sanitizers.txt" "$sanitizer_configured"; fi; for i in 1 2 3 4 5; do if build/OSX/Release/test_bin/policy_lifetime_multi_account_test --gtest_filter='PolicyLifetimeMultiAccountTest.PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin'; then printf 'passive_lifetime_repeat_%s=PASS\n' "$i"; else exit 1; fi; done</automated>
  </verify>
  <acceptance_criteria>
    - Focused XML and comment/string-aware source declaration scans both report the exact unique 15-name set, including `TrustFirstBootE2ETest.PolicyV2BeforeInitialBurnCannotStrandStartup`, with `focused_executed=15 focused_passed=15` and zero failure/error/skip/not-run/disabled/disabled-counterpart result.
    - Plan 13-22's direct-hook and passive-policy source guards pass unchanged; the three new source-body guards report no receiver admin/direct activation, no post-reconstruction write, and true existing-path restart.
    - Mechanical CTest evidence proves the enumerated CTest name set and JUnit testcase-name set each equal the explicit expected 25-name set, then reports exactly `selected=25 executed=25 passed=25 failed=0 skipped=0 disabled=0` from freshly linked targets.
    - Every reported address/thread sanitizer tree/kind has target-scoped proof that all effective compile commands for both exact lifecycle targets carry that sanitizer and that each final executable either links with the same flag or imports the matching sanitizer runtime, then emits mechanically validated passive/historical PASS results; unrelated cache/toolchain/fuzz flags never qualify a tree, and zero fully proven pairs emits `sanitizer_configured=0 sanitizer_status=NOT_RUN`, never PASS.
    - Five separate focused passive-lifetime invocations emit `passive_lifetime_repeat_1=PASS` through `_5=PASS`.
    - `13-26-SUMMARY.md` maps T13-G01..G25 to named passing evidence and records no unresolved HIGH.
  </acceptance_criteria>
  <done>All twelve earlier and three new Phase 13 counterexamples, the explicit exact 25-target-name gate, every target-proven sanitizer tree/kind path, and five corrected passive-lifetime repetitions have mechanically honest results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Focused counterexamples to cumulative release evidence | New fixes cannot replace prior closure tests, and renamed/disabled/zero-match filters cannot count as execution. |
| CTest enumeration to pass claims | Exact enumeration and JUnit name-set equality prevents target substitution, lost registration, skipped tests, stale links, or disabled cases from passing through equal counts. |
| Lifecycle race fixes to runtime confidence | Only both targets' own effective compile commands plus each target's final link command or executable sanitizer-runtime imports may establish sanitizer eligibility; unrelated cache/toolchain/fuzz flags are excluded, every target-proven tree/kind must report both cases, and zero eligible pairs is NOT_RUN and never replaces Release. |

## STRIDE Threat Register

| Threat ID | Gap | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|-----|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G01..G16 | prior closure | Tampering/Elevation/Repudiation/DoS | HIGH | prior durable/startup/passive-policy boundaries | mitigate | Require all twelve extant prior focused names, including the CR-01 PolicyV2-before-initial-burn case, Plan 13-22 structural guards, and unchanged broad accounting. | Exact 15-name XML/declaration parser contains all twelve earlier names; explicit 25-target enumeration/JUnit name equality |
| T13-G17..G22 | CR-08/CR-09/WR-07 | Tampering/Elevation/Repudiation/DoS | HIGH | passive burn and retained policy replay | mitigate | Require exact passive-burn and retained-policy reconstruction cases plus no-admin/no-write guards and five passive repeats. | New focused XML, source guards, repeated filter |
| T13-G23..G25 | CR-10/WR-07 | Tampering/Repudiation/DoS | HIGH | serialized transitions, callback ownership, historical restart | mitigate | Exact historical case, one construction/start, functional callback assertions, mandatory Release, and every sanitizer run whose kind is proven in both exact targets' own compile commands plus final link command or executable runtime imports. | New focused XML, broad `multi_account_test`, mechanically checked target-scoped compile/link plus tree/kind/per-case records or NOT_RUN |
| T-13-SC | — | Tampering | LOW | package supply chain | accept | No package or dependency install occurs in Plans 13-23 through 13-26. | Summary dependency/change inspection |
</threat_model>

<verification>
The automated command is the final cumulative security/release gate. It must run with local-listener permission where the established production-composition tests require it and must restart from the beginning after any failure or code/test change. Its explicit 15-case and 25-target sets are equality gates, not count-only evidence; sanitizer eligibility requires each target's own compile proof plus final-link-command or executable-runtime proof, and absence of an eligible pair is recorded as NOT_RUN.
</verification>

<success_criteria>
- BOOT-04, POLICY-01, TEST-01, TPR-02, and BURN-03 have direct passing evidence for CR-08, CR-09, CR-10, and WR-07.
- All twelve earlier focused cases and three new focused cases remain uniquely declared, selected, executed, and green.
- The explicit expected 25 CTest targets equal both enumeration and JUnit name sets, preventing same-count target drift.
- No unresolved HIGH finding, target-name drift, skip, disabled test, receiver-side approval bypass, fresh-storage substitution, target-proven sanitizer failure, fuzz/unrelated-flag inference, or false sanitizer PASS remains.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable authenticated policy and live burn behavior converge across running/restarted production nodes and account lifetime | 13-01..13-22, 13-23..13-26 | COVERED | Third-gap plans close the remaining passive successor, reconstruction, and single-manager paths without changing the phase boundary. |
| REQ | BOOT-04 | Verified persisted state remains safe and live across restart | 13-24, 13-25, 13-26 | COVERED | Retained policy replay and true historical persisted startup directly close both restart blockers. |
| REQ | POLICY-01 | Current-policy quorum successor becomes durable after reconstruction | 13-24, 13-26 | COVERED | Existing approvals are listed and activated without a new write. |
| REQ | TEST-01 | Passive burn, retained policy, historical restart, exact focused/CTest name accounting, sanitizer availability, and repetitions | 13-23..13-26 | COVERED | Three new cases extend the exact twelve-case prior set to 15; both CTest enumeration and JUnit must equal the explicit 25-target set. |
| REQ | TPR-02 | Current-set N-of-M policy changes converge on passive/reconstructed receivers | 13-24, 13-26 | COVERED | A/B approvals advance reconstructed C with no admin action. |
| REQ | BURN-03 | Default burn remains live until explicit quorum successor changes passive PayEscrow | 13-23, 13-26 | COVERED | A proposes, B approves, passive C commits v2 and changes real output. |
| REQ | BOOT-01, BOOT-02, BOOT-03, VALID-01, SCRDT-04, TPR-01, BURN-01, BURN-02, MIG-05, MIG-06 | Remaining Phase 13 IDs | 13-01..13-22, 13-23, 13-25, 13-26 | COVERED | Prior accepted implementation is preserved and rerun; SCRDT-only transport and node-scoped provider are exercised by the new passive/lifetime cases. |
| CONTEXT | D-01..D-08 | Reviewed genesis, persisted identity, exact quorum floors, and current-policy successor authorization | 13-01..13-22, 13-24..13-26 | COVERED | No genesis/policy schema or authority rule changes; retained replay reuses the current-policy verifier. |
| CONTEXT | D-09, D-10, D-11, D-12 | Authenticated content-addressed CRDT candidates and explicit local approval | 13-23, 13-24, 13-26 | COVERED | CRDT remains the only transport; A/B are explicit; passive C never signs/administers. |
| CONTEXT | D-13, D-14, D-15 | Restricted startup, persisted restart authority, and rollback/fork safety | 13-24, 13-25, 13-26 | COVERED | Retained candidates retry from verified LKG and persisted-ready startup is idempotent. |
| CONTEXT | D-16 | Node-scoped policy and callback lifetime across account selection | 13-23, 13-25, 13-26 | COVERED | Passive economics survive manager lifetime changes; callback ownership is stop-before-replace. |
| RESEARCH | — | Persist-before-publish and current-predecessor deterministic candidate processing | 13-23, 13-24 | COVERED | Burn and policy candidates are listed, sorted, verified, committed, then published. |
| RESEARCH | — | Restricted serialized startup state machine and node-scoped callback ownership | 13-25, 13-26 | COVERED | Same-state suppression, transition epochs, single manager owner, historical restart evidence, and target-scoped effective compile/link sanitizer proof with per-case results. |
| RESEARCH | — | CRDT-only transport, no new package/RPC/topic, existing GoogleTest/CTest stack | 13-23..13-26 | COVERED | No dependency or external surface is introduced; exact existing target set remains 25. |
| REVIEW | CR-08 | Ready return strands passive burn successors | 13-23, 13-26 | COVERED | Successor processing precedes readiness; passive C PayEscrow proves convergence. |
| REVIEW | CR-09 | Reconstructed controller never lists retained policy quorum | 13-24, 13-26 | COVERED | `ListPendingPolicyCandidates` is in every startup/Refresh retry. |
| REVIEW | CR-10 | Persisted-ready startup constructs/starts two managers | 13-25, 13-26 | COVERED | Serialized/idempotent transition plus exactly-one historical restart test. |
| REVIEW | WR-07 | Fresh storage and same-node proposal substitute for required fixture paths | 13-23, 13-25, 13-26 | COVERED | Adds passive burn lifetime and true persisted historical restart; fresh-client coverage remains separate. |

Deferred bridge timing/RPC work, new transport, remote administration, and the accepted whole-disk/all-anchor rollback boundary remain excluded. No source item is missing.

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-26-SUMMARY.md` when done.
</output>
