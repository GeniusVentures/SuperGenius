---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 15
type: execute
wave: 2
depends_on: [13-13]
files_modified:
  - src/account/BurnConfig.cpp
  - test/src/startup/trust_restart_test.cpp
autonomous: true
gap_closure: true
requirements: [BOOT-04, BURN-02, BURN-03, TEST-01]
must_haves:
  truths:
    - "D-14: a burn head verified against its persisted authorizing policy remains economically authoritative after a later current policy changes burn_threshold."
    - "BootstrapOnly remains unready, while PeerQuorum readiness never depends on comparing historical proof cardinality with the current policy threshold."
    - "Restart restores the exact verified burn value and ConfirmedReady state without mutating either durable head."
  artifacts:
    - path: "src/account/BurnConfig.cpp"
      provides: "Verified authorization-kind readiness projection"
    - path: "test/src/startup/trust_restart_test.cpp"
      provides: "Historical-authorizer/current-threshold restart counterexample"
  key_links:
    - from: "BurnConfig::NewProduction"
      to: "ConfirmedTrustSnapshot::burn_authorization"
      via: "publish only a TrustStateStore-verified PeerQuorum burn"
      pattern: "burn_authorization.*PeerQuorum"
---

<objective>
Close CR-02 by making verified historical authorization, not the current policy threshold, authoritative during BurnConfig restart projection.

Purpose: Keep a valid durable economic head live across later quorum-policy evolution per D-14.
Output: Correct restart publication logic and a threshold-change regression.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-13-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore verified historical burn authority after policy-threshold evolution</name>
  <files>src/account/BurnConfig.cpp, test/src/startup/trust_restart_test.cpp</files>
  <read_first>
    - src/account/BurnConfig.hpp
    - src/account/BurnConfig.cpp
    - src/account/TrustStartupController.cpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/trustedpeer/TrustStateStore.cpp
    - test/src/startup/trust_restart_test.cpp
  </read_first>
  <behavior>
    - Burn v1 authorized by two signatures under policy v1 remains ready after policy v2 raises burn_threshold from 2 to 3.
    - Reopening TrustStateStore verifies burn v1 against policy v1, marks it PeerQuorum, and BurnConfig publishes value 100 without re-counting against policy v2.
    - A genesis/bootstrap-only burn snapshot remains WaitingForInitialBurn and is never published.
    - Restart leaves policy-v2 and burn-v1 hashes unchanged.
  </behavior>
  <action>RED: extend `trust_restart_test` with `HistoricalBurnProofRemainsReadyAfterCurrentThresholdIncrease`: commit genesis, peer-confirm burn v1 with policy-v1 threshold 2, commit a current-policy-authorized policy v2 over the same three peers with burn threshold 3, close/reopen through `TrustStartupController`, and prove the current code incorrectly remains unready. Commit as `test(13-15): add historical burn authority restart regression`. GREEN: in `BurnConfig::NewProduction`, delete the bootstrap-signature/proof-size/current-`burn_threshold` heuristic. Publish the snapshot exactly when `LoadAndVerify` reports `BurnAuthorizationKind::PeerQuorum`; leave `BootstrapOnly` unpublished. Do not re-run quorum evaluation in BurnConfig, do not compare `burn_proof.size()` with the current policy, and do not weaken TrustStateStore verification. Commit as `fix(13-15): trust verified burn authorizing policy on restart`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trust_restart_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_restart_test --gtest_filter='*HistoricalBurnProofRemainsReadyAfterCurrentThresholdIncrease*:*OmittedTrustConfigRestoresIdenticalDurableAuthority*'</automated>
  </verify>
  <acceptance_criteria>
    - The new test proves policy v1 threshold 2 authorized burn v1 and policy v2 current threshold 3 does not demote it.
    - Restart reports ConfirmedReady, `IsEconomicallyReady()==true`, value 100, and unchanged policy/burn hashes.
    - `rg -n "burn_proof.*size|policy\.burn_threshold" src/account/BurnConfig.cpp` finds no restart-readiness comparison.
    - A bootstrap-only regression remains unready and the automated command exits 0.
  </acceptance_criteria>
  <done>Every non-bootstrap burn head accepted by LoadAndVerify is authoritative after restart regardless of later policy-threshold changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verified persisted burn chain to runtime economic cache | Runtime must preserve the historical policy proof semantics established by the durable verifier. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G05 | Tampering/Denial of Service | HIGH | `BurnConfig::NewProduction` | mitigate | Consume explicit PeerQuorum classification from LoadAndVerify and never reinterpret proof count under the current policy. | `trust_restart_test --gtest_filter='*HistoricalBurnProofRemainsReadyAfterCurrentThresholdIncrease*'` |
| T-13-SC | Tampering | LOW | package supply chain | accept | No dependency or package change occurs. | Build graph inspection |
</threat_model>

<verification>
Run the focused restart cases, then the complete `trust_restart_test` binary.
</verification>

<success_criteria>
- CR-02 is closed for threshold increases and all valid historical-authorizer chains.
- Bootstrap-only state remains explicitly non-economic.
- Runtime publication consumes rather than contradicts durable verification.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-15-SUMMARY.md` when done.
</output>
