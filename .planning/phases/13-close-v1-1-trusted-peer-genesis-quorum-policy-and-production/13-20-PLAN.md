---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 20
type: execute
wave: 1
depends_on: []
files_modified:
  - src/account/TrustStartupController.hpp
  - src/account/TrustStartupController.cpp
  - test/src/startup/trust_first_boot_e2e_test.cpp
autonomous: true
gap_closure: true
requirements: [BOOT-04, BURN-03, TEST-01]
must_haves:
  truths:
    - "D-13: after verified TPR genesis, every eligible current peer idempotently submits its own deterministic burn-v1/value-100 approval through production startup orchestration; a non-member remains an expected signer-free waiting node."
    - "Restart between approval persistence and durable activation rediscovers the CRDT candidate, retries activation without an operator/admin trigger, and reaches ConfirmedReady after a successful durable commit."
    - "WR-06: Refresh treats only authenticated below-quorum and local non-membership as non-errors; preloaded genesis/burn validation or commit failures emit TRUST_ACTIVATION_FAILED and propagate the actionable error."
  artifacts:
    - path: "src/account/TrustStartupController.hpp"
      provides: "Node-local signer eligibility and reusable startup activation-result handling state"
    - path: "src/account/TrustStartupController.cpp"
      provides: "Production initial-burn initiation, CRDT rediscovery/retry, and actionable Refresh failure propagation"
    - path: "test/src/startup/trust_first_boot_e2e_test.cpp"
      provides: "Production composition, restart window, non-member no-sign, and preloaded failure counterexamples"
  key_links:
    - from: "TrustStartupController::Refresh"
      to: "BurnConfig::OnTrustedPeerGenesisConfirmed"
      via: "verified BootstrapOnly snapshot plus exact current-policy membership eligibility"
      pattern: "OnTrustedPeerGenesisConfirmed"
    - from: "TrustStartupController::Refresh"
      to: "BurnConfig::ListPendingBurnCandidates and TryActivateBurnCandidate"
      via: "restart-safe CRDT rediscovery followed by durable activation"
      pattern: "ListPendingBurnCandidates|TryActivateBurnCandidate"
---

<objective>
Close CR-05 and WR-06 by making production startup own deterministic initial-burn progress and preserve the activation result contract during refresh and restart.

Purpose: Ensure honest production nodes can leave WaitingForInitialBurn without a test-only hook or extra admin action, while operators see every actionable failure.
Output: Idempotent eligible-peer initiation, passive rediscovery/activation, strict pending-versus-error handling, and production-composition regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-14-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initiate and recover initial burn through production startup only</name>
  <files>src/account/TrustStartupController.hpp, src/account/TrustStartupController.cpp, test/src/startup/trust_first_boot_e2e_test.cpp</files>
  <read_first>
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - test/src/startup/trust_first_boot_e2e_test.cpp
    - src/account/BurnConfig.hpp
    - src/account/BurnConfig.cpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/TrustedPeerRegistry.cpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/securecrdt/SecureCrdt.hpp
    - src/crdt/globaldb/GlobalDbNetworkComposition.hpp
    - src/trustedpeer/genesis_tool/GenesisCeremony.hpp
  </read_first>
  <behavior>
    - `ProductionControllerInitiatesBurnAndRestartRecoversPersistedApprovals` connects two peer controllers and one genesis ceremony over production `GlobalDbNetworkComposition`; neither the test nor ceremony directly calls `OnTrustedPeerGenesisConfirmed` or a burn activation method.
    - After durable TPR genesis, each eligible peer contributes at most one exact burn-v1/value-100 approval, while an attached non-member controller contributes no approval and reports no activation failure merely because it cannot sign.
    - A fault-injected `COMMIT_FAILED` occurs after quorum approvals are retained but before one node durably activates burn v1. Reconstructing that node's controller over the retained GlobalDB and reopened normal store rediscovers the candidate, commits it, and reaches ConfirmedReady without resubmission or local admin action.
    - Repeated Refresh calls and callback delivery do not duplicate a peer's approval, change the deterministic candidate ID, or publish economic readiness before the durable peer-quorum record exists.
  </behavior>
  <action>RED: replace the existing direct `controller->burn_config()->OnTrustedPeerGenesisConfirmed()` first-boot shortcut with the named production-composition regression. Construct distinct stores/controllers for two manifest peers on the existing production CRDT topic and an additional non-member controller; use the existing `BatchCommitter` seam on one peer to fail the burn commit only after both CRDT approvals are present. Destroy that peer's controller, reopen its store without the failing committer while retaining its GlobalDB/SecureCrdt composition, and reconstruct the controller. Assert exact candidate/approval counts, non-member no-sign behavior, pre-commit unready state, and post-restart readiness. Commit RED as `test(13-20): add production initial-burn recovery counterexample`. GREEN: retain the local signer address in `TrustStartupController`. In `Refresh`, after a verified BootstrapOnly snapshot, check exact membership in the verified current policy before calling `BurnConfig::OnTrustedPeerGenesisConfirmed`; membership absence is the only expected no-sign branch. Merge the returned deterministic ID with `ListPendingBurnCandidates()` results, deduplicate, and attempt `TryActivateBurnCandidate` without holding `candidate_mutex_`. Treat `false` as valid pending, `true` as a durable commit that permits a verified reload/publication, and every error as actionable. Run the listing/activation path even for non-members so passive/restarted nodes can persist an already-quorate candidate. Preserve D-09/D-11: this automatic behavior is limited to the exact deterministic burn-v1/value-100 candidate following verified genesis and must never sign policy or later burn successors. Commit GREEN as `fix(13-20): drive initial burn from production startup`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trust_first_boot_e2e_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='*ProductionControllerInitiatesBurnAndRestartRecoversPersistedApprovals*'</automated>
  </verify>
  <acceptance_criteria>
    - The production test reaches burn readiness with zero direct calls to `OnTrustedPeerGenesisConfirmed`, `TryActivateBurnCandidate`, or an admin burn activation method from test code.
    - The test observes the same deterministic candidate before and after controller reconstruction, exactly one approval per eligible peer, and zero non-member approval.
    - The injected failed commit leaves the node unready; reconstruction and Refresh alone durably activate value 100 and reach ConfirmedReady.
    - `rg -n "OnTrustedPeerGenesisConfirmed|ListPendingBurnCandidates|TryActivateBurnCandidate" src/account/TrustStartupController.cpp` finds all three production links.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Production controllers autonomously and idempotently progress exact initial burn, including restart recovery after retained approvals, without broadening automatic signing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate preloaded Refresh activation failures without misreporting pending or no-sign</name>
  <files>src/account/TrustStartupController.cpp, test/src/startup/trust_first_boot_e2e_test.cpp</files>
  <read_first>
    - src/account/TrustStartupController.cpp
    - test/src/startup/trust_first_boot_e2e_test.cpp
    - src/account/TrustStartupController.hpp
    - src/account/BurnConfig.hpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/securecrdt/SecureCrdt.hpp
  </read_first>
  <behavior>
    - `PreloadedRefreshActivationFailuresAreReturnedAndEmitted` preloads a genesis candidate before controller construction and proves a forced genesis commit failure emits `TRUST_ACTIVATION_FAILED` with domain/version/hash/error and makes construction/Refresh fail with the same actionable error.
    - The same regression preloads a quorum burn candidate, forces `COMMIT_FAILED` during construction/restart Refresh, and proves the event and returned error are preserved rather than represented as WaitingForInitialBurn success.
    - A valid below-quorum preloaded candidate returns success in WaitingForInitialBurn without an activation-failure event; a non-member controller likewise waits signer-free without an error.
    - Invalid/actionable pending IDs are removed from the controller's in-memory retry list before returning, while valid below-quorum and transiently commit-failed CRDT candidates remain rediscoverable from authoritative CRDT storage on a later restart.
  </behavior>
  <action>RED: replace the remaining direct-hook callback-failure setup with `PreloadedRefreshActivationFailuresAreReturnedAndEmitted`; preload through `SecureCrdt::SubmitCandidateApproval` before constructing/reconstructing the controller so the test exercises Refresh rather than callback timing. Cover both reviewed genesis and burn activation failures, plus explicit below-quorum and non-member controls. Commit RED as `test(13-20): add preloaded refresh failure counterexamples`. GREEN: make the NOT_FOUND genesis activation branch inspect `TryActivateReviewedGenesisCandidate`: `false` is genuine pending, `true` requires a verified reload, and error emits `TRUST_ACTIVATION_FAILED` with candidate identity plus typed context and is returned unchanged. Apply the same contract to every burn candidate processed by Refresh. Do not discard errors, translate them to waiting success, or emit for `false`/non-membership. Callback paths must not call Refresh after an activation error; retain the CRDT record for restart/fault recovery but remove failed IDs from the ephemeral vector to avoid a tight in-process retry loop. Commit GREEN as `fix(13-20): propagate refresh activation failures`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trust_first_boot_e2e_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='*PreloadedRefreshActivationFailuresAreReturnedAndEmitted*:*ProductionControllerInitiatesBurnAndRestartRecoversPersistedApprovals*'</automated>
  </verify>
  <acceptance_criteria>
    - Preloaded genesis and burn `COMMIT_FAILED` cases each emit candidate identity/error context and return an error from construction/Refresh.
    - Below-quorum and non-member cases emit no `TRUST_ACTIVATION_FAILED`, return success, and remain WaitingForInitialBurn.
    - No test in `trust_first_boot_e2e_test.cpp` directly calls `OnTrustedPeerGenesisConfirmed` or `TryActivateBurnCandidate` after this task.
    - Retrying through a reconstructed controller succeeds after the injected committer failure is removed.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>WR-06 is closed for preloaded and restart paths: only genuine pending/no-sign is quiet, and every actionable activation failure is returned and observable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verified bootstrap-only state to automatic local approval | Automatic signing is allowed only for the canonical burn-v1/value-100 candidate and only by a current peer. |
| Preloaded CRDT candidates to startup state | Remote retained data may trigger verification/activation but must not hide corruption or durable failure. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G12 | Denial of Service | HIGH | initial burn startup lifecycle | mitigate | Production controllers submit/deduplicate eligible burn-v1 approvals and rediscover/activate them after restart. | `ProductionControllerInitiatesBurnAndRestartRecoversPersistedApprovals` |
| T13-G13 | Elevation of Privilege | HIGH | automatic signing boundary | mitigate | Require verified current-policy membership and exact deterministic burn-v1 core; non-members remain signer-free and later candidates are never auto-signed. | Non-member and exact-approval assertions in `ProductionControllerInitiatesBurnAndRestartRecoversPersistedApprovals` |
| T13-G14 | Repudiation/Denial of Service | HIGH | `Refresh` activation outcomes | mitigate | Emit candidate identity plus typed error and propagate genesis/burn activation failures; quiet only for false pending or no-sign. | `PreloadedRefreshActivationFailuresAreReturnedAndEmitted` |
| T-13-SC | Tampering | LOW | package supply chain | accept | Existing repository libraries only; no install or dependency change. | Changed-file inspection |
</threat_model>

<verification>
Run both task commands, then the complete `trust_first_boot_e2e_test` target to ensure the existing restricted-first-boot and activation diagnostics remain green.
</verification>

<success_criteria>
- CR-05 is closed by a production-owned path with restart recovery and no direct test hook.
- WR-06 is closed for preloaded genesis and burn candidates with exact pending/error semantics.
- BURN-03 value 100 reaches durable economic readiness without auto-signing any successor class.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-20-SUMMARY.md` when done.
</output>
