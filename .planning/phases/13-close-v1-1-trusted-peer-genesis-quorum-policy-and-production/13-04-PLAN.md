---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 04
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - src/securecrdt/SecureCrdtCandidate.hpp
  - src/securecrdt/SecureCrdtCandidate.cpp
  - src/securecrdt/SecureCrdt.hpp
  - src/securecrdt/SecureCrdt.cpp
  - src/securecrdt/SecureCrdtRegistry.hpp
  - src/securecrdt/CMakeLists.txt
  - test/src/securecrdt/securecrdt_candidate_test.cpp
  - test/src/securecrdt/securecrdt_candidate_race_test.cpp
  - test/src/securecrdt/CMakeLists.txt
autonomous: true
requirements: [SCRDT-04, TPR-02, BURN-01, POLICY-01, VALID-01, TEST-01]
must_haves:
  truths:
    - "D-09/D-11: a proposal is a signed candidate approval record written directly through SecureCrdt; receipt/relay never invokes signing, and the explicit proposer approval is the first signature."
    - "D-10: local and remote gates reject malformed, oversized, or non-current-peer-authenticated candidate records before datastore retention or candidate notification."
    - "D-12: version/content-hash keys isolate concurrent candidates and their exact-byte signatures; candidate A approvals cannot count for B."
    - "D-12: candidate resources are bounded to 64 KiB each, 32 active candidates per predecessor, 256 approvals per candidate, and 64 MiB active approval bytes per predecessor."
  artifacts:
    - path: "src/securecrdt/SecureCrdtCandidate.hpp"
      provides: "Canonical candidate core, signed approval record, exact key codec, and resource constants"
      exports: ["CandidateCore", "CandidateApprovalRecord", "CandidateId", "CandidateLimits"]
    - path: "src/securecrdt/SecureCrdt.hpp"
      provides: "Submit, query, filter, and callback APIs for authenticated candidate approvals"
    - path: "test/src/securecrdt/securecrdt_candidate_test.cpp"
      provides: "Local/remote authorization, addressing, binding, and cap coverage"
  key_links:
    - from: "src/securecrdt/SecureCrdt.cpp"
      to: "src/trustedpeer/TrustStateStore.hpp"
      via: "registered authorization source supplies current confirmed policy snapshot"
      pattern: "authorization_source|ConfirmedTrustSnapshot"
    - from: "src/securecrdt/SecureCrdt.cpp"
      to: "src/multisig/MultiSig.cpp"
      via: "exact canonical candidate-byte signature verification"
      pattern: "VerifyPayloadSignature"
---

<objective>
Add an ordering-independent, authenticated, bounded, content-addressed candidate path to SecureCrdt while retaining the legacy Phase 9 API for compatibility.

Purpose: Give production TPR and BurnConfig an authorized proposal/approval transport with no new RPC, pubsub topic, or consensus protocol.
Output: Candidate/key codecs, shared local/remote validation, SecureCrdt candidate operations, callbacks, resource enforcement, race scaffolding, and tests.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VALIDATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-01-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-02-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define bounded candidate and approval codecs</name>
  <files>src/securecrdt/SecureCrdtCandidate.hpp, src/securecrdt/SecureCrdtCandidate.cpp, src/securecrdt/CMakeLists.txt, test/src/securecrdt/securecrdt_candidate_test.cpp, test/src/securecrdt/CMakeLists.txt</files>
  <read_first>
    - src/trustedpeer/CanonicalTrustCodec.hpp
    - src/trustedpeer/QuorumPolicy.hpp
    - src/securecrdt/SecureCrdt.hpp
    - src/crdt/hierarchical_key.hpp
    - src/base/blob.hpp
    - src/multisig/MultiSig.hpp
    - test/src/securecrdt/securecrdt_quorum_gate_test.cpp
    - src/securecrdt/CMakeLists.txt
    - test/src/securecrdt/CMakeLists.txt
  </read_first>
  <behavior>
    - Canonical core binds domain, codec version, network, candidate kind, version, expected predecessor hash, authorizing policy hash, and payload.
    - Exact key is `<domain>/candidate/v<version>/<64-lower-hex-hash>/approval/<128-lower-hex-signer>` and rejects missing/extra/malformed segments.
    - Decode rejects candidate bytes over 65,536, unknown kinds/version, trailing bytes, key/core version mismatch, hash mismatch, and signer mismatch.
  </behavior>
  <action>Write codec/path/cap tests first and register `SecureCrdtCandidate.cpp` plus `securecrdt_candidate_test` in the existing CMake files before invoking the target. Implement `CandidateKind::{TrustPolicy,BurnConfig,TrustedPeerGenesis}`, `CandidateCore`, `CandidateId`, `CandidateKey`, and `CandidateApprovalRecord` with canonical bytes reusing the Plan 01 writer/reader. Define `MAX_CANDIDATE_BYTES=65536`, `MAX_ACTIVE_CANDIDATES_PER_PREDECESSOR=32`, `MAX_APPROVALS_PER_CANDIDATE=256`, and `MAX_ACTIVE_APPROVAL_BYTES_PER_PREDECESSOR=64*1024*1024`. Hash exactly the canonical core. Require lowercase canonical hash/signer path segments and full parse consumption. Each approval record contains the exact canonical core bytes plus its signature, so an approval is independently authenticated regardless of CRDT element arrival order.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target securecrdt_candidate_test -j8 &amp;&amp; build/OSX/Release/test_bin/securecrdt_candidate_test --gtest_filter='*Codec*:*Key*:*Bounds*'</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover exactly 65,536 accepted and 65,537 rejected bytes plus 31/32/33 candidate, 255/256/257 approval, and 64 MiB boundary accounting helpers.
    - Cross-candidate and mixed-case/path-confusion vectors fail before signature/quorum logic.
    - `rg -n "MAX_CANDIDATE_BYTES|MAX_ACTIVE_CANDIDATES_PER_PREDECESSOR|MAX_APPROVALS_PER_CANDIDATE|MAX_ACTIVE_APPROVAL_BYTES_PER_PREDECESSOR" src/securecrdt/SecureCrdtCandidate.hpp` finds all exact caps.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Candidate content, identity, approval signer, and resource use have one bounded canonical representation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce one authorization gate for local and remote candidate writes</name>
  <files>src/securecrdt/SecureCrdt.hpp, src/securecrdt/SecureCrdt.cpp, src/securecrdt/SecureCrdtRegistry.hpp, test/src/securecrdt/securecrdt_candidate_test.cpp</files>
  <read_first>
    - src/securecrdt/SecureCrdtCandidate.hpp
    - src/securecrdt/SecureCrdtRegistry.hpp
    - src/securecrdt/SecureCrdt.hpp
    - src/securecrdt/SecureCrdt.cpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/multisig/MultiSig.cpp
    - src/crdt/globaldb/globaldb.hpp
    - src/crdt/impl/crdt_callback_manager.cpp
    - test/src/securecrdt/securecrdt_candidate_test.cpp
  </read_first>
  <behavior>
    - Local submit and remote filter run the same bounded parse, key/hash/context/current-authorizer/signer-membership/signature/cap sequence before `GlobalDB::Put` retention.
    - A current peer's valid record is retained and surfaced, but no signing method is called by the receive callback.
    - Outsiders, old-policy signers, wrong network/type/predecessor/version records, invalid signatures, duplicate signer records, and cap-exceeding new candidates are rejected.
    - Existing-candidate approvals remain admissible until its 256-approval/64 MiB bounds even when the 32-candidate creation cap is full.
  </behavior>
  <action>Add `CandidateAuthorizationSource` and candidate-domain registration to the instance registry, where the callback resolves either the reviewed bootstrap authority for `TrustedPeerGenesis` or a verified current `ConfirmedTrustSnapshot` for successors. Add `SecureCrdt::SubmitCandidateApproval`, `ReadCandidateApprovals`, `ListCandidates(predecessor)`, and `RegisterCandidateCallback`; route local and remote writes through one private `ValidateCandidateApproval` with the exact validation order named in the behavior. Use the existing CRDT topic and `GlobalDB::Put` only after validation. Register exact candidate-path filters once, check registration results, and copy callbacks before invoking them. Candidate callbacks receive metadata/bytes for display only and contain no signer dependency. Keep legacy `ProposeValue`, `AddSignature`, and `ReadIfQuorum` working for pre-Phase-13 consumers.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target securecrdt_candidate_test securecrdt -j8 &amp;&amp; build/OSX/Release/test_bin/securecrdt_candidate_test --gtest_filter='*Authorization*:*LocalRemote*:*Callback*:*Limit*'</automated>
  </verify>
  <acceptance_criteria>
    - A spy signer/approval callback proves remote reception changes candidate visibility but produces zero new signatures.
    - Rejected outsider/malformed/oversize elements are absent from `GlobalDB::Get`/`QueryKeyValues` and never invoke candidate callbacks.
    - An approval signed over candidate A is rejected at candidate B's key.
    - No new RPC method, pubsub topic constant, consensus proposal, vote, or certificate type is introduced.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Only bounded current-authority signed approval records enter the CRDT candidate keyspace, using identical local and remote enforcement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove coexistence, deduplication, and stale isolation</name>
  <files>src/securecrdt/CMakeLists.txt, test/src/securecrdt/securecrdt_candidate_test.cpp, test/src/securecrdt/securecrdt_candidate_race_test.cpp, test/src/securecrdt/CMakeLists.txt</files>
  <read_first>
    - src/securecrdt/SecureCrdtCandidate.hpp
    - src/securecrdt/SecureCrdt.hpp
    - test/src/securecrdt/securecrdt_test_node.hpp
    - test/src/securecrdt/securecrdt_propose_sign_quorum_test.cpp
    - test/src/securecrdt/securecrdt_candidate_test.cpp
    - test/src/securecrdt/securecrdt_candidate_race_test.cpp
    - test/src/securecrdt/CMakeLists.txt
    - src/securecrdt/CMakeLists.txt
  </read_first>
  <behavior>
    - Two candidates for the same predecessor/version coexist under different content hashes.
    - Duplicate approval by one signer counts once; approvals never mix between candidate IDs.
    - Marking a predecessor non-current makes all its unfinished candidates stale and unable to be returned as activatable, while preserving bounded audit visibility.
  </behavior>
  <action>Wire production and both validation targets. Build a barrier-driven two-candidate fixture without sleeps. At this transport layer prove coexistence, per-candidate deduplication, and stale isolation through an injected current-snapshot source; leave the exactly-one durable activation assertion for Plan 05, which will extend the same race target using `TrustStateStore`. Ensure test teardown unregisters callbacks/filters for the owning SecureCrdt only.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target securecrdt_candidate_test securecrdt_candidate_race_test -j8 &amp;&amp; ctest --test-dir build/OSX/Release --output-on-failure -R 'securecrdt_candidate(_race)?'</automated>
  </verify>
  <acceptance_criteria>
    - Race test observes both distinct candidate IDs and isolated approval counts under synchronized delivery.
    - Repeated same-signer approval does not increase the count.
    - Changing the injected current predecessor makes both unfinished candidates stale without re-keying or merging them.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>The candidate transport preserves concurrent content, deduplicates signatures, and prevents stale predecessor data from becoming current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote CRDT element to local datastore | Untrusted network bytes attempt to enter candidate storage/callbacks. |
| Candidate key/record to approval count | Path metadata and signed bytes must describe the same candidate and signer. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-03 | Spoofing/DoS | HIGH | candidate ingress | mitigate | Bounded exact parse, current-policy signer membership, signature verification, and caps before retention. | `securecrdt_candidate_test --gtest_filter='*Authorization*:*LocalRemote*:*Limit*'` |
| T13-04 | Tampering | HIGH | approval storage | mitigate | Content hash and signer in exact path; record repeats canonical bytes; signature binds those bytes. | `securecrdt_candidate_test --gtest_filter='*Key*:*Authorization*'` |
| T13-06 | Tampering | HIGH | concurrent candidates | mitigate | Content-addressed isolation and stale predecessor checks; durable single winner added in Plan 05. | `securecrdt_candidate_race_test` |
| T13-12 | Denial of Service | MEDIUM | trusted proposer inbox | mitigate | Exact 64 KiB/32/256/64 MiB caps, bounded indexes, and existing-candidate approval priority. | `securecrdt_candidate_test --gtest_filter='*Bounds*:*Limit*'` |
| T-13-SC | Tampering | LOW | package supply chain | accept | No package install; existing CRDT and multisig only. | CMake build graph inspection |
</threat_model>

<verification>
Run `ctest --test-dir build/OSX/Release --output-on-failure -R 'securecrdt_candidate|securecrdt_registry|securecrdt_quorum'`.
</verification>

<success_criteria>
- Authenticated signed approvals are the only retained Phase 13 candidate elements.
- Candidate keys and signatures are content/version/predecessor/policy bound and cannot mix.
- Resource caps and concurrent-candidate behavior are exact, automated, and ordering-independent.
- CRDT remains the sole existing transport; no remote administration or consensus protocol is added.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-04-SUMMARY.md` when done.
</output>
