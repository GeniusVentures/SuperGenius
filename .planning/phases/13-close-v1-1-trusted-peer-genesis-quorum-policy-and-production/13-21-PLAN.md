---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 21
type: execute
wave: 2
depends_on: [13-20]
files_modified:
  - src/account/TrustStartupController.cpp
  - test/src/startup/trust_first_boot_e2e_test.cpp
autonomous: true
gap_closure: true
requirements: [POLICY-01, TPR-02, TEST-01]
must_haves:
  truths:
    - "D-05/D-08/D-12: a passive receiving node durably activates the first trusted-peer policy candidate that reaches current-policy quorum, without a local signature or extra administrative call."
    - "The trusted-peer callback treats false as authenticated pending, reports actionable validation/commit errors, and calls Refresh only after successful durable activation."
    - "D-16: the node-scoped trusted-peer callback is registered once, captures the controller weakly, and is owner-safely unregistered so later approvals cannot call or advance a destroyed controller."
  artifacts:
    - path: "src/account/TrustStartupController.cpp"
      provides: "Signer-free trusted-peer passive activation callback and safe lifecycle teardown"
    - path: "test/src/startup/trust_first_boot_e2e_test.cpp"
      provides: "Three-node passive policy activation, no-auto-sign, failure, and unregister regression"
  key_links:
    - from: "SecureCrdt trusted-peer candidate callback"
      to: "TrustedPeerRegistry::TryActivatePolicyCandidate"
      via: "receive-side quorum re-derivation without any Propose/Approve/sign call"
      pattern: "RegisterCandidateCallback.*trusted-peer|TryActivatePolicyCandidate"
    - from: "successful policy activation"
      to: "TrustStartupController::Refresh"
      via: "refresh occurs only when TryActivatePolicyCandidate returns true"
      pattern: "activated.*value|Refresh"
---

<objective>
Close CR-06 by activating quorum-confirmed policy on passive production nodes through the existing CRDT callback lifecycle.

Purpose: Keep all receivers on the same durable current policy without inventing a transport or requiring a third operator action.
Output: A signer-free trusted-peer callback, exact pending/error/success behavior, owner-safe unregister, and a multi-node production regression.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-20-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist quorum policy on passive receivers without signing</name>
  <files>src/account/TrustStartupController.cpp, test/src/startup/trust_first_boot_e2e_test.cpp</files>
  <read_first>
    - src/account/TrustStartupController.cpp
    - test/src/startup/trust_first_boot_e2e_test.cpp
    - src/account/TrustStartupController.hpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/TrustedPeerRegistry.cpp
    - src/trustedpeer/genesis_tool/LocalTrustAdmin.hpp
    - src/trustedpeer/genesis_tool/LocalTrustAdmin.cpp
    - src/account/BurnConfig.hpp
    - src/securecrdt/SecureCrdt.hpp
    - src/crdt/globaldb/globaldb.hpp
    - src/crdt/impl/crdt_callback_manager.cpp
  </read_first>
  <behavior>
    - `PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall` starts three production controllers on one CRDT topic, reaches initial-burn readiness through Plan 13-20 behavior, then has operator A propose policy v2 and operator B approve it exactly once.
    - The third node performs no proposal, approval, signing callback, direct activation call, or admin call for policy v2; after receiving the two approvals it independently persists the identical policy-v2 hash and remains ConfirmedReady.
    - The approval set remains exactly the two explicit operators, proving passive receipt never signs. An authenticated one-approval candidate remains pending without event or Refresh-driven state change.
    - A forced passive-node policy commit failure emits `TRUST_ACTIVATION_FAILED` with trusted-peer candidate identity and leaves the durable v1 head unchanged; it is not converted to pending.
    - Resetting the passive controller unregisters its trusted-peer callback; a later v3 quorum can propagate without a crash, callback into the expired owner, or mutation of the passive node's v2 store.
  </behavior>
  <action>RED: extend the production first-boot E2E with the named three-node test. Name operator B's local surface `operator_b_admin` and use `LocalTrustAdmin` only for operator A's proposal and exactly one `operator_b_admin.Approve(...)` decision. Do not construct a passive-node admin surface. Remove the old pattern that injected the second signature and then called `admin.Approve()` again merely to trigger activation. Count CRDT approvals before and after passive activation, inject a passive-store commit failure for a separate candidate, and reset the passive controller before publishing a later quorum to exercise unregister. Commit RED as `test(13-21): add passive policy activation counterexample`. GREEN: register a `trusted-peer` candidate callback alongside `trusted-peer-genesis` and `burn-config` using the same owner token and weak controller capture. The callback calls only `registry_->TryActivatePolicyCandidate(id)`: `false` is quiet pending with no Refresh, error emits `TRUST_ACTIVATION_FAILED` with domain/version/hash/typed context and performs no Refresh, and `true` calls Refresh so controller state/caches are derived from the successful durable commit. It must never call a proposal, approval, signer, or `SubmitCandidateApproval` path. Update the destructor to owner-safely unregister `trusted-peer` before releasing dependencies; partial registration failure must unwind through the same safe destructor path without removing another owner's callback. Commit GREEN as `fix(13-21): activate replicated policy on passive nodes`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trust_first_boot_e2e_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='*PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall*:*ProductionControllerInitiatesBurnAndRestartRecoversPersistedApprovals*'</automated>
  </verify>
  <acceptance_criteria>
    - Two and only two explicit operator approvals produce the same durable policy-v2 hash on the passive third node.
    - The passive node's signing callback count does not increase for the policy candidate, and no passive admin/direct activation call exists in the test.
    - Pending false emits no failure and causes no refresh; forced commit error emits `TRUST_ACTIVATION_FAILED`, preserves the prior durable head, and causes no refresh.
    - After passive-controller destruction, later candidate delivery neither crashes nor advances its store; the other nodes continue normally.
    - `rg -n 'RegisterCandidateCallback\(|UnregisterCandidateCallbackIf\(' src/account/TrustStartupController.cpp` shows all three domains symmetrically registered/unregistered.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Every receiving production node independently persists a quorum-confirmed policy without automatic signing, extra admin calls, silent errors, or callback lifetime hazards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Replicated policy approvals to passive durable authority | Receipt may trigger validation/commit, never a signature or administrative decision. |
| GlobalDB callback registry to controller lifetime | A process-lifetime callback must not outlive or replace its owning node controller. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G15 | Tampering/Elevation of Privilege | HIGH | passive policy activation | mitigate | Re-derive current-policy quorum in `TryActivatePolicyCandidate`, commit before refresh, and assert identical passive durable head with no passive signature. | `PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall` |
| T13-G16 | Repudiation/Denial of Service | HIGH | policy callback result/lifetime | mitigate | Distinguish false/error/true, emit typed errors, refresh only on true, weak-capture, and owner-safe unregister. | Pending, forced-error, and post-destruction sections of `PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall` |
| T-13-SC | Tampering | LOW | package supply chain | accept | No new package, transport, topic, or RPC is permitted. | Changed-file and registration inspection |
</threat_model>

<verification>
Run the focused test and then the complete `trust_first_boot_e2e_test`; inspect the production callback for absence of signing/proposal/admin calls.
</verification>

<success_criteria>
- CR-06 is closed on a passive third production node.
- POLICY-01 and TPR-02 activation converges through CRDT approvals alone.
- Pending, actionable error, successful refresh, and destruction behaviors all have automated evidence.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-21-SUMMARY.md` when done.
</output>
