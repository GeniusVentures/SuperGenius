---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 25
type: execute
wave: 1
depends_on: []
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/multiaccount/multi_account_sync.cpp
autonomous: true
gap_closure: true
requirements: [BOOT-04, BURN-02, TEST-01]
must_haves:
  truths:
    - "CR-10: a persisted-ready restart cannot re-enter the in-progress `INITIALIZING_TRANSACTIONS` transition and constructs/starts exactly one TransactionManager."
    - "GeniusNode transitions are serialized and idempotent: same-state requests are suppressed, stale trust-ready posts cannot move a later state backward, and legitimate waiting-to-transaction initialization remains possible."
    - "Before any TransactionManager replacement, the prior manager and its bridge, GlobalDB, account, state, and blockchain callback ownership are stopped/unregistered/released before the replacement registers callbacks."
    - "WR-07: a true same-path historical restart reopens persisted trust plus transaction data and proves the replacement node has one live manager owner with stable GlobalDB/account/blockchain callbacks."
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "Serialized transition ownership and TransactionManager lifecycle generation/count invariants"
    - path: "src/account/GeniusNode.cpp"
      provides: "Same-state suppression, trust callback gating, and stop-before-replace manager ownership"
    - path: "test/src/multiaccount/multi_account_sync.cpp"
      provides: "Historical persisted trust/transaction restart counterexample plus retained fresh-client recovery coverage"
  key_links:
    - from: "TrustStartupController::StateCallback"
      to: "GeniusNode::StateTransition"
      via: "ConfirmedReady schedules transaction initialization only from trust waiting states and carries a transition generation"
      pattern: "ConfirmedReady|INITIALIZING_TRANSACTIONS"
    - from: "GeniusNode transaction initialization"
      to: "TransactionManager::New and Start"
      via: "serialized same-state guard and stop-before-replace owner handoff"
      pattern: "TransactionManager::New|transaction_manager_->Start"
---

<objective>
Close CR-10 and the restart half of WR-07 by making GeniusNode lifecycle transitions single-owner, idempotent, and safe for persisted-ready startup.

Purpose: Prevent duplicate TransactionManagers and callback loss/corruption when synchronous trust readiness posts the same state already being initialized.
Output: Serialized transition rules, stop-before-replace ownership, lifecycle diagnostics, and a true historical persisted trust/transaction restart regression.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-08-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-22-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize persisted-ready transaction initialization and callback ownership</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp</files>
  <read_first>
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - src/account/GeniusAccount.hpp
    - src/account/GeniusAccount.cpp
    - src/blockchain/Blockchain.hpp
    - src/blockchain/impl/Blockchain.cpp
    - src/crdt/globaldb/globaldb.hpp
    - src/crdt/globaldb/globaldb.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/multiaccount/policy_lifetime_multi_account_test.cpp
    - test/src/blockchain/node_startup_test.cpp
    - test/testutil/genius_node_test_access.hpp
  </read_first>
  <behavior>
    - `MultiAccountTest.PersistedHistoricalTrustAndTransactionsRestartWithSingleManagerOwnership` first reaches reviewed trust/burn readiness, persists at least one confirmed transaction and its CID, destroys the node, and reconstructs the same account through the fixture's `historical_base_path` branch without deleting or replacing trust/transaction storage.
    - On reconstructed persisted-ready startup, lifecycle counters report exactly one TransactionManager construction and one Start call; the manager/provider identity remains unchanged through READY and no displaced-manager destructor can unregister replacement callbacks.
    - Historical transaction count/CID are visible after restart, one new transaction is observed exactly once, the account transaction-CID callback resolves through the current manager, and the blockchain slot-hash callback owner generation equals the live manager generation.
    - The pre-existing fresh-client validator-registry recovery scenario remains separate and green; it is not accepted as the historical restart assertion.
  </behavior>
  <action>RED: keep `MissingRegistryBlockIsFetchedFromPeerByCid` as the separate fresh-client coverage and add the exact named historical restart test. Name the retained path variable `historical_base_path` and pass that unchanged value through the fixture's existing-base-path branch to reopen the same account/network/trust/transaction databases after reviewed trust is durable and a confirmed transaction/CID exists. Through the existing friend `MultiAccountTestAccess`, expose per-node TransactionManager construction count, start count, owner generation, and current account/blockchain callback owner generation; assert all are exactly one/current and exercise historical plus one new transaction flow. Commit as `test(13-25): restore persisted historical restart counterexample`. GREEN: serialize `GeniusNode::StateTransition` with a lifecycle mutex and explicit in-progress/epoch state that supports existing same-thread nested transitions without allowing concurrent duplicate execution. Suppress an exact same-state request while that transition is in progress, and make posted callbacks validate their captured transition epoch/current source state before invoking a later transition. In the trust `StateCallback`, schedule `INITIALIZING_TRANSACTIONS` only when `ConfirmedReady` advances a node currently in `WAITING_FOR_TRUST_GENESIS` or `WAITING_FOR_BURN_GENESIS`; persisted-ready construction already executing `INITIALIZING_TRANSACTIONS` must not post it. Centralize TransactionManager replacement in a helper that invalidates bridge observers, unregisters the old state callback, clears the old blockchain slot-hash populator, calls `Stop`, resets bridge/manager ownership so the old destructor clears GlobalDB/account callbacks, and only then constructs the replacement and registers callbacks capturing the new owner generation. Increment construction/start counters at the exact successful calls, not on attempts. Preserve legitimate SelectAccount recreation, transition error logging, and node-scoped trust services per D-14/D-16. Commit as `fix(13-25): make GeniusNode transitions and manager ownership idempotent`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_account_test node_startup_test -j8 &amp;&amp; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.PersistedHistoricalTrustAndTransactionsRestartWithSingleManagerOwnership:ValidatorRegistryTest.MissingRegistryBlockIsFetchedFromPeerByCid' &amp;&amp; build/OSX/Release/test_bin/node_startup_test --gtest_filter='NodeStartupTest.GenesisNodeDefaultBurnRateIsOnePercent'</automated>
  </verify>
  <acceptance_criteria>
    - The persisted historical case passes the identical `historical_base_path` through the existing-base-path branch for the same account, loads durable trust plus transaction history, and reports construction=1/start=1 for the restarted node.
    - GlobalDB transaction observation, account CID lookup, blockchain slot-hash owner generation, and confirmed burn provider remain owned by the single live manager; a new transaction is not duplicated.
    - The separate fresh-client registry case and normal node-startup case remain green.
    - Same-state transition suppression and trust-ready source-state gating are visible in production code; every replacement executes Stop/unregister/reset before New/register/Start.
    - The automated command exits 0 with no skip or disabled counterpart.
  </acceptance_criteria>
  <done>Persisted-ready startup and account recreation maintain exactly one TransactionManager and one coherent set of GlobalDB/account/blockchain callbacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trust readiness callback to GeniusNode lifecycle | Synchronous/posted readiness must not duplicate or rewind transaction initialization. |
| Old TransactionManager to replacement callback owners | Destruction/unregistration of the old owner must complete before the new owner registers shared callback patterns. |
| Persisted historical stores to restart services | Verified trust and transaction history must be reopened without fresh-storage substitution. |

## STRIDE Threat Register

| Threat ID | Gap | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|-----|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G23 | CR-10 | Denial of Service | HIGH | `GeniusNode::StateTransition` | mitigate | Serialize transitions, suppress same-state re-entry, and gate posted trust-ready transitions by source state/epoch. | `PersistedHistoricalTrustAndTransactionsRestartWithSingleManagerOwnership` exact construction/start counts |
| T13-G24 | CR-10 | Tampering/Denial of Service | HIGH | TransactionManager callback ownership | mitigate | Stop/unregister/reset old manager and callback owners before constructing/registering the replacement; generation-check callbacks. | Same test owner-generation and functional callback assertions |
| T13-G25 | WR-07/BOOT-04 | Repudiation | HIGH | historical restart evidence | mitigate | Reopen identical persisted trust/transaction paths and retain fresh-client recovery as a distinct case. | Same historical test plus separate `MissingRegistryBlockIsFetchedFromPeerByCid` |
| T-13-SC | — | Tampering | LOW | package supply chain | accept | No package or dependency installation occurs. | Changed-file/build inspection |
</threat_model>

<verification>
Run the deterministic normal focused command and complete `multi_account_test`. Plan 13-26 also runs the historical and passive lifetime cases under any already-configured ASan/TSan build trees and always reruns the Release gate.
</verification>

<success_criteria>
- CR-10's duplicate-manager call chain is suppressed at both callback and transition boundaries.
- Manager replacement is stop-before-replace with generation-owned callbacks.
- WR-07 contains a true historical persisted trust/transaction restart rather than fresh storage.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Persisted trust and live transaction services restart safely with node-scoped policy ownership | 13-25 | COVERED | Historical same-path restart reaches READY with one manager. |
| REQ | BOOT-04 | Verified persisted state remains stable restart authority | 13-25 | COVERED | Identical trust/transaction paths reopen without rebootstrap. |
| REQ | BURN-02 | Replacement manager consumes the node-scoped confirmed provider | 13-25 | COVERED | Provider/callback owner generation remains current after restart. |
| REQ | TEST-01 | Historical restart and account-lifetime behavior | 13-25 | COVERED | Exactly-one construction/start and functional callback assertions. |
| CONTEXT | D-14, D-16 | Persisted restart authority and node/account lifetime split | 13-25 | COVERED | Trust remains node-scoped; only manager ownership is initialized once/replaced safely. |
| RESEARCH | — | Restricted startup state machine and callback lifetime | 13-25 | COVERED | Serialized transitions, weak/generation callbacks, stop-before-replace. |
| REVIEW | CR-10, WR-07 | Duplicate manager startup and fresh-storage fixture substitution | 13-25 | COVERED | True historical restart directly exercises the reachable call chain. |

Other phase source items remain covered by Plans 13-01..13-24 and are mechanically rerun by Plan 13-26. The separate fresh-client registry case is retained but does not stand in for this historical path. No scoped source item is missing.

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-25-SUMMARY.md` when done.
</output>
