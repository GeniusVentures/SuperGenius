---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 08
type: execute
wave: 7
depends_on: [13-07]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - src/account/TransactionManager.hpp
  - src/account/TransactionManager.cpp
  - test/src/account/burnconfig_policy_e2e_test.cpp
  - test/src/account/CMakeLists.txt
  - test/src/multiaccount/policy_lifetime_multi_account_test.cpp
  - test/src/multiaccount/CMakeLists.txt
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/08-multisig-primitive/08-01-SUMMARY.md
  - .planning/phases/12-validatorregistry-migration/12-01-SUMMARY.md
autonomous: true
requirements: [BURN-01, BURN-02, BURN-03, TPR-01, TPR-02, SCRDT-04, TEST-01, MIG-05, MIG-06]
must_haves:
  truths:
    - "D-16: SelectAccount recreates only account-bound services; SecureCrdt, registry, store, TPR, BurnConfig, callbacks, policy hashes, and confirmed burn provider retain identity for the GlobalDB lifetime."
    - "D-13/D-16: every replacement TransactionManager consumes the same node-scoped confirmed burn provider and fails closed while it is not economically ready."
    - "D-11/D-15: an explicitly approved current-policy Burn successor persists before publication and changes actual PayEscrow output; a stale-policy or failed-commit candidate does not."
  artifacts:
    - path: "src/account/TransactionManager.cpp"
      provides: "Fail-closed PayEscrow consumption of node-scoped confirmed burn state"
    - path: "test/src/multiaccount/policy_lifetime_multi_account_test.cpp"
      provides: "Repeated account-switch object/callback identity and post-switch live-update proof"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Evidence-backed v1.1 requirement status and approved MIG-05 wording"
  key_links:
    - from: "src/account/GeniusNode.cpp"
      to: "src/account/TransactionManager.cpp"
      via: "account recreation injects retained BurnConfig confirmed provider"
      pattern: "TransactionManager::New.*burn|ConfirmedBurn"
    - from: "src/account/TransactionManager.cpp"
      to: "src/account/BurnConfig.hpp"
      via: "PayEscrow reads readiness/value without CRDT access"
      pattern: "IsEconomicallyReady|Get.*BasisPoints"
---

<objective>
Complete node/account lifetime integration, prove live quorum-approved economics across account selection, and reconcile v1.1 requirement metadata against the final executable evidence.

Purpose: Close the last production regression from the milestone audit and leave canonical planning documents truthful after the full Phase 13 gate.
Output: Node-scoped teardown boundary, retained burn provider, fail-closed PayEscrow, live economic and repeated multi-account tests, and evidence-backed requirement reconciliation.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VALIDATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split account teardown from node policy lifetime</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, src/account/TransactionManager.hpp, src/account/TransactionManager.cpp, test/src/multiaccount/policy_lifetime_multi_account_test.cpp</files>
  <read_first>
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - src/account/BurnConfig.hpp
    - src/crdt/globaldb/globaldb.hpp
    - test/src/account/account_management_test.cpp
    - test/src/multiaccount/multi_account_sync.cpp
  </read_first>
  <behavior>
    - SelectAccount changes account and TransactionManager identity but preserves exact addresses of registry, SecureCrdt, TrustStateStore, TPR, BurnConfig, confirmed provider, and callback/filter registration counts.
    - Full node/GlobalDB shutdown destroys/unregisters policy services exactly once in dependency-reverse order.
    - Replacement TransactionManager observes the retained confirmed value and a later live update; pre-ready manager returns a typed trust-not-ready error from PayEscrow.
  </behavior>
  <action>Write `policy_lifetime_multi_account_test` first. Split `ShutdownAccountBoundServices` from `ShutdownNodePolicyServices`; make `SelectAccount` invoke only the former and reserve `ResetQuorumMembers`/policy teardown for full GlobalDB/node shutdown. Retain `secure_crdt_`, its registry, `trust_state_store_`, `trusted_peer_registry_`, `burn_config_`, and registrations across selection. Inject BurnConfig's shared confirmed provider into every `TransactionManager::New`; remove per-manager append-only BurnConfig refresh registration if the provider makes it redundant. In `PayEscrow`, read the provider atomically with no CRDT access and return a typed `TRUST_POLICY_NOT_READY` before constructing/submitting economic state when not ready.</action>
  <verify><automated>cmake --build build/OSX/Release --target policy_lifetime_multi_account_test genius_node -j8 &amp;&amp; build/OSX/Release/test_bin/policy_lifetime_multi_account_test</automated></verify>
  <acceptance_criteria>
    - Test captures all policy object addresses and registration counts before/after two account switches and asserts equality; TransactionManager/account identities differ.
    - Post-switch confirmed burn update reaches replacement manager and pre-ready PayEscrow returns `TRUST_POLICY_NOT_READY` with no persisted transaction.
    - Full teardown produces no expired callback, duplicate-registration, use-after-free, or double-unregister failure.
    - `rg -n "ResetQuorumMembers" src/account/GeniusNode.cpp` shows no call on the SelectAccount/account-only teardown path.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Policy services and caches are node-scoped, account selection is consumer-only recreation, and economic access fails closed until confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove live quorum-approved burn changes actual PayEscrow</name>
  <files>src/account/TransactionManager.cpp, test/src/account/burnconfig_policy_e2e_test.cpp, test/src/account/CMakeLists.txt, test/src/multiaccount/policy_lifetime_multi_account_test.cpp, test/src/multiaccount/CMakeLists.txt</files>
  <read_first>
    - src/account/TransactionManager.cpp
    - src/account/BurnConfig.hpp
    - test/src/account/burnconfig_test.cpp
    - test/src/account/account_management_test.cpp
    - test/src/multiaccount/CMakeLists.txt
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VALIDATION.md
  </read_first>
  <behavior>
    - With confirmed v1=100, PayEscrow burns exactly 1 percent; an explicit current-policy quorum successor changes the actual burn output to the new basis points.
    - Under-signed, stale-policy, wrong predecessor, outsider, and failed-store candidates leave PayEscrow output unchanged.
    - The same live change reaches a TransactionManager created after SelectAccount.
  </behavior>
  <action>Extend `burnconfig_policy_e2e_test` from Plan 05 through real TransactionManager/PayEscrow construction and observable payout/burn outputs; do not assert only a static getter. Extend the multi-account test with a post-switch explicit candidate approval/quorum/activation and a second PayEscrow. Register both targets using existing account/multiaccount link conventions. Use wait-condition utilities for callbacks/readiness and deterministic transaction fixtures; no sleeps.</action>
  <verify><automated>cmake --build build/OSX/Release --target burnconfig_policy_e2e_test policy_lifetime_multi_account_test -j8 &amp;&amp; build/OSX/Release/test_bin/burnconfig_policy_e2e_test &amp;&amp; build/OSX/Release/test_bin/policy_lifetime_multi_account_test</automated></verify>
  <acceptance_criteria>
    - Tests compare exact PayEscrow burn amounts before and after a confirmed successor, including after SelectAccount.
    - All invalid/stale/failure cases assert unchanged durable head, cache value, and payout/burn result.
    - Test bodies contain a real `PayEscrow` call and no static-default-only assertion, fixed sleep, skip, or disabled case.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Quorum-approved BurnConfig state drives real economics before and after account selection, while rejected candidates have no effect.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete security gate and reconcile milestone metadata</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/08-multisig-primitive/08-01-SUMMARY.md, .planning/phases/12-validatorregistry-migration/12-01-SUMMARY.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/v1.1-MILESTONE-AUDIT.md
    - .planning/phases/08-multisig-primitive/08-VERIFICATION.md
    - .planning/phases/08-multisig-primitive/08-01-SUMMARY.md
    - .planning/phases/12-validatorregistry-migration/12-CONTEXT.md
    - .planning/phases/12-validatorregistry-migration/12-01-SUMMARY.md
  </read_first>
  <action>Run the full Phase 13 regex and five consecutive `policy_lifetime_multi_account_test` executions before changing statuses. If any required test or HIGH threat fails, leave requirements pending and report the blocker. When green, update PROJECT active items and REQUIREMENTS checkboxes/traceability from evidence, retain the approved narrowed MIG-05 wording (shared multisig genesis verification, not full ISignedCRDTData migration), add `requirements-completed: [MSIG-01, MSIG-02, MSIG-03]` to Phase 8's summary and `[MIG-05, MIG-06]` to Phase 12's summary with their existing warnings preserved, and record Phase 13 as the closure evidence for previously partial TPR/SCRDT/BURN rows. Do not claim the documented whole-disk rollback boundary is solved.</action>
  <verify><automated>ctest --test-dir build/OSX/Release --output-on-failure -R 'securecrdt|trustedpeer|burnconfig|account_management|node_startup|startup|multi_account' &amp;&amp; for i in 1 2 3 4 5; do build/OSX/Release/test_bin/policy_lifetime_multi_account_test || exit 1; done &amp;&amp; rg -n "MIG-05.*adjusted scope|BOOT-04.*Phase 13.*(Complete|Satisfied)|requirements-completed" .planning/REQUIREMENTS.md .planning/phases/08-multisig-primitive/08-01-SUMMARY.md .planning/phases/12-validatorregistry-migration/12-01-SUMMARY.md</automated></verify>
  <acceptance_criteria>
    - Full phase regex passes and policy lifetime passes five consecutive runs.
    - Every T13 HIGH threat is mitigated by an implemented control and passing named test; accepted host rollback limitation remains explicit.
    - REQUIREMENTS/PROJECT statuses and both summary metadata blocks match executed evidence and no retired broad MIG-05 claim remains.
    - No bridge timing todo, remote admin RPC/pubsub, consensus proposal protocol, or broader ValidatorRegistry migration is introduced.
  </acceptance_criteria>
  <done>Phase 13 passes its full/repeated security gate and canonical milestone metadata truthfully reflects the implemented, adjusted v1.1 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Account selection to node policy services | Account lifecycle must not destroy network trust ownership/callbacks. |
| Confirmed burn cache to PayEscrow | Economic output must consume only durable-ready current policy state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|-----------|-------------|-----------------|------------------------|
| T13-09 | Elevation/Tampering | BurnConfig to PayEscrow | mitigate | Current-policy binding, durable activation, shared confirmed provider, stale/failure no-op. | live PayEscrow negative/positive cases |
| T13-10 | Denial of Service | SelectAccount callbacks | mitigate | Retain policy services/registrations; recreate consumers only; full teardown once. | repeated multi-account identity/update test |
| T13-11 | Tampering | cache publication | mitigate | TransactionManager reads provider published only after verified store commit. | failed-commit unchanged-output test |
| T-13-SC | Tampering | package supply chain | accept | No external package install. | build graph |
</threat_model>

<verification>
Run the full Phase 13 CTest regex, five repeated policy-lifetime runs, and confirm no HIGH threat remains unmitigated before updating canonical statuses.
</verification>

<success_criteria>
- Account selection preserves policy objects, registrations, durable heads, and live update delivery.
- Real PayEscrow output changes only after current-policy quorum and durable BurnConfig activation.
- Full/repeated automated gates pass without skips/flakes accepted as success.
- v1.1 requirement wording/status/summary metadata matches actual verified scope.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-08-SUMMARY.md` when done.
</output>
