---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 23
type: execute
wave: 1
depends_on: []
files_modified:
  - src/account/TrustStartupController.hpp
  - src/account/TrustStartupController.cpp
  - test/src/multiaccount/policy_lifetime_multi_account_test.cpp
autonomous: true
gap_closure: true
requirements: [BURN-03, SCRDT-04, TEST-01]
must_haves:
  truths:
    - "D-09/D-11: after burn v1 is ready, a successor proposed by A and explicitly approved by B is verified and durably activated by passive C without C signing, constructing LocalTrustAdmin, or calling a direct activation API."
    - "CR-08: ready-state publication happens only after every retained/callback burn successor eligible for the current durable predecessor has been discovered and attempted in deterministic order."
    - "WR-07: passive C's node-scoped confirmed provider advances to burn v2 and a real PayEscrow call changes output after account/TransactionManager lifetime transitions; failed, stale, and below-quorum candidates do not change C."
  artifacts:
    - path: "src/account/TrustStartupController.cpp"
      provides: "Passive burn-successor discovery and activation before the ready return"
    - path: "test/src/multiaccount/policy_lifetime_multi_account_test.cpp"
      provides: "Three-peer passive burn lifetime and real PayEscrow counterexample"
  key_links:
    - from: "TrustStartupController::Refresh"
      to: "BurnConfig::ListPendingBurnCandidates"
      via: "every refresh merges authoritative retained candidates before readiness can return"
      pattern: "ListPendingBurnCandidates"
    - from: "TrustStartupController::Refresh"
      to: "BurnConfig::TryActivateBurnCandidate"
      via: "sorted, deduplicated passive activation without a successor signing call"
      pattern: "TryActivateBurnCandidate"
    - from: "test/src/multiaccount/policy_lifetime_multi_account_test.cpp"
      to: "TransactionManager::PayEscrow"
      via: "passive C consumes the durable node-scoped burn-v2 provider"
      pattern: "PayEscrow"
---

<objective>
Close CR-08 and the passive half of WR-07 by making burn-ready controllers process quorum-approved successors before returning readiness.

Purpose: Prevent receiving nodes from remaining indefinitely on an older economic policy while preserving explicit successor approval and node-scoped PayEscrow behavior.
Output: Correctly ordered passive burn activation and a three-peer lifetime regression where only A and B make administrative decisions and C's real economic output changes.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-20-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-21-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-22-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate passive burn successors before publishing ready</name>
  <files>src/account/TrustStartupController.hpp, src/account/TrustStartupController.cpp, test/src/multiaccount/policy_lifetime_multi_account_test.cpp</files>
  <read_first>
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - src/account/BurnConfig.hpp
    - src/account/BurnConfig.cpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/genesis_tool/LocalTrustAdmin.hpp
    - src/trustedpeer/genesis_tool/LocalTrustAdmin.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - test/src/multiaccount/policy_lifetime_multi_account_test.cpp
    - test/src/startup/trust_first_boot_e2e_test.cpp
    - test/src/multiaccount/CMakeLists.txt
  </read_first>
  <behavior>
    - `PolicyLifetimeMultiAccountTest.PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin` brings A, B, and passive C to the same durable policy/burn-v1 authority over the production CRDT topic, then performs exactly one `operator_a_admin.ProposeBurn(250)` and one `operator_b_admin.Approve(...)`.
    - Between exact `PASSIVE_BURN_NO_RECEIVER_ACTION_BEGIN` / `PASSIVE_BURN_NO_RECEIVER_ACTION_END` comments, the test contains no C `LocalTrustAdmin`, `ProposeBurnCandidate`, `ApproveBurnCandidate`, `SubmitCandidateApproval`, `TryActivateBurnCandidate`, `OnTrustedPeerGenesisConfirmed`, raw GlobalDB write, or callback injection; C's approval count remains zero.
    - C independently commits burn v2, its verified store and confirmed provider both report 250, and real C PayEscrow output changes from the burn-v1 result to the exact burn-v2 result after C's account/TransactionManager lifetime transition.
    - An authenticated one-approval successor stays pending on C; a stale predecessor and an injected C commit failure preserve C's durable burn head, provider, and PayEscrow output and emit the existing typed activation failure for the actionable failure.
  </behavior>
  <action>RED: replace the local same-node burn proposal section of the lifetime fixture with the named three-peer passive counterexample while retaining its existing account-switch and provider-identity assertions. Build A, B, and C through production `GeniusNode`/GlobalDB trust startup, record C's burn-v1 durable head/provider/PayEscrow result after a TransactionManager replacement, name the only successor decision handles `operator_a_admin` and `operator_b_admin`, let A propose burn v2 and B approve exactly once, then place the exact comments `PASSIVE_BURN_NO_RECEIVER_ACTION_BEGIN` immediately after B's approval and `PASSIVE_BURN_NO_RECEIVER_ACTION_END` immediately after C's durable/provider/PayEscrow convergence. Observe C only through its store/provider/manager inside that marked window. Add below-quorum, stale-predecessor, and injected C commit-failure sections that prove no economic publication. Commit the failing test as `test(13-23): add passive burn lifetime counterexample`. GREEN: restructure `TrustStartupController::Refresh` so the automatic signing block remains guarded by `BootstrapOnly` plus verified local current membership per D-09/D-11, but authoritative `ListPendingBurnCandidates()` discovery and `TryActivateBurnCandidate()` processing run for every verified snapshot, including `PeerQuorum`/economically-ready snapshots. Remove the one-shot `burn_candidates_discovered_` behavior or replace it with predecessor-scoped discovery that necessarily changes when the durable burn head changes; merge callback IDs with listed IDs, exclude in-controller failed IDs, sort by version then content hash, deduplicate, and attempt activation before any `ConfirmedReady` return. Preserve `false` as quiet authenticated pending, return and emit typed actionable errors, reload after `true`, and never call `OnTrustedPeerGenesisConfirmed` for burn v2 or later. Publish `ConfirmedReady` only after this pass. Commit as `fix(13-23): process passive burn successors before readiness`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target policy_lifetime_multi_account_test -j8 &amp;&amp; build/OSX/Release/test_bin/policy_lifetime_multi_account_test --gtest_filter='PolicyLifetimeMultiAccountTest.PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin'</automated>
  </verify>
  <acceptance_criteria>
    - The exact named test executes once and exits 0 with A and B as the only successor approval sources.
    - C's durable burn version/hash/value, confirmed provider, and real PayEscrow output all advance only after the two replicated approvals; the exact marked no-receiver-action window contains no C signature, admin/direct activation, raw write, or callback nudge.
    - Below-quorum, stale, and failed-commit sections leave C's durable head/provider/PayEscrow result unchanged; failed commit carries candidate identity and typed error.
    - `TrustStartupController::Refresh` contains no economically-ready return before authoritative burn listing and activation.
    - The automated command exits 0 with no skip or disabled counterpart.
  </acceptance_criteria>
  <done>Burn-ready passive nodes durably converge on explicit quorum-approved successors and consume them through real node-scoped economics without automatic successor signing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Replicated burn approvals to passive durable economic authority | C may verify and commit an exact quorum-approved successor but must never sign or administratively approve it. |
| Durable burn head to C's PayEscrow provider | Economic publication must follow verified synchronous commit and must not remain stale after a valid successor. |

## STRIDE Threat Register

| Threat ID | Gap | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|-----|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G17 | CR-08 | Tampering/Elevation of Privilege | HIGH | passive burn successor activation | mitigate | Discover and verify current-predecessor candidates before readiness; keep signing limited to deterministic BootstrapOnly burn v1. | `PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin` approval/signature assertions |
| T13-G18 | CR-08/WR-07 | Denial of Service/Policy divergence | HIGH | `TrustStartupController::Refresh` and confirmed provider | mitigate | Process retained/callback burn successors on every relevant refresh, persist before publish, and prove C's PayEscrow changes. | Same named test durable-head/provider/PayEscrow assertions |
| T13-G19 | WR-07 | Repudiation | HIGH | passive lifetime evidence | mitigate | Enforce two named operator decisions, zero receiver admin/direct activation, and negative stale/failure controls. | Same named test plus Plan 13-26 structural guard |
| T-13-SC | — | Tampering | LOW | package supply chain | accept | No package, dependency, topic, endpoint, or RPC is added. | Changed-file/build-graph inspection |
</threat_model>

<verification>
Run the focused counterexample, then the complete `policy_lifetime_multi_account_test` once. Plan 13-26 performs five additional focused passive-lifetime repetitions and the exact broad gate.
</verification>

<success_criteria>
- CR-08 is unreachable because passive burn discovery/activation precedes the ready return.
- D-09/D-11 remain exact: A proposes, B approves, and C never signs or administers the successor.
- WR-07 has real passive lifetime and PayEscrow evidence rather than a same-node LocalTrustAdmin path.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Live quorum-approved burn behavior converges across running nodes and account lifetime | 13-23 | COVERED | Passive C durably commits and consumes burn v2. |
| REQ | BURN-03 | Default 100 remains until quorum successor changes real PayEscrow | 13-23 | COVERED | Exact pre/post PayEscrow outputs are asserted. |
| REQ | SCRDT-04 | Proposal/signature/quorum transport remains CRDT-only | 13-23 | COVERED | A/B use existing candidate approvals; no new transport. |
| REQ | TEST-01 | Live PayEscrow and account-lifetime behavior | 13-23 | COVERED | Corrected passive lifetime case replaces local-proposal substitution. |
| CONTEXT | D-09, D-11, D-16 | Receive never signs, approval is explicit, provider/services are node-scoped | 13-23 | COVERED | Exactly A proposes/B approves; C remains passive across manager lifetime. |
| RESEARCH | — | Persist before publish and node-scoped confirmed provider | 13-23 | COVERED | C's store advances before provider/PayEscrow observation. |
| REVIEW | CR-08, WR-07 | Ready-return burn divergence and passive-lifetime fixture bypass | 13-23 | COVERED | Production order and three-peer fixture directly cover both findings. |

Other GOAL/REQ/CONTEXT/RESEARCH items remain covered by Plans 13-01..13-22 and are rerun by Plan 13-26; this plan changes no unrelated authority, schema, transport, or rollback boundary. No scoped source item is missing.

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-23-SUMMARY.md` when done.
</output>
