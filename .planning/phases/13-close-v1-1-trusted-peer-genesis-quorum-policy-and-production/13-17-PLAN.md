---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - src/securecrdt/SecureCrdt.hpp
  - src/securecrdt/SecureCrdt.cpp
  - test/src/securecrdt/securecrdt_quorum_gate_test.cpp
  - test/src/securecrdt/securecrdt_quorum_fixture.hpp
  - test/src/securecrdt/securecrdt_candidate_test.cpp
  - test/src/securecrdt/securecrdt_candidate_race_test.cpp
autonomous: true
gap_closure: true
requirements: [TEST-01]
must_haves:
  truths:
    - "D-10: legacy SecureCrdt signature children are retained only for canonical addresses in the current signer-set snapshot."
    - "A cryptographically valid outsider signature is rejected before local Put and before remote filter acceptance, so it is neither persisted nor replicated."
    - "Retained legacy signature children are pruned/bounded to the current authorized signer set and cannot grow with attacker-controlled keypairs."
    - "Partially initialized SecureCrdt test fixtures tear down safely and preserve the original setup failure (bounded WR-04 repair)."
  artifacts:
    - path: "src/securecrdt/SecureCrdt.cpp"
      provides: "Shared canonical-membership and retained-child gate for local, remote, and read paths"
    - path: "test/src/securecrdt/securecrdt_quorum_gate_test.cpp"
      provides: "Local/remote valid-outsider rejection and authorized-set retention bound"
  key_links:
    - from: "SecureCrdt::AddSignature"
      to: "SecureCrdtRegistryEntry::signer_set_source"
      via: "resolve one current signer snapshot before signature verification and Put"
      pattern: "signer_set_source"
    - from: "SecureCrdt::FilterSecureCrdtUpdate"
      to: "base::IsHexAddress"
      via: "exact canonical lower-hex address and membership validation before filter acceptance"
      pattern: "IsHexAddress"
---

<objective>
Close CR-04 by applying current signer membership, canonical-key validation, and an authorized-set retention bound to the legacy SecureCrdt signature path.

Purpose: Prevent valid attacker keypairs from consuming unbounded replicated storage while preserving CRDT-only propose/sign/quorum behavior.
Output: Shared legacy signature ingress validation, bounded cleanup, local/remote regressions, and safe fixture teardown.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-03-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject and prune unauthorized legacy signature children</name>
  <files>src/securecrdt/SecureCrdt.hpp, src/securecrdt/SecureCrdt.cpp, test/src/securecrdt/securecrdt_quorum_gate_test.cpp</files>
  <read_first>
    - src/securecrdt/SecureCrdt.hpp
    - src/securecrdt/SecureCrdt.cpp
    - src/securecrdt/SecureCrdtRegistry.hpp
    - src/trustedpeer/CanonicalTrustCodec.hpp
    - src/base/hexutil.hpp
    - src/multisig/MultiSig.cpp
    - src/crdt/globaldb/globaldb.hpp
    - test/src/securecrdt/securecrdt_quorum_gate_test.cpp
  </read_first>
  <behavior>
    - AddSignature rejects malformed/mixed-case/noncanonical addresses and valid signatures from addresses absent from the current signer set before db_->Put.
    - FilterSecureCrdtUpdate accepts only an exact `base/sig/<canonical-address>` key whose address is currently authorized and whose signature verifies against the current base payload.
    - Repeated local and remote attempts from many valid outsider keypairs leave QueryKeyValues(base/sig) unchanged and never appear on a receiving peer.
    - Existing malformed/outsider/stale children are removed on the next local add or quorum read; authorized retained children are unique by address and never exceed the current authorized signer-set size.
  </behavior>
  <action>RED: create the failing cases `LocalOutsiderSignatureNeverPersists` and `RemoteOutsiderSignatureNeverReplicatesAndRetentionBoundTracksAuthorizedSet`; use a real outsider GeniusSigner and an adversarial raw GlobalDB Put only for the remote-originated test. Commit as `test(13-17): add legacy outsider signature counterexamples`. GREEN: add typed `UNAUTHORIZED_SIGNER` and `SIGNATURE_LIMIT_EXCEEDED` errors. Factor one private helper that resolves `entry.signer_set_source(base_key.GetKey())`, validates the snapshot is non-empty/unique/canonical and at most `CanonicalTrustCodec::MAX_TRUSTED_PEERS`, validates the claimed address with `base::IsHexAddress`, and tests exact membership. Call it from AddSignature before cryptographic verification/Put and from FilterSecureCrdtUpdate before accepting the element. Require the remote element key to equal exactly `base_key.ChildString("sig").ChildString(address)` so extra path segments or alternate spellings cannot bind a signer. Query retained children and remove malformed/noncanonical/non-current children through `GlobalDB::Remove`; perform the same pruning before ReadIfQuorum collects signatures. Count unique authorized child addresses, allow replacement of the same address, and reject a new child at the authorized-set-size bound. Never count outsider children toward quorum and never add a new RPC/topic. Commit as `fix(13-17): authorize and bound legacy signature children`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target securecrdt_quorum_gate_test -j8 &amp;&amp; build/OSX/Release/test_bin/securecrdt_quorum_gate_test --gtest_filter='*OutsiderSignatureNever*:*RetentionBoundTracksAuthorizedSet*:*UnderSignedWriteNeverReportsQuorum*'</automated>
  </verify>
  <acceptance_criteria>
    - Local outsider AddSignature returns `UNAUTHORIZED_SIGNER` and the outsider child key is absent.
    - A remote valid outsider child is rejected by the receiving filter and is absent after propagation settles.
    - The retained query contains only current canonical members, has size at most the signer-set size after signer-set shrink/pruning, and authorized quorum behavior remains green.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Legacy signature storage is current-member authenticated and bounded before persistence on both ingress paths.</done>
</task>

<task type="auto">
  <name>Task 2: Make directly exercised SecureCrdt fixtures safe after partial setup</name>
  <files>test/src/securecrdt/securecrdt_quorum_gate_test.cpp, test/src/securecrdt/securecrdt_quorum_fixture.hpp, test/src/securecrdt/securecrdt_candidate_test.cpp, test/src/securecrdt/securecrdt_candidate_race_test.cpp</files>
  <read_first>
    - test/src/securecrdt/securecrdt_quorum_gate_test.cpp
    - test/src/securecrdt/securecrdt_quorum_fixture.hpp
    - test/src/securecrdt/securecrdt_candidate_test.cpp
    - test/src/securecrdt/securecrdt_candidate_race_test.cpp
  </read_first>
  <action>Apply the bounded WR-04 repair to each reviewed fixture: guard every TearDown dereference/unregister with the owning pointer, reset only constructed resources, and keep storage-factory/path cleanup idempotent. Do not change setup assertions or convert listener/setup failures into skips. Add or use the existing partial-initialization seam to invoke TearDown after `node_`/`secure_crdt_` creation fails and prove the original assertion is not replaced by a null-dereference crash. Commit as `test(13-17): make securecrdt fixture teardown partial-safe`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target securecrdt_quorum_gate_test securecrdt_candidate_test securecrdt_candidate_race_test -j8 &amp;&amp; ctest --test-dir build/OSX/Release --output-on-failure -R 'securecrdt_quorum_gate_test|securecrdt_candidate(_race)?_test'</automated>
  </verify>
  <acceptance_criteria>
    - Every affected TearDown checks `secure_crdt_`/`node_` before dereference.
    - Partial initialization exits without SIGSEGV and retains the original setup failure signal.
    - The three named targets pass in the normal listener-capable environment.
  </acceptance_criteria>
  <done>Blocker regressions cannot be masked by unsafe teardown after environment or setup failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local signer input to replicated legacy child | A valid signature is insufficient unless the signer is a canonical current member. |
| Remote CRDT element to local retained state | Attacker-controlled keys/values must be rejected before retention. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G08 | Spoofing/Elevation of Privilege | HIGH | legacy `AddSignature` and remote filter | mitigate | Exact canonical address, current signer snapshot membership, exact key binding, then cryptographic verification. | `securecrdt_quorum_gate_test --gtest_filter='*OutsiderSignatureNever*'` |
| T13-G09 | Denial of Service | HIGH | replicated signature children | mitigate | Prune unauthorized children and cap unique retained addresses at current signer-set size. | `securecrdt_quorum_gate_test --gtest_filter='*RetentionBoundTracksAuthorizedSet*'` |
| T-13-SC | Tampering | LOW | package supply chain | accept | Existing repository libraries only; no install. | Build graph inspection |
</threat_model>

<verification>
Run both task commands, then `ctest --test-dir build/OSX/Release --output-on-failure -R 'securecrdt'`.
</verification>

<success_criteria>
- CR-04 outsiders are absent locally and remotely even with valid signatures.
- Retained legacy signer children are bounded to current authorized identities.
- Existing quorum/candidate behavior remains green and setup failures no longer crash teardown.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-17-SUMMARY.md` when done.
</output>
