---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - src/account/TransactionManager.cpp
  - test/src/account/burnconfig_policy_e2e_test.cpp
autonomous: true
gap_closure: true
requirements: [BURN-02, BURN-03, TEST-01]
must_haves:
  truths:
    - "D-11/D-16: PayEscrow computes floor(amount*basis_points/10000) exactly across the complete uint64_t escrow domain."
    - "UINT64_MAX burns exactly 1,844,674,407,370,955 at 1 bp, 184,467,440,737,095,516 at 100 bp, and UINT64_MAX at 10,000 bp."
    - "An out-of-range basis-point input fails before transaction/output persistence; valid-domain arithmetic has no overflow failure path."
  artifacts:
    - path: "src/account/TransactionManager.cpp"
      provides: "Overflow-safe exact PayEscrow burn calculation"
    - path: "test/src/account/burnconfig_policy_e2e_test.cpp"
      provides: "UINT64_MAX live PayEscrow boundary and no-persistence regression"
  key_links:
    - from: "TransactionManager::PayEscrow"
      to: "ConfirmedBurnValueProvider"
      via: "validated live basis points feed quotient/remainder arithmetic"
      pattern: "BASIS_POINTS_TOTAL|quotient|remainder"
---

<objective>
Close CR-03 with exact overflow-safe PayEscrow arithmetic and full-domain economic-output tests.

Purpose: Prevent wrapped multiplication from under-burning large escrows while retaining the quorum-confirmed provider path.
Output: Checked basis-point handling, quotient/remainder arithmetic, and exact UINT64_MAX regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make PayEscrow percentage arithmetic exact for uint64_t maximum</name>
  <files>src/account/TransactionManager.cpp, test/src/account/burnconfig_policy_e2e_test.cpp</files>
  <read_first>
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - src/account/BurnConfig.hpp
    - src/account/TransferTransaction.hpp
    - test/src/account/burnconfig_policy_e2e_test.cpp
  </read_first>
  <behavior>
    - PayEscrow(UINT64_MAX, 1 bp) writes a zero-address output of 1844674407370955.
    - PayEscrow(UINT64_MAX, 100 bp) writes a zero-address output of 184467440737095516.
    - PayEscrow(UINT64_MAX, 10000 bp) writes a zero-address output of 18446744073709551615 and distributes zero available value without wraparound.
    - A basis-point value above 10000 returns invalid_argument before fetching/persisting payout state and leaves transaction/output counts unchanged.
  </behavior>
  <action>RED: add `PayEscrowUsesExactOverflowSafeBurnForUint64Maximum` with the three exact output constants and an invalid-basis/no-persistence case; commit as `test(13-16): add maximum escrow arithmetic counterexample`. GREEN: after reading the confirmed or fallback basis points, reject values greater than `BASIS_POINTS_TOTAL` before escrow lookup or any transaction mutation. Replace `(escrow_amount * burn_basis_points) / BASIS_POINTS_TOTAL` with the exact decomposition `quotient = escrow_amount / BASIS_POINTS_TOTAL`, `remainder = escrow_amount % BASIS_POINTS_TOTAL`, and `quotient * burn_basis_points + (remainder * burn_basis_points) / BASIS_POINTS_TOTAL`; the validated bounds make both products and their sum fit uint64_t while preserving mathematical floor semantics. Keep burn subtraction, peer/dev distribution, and confirmed-provider wiring unchanged. Commit as `fix(13-16): use exact overflow-safe escrow burn arithmetic`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target burnconfig_policy_e2e_test -j8 &amp;&amp; build/OSX/Release/test_bin/burnconfig_policy_e2e_test --gtest_filter='*PayEscrowUsesExactOverflowSafeBurnForUint64Maximum*:*PersistBeforeCache*'</automated>
  </verify>
  <acceptance_criteria>
    - All three UINT64_MAX expected outputs match exactly, including 100% burn.
    - The invalid basis-point case returns before payout persistence and proves unchanged counts.
    - `rg -n "escrow_amount \* burn_basis_points" src/account/TransactionManager.cpp` returns no match.
    - The focused and existing persist-before-cache cases exit 0.
  </acceptance_criteria>
  <done>PayEscrow produces exact burns for every valid uint64_t amount/basis-point pair and fails before mutation on invalid policy input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Confirmed burn policy and escrow amount to financial outputs | Valid maximum-width inputs must not wrap before output construction. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G06 | Tampering | HIGH | `TransactionManager::PayEscrow` accounting | mitigate | Validate basis-point range and use exact quotient/remainder arithmetic with bounded intermediates. | `burnconfig_policy_e2e_test --gtest_filter='*PayEscrowUsesExactOverflowSafeBurnForUint64Maximum*'` |
| T13-G07 | Repudiation | HIGH | invalid arithmetic input | mitigate | Return before escrow fetch/output persistence and assert unchanged transaction counts. | Same test's invalid-basis case |
| T-13-SC | Tampering | LOW | package supply chain | accept | No package install; standard uint64_t arithmetic only. | Changed-file inspection |
</threat_model>

<verification>
Run the focused test and then the complete `burnconfig_policy_e2e_test` binary.
</verification>

<success_criteria>
- CR-03 exact outputs pass at 1, 100, and 10,000 basis points.
- No uint64_t multiplication can overflow in the chosen arithmetic domain.
- Invalid policy input cannot produce partial payout persistence.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-16-SUMMARY.md` when done.
</output>
