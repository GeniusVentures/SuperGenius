---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 19
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trustedpeer/TrustStateStore.hpp
  - src/trustedpeer/TrustStateStore.cpp
  - test/src/trustedpeer/trust_state_store_test.cpp
autonomous: true
gap_closure: true
requirements: [BOOT-04, TEST-01]
must_haves:
  truths:
    - "D-02/D-14/D-15: every public LoadAndVerify call verifies genesis, policy history/head, and burn history/head from one coherent transition view."
    - "A reader racing valid policy and burn transitions returns the complete old snapshot or a subsequently complete new snapshot, never INVALID_BURN_PROOF, TRUST_LOCAL_STATE_CORRUPT, or another false corruption result."
    - "CommitGenesis, CommitPolicySuccessor, and both CommitBurnSuccessor branches retain their synchronous persist-before-publish behavior without self-deadlocking when verification acquires the public transition lock."
  artifacts:
    - path: "src/trustedpeer/TrustStateStore.hpp"
      provides: "Public locked reader, internal lock-aware verification contract, and deterministic test observation seam"
    - path: "src/trustedpeer/TrustStateStore.cpp"
      provides: "One-transition-view verification and commit-path use of the unlocked helper"
    - path: "test/src/trustedpeer/trust_state_store_test.cpp"
      provides: "Barrier-driven torn-read and commit self-deadlock regressions"
  key_links:
    - from: "TrustStateStore::LoadAndVerify"
      to: "TrustStateStore::LoadAndVerifyUnlocked"
      via: "the public method holds transition_mutex_ for the complete multi-record verification"
      pattern: "lock.*transition_mutex_|LoadAndVerifyUnlocked"
    - from: "TrustStateStore::CommitPolicySuccessor and CommitBurnSuccessor"
      to: "TrustStateStore::LoadAndVerifyUnlocked"
      via: "commit methods already own transition_mutex_ and must not re-enter the public locked method"
      pattern: "LoadAndVerifyUnlocked"
---

<objective>
Close CR-07 by making verified trust-state reads coherent with durable transitions while preserving non-reentrant commit behavior.

Purpose: Prevent a normal concurrent policy/burn advancement from being misclassified as local corruption or taking the node to FatalMismatch.
Output: A locked public read, an internal lock-aware helper used by commit paths, and deterministic old-or-new snapshot/deadlock regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-13-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize complete trust verification with valid policy and burn transitions</name>
  <files>src/trustedpeer/TrustStateStore.hpp, src/trustedpeer/TrustStateStore.cpp, test/src/trustedpeer/trust_state_store_test.cpp</files>
  <read_first>
    - src/trustedpeer/TrustStateStore.hpp
    - src/trustedpeer/TrustStateStore.cpp
    - test/src/trustedpeer/trust_state_store_test.cpp
    - src/storage/rocksdb/rocksdb.hpp
    - src/storage/rocksdb/rocksdb.cpp
    - src/storage/rocksdb/rocksdb_batch.hpp
    - src/storage/rocksdb/rocksdb_batch.cpp
    - src/trustedpeer/QuorumPolicy.hpp
    - src/securecrdt/SecureCrdtCandidate.hpp
  </read_first>
  <behavior>
    - `LoadAndVerifySerializesPolicyAndBurnTransitionWithoutTornSnapshot` pauses a reader after it has verified the policy history but before it reads the burn head, starts a writer that commits a valid policy successor followed by its valid burn successor, and proves the writer cannot cross the transition boundary until the reader completes.
    - The paused reader returns the complete pre-transition snapshot; after the writer completes, a new reader returns the complete policy-v2/burn-v2 snapshot. Neither read returns `INVALID_BURN_PROOF`, another corruption error, nor a mixed head.
    - `CommitPathsUseUnlockedVerifierWithoutSelfDeadlock` executes genesis commit, same-version burn-v1 peer-proof replacement, policy successor commit, and burn successor commit under bounded futures; every call completes before the timeout and reopens to the expected verified snapshot.
    - Existing corrupt, fork, stale-head, commit-failure, bootstrap-only, and peer-quorum classifications remain unchanged.
  </behavior>
  <action>RED: add the two named tests. Introduce a narrowly scoped optional `LoadObserver` test seam on `TrustStateStore::Open`, with a named stage fired after the complete policy history/proofs are verified and immediately before the burn head is read; use condition variables, not sleeps, so the regression deterministically recreates CR-07's old-policy/new-burn interleaving. The test must first fail by allowing the writer to reach its commit while the reader is paused or by returning a mixed-view error. Add bounded `std::future::wait_for` assertions around all commit entry points to expose self-deadlock. Commit RED as `test(13-19): add coherent trust read counterexamples`. GREEN: extract the current body into private `LoadAndVerifyUnlocked() const`. Make public `LoadAndVerify() const` acquire `transition_mutex_` for the full helper call. Every method that already holds `transition_mutex_` (`CommitGenesis`, `CommitPolicySuccessor`, both same-version and advancing branches of `CommitBurnSuccessor`) must call `LoadAndVerifyUnlocked()` for its precondition reload and post-commit verification; no commit path may call the public locking wrapper. Fire the optional observer only inside the helper at the documented stage, without releasing the transition lock and without exposing database mutation. Keep the existing synchronous RocksDB batch, whole-disk/all-anchor rollback boundary, error taxonomy, canonical proof checks, and CR-01 through CR-04 behavior intact. Commit GREEN as `fix(13-19): verify trust state from one transition view`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trust_state_store_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_state_store_test --gtest_filter='*LoadAndVerifySerializesPolicyAndBurnTransitionWithoutTornSnapshot*:*CommitPathsUseUnlockedVerifierWithoutSelfDeadlock*:*ConcurrentSuccessorsHaveOneWinnerAndOneStableStaleResult*'</automated>
  </verify>
  <acceptance_criteria>
    - The barrier test proves the writer remains blocked while the reader is paused and records exactly the complete old snapshot followed by the complete new snapshot.
    - No test accepts `INVALID_BURN_PROOF`, `TRUST_LOCAL_STATE_CORRUPT`, or any other error as an alternative successful result.
    - All four durable commit shapes finish within the bounded future timeout and the final reopened snapshot contains policy v2 and burn v2.
    - `rg -n "LoadAndVerifyUnlocked|transition_mutex_|PolicyHistory" src/trustedpeer/TrustStateStore.{hpp,cpp} test/src/trustedpeer/trust_state_store_test.cpp` finds the lock boundary, internal helper, and deterministic barrier stage.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>TrustStateStore readers and writers observe one serialized durable transition view, with no torn-read false corruption and no reentrant mutex deadlock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent durable transition to restart verification | Independently valid records must be evaluated from one coherent authority view. |
| Public reader to lock-owning commit path | The same lock enforces coherence but must not be recursively acquired. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G10 | Tampering/Denial of Service | HIGH | `TrustStateStore::LoadAndVerify` | mitigate | Hold `transition_mutex_` for the complete policy-and-burn read/verification sequence and prove deterministic old-or-new results. | `LoadAndVerifySerializesPolicyAndBurnTransitionWithoutTornSnapshot` |
| T13-G11 | Denial of Service | HIGH | lock-owning commit paths | mitigate | Route already-locked commits through `LoadAndVerifyUnlocked` and bound every commit shape with a timeout regression. | `CommitPathsUseUnlockedVerifierWithoutSelfDeadlock` |
| T-13-SC | Tampering | LOW | package supply chain | accept | No dependency or package change is permitted. | Changed-file and build-graph inspection |
</threat_model>

<verification>
Run the focused command, then run the complete `trust_state_store_test` binary to preserve all durable sequencing, corruption, restart, and race contracts.
</verification>

<success_criteria>
- CR-07's deterministic interleaving cannot produce a mixed trust snapshot or false corruption.
- Commit paths remain live and keep persist-before-publish verification.
- BOOT-04 and TEST-01 have direct automated coherent-read evidence.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-19-SUMMARY.md` when done.
</output>
