---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 09
type: execute
wave: 6
depends_on: [13-06]
files_modified:
  - src/trustedpeer/genesis_tool/GenesisCeremony.hpp
  - src/trustedpeer/genesis_tool/GenesisCeremony.cpp
  - src/trustedpeer/genesis_tool/LocalTrustAdmin.hpp
  - src/trustedpeer/genesis_tool/LocalTrustAdmin.cpp
  - src/trustedpeer/genesis_tool/main.cpp
  - src/trustedpeer/genesis_tool/CMakeLists.txt
  - src/trustedpeer/CMakeLists.txt
  - test/src/trustedpeer/trust_genesis_tool_test.cpp
  - test/src/trustedpeer/CMakeLists.txt
autonomous: true
requirements: [BOOT-01, BOOT-02, BOOT-03, SCRDT-04, TPR-01, TPR-02, BURN-01, TEST-01]
must_haves:
  truths:
    - "D-01/D-03/D-04: `sgns-trust genesis` validates and displays the canonical manifest, accepts a protected ephemeral key, requires typed fingerprint confirmation, submits through normal SecureCrdt, and removes the key only after durable confirmation."
    - "D-09/D-11: local list/propose/approve commands are explicit and never add a remote administration surface or receive-side signing."
  artifacts:
    - path: "src/trustedpeer/genesis_tool/GenesisCeremony.hpp"
      provides: "Reusable validation, review, ephemeral signing, submit, confirm, and cleanup service"
    - path: "src/trustedpeer/genesis_tool/main.cpp"
      provides: "Local-only sgns-trust command surface"
  key_links:
    - from: "src/trustedpeer/genesis_tool/GenesisCeremony.cpp"
      to: "src/trustedpeer/TrustedPeerRegistry.hpp"
      via: "normal candidate submission and durable confirmation"
      pattern: "SubmitReviewedGenesisApproval|TryActivate"
    - from: "src/trustedpeer/genesis_tool/main.cpp"
      to: "src/crdt/globaldb/GlobalDbNetworkComposition.hpp"
      via: "reusable production network composition"
      pattern: "GlobalDbNetworkComposition"
---

<objective>
Ship the one-shot genesis ceremony and explicit local policy administration command on the reusable production GlobalDB composition.

Purpose: Make D-01..D-04 and D-09..D-11 operable without storing the bootstrap key or adding a remote control plane.
Output: Genesis/admin services, `sgns-trust`, secret-lifecycle tests, and build wiring.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VALIDATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and prove the one-shot genesis ceremony</name>
  <files>src/trustedpeer/genesis_tool/GenesisCeremony.hpp, src/trustedpeer/genesis_tool/GenesisCeremony.cpp, src/trustedpeer/CMakeLists.txt, test/src/trustedpeer/trust_genesis_tool_test.cpp, test/src/trustedpeer/CMakeLists.txt</files>
  <read_first>
    - src/crdt/globaldb/GlobalDbNetworkComposition.hpp
    - src/trustedpeer/GenesisManifest.hpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/account/GeniusSigner.hpp
    - ProofSystem/src/EthereumKeyGenerator.cpp
    - src/account/GeniusAccount.cpp
    - test/src/trustedpeer/genesis_ceremony_helper.hpp
    - src/trustedpeer/CMakeLists.txt
    - test/src/trustedpeer/CMakeLists.txt
  </read_first>
  <behavior>
    - Genesis displays network/ordered peers/thresholds/value/fingerprint, requires exact typed confirmation, submits, waits for durable confirmation, then cleanses memory and unlinks a supplied key file.
    - Key material is rejected from argv/environment; file input requires owner, regular-file/no-symlink, and mode 0600 checks; failure retains the file with a critical instruction.
  </behavior>
  <action>Write `trust_genesis_tool_test` first and register `GenesisCeremony.cpp` plus the test target before invoking it. Implement injected input/output, signer, network composition, and confirmation-wait seams. Accept key material only through protected stdin or a 0600 owner regular file, import via `EthereumKeyGenerator(std::string_view)` and `GeniusSigner`, compare the derived bootstrapper, require the displayed fingerprint, submit `SubmitReviewedGenesisApproval`, wait for verified `TrustStateStore` confirmation, call `OPENSSL_cleanse`, and unlink only on success per D-03. Never call `GeniusAccount::NewFromPrivateKey` or put secret bytes in argv, environment, or logs.</action>
  <verify><automated>cmake --build build/OSX/Release --target trust_genesis_tool_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_genesis_tool_test</automated></verify>
  <done>Genesis ceremony logic proves canonical review, normal SecureCrdt submission, durable confirmation, and success-only secret cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire sgns-trust and explicit local administration</name>
  <files>src/trustedpeer/genesis_tool/LocalTrustAdmin.hpp, src/trustedpeer/genesis_tool/LocalTrustAdmin.cpp, src/trustedpeer/genesis_tool/main.cpp, src/trustedpeer/genesis_tool/CMakeLists.txt, src/trustedpeer/CMakeLists.txt, test/src/trustedpeer/trust_genesis_tool_test.cpp</files>
  <read_first>
    - src/trustedpeer/genesis_tool/GenesisCeremony.hpp
    - src/trustedpeer/genesis_tool/GenesisCeremony.cpp
    - src/trustedpeer/genesis_tool/main.cpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/account/BurnConfig.hpp
    - src/crdt/globaldb/GlobalDbNetworkComposition.hpp
    - example/crdt_globaldb/globaldb_app.cpp
    - src/trustedpeer/CMakeLists.txt
    - test/src/trustedpeer/trust_genesis_tool_test.cpp
  </read_first>
  <behavior>
    - `genesis`, `list`, `propose-policy`, `propose-burn`, and `approve` use explicit local invocation and the existing CRDT topic.
    - Candidate receipt/listing produces no signature; explicit propose contributes one approval and explicit approve adds only the selected signer once.
  </behavior>
  <action>Implement the thin `sgns-trust` executable and `LocalTrustAdmin`, wire the executable in both CMake files, and extend the existing test. `genesis` accepts manifest/network/database and key-file-or-stdin selectors but never secret bytes. Local administration lists metadata and calls the TPR/BurnConfig propose/approve APIs for exact candidate IDs. Do not create HTTP, JSON-RPC, new pubsub, admin-session, or automatic receive-signing code.</action>
  <verify><automated>cmake --build build/OSX/Release --target sgns-trust trust_genesis_tool_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_genesis_tool_test --gtest_filter='*Admin*:*Argv*:*Secret*'</automated></verify>
  <done>The local command performs genesis and explicit approvals through production CRDT without key persistence or a remote control plane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewed manifest/key input to local tool | Human-reviewed trust and ephemeral secret enter the process. |
| Local tool to production CRDT | Explicit local intent becomes an authenticated candidate approval. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-01 | Spoofing/Tampering | HIGH | genesis review | mitigate | Derived bootstrapper match, canonical displayed fingerprint/list, exact typed confirmation. | `trust_genesis_tool_test` tamper/confirmation cases |
| T13-02 | Information Disclosure | HIGH | ephemeral key | mitigate | stdin/0600 no-symlink file only, in-memory signer, no account storage/logging, cleanse, success-only unlink. | `trust_genesis_tool_test --gtest_filter='*Secret*:*Key*'` |
| T13-04 | Tampering | HIGH | admin approval | mitigate | Exact candidate ID/bytes and explicit command; receive path cannot sign. | `trust_genesis_tool_test --gtest_filter='*Admin*'` |
| T-13-SC | Tampering | LOW | package supply chain | accept | No new package; existing Boost/OpenSSL/CRDT targets. | CMake build graph inspection |
</threat_model>

<verification>
Run `ctest --test-dir build/OSX/Release --output-on-failure -R 'trust_genesis_tool|operator_approval|securecrdt_candidate'` and inspect `sgns-trust --help` for no secret-valued option.
</verification>

<success_criteria>
- Genesis key material never enters argv/environment/logs/account persistence and is removed only after durable confirmation.
- Later candidate operations require explicit local commands and add no remote API/protocol.
- The command uses `GlobalDbNetworkComposition` and the production SecureCrdt keyspace.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-09-SUMMARY.md` when done.
</output>
