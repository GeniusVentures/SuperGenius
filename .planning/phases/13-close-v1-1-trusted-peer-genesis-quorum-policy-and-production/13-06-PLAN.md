---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 06
type: execute
wave: 5
depends_on: [13-05]
files_modified:
  - src/crdt/globaldb/GlobalDbNetworkComposition.hpp
  - src/crdt/globaldb/GlobalDbNetworkComposition.cpp
  - src/crdt/globaldb/CMakeLists.txt
autonomous: true
requirements: [BOOT-03, SCRDT-04, TPR-01, TEST-01]
must_haves:
  truths:
    - "D-03/D-09: local trust tools can join the exact production GlobalDB/CRDT transport without constructing a GeniusNode or introducing another topic."
    - "The reusable composition owns and stops its networking, scheduler, graphsync, and GlobalDB resources in dependency-safe order."
  artifacts:
    - path: "src/crdt/globaldb/GlobalDbNetworkComposition.hpp"
      provides: "Reusable minimal production GlobalDB networking composition for local trust tools"
      exports: ["GlobalDbNetworkComposition"]
  key_links:
    - from: "src/crdt/globaldb/GlobalDbNetworkComposition.cpp"
      to: "src/crdt/globaldb/globaldb.hpp"
      via: "production-compatible GlobalDB::New construction and existing topic wiring"
      pattern: "GlobalDB::New|Start|Stop"
---

<objective>
Extract the production-compatible GlobalDB networking composition required by the one-shot trust tool and production-path E2E.

Purpose: Give local trust operations one reusable route into the existing CRDT keyspace without account/private-key ownership or a new transport.
Output: `GlobalDbNetworkComposition` implementation and production build wiring.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract reusable minimal GlobalDB networking composition</name>
  <files>src/crdt/globaldb/GlobalDbNetworkComposition.hpp, src/crdt/globaldb/GlobalDbNetworkComposition.cpp, src/crdt/globaldb/CMakeLists.txt</files>
  <read_first>
    - example/crdt_globaldb/globaldb_app.cpp
    - src/crdt/globaldb/globaldb.hpp
    - src/crdt/globaldb/globaldb.cpp
    - src/crdt/globaldb/CMakeLists.txt
    - src/account/GeniusNode.cpp
    - test/src/securecrdt/securecrdt_test_node.hpp
  </read_first>
  <action>Extract the existing production-compatible io_context, GossipPubSub, scheduler, graphsync request/network, datastore path, and `GlobalDB::New` construction into `GlobalDbNetworkComposition::Create(Config)`, returning an owned composition with `Start`, `Stop`, and `db()` lifecycle. `Config` contains only network config path, database path, listen/broadcast topic, and logger; no account or private key. Reuse the same network configuration parsing and CRDT topic already used by nodes. Add the implementation to `src/crdt/globaldb/CMakeLists.txt` before building. The helper must support both `sgns-trust` and the production-path first-boot E2E, must permit two in-process compositions to exchange on the existing topic, and must not instantiate `GeniusNode` or introduce a new pubsub channel.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target crdt_globaldb globaldb_app securecrdt -j8</automated>
  </verify>
  <acceptance_criteria>
    - `GlobalDbNetworkComposition::Create`, `Start`, `Stop`, and `db` compile in `crdt_globaldb`.
    - No private/account dependency or new topic constant exists in the helper.
    - Existing `globaldb_app` and `securecrdt` targets still build.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Local tools and tests can join the production CRDT keyspace through one reusable composition without a full node or secret-bearing account.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local process to production CRDT network | The helper must reuse the production transport and topic without adding an unauthenticated side channel. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-03 | Spoofing | HIGH | network composition | mitigate | Reuse the production GlobalDB transport and candidate filters; expose no bypass write path. | Plan 07 `trust_first_boot_e2e_test` real network genesis path |
| T13-12 | Denial of Service | MEDIUM | composition lifecycle | mitigate | Owned `Start`/`Stop` lifecycle and dependency-reverse teardown prevent orphan listeners/tasks. | production-path `trust_first_boot_e2e_test` in Plan 07 |
| T-13-SC | Tampering | LOW | package supply chain | accept | No new package; existing networking and CRDT targets only. | CMake build graph inspection |
</threat_model>

<verification>
Build `crdt_globaldb`, `globaldb_app`, and `securecrdt`; the real two-composition exchange is exercised by Plan 07's production-path first-boot E2E.
</verification>

<success_criteria>
- One reusable production-compatible composition owns all minimal GlobalDB network dependencies.
- It contains no account/private-key lifecycle and adds no RPC/pubsub/proposal protocol.
- Existing GlobalDB and SecureCrdt consumers continue to build.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-06-SUMMARY.md` when done.
</output>
