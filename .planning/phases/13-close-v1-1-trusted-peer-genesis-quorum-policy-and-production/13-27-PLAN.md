---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 27
type: execute
wave: 1
depends_on: []
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/multiaccount/multi_account_sync.cpp
  - test/src/multiaccount/policy_lifetime_multi_account_test.cpp
autonomous: true
gap_closure: true
requirements: [TPR-02, BURN-02, BURN-03, TEST-01]
must_haves:
  truths:
    - "CR-11/D-16: SelectAccount, GetAddress, GetTransactionManager, public account/transaction calls, and catch-up callbacks use one generation-aware ownership snapshot; no thread reads or writes the account_ or transaction_manager_ shared_ptr objects outside that protocol."
    - "CR-11: account selection unpublishes the old generation, stops and drains its catch-up watcher and account-bound services outside the lifecycle lock, then publishes one fully initialized replacement generation; stale callbacks cannot mutate the old or new account."
    - "CR-12/D-16: the trusted-peer signer is one immutable node-scoped address/key pair captured together for the controller lifetime and is never resolved through the mutable selected account."
    - "Switching an active member before initial-burn readiness and after readiness leaves trust signatures labeled with and verifiable by the pinned trust address while the selected transaction account changes normally."
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "AccountServiceSnapshot generation contract and immutable NodeTrustSigner ownership"
    - path: "src/account/GeniusNode.cpp"
      provides: "Serialized unpublish/drain/publish account switching and generation-checked async/public consumers"
    - path: "test/src/multiaccount/multi_account_sync.cpp"
      provides: "Concurrent SelectAccount/read/catch-up generation counterexample"
    - path: "test/src/multiaccount/policy_lifetime_multi_account_test.cpp"
      provides: "Active trust-signer switch counterexamples before and after readiness"
  key_links:
    - from: "GeniusNode::SelectAccount"
      to: "GetAddress/GetTransactionManager/catch-up callbacks"
      via: "AccountServiceSnapshot copied under lifecycle_mutex_ and checked against account_service_generation_"
      pattern: "AccountServiceSnapshot|account_service_generation"
    - from: "GeniusNode trust-controller construction"
      to: "TrustedPeerRegistry/BurnConfig signatures"
      via: "one immutable NodeTrustSigner address and signing object captured together"
      pattern: "NodeTrustSigner|trust_signer"
---

<objective>
Close CR-11 and CR-12 with one atomic account-lifetime and trust-identity contract.

Purpose: Make live account selection race-free without allowing the selected transaction account to change the node's trusted-peer signing identity.
Output: Generation-aware account/manager publication, drained catch-up ownership, immutable trust signer binding, and three exact regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-25-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — expose concurrent ownership and mutable-signer failures</name>
  <files>test/src/multiaccount/multi_account_sync.cpp, test/src/multiaccount/policy_lifetime_multi_account_test.cpp</files>
  <read_first>
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/GeniusAccount.hpp
    - src/account/TransactionManager.hpp
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - src/migration/evm/BridgeCatchupWatcher.hpp
    - src/migration/evm/BridgeCatchupWatcher.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/multiaccount/policy_lifetime_multi_account_test.cpp
    - test/testutil/genius_node_test_access.hpp
  </read_first>
  <behavior>
    - `MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent` starts transaction-manager reads and injected catch-up callbacks behind a barrier while repeatedly selecting two real accounts; every observed snapshot has a non-empty address, matching manager/account generation, and no stale callback side effect, crash, deadlock, or duplicate manager owner.
    - `PolicyLifetimeMultiAccountTest.ActiveTrustSignerSurvivesAccountSwitchBeforeInitialBurnReadiness` switches the active member after TPR genesis but before burn-v1 confirmation, then proves the automatic burn-v1 approval is labeled by and cryptographically verifies under the original pinned trust address, reaches durable value 100, and does not use the newly selected account key.
    - `PolicyLifetimeMultiAccountTest.ActiveTrustSignerSurvivesAccountSwitchAfterReadiness` switches after ConfirmedReady, explicitly proposes/approves one exact successor, and proves the produced approval label/signature still matches the pinned trust authority while the replacement TransactionManager uses the newly selected account and existing confirmed burn provider.
  </behavior>
  <action>RED first: add the three exact tests using barriers/condition variables and existing real node/account fixtures, not sleeps as correctness conditions. Name the synchronization point `selection_barrier`, and extend friend test access only enough to observe a coherent `{account, manager, generation}` snapshot, the pinned trust signer address, catch-up generation, and manager callback-owner generation. For the CR-11 case, arrange overlap between `SelectAccount`, `GetAddress`, `GetTransactionManager`, a representative public manager operation, and catch-up callbacks; record each coherent snapshot/callback generation in `observed_generations`, assert every account/manager/callback tuple carries the same current generation, and reject any effect after that generation is unpublished in a zero `stale_callback_side_effects` assertion. Give its pre-fix assertion the diagnostic token `CR-11 generation mismatch`. For both CR-12 cases, inspect the exact `CandidateApprovalRecord`, name the production canonical bytes `canonical_candidate`, name the two compared identities `pinned_trust_address` and `replacement_address`, call `multisig::VerifyPayloadSignature` against the recorded signer and bytes, and prove verification under the replacement selected-account address fails; give each pre-fix assertion the diagnostic token `CR-12 pinned signer mismatch`. Run every exact RED case as an independent process with its own XML and require that exact case—not merely another member of a combined filter—to fail with its reviewed diagnostic before changing production. Do not weaken assertions, disable RPC catch-up in the CR-11 fixture, invoke direct activation, or replace production signing/selection paths with mocks. Commit the RED tests before GREEN production changes.</action>
  <verify>
    <automated>set -euo pipefail; red_dir="$(mktemp -d /tmp/phase13-27-red.XXXXXX)"; cmake --build build/OSX/Release --target multi_account_test policy_lifetime_multi_account_test -j8; if build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent' --gtest_output="xml:$red_dir/cr11.xml"; then exit 1; fi; if build/OSX/Release/test_bin/policy_lifetime_multi_account_test --gtest_filter='PolicyLifetimeMultiAccountTest.ActiveTrustSignerSurvivesAccountSwitchBeforeInitialBurnReadiness' --gtest_output="xml:$red_dir/cr12-before.xml"; then exit 1; fi; if build/OSX/Release/test_bin/policy_lifetime_multi_account_test --gtest_filter='PolicyLifetimeMultiAccountTest.ActiveTrustSignerSurvivesAccountSwitchAfterReadiness' --gtest_output="xml:$red_dir/cr12-after.xml"; then exit 1; fi; python3 -c 'import sys,xml.etree.ElementTree as E
expected=(("MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent","CR-11 generation mismatch"),("PolicyLifetimeMultiAccountTest.ActiveTrustSignerSurvivesAccountSwitchBeforeInitialBurnReadiness","CR-12 pinned signer mismatch"),("PolicyLifetimeMultiAccountTest.ActiveTrustSignerSurvivesAccountSwitchAfterReadiness","CR-12 pinned signer mismatch"))
for path,(name,reason) in zip(sys.argv[1:],expected):
 cases=list(E.parse(path).getroot().iter("testcase")); assert len(cases)==1 and cases[0].get("classname")+"."+cases[0].get("name")==name,(path,name)
 failures=[x for x in cases[0] if x.tag in ("failure","error")]; assert failures,(path,"expected RED failure")
 evidence=" ".join((x.get("message") or "")+" "+(x.text or "") for x in failures); assert reason in evidence,(path,reason,evidence)
print("cr11_red_reason=PASS cr12_before_red_reason=PASS cr12_after_red_reason=PASS")' "$red_dir/cr11.xml" "$red_dir/cr12-before.xml" "$red_dir/cr12-after.xml"</automated>
  </verify>
  <acceptance_criteria>
    - All three exact declarations exist once with no `DISABLED_` counterpart and fail before production changes.
    - The concurrent test uses an explicit barrier and exercises real public snapshots plus catch-up delivery during account replacement.
    - Both signer tests verify exact canonical approval bytes and distinguish the pinned trust key from the selected transaction-account key.
    - No test calls `TryActivate*`, `OnTrustedPeerGenesisConfirmed`, raw GlobalDB `Put`, or receiver-side admin as a substitute for the production callback path.
  </acceptance_criteria>
  <done>The regression suite deterministically exposes unsynchronized shared_ptr publication and the fixed-label/mutable-key mismatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — publish coherent account generations and pin the trust signer pair</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/multiaccount/policy_lifetime_multi_account_test.cpp</files>
  <read_first>
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/GeniusAccount.hpp
    - src/account/GeniusAccount.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - src/migration/evm/BridgeCatchupWatcher.hpp
    - src/migration/evm/BridgeCatchupWatcher.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/multiaccount/policy_lifetime_multi_account_test.cpp
  </read_first>
  <behavior>
    - Every public/asynchronous owner read obtains one coherent generation snapshot; a switch-in-progress reports typed service-unavailable where a manager is required rather than exposing a mixed pair.
    - Old catch-up/bridge/manager work is drained before replacement publication and generation checks reject late work.
    - Trust approvals always bind the immutable trust signer address to the same key, independent of selected account changes.
  </behavior>
  <action>Implement CR-11 under the existing recursive `lifecycle_mutex_` per D-16. Add `AccountServiceSnapshot` containing strong account/manager owners plus one `account_service_generation_`, and make one private snapshot helper the only way concurrent public methods and asynchronous catch-up/bridge callbacks copy those members. Make lifecycle-owned writes occur only inside the switch protocol: under the lock mark switching, increment/invalidate the generation, move the old manager/watcher/account-bound handles to locals, and unpublish manager access; release the lock before blocking `stopWatching`, callback removal, `Stop`, or joins; build/configure the replacement; then reacquire the lock and publish a complete account/manager generation only if the captured switch generation is still current. Recreate catch-up watching for that generation. Each posted callback captures strong snapshot owners and its generation, rechecks current generation before side effects, and never dereferences `self->account_` or `self->transaction_manager_` directly. Route `GetAddress`, `GetTransactionManager`, and every public or asynchronous path reachable during `SelectAccount` through this protocol; document any lifecycle-lock-only direct access and leave no unsynchronized copy/dereference found by the acceptance grep. Preserve Plan 13-25 stop-before-replace order and avoid holding `lifecycle_mutex_` across blocking stop/join calls.

Implement CR-12 as a node-scoped immutable `NodeTrustSigner` pair created once from the account that establishes the controller: store its canonical address and a strong signing authority together, never mutate it in `SelectAccount`, and pass both controller signer label and signing callback from that same captured object. The callback must not lock the node or read `account_`; it signs only through the pinned authority. The old selected account may be deconfigured as an account service, but its pinned signing capability remains alive until node policy teardown. Do not silently migrate trust identity on selection, add a second automatic signing surface, or alter current-peer/quorum validation. Make all three RED cases GREEN, then rerun the complete two binaries. Commit GREEN after tests pass.</action>
  <verify>
    <automated>set -euo pipefail; cmake --build build/OSX/Release --target multi_account_test policy_lifetime_multi_account_test -j8; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent:MultiAccountTest.PersistedHistoricalTrustAndTransactionsRestartWithSingleManagerOwnership'; build/OSX/Release/test_bin/policy_lifetime_multi_account_test --gtest_filter='PolicyLifetimeMultiAccountTest.ActiveTrustSignerSurvivesAccountSwitchBeforeInitialBurnReadiness:PolicyLifetimeMultiAccountTest.ActiveTrustSignerSurvivesAccountSwitchAfterReadiness:PolicyLifetimeMultiAccountTest.PassiveBurnSuccessorChangesPayEscrowWithoutReceiverAdmin'; build/OSX/Release/test_bin/multi_account_test; build/OSX/Release/test_bin/policy_lifetime_multi_account_test</automated>
  </verify>
  <acceptance_criteria>
    - The five exact focused cases pass with no skip/disabled counterpart; complete `multi_account_test` and `policy_lifetime_multi_account_test` also pass.
    - `GetAddress`, `GetTransactionManager`, catch-up lambdas, and representative public transaction calls obtain a coherent snapshot and never copy/dereference mutable member shared_ptrs without the lifecycle protocol.
    - Account switch unpublishes, drains outside the lock, then publishes exactly one complete new generation; stale callback counters remain zero and manager owner generations match.
    - Trust signer address and signing object are initialized together once, survive both switch timings, and exact approvals verify only under the pinned address.
    - No new key input, RPC, topic, package, direct activation path, or automatic successor signer is introduced.
  </acceptance_criteria>
  <done>Account selection is generation-safe for public/async consumers and cannot change the cryptographic key behind the node's trusted-peer identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Account selection to public and asynchronous consumers | Mutable account/manager owners cross concurrent API, IO, and catch-up threads. |
| Selected transaction account to trusted-peer approval | Account selection must not relabel signatures or change trust authority without quorum. |

## STRIDE Threat Register

| Threat ID | Gap | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|-----|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G26 | CR-11 | Tampering/Denial of Service | HIGH | account/manager shared ownership | mitigate | Coherent generation snapshot, unpublish/drain/publish switch, strong callback captures, and stale-generation rejection. | `ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent` |
| T13-G27 | CR-11 | Elevation of Privilege | HIGH | wrong-account async mutation | mitigate | Stop watcher before replacement and reject every callback whose captured generation is no longer current. | Same barrier/callback side-effect assertions |
| T13-G28 | CR-12 | Spoofing/Tampering | HIGH | trusted-peer signer label/key binding | mitigate | Immutable node-scoped address/signing authority captured together; selected account never resolves trust signatures. | Both `ActiveTrustSignerSurvivesAccountSwitch*` cases with cryptographic verification |
| T-13-SC | — | Tampering | LOW | package supply chain | accept | No package or dependency installation occurs. | Changed-file/build inspection |
</threat_model>

<verification>
Run the RED failure commits first, then the GREEN focused command, complete both affected binaries, and run any already-configured TSan form of the exact concurrent case. TSan absence is recorded as NOT_RUN and is handled mechanically by Plan 13-29.
</verification>

<success_criteria>
- CR-11 has no unsynchronized shared_ptr publication/reader or stale catch-up mutation path.
- CR-12 has one explicit immutable node trust identity whose address and key cannot diverge across SelectAccount.
- D-16 and the earlier single-manager/provider/passive-burn regressions remain green.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Preserve policy services and live burn behavior across account selection | 13-27 | COVERED | Coherent generations retain node policy while replacing account consumers. |
| REQ | BURN-02 | Replacement manager consumes node-scoped confirmed provider safely | 13-27 | COVERED | Manager/provider publication is generation-owned. |
| REQ | BURN-03 | Initial/default and successor burn remain live through selection | 13-27 | COVERED | Before/after readiness signer tests cover burn-v1 and successor behavior. |
| REQ | TPR-02 | Current-member approvals use the intended authenticated signer | 13-27 | COVERED | Approval label and key remain the pinned trust pair. |
| REQ | TEST-01 | Account-switch lifetime and race evidence | 13-27 | COVERED | One concurrent ownership case plus two active-signer switch cases. |
| CONTEXT | D-05, D-08 | Current durable policy exclusively authorizes successors | 13-27 | COVERED | Trust identity pinning changes no quorum/authorization rule. |
| CONTEXT | D-13, D-16 | Restricted readiness and node/account lifetime split | 13-27 | COVERED | Before-ready switching stays restricted and node policy survives selection. |
| RESEARCH | — | Node/account lifetime split and weak/generation callback ownership | 13-27 | COVERED | Account consumers change; policy ownership and signer pair remain node-scoped. |
| REVIEW/VERIFY | CR-11 | Shared_ptr data race/UAF/wrong-account callbacks | 13-27 | COVERED | One ownership protocol covers writer, public readers, and catch-up callbacks. |
| REVIEW/VERIFY | CR-12 | Fixed signer label uses mutable selected-account key | 13-27 | COVERED | Immutable address/key pair with both readiness timings. |
| PRIOR | CR-01..CR-10, WR-07 | Preserve earlier HIGH regressions and exact product paths | 13-27, 13-29 | COVERED | Focused prior cases are retained; Plan 13-29 reruns exact cumulative gate. |

Deferred whole-disk rollback, new transport/RPC, and remote administration remain excluded. No scoped source item is missing.

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-27-SUMMARY.md` when done.
</output>
