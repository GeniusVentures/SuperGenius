---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 24
type: execute
wave: 2
depends_on: [13-23]
files_modified:
  - src/account/TrustStartupController.hpp
  - src/account/TrustStartupController.cpp
  - test/src/startup/trust_first_boot_e2e_test.cpp
autonomous: true
gap_closure: true
requirements: [BOOT-04, POLICY-01, TEST-01, TPR-02]
must_haves:
  truths:
    - "D-08/D-14/D-15: startup and every Refresh rediscover retained policy candidates for the current durable predecessor, merge them with live callback IDs, and process them in deterministic version/content-hash order."
    - "CR-09: after a passive policy commit failure destroys the first controller, a reconstructed controller over the same retained CRDT approvals advances the durable policy without any new CRDT write, approval, proposal, or admin action."
    - "Authenticated below-quorum policy candidates remain retryable; malformed, wrong-head, corrupt, and commit-failed candidates return actionable typed errors without replacing the last-known-good head or causing an in-process retry loop."
  artifacts:
    - path: "src/account/TrustStartupController.hpp"
      provides: "Separate policy pending/failure suppression state scoped to one controller"
    - path: "src/account/TrustStartupController.cpp"
      provides: "Retained policy discovery, deterministic merge, retry, and activation"
    - path: "test/src/startup/trust_first_boot_e2e_test.cpp"
      provides: "Commit-failure plus controller-reconstruction policy replay counterexample"
  key_links:
    - from: "TrustStartupController::Refresh"
      to: "TrustedPeerRegistry::ListPendingPolicyCandidates"
      via: "authoritative retained-candidate discovery for the current durable predecessor"
      pattern: "ListPendingPolicyCandidates"
    - from: "retained and callback policy candidate IDs"
      to: "TrustedPeerRegistry::TryActivatePolicyCandidate"
      via: "separate deterministic policy queue with controller-local failure suppression"
      pattern: "TryActivatePolicyCandidate"
---

<objective>
Close CR-09 by making controller construction and Refresh replay retained policy quorum from authoritative CRDT storage.

Purpose: Ensure transient durable failures or controller reconstruction cannot strand a valid current-policy successor until another operator/network write happens.
Output: Deterministic retained-policy discovery/retry and a no-new-write reconstruction counterexample.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-21-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-23-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rediscover retained policy quorum after controller reconstruction</name>
  <files>src/account/TrustStartupController.hpp, src/account/TrustStartupController.cpp, test/src/startup/trust_first_boot_e2e_test.cpp</files>
  <read_first>
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/TrustedPeerRegistry.cpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/trustedpeer/TrustStateStore.cpp
    - src/securecrdt/SecureCrdt.hpp
    - src/securecrdt/SecureCrdt.cpp
    - src/trustedpeer/genesis_tool/LocalTrustAdmin.hpp
    - src/trustedpeer/genesis_tool/LocalTrustAdmin.cpp
    - test/src/startup/trust_first_boot_e2e_test.cpp
    - test/src/trustedpeer/operator_approval_test.cpp
  </read_first>
  <behavior>
    - `TrustFirstBootE2ETest.RetainedPolicyQuorumRetriesAfterControllerReconstructionWithoutNewWrite` has A propose policy v2 and B approve once, retains both approvals on passive C, and injects `COMMIT_FAILED` on C's first durable activation attempt.
    - After observing the exact failure and unchanged C policy-v1 head, the test destroys C's controller, removes the failing committer, reopens the same store, and reconstructs C over the same SecureCrdt/GlobalDB instance.
    - Between exact `RECONSTRUCTION_NO_WRITE_WINDOW_BEGIN` / `RECONSTRUCTION_NO_WRITE_WINDOW_END` comments, spanning reconstruction through policy-v2 durable convergence, the test performs zero `Propose`, `Approve`, `SubmitCandidateApproval`, raw GlobalDB Put, candidate callback injection, direct `TryActivatePolicyCandidate`, or LocalTrustAdmin operation.
    - The reconstructed controller lists the retained candidate, commits the exact policy-v2 hash, remains economically ready, and does not duplicate approvals; below-quorum remains pending while wrong-head/corrupt/actionable failures preserve LKG and return typed context.
  </behavior>
  <action>RED: add the named reconstruction test using the existing production three-node fixture and `TrustStateStore::BatchCommitter` seam. Mark the last pre-reconstruction registry write, record candidate ID and approval count, inject C commit failure, destroy only C's controller, reopen C's store with the normal committer while retaining C's GlobalDB/SecureCrdt, then place `RECONSTRUCTION_NO_WRITE_WINDOW_BEGIN` immediately before constructing the replacement controller and `RECONSTRUCTION_NO_WRITE_WINDOW_END` immediately after exact durable-v2 convergence. Assert unchanged approval count and no write/admin/direct-activation call inside that marked window. Commit as `test(13-24): add retained policy reconstruction counterexample`. GREEN: introduce policy-specific pending and failed candidate containers rather than storing policy IDs in `pending_burn_candidates_`/`failed_burn_candidates_`. On every verified startup/Refresh, call `registry_->ListPendingPolicyCandidates()` for the current durable predecessor, merge those IDs with callback-enqueued policy IDs, filter only controller-local failed IDs, sort deterministically by version then content hash, and deduplicate before calling `TryActivatePolicyCandidate` outside `candidate_mutex_`. Keep `false` in pending state, remove successful/stale IDs after durable reload, and on actionable validation/commit errors emit `TRUST_ACTIVATION_FAILED`, remove the ID from the current pending queue, add it to controller-local suppression, and return the typed error. Reconstruction necessarily starts with empty suppression and therefore rediscovers the authoritative CRDT record. Do not add signing, admin, RPC, a new topic, or automatic proposal behavior. Commit as `fix(13-24): replay retained policy quorum on refresh`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trust_first_boot_e2e_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='TrustFirstBootE2ETest.RetainedPolicyQuorumRetriesAfterControllerReconstructionWithoutNewWrite:TrustFirstBootE2ETest.PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall'</automated>
  </verify>
  <acceptance_criteria>
    - Both exact cases execute and pass with no skip/disabled counterpart.
    - The reconstruction case observes one injected failure, unchanged v1 LKG, then exact durable v2 from retained approvals; the exact marked reconstruction window contains zero write/admin/direct-activation/callback-nudge action.
    - Policy IDs are no longer stored in burn-named queues or discovery flags; policy and burn retries have distinct state and error context.
    - Candidate order is stable by version/content hash and duplicate callback/list results produce one activation attempt per refresh.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>A reconstructed controller automatically and deterministically advances retained current-policy quorum without another network or operator write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retained CRDT approvals to reconstructed controller | Existing authenticated data must be replayable without trusting ephemeral callback delivery or in-memory queues. |
| Candidate retry to durable policy head | Retry must preserve current-policy authorization, deterministic winner ordering, and last-known-good authority. |

## STRIDE Threat Register

| Threat ID | Gap | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|-----|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G20 | CR-09 | Denial of Service | HIGH | controller reconstruction policy retry | mitigate | List current-predecessor policy candidates on every startup/Refresh and merge with callbacks deterministically. | `RetainedPolicyQuorumRetriesAfterControllerReconstructionWithoutNewWrite` |
| T13-G21 | CR-09 | Tampering/Elevation of Privilege | HIGH | retained policy activation | mitigate | Reuse `TryActivatePolicyCandidate` current-policy quorum and store CAS; never sign or write during reconstruction retry. | Same test exact hash/approval/no-write assertions |
| T13-G22 | CR-09 | Repudiation | HIGH | activation errors and suppression | mitigate | Typed candidate failure event, LKG preservation, controller-local suppression, and authoritative rediscovery after reconstruction. | Same test failure/reconstruction sections |
| T-13-SC | — | Tampering | LOW | package supply chain | accept | No package or dependency change occurs. | Changed-file inspection |
</threat_model>

<verification>
Run the focused reconstruction and existing passive-policy cases, then the complete `trust_first_boot_e2e_test` binary.
</verification>

<success_criteria>
- CR-09 is closed across injected commit failure and complete controller reconstruction.
- BOOT-04 restart authority, POLICY-01 current-policy authorization, and TPR-02 passive quorum convergence remain explicit.
- No new write, signer, admin surface, transport, or callback replay assumption is introduced.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Persisted current-policy authority converges after restart/reconstruction | 13-24 | COVERED | Retained quorum advances reconstructed C without another write. |
| REQ | BOOT-04 | Verified persisted state is restart authority | 13-24 | COVERED | LKG survives failure and reconstructed retry. |
| REQ | POLICY-01 | Current policy exclusively authorizes its durable successor | 13-24 | COVERED | Existing `TryActivatePolicyCandidate`/store CAS remain authoritative. |
| REQ | TPR-02 | Current-set N-of-M membership change converges | 13-24 | COVERED | A/B retained approvals are rediscovered by passive C. |
| REQ | TEST-01 | Restart, explicit approval, and failure evidence | 13-24 | COVERED | Injected commit failure plus no-write controller reconstruction. |
| CONTEXT | D-05, D-08, D-12, D-14, D-15 | Versioned authority, current-policy links, one winner, persisted LKG | 13-24 | COVERED | Listing is current-predecessor-scoped and activation reuses exact verifier. |
| RESEARCH | — | Deterministic candidate processing and persist-before-publish | 13-24 | COVERED | Listed/callback IDs sort/dedupe before store commit/reload. |
| REVIEW | CR-09 | Retained policy quorum is never replayed after reconstruction | 13-24 | COVERED | Production calls `ListPendingPolicyCandidates` on startup/Refresh. |

Other phase source items remain covered by Plans 13-01..13-23 and Plan 13-25, with cumulative rerun/accounting in Plan 13-26. No scoped source item is missing.

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-24-SUMMARY.md` when done.
</output>
