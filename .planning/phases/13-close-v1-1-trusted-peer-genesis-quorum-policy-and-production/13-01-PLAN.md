---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trustedpeer/CanonicalTrustCodec.hpp
  - src/trustedpeer/CanonicalTrustCodec.cpp
  - src/trustedpeer/GenesisManifest.hpp
  - src/trustedpeer/GenesisManifest.cpp
  - src/trustedpeer/QuorumPolicy.hpp
  - src/trustedpeer/QuorumPolicy.cpp
  - src/securecrdt/QuorumThresholdValidation.hpp
  - src/trustedpeer/CMakeLists.txt
  - test/src/trustedpeer/genesis_manifest_test.cpp
  - test/src/trustedpeer/quorum_policy_test.cpp
  - test/src/trustedpeer/CMakeLists.txt
autonomous: true
requirements: [BOOT-02, POLICY-01, VALID-01, TPR-01, TPR-02, BURN-01, BURN-03]
must_haves:
  truths:
    - "D-02/D-04: the same reviewed genesis fields always produce the same fingerprint regardless of input peer order, while empty, duplicate, malformed, non-canonical, oversized, or tampered manifests are rejected."
    - "D-05/D-06/D-07: confirmed policy contains both thresholds and enforces 1..M bounds, floor(M/2)+1 for membership, and ceil(2M/3) for BurnConfig."
    - "D-08: a policy successor is valid only at current.version+1, linked to the current hash, and authorized by the current policy rather than its proposed signer set."
  artifacts:
    - path: "src/trustedpeer/CanonicalTrustCodec.hpp"
      provides: "Domain-separated bounded canonical encoding and SHA-256 identity API"
      exports: ["CanonicalTrustCodec"]
    - path: "src/trustedpeer/GenesisManifest.hpp"
      provides: "Reviewed genesis manifest model and fingerprint validation"
      exports: ["GenesisManifest"]
    - path: "src/trustedpeer/QuorumPolicy.hpp"
      provides: "Versioned peer and threshold policy with successor checks"
      exports: ["QuorumPolicyState", "ValidatePolicySuccessor"]
  key_links:
    - from: "src/trustedpeer/GenesisManifest.cpp"
      to: "src/trustedpeer/CanonicalTrustCodec.cpp"
      via: "fingerprint over canonical manifest bytes"
      pattern: "Canonical.*Genesis|sha256"
    - from: "src/trustedpeer/QuorumPolicy.cpp"
      to: "src/securecrdt/QuorumThresholdValidation.hpp"
      via: "exact policy-specific integer floor validation"
      pattern: "Membership.*Floor|Burn.*Floor|Validate"
---

<objective>
Create the canonical trust identity and versioned quorum-policy contracts that every later Phase 13 boundary consumes.

Purpose: Make genesis fingerprints, candidate hashes, threshold floors, and successor authorization deterministic and independently testable before storage or networking is introduced.
Output: Canonical codec, genesis manifest, quorum policy, complete validation, golden-vector tests, and build wiring.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VALIDATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and prove canonical genesis identity</name>
  <files>src/trustedpeer/CanonicalTrustCodec.hpp, src/trustedpeer/CanonicalTrustCodec.cpp, src/trustedpeer/GenesisManifest.hpp, src/trustedpeer/GenesisManifest.cpp, test/src/trustedpeer/genesis_manifest_test.cpp</files>
  <read_first>
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
    - src/base/buffer.hpp
    - src/base/buffer.cpp
    - src/crypto/sha/sha256.hpp
    - src/crypto/sha/sha256.cpp
    - src/trustedpeer/TrustedPeerRegistry.cpp
    - test/src/trustedpeer/trustedpeerregistry_genesis_test.cpp
  </read_first>
  <behavior>
    - Peer permutations normalize to one sorted lowercase list and one golden fingerprint.
    - Empty peers, duplicates after normalization, malformed 128-hex keys, more than 256 peers, unknown codec version, trailing bytes, length overflow, and any field tamper fail closed.
    - Fingerprint changes when network ID, bootstrapper, policy version, either threshold, or initial burn basis points changes.
  </behavior>
  <action>Write the failing `genesis_manifest_test` cases first. Implement `CanonicalTrustCodec` as a map-free codec using domain tag `SGNS_TRUST_GENESIS_V1`, fixed-width big-endian integers, fixed 64-byte decoded public keys, explicit length/count fields, full-consumption decoding, and one `crypto::sha256` helper. Define `GenesisManifest` with encoding version 1, `uint16_t network_id`, bootstrapper public key, `QuorumPolicyState`-compatible version/peers/threshold fields, and `initial_burn_basis_points`; validate value 100 for the milestone's initial manifest and include it in the fingerprint so the D-13 economic gate is unambiguous. Set `MAX_TRUSTED_PEERS=256`; validate, lowercase, deduplicate, and sort before encoding per D-02/D-04 and BOOT-02. Do not use protobuf default serialization, newline-delimited payloads, display strings, `std::hash`, or mutable JSON bytes as identity material.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target genesis_manifest_test -j8 &amp;&amp; build/OSX/Release/test_bin/genesis_manifest_test</automated>
  </verify>
  <acceptance_criteria>
    - `genesis_manifest_test` contains a fixed expected 64-lowercase-hex fingerprint vector and passes twice with reversed peer input order.
    - Tests reject 0 and 257 peers, duplicate mixed-case peers, malformed keys, unknown version, trailing bytes, truncated lengths, and single-field tampering.
    - `rg -n "SGNS_TRUST_GENESIS_V1|MAX_TRUSTED_PEERS|initial_burn_basis_points" src/trustedpeer/{CanonicalTrustCodec,GenesisManifest}.{hpp,cpp}` finds all three invariants.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>A deterministic, bounded, network-bound genesis manifest produces stable bytes and a stable fingerprint, with every malformed or tampered case rejected before use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact current-policy successor rules</name>
  <files>src/trustedpeer/QuorumPolicy.hpp, src/trustedpeer/QuorumPolicy.cpp, src/securecrdt/QuorumThresholdValidation.hpp, src/trustedpeer/CMakeLists.txt, test/src/trustedpeer/quorum_policy_test.cpp, test/src/trustedpeer/CMakeLists.txt</files>
  <read_first>
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
    - src/securecrdt/QuorumThresholdValidation.hpp
    - src/multisig/MultiSig.hpp
    - src/multisig/MultiSig.cpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/CMakeLists.txt
    - test/src/trustedpeer/trustedpeerregistry_threshold_floor_test.cpp
    - test/src/trustedpeer/CMakeLists.txt
  </read_first>
  <behavior>
    - Membership floors for M=1,2,3,4,100,101 are 1,2,2,3,51,51; Burn floors are 1,2,2,3,67,68.
    - Threshold 0, threshold greater than M, a value below its policy floor, empty/duplicate/malformed/over-cap peers, and non-canonical decoded ordering are rejected.
    - A successor is accepted only when version increments by exactly one and both expected previous hash and authorizing policy hash equal the persisted current policy hash.
    - Newly proposed peers and thresholds never supply the signatures that authorize that proposal.
  </behavior>
  <action>Write `quorum_policy_test` first, then implement `QuorumPolicyState`, `MembershipQuorumFloor`, `BurnQuorumFloor`, `ValidateQuorumPolicy`, and `ValidatePolicySuccessor(current, candidate)`. Replace the generic `ceil(0.51*M)` behavior in `QuorumThresholdValidation.hpp` with explicit reusable validators: bounds first, then `M/2+1` for membership and `M-M/3` for burn to avoid overflow. The policy canonical bytes must include domain, network, version, previous hash, sorted peers, and both thresholds. `ValidatePolicySuccessor` must take an already-confirmed current snapshot and compare both candidate linkage hashes against it per D-08; it must never consult candidate peers for authorization. Wire source and tests into existing `trustedpeer` CMake targets without adding packages.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target quorum_policy_test trustedpeer -j8 &amp;&amp; build/OSX/Release/test_bin/quorum_policy_test</automated>
  </verify>
  <acceptance_criteria>
    - Boundary vectors for M=0,1,2,3,4,100,101 and M+1 thresholds pass with exact D-06/D-07 results.
    - Tests prove a candidate signed only by newly added peers remains unauthorized and that skipped versions, wrong predecessor, and wrong authorizing-policy hash fail.
    - `rg -n "51|0\.51" src/securecrdt/QuorumThresholdValidation.hpp src/trustedpeer/QuorumPolicy.cpp` returns no policy-floor implementation using percentages.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Versioned policy validation enforces complete signer constraints, exact locked floors, and exclusively current-policy authorization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewed manifest bytes to canonical model | Untrusted operator/deployment input becomes a network trust identity. |
| Proposed policy to current policy | A successor attempts to cross the authorization boundary controlled by the persisted current signer set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|-----------|-------------|-----------------|------------------------|
| T13-01 | Spoofing/Tampering | `GenesisManifest` | mitigate | Domain-separated canonical bytes, derived fingerprint, strict peer/bootstrapper/threshold validation, and golden tamper vectors. | `genesis_manifest_test` |
| T13-05 | Elevation of Privilege | `QuorumPolicyState` | mitigate | Current snapshot supplies authorizer; exact version/hash links and D-06/D-07 floors reject self-authorization and threshold lowering. | `quorum_policy_test` |
| T13-12 | Denial of Service | canonical decoders | mitigate | 256-peer bound, checked lengths, full-consumption decode, and malformed/trailing-byte rejection. | boundary cases in both tests |
| T-13-SC | Tampering | package supply chain | accept | No package-manager install occurs; only existing repository libraries are linked. | configure/build targets show no new package dependency |
</threat_model>

<verification>
Run `ctest --test-dir build/OSX/Release --output-on-failure -R 'genesis_manifest|quorum_policy|trustedpeerregistry_threshold'` after both tasks.
</verification>

<success_criteria>
- Canonical genesis and policy byte identities are deterministic and reject malformed/non-canonical inputs.
- All D-06/D-07 threshold boundaries and D-08 successor checks are executable unit contracts.
- No proposed policy can select the signers or threshold that authorize itself.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-01-SUMMARY.md` when done.
</output>
