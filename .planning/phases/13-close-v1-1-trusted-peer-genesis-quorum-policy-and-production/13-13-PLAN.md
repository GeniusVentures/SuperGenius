---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trustedpeer/TrustStateStore.hpp
  - src/trustedpeer/TrustStateStore.cpp
  - test/src/trustedpeer/trust_state_store_test.cpp
autonomous: true
gap_closure: true
requirements: [BOOT-04, BURN-03, TEST-01]
must_haves:
  truths:
    - "D-13/D-15: a policy successor cannot become durable while burn version 1/value 100 is authenticated only by the bootstrap proof."
    - "D-14: a verified snapshot explicitly distinguishes bootstrap-only burn state from peer-quorum-authorized burn state."
    - "After peer-confirmed burn version 1 is durable, the same valid policy successor can commit without weakening current-policy authorization."
  artifacts:
    - path: "src/trustedpeer/TrustStateStore.hpp"
      provides: "Explicit burn authorization kind and typed initial-burn sequencing failure"
      exports: ["BurnAuthorizationKind", "ConfirmedTrustSnapshot", "TrustStateStore::Error"]
    - path: "src/trustedpeer/TrustStateStore.cpp"
      provides: "Durable initial-burn precondition inside CommitPolicySuccessor"
    - path: "test/src/trustedpeer/trust_state_store_test.cpp"
      provides: "Policy-v2-before-burn-v1 deadlock counterexample and recovery proof"
  key_links:
    - from: "src/trustedpeer/TrustStateStore.cpp"
      to: "ConfirmedTrustSnapshot::burn_authorization"
      via: "LoadAndVerify classifies the proof that actually authorized the current burn head"
      pattern: "BurnAuthorizationKind|burn_authorization"
    - from: "TrustStateStore::CommitPolicySuccessor"
      to: "TrustStateStore::CommitWrites"
      via: "peer-confirmed initial-burn check runs under transition_mutex_ before any policy write"
      pattern: "INITIAL_BURN_NOT_CONFIRMED"
---

<objective>
Close CR-01 at the durable transition boundary by making peer-confirmed initial burn an explicit prerequisite for policy advancement.

Purpose: Preserve the D-13 TPR-genesis -> deterministic burn-v1/value-100 -> later-policy ordering even when callers or callbacks attempt transitions out of order.
Output: An explicit verified burn-authorization classification, a typed policy-transition rejection, and a restart-safe regression proving the node cannot be stranded.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-02-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject durable policy advancement until burn v1 has peer-quorum authority</name>
  <files>src/trustedpeer/TrustStateStore.hpp, src/trustedpeer/TrustStateStore.cpp, test/src/trustedpeer/trust_state_store_test.cpp</files>
  <read_first>
    - src/trustedpeer/TrustStateStore.hpp
    - src/trustedpeer/TrustStateStore.cpp
    - src/trustedpeer/QuorumPolicy.hpp
    - src/account/BurnConfig.hpp
    - test/src/trustedpeer/trust_state_store_test.cpp
  </read_first>
  <behavior>
    - Immediately after CommitGenesis, LoadAndVerify returns burn version 1/value 100 with BurnAuthorizationKind::BootstrapOnly.
    - CommitPolicySuccessor returns Error::INITIAL_BURN_NOT_CONFIRMED before CommitWrites while the durable burn authorization is BootstrapOnly; policy head, burn head, and complete snapshot remain byte-for-byte unchanged after reopen.
    - CommitBurnSuccessor may replace the same burn-v1 record only with a valid current-policy burn-threshold proof over the canonical burn CandidateCore; LoadAndVerify then returns BurnAuthorizationKind::PeerQuorum.
    - The previously rejected, otherwise valid policy-v2 candidate commits after peer-confirmed burn v1, and reopen preserves both heads and PeerQuorum classification.
  </behavior>
  <action>RED: add the named counterexample `PolicySuccessorRejectedUntilInitialBurnPeerConfirmed` and commit it as `test(13-13): add initial-burn sequencing counterexample`; prove the current code advances policy v2 before burn v1 peer confirmation. GREEN: add `BurnAuthorizationKind { BootstrapOnly, PeerQuorum }` to the verified snapshot contract and `INITIAL_BURN_NOT_CONFIRMED` to `TrustStateStore::Error`. In `LoadAndVerify`, classify the current burn from the already verified authorizing path: only the genesis/bootstrap authorization bytes and bootstrap signature yield `BootstrapOnly`; a valid `AuthorizationBindsBurn` proof checked against its named historical authorizing policy yields `PeerQuorum`, including all burn versions greater than 1. In `CommitPolicySuccessor`, immediately after reloading the durable snapshot under `transition_mutex_`, reject unless `burn_authorization == PeerQuorum`; perform this check before version/link/proof processing and before `CommitWrites`. Preserve the existing same-version burn-v1 proof-replacement path so current peers can move the snapshot from BootstrapOnly to PeerQuorum. Update snapshot equality and error-category text. Do not infer readiness from proof count or the current policy threshold. GREEN commit: `fix(13-13): enforce durable initial-burn sequencing`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trust_state_store_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_state_store_test --gtest_filter='*PolicySuccessorRejectedUntilInitialBurnPeerConfirmed*:*Restart*'</automated>
  </verify>
  <acceptance_criteria>
    - The counterexample observes `INITIAL_BURN_NOT_CONFIRMED` and identical policy/burn heads before and after reopen.
    - The same test observes `BootstrapOnly` before peer proof, `PeerQuorum` after peer proof, and a successful policy-v2 commit only in the latter state.
    - `rg -n "INITIAL_BURN_NOT_CONFIRMED|BurnAuthorizationKind|PeerQuorum" src/trustedpeer/TrustStateStore.{hpp,cpp} test/src/trustedpeer/trust_state_store_test.cpp` finds the public state, durable guard, and assertions.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>There is no durable path to a policy successor until deterministic burn v1/value 100 has current-peer quorum authorization, and failed attempts cannot change restart authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy candidate to durable policy head | A valid current-policy proof still must not bypass required initial economic sequencing. |
| Persisted burn proof to readiness classification | Bootstrap identity proof and current-peer economic authorization have different authority. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G01 | Tampering/Elevation of Privilege | HIGH | `TrustStateStore::CommitPolicySuccessor` | mitigate | Reload under the transition mutex and require verified `PeerQuorum` burn authority before any policy write. | `trust_state_store_test --gtest_filter='*PolicySuccessorRejectedUntilInitialBurnPeerConfirmed*'` |
| T13-G02 | Denial of Service | HIGH | initial trust-state sequence | mitigate | Preserve same-version burn-v1 peer-proof replacement and prove the rejected policy can commit after burn confirmation. | Same counterexample recovery assertions |
| T-13-SC | Tampering | LOW | package supply chain | accept | No package install or dependency change occurs. | Changed-file/CMake graph inspection |
</threat_model>

<verification>
Run the focused test, then `build/OSX/Release/test_bin/trust_state_store_test` to preserve all existing durable-store contracts.
</verification>

<success_criteria>
- CR-01 is closed at the only authoritative write boundary.
- Bootstrap-only and peer-confirmed burn states are explicit and restart-stable.
- No policy write occurs on the rejected path.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-13-SUMMARY.md` when done.
</output>
