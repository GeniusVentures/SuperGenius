---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trustedpeer/TrustStateStore.hpp
  - src/trustedpeer/TrustStateStore.cpp
  - test/src/trustedpeer/trust_state_store_test.cpp
autonomous: true
gap_closure: true
requirements: [BOOT-04, BURN-03, TEST-01]
must_haves:
  truths:
    - "D-13/D-15: neither a policy successor nor burn version 2 can become durable while burn version 1/value 100 is authenticated only by the bootstrap proof."
    - "D-14: a verified snapshot explicitly distinguishes bootstrap-only burn state from peer-quorum-authorized burn state."
    - "Only an exact same-version burn-v1/value-100 peer-proof replacement can leave BootstrapOnly; after it is durable, otherwise valid policy-v2 and burn-v2 successors can commit without weakening current-policy authorization."
  artifacts:
    - path: "src/trustedpeer/TrustStateStore.hpp"
      provides: "Explicit burn authorization kind and typed initial-burn sequencing failure"
      exports: ["BurnAuthorizationKind", "ConfirmedTrustSnapshot", "TrustStateStore::Error"]
    - path: "src/trustedpeer/TrustStateStore.cpp"
      provides: "Durable BootstrapOnly preconditions inside CommitPolicySuccessor and CommitBurnSuccessor"
    - path: "test/src/trustedpeer/trust_state_store_test.cpp"
      provides: "Direct-store policy-v2 and burn-v2 bypass counterexamples with restart-safe recovery proof"
  key_links:
    - from: "src/trustedpeer/TrustStateStore.cpp"
      to: "ConfirmedTrustSnapshot::burn_authorization"
      via: "LoadAndVerify classifies the proof that actually authorized the current burn head"
      pattern: "BurnAuthorizationKind|burn_authorization"
    - from: "TrustStateStore::CommitPolicySuccessor and TrustStateStore::CommitBurnSuccessor"
      to: "TrustStateStore::CommitWrites"
      via: "peer-confirmed initial-burn guards run under transition_mutex_ before any successor write; CommitBurnSuccessor permits only exact burn-v1/value-100 proof replacement while BootstrapOnly"
      pattern: "INITIAL_BURN_NOT_CONFIRMED"
---

<objective>
Close CR-01 at the durable transition boundary by making peer-confirmed initial burn an explicit prerequisite for both policy and later-burn advancement.

Purpose: Preserve the D-13 TPR-genesis -> deterministic burn-v1/value-100 -> later-policy/later-burn ordering even when callers or callbacks attempt transitions out of order.
Output: An explicit verified burn-authorization classification, typed policy/burn-transition rejection, and restart-safe regressions proving the node cannot bypass or become stranded in the initial-burn sequence.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-02-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject durable policy and burn-v2 advancement until burn v1 has peer-quorum authority</name>
  <files>src/trustedpeer/TrustStateStore.hpp, src/trustedpeer/TrustStateStore.cpp, test/src/trustedpeer/trust_state_store_test.cpp</files>
  <read_first>
    - src/trustedpeer/TrustStateStore.hpp
    - src/trustedpeer/TrustStateStore.cpp
    - src/trustedpeer/QuorumPolicy.hpp
    - src/account/BurnConfig.hpp
    - test/src/trustedpeer/trust_state_store_test.cpp
  </read_first>
  <behavior>
    - Immediately after CommitGenesis, LoadAndVerify returns burn version 1/value 100 with BurnAuthorizationKind::BootstrapOnly.
    - CommitPolicySuccessor returns Error::INITIAL_BURN_NOT_CONFIRMED before CommitWrites while the durable burn authorization is BootstrapOnly; policy head, burn head, and complete snapshot remain byte-for-byte unchanged after reopen.
    - CommitBurnSuccessor returns Error::INITIAL_BURN_NOT_CONFIRMED before CommitWrites for an otherwise valid, current-policy peer-quorum-signed burn-v2 successor while the reloaded durable snapshot is BootstrapOnly; reopen preserves byte-identical policy and burn heads, burn-v1/value-100, and BootstrapOnly classification.
    - While BootstrapOnly, CommitBurnSuccessor accepts only an exact same-version replacement of the durable burn-v1/value-100 record (identical canonical fields/hash) with a valid current-policy burn-threshold proof over the canonical burn CandidateCore; LoadAndVerify then returns BurnAuthorizationKind::PeerQuorum.
    - The previously rejected, otherwise valid policy-v2 and burn-v2 candidates commit only after peer-confirmed burn v1, and reopen preserves the resulting heads and PeerQuorum classification.
  </behavior>
  <action>RED: add the named counterexamples `PolicySuccessorRejectedUntilInitialBurnPeerConfirmed` and `BurnV2RejectedUntilInitialBurnPeerConfirmed`, then commit them as `test(13-13): add initial-burn sequencing counterexamples`. The second test must construct an otherwise valid burn-v2 successor linked to the deterministic burn-v1 head, authorize its canonical BurnConfig `CandidateCore` with the current policy's burn quorum, and prove the current direct store API advances it before burn-v1 peer confirmation. GREEN: add `BurnAuthorizationKind { BootstrapOnly, PeerQuorum }` to the verified snapshot contract and `INITIAL_BURN_NOT_CONFIRMED` to `TrustStateStore::Error`. In `LoadAndVerify`, classify the current burn from the already verified authorizing path: only the genesis/bootstrap authorization bytes and bootstrap signature yield `BootstrapOnly`; a valid `AuthorizationBindsBurn` proof checked against its named historical authorizing policy yields `PeerQuorum`, including all burn versions greater than 1. In `CommitPolicySuccessor`, immediately after reloading the durable snapshot under `transition_mutex_`, reject unless `burn_authorization == PeerQuorum`; perform this check before version/link/proof processing and before `CommitWrites`. In `CommitBurnSuccessor`, inspect that same reloaded snapshot while still holding `transition_mutex_`: when it is `BootstrapOnly`, return `INITIAL_BURN_NOT_CONFIRMED` before `CommitWrites` for every candidate except an exact same-version replacement of the durable burn version 1/value 100 record whose canonical fields and hash equal the current burn and whose authorization bytes are the canonical burn `CandidateCore` carrying a valid current-policy burn-threshold peer proof. In particular, reject an otherwise valid version-2 successor rather than allowing the normal version/link/proof branch to run. After the exact replacement commits and reloads as `PeerQuorum`, preserve the existing successor validation paths. Update snapshot equality and error-category text. Do not infer readiness from proof count or the current policy threshold. GREEN commit: `fix(13-13): enforce durable initial-burn sequencing`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trust_state_store_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_state_store_test --gtest_filter='*PolicySuccessorRejectedUntilInitialBurnPeerConfirmed*:*BurnV2RejectedUntilInitialBurnPeerConfirmed*:*Restart*'</automated>
  </verify>
  <acceptance_criteria>
    - The counterexample observes `INITIAL_BURN_NOT_CONFIRMED` and identical policy/burn heads before and after reopen.
    - `BurnV2RejectedUntilInitialBurnPeerConfirmed` proves an otherwise valid peer-quorum burn-v2 candidate receives `INITIAL_BURN_NOT_CONFIRMED`, performs no write, and leaves the byte-identical burn-v1/value-100 and policy heads classified BootstrapOnly after reopen.
    - The tests observe `BootstrapOnly` before the exact same-version peer-proof replacement, `PeerQuorum` after it, and successful policy-v2 and burn-v2 commits only in the latter state.
    - `rg -n "INITIAL_BURN_NOT_CONFIRMED|BurnAuthorizationKind|PeerQuorum" src/trustedpeer/TrustStateStore.{hpp,cpp} test/src/trustedpeer/trust_state_store_test.cpp` finds the public state, durable guard, and assertions.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>There is no durable path to a policy successor or burn-v2 successor until deterministic burn v1/value 100 has current-peer quorum authorization, and failed attempts cannot change restart authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy/burn successor candidate to durable heads | A valid current-policy proof still must not bypass required initial economic sequencing. |
| Persisted burn proof to readiness classification | Bootstrap identity proof and current-peer economic authorization have different authority. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G01 | Tampering/Elevation of Privilege | HIGH | `TrustStateStore::CommitPolicySuccessor` and `CommitBurnSuccessor` | mitigate | Reload under the transition mutex; require verified `PeerQuorum` burn authority before policy/burn-v2 writes, permitting only exact same-version burn-v1/value-100 peer-proof replacement while BootstrapOnly. | `trust_state_store_test --gtest_filter='*PolicySuccessorRejectedUntilInitialBurnPeerConfirmed*:*BurnV2RejectedUntilInitialBurnPeerConfirmed*'` |
| T13-G02 | Denial of Service | HIGH | initial trust-state sequence | mitigate | Prove both rejected successors leave identical restart heads, then the exact burn-v1 peer-proof replacement unlocks normal policy-v2 and burn-v2 commits. | Both named counterexamples' rejection, reopen, and recovery assertions |
| T-13-SC | Tampering | LOW | package supply chain | accept | No package install or dependency change occurs. | Changed-file/CMake graph inspection |
</threat_model>

<verification>
Run both focused counterexamples, then `build/OSX/Release/test_bin/trust_state_store_test` to preserve all existing durable-store contracts.
</verification>

<success_criteria>
- CR-01 is closed at both authoritative successor write entry points.
- Bootstrap-only and peer-confirmed burn states are explicit and restart-stable.
- No policy or burn write occurs on either rejected bypass path.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-13-SUMMARY.md` when done.
</output>
