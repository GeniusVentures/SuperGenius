---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 07
type: execute
wave: 7
depends_on: [13-05, 13-09]
files_modified:
  - src/account/TrustStartupController.hpp
  - src/account/TrustStartupController.cpp
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - src/account/CMakeLists.txt
  - test/src/startup/trust_first_boot_e2e_test.cpp
  - test/src/startup/trust_restart_test.cpp
  - test/src/startup/trust_tamper_e2e_test.cpp
  - test/src/startup/CMakeLists.txt
autonomous: true
requirements: [BOOT-03, BOOT-04, POLICY-01, SCRDT-04, TPR-01, BURN-01, BURN-03, TEST-01]
must_haves:
  truths:
    - "D-13: a fresh node starts networking/CRDT in `WAITING_FOR_TRUST_GENESIS`, has no active configured quorum, cannot approve successors, and cannot execute BurnConfig-dependent economics."
    - "D-03/D-13: the real `GlobalDbNetworkComposition`/`sgns-trust` path drives a fresh running node to durable TPR confirmation, success-only key removal, `WAITING_FOR_BURN_GENESIS`, and then economic readiness after BurnConfig v1 quorum."
    - "D-14: a valid persisted snapshot is restart authority; conflicting peer/bootstrapper/quorum JSON is ignored with a critical diagnostic, while network-ID mismatch fails startup."
    - "D-15: missing, older, corrupt, or fork CRDT data raises an alert and cannot erase or replace persisted LKG state."
  artifacts:
    - path: "src/account/TrustStartupController.hpp"
      provides: "Fresh/restart/mismatch/rollback startup state machine isolated from GeniusNode orchestration"
    - path: "test/src/startup/trust_tamper_e2e_test.cpp"
      provides: "Altered JSON/manifest/bootstrapper/threshold/rollback/fork multi-node evidence"
  key_links:
    - from: "src/account/GeniusNode.cpp"
      to: "src/account/TrustStartupController.cpp"
      via: "startup dispatch after GlobalDB networking exists and before economic services start"
      pattern: "TrustStartupController"
    - from: "src/account/TrustStartupController.cpp"
      to: "src/trustedpeer/TrustStateStore.cpp"
      via: "verified durable snapshot selects fresh versus restart authority"
      pattern: "LoadAndVerify"
---

<objective>
Integrate confirmed trust into GeniusNode startup as an explicit fresh/restart state machine with restricted first boot, persisted authority, and rollback-safe synchronization.

Purpose: Ensure production nodes no longer activate configured peers or economic behavior before confirmed genesis and cannot be redefined by mutable JSON after restart.
Output: Thin startup controller, GeniusNode wiring, first-boot/restart/tamper tests, and build integration.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VALIDATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-06-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement restricted first-boot state transitions</name>
  <files>src/account/TrustStartupController.hpp, src/account/TrustStartupController.cpp, src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, src/account/CMakeLists.txt, test/src/startup/trust_first_boot_e2e_test.cpp, test/src/startup/CMakeLists.txt</files>
  <read_first>
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/account/BurnConfig.hpp
    - src/crdt/globaldb/GlobalDbNetworkComposition.hpp
    - src/trustedpeer/genesis_tool/GenesisCeremony.hpp
    - test/src/startup/startup_wiring_test.cpp
    - test/src/blockchain/node_startup_test.cpp
    - src/account/CMakeLists.txt
    - test/src/startup/CMakeLists.txt
  </read_first>
  <behavior>
    - No local trust store yields networking/GlobalDB alive plus `WAITING_FOR_TRUST_GENESIS`, empty current peers, denied approvals, and denied economic readiness.
    - A real `GlobalDbNetworkComposition` running `GenesisCeremony`/`sgns-trust` submits into a fresh running node's production CRDT keyspace; durable confirmation transitions to `WAITING_FOR_BURN_GENESIS` and removes the 0600 key file only on success.
    - Confirmed deterministic Burn v1/value 100 transitions through account-service initialization to READY/economic-ready.
  </behavior>
  <action>Write and register `TrustStartupController.cpp` plus `trust_first_boot_e2e_test` in the production/test CMake files before invoking either target. Add `TrustStartupController` with explicit outcomes/states `FreshWaitingForGenesis`, `WaitingForInitialBurn`, `ConfirmedReady`, and `FatalMismatch`; add corresponding observable GeniusNode states without blocking the io_context. Construct the node-scoped instance registry, SecureCrdt, TrustStateStore, TPR, and BurnConfig once after `tx_globaldb_` starts, before account TransactionManager creation. On fresh state, provide the validated reviewed manifest only to the genesis authorization filter; never populate active TPR peers/thresholds from JSON. The E2E must launch a fresh running node and a separate real `GlobalDbNetworkComposition`, invoke `GenesisCeremony` through the same service used by `sgns-trust` with no injected networking or confirmation seam, submit into the production candidate keyspace, observe the node's durable TPR record and `WAITING_FOR_BURN_GENESIS` transition, and assert success-only key-file removal. Then drive real BurnConfig version-one approvals to READY. Keep networking/sync alive while waiting and forbid approval/economic paths via typed not-ready results.</action>
  <verify><automated>cmake --build build/OSX/Release --target trust_first_boot_e2e_test genius_node -j8 &amp;&amp; build/OSX/Release/test_bin/trust_first_boot_e2e_test</automated></verify>
  <acceptance_criteria>
    - Test observes network/GlobalDB running during both waiting states, empty active peers before genesis, and no Burn readiness before burn quorum.
    - Configured peers cannot propose/approve while fresh; exact manifest/bootstrap genesis can confirm.
    - The real tool composition reaches the fresh node's production keyspace, persists the identical fingerprint/policy head, transitions out of `WAITING_FOR_TRUST_GENESIS`, and removes the protected key file only after that durable confirmation.
    - READY/economic readiness occurs only after persisted TPR plus persisted Burn v1/value 100.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Fresh production startup synchronizes safely but remains trust/economic restricted until both durable genesis stages complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce persisted restart authority and tamper behavior</name>
  <files>src/account/TrustStartupController.cpp, src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/startup/trust_restart_test.cpp, test/src/startup/trust_tamper_e2e_test.cpp, test/src/startup/CMakeLists.txt</files>
  <read_first>
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/trustedpeer/GenesisManifest.hpp
    - test/src/startup/startup_wiring_test.cpp
    - example/node_test/sgns_config.json
    - test/src/startup/trust_restart_test.cpp
    - test/src/startup/trust_tamper_e2e_test.cpp
    - test/src/startup/CMakeLists.txt
  </read_first>
  <behavior>
    - Valid persisted state reaches confirmed readiness without configured trust fields.
    - Changed peers, bootstrapper, membership threshold, or burn threshold emit a critical diagnostic and are ignored; network-ID mismatch fails.
    - Manifest-byte tamper, older/missing CRDT head, rollback, and fork never replace LKG; corrupt local store fails closed rather than falling back to JSON/rebootstrap.
  </behavior>
  <action>Register `trust_restart_test` and `trust_tamper_e2e_test` before invoking them. On restart call `LoadAndVerify` before exposing policy. Compare legacy/bootstrap diagnostic JSON and optional manifest reference to persisted identity: log one structured critical event naming mismatched fields and persisted fingerprint, continue with persisted state for trust-field conflict, but return fatal startup error on network mismatch or corrupt local trust store. During CRDT sync, validate every candidate against durable ancestry; alert on missing/older/fork data while keeping LKG caches. Never delete/reset the store or re-enter bootstrap because replicated state is absent. Tests must capture log/event codes, not depend on prose.</action>
  <verify><automated>cmake --build build/OSX/Release --target trust_restart_test trust_tamper_e2e_test -j8 &amp;&amp; build/OSX/Release/test_bin/trust_restart_test &amp;&amp; build/OSX/Release/test_bin/trust_tamper_e2e_test</automated></verify>
  <acceptance_criteria>
    - Restart succeeds with trust fields omitted and loads identical fingerprint/policy/burn heads.
    - Each individual trust-field mutation produces `TRUST_CONFIG_CONFLICT` and unchanged current hashes; network mutation produces fatal `TRUST_NETWORK_MISMATCH`.
    - Tamper suite covers manifest bytes, bootstrap replacement, lowered/oversized thresholds, missing/old/fork CRDT state, and corrupt local state with exact expected outcomes.
    - No test accepts JSON fallback after a local trust record has existed.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Restart trust comes only from verified persisted state, with exact conflict/fatal distinctions and rollback-safe LKG preservation.</done>
</task>

<task type="auto">
  <name>Task 3: Wire startup targets and run the first-boot/restart gate</name>
  <files>src/account/CMakeLists.txt, test/src/startup/CMakeLists.txt</files>
  <read_first>
    - src/account/CMakeLists.txt
    - test/src/startup/CMakeLists.txt
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VALIDATION.md
  </read_first>
  <action>Audit the production/test wiring completed in Tasks 1 and 2 against the existing platform whole-archive/force-load block, correcting incomplete links before running core trust plus startup tests. Do not defer target registration to this task; do not add skips or sleeps.</action>
  <verify><automated>ctest --test-dir build/OSX/Release --output-on-failure -R 'trust_first_boot|trust_restart|trust_tamper|node_startup|startup_wiring'</automated></verify>
  <acceptance_criteria>
    - CTest discovers all three exact new targets.
    - All new and existing startup targets pass.
    - `rg -n "sleep_for|GTEST_SKIP|DISABLED_" test/src/startup/trust_*` returns zero matches.
  </acceptance_criteria>
  <done>Restricted boot, confirmed restart, and tamper behavior compile and pass the production startup gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutable startup config to active trust | Operator/deployment JSON must not override confirmed authority. |
| Replicated CRDT state to persisted LKG | Network state attempts to advance, fork, or roll back local trust. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-03 | Spoofing | HIGH | fresh genesis ingress | mitigate | Only reviewed manifest bootstrapper/fingerprint may cross the fresh authorization filter. | `trust_first_boot_e2e_test` real network genesis path and `trust_tamper_e2e_test` |
| T13-07 | Tampering | HIGH | restart/sync | mitigate | Verified LKG stays active; older/missing/fork CRDT data alerts and cannot overwrite. | `trust_restart_test` and `trust_tamper_e2e_test` |
| T13-08 | Tampering | HIGH | mutable JSON | mitigate | Ignore trust conflicts with structured critical event; fail network mismatch; no fallback. | `trust_tamper_e2e_test` per-field mutation cases |
| T13-11 | Tampering | HIGH | corrupt local state | mitigate | Load-and-verify fatal failure; never rebootstrap from JSON. | `trust_restart_test` corrupt-store case |
| T-13-SC | Tampering | LOW | package supply chain | accept | No new packages. | CMake build graph inspection |
</threat_model>

<verification>
Run `ctest --test-dir build/OSX/Release --output-on-failure -R 'genesis_manifest|trust_state_store|trust_first_boot|trust_restart|trust_tamper|startup'`.
</verification>

<success_criteria>
- Fresh networking is available without granting policy/economic authority.
- Confirmed TPR and Burn v1 state are durable prerequisites for economic readiness.
- Persisted state overrides conflicting trust JSON; only network mismatch/corruption fails startup.
- CRDT rollback/fork/missing data never erases LKG.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-07-SUMMARY.md` when done.
</output>
