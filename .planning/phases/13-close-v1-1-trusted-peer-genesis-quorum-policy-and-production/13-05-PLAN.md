---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 05
type: execute
wave: 5
depends_on: [13-02, 13-04]
files_modified:
  - src/trustedpeer/TrustedPeerRegistry.hpp
  - src/trustedpeer/TrustedPeerRegistry.cpp
  - src/account/BurnConfig.hpp
  - src/account/BurnConfig.cpp
  - test/src/trustedpeer/operator_approval_test.cpp
  - test/src/securecrdt/securecrdt_candidate_race_test.cpp
  - test/src/account/burnconfig_policy_e2e_test.cpp
  - test/src/trustedpeer/CMakeLists.txt
  - test/src/account/CMakeLists.txt
autonomous: true
requirements: [BOOT-03, POLICY-01, SCRDT-04, TPR-01, TPR-02, BURN-01, BURN-02, BURN-03, TEST-01]
must_haves:
  truths:
    - "D-05/D-06/D-07/D-08: TPR and Burn activation always evaluate approvals under the durably confirmed current policy and exact policy-specific threshold."
    - "D-09/D-10/D-11: production APIs surface candidates without auto-signing and require an explicit local propose/approve action for every non-genesis successor."
    - "D-12/D-15: one transition mutex and durable predecessor compare permit exactly one concurrent successor; losers remain stale and never overwrite the LKG cache."
    - "D-13: only after durable TPR genesis do initial trusted peers auto-approve the single deterministic BurnConfig v1/value 100 candidate; no later version is auto-approved."
  artifacts:
    - path: "src/trustedpeer/TrustedPeerRegistry.hpp"
      provides: "Genesis, candidate inbox, explicit policy approval, and durable activation service"
    - path: "src/account/BurnConfig.hpp"
      provides: "Versioned burn proposal/approval/activation and node-scoped confirmed cache"
    - path: "test/src/securecrdt/securecrdt_candidate_race_test.cpp"
      provides: "Exactly-one durable winner and stale loser proof"
  key_links:
    - from: "src/trustedpeer/TrustedPeerRegistry.cpp"
      to: "src/trustedpeer/TrustStateStore.cpp"
      via: "commit verified successor before updating current peer/policy cache"
      pattern: "Commit.*Successor"
    - from: "src/account/BurnConfig.cpp"
      to: "src/trustedpeer/TrustedPeerRegistry.cpp"
      via: "current confirmed policy supplies burn signers, threshold, and authorizing hash"
      pattern: "CurrentPolicy|authorizing_policy"
---

<objective>
Turn the authenticated candidate transport into production TPR and BurnConfig operations with explicit approval, current-policy quorum, deterministic genesis sequencing, and persist-before-cache activation.

Purpose: Close the audit's orphaned production mutation flow while preserving CRDT as the only transport and forbidding self-authorization or speculative cache changes.
Output: TPR/Burn candidate APIs, operator inbox hooks, atomic activation, deterministic Burn v1 confirmation, and component/race tests.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VALIDATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-02-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate trusted-peer genesis and policy successors under durable current policy</name>
  <files>src/trustedpeer/TrustedPeerRegistry.hpp, src/trustedpeer/TrustedPeerRegistry.cpp, test/src/trustedpeer/operator_approval_test.cpp, test/src/securecrdt/securecrdt_candidate_race_test.cpp, test/src/trustedpeer/CMakeLists.txt</files>
  <read_first>
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/TrustedPeerRegistry.cpp
    - src/trustedpeer/GenesisManifest.hpp
    - src/trustedpeer/QuorumPolicy.hpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/securecrdt/SecureCrdtCandidate.hpp
    - src/securecrdt/SecureCrdt.hpp
    - test/src/trustedpeer/trustedpeerregistry_genesis_test.cpp
    - test/src/trustedpeer/trustedpeerregistry_quorum_test.cpp
    - test/src/trustedpeer/operator_approval_test.cpp
    - test/src/securecrdt/securecrdt_candidate_race_test.cpp
    - test/src/trustedpeer/CMakeLists.txt
  </read_first>
  <behavior>
    - Fresh construction exposes no active peers/policy until a reviewed bootstrapper-signed genesis candidate commits durably.
    - Receiving any candidate only records/surfaces it; `ProposePolicyCandidate` and `ApprovePolicyCandidate` are the only signing call paths and each signer is deduplicated.
    - `TryActivatePolicyCandidate` re-reads durable current policy, validates exact successor links, derives current-policy quorum, commits, reloads, then updates cache/callbacks.
    - Two quorum candidates for one predecessor yield one durable winner and a permanently stale loser, including after reopen.
  </behavior>
  <action>Register `operator_approval_test` in `test/src/trustedpeer/CMakeLists.txt` before invoking it. Replace the pre-confirmation configured-peer cache and legacy single-slot production path with candidate APIs `SubmitReviewedGenesisApproval`, `ListPendingPolicyCandidates`, `ProposePolicyCandidate`, `ApprovePolicyCandidate`, and `TryActivatePolicyCandidate`. Genesis authorization is the exact bootstrapper and fingerprint from a previously validated reviewed manifest, threshold 1; on success call `TrustStateStore::CommitGenesis` before exposing peers. For successors, load the current snapshot, require D-08 version/predecessor/authorizer links, evaluate unique approvals using current peers and current membership threshold, and call `CommitPolicySuccessor` before publishing the cache. The proposer method accepts already explicit operator intent, signs exact bytes, and submits its first approval; callback reception never calls it. Extend the race test to exercise the store-backed transition mutex and durable winner.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target operator_approval_test securecrdt_candidate_race_test trustedpeer -j8 &amp;&amp; build/OSX/Release/test_bin/operator_approval_test &amp;&amp; build/OSX/Release/test_bin/securecrdt_candidate_race_test</automated>
  </verify>
  <acceptance_criteria>
    - Before genesis commit, `GetCurrentPeers()` is empty and successor propose/approve/activate returns a not-confirmed error.
    - A receive-only test observes candidate count change and zero signer invocations; explicit proposal contributes exactly one approval and repeated approval stays one.
    - New peers alone cannot authorize their own candidate; wrong predecessor/version/policy hash never reaches the store.
    - Race test asserts one activated, one stale, one durable version/hash after reopen, and no cache publish before commit.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>TPR genesis and successors activate only through durable current-authority quorum, with explicit non-genesis approval and one race-safe winner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate BurnConfig successors and deterministic version-one genesis</name>
  <files>src/account/BurnConfig.hpp, src/account/BurnConfig.cpp, test/src/account/burnconfig_policy_e2e_test.cpp, test/src/account/CMakeLists.txt</files>
  <read_first>
    - src/account/BurnConfig.hpp
    - src/account/BurnConfig.cpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/TrustStateStore.hpp
    - src/securecrdt/SecureCrdtCandidate.hpp
    - src/account/TransactionManager.hpp
    - test/src/account/burnconfig_test.cpp
    - test/src/account/burnconfig_policy_e2e_test.cpp
    - test/src/account/CMakeLists.txt
  </read_first>
  <behavior>
    - Before durable TPR genesis, no burn signer set, candidate, or confirmed-cache readiness exists.
    - After durable TPR genesis, each initial trusted-peer node may automatically submit at most one approval for the identical BurnConfig version 1/value 100 candidate bound to that genesis policy.
    - Economic readiness becomes true only after the v1 candidate reaches the current burn threshold and commits durably.
    - Version 2+ values require explicit local propose/approve; any unfinished candidate bound to an old policy hash becomes stale after a membership transition.
  </behavior>
  <action>Register `burnconfig_policy_e2e_test` in `test/src/account/CMakeLists.txt` before invoking it. Replace constructor-time auto-seeding from unconfirmed peers with `OnTrustedPeerGenesisConfirmed`. Derive the version-one burn predecessor from a domain-separated `BurnGenesisAnchorHash(genesis_fingerprint)`, set `version=1`, `value=100`, and `authorizing_policy_hash=genesis_policy_hash`; initial peer nodes auto-sign only this exact tuple and remember submission idempotently. Add `ListPendingBurnCandidates`, `ProposeBurnCandidate`, `ApproveBurnCandidate`, `TryActivateBurnCandidate`, `IsEconomicallyReady`, and a shared node-scoped confirmed-value provider. Validate 0..10000 basis points, current burn predecessor, and current policy hash; use the current policy's burn threshold. Call `CommitBurnSuccessor`, reload, then publish the atomic/cache and callbacks. A changed policy hash must stale all unfinished old-policy burn candidates, preventing old/new signer mixing per T13-09.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target burnconfig_policy_e2e_test burnconfig_test -j8 &amp;&amp; build/OSX/Release/test_bin/burnconfig_policy_e2e_test --gtest_filter='*Genesis*:*PolicyBinding*:*PersistBeforeCache*'</automated>
  </verify>
  <acceptance_criteria>
    - Tests observe no pre-TPR burn candidate or ready cache, identical v1 candidate hashes across peer order/nodes, and readiness only after exact two-thirds-or-higher quorum.
    - A v2 candidate gets zero automatic approvals; explicit propose adds one and explicit peer approvals are deduplicated.
    - Old-policy-only and mixed-old/new signatures cannot confirm after policy transition.
    - Injected store failure leaves the previous burn value/readiness/callback count unchanged.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>BurnConfig has a deterministic quorum-confirmed v1/value 100 sequence and explicit, current-policy-bound later updates with durable-before-cache publication.</done>
</task>

<task type="auto">
  <name>Task 3: Wire targets and run the policy activation gate</name>
  <files>test/src/trustedpeer/CMakeLists.txt, test/src/account/CMakeLists.txt</files>
  <read_first>
    - test/src/trustedpeer/CMakeLists.txt
    - test/src/account/CMakeLists.txt
    - src/trustedpeer/CMakeLists.txt
    - src/account/CMakeLists.txt
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VALIDATION.md
  </read_first>
  <action>Audit the registrations created in Tasks 1 and 2 against existing `addtest`, link, and platform whole-archive/force-load conventions, correcting any incomplete wiring before running the combined codec/store/candidate/TPR/Burn gate. Do not defer target registration to this task and do not weaken or disable legacy tests.</action>
  <verify>
    <automated>ctest --test-dir build/OSX/Release --output-on-failure -R 'genesis_manifest|quorum_policy|trust_state_store|securecrdt_candidate|operator_approval|trustedpeerregistry|burnconfig'</automated>
  </verify>
  <acceptance_criteria>
    - CTest discovers both new targets by exact name.
    - All focused Phase 13 core and existing TPR/Burn tests pass.
    - No `DISABLED_`, `GTEST_SKIP`, fixed sleeps, or reduced assertion path was added to make the gate green.
  </acceptance_criteria>
  <done>The complete non-node trust-policy stack builds and passes its focused unit/integration gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Candidate approvals to active policy/burn cache | Replicated approvals attempt to change authoritative node behavior. |
| TPR genesis to initial BurnConfig | Confirmed membership becomes authority for the economic genesis value. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-04 | Tampering | HIGH | explicit approval | mitigate | Exact-byte signature, deduplication, and no signing from receive callback. | `operator_approval_test` |
| T13-05 | Elevation of Privilege | HIGH | policy activation | mitigate | Persisted current policy supplies signer set/threshold; exact successor/floor validation. | `operator_approval_test` self-authorization cases and `quorum_policy_test` |
| T13-06 | Tampering/Repudiation | HIGH | concurrent activation | mitigate | Store transition mutex, durable predecessor compare, persist-before-cache, structured outcomes. | `securecrdt_candidate_race_test` |
| T13-09 | Elevation/Tampering | HIGH | burn across policy change | mitigate | Burn candidate binds current authorizing policy hash and becomes stale on transition. | `burnconfig_policy_e2e_test --gtest_filter='*PolicyBinding*'` |
| T13-12 | Denial of Service | MEDIUM | candidate inbox | mitigate | Plan 04 caps plus stale isolation; winner activation stops old-predecessor eligibility. | `securecrdt_candidate_test --gtest_filter='*Bounds*:*Limit*'` |
| T-13-SC | Tampering | LOW | package supply chain | accept | No external packages. | CMake build graph inspection |
</threat_model>

<verification>
Run the Plan 05 gate plus `ctest --test-dir build/OSX/Release --output-on-failure -R 'securecrdt|trustedpeer|burnconfig'`.
</verification>

<success_criteria>
- TPR genesis and policy successors commit before active signer caches change.
- Candidate receipt never signs; only explicit non-genesis local operations create approvals.
- Burn v1/value 100 begins only after durable TPR genesis and reaches readiness only at current burn quorum.
- Concurrent candidates yield one durable winner and stale losers.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-05-SUMMARY.md` when done.
</output>
