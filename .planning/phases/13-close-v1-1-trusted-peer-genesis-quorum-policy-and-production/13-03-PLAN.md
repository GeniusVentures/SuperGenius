---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/securecrdt/SecureCrdtRegistry.hpp
  - src/securecrdt/SecureCrdt.hpp
  - src/securecrdt/SecureCrdt.cpp
  - src/trustedpeer/TrustedPeerRegistry.cpp
  - src/account/BurnConfig.cpp
  - test/src/securecrdt/securecrdt_registry_test.cpp
  - test/src/securecrdt/securecrdt_quorum_fixture.hpp
  - test/src/securecrdt/securecrdt_quorum_gate_test.cpp
autonomous: true
requirements: [SCRDT-04, BURN-02, TEST-01]
must_haves:
  truths:
    - "D-16: every `SecureCrdt` owns an independent policy registry; two nodes in one process cannot replace or resolve each other's signer sources."
    - "D-16: existing legacy SecureCrdt value/signature behavior remains green while production owners register through their node's registry instance."
  artifacts:
    - path: "src/securecrdt/SecureCrdtRegistry.hpp"
      provides: "Thread-safe instance-scoped Register, UnregisterIf, Resolve, and AllEntries"
      exports: ["SecureCrdtRegistry"]
    - path: "src/securecrdt/SecureCrdt.hpp"
      provides: "Ownership/accessor for one registry per SecureCrdt instance"
      exports: ["SecureCrdt::Registry"]
  key_links:
    - from: "src/trustedpeer/TrustedPeerRegistry.cpp"
      to: "src/securecrdt/SecureCrdt.hpp"
      via: "register signer source on owning SecureCrdt registry"
      pattern: "secure_crdt_.*Registry"
    - from: "src/account/BurnConfig.cpp"
      to: "src/securecrdt/SecureCrdt.hpp"
      via: "register burn policy on the same node-scoped registry"
      pattern: "secure_crdt_.*Registry"
---

<objective>
Remove the process-global SecureCrdt policy registry and migrate all existing owners/tests onto instance-scoped registration without changing legacy quorum behavior.

Purpose: Eliminate cross-node signer-source replacement and establish the node ownership boundary required before production candidate callbacks are added.
Output: Instance registry, SecureCrdt ownership, migrated TPR/Burn registrations, and two-node isolation regression coverage.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/09-securecrdt-layer/09-01-SUMMARY.md
@.planning/phases/09-securecrdt-layer/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert SecureCrdtRegistry to per-instance state</name>
  <files>src/securecrdt/SecureCrdtRegistry.hpp, src/securecrdt/SecureCrdt.hpp, src/securecrdt/SecureCrdt.cpp, test/src/securecrdt/securecrdt_registry_test.cpp</files>
  <read_first>
    - src/securecrdt/SecureCrdtRegistry.hpp
    - src/securecrdt/SecureCrdt.hpp
    - src/securecrdt/SecureCrdt.cpp
    - test/src/securecrdt/securecrdt_registry_test.cpp
    - src/crdt/impl/crdt_callback_manager.cpp
  </read_first>
  <behavior>
    - Registries A and B may register the same pattern with different signer sources and resolve only their own entry.
    - `UnregisterIf` removes only a matching owner token from the same registry.
    - Existing exact base and `sig/address` matching remains segment-aware and thread-safe.
  </behavior>
  <action>Write the two-registry isolation test first. Replace function-local static map/mutex storage with ordinary `registry_` and `registry_mutex_` members and non-static `Register`, `UnregisterIf`, `Resolve`, and `AllEntries`. Make `SecureCrdt` own a `std::shared_ptr&lt;SecureCrdtRegistry&gt;`, accept an optional injected registry in its constructor for tests/composition, create a fresh registry when omitted, and expose `Registry()` by reference or shared pointer. Route every existing `SecureCrdt.cpp` lookup/enumeration through that owned registry. Preserve the legacy regex contract during this refactor; candidate segment parsing is Plan 04.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target securecrdt_registry_test securecrdt -j8 &amp;&amp; build/OSX/Release/test_bin/securecrdt_registry_test</automated>
  </verify>
  <acceptance_criteria>
    - The isolation test registers identical patterns in two registries and observes distinct signer snapshots with no replacement.
    - `rg -n "static .*registry|function-local|static std::unordered_map" src/securecrdt/SecureCrdtRegistry.hpp` finds no process-global storage.
    - Existing thread-safety and owner-token tests pass.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Every SecureCrdt instance owns an isolated, thread-safe registry and legacy lookups use it.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate production policy owners to their SecureCrdt registry</name>
  <files>src/trustedpeer/TrustedPeerRegistry.cpp, src/account/BurnConfig.cpp</files>
  <read_first>
    - src/securecrdt/SecureCrdt.hpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/TrustedPeerRegistry.cpp
    - src/account/BurnConfig.hpp
    - src/account/BurnConfig.cpp
  </read_first>
  <action>Change `TrustedPeerRegistry::RegisterSignerSetSource`/`Unregister` and `BurnConfig::RegisterSignerSetSource`/`Unregister` to call the registry owned by `secure_crdt_`. Keep owner-token compare-and-remove and weak signer-source captures. Check/propagate registration outcomes using the existing factory/error pattern; do not silently replace an existing same-node owner. Remove all production `SecureCrdtRegistry::` static calls. Do not alter pre-confirmation signer semantics or BurnConfig seeding in this plan; Plans 05 and 07 change those behaviors after the canonical/store contracts exist.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target trustedpeer genius_node -j8 &amp;&amp; ctest --test-dir build/OSX/Release --output-on-failure -R 'trustedpeerregistry|burnconfig'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SecureCrdtRegistry::" src/trustedpeer src/account/BurnConfig.cpp` returns zero matches.
    - TPR and BurnConfig register/unregister against the exact `SecureCrdt` object passed to their factories.
    - Existing trustedpeerregistry and burnconfig tests pass unchanged in behavior.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Production TPR and BurnConfig registrations are isolated to their node's SecureCrdt instance without behavior regression.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate legacy fixtures and prove cross-node isolation</name>
  <files>test/src/securecrdt/securecrdt_quorum_fixture.hpp, test/src/securecrdt/securecrdt_quorum_gate_test.cpp, test/src/securecrdt/securecrdt_registry_test.cpp</files>
  <read_first>
    - test/src/securecrdt/securecrdt_quorum_fixture.hpp
    - test/src/securecrdt/securecrdt_quorum_gate_test.cpp
    - test/src/securecrdt/securecrdt_registry_test.cpp
    - test/src/securecrdt/securecrdt_test_node.hpp
    - src/securecrdt/SecureCrdt.hpp
  </read_first>
  <action>Replace static registry setup/teardown in all affected fixtures with each fixture's `secure_crdt_-&gt;Registry()`. Add a regression in which two in-process SecureCrdt instances register the same logical key with different authorized signers, process values/signatures independently, and retain their original registry entries after the peer fixture is destroyed. Use wait-condition helpers for asynchronous observations; do not add sleeps.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target securecrdt_registry_test securecrdt_quorum_gate_test securecrdt_propose_sign_quorum_test -j8 &amp;&amp; ctest --test-dir build/OSX/Release --output-on-failure -R 'securecrdt_registry|securecrdt_quorum_gate|securecrdt_propose_sign_quorum'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SecureCrdtRegistry::" test/src/securecrdt` returns zero matches.
    - Destroying one fixture neither removes nor changes the other fixture's same-pattern registration.
    - Legacy propose/sign/read quorum tests remain green.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>All existing fixtures use instance registries and prove that in-process nodes cannot collide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node A policy registry to Node B | In-process nodes must not share or replace authorization callbacks. |
| Owner lifetime to registry entry | Expired/different owners must not remove a live registration. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-03 | Spoofing/DoS | HIGH | registry signer source | mitigate | Per-SecureCrdt map and exact owner registration prevents another node from replacing the signer source. | `securecrdt_registry_test` two-registry isolation case |
| T13-10 | Denial of Service | MEDIUM | callback/owner lifecycle | mitigate | Node-owned registry plus compare-and-remove makes teardown local to one owner. | `securecrdt_registry_test` fixture-destruction regression |
| T-13-SC | Tampering | LOW | package supply chain | accept | No package install or dependency change. | CMake build graph inspection |
</threat_model>

<verification>
Run `ctest --test-dir build/OSX/Release --output-on-failure -R 'securecrdt|trustedpeer|burnconfig'`.
</verification>

<success_criteria>
- No process-global SecureCrdt registry remains.
- All production and test registrations bind to the correct SecureCrdt instance.
- Existing value/signature quorum behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-03-SUMMARY.md` when done.
</output>
