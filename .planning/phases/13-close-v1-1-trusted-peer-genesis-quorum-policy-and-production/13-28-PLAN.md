---
phase: 13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production
plan: 28
type: execute
wave: 2
depends_on: [13-27]
files_modified:
  - src/account/TrustStartupController.hpp
  - src/account/TrustStartupController.cpp
  - src/account/GeniusNode.cpp
  - test/src/startup/trust_first_boot_e2e_test.cpp
autonomous: true
gap_closure: true
requirements: [POLICY-01, TPR-02, BURN-03, TEST-01]
must_haves:
  truths:
    - "CR-13/D-08/D-12: one transient policy or burn listing failure emits a typed retry event and retries with bounded exponential backoff until the retained quorum is durably activated, without another CRDT write or operator action."
    - "Actionable candidate validation/commit failures retain candidate-scoped suppression and never enter the infrastructure retry loop; one retry cycle is exactly one initial attempt plus six scheduled retries (seven total attempts), and repeated requests during that cycle are coalesced rather than spinning."
    - "CR-14/D-16: queued refresh jobs own only independent dispatch state plus a weak controller reference; a job holds one strong controller only for Refresh, and a synchronous state/event callback may release the last external controller owner safely."
    - "Controller destruction cancels future retries and callbacks without joining/detaching a controller-owned thread, and queued work touches no controller member after final release."
  artifacts:
    - path: "src/account/TrustStartupController.hpp"
      provides: "Typed refresh stage/disposition, retry policy, and independently owned RefreshDispatchState contract"
    - path: "src/account/TrustStartupController.cpp"
      provides: "System-executor serialized refresh, typed retry events, bounded backoff, and owner-safe cancellation"
    - path: "src/account/GeniusNode.cpp"
      provides: "Operational labels for refresh retry scheduled/exhausted events"
    - path: "test/src/startup/trust_first_boot_e2e_test.cpp"
      provides: "No-new-write policy/burn retry and callback final-owner-release regressions"
  key_links:
    - from: "ListPendingPolicyCandidates/ListPendingBurnCandidates failure"
      to: "RefreshDispatchState"
      via: "typed transient RefreshAttempt and coalesced capped-backoff scheduling"
      pattern: "RefreshStage|RetryDisposition|RETRY_SCHEDULED"
    - from: "SecureCrdt callback"
      to: "TrustStartupController::Refresh"
      via: "boost::asio::system_executor job locking a weak controller only for one attempt"
      pattern: "system_executor|weak_ptr|RefreshDispatchState"
---

<objective>
Close CR-13 and CR-14 by replacing the raw controller worker with an owner-safe serialized dispatcher that observes and retries transient discovery failures.

Purpose: Preserve passive quorum liveness and callback safety without reentering GlobalDB delivery, spinning on permanent failures, or joining a worker from itself.
Output: Typed retry policy/events, independently owned dispatch state, and four deterministic regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-CONTEXT.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-RESEARCH.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-PATTERNS.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-REVIEW.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-VERIFICATION.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-24-SUMMARY.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-27-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — prove no-write transient recovery and callback final release</name>
  <files>src/account/TrustStartupController.hpp, src/account/TrustStartupController.cpp, test/src/startup/trust_first_boot_e2e_test.cpp</files>
  <read_first>
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/TrustedPeerRegistry.cpp
    - src/account/BurnConfig.hpp
    - src/account/BurnConfig.cpp
    - src/securecrdt/SecureCrdt.hpp
    - src/securecrdt/SecureCrdt.cpp
    - src/trustedpeer/TrustStateStore.hpp
    - test/src/startup/trust_first_boot_e2e_test.cpp
  </read_first>
  <behavior>
    - `TrustFirstBootE2ETest.TransientPolicyListingRetriesWithoutNewWrite` retains a quorum-approved current-head policy, injects exactly one retryable policy-list failure on the refresh caused by the final approval, observes a typed stage/error/attempt/delay event, performs no write/admin/direct activation, and reaches the exact durable successor on the retry.
    - `TrustFirstBootE2ETest.TransientBurnListingRetriesWithoutNewWrite` proves the same one-failure/one-success sequence for a retained burn successor and durable confirmed provider.
    - `TrustFirstBootE2ETest.TransientRefreshRetryExhaustionIsCappedCoalescedAndIdle` keeps policy listing transiently failing, proves one initial attempt plus exactly six retries at 100/200/400/800/1600/3200ms, coalesces duplicate requests during that cycle, observes one `TRUST_REFRESH_RETRY_EXHAUSTED`, and proves the dispatcher becomes idle with no eighth attempt or write.
    - `TrustFirstBootE2ETest.WorkerCallbackCanReleaseLastControllerOwnerSafely` resets the last external controller shared_ptr from a worker-delivered state or event callback, then proves weak expiration and dispatch drain with no terminate, detach, deadlock, or post-destruction access.
  </behavior>
  <action>RED first: add the four exact cases. Add only the compile-enabling test seam before the RED run: a narrow optional `RefreshTestHooks`/options input at the end of `New` that wraps the default policy and burn listing functions, observes dispatch attempts/dispatcher-idle state, and can drive a deterministic virtual retry-delay hook. Its production defaults must call the real registry/BurnConfig methods and real steady timers, and this RED commit must not add result inspection, retry scheduling, lifetime repair, or otherwise change the current ignored-result/raw-worker behavior. Surround the recovery tests' interval from final approval through durable convergence with exact `TRANSIENT_POLICY_NO_WRITE_BEGIN/END` or `TRANSIENT_BURN_NO_WRITE_BEGIN/END` comments. Surround the exhaustion cycle with `TRANSIENT_EXHAUSTION_NO_WRITE_BEGIN/END`. Inside those windows perform zero proposal, approval, raw Put, callback injection, LocalTrustAdmin operation, direct `TryActivate*`, or manual `Refresh`; wait on events/condition variables rather than adding a new CRDT stimulus. Each recovery body records `observed_attempts` and `observed_delays`, asserts exact `[1,2]` and `[100]`, and asserts the `LoadAndVerify` result equals its named `expected_successor_hash`. In the exhaustion case make the policy-list hook return seven consecutive transient failures and record exact named observations: `observed_attempts`, `observed_delays`, `retry_scheduled_events`, `retry_exhausted_events`, `coalesced_request_count`, `dispatcher_idle`, `attempt_count_after_idle`, and `write_count_after_exhaustion`. Issue at least two duplicate request notifications while the first attempt/retry is active and prove they coalesce into the same cycle. Require exact attempts `[1,2,3,4,5,6,7]`, exact scheduled delays `[100,200,400,800,1600,3200]`, exactly six scheduled events, exactly one exhausted event after attempt 7, idle state, unchanged attempt count 7 after draining the virtual timer/executor, and zero writes. In the owner-release case, bracket the actual dispatched callback with `DISPATCHED_LAST_OWNER_CALLBACK_BEGIN/END`, capture the external shared_ptr by reference, reset it inside that callback, then explicitly `EXPECT_TRUE` weak expiration and the `dispatch_drained` sentinel and fail on any later controller access. Give the four pre-fix assertions the respective diagnostic tokens `CR-13 policy transient retry missing`, `CR-13 burn transient retry missing`, `CR-13 retry exhaustion cap missing`, and `CR-14 callback owner unsafe`. Run each exact RED case independently with its own XML and require its reviewed diagnostic before production changes; commit the seam and RED tests separately from GREEN.</action>
  <verify>
    <automated>set -euo pipefail; red_dir="$(mktemp -d /tmp/phase13-28-red.XXXXXX)"; cmake --build build/OSX/Release --target trust_first_boot_e2e_test -j8; if build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='TrustFirstBootE2ETest.TransientPolicyListingRetriesWithoutNewWrite' --gtest_output="xml:$red_dir/policy.xml"; then exit 1; fi; if build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='TrustFirstBootE2ETest.TransientBurnListingRetriesWithoutNewWrite' --gtest_output="xml:$red_dir/burn.xml"; then exit 1; fi; if build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='TrustFirstBootE2ETest.TransientRefreshRetryExhaustionIsCappedCoalescedAndIdle' --gtest_output="xml:$red_dir/exhaustion.xml"; then exit 1; fi; if build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='TrustFirstBootE2ETest.WorkerCallbackCanReleaseLastControllerOwnerSafely' --gtest_output="xml:$red_dir/owner.xml"; then exit 1; fi; python3 -c 'import sys,xml.etree.ElementTree as E
expected=(("TrustFirstBootE2ETest.TransientPolicyListingRetriesWithoutNewWrite","CR-13 policy transient retry missing"),("TrustFirstBootE2ETest.TransientBurnListingRetriesWithoutNewWrite","CR-13 burn transient retry missing"),("TrustFirstBootE2ETest.TransientRefreshRetryExhaustionIsCappedCoalescedAndIdle","CR-13 retry exhaustion cap missing"),("TrustFirstBootE2ETest.WorkerCallbackCanReleaseLastControllerOwnerSafely","CR-14 callback owner unsafe"))
for path,(name,reason) in zip(sys.argv[1:],expected):
 cases=list(E.parse(path).getroot().iter("testcase")); assert len(cases)==1 and cases[0].get("classname")+"."+cases[0].get("name")==name,(path,name)
 failures=[x for x in cases[0] if x.tag in ("failure","error")]; assert failures,(path,"expected RED failure")
 evidence=" ".join((x.get("message") or "")+" "+(x.text or "") for x in failures); assert reason in evidence,(path,reason,evidence)
print("policy_red_reason=PASS burn_red_reason=PASS exhaustion_red_reason=PASS owner_red_reason=PASS")' "$red_dir/policy.xml" "$red_dir/burn.xml" "$red_dir/exhaustion.xml" "$red_dir/owner.xml"</automated>
  </verify>
  <acceptance_criteria>
    - All four exact cases exist once, have no disabled counterpart, and fail on the current ignored-result/raw-worker implementation.
    - Each transient case injects exactly one listing error and contains an exact marked no-write window through durable convergence.
    - The release case drops the final external owner from the actual dispatched callback, not from the test thread after work finishes.
    - No fixed sleep is the correctness oracle and no manual Refresh/new write rescues either retained candidate.
  </acceptance_criteria>
  <done>The tests deterministically expose silent transient-loss and raw-worker self-destruction/UAF paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — dispatch owner-safe refresh attempts with typed bounded retry</name>
  <files>src/account/TrustStartupController.hpp, src/account/TrustStartupController.cpp, src/account/GeniusNode.cpp, test/src/startup/trust_first_boot_e2e_test.cpp</files>
  <read_first>
    - src/account/TrustStartupController.hpp
    - src/account/TrustStartupController.cpp
    - src/account/GeniusNode.cpp
    - src/securecrdt/SecureCrdt.hpp
    - src/securecrdt/SecureCrdt.cpp
    - src/trustedpeer/TrustedPeerRegistry.hpp
    - src/trustedpeer/TrustedPeerRegistry.cpp
    - src/account/BurnConfig.hpp
    - src/account/BurnConfig.cpp
    - test/src/startup/trust_first_boot_e2e_test.cpp
  </read_first>
  <behavior>
    - Serialized callback work never runs inline on GlobalDB delivery and at most one immediate/retry attempt is queued for a controller.
    - Policy/burn listing infrastructure failures are retryable; candidate validation/commit failures remain actionable and suppressed per candidate.
    - Final controller release from a dispatched callback completes cleanly while state-only queued/timer callbacks safely observe cancellation.
  </behavior>
  <action>Replace `refresh_worker_`, its raw pointer capture, mutex/condition variable, stop flag, join, and any detach possibility with a shared `RefreshDispatchState` that contains only queue/retry/cancellation state, a weak controller reference, and executor/timer handles. Post serialized work on `boost::asio::system_executor` (already used in GeniusNode) so executor lifetime is independent of the controller and callback delivery never performs activation writes inline. A posted attempt may lock the weak controller once, hold that strong owner only across `Refresh`, and after `Refresh` returns access only dispatch state. Controller destruction first marks/cancels dispatch state and unregisters callbacks; it performs no thread join/detach and queued/timer work must no-op when stopped or weak-lock fails.

Split the private attempt result from the public `outcome::result<void>` into exact stage and disposition: policy-list and burn-list discovery/infrastructure failures are `Transient`; malformed/wrong-head/candidate activation/commit failures remain `Actionable` and keep existing candidate-scoped event/suppression; fatal durable state remains `Fatal`. Inspect every dispatched result. Define one retry cycle unambiguously as attempt 1 (the initial immediate attempt) plus retries 1..6 (overall attempts 2..7) after exact delays 100ms, 200ms, 400ms, 800ms, 1600ms, and 3200ms. After each of failures 1..6 emit one `TRUST_REFRESH_RETRY_SCHEDULED` carrying stage, error category/message, `attempt` equal to the upcoming overall attempt 2..7, `retry` equal to 1..6, and the matching delay. If attempt 7 fails, emit exactly one `TRUST_REFRESH_RETRY_EXHAUSTED` carrying `attempt=7` and `retry_count=6`, cancel pending timers, mark the dispatcher idle, and schedule nothing further. Requests arriving while an attempt or retry timer is active set/coalesce one pending bit and do not reset the cycle or add an attempt; only a request arriving after success or exhaustion starts a fresh cycle at attempt 1. Reset counters on success. Never spin. Add both event codes to GeniusNode's exact logging switch. Preserve deterministic policy/burn sort/dedupe, durable-first publication, foreign-only passive callback rules, and no successor signing. Make all four RED cases and the existing activation/reconstruction/passive cases GREEN; commit after the tests pass.</action>
  <verify>
    <automated>set -euo pipefail; cmake --build build/OSX/Release --target trust_first_boot_e2e_test -j8; build/OSX/Release/test_bin/trust_first_boot_e2e_test --gtest_filter='TrustFirstBootE2ETest.TransientPolicyListingRetriesWithoutNewWrite:TrustFirstBootE2ETest.TransientBurnListingRetriesWithoutNewWrite:TrustFirstBootE2ETest.TransientRefreshRetryExhaustionIsCappedCoalescedAndIdle:TrustFirstBootE2ETest.WorkerCallbackCanReleaseLastControllerOwnerSafely:TrustFirstBootE2ETest.RetainedPolicyQuorumRetriesAfterControllerReconstructionWithoutNewWrite:TrustFirstBootE2ETest.PassiveNodeDurablyActivatesPolicyWithoutExtraAdminCall:TrustFirstBootE2ETest.PreloadedRefreshActivationFailuresAreReturnedAndEmitted'; build/OSX/Release/test_bin/trust_first_boot_e2e_test</automated>
  </verify>
  <acceptance_criteria>
    - All seven exact cases pass, followed by the complete `trust_first_boot_e2e_test` binary, without skip/disabled counterparts.
    - Both transient cases emit exactly one scheduled retry after the injected failure, perform no new write/manual refresh, and commit the exact retained candidate on the next attempt.
    - The exhaustion case records exactly seven attempts and six delays `[100, 200, 400, 800, 1600, 3200]`, coalesces duplicate in-cycle requests, emits exactly one exhausted event after attempt 7, becomes idle, and performs no eighth attempt or write.
    - The release case observes weak expiration and drained state without self-join, detach, terminate, deadlock, or post-destruction access.
    - `rg -n 'refresh_worker_|worker_self|\.detach\(' src/account/TrustStartupController.{hpp,cpp}` returns zero matches; `system_executor`, weak controller locking, retry events, six-attempt cap, and 3200ms maximum are visible.
    - Actionable activation failure behavior and controller-local candidate suppression remain unchanged and do not schedule infrastructure retries.
  </acceptance_criteria>
  <done>Passive retained quorum recovers from transient discovery failure and controller callbacks can release the final owner safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retained authenticated CRDT data to refresh dispatcher | Infrastructure failures must not silently consume the only convergence trigger. |
| Dispatched Refresh to caller-owned callbacks | Synchronous callbacks may change controller ownership while work is executing. |

## STRIDE Threat Register

| Threat ID | Gap | Category | Severity | Component | Disposition | Mitigation Plan | Automated verification |
|-----------|-----|----------|----------|-----------|-------------|-----------------|------------------------|
| T13-G29 | CR-13 | Denial of Service/Repudiation | HIGH | policy candidate discovery | mitigate | Typed stage event, coalesced six-attempt exponential retry, and no-write durable convergence. | `TransientPolicyListingRetriesWithoutNewWrite` |
| T13-G30 | CR-13 | Denial of Service/Repudiation | HIGH | burn candidate discovery | mitigate | Same bounded retry and durable provider proof for burn listing. | `TransientBurnListingRetriesWithoutNewWrite` |
| T13-G31 | CR-13 | Denial of Service | HIGH | retry scheduler | mitigate | One initial plus six scheduled attempts, exact capped delay sequence, in-cycle request coalescing, exhaustion event, idle proof, cancellation, and no retry for actionable candidate errors. | `TransientRefreshRetryExhaustionIsCappedCoalescedAndIdle` plus activation-failure regression |
| T13-G32 | CR-14 | Denial of Service/Memory Safety | HIGH | callback/worker owner lifetime | mitigate | Independent system executor/state, one weak-lock per job, state-only post-Refresh code, no join/detach. | `WorkerCallbackCanReleaseLastControllerOwnerSafely` |
| T-13-SC | — | Tampering | LOW | package supply chain | accept | Existing Boost.Asio only; no package install. | Changed-file/build inspection |
</threat_model>

<verification>
Run RED first, then the GREEN focused command and complete startup binary. Plan 13-29 repeats these exact cases in source/XML accounting and under any fully target-proven sanitizer tree; absent instrumentation is NOT_RUN.
</verification>

<success_criteria>
- CR-13 cannot strand one-failure retained policy or burn quorum without typed evidence and retry, and repeated transient failure stops after exactly one initial plus six retry attempts with a typed exhausted event and idle dispatcher.
- CR-14 has no raw controller worker, self-join, detach, or post-destruction access path.
- Existing passive activation, actionable error, durable-first, and explicit-signing boundaries remain green.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Current-peer quorum successors converge and policy services remain safe | 13-28 | COVERED | Retained policy/burn retry and owner-safe dispatch preserve live convergence. |
| REQ | POLICY-01 | Current-policy-authorized successor becomes durable | 13-28 | COVERED | Transient policy listing cannot consume final quorum trigger. |
| REQ | TPR-02 | Current-set quorum membership converges | 13-28 | COVERED | Exact retained policy is retried without new approval. |
| REQ | BURN-03 | Quorum burn update remains live | 13-28 | COVERED | Retained burn successor retries into durable provider. |
| REQ | TEST-01 | Error, retry, and callback lifetime evidence | 13-28 | COVERED | Two no-write recovery cases, one bounded exhaustion/coalescing/idle case, and one final-owner-release case. |
| CONTEXT | D-08, D-12 | Current-head content-addressed winner remains authoritative | 13-28 | COVERED | Retry reuses existing verifier/CAS and adds no write/signing path. |
| CONTEXT | D-15, D-16 | Durable LKG plus node-scoped callback lifecycle | 13-28 | COVERED | State survives failure; dispatch teardown is controller-owner safe. |
| RESEARCH | — | Serialized callbacks off GlobalDB thread, deterministic candidates, bounded failure handling | 13-28 | COVERED | System executor, coalescing, typed stage, capped exponential retry. |
| REVIEW/VERIFY | CR-13 | Ignored Refresh/list errors strand retained quorum | 13-28 | COVERED | Result inspection, typed events, two no-write recovery tests, and exact capped exhaustion/coalescing/idle proof. |
| REVIEW/VERIFY | CR-14 | Raw worker allows self-join/UAF | 13-28 | COVERED | Independent dispatch state/executor and final-owner-release test. |
| PRIOR | CR-01..CR-12, WR-07 | Preserve earlier HIGH regressions | 13-28, 13-29 | COVERED | Prior focused cases remain; cumulative gate follows. |

No deferred phase item, new transport, remote admin, automatic successor signing, or package install is introduced. No scoped source item is missing.

<output>
Create `.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-28-SUMMARY.md` when done.
</output>
