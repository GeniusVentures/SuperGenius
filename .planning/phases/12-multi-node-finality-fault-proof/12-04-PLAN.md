---
phase: 12-multi-node-finality-fault-proof
plan: "04"
type: execute
wave: 4
depends_on: [12-03]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
autonomous: true
gap_closure: true
requirements: [TEST-04, TEST-05]
must_haves:
  truths:
    - "The three durable-boundary restart subcases reconnect over real local PubSub without requiring every peer to be directly meshed with every other peer."
    - "Restart and publisher-loss tests prove exact-once outcomes from reopened durable certificate, UTXO/outpoint, and bridge-marker state rather than from a fresh process's in-memory Mint counter."
    - "Publisher-loss recovery treats the already-persisted CRDT certificate value as authoritative and leaves PubSub as best-effort cleanup, with no successor re-advertisement."
  artifacts:
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "Reliable topology readiness plus durable-state assertions for the existing TEST-04 and TEST-05 scenarios"
      contains: "RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce"
  key_links:
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "GossipPubSub::AddPeers, GossipPubSub::GetHost, GossipPubSub::getPeerCount"
      via: "bounded connected-topology and one-topic-neighbor readiness predicate"
      pattern: "ConnectAndWaitForPeers"
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "RocksDB-backed UTXO and /bridge/executed durable records"
      via: "same-root StopPeer/recreate readback before and after restart"
      pattern: "HasBridgeMarker"
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "ConsensusManager::GetCertificateBySlot"
      via: "CRDT-backed authoritative certificate readback after publisher loss"
      pattern: "GetCertificateBySlot"
---

<objective>
Close only the verifier's TEST-04 and TEST-05 gaps in the existing Phase 12 real-socket regression target.

Purpose: Make restart readiness describe an attainable production topology, then prove exact-once recovery from durable storage instead of ephemeral instrumentation.
Output: A test-only fix to the existing four-peer suite; no protocol, CMake, transport, CRDT-authority, or PubSub-retry changes.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md
@.planning/phases/12-multi-node-finality-fault-proof/12-PATTERNS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VALIDATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-01-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-02-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-03-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW-FIX.md
@test/src/blockchain/multi_node_finality_fault_test.cpp
@test/src/blockchain/CMakeLists.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace all-to-all reconnect assumptions with real reachable-topology readiness</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp — read `PeersAreConnectedAndMeshed`, `ConnectAndWaitForPeers`, `RestartPeer`, `RestartAndReconnect`, and all three TEST-04 subcases before changing readiness.
    - /Users/henriqueklein/gnus/thirdparty/build/OSX/Release/ipfs-pubsub/include/ipfs_pubsub/gossip_pubsub.hpp — use only the installed public `AddPeers`, `GetHost`, `getPeerCount`, and `IsStarted` APIs; it provides no public per-peer disconnect API.
    - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md — the observed failure is the strict four-way host and consensus-mesh predicate at line 424, not a protocol-finality failure.
  </read_first>
  <behavior>
    - RED: the named TEST-04 scenario must fail under the former `Count - 1` per-topic-mesh expectation after recreating peers, documenting that GossipPubSub topic meshes are not a guaranteed all-to-all graph.
    - GREEN: the real `AddPeers` lifecycle is complete only when every participating PubSub is started, all peers are in one public libp2p-connected component, and every participant has at least one public consensus-topic neighbor; no participant is required to connect or mesh directly with every other participant.
    - The existing restart cases still wait for their actual durable vote/certificate/Mint outcome after topology readiness; readiness itself never manufactures a proposal, vote, certificate, timer event, or CRDT write.
  </behavior>
  <action>Per D-01, D-02, D-03, D-04, D-06, D-08, and D-09, keep the persistent four-peer fixture and its real `StopPeer`/recreate-plus-`AddPeers` recovery route, but replace the impossible `PeersAreConnectedAndMeshed` success criterion. In `test/src/blockchain/multi_node_finality_fault_test.cpp`, refactor the helper used by `ConnectPeers`, `ConnectValidatorPeers`, and `RestartAndReconnect` so it first calls the existing public `GossipPubSub::AddPeers` bootstrap API for the intended participants, then uses a bounded predicate composed only of public state: `IsStarted()` for each PubSub, graph reachability of all participating hosts through public `Host::connectedness(...) == CONNECTED` edges, and `getPeerCount(ConsensusTopic(...)) >= 1` for every participating consensus peer. Implement graph reachability rather than requiring every ordered source/target pair to be directly `CONNECTED`, and require one topic neighbor rather than `Count - 1`; a GossipSub mesh is an overlay and must not be asserted as an all-to-all mesh. Keep the named timeout diagnostic explicit about the attainable condition (one connected libp2p topology plus a consensus-topic neighbor). Retain a separate bounded durable-state predicate after every restart/reconnect so TEST-04 proves recovered behavior, not merely connectivity. Do not alter `GossipPubSub`, `ConsensusManager`, production timers, CMake registration, or the existing public submission route; do not add sleeps, direct handlers, direct CRDT writes, mock transport, or a test-controlled timer.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' &amp;&amp; ! rg -n 'ConsensusPendingLifecycleTestAccess::(Handle|CertificateReceived|WriteLiveCertificate|Force)|\bHandle[A-Za-z]*\(|\bCertificateReceived\(|\bOnConsensusCertificate\(|\bGlobalDB::Put|\bPutConvergentImmutable\(|sleep_for|sleep_until|Force[A-Za-z]*(Timer|Due|Round)' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
  </verify>
  <done>The focused TEST-04 GoogleTest passes across its vote, accepted-certificate, and Mint-before-marker restart subcases using a bounded real connected-topology/one-topic-neighbor readiness predicate; it no longer waits for a full host or GossipSub mesh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert durable exact-once Mint state across restart and CRDT-first publisher loss</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp — read `HasBridgeMarker`, `HasOnlyWinnerOutput`, `AssertSingleDurableMint`, every post-restart assertion in TEST-04, and `PublisherLossAfterPersistenceUsesDeterministicFailover`.
    - .planning/phases/12-multi-node-finality-fault-proof/12-03-SUMMARY.md — CRDT immutable finality is authoritative after publisher loss; PubSub is best-effort cleanup and no successor notification retry/re-advertisement is permitted.
    - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md — TEST-05 fails because a fresh runtime's in-memory Mint counter is treated as persistent evidence even though durable UTXO/marker state is the actual invariant.
    - src/account/TransactionManager.cpp — the counter is a test observer after production Mint effects, while durable effects and the bridge marker are the restart contract.
  </read_first>
  <behavior>
    - RED: a newly recreated peer may execute normal recovery and increment its fresh observer counter; therefore a post-restart expectation of `MintEffects == 0` is not an exact-once proof and must be removed.
    - GREEN: before a peer is restarted, current-runtime counter assertions remain `== 1` to show the live production Mint boundary was crossed once; after reopening a root, assertions use only durable certificate, exact winner UTXO/outpoint set, absence of loser outpoints, and the one bridge-executed marker key.
    - In TEST-05, the original selected publisher has a readable authoritative CRDT certificate while its outbound notification count is zero; after it stops, other peers and its recreated instance recover that same CRDT value through normal paths without successor certificate re-advertisement, direct certificate writes, or direct handler calls.
  </behavior>
  <action>Per D-02, D-03, D-05, D-06, D-07, D-08, D-09, and D-10, split the current assertion helper into two unambiguous forms in `test/src/blockchain/multi_node_finality_fault_test.cpp`: a durable-state helper and a live-runtime instrumentation helper. The durable helper must read the canonical slot certificate and confirm its embedded winning Mint hash, enumerate the recipient's persisted UTXOs/outpoints for the Mint destination and require exactly the winner's expected outpoint set with no losing-Mint outpoint, and read the single `/bridge/executed/&lt;chain&gt;:&lt;source&gt;` marker key. It must work immediately after recreation and must not inspect `TransactionManager::mint_effects_for_test_`. The live helper may assert the existing read-only Mint-effect counter is exactly one only before that same process is stopped, where it proves one real production boundary crossing. Apply this split to all three TEST-04 subcases and the TEST-05 final-reopen pass: replace every fresh-process `expected_mint_effects == 0` claim with the durable helper, while retaining the current-process counter checks before shutdown and at the observed Mint-effects barrier. In `PublisherLossAfterPersistenceUsesDeterministicFailover`, retain the proof that the selected publisher successfully persisted the certificate and made zero outbound notifications; after stopping it, assert recovery by durable CRDT certificate readback and exact winner state on live peers, then restart/reconnect the original peer and assert its durable certificate bytes/value and durable Mint state converge without requiring any successor `Publish` or successor notification counter. Do not create a successor certificate, re-advertise a certificate, force a round/timer, inject a certificate, invoke a receive/handler API, or write a CRDT key from the test. The pre-existing later-round eligibility observation may remain read-only, but PubSub must be treated only as faster cleanup after the persisted CRDT value, per the locked Phase 12 decision.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover' &amp;&amp; ctest --test-dir build/OSX/Release --output-on-failure --timeout 300 -R '^multi_node_finality_fault_test$' &amp;&amp; ! rg -n 'ConsensusPendingLifecycleTestAccess::(Handle|CertificateReceived|WriteLiveCertificate|Force)|\bHandle[A-Za-z]*\(|\bCertificateReceived\(|\bOnConsensusCertificate\(|\bGlobalDB::Put|\bPutConvergentImmutable\(|sleep_for|sleep_until|Force[A-Za-z]*(Timer|Due|Round)' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
  </verify>
  <done>The focused TEST-05 rerun and clean serial real-socket CTest target pass; durable UTXO/outpoint/marker state—not a fresh counter—proves exact-once recovery, and publisher loss remains CRDT-first with zero original notification and no successor certificate re-advertisement.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Required behavior | Plan | Status | Notes |
|--------|----|-------------------|------|--------|-------|
| GOAL | Phase 12 | Production-path finality remains safe and live through restart and publisher loss. | 12-04 | COVERED | Closes the only failed restart and publisher-loss execution proofs. |
| REQ | TEST-01, TEST-02, TEST-03, TEST-06 | Contention, late contender/receive-only behavior, and production-route audit. | 12-01, 12-02 | COVERED | Already verified; deliberately outside this gap closure. |
| REQ | TEST-04 | Real restart recovery preserves vote and avoids duplicate Mint. | 12-04 Task 1 and Task 2 | COVERED | Corrects topology readiness and post-restart durable proof. |
| REQ | TEST-05 | Publisher loss proves persistence-before-advertisement and no conflicting record. | 12-04 Task 2 | COVERED | Retains original publisher zero-notification proof and CRDT-first recovery. |
| RESEARCH | Real PubSub/CRDT/RocksDB with condition waits; no shortcuts or sleeps. | 12-04 Task 1 and Task 2 | COVERED | Automated source gate enforces it. |
| RESEARCH | Durable UTXO-before-marker recovery is the exactly-once boundary. | 12-04 Task 2 | COVERED | Replaces ephemeral post-restart counter claims with durable state. |
| CONTEXT | D-01, D-02, D-03, D-04 | Four real persistent peers and lifecycle-controlled production routes. | 12-01 through 12-04 | COVERED | Task 1 retains/reliably observes the existing real topology. |
| CONTEXT | D-05, D-06, D-07 | Actual durability barriers and read-only observations. | 12-03, 12-04 | COVERED | Task 2 changes evidence only; it adds no production control. |
| CONTEXT | D-08, D-09, D-10 | Named normal CTest scenarios with bounded waits and per-node durable proof. | 12-04 | COVERED | Focused reruns plus enabled serial CTest verify closure. |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live PubSub topology → restarted peer | Reconnection can be falsely reported if a test assumes a full mesh the protocol does not guarantee. |
| durable RocksDB state → fresh runtime observer | A new in-memory counter cannot establish whether durable Mint effects were repeated. |
| persisted certificate → post-loss recovery | A test must not turn loss recovery into a second publisher or test-authored authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | D | restart topology readiness | mitigate | Require a public connected component plus one consensus-topic neighbor with bounded predicates; never require a full all-to-all host/GossipSub mesh. |
| T-12-13 | T/R | exact-once Mint recovery assertion | mitigate | Read canonical certificate, exact durable winner outpoints, no loser outpoint, and bridge marker after same-root recreation; use Mint counters only in the live process. |
| T-12-14 | T/E | publisher-loss test control | mitigate | Preserve D-03/D-05 boundaries: observe persisted CRDT value, stop/recreate via real transport, prohibit direct writes/handlers/timers and successor certificate re-advertisement. |
| T-12-SC | Tampering | test-only source changes | mitigate | No package install; the automated source gate rejects direct receive/handler calls, CRDT writes, forced timers, mock transport, and sleeps. |
</threat_model>

<verification>
Run the Task 1 focused TEST-04 command once after its change. Run the Task 2 focused TEST-05 command, then the clean serial real-socket CTest command exactly as declared in Task 2. The registered target already has `RUN_SERIAL TRUE` and `TIMEOUT 300`; do not add a broad CTest run in this closure plan. Both tasks include the static anti-shortcut gate.
</verification>

<success_criteria>
1. `RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce` completes over a real connected topology with a consensus-topic neighbor per peer, without asserting an all-to-all GossipSub mesh.
2. All restart and publisher-loss post-reopen checks prove canonical certificate, exact winner UTXO/outpoints, no losing output, and one bridge marker from the retained RocksDB roots; no fresh-process Mint-counter equality is used as persistence evidence.
3. `PublisherLossAfterPersistenceUsesDeterministicFailover` preserves the persisted CRDT certificate as authority, keeps the original publisher's notification count at zero, and adds neither successor certificate re-advertisement nor any direct test shortcut.
4. The clean serial real-socket `multi_node_finality_fault_test` CTest target and both focused scenarios pass.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-04-SUMMARY.md` when done.
</output>
