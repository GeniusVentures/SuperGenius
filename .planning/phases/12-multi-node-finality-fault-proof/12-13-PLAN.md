---
phase: 12-multi-node-finality-fault-proof
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - test/testutil/storage/base_crdt_test.cpp
  - src/blockchain/Consensus.cpp
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-06]

must_haves:
  truths:
    - "A leftover CRDT.Datastore.TEST.* database from a previously killed or crashed run cannot be reopened by a later run — every CRDTFixture derives a run-unique db path (pid + fixture counter)"
    - "transaction_manager_certificate_fallback_test passes even when a stale legacy db dir (CRDT.Datastore.TEST.unit_12) is present in the test working directory (UAT round-2 test 2 truth restored)"
    - "A certificate rejected because its votes carry no quorum emits a warning-level log line instead of failing silently"
    - "All other CRDTFixture-based suites stay green (no blast-radius regression)"
  artifacts:
    - path: "test/testutil/storage/base_crdt_test.cpp"
      provides: "Run-unique fixture db/keypair paths plus construction-time reap of the exact derived paths"
      contains: "getpid"
    - path: "src/blockchain/Consensus.cpp"
      provides: "Warning log on no-quorum certificate rejection in ConsensusManager::ValidateCertificate"
      contains: "has_quorum"
  key_links:
    - from: "test/testutil/storage/base_crdt_test.cpp (CRDTFixture ctor)"
      to: "boost::filesystem::remove_all"
      via: "reap of db_path_/keypair_path_ BEFORE KeyPairFileStorage/GlobalDB::New construction"
      pattern: "remove_all.*(db_path_|keypair_path_)"
    - from: "test/testutil/storage/base_crdt_test.cpp (suffix derivation)"
      to: "process id"
      via: "suffix contains ::getpid() so cross-run and cross-binary collisions are impossible"
      pattern: "getpid"
    - from: "src/blockchain/Consensus.cpp (ValidateCertificate tally reject branch)"
      to: "ConsensusManagerLogger"
      via: "warn-level line with slot key, registry cid, vote count, tally error"
      pattern: "ConsensusManagerLogger\\(\\)->warn"
---

<objective>
Close UAT round-2 test 2 (CertificateFallbackTest.SharedMintSlotConfirmsOnlyTheCertifiedTransaction fails at transaction_manager_certificate_fallback_test.cpp:740) by eliminating its root cause: stale test-fixture RocksDB reuse through the deterministic, cross-run-colliding CRDTFixture db naming scheme. Also make the silent no-quorum certificate rejection that masked this failure for an entire diagnosis session visible at warning level.

Purpose: The failure is NOT a CR-02/WR-07 regression — it is fixture/environment poisoning that can strike any CRDTFixture suite after any killed or crashed run (diagnosis: .planning/debug/cert-fallback-loaded-missing.md, decisively reproduced). Left unfixed, every future crash (including the SIGSEGV closed by plan 12-14) can convert the NEXT run of any CRDTFixture binary into a confusing, silent validation failure.

Output: A fixture that always opens a fresh, run-unique database; a warn log for no-quorum certificate rejection; regression evidence across all seven CRDTFixture binaries with the stale-db precondition deliberately seeded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-UAT.md
@.planning/debug/cert-fallback-loaded-missing.md

@src/blockchain/ValidatorRegistry.cpp
@src/blockchain/Consensus.cpp
@test/testutil/storage/base_crdt_test.cpp
@test/testutil/storage/base_crdt_test.hpp
@test/testutil/storage/base_fs_test.cpp

<interfaces>
<!-- Current CRDTFixture construction (test/testutil/storage/base_crdt_test.cpp:54-84) — the exact code to change -->

Constructor body order today:
1. fixture_id = fixture_counter_.fetch_add(1) + 1; suffix = std::to_string(fixture_id)
2. keypair_path_ = basePath + "/unit_test_" + suffix   (basePath is the static "CRDT.Datastore.TEST")
3. db_path_ = basePath + ".unit_" + suffix             (NOTE: sibling of basePath, NOT under FSFixture base_path)
4. io_ = make_shared<io_context>()
5. pubs_ = make_shared<GossipPubSub>( KeyPairFileStorage( keypair_path_ ).GetKeyPair().value() )
6. GlobalDB::New( io_, db_path_, pubs_, ... )  → db_

Static members (hpp): basePath (const std::string), fixture_counter_ (std::atomic<uint64_t>).
Destructor (86-105): pubs_->Stop(); db_.reset(); pubs_.reset(); io_.reset(); then fs::remove_all(keypair_path_) and fs::remove_all(db_path_) in try/catch(fsync filesystem_error) with std::cerr.

FSFixture (base_fs_test.cpp:5-12): constructor calls clear() (fs::remove_all(base_path)) then create_directory — this is the established reap-at-construction pattern to mirror. CRDTFixture's derived paths escape it because db_path_ is a dot-sibling of basePath, not under the per-test FSFixture base_path.

Poison chain (why stale db fails the test): GlobalDB::New OPENS an existing rocksdb without wiping → ValidatorRegistry::InitializeCache loads the stale registry → StoreGenesisRegistry takes its cache_initialized_ skip branch (src/blockchain/ValidatorRegistry.cpp:413-418) → BuildSignedCertificate embeds stale registry_cid while its vote is signed by this run's random non-member account → TallyVotes drops the vote (src/blockchain/Consensus.cpp:1894-1903, debug-only) → approved_weight=0 → silent Check::Reject (Consensus.cpp:2792-2795) → outcome::failure(invalid_argument) (Consensus.cpp:3843-3846) → loaded.has_value()==false at test line 740.

ConsensusManager::ValidateCertificate reject site (src/blockchain/Consensus.cpp:2791-2795):
auto tally = TallyVotes( proposal, votes, registry, certificate.registry_cid() );
if ( tally.has_error() || !tally.value().has_quorum ) { return Check::Reject; }
Available in scope there: certificate (has proposal(), votes_size(), registry_cid()), GetSlotKey( certificate.proposal() ), ConsensusManagerLogger().

CRDTFixture consumers (7 binaries — the blast radius):
test/src/account/transaction_manager_certificate_fallback_test.cpp
test/src/account/transaction_manager_pending_lifecycle_test.cpp
test/src/blockchain/consensus_certificate_test.cpp
test/src/blockchain/consensus_pending_lifecycle_test.cpp
test/src/blockchain/multi_node_finality_fault_compatibility_smoke_test.cpp
test/src/blockchain/multi_node_finality_fault_test.cpp
test/src/processing/task_queue_test.cpp
Build layout: cmake/functions.cmake addtest() registers tests with NO WORKING_DIRECTORY property → under ctest all binaries share the ctest --test-dir as cwd, so counter-only names collide across concurrently running binaries (ctest -j) as well as across killed-run leftovers. CRDTFixture also binds GossipPubSub port 40001 (base_crdt_test.cpp:81) — concurrent CRDTFixture binaries additionally collide on that port, which is why these suites must be run SERIALLY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make CRDTFixture db paths run-unique and reap them at construction</name>
  <files>test/testutil/storage/base_crdt_test.cpp</files>
  <action>
  In test/testutil/storage/base_crdt_test.cpp only (no header change needed):

  1. Add the POSIX process-id include next to the existing includes (follow the project convention of plain ::getpid() seen in test/src/blockchain/multi_node_finality_fault_test.cpp:755 — include unistd.h directly here since this translation unit does not currently pull it in).

  2. In the CRDTFixture constructor, change the suffix derivation so it embeds the process id ahead of the per-process fixture counter: suffix = std::to_string( ::getpid() ) + "_" + std::to_string( fixture_id ). Keep the existing keypair_path_ = basePath + "/unit_test_" + suffix and db_path_ = basePath + ".unit_" + suffix formulas otherwise identical. Rationale: the counter alone restarts at 1 every process, so a killed/crashed run's unit_N dir is silently reopened by the next run's Nth fixture (the UAT test 2 root cause), and under parallel ctest two live binaries sharing the cwd would fight over the same paths — pid uniqueness removes both classes. Do NOT make the reap sweep anything broader than the exact derived paths: a wholesale sweep of basePath would delete other live fixtures' keypair dirs inside the same process, and sweeping legacy counter-only paths would delete a concurrently running binary's live db in a mixed build.

  3. Immediately AFTER computing keypair_path_/db_path_ and BEFORE the KeyPairFileStorage/GetKeyPair() call that constructs pubs_, reap both exact derived paths if they exist: for each of keypair_path_ and db_path_, if fs::exists, call fs::remove_all wrapped in the same try/catch( fs::filesystem_error ) + std::cerr pattern the destructor already uses, and print one std::cerr line when something was actually removed, e.g. "[CRDTFixture] removed pre-existing <path>", so future stale-state poisoning is self-announcing. This mirrors FSFixture::clear() for base_path, protects the pid-reuse edge case, and makes the always-fresh-db invariant explicit.

  4. Leave the destructor cleanup unchanged (it still removes the same derived paths at end of run).

  5. Do not touch basePath, fixture_counter_, SetUpTestSuite logging, or the pubsub/GlobalDB construction sequence.

  Gap reason: UAT round-2 test 2 — stale unit_12 db reopened by the 12th fixture poisoned ValidatorRegistry and produced the silent no-quorum rejection (see .planning/debug/cert-fallback-loaded-missing.md Resolution).
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target transaction_manager_certificate_fallback_test transaction_manager_pending_lifecycle_test consensus_certificate_test consensus_pending_lifecycle_test multi_node_finality_fault_compatibility_smoke_test multi_node_finality_fault_test task_queue_test && grep -c "getpid" test/testutil/storage/base_crdt_test.cpp && awk '/CRDTFixture::CRDTFixture/,/KeyPairFileStorage/' test/testutil/storage/base_crdt_test.cpp | grep -c "remove_all"</automated>
  </verify>
  <done>CRDTFixture compiles into all seven consumer binaries; suffix embeds ::getpid(); both derived paths are removed-at-construction before KeyPairFileStorage/GlobalDB::New; no other fixture behavior changed.</done>
</task>

<task type="auto">
  <name>Task 2: Log no-quorum certificate rejection at warning level (log-only)</name>
  <files>src/blockchain/Consensus.cpp</files>
  <action>
  In src/blockchain/Consensus.cpp, ConsensusManager::ValidateCertificate, the tally reject branch at lines 2791-2795 (auto tally = TallyVotes(...); if ( tally.has_error() || !tally.value().has_quorum ) return Check::Reject;):

  Insert a ConsensusManagerLogger()->warn(...) call immediately before the return Check::Reject, covering both reject causes (tally error and quorum miss). Include: __func__, the slot key via GetSlotKey( certificate.proposal() ), certificate.registry_cid(), the vote count certificate.votes_size(), and the tally error message when tally.has_error() (use the same outcome-error formatting pattern used elsewhere in this file). Keep it a single log line on the existing logger — NO change to control flow, return values, or any other statement.

  Do NOT elevate the per-vote non-member drop at lines 1894-1903 (stays debug — it is a hot path that legitimately fires for non-member gossip); the aggregate reject here is the only silent stage (every other failure stage in GetCertificateBySlot at lines 3836-3846 already logs at error level — this branch was the sole blind spot that cost a full diagnosis session).

  This is observability only: it must not alter the review-fix invariants verified by consensus_pending_lifecycle_test and transaction_manager_certificate_fallback_test.
  </action>
  <verify>
    <automated>grep -n "ConsensusManagerLogger()->warn" src/blockchain/Consensus.cpp | head -5 && git diff --stat src/blockchain/Consensus.cpp</automated>
  </verify>
  <done>ValidateCertificate's no-quorum/tally-error reject branch emits one warn line with slot key, registry cid, and vote count; diff shows Consensus.cpp touched by log statements only (no control-flow changes outside the inserted call).</done>
</task>

<task type="auto">
  <name>Task 3: Prove stale-db immunity and blast-radius safety across all CRDTFixture suites</name>
  <files></files>
  <action>
  All commands run SERIALLY from build/OSX/Release (CRDTFixture binaries bind port 40001 — never run two of them concurrently; never use ctest -j for these targets). Test binaries live in build/OSX/Release/test_bin/.

  1. Seed the legacy stale-db precondition in the ctest working directory: create an empty directory build/OSX/Release/CRDT.Datastore.TEST.unit_12 (content is irrelevant — with pid-unique paths the fixture must never open it; this proves non-interference the way the decisive repro in .planning/debug/cert-fallback-loaded-missing.md proved interference).

  2. Run the certificate-fallback binary directly with output captured: from build/OSX/Release, run ./test_bin/transaction_manager_certificate_fallback_test > /tmp/p12_13_stale_seed.log 2>&1. Then assert three gates on the captured log and tree: (a) all 20 cases pass (exit 0); (b) the log contains NO "registry already initialized, skipping" line and NO "InitializeCache: cache initialized" line attributable to this run's fixture dbs (grep -c, expect 0); (c) the GlobalDB path log line ("~GlobalDB CALLED ... on <path>") shows db paths matching the new CRDT.Datastore.TEST.unit_<pid>_<N> pattern — confirm via grep -E "CRDT\.Datastore\.TEST\.unit_[0-9]+_" returning at least one match — and the seeded legacy dir is still present and untouched afterward.

  3. Run the official ctest gate for the closed gap: ctest --test-dir build/OSX/Release -R '^transaction_manager_certificate_fallback_test$' --output-on-failure (expect pass).

  4. Blast-radius regression, serially, one at a time: ctest --test-dir build/OSX/Release -R '^transaction_manager_pending_lifecycle_test$|^consensus_certificate_test$|^consensus_pending_lifecycle_test$|^consensus_subject_test$|^multi_node_finality_fault_compatibility_smoke_test$|^task_queue_test$' --output-on-failure (all must pass; consensus_subject_test is NOT a CRDTFixture suite — it is re-run as symmetric evidence for the Task 2 Consensus.cpp edit because it carries the WR-03 SizeGate coverage from UAT round-2 test 3; multi_node_finality_fault_test itself is covered by plan 12-14 after its own fixture change). If any suite fails, capture its log to /tmp/p12_13_<suite>.log and diagnose against the fixture diff before proceeding — do NOT mark this task done on a red suite.

  5. Clean up: remove the seeded build/OSX/Release/CRDT.Datastore.TEST.unit_12 directory and any CRDT.Datastore.TEST* leftovers the runs left behind (each green run's destructor should already have removed its own pid-suffixed dirs — if leftovers persist, record that in the SUMMARY as a finding).
  </action>
  <verify>
    <automated>grep -c "registry already initialized, skipping" /tmp/p12_13_stale_seed.log; grep -Ec "CRDT\.Datastore\.TEST\.unit_[0-9]+_" /tmp/p12_13_stale_seed.log; ctest --test-dir build/OSX/Release -R '^transaction_manager_certificate_fallback_test$|^transaction_manager_pending_lifecycle_test$|^consensus_certificate_test$|^consensus_pending_lifecycle_test$|^consensus_subject_test$|^multi_node_finality_fault_compatibility_smoke_test$|^task_queue_test$' --output-on-failure</automated>
  </verify>
  <done>With a stale legacy db dir deliberately present, transaction_manager_certificate_fallback_test passes with zero "already initialized, skipping" lines and pid-pattern db paths; all five other CRDTFixture suites pass, as does consensus_subject_test (WR-03 SizeGate symmetric evidence for the Task 2 Consensus.cpp edit); seeded dir and test leftovers removed; UAT round-2 test 2 truth (cert-fallback target green under the poisoning precondition) is restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (test cwd) → fixture | Files in the shared build/test working directory are interpreted as fixture-owned RocksDB dirs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13-01 | Tampering | CRDTFixture construction-time fs::remove_all | mitigate | Reap strictly limited to the exact pid-unique derived paths (db_path_/keypair_path_); no wildcard sweep of basePath or legacy counter-only names, so a concurrently running binary's live db can never be deleted |
| T-12-13-02 | Information Disclosure | New warn log in ValidateCertificate | accept | Logs slot key, registry cid, vote count only — no key material, no vote signatures; same logger and redaction level as adjacent error paths |
| T-12-13-03 | DoS | Leftover pid-suffixed dirs from killed runs accumulate | accept | Build-tree garbage equivalent to any killed test's temp files; removed by ninja clean; frequency bounded by crash frequency |
| T-12-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (C++ targets only, no new dependencies) |
</threat_model>

<verification>
- test/testutil/storage/base_crdt_test.cpp suffix contains ::getpid(); construction removes db_path_ and keypair_path_ before pubs_/db_ creation; destructor unchanged.
- src/blockchain/Consensus.cpp diff is log-statement-only inside ValidateCertificate's tally reject branch.
- Stale-precondition run (Task 3 step 2): exit 0, zero "registry already initialized, skipping" matches, at least one pid-pattern db path match, legacy seeded dir untouched.
- All six non-multi-node CRDTFixture suites green under ctest, plus consensus_subject_test green as symmetric evidence for the Consensus.cpp log edit (multi-node suite gated by plan 12-14).
</verification>

<success_criteria>
- UAT round-2 test 2 gap closed: the cert-fallback target is immune to leftover CRDT.Datastore.TEST.* databases, proven by seeding the exact precondition that reproduced the failure.
- The silent no-quorum rejection now logs — a future stale-state or validator-set defect is diagnosable from the test log directly.
- No CRDTFixture consumer suite regressed; review-fix invariants (tests 3 and 4 of UAT round 2) untouched by these changes.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-13-SUMMARY.md` when done
</output>
