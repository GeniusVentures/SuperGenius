---
phase: 12-multi-node-finality-fault-proof
plan: 14
type: execute
wave: 2
depends_on: ["12-13"]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [TEST-03, TEST-06]

must_haves:
  truths:
    - "Peer::Stop releases the GlobalDB host co-owner (db, and the account) BEFORE GossipPubSub::Stop destroys the asio io_context, so no TcpConnection deregisters from a freed kqueue reactor (UAT round-2 tests 1 and 5 truth restored)"
    - "Three consecutive serial full multi_node_finality_fault_test runs pass with no teardown crash and no new multi_node_finality_fault_test crash reports in ~/Library/Logs/DiagnosticReports"
    - "consensus_pending_lifecycle_test passes on the same build (UAT round-2 test 1 covers both full targets)"
    - "Restart exact-once and publisher-loss cases inside the multi-node suite still pass — restart semantics and review-fix invariants unchanged"
  artifacts:
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "Reordered FinalityFaultNetwork::Peer::Stop with documented io_context-outlives-I/O-objects teardown invariant"
      contains: "db.reset()"
    - path: ".planning/STATE.md"
      provides: "Deferred Items entries for thirdparty StopImpl hardening and the MintRecoveryDiagnostics UAF"
      contains: "Deferred"
  key_links:
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp (Peer::Stop)"
      to: "GossipPubSub::Stop"
      via: "db.reset() (with transactions/blockchain/consensus/account already released) textually and semantically BEFORE pubsub->Stop()"
      pattern: "db\\.reset\\(\\)"
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp (Peer::Stop)"
      to: "active-vote snapshots"
      via: "after_consensus_manager_close / after_manager_ownership_release Snapshot(db, ...) still taken while db is alive"
      pattern: "after_manager_ownership_release = Snapshot"
---

<objective>
Close UAT round-2 tests 1 and 5 (one crash event: multi_node_finality_fault_test teardown SIGSEGV in boost::asio::detail::kqueue_reactor::deregister_descriptor via libp2p::transport::TcpConnection::~TcpConnection) by fixing the fixture teardown-order defect at its SuperGenius-repo root: Peer::Stop destroys the io_context owner (GossipPubSub) while GlobalDB's graphsync chain still co-owns the libp2p host.

Purpose: The crash is PRE-EXISTING (identical stack in nine crash reports back to 2026-08-26, six days before review fixes 2b1a8e47..caf34458 — diagnosis: .planning/debug/multi-node-teardown-sigsegv.md) and probabilistic under full-suite load (~4-5x more Peer::Stop calls than focused runs). It blocks trustworthy full-regression evidence for the whole phase. The thirdparty GossipPubSub::StopImpl hardening is explicitly OUT of scope (separate change control, separate checkout at /Users/henriqueklein/gnus/thirdparty) and is recorded as a deferred item instead.

Output: A reordered Peer::Stop satisfying asio's io_context-outlives-I/O-objects invariant; three consecutive serial full-suite passes plus crash-report absence as closure evidence; deferred follow-ups recorded in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-UAT.md
@.planning/debug/multi-node-teardown-sigsegv.md
@.planning/phases/12-multi-node-finality-fault-proof/12-13-SUMMARY.md

@src/crdt/globaldb/globaldb.cpp

<interfaces>
<!-- Current FinalityFaultNetwork::Peer::Stop (test/src/blockchain/multi_node_finality_fault_test.cpp:377-394) — the exact code to reorder -->

void Stop() noexcept
{
    if ( transactions ) transactions->Stop();
    transactions.reset();
    if ( blockchain ) (void) blockchain->Stop();
    if ( consensus ) consensus->Close();
    after_consensus_manager_close = Snapshot( db, active_vote_diagnostic_key );
    consensus.reset();
    blockchain.reset();
    after_manager_ownership_release = Snapshot( db, active_vote_diagnostic_key );
    if ( io ) io->stop();
    if ( io_thread.joinable() ) io_thread.join();
    if ( pubsub ) pubsub->Stop();   // line 389 — destroys m_context while db still co-owns the host
    db.reset();                     // line 390 — host release AFTER the io_context is gone
    pubsub.reset();
    account.reset();
    io.reset();
}

Ownership chain (verified in diagnosis): StartPeer line 993 constructs graphsync::Network over peer.pubsub->GetHost() (GossipPubSub::m_host) with a scheduler on peer.io; GlobalDB::Init (src/crdt/globaldb/globaldb.cpp:300-326) hands that host to GraphsyncImpl/GraphsyncDAGSyncer AND holds m_pubsub itself, so db_ is a co-owner of the host. GossipPubSub::StopImpl (thirdparty/ipfs-pubsub gossip_pubsub.cpp:830-846, OUT OF SCOPE to edit) ends with m_host.reset(); m_strand.reset(); m_context.reset() — destroying the asio io_context/kqueue reactor while the db still holds host references. When db.reset() later destroys ~BasicHost, reverse member order kills transport_manager_ (TcpTransport holds the last shared_ptr<io_context>) BEFORE network_ (Multiselect → TcpConnection), and any connection not cleanly closed during StopImpl deregisters from the freed reactor → SIGSEGV at small offsets (0x30/0x88). GlobalDB::ShutdownNow (globaldb.cpp:113-141) calls m_broadcaster->Stop() and resets datastore members — safe to run while pubsub is still started, since GlobalDB holds its own m_pubsub reference keeping the object alive.

Also relevant: Peer::Stop is invoked from RestartPeer (StopPeer then StartPeer recreates from the same peer.root on disk — durable restart semantics do not depend on teardown order inside Stop), from Peer::~Peer, and from the move-assignment operator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Release the GlobalDB host co-owner before GossipPubSub::Stop in Peer::Stop</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp</files>
  <action>
  Rewrite FinalityFaultNetwork::Peer::Stop (test/src/blockchain/multi_node_finality_fault_test.cpp:377-394) to this exact statement order, changing ONLY where db.reset()/account.reset() appear (everything else stays byte-identical):

  1. transactions->Stop(); transactions.reset();
  2. blockchain->Stop(); consensus->Close();
  3. after_consensus_manager_close = Snapshot( db, active_vote_diagnostic_key );
  4. consensus.reset(); blockchain.reset();
  5. after_manager_ownership_release = Snapshot( db, active_vote_diagnostic_key );   (both snapshots MUST remain while db is alive)
  6. io->stop(); io_thread.join();   (unchanged — graphsync's scheduler io_context is stopped before the db chain is destroyed, same as today)
  7. db.reset();   account.reset();   ← MOVED UP: releases every external co-owner of the pubsub host (GlobalDB → CrdtDatastore → GraphsyncDAGSyncer → graphsync Network::host_, plus the account's db-backed handles) BEFORE the pubsub is stopped
  8. pubsub->Stop();   ← now the FINAL host release: StopImpl enumerates and cleanly closes connections, then its m_host.reset()/m_context.reset() run with no external owners left
  9. pubsub.reset(); io.reset();

  Add a block comment above the reordered release section stating the invariant in these terms: the asio io_context owned by GossipPubSub must outlive every I/O object that touches it; GlobalDB's graphsync chain co-owns the libp2p host wired from pubsub->GetHost() at StartPeer, so db MUST be reset before pubsub->Stop() — otherwise ~BasicHost deregisters leftover TcpConnections from the freed kqueue reactor (teardown SIGSEGV; crash reports 2026-08-26..2026-09-02, e.g. multi_node_finality_fault_test-2026-09-02-075842.ips).

  Keep Stop() noexcept and all null guards. Do not change RestartPeer, StartPeer, StopPeer, the Peer member declarations, or any other fixture code — the crash fix is solely this release ordering. Do not touch thirdparty/ipfs-pubsub (separate change control).
  </action>
  <verify>
    <automated>grep -n "pubsub->Stop()\|db.reset()\|account.reset()\|after_manager_ownership_release = Snapshot" test/src/blockchain/multi_node_finality_fault_test.cpp | head -10 && cmake --build build/OSX/Release --target multi_node_finality_fault_test</automated>
  </verify>
  <done>Stop() compiles; db.reset() and account.reset() occur before pubsub->Stop() and after both db snapshots and the io_thread join; invariant comment present; diff limited to Peer::Stop plus the comment.</done>
</task>

<task type="auto">
  <name>Task 2: Three-pass teardown evidence and deferred-item recording</name>
  <files>.planning/STATE.md</files>
  <action>
  PART A — Teardown regression evidence (run SERIALLY, never ctest -j; record load conditions in the SUMMARY per the 12-12 discipline):

  1. Snapshot the existing crash-report baseline: ls -la ~/Library/Logs/DiagnosticReports/ | grep multi_node_finality_fault_test (record the newest .ips timestamp — the evidence runs must add ZERO new reports).
  2. Run the full multi_node_finality_fault_test THREE consecutive serial times, each captured to its own log: ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure | tee /tmp/p12_14_full_{a,b,c}.log. All three must pass. This exercises ~30-40 Peer::Stop invocations per run — the exposure multiplier that made focused runs pass while full runs crashed probabilistically.
  3. Run ctest --test-dir build/OSX/Release -R '^consensus_pending_lifecycle_test$' --output-on-failure (UAT round-2 test 1 truth covers BOTH full targets; must pass).
  4. Re-check ~/Library/Logs/DiagnosticReports for any multi_node_finality_fault_test .ips newer than the step-1 baseline — expect none; if one exists, do NOT close the gap: capture it and report back (the thirdparty StopImpl hardening deferred item would then need promotion).
  5. Confirm restart/publisher-loss invariants inside the three passing runs: /tmp/p12_14_full_*.log contain passing results for RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce and the PublisherLoss cases (the exact cases that owned the historical crash reports).

  PART B — Record the separately-scoped follow-ups in .planning/STATE.md (mirrors the 12-12 deferred-item precedent). Append two rows to the "## Deferred Items" table with category "thirdparty-hardening" and "teardown-uaf" respectively, status "Deferred", deferred-at "Phase 12 UAT round-2 gap closure 12-14":
  - GossipPubSub::StopImpl hardening in the thirdparty checkout (/Users/henriqueklein/gnus/thirdparty/ipfs-pubsub, gossip_pubsub.cpp:695-848): defer m_context destruction when m_host.use_count() > 1 (e.g. to the GossipPubSub destructor) and force-close ALL connections including unresolved-remotePeer() ones; optionally revisit the 1000ms shutdown deadline. Rationale: protects non-test owners against this same lifetime inversion; kept out of in-phase repair by the established thirdparty change-control constraint.
  - MintRecoveryDiagnostics destructor → UTXOManager::GetUTXOs destroyed-mutex use-after-free (crash report multi_node_finality_fault_test-2026-08-26-173919.ips, distinct signature from the SIGSEGV closed here): destroyed-manager use during diagnostics destruction in RestartAtVote teardown; needs its own scoped diagnosis.
  Make no other STATE.md edits (do not flip milestone status, do not clear the existing Phase 12 blocker history notes).
  </action>
  <verify>
    <automated>grep -c "Passed" /tmp/p12_14_full_a.log /tmp/p12_14_full_b.log /tmp/p12_14_full_c.log; ls ~/Library/Logs/DiagnosticReports/ 2>/dev/null | grep -c "multi_node_finality_fault_test-2026-09-0[2-9]"; grep -c "StopImpl\|173919" .planning/STATE.md</automated>
  </verify>
  <done>Three consecutive serial multi_node_finality_fault_test passes plus a green consensus_pending_lifecycle_test with zero new crash reports; restart exact-once and publisher-loss cases green in all three logs; STATE.md Deferred Items carries the thirdparty StopImpl hardening and the MintRecoveryDiagnostics UAF rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Test-fixture teardown ordering only; no production trust boundary, input parsing, or network surface is added or altered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14-01 | DoS | Peer::Stop reordered db release | mitigate | GlobalDB::ShutdownNow (globaldb.cpp:113-141) stops the broadcaster and closes the datastore synchronously while Peer.pubsub still holds the object alive; three serial full-suite passes with restart-recovery cases green demonstrate no teardown hang or lost durability |
| T-12-14-02 | Tampering | STATE.md deferred-item rows | accept | Documentation-only rows citing thirdparty paths and a dated crash report; no code or workflow authority derived from them |
| T-12-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (C++ test target rebuild only) |
</threat_model>

<verification>
- Structural: in Peer::Stop, db.reset() precedes pubsub->Stop(); both Snapshot calls still execute while db is alive; io_thread joined before db release; Stop remains noexcept with null guards.
- Behavioral: three consecutive serial full multi_node_finality_fault_test passes (logs /tmp/p12_14_full_{a,b,c}.log), one consensus_pending_lifecycle_test pass, zero new .ips crash reports versus the pre-run baseline.
- Invariants: RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce and PublisherLoss cases pass in every full run (review-fix and restart invariants intact — UAT round-2 tests 3 and 4 stay green).
</verification>

<success_criteria>
- UAT round-2 tests 1 and 5 (single crash event) closed at the fixture level: the io_context owner is stopped only after every co-owner of the pubsub host is released, satisfying asio's documented invariant.
- Full-suite regression evidence restored to the 12-12 standard (three consecutive serial passes) on the post-review-fix build with plan 12-13's fixture hygiene in place.
- Thirdparty StopImpl hardening and the MintRecoveryDiagnostics UAF are recorded as deferred items, not silently dropped and not smuggled into thirdparty in this phase.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-14-SUMMARY.md` when done
</output>
