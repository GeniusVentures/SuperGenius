---
phase: 12-multi-node-finality-fault-proof
plan: "08"
type: execute
wave: 8
depends_on: ["12-07"]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-run-1.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-run-2.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-run-3.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-repair-run-1.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-repair-run-2.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-repair-run-3.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/serial-suite-run-1.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/serial-suite-run-2.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/serial-suite-run-3.log
autonomous: true
gap_closure: true
requirements: [TEST-05, TEST-06]
must_haves:
  truths:
    - "Per D-17 through D-19, the publisher-loss scenario's pre-fault ConnectAndWaitForPeers failure is classified from passive public readiness evidence across three independently started real-socket processes."
    - "Per D-20, a test-harness lifecycle/ownership repair is considered only after at least two fresh failures share one first readiness boundary and normalized state/error, and only if that evidence proves the defect is fixture-owned."
    - "Per TEST-06 and D-03, no diagnostic or repair introduces a direct handler/author route, transport workaround, CRDT write, timer forcing, retry, timeout change, or production-source edit."
    - "If the gate is not repeatable or fixture-owned, an evidence report records the terminal blocked result and Phase 12 remains blocked with no Task-2 source change."
    - "Task 1 commits the passive snapshot before Task 2 and records its immutable task1_snapshot_commit_sha in the evidence summary."
  artifacts:
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "Passive, test-owned public readiness snapshots at the existing publisher-loss ConnectAndWaitForPeers predicate and any evidence-authorized fixture lifecycle repair."
    - path: ".planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md"
      provides: "Three fresh publisher-loss classifications, the repair authorization branch, static-gate results, and final Phase-12 disposition."
    - path: ".planning/phases/12-multi-node-finality-fault-proof/12-08-traces/"
      provides: "Stable complete logs for every fresh publisher-loss process and, only after an authorized repair, three serial full-suite CTest processes."
  key_links:
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "GossipPubSub public readiness state"
      via: "IsStarted, GetHost connectedness, getPeerCount(consensus topic), peer identity, and fixture listener/root lifecycle snapshots"
      pattern: "IsStarted|connectedness|getPeerCount"
    - from: "FinalityFaultNetwork::ConnectAndWaitForPeers"
      to: "PublisherLossAfterPersistenceUsesDeterministicFailover"
      via: "unchanged pre-fault topology predicate before selected-publisher persistence, barrier entry, or StopPeer"
      pattern: "PublisherLossAfterPersistenceUsesDeterministicFailover|ConnectAndWaitForPeers"
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "multi_node_finality_fault_test CTest"
      via: "three independent focused processes and, conditionally, three RUN_SERIAL full-suite processes"
      pattern: "PublisherLossAfterPersistenceUsesDeterministicFailover"
---

<objective>
Diagnose the intermittent public readiness failure in the publisher-loss regression before the selected publisher persists a certificate or the publisher-loss fault starts; conditionally repair only a twice-reproduced fixture lifecycle/ownership defect.

Purpose: Close the remaining publisher-loss evidence gate for TEST-05 without mistaking a pre-fault topology failure for a certificate, CRDT, PubSub, receiver, restart, or late-contender defect.
Output: Passive readiness evidence plus either a narrowly tested harness-only lifecycle repair or a terminal blocked report with no additional source change.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md
@.planning/phases/12-multi-node-finality-fault-proof/12-PATTERNS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-07-SUMMARY.md
@test/src/blockchain/multi_node_finality_fault_test.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add passive public-readiness snapshots and run the publisher-loss reproduction gate</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md, .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-run-1.log, .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-run-2.log, .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-run-3.log</files>
  <read_first>
    - .planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md — D-17 through D-20 are locked: inspect only pre-fault publisher readiness, use passive public snapshots, require a repeated normalized boundary before any fixture-only repair, and prohibit protocol changes.
    - .planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md — two fresh publisher runs passed and one stopped at line 520 before the fault; retain that distinction rather than diagnosing post-persistence behavior.
    - .planning/phases/12-multi-node-finality-fault-proof/12-07-SUMMARY.md — the restart reproduction gate closed with no repair; do not reopen, modify, or use the restart scenario as evidence for this publisher-only plan.
    - test/src/blockchain/multi_node_finality_fault_test.cpp — inspect `Peer`, `StartPeer`, `StopPeer`, `RestartPeer`, `PeersFormConnectedTopology`, `ConnectAndWaitForPeers`, and `PublisherLossAfterPersistenceUsesDeterministicFailover`. Preserve the existing `AddPeers` call, five-second condition wait, selected-publisher barrier, public `CreateProposal`/`SubmitProposal` ingress, and all later fault assertions.
    - .planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md and 12-PATTERNS.md — use only existing real `GossipPubSub`/`GlobalDB`/RocksDB construction and `ASSERT_WAIT_FOR_CONDITION`; do not copy private handler, direct datastore, mock, sleep, or forced-round patterns.
  </read_first>
  <behavior>
    - Per D-18, every failed readiness wait emits one `P12_PUBLISHER_READINESS_DIAG` record whose first predicate is one of `missing-peer`, `pubsub-not-started`, `missing-host`, `missing-consensus`, `disconnected-intended-peer`, or `zero-consensus-topic-mesh`; it also includes peer identity, intended-peer connectedness, consensus-topic mesh count, listener state, root/lifecycle state, and normalized state/error.
    - A successful readiness wait emits `boundary=none state=ready error=none`, so passing runs cannot be treated as matching failures.
    - Per D-17 and D-18, observation occurs only in test-owned code around the existing predicate and reads current public facts. It does not call AddPeers, Publish, SubmitProposal, a handler, a CRDT writer, a timer, a barrier, or any retry method.
    - Three independently started focused publisher-loss processes run sequentially, with no live `multi_node_finality_fault_test` process before each next launch; each gets its own stable full log.
    - Before Task 2 can begin, the passive test snapshot is committed and `task1_snapshot_commit_sha` in the summary resolves to that commit; the summary/traces may be committed separately after that immutable source snapshot.
    - The gate can set `repair_authorization=fixture-lifecycle` only when at least two failed fresh processes have the identical first readiness boundary and normalized state/error and their snapshots prove a listener/root/ownership lifecycle defect. All other evidence produces `repair_authorization=none`.
  </behavior>
  <action>Capture `pre_task1_commit_sha` from the current `HEAD` before editing and write it once at the start of `12-08-SUMMARY.md`; retain it unchanged for later additions-only audits. Per D-17 and D-18, add only a passive, test-owned readiness snapshot type and collector in `test/src/blockchain/multi_node_finality_fault_test.cpp`. Do not edit the definition or body of `PeersFormConnectedTopology` or `ConnectAndWaitForPeers`. Immediately after the existing publisher-loss test's unmodified `ConnectPeers(...)` call returns, collect the public facts and, if that prior bounded assertion set a fatal failure, classify the same current checks in its existing deterministic order to record the first false readiness predicate. For every peer and intended peer, read existing `GossipPubSub::IsStarted()`, public host availability and connectedness to each intended peer, consensus-topic `getPeerCount`, public peer identity, and test-owned listener/root lifecycle facts already available from `Peer` (port, root, I/O-thread/lifetime status). Record the first false readiness predicate and normalize the observed state/error; never serialize certificate, transaction, RocksDB value, key material, or any secret. Snapshot collection must be read-only and must not create a peer, call `AddPeers`, publish/retry any message, pause/release a barrier, reorder transport, inject failure, alter a wait, or touch production source, CMake, timeout configuration, selected publisher choice, receiver, restart, or late-contender behavior.

Preserve the previously delivered finality boundaries and prove that preservation in the additions-only static gate: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, and D-16. In particular, retain the four real peers and independent roots; real public ingress and transport; the established durable barriers/counters; bounded condition waits; the enabled normal CTest; exact durable-Mint proof; the separate restart gate; and late-contender diagnostics. Do not add source changes outside this test-owned snapshot seam.

Build the target, then launch exactly three independent fresh, sequential `FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover` real-socket processes, waiting for the prior process to exit and confirming no test process remains before starting the next. Store complete stdout/stderr at `12-08-traces/publisher-run-{1,2,3}.log`. Append one `P12_PUBLISHER_READINESS_RUN` and one `P12_PUBLISHER_READINESS_DIAG` line per run to the summary, including run number, process exit, relative trace path, boundary, normalized state/error, and the permitted public/lifecycle facts. Determine whether two failed records share exactly the first boundary plus normalized state/error. Set `repair_authorization=fixture-lifecycle` only if those matching snapshots directly demonstrate a fixture listener/root ownership or teardown/startup lifecycle defect; otherwise set `repair_authorization=none`. Record `phase_disposition=blocked-awaiting-evidence-report` before Task 2.

Before Task 2, commit only the passive test snapshot with `gsd-sdk query commit "test(12-08): add passive publisher readiness snapshots" --files test/src/blockchain/multi_node_finality_fault_test.cpp`, capture the returned hash as `task1_snapshot_commit_sha`, and append that exact field to the summary. Then commit the Task-1 summary and traces with a separate docs commit, preserving the immutable source-snapshot commit as the Task-2 baseline. Do not speculate about `SubmitCertificate`, CRDT writes/filtering, selected-publisher authority, PubSub retry/notification, receiver behavior, restart, or late contenders: the targeted process has not reached them when readiness fails.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; p12_dir='.planning/phases/12-multi-node-finality-fault-proof' &amp;&amp; p12_summary="$p12_dir/12-08-SUMMARY.md" &amp;&amp; p12_test='build/OSX/Release/test_bin/multi_node_finality_fault_test' &amp;&amp; mkdir -p "$p12_dir/12-08-traces" &amp;&amp; test "$(sed -n 's/^pre_task1_commit_sha=//p' "$p12_summary" | rg -x '[0-9a-f]{40}' | wc -l | tr -d '[:space:]')" -eq 1 &amp;&amp; for p12_run in 1 2 3; do if pgrep -f 'multi_node_finality_fault_test' &gt;/dev/null; then exit 1; fi; P12_08_RUN="$p12_run" "$p12_test" --gtest_filter='FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover' --gtest_brief=1 &gt;"$p12_dir/12-08-traces/publisher-run-$p12_run.log" 2&gt;&amp;1; p12_exit=$?; test -f "$p12_dir/12-08-traces/publisher-run-$p12_run.log" || exit 1; test "$p12_exit" -ge 0 || exit 1; done &amp;&amp; test "$(rg -c '^P12_PUBLISHER_READINESS_RUN run=[123] process_exit=-?[0-9]+ trace=\.planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-run-[123]\.log$' "$p12_summary")" -eq 3 &amp;&amp; test "$(rg -c '^P12_PUBLISHER_READINESS_DIAG run=[123] outcome=(pass|failure) boundary=(none|missing-peer|pubsub-not-started|missing-host|missing-consensus|disconnected-intended-peer|zero-consensus-topic-mesh) state=[^[:space:]]+ error=[^[:space:]]+ peer_identity=[^[:space:]]+ listener=[^[:space:]]+ root_lifecycle=[^[:space:]]+ intended_connectedness=[^[:space:]]+ consensus_mesh=[0-9]+$' "$p12_summary")" -eq 3 &amp;&amp; p12_base="$(sed -n 's/^pre_task1_commit_sha=//p' "$p12_summary")" &amp;&amp; ! git diff --unified=0 "$p12_base"..HEAD -- test/src/blockchain/multi_node_finality_fault_test.cpp | rg '^\+[^+].*(AddPeers|SubmitProposal|Publish|CertificateReceived|OnConsensusCertificate|GlobalDB::Put|PutConvergentImmutable|Handle[A-Za-z]*|Force[A-Za-z]*(Timer|Due|Round)|sleep_for|sleep_until|retry|TIMEOUT|timeout)' &amp;&amp; test -z "$(git diff --name-only "$p12_base"..HEAD -- src test/src/blockchain/CMakeLists.txt)"</automated>
    <automated>p12_dir='.planning/phases/12-multi-node-finality-fault-proof'; p12_summary="$p12_dir/12-08-SUMMARY.md"; p12_file='test/src/blockchain/multi_node_finality_fault_test.cpp'; p12_base="$(sed -n 's/^pre_task1_commit_sha=//p' "$p12_summary")"; p12_snapshot="$(sed -n 's/^task1_snapshot_commit_sha=//p' "$p12_summary")"; test "$p12_snapshot" = "$(git rev-parse "$p12_snapshot")" &amp;&amp; git merge-base --is-ancestor "$p12_base" "$p12_snapshot" &amp;&amp; p12_source_diffs="$( { git diff --unified=0 "$p12_base"..HEAD -- "$p12_file"; git diff --cached --unified=0 "$p12_base" -- "$p12_file"; git diff --unified=0 "$p12_base" -- "$p12_file"; } )" &amp;&amp; p12_connect_diffs="$(printf '%s\n' "$p12_source_diffs" | rg '^[+-][^-+].*ConnectAndWaitForPeers' || true)" &amp;&amp; test -z "$p12_connect_diffs" &amp;&amp; test "$(git show "$p12_base:$p12_file" | awk '/static void ConnectAndWaitForPeers/{capture=1} capture{print; opens=gsub(/\{/,"{"); closes=gsub(/\}/,"}"); depth+=opens-closes; if(opens) started=1; if(started &amp;&amp; depth==0) exit}')" = "$(awk '/static void ConnectAndWaitForPeers/{capture=1} capture{print; opens=gsub(/\{/,"{"); closes=gsub(/\}/,"}"); depth+=opens-closes; if(opens) started=1; if(started &amp;&amp; depth==0) exit}' "$p12_file")" &amp;&amp; ! printf '%s\n' "$p12_source_diffs" | rg '^[+-][^-+].*(SubmitCertificate|FilterCertificate|CertificateReceived|OnConsensusCertificate|ProcessCommittedCertificate|ProcessCertificates|IsCurrentAggregator|OrderedActiveValidators|GetAggregatorRole|CertificateWrite|CertificatePersistedBarrier|CertificateNotifications|PutConvergentImmutable|GlobalDB::Put|GetDataStore\(\)->(put|remove)|\bPublish\(|retry|Retry|AddPeers|ConnectPeers|ConnectValidatorPeers|PeersFormConnectedTopology|ASSERT_WAIT_FOR_CONDITION|waitForCondition|sleep_(for|until)|std::chrono::seconds|TIMEOUT|timeout)' &amp;&amp; p12_restricted_changes="$( { git diff --name-only "$p12_base"..HEAD -- src test/src/blockchain/CMakeLists.txt; git diff --cached --name-only "$p12_base" -- src test/src/blockchain/CMakeLists.txt; git diff --name-only "$p12_base" -- src test/src/blockchain/CMakeLists.txt; } | sort -u)" &amp;&amp; test -z "$p12_restricted_changes" &amp;&amp; git diff --check "$p12_base"..HEAD &amp;&amp; git diff --cached --check &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>Exactly three isolated publisher-loss process logs and exactly three permitted structured readiness records exist. Each record identifies either `boundary=none state=ready error=none` or the first public readiness predicate with normalized state/error and the required public/lifecycle facts. Before Task 2, `task1_snapshot_commit_sha` resolves to a commit descended from `pre_task1_commit_sha`; committed, staged, and unstaged diffs reject shortcuts and mutations to certificate authority/selection, receiver/CRDT behavior, publication/retry, topology, waits/timeouts, CMake, and production source.</acceptance_criteria>
  <done>The pre-fault readiness failure has a deterministic evidence gate. A repeated fixture lifecycle/ownership defect is explicitly authorized for Task 2; otherwise the plan has a complete no-repair evidence record and remains blocked pending Task 2's terminal report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch to a fixture-only RED/GREEN repair or an evidence-report terminal stop</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md, .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-repair-run-1.log, .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-repair-run-2.log, .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-repair-run-3.log, .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/serial-suite-run-1.log, .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/serial-suite-run-2.log, .planning/phases/12-multi-node-finality-fault-proof/12-08-traces/serial-suite-run-3.log</files>
  <read_first>
    - .planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md — revalidate `pre_task1_commit_sha`, `task1_snapshot_commit_sha`, the committed passive snapshot, three fresh records, matching boundary/state/error count, lifecycle evidence, and `repair_authorization`; do not infer authorization from a timeout alone.
    - test/src/blockchain/multi_node_finality_fault_test.cpp — if authorized, locate only the test fixture object-lifetime/ownership site proven by the snapshots. Preserve `ConnectAndWaitForPeers`, real peer construction, public ingress, selected-publisher persistence barrier, and all publisher-loss assertions.
    - .planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md — D-19/D-20 forbid retries, timeouts, protocol changes, and non-fixture repairs; D-03/TEST-06 prohibit direct author/receiver shortcuts.
    - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md — TEST-05 remains unproven until targeted and serial proof pass; TEST-06's production-route guard must stay green.
  </read_first>
  <behavior>
    - Terminal branch: if fewer than two failures share the same first readiness boundary and normalized state/error, or matching evidence does not prove fixture lifecycle/ownership, Task 2 makes no source change. It appends a normalized evidence report, `repair_authorization=none`, and `PHASE_12_STATUS=BLOCKED`.
    - Authorized RED: a test-harness-only regression exercises the exact evidenced lifecycle/ownership sequence with real sockets and the unchanged readiness predicate; it fails before the repair for the diagnosed boundary rather than asserting elapsed time.
    - Authorized GREEN: modify only the smallest fixture lifecycle/ownership code in `multi_node_finality_fault_test.cpp` required by the RED evidence, such as an established stop/join/release/ownership ordering or partial-start cleanup. It must not add peers, publication retries, pauses, reordered transport, new waits, injection, source files, CMake changes, timeout changes, or production APIs.
    - Repair proof requires three fresh targeted publisher-loss passes, each retained as a `publisher-repair-run-{1,2,3}.log` with one `P12_PUBLISHER_REPAIR_RUN` summary record, and three independent serial CTest full-suite passes. A failed targeted or serial pass leaves TEST-05 and Phase 12 blocked; no workaround branch is permitted.
  </behavior>
  <action>Start by calculating the repeated classifications from the three Task-1 summary records and inspect their public peer/listener/root lifecycle snapshots. This is a hard branch. If there are not at least two identical failure boundary/state/error records, or the matching records do not prove a fixture lifecycle/ownership defect, make no source change in this task. Append an `evidence_report` section that names all three runs, their first predicate/state/error, why the threshold or fixture-ownership proof failed, `repair_authorization=none`, `PHASE_12_STATUS=BLOCKED`, and `next_action=separately-scoped-diagnosis`. Preserve the Task-1 test snapshot commit untouched; do not rerun or alter any production, CMake, timeout, retry, publisher, receiver, restart, or late-contender code.

If and only if Task 1 recorded `repair_authorization=fixture-lifecycle` with the repeated proof, perform TDD entirely in `test/src/blockchain/multi_node_finality_fault_test.cpp`. RED first: add a focused regression that invokes the exact existing four-peer fixture start/stop/restart ownership sequence implicated by the snapshots and makes one allowed new call, `ConnectAndWaitForPeers( Peers( network ) );`, to the existing public readiness helper through real local sockets. It must demonstrate the recorded boundary/state/error before the repair and must not manufacture protocol data or connection state. Commit the failing test as `test(12-08): reproduce publisher readiness lifecycle defect`. GREEN next: make the smallest fixture-owned lifecycle/ownership correction indicated by that failure—limited to existing Peer/FinalityFaultNetwork resource ownership, listener stop/release, I/O join, root retention, or partial-construction cleanup—and keep it in the test file. Do not modify the definition/body of `ConnectAndWaitForPeers`, its connection loop, predicate, wait duration/message, peer membership, or lifecycle ordering; the single exact RED-test call is the only permitted diff involving that helper. Do not modify `SubmitCertificate`, CRDT writes/filtering, selected publisher choice, PubSub retry/notification, receiver behavior, restart scenario, late-contender scenario, production source, CMake, or timeout values. Commit the passing repair as `fix(12-08): stabilize publisher fixture lifecycle`.

After GREEN, build once and run exactly three fresh sequential focused `PublisherLossAfterPersistenceUsesDeterministicFailover` processes. Save each complete output at `publisher-repair-run-{1,2,3}.log` and append exactly one `P12_PUBLISHER_REPAIR_RUN run=&lt;1|2|3&gt; process_exit=&lt;status&gt; trace=&lt;relative-path&gt;` record to the summary; never rerun a failed process as a substitute for recording it. Then run exactly three separate normal serial `ctest --test-dir build/OSX/Release --output-on-failure --timeout 300 -R '^multi_node_finality_fault_test$'` processes and save each complete output in `serial-suite-run-{1,2,3}.log`. Only if all six processes pass may the summary set `repair_verification=passed` and `PHASE_12_STATUS=READY_FOR_REVERIFY`. Otherwise write the failed command/run/trace, set `repair_verification=failed` and `PHASE_12_STATUS=BLOCKED`, and stop without additional fixes. In both branches, run the additions-only shortcut/diff gate against `pre_task1_commit_sha` across committed, staged, and unstaged changes and record its result.</action>
  <verify>
    <automated>p12_dir='.planning/phases/12-multi-node-finality-fault-proof' &amp;&amp; p12_summary="$p12_dir/12-08-SUMMARY.md" &amp;&amp; p12_base="$(sed -n 's/^pre_task1_commit_sha=//p' "$p12_summary")" &amp;&amp; p12_diag_count="$(rg '^P12_PUBLISHER_READINESS_DIAG .*outcome=failure ' "$p12_summary" | sed -E 's/.*boundary=([^ ]+) state=([^ ]+) error=([^ ]+).*/\1 \2 \3/' | sort | uniq -c | awk '$1 &gt;= 2 {print $1}' | wc -l | tr -d '[:space:]')" &amp;&amp; if rg -x 'repair_authorization=fixture-lifecycle' "$p12_summary"; then test "$p12_diag_count" -ge 1 || exit 1; cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; p12_test='build/OSX/Release/test_bin/multi_node_finality_fault_test' &amp;&amp; for p12_run in 1 2 3; do if pgrep -f 'multi_node_finality_fault_test' &gt;/dev/null; then exit 1; fi; "$p12_test" --gtest_filter='FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover' --gtest_brief=1 || exit 1; done &amp;&amp; for p12_run in 1 2 3; do ctest --test-dir build/OSX/Release --output-on-failure --timeout 300 -R '^multi_node_finality_fault_test$' &gt;"$p12_dir/12-08-traces/serial-suite-run-$p12_run.log" 2&gt;&amp;1 || exit 1; done &amp;&amp; rg -x 'repair_verification=passed' "$p12_summary" &amp;&amp; rg -x 'PHASE_12_STATUS=READY_FOR_REVERIFY' "$p12_summary"; else test "$p12_diag_count" -eq 0 || rg -q '^fixture_lifecycle_proof=absent$' "$p12_summary" || exit 1; test -z "$(git diff --name-only HEAD -- test/src/blockchain/multi_node_finality_fault_test.cpp src test/src/blockchain/CMakeLists.txt)" &amp;&amp; rg -x 'repair_authorization=none' "$p12_summary" &amp;&amp; rg -x 'PHASE_12_STATUS=BLOCKED' "$p12_summary"; fi &amp;&amp; ! git diff --unified=0 "$p12_base"..HEAD -- test/src/blockchain/multi_node_finality_fault_test.cpp | rg '^\+[^+].*(AddPeers|SubmitProposal|Publish|CertificateReceived|OnConsensusCertificate|GlobalDB::Put|PutConvergentImmutable|Handle[A-Za-z]*|Force[A-Za-z]*(Timer|Due|Round)|sleep_for|sleep_until|retry|TIMEOUT|timeout)' &amp;&amp; test -z "$(git diff --name-only "$p12_base"..HEAD -- src test/src/blockchain/CMakeLists.txt)"</automated>
    <automated>p12_dir='.planning/phases/12-multi-node-finality-fault-proof'; p12_summary="$p12_dir/12-08-SUMMARY.md"; p12_file='test/src/blockchain/multi_node_finality_fault_test.cpp'; p12_base="$(sed -n 's/^pre_task1_commit_sha=//p' "$p12_summary")"; p12_snapshot="$(sed -n 's/^task1_snapshot_commit_sha=//p' "$p12_summary")"; test "$p12_snapshot" = "$(git rev-parse "$p12_snapshot")" &amp;&amp; git merge-base --is-ancestor "$p12_base" "$p12_snapshot" &amp;&amp; if rg -x 'repair_authorization=fixture-lifecycle' "$p12_summary"; then for p12_run in 1 2 3; do test -s "$p12_dir/12-08-traces/publisher-repair-run-$p12_run.log" || exit 1; done &amp;&amp; test "$(rg -c '^P12_PUBLISHER_REPAIR_RUN run=[123] process_exit=0 trace=\.planning/phases/12-multi-node-finality-fault-proof/12-08-traces/publisher-repair-run-[123]\.log$' "$p12_summary")" -eq 3; else rg -x 'repair_authorization=none' "$p12_summary" &amp;&amp; rg -x 'PHASE_12_STATUS=BLOCKED' "$p12_summary"; fi &amp;&amp; p12_source_diffs="$( { git diff --unified=0 "$p12_base"..HEAD -- "$p12_file"; git diff --cached --unified=0 "$p12_base" -- "$p12_file"; git diff --unified=0 "$p12_base" -- "$p12_file"; } )" &amp;&amp; p12_connect_diffs="$(printf '%s\n' "$p12_source_diffs" | rg '^[+-][^-+].*ConnectAndWaitForPeers' || true)" &amp;&amp; if test -n "$p12_connect_diffs"; then test "$(printf '%s\n' "$p12_connect_diffs" | sed '/^$/d' | wc -l | tr -d '[:space:]')" -eq 1 &amp;&amp; printf '%s\n' "$p12_connect_diffs" | rg -x '^\+[[:space:]]*ConnectAndWaitForPeers\( Peers\( network \) \);$'; fi &amp;&amp; test "$(git show "$p12_snapshot:$p12_file" | awk '/static void ConnectAndWaitForPeers/{capture=1} capture{print; opens=gsub(/\{/,"{"); closes=gsub(/\}/,"}"); depth+=opens-closes; if(opens) started=1; if(started &amp;&amp; depth==0) exit}')" = "$(awk '/static void ConnectAndWaitForPeers/{capture=1} capture{print; opens=gsub(/\{/,"{"); closes=gsub(/\}/,"}"); depth+=opens-closes; if(opens) started=1; if(started &amp;&amp; depth==0) exit}' "$p12_file")" &amp;&amp; ! printf '%s\n' "$p12_source_diffs" | rg '^[+-][^-+].*(SubmitCertificate|FilterCertificate|CertificateReceived|OnConsensusCertificate|ProcessCommittedCertificate|ProcessCertificates|IsCurrentAggregator|OrderedActiveValidators|GetAggregatorRole|CertificateWrite|CertificatePersistedBarrier|CertificateNotifications|PutConvergentImmutable|GlobalDB::Put|GetDataStore\(\)->(put|remove)|\bPublish\(|retry|Retry|AddPeers|ConnectPeers|ConnectValidatorPeers|PeersFormConnectedTopology|ASSERT_WAIT_FOR_CONDITION|waitForCondition|sleep_(for|until)|std::chrono::seconds|TIMEOUT|timeout)' &amp;&amp; p12_restricted_changes="$( { git diff --name-only "$p12_base"..HEAD -- src test/src/blockchain/CMakeLists.txt; git diff --cached --name-only "$p12_base" -- src test/src/blockchain/CMakeLists.txt; git diff --name-only "$p12_base" -- src test/src/blockchain/CMakeLists.txt; } | sort -u)" &amp;&amp; test -z "$p12_restricted_changes" &amp;&amp; git diff --check "$p12_base"..HEAD &amp;&amp; git diff --cached --check &amp;&amp; git diff --check</automated>
    <automated>p12_summary='.planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md'; p12_file='test/src/blockchain/multi_node_finality_fault_test.cpp'; p12_snapshot="$(sed -n 's/^task1_snapshot_commit_sha=//p' "$p12_summary")"; test "$(git show "$p12_snapshot:$p12_file" | awk '/TEST_F\( FinalityFaultNetwork, PublisherLossAfterPersistenceUsesDeterministicFailover \)/{capture=1} capture{print; opens=gsub(/\{/,"{"); closes=gsub(/\}/,"}"); depth+=opens-closes; if(opens) started=1; if(started &amp;&amp; depth==0) exit}')" = "$(awk '/TEST_F\( FinalityFaultNetwork, PublisherLossAfterPersistenceUsesDeterministicFailover \)/{capture=1} capture{print; opens=gsub(/\{/,"{"); closes=gsub(/\}/,"}"); depth+=opens-closes; if(opens) started=1; if(started &amp;&amp; depth==0) exit}' "$p12_file")"</automated>
  </verify>
  <acceptance_criteria>The non-repeatable/non-fixture branch produces a complete terminal report with no Task-2 source edit and keeps Phase 12 blocked. The authorized branch contains a recorded RED failure, a test-harness-only GREEN repair, three retained `publisher-repair-run-{1,2,3}.log` files with matching `P12_PUBLISHER_REPAIR_RUN` records, three passing full serial suite processes, and static proof from committed, staged, and unstaged diffs that no shortcut, certificate/authority/receiver/CRDT/publication mutation, topology/wait/order change, timeout/CMake, or production-source addition was made. The original publisher-loss test body is byte-for-byte unchanged from `task1_snapshot_commit_sha`; the only allowed helper diff is the exact RED-test call allowlisted above.</acceptance_criteria>
  <done>Either evidence authorizes and verifies only the smallest fixture lifecycle/ownership repair, or the phase is explicitly blocked with a durable explanation and no speculative implementation change. TEST-05 is claimed ready only after all six required process runs pass; TEST-06 remains protected by the static additions gate.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Required behavior | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 12 | Production-path proof remains safe and live through publisher loss. | 12-08 Tasks 1–2 | COVERED | Resolves only the pre-fault readiness blocker; it does not claim post-fault protocol proof without the required passes. |
| REQ | TEST-05 | Persistence-before-advertisement and deterministic non-conflicting publisher-loss recovery. | 12-08 Tasks 1–2 | COVERED | Task 1 establishes that the fault can be reached; authorized Task 2 proves it with three focused and three serial runs. |
| REQ | TEST-06 | Real PubSub, CRDT, persistence, and Mint ingress without local shortcuts. | 12-08 Tasks 1–2 | COVERED | Public-state-only diagnostics and additions-only static gate preserve production-route evidence. |
| RESEARCH | — | Use real peers and bounded existing predicates; no sleeps, mocks, direct handlers, or direct datastore writes. | 12-08 Tasks 1–2 | COVERED | Task actions restrict edits to a passive test seam and fixture ownership only. |
| RESEARCH | — | `AddPeers` readiness is asynchronous and must be observed, not assumed. | 12-08 Task 1 | COVERED | Snapshot identifies the first existing public readiness predicate without changing connection behavior. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16 | Existing topology, durability, ingress, restart, and diagnostic constraints remain non-regression boundaries. | 12-08 Task 1 | COVERED | Explicit preservation and additions-only static audit; no earlier-scope behavior is reopened. |
| CONTEXT | D-17 | Diagnose publisher readiness before persistence/barrier/fault. | 12-08 Task 1 | COVERED | Focused three-process publisher-loss gate ends at ConnectAndWaitForPeers evidence. |
| CONTEXT | D-18 | Snapshot only named public readiness and lifecycle facts. | 12-08 Task 1 | COVERED | Exact snapshot schema and prohibitions are in Task 1. |
| CONTEXT | D-19 | Require two matching fresh failures before repair; otherwise block. | 12-08 Tasks 1–2 | COVERED | Deterministic authorization branch and terminal evidence-report branch. |
| CONTEXT | D-20 | Repair only fixture lifecycle/ownership, then prove targeted and serial passes. | 12-08 Task 2 | COVERED | RED/GREEN scope and six-run verification enforce the limit. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture-owned observer → real GossipPubSub host | Diagnostic code may read public state but must not affect peer membership, transport, or message publication. |
| peer lifecycle → listener/RocksDB/I/O ownership | A retained runtime object or port/root lifecycle error can create a false readiness failure before protocol execution. |
| fresh evidence → repair authority | Intermittent symptoms must not authorize a retry, timeout, or production workaround. |
| test harness → finality protocol | A fixture repair must not become an author, receiver, CRDT writer, publisher selector, or Mint driver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-21 | T/E | readiness snapshot seam | mitigate | D-18 confines it to existing public `IsStarted`, host connectedness, topic mesh count, identity, and lifecycle reads; additions-only scan rejects delivery/author methods. |
| T-12-22 | D | `Peer` listener/root/I/O lifecycle | mitigate | Three isolated real-socket runs normalize the first predicate; only repeated fixture-owned evidence can enter RED/GREEN repair. |
| T-12-23 | T/D | publisher-loss regression | mitigate | D-19 rejects timeout/retry/ordering changes and requires three targeted plus three serial passes after any repair. |
| T-12-24 | T/E | finality ingress and certificate authority | mitigate | TEST-06 static diff gate rejects direct handlers, CRDT writes, forced rounds, sleeps, retry/publication changes, CMake, and production-source edits. |
| T-12-SC | Tampering | test/build changes | mitigate | No package installation occurs; `git diff --check` and additions-only static checks are required before either terminal branch. |
</threat_model>

<verification>
1. Build `multi_node_finality_fault_test` before each evidence/repair cycle.
2. Task 1 runs three fresh sequential publisher-loss processes and persists full traces plus three normalized readiness classifications.
3. Task 2 blocks with an evidence report unless repeated diagnostics prove a fixture lifecycle/ownership defect.
4. An authorized repair follows RED then GREEN in the test harness only, passes three fresh focused publisher-loss processes and three normal serial full-suite CTest processes, and passes `git diff --check` plus additions-only shortcut/protocol scans.
</verification>

<success_criteria>
- TEST-05 has either reproducible pre-fault fixture-owned evidence followed by six passing publisher/suite processes, or an explicit blocked no-repair diagnosis; it is never papered over by retries or longer waits.
- TEST-06 remains demonstrably production-route-only: no direct author/receiver/CRDT route, mock, forced timing, timeout/CMake change, or production source edit is introduced.
- The plan's work stays confined to `PublisherLossAfterPersistenceUsesDeterministicFailover` before selected-publisher persistence, barrier entry, and fault execution.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md` when done.
</output>
