---
phase: 12-multi-node-finality-fault-proof
plan: "02"
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "Competing different Mint proposals for one verified burn converge on one canonical slot, one durable authoritative certificate, and one exact Mint winner on every peer."
    - "A contender introduced after durable vote or certificate boundaries cannot create a second usable vote, certificate, Mint effect, UTXO, or bridge marker."
    - "A passive PubSub recipient receives and durably recovers finality without attempting an authoritative certificate write or stalling on self-written CID synchronization."
  artifacts:
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "Same-burn contention and late-contender/passive-recipient production-route scenarios"
      contains: "SameBurnContentionUsesOneCanonicalSlotAndExactMint"
  key_links:
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "ConsensusManager::SubmitProposal"
      via: "public proposal submission across disconnected then connected real peers"
      pattern: "SubmitProposal"
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "GlobalDB /cert/<slot>"
      via: "durable readback after real propagation and after peer recreation"
      pattern: "/cert/"
---

<objective>
Prove multi-node same-burn contention, late-contender safety, and passive-recipient receive-only behavior using the completed real-route harness.

Purpose: Turn Phase 8–11 finality guarantees into hostile-delivery regression proof without bypassing their production ownership.
Output: Two named production-path scenarios that cover TEST-01, TEST-02, and TEST-03.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md
@.planning/phases/12-multi-node-finality-fault-proof/12-PATTERNS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VALIDATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove same-burn contention resolves one canonical exact winner</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp — extend the completed `FinalityFaultNetwork` and its shared durable assertion helpers; do not duplicate lifecycle or persistence utilities.
    - .planning/phases/12-multi-node-finality-fault-proof/12-01-SUMMARY.md — source-of-truth for the completed friend accessors, counter names, barrier lifecycle, and harness ownership.
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp — source-of-truth for constructing signed competing proposals through public `CreateProposal` and `SubmitProposal`.
    - test/src/account/transaction_manager_certificate_fallback_test.cpp — source-of-truth for exact winner/loser certificate and Mint assertions.
    - test/testutil/wait_condition.hpp — mandatory source-of-truth for bounded condition waits.
  </read_first>
  <behavior>
    - Two differently proposed Mint V2 transactions representing the same verified burn return the same canonical slot before networking starts.
    - Three real validators start disconnected, submit the competing proposals through public production APIs, then connect at a named real-peer barrier and converge on one durable certificate/winner.
    - The passive fourth peer receives the exact winner through normal propagation and all four durable stores pass `AssertSingleDurableMint` before and after restart.
  </behavior>
  <action>Per D-01 through D-04, D-08, and D-10, add the named `SameBurnContentionUsesOneCanonicalSlotAndExactMint` GoogleTest case. Construct two valid, differently sourced/proposed `MintTransactionV2` candidates that preserve the same verified chain, token, source transaction, amount, and destination; first assert equal `GetSlotID()` values. Start the three validators isolated, create and submit both proposals only through public `ConsensusManager::CreateProposal`/`SubmitProposal`, then connect the real validator topology through `AddPeers` at a named condition barrier. Wait with bounded predicates for the existing production consensus/CRDT path to produce one `/cert/&lt;slot&gt;`, and assert its embedded proposal and exact Mint hash select one winner rather than the loser. Connect the passive recipient through real peers and assert all four nodes recover the same accepted winner through ordinary notification/committed readback/Mint handler flow. Reuse the harness durable helper to restart every peer from its own root and prove the winner-only UTXO/outpoints, no losing-Mint effect, one Mint counter, and one bridge marker. Do not inject proposals, votes, certificates, transaction data, or finality through `Handle*`, `CertificateReceived`, `OnConsensusCertificate`, `GlobalDB::Put*`, mocked transport, forced timers, or sleeps.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure &amp;&amp; ! rg -n 'ConsensusPendingLifecycleTestAccess::(Handle|CertificateReceived|WriteLiveCertificate|Force)|\bHandle[A-Za-z]*\(|\bCertificateReceived\(|\bOnConsensusCertificate\(|\bGlobalDB::Put|\bPutConvergentImmutable\(|sleep_for|sleep_until|Force[A-Za-z]*(Timer|Due|Round)' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
  </verify>
  <acceptance_criteria>The named test proves TEST-01 through one shared slot, one authoritative `/cert/&lt;slot&gt;`, one exact winner, and durable per-node winner-only effects across restart. Its test source contains no handler, direct-datastore, forced-timer, mock-transport, or sleep synchronization shortcut capable of creating protocol state.</acceptance_criteria>
  <done>The same-burn contention proof passes on the normal CTest target using four real peers and durable per-node assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove late contenders fail closed and passive recipients stay receive-only</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp — extend the completed fixture, route counters, and first scenario; keep each GoogleTest case self-contained.
    - .planning/phases/12-multi-node-finality-fault-proof/12-01-SUMMARY.md — source-of-truth for safe counter semantics and real-peer lifecycle helpers.
    - .planning/phases/09-durable-one-vote-finality/09-02-SUMMARY.md — source-of-truth for active-vote no-revote and durable accepted-certificate boundaries.
    - .planning/phases/10-authoritative-slot-certificate-publication/10-02-SUMMARY.md — source-of-truth for selected-publisher authority and receiver-no-write behavior.
    - test/testutil/wait_condition.hpp — mandatory source-of-truth for bounded waits only.
  </read_first>
  <behavior>
    - A losing contender offered after durable vote persistence and again after final certificate acceptance does not produce a new usable vote or certificate.
    - The passive peer reports zero authoritative certificate-write attempts while receiving notification and completing durable readback/Mint recovery.
    - Counter and reopened-store assertions distinguish receive-only behavior from a merely converged in-memory network.
  </behavior>
  <action>Per D-03, D-04, D-07, D-08, and D-10, add `LateContenderAndPassiveRecipientRemainReceiveOnly`. Drive an initial winning proposal through the real validator mesh and wait for the read-only post-active-vote-persistence boundary before introducing the losing contender through public proposal submission; repeat the late admission after a bounded predicate proves accepted authoritative certificate state. Assert no second exact active-vote publication/record, no second usable certificate content, and no losing Mint outcome after normal propagation. Keep the passive non-validator disconnected until the named topology point, reconnect it using real `AddPeers`, then assert its certificate-write-attempt counter remains zero while notification receipt and committed durable recovery complete without self-CID timeout. Assert the original immutable `/cert/&lt;slot&gt;` remains the only durable authority on every peer, then use the existing restart/readback helper to prove one exact winner UTXO effect and bridge marker per peer. Test synchronization may inspect counters or arm/release the existing post-action barriers only; it must not invoke `Handle*`, `CertificateReceived`, `OnConsensusCertificate`, `GlobalDB::Put*`, `PutConvergentImmutable`, forced timers, a mock transport, or a sleep.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure &amp;&amp; ! rg -n 'ConsensusPendingLifecycleTestAccess::(Handle|CertificateReceived|WriteLiveCertificate|Force)|\bHandle[A-Za-z]*\(|\bCertificateReceived\(|\bOnConsensusCertificate\(|\bGlobalDB::Put|\bPutConvergentImmutable\(|sleep_for|sleep_until|Force[A-Za-z]*(Timer|Due|Round)' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
  </verify>
  <acceptance_criteria>The named scenario proves TEST-02 and TEST-03 with a late contender after both required durable boundaries, zero passive-recipient certificate-write attempts, bounded progress to durable recovery, and reopened per-node stores showing no duplicate or losing effect.</acceptance_criteria>
  <done>Late contention and receive-only propagation are covered by a passing self-contained real-route regression.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - D-01/D-02: The fixed four-peer network and independent durable roots prove outcomes beyond a single-node memory state.
    - D-03/D-04: Both scenarios establish ordering through public ingress and actual connect/reconnect lifecycle only.
    - D-07: Counter values are corroborated by durable readback, especially the passive peer's zero authority writes.
    - D-08/D-10: Named scenarios prove one exact winner and no losing or duplicate per-node effect.
  </truths>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| competing remote proposal → validator consensus | Late or conflicting data must not escape the canonical-slot active-vote lock. |
| selected publisher → passive recipient | A receiver is untrusted to confer authority and must only recover a committed certificate. |
| test observation → production path | Tests may observe durable outcomes but cannot synthesize a valid state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | T | late contender / active-vote record | mitigate | Submit contender only via real PubSub path after observed durable boundaries; assert no second vote or immutable certificate and no losing Mint after reopen. |
| T-12-06 | T/E | passive recipient / certificate authority | mitigate | Assert zero authoritative-write attempts while requiring real notification, committed readback, and Mint completion. |
| T-12-07 | R/T | integration test driver | mitigate | Source gate forbids direct `Handle*`, certificate receive/consumer calls, GlobalDB writes, forced timers, mock transport, and sleeps in scenario creation. |
</threat_model>

<verification>
After each task, build and run `multi_node_finality_fault_test`, then run its anti-shortcut source gate. At wave completion, also run `consensus_pending_lifecycle_test` and `transaction_manager_certificate_fallback_test` alongside the new target.
</verification>

<success_criteria>
1. Same-burn contention produces exactly one canonical slot/certificate/winner and one exact durable Mint per peer.
2. Late competitors cannot acquire a second usable vote or certificate.
3. A passive recipient performs zero certificate authority writes, still makes bounded durable progress, and never duplicates a Mint effect.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-02-SUMMARY.md` when done.
</output>
