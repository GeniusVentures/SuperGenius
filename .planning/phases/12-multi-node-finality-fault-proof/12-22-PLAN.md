---
phase: 12-multi-node-finality-fault-proof
plan: 22
type: execute
wave: 22
depends_on: ["12-21"]
files_modified:
  - .planning/STATE.md
  - .planning/phases/12-multi-node-finality-fault-proof/round5-traces/
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]
must_haves:
  truths:
    - "The gate-entry rule is re-evaluated on the record BEFORE any run, per 12-20's own contract with round-5 inputs: the graduated Option A attributed fix exists on disk (round5-traces/option-a-restart-{1,2,3}.log all green), the build is current (every phase binary postdates every source this round touched), and the only live residual beyond the repair is the pre-declared gate-eligible PublisherLoss child-readiness (~18%/run) — or the series is withheld again with the STOP recorded, the no-reroll budget unspent, and nothing synthesized"
    - "Three consecutive serial full multi_node_finality_fault_test runs pass on the final build — per-run xunit XML copies (round5-traces/gate-{a,b,c}.xml) each carrying all four fault-scenario case names as green testcase entries with zero <failure> elements, zero new crash reports against the recorded baseline — OR the series stops at the first strike with every log preserved verbatim, the strike attributed by name, and no reroll (restarting the exact-once and publisher-loss stability property of VERIFICATION gap 2 inside all three XMLs)"
    - "The sibling matrix — smoke, lifecycle, cert-fallback (its FIRST run on the round-4+ build generation), ownership — passes serially on the same build generation as the gate series"
    - "STATE.md resolves the 12-14 evidence-gap blocker honestly: closed with citations on a passed series, or recorded still-open with the strike attributed on a failed series; REQUIREMENTS.md is untouched (TEST-01..TEST-06 are already marked Complete — the outstanding obligation was always the evidence gate, not the semantics)"
  artifacts:
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/crash-baseline.txt"
      provides: "the pre-series crash-report baseline (date/time, 1-min load, newest ~/Library/Logs/DiagnosticReports/*.ips identity or an explicit none-exist note) enabling the zero-new-crash assertion"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-a.log"
      provides: "gate run A: full ctest output including the '100% tests passed, 0 tests failed' trailer — case-level evidence lives in gate-a.xml because the single-entry CTest registration never prints passing gtest case lines"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-a.xml"
      provides: "gate run A: per-run copy of build/OSX/Release/xunit/xunit-multi_node_finality_fault_test.xml with the four scenario case names as passing testcase entries and zero <failure> elements"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-b.log"
      provides: "gate run B: ctest trailer (present only if run B executed — absent after an honest run-A strike)"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-b.xml"
      provides: "gate run B: per-run xunit copy with the four case names green"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-c.log"
      provides: "gate run C: ctest trailer (present only on the all-green path)"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-c.xml"
      provides: "gate run C: per-run xunit copy with the four case names green"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/sibling-matrix.log"
      provides: "the four sibling suites' serial results on the same build generation, cert-fallback included"
    - path: ".planning/STATE.md"
      provides: "the dated 12-22 record: gate-entry outcome, series outcome with per-run loads/durations/XML paths, crash-baseline identity with zero new reports, sibling-matrix outcome, and the honest 12-14 blocker resolution"
  key_links:
    - from: "12-22 gate-entry check"
      to: "the round-5 graduated attributed fix"
      via: "direct verification that round5-traces/option-a-restart-{1,2,3}.log each carry the RestartAtVote OK line and zero wait-timeout lines BEFORE run A"
      pattern: "option-a-restart-[123]\\.log"
    - from: "per-run gate XML copies"
      to: "the four fault-scenario case names"
      via: "xunit testcase entries (classname FinalityFaultNetwork) with zero <failure> children, asserted on the COPIES because every ctest run regenerates and overwrites the shared build-tree xunit file"
      pattern: "name=\"(SameBurnContentionUsesOneCanonicalSlotAndExactMint|LateContenderAndPassiveRecipientRemainReceiveOnly|RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce|PublisherLossAfterPersistenceUsesDeterministicFailover)\""
    - from: "the sibling matrix"
      to: "the four sibling CTest targets"
      via: "one serial ctest -R alternation on the same build generation as the gate series, cert-fallback explicitly inside the pattern (no recorded run on the round-4+ generation exists)"
      pattern: "transaction_manager_certificate_fallback_test"
---

<objective>
Round-5 gap closure, part 2 of 2: re-attempt the three-consecutive-serial-pass evidence gate (VERIFICATION gap 1 / truth 11) under the no-reroll rule carrying 12-21's graduated Option A fix, re-demonstrate restart exact-once and publisher-loss as a stable property inside all three per-run gate XMLs (gap 2 / truth 13), run the sibling matrix including the never-yet-run cert-fallback on this build generation, and resolve the 12-14 evidence-gap blocker honestly in STATE.md.

Purpose: this is the standard that has stood open since 12-14. Rounds 3 and 4 each ended in a plan-faithful gate-entry STOP (12-17, 12-20) because no attributed fix carried. Round 5 changes that input: 12-21 graduates the developer-funded Option A repair with all-green focused evidence. This plan spends the round's single no-reroll evidence budget ONLY if the gate-entry check passes clause-by-clause — and if it does not, withholding the series again with the budget unspent is the correct, plan-faithful outcome, never a failure.

Output: round5-traces/crash-baseline.txt; gate-{a,b,c}.log with per-run gate-{a,b,c}.xml copies (or the honest stop at the first strike); round5-traces/sibling-matrix.log; the dated STATE.md resolution. All evidence in-repo, never /tmp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-20-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-21-SUMMARY.md

<interfaces>
Command facts (12-20's verified contract, carried forward unchanged except the traces directory):

- Full-suite serial invocation: ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure (CTest TIMEOUT 300s; a healthy full pass is around 211s).
- Sibling targets (one serial alternation): multi_node_finality_fault_compatibility_smoke_test, consensus_pending_lifecycle_test, transaction_manager_certificate_fallback_test, multi_node_finality_fault_process_ownership_test.
- Port preflight: lsof -nP -iTCP:54601-54634 -sTCP:LISTEN must be empty.
- Crash reports: ~/Library/Logs/DiagnosticReports/*.ips (newest phase-related report before round 4: 2026-09-02-125541).
- The xunit output-capture fact (verified against test/src/blockchain/CMakeLists.txt): the registration is single-entry (:108-110), the command appends --gtest_output=xml:${CMAKE_BINARY_DIR}/xunit/xunit-multi_node_finality_fault_test.xml (:104-107), and the TEST_LAUNCHER runner REGENERATES and OVERWRITES that file on every ctest run; a green run under --output-on-failure prints NO gtest case lines. Therefore: the per-run copy IS the case-name evidence, taken immediately after each run; the ctest trailer is asserted in the log; on a FAILED run --output-on-failure DOES print the failing output, so strike attribution uses the log plus that run's XML.

The four case names (exact, classname FinalityFaultNetwork) asserted per XML:

```xml
name="SameBurnContentionUsesOneCanonicalSlotAndExactMint"
name="LateContenderAndPassiveRecipientRemainReceiveOnly"
name="RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce"
name="PublisherLossAfterPersistenceUsesDeterministicFailover"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate-entry re-check on the round-5 record, then crash baseline + phase-target rebuild + port preflight</name>
  <files>.planning/phases/12-multi-node-finality-fault-proof/round5-traces/crash-baseline.txt, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/12-21-SUMMARY.md (the graduation outcome — this task's clause-(a) input)
  - .planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-{1,2,3}.log (verify directly on disk, never from the summary alone: the OK lines and the zero-timeout condition)
  - .planning/phases/12-multi-node-finality-fault-proof/12-20-SUMMARY.md "Gate-Entry Check" and "Round-5 Re-Entry Requirements" (the clause structure this task re-evaluates with round-5 inputs)
  - test/src/blockchain/multi_node_finality_fault_test.cpp restart-mint block (verify Option A is present in source: the two new wait messages and their placement)
  - .planning/phases/12-multi-node-finality-fault-proof/12-15-SUMMARY.md verdict table (clause (c): SameBurn repaired; PublisherLoss child-readiness ~18%/run the one gate-eligible residual)
  - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md gaps 1-3 (the re-attempt scope: cert-fallback rides the sibling matrix)
  </read_first>
  <action>
  GATE-ENTRY CHECK — every input verified by direct read/grep this session, never trusted from a summary. If ANY clause fails, do NOT start the series: write the gate-entry STOP record into STATE.md (dated 12-22, naming the failed clause with citations), commit it, and END this plan with Tasks 2-3 marked N/A-by-stop — mirroring 12-17/12-20. Do not touch the 12-17/12-20 records.

  Clause (a) — a graduated attributed fix carries: round5-traces/option-a-restart-1.log, -2.log, AND -3.log each contain the line "[       OK ] FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce" and zero "Timed out waiting for condition" lines. If 12-21 ended in its honest STOP, this clause FAILS by design and the series is withheld.

  Clause (b) — build currency: rebuild all four phase targets (cmake --build build/OSX/Release --target multi_node_finality_fault_test --target multi_node_finality_fault_compatibility_smoke_test --target consensus_pending_lifecycle_test --target transaction_manager_certificate_fallback_test; the ownership target rides the multi_node binary), then assert that the four rebuilt phase-target binaries — build/OSX/Release/test_bin/multi_node_finality_fault_test, test_bin/multi_node_finality_fault_compatibility_smoke_test, test_bin/consensus_pending_lifecycle_test, test_bin/transaction_manager_certificate_fallback_test (the ownership CTest entry reuses the multi_node binary) — each have an mtime NEWER than the two round-5 sources (test/src/blockchain/multi_node_finality_fault_test.cpp and src/crdt/impl/graphsync_dagsyncer.cpp — the only sources round 5 touched; consensus_pending_lifecycle_test.cpp was a round-4 edit and is NOT in this comparison) — compare stat epochs and record them. Scope is per T-12-20-05: assert ONLY these four binaries vs these two sources, never every binary under test_bin/ (the directory holds ~136 entries including unrelated Sep-1 binaries such as multi_node_finality_runner that predate the round-5 sources — a directory-wide assert is unsatisfiable and would force a spurious gate-entry STOP).

  Clause (c) — no live-unrepaired signature beyond the pre-declared gate-eligible residual: the two Option A waits are present in the restart-mint block (grep the exact messages from 12-21); SameBurn's four-HasBridgeMarker predicate intact (test :2090-2096 region); CR-03 loops intact (consensus_pending_lifecycle_test.cpp registry reset, count 2); the PublisherLoss child-readiness intermittence (~18%/run, 12-15 Verdict 3, twice-reproduced discipline) is the one residual the gate declares eligible without a new decision — quote that characterization in the SUMMARY before run A.

  On all clauses passing: write round5-traces/crash-baseline.txt recording date/time, the current 1-min load, and the newest crash report identity (the output of: ls -t ~/Library/Logs/DiagnosticReports/*.ips | head -1 — or an explicit "none exist" if none). Port preflight: lsof -nP -iTCP:54601-54634 -sTCP:LISTEN must return empty (terminate stale holders and re-check if not). Record the gate-entry GO in the SUMMARY with the clause citations.
  </action>
  <acceptance_criteria>
  - Either the GO branch holds — crash-baseline.txt exists and predates gate-a.log, all three option-a logs verified green on disk, binary mtimes strictly newer than source mtimes (epochs recorded), both Option A wait messages present in source, port preflight empty — or the STOP branch holds: a dated 12-22 STATE.md entry names the failed clause with citations, no gate run was started, and round5-traces/ contains crash-baseline.txt and the gate/sibling artifacts NOT AT ALL (absence is the prescribed outcome, verified — never synthesized).
  - Clause (c)'s residual quote (PublisherLoss child-readiness, ~18%/run, 12-15 Verdict 3) appears in the SUMMARY before any run record.
  - The 12-17-SUMMARY.md and 12-20-SUMMARY.md records are untouched (git status shows no modification to them).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && ls .planning/phases/12-multi-node-finality-fault-proof/round5-traces/ ; for n in 1 2 3; do grep -c '\[       OK \] FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' .planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-$n.log; done ; grep -c "surviving peers retained the accepted certificate before publisher restart\|consensus-topic mesh re-formed with at least two topic peers" test/src/blockchain/multi_node_finality_fault_test.cpp ; lsof -nP -iTCP:54601-54634 -sTCP:LISTEN ; echo "preflight-done"</automated>
  </verify>
  <done>The gate-entry decision is made clause-by-clause on direct evidence before a single minute of the no-reroll budget is spent: GO with baseline+preflight in place, or the honest STOP with the failed clause named and the series withheld.</done>
</task>

<task type="auto">
  <name>Task 2 (entry condition: Task 1 GO): the three-run series — per-run xunit copies, four case names green, STOP-on-first-strike</name>
  <files>.planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-a.log, .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-a.xml, .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-b.log, .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-b.xml, .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-c.log, .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-c.xml, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/12-20-PLAN.md Task 1 Steps 2-4 (the run protocol this task executes verbatim, re-pointed at round5-traces/ — the immediate XML copy, the trailer assertion, the per-run crash check)
  - test/src/blockchain/CMakeLists.txt lines 100-135 (the single-entry registration, the xunit output flag, the 300s timeout, RUN_SERIAL, RESOURCE_LOCK — why the copy-before-next-run rule exists)
  - .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-2.log (the strike anatomy if RestartAtVote regresses: the wait-timeout lines that name the leg)
  </read_first>
  <action>
  For each run X in a, b, c — strictly serial, fresh port preflight and a recorded pre-run 1-min load header before each (below 2 preferred; if the external plateau persists, proceed only comfortably below the 12-12 contamination regime 15-20 and record the actual load — never proceed into contamination, never silently retry a completed run):

  1. Execute: ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure 2>&1 | tee .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-X.log (expect around 211s on a healthy pass; CTest budget 300s).
  2. IMMEDIATELY copy the xunit output before anything else can overwrite it: cp build/OSX/Release/xunit/xunit-multi_node_finality_fault_test.xml .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-X.xml — the file is regenerated by EVERY ctest run of this suite (CMakeLists.txt :104-110 via the TEST_LAUNCHER runner), so the copy IS the per-run case-name evidence. If the suite crashed or was killed before gtest wrote the XML, the missing copy is itself strike evidence: record that explicitly, never synthesize a placeholder.
  3. Assert on the run: gate-X.log contains "100% tests passed, 0 tests failed"; gate-X.xml contains all four exact case-name testcase entries (classname FinalityFaultNetwork: SameBurnContentionUsesOneCanonicalSlotAndExactMint, LateContenderAndPassiveRecipientRemainReceiveOnly, RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce, PublisherLossAfterPersistenceUsesDeterministicFailover) and zero <failure elements.
  4. Crash-freedom: the newest ~/Library/Logs/DiagnosticReports/*.ips still matches the crash-baseline.txt identity; a new .ips fails the series honestly on the crash-freedom clause.

  STOP-ON-FIRST-STRIKE: if ANY assertion fails (test failure, timeout, missing case name, missing XML copy, new crash report) — do not start the next run. Preserve the failing log verbatim (and that run's XML if produced), attribute the striking signature by name from the log lines and XML failure bodies (SameBurn / LateContender / RestartAtVote / PublisherLoss-child-readiness / unexpected-new — a PublisherLoss child-readiness strike is NOT forgiven mid-series; gate-eligibility was an entry condition, not a pass condition), write the honest series-FAILED record into STATE.md (the 12-14 blocker remains open; no reroll; route the next decision), commit, and end this task.

  On three green runs: write the series-PASSED record into STATE.md — the three log paths with durations and pre-run loads, the three XML paths carrying the four green case names (naming the RestartAtVote and PublisherLoss entries explicitly: gap 2's stable-property demonstration), the crash-baseline identity with zero new reports, and that the 12-14 evidence-gap blocker is CLOSED under the no-reroll rule carrying 12-21's graduated Option A fix. Update Session Continuity. Commit everything under round5-traces/ plus STATE.md.
  </action>
  <acceptance_criteria>
  - PASSED branch: gate-{a,b,c}.log each contain "100% tests passed, 0 tests failed"; gate-{a,b,c}.xml each exist, contain all four exact case-name testcase entries, and grep -c "<failure" returns 0 in each; no new ~/Library/Logs/DiagnosticReports/*.ips versus crash-baseline.txt; STATE.md closes the 12-14 blocker with these citations.
  - FAILED branch (either branch is plan-faithful): the striking run's log (and XML, if produced) is preserved verbatim; NO log or XML exists for any run after the strike; the strike is attributed by name in STATE.md; the 12-14 blocker is recorded still-open; no reroll occurred.
  - No /tmp path appears anywhere in the series evidence; the per-run XML copies are distinct files (gate-a.xml != gate-b.xml != gate-c.xml — e.g. differing timestamps/durations — proving they are per-run captures, not one file copied thrice).
  - Every log carries a pre-run load header; no run proceeded at contamination-range load.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && ls .planning/phases/12-multi-node-finality-fault-proof/round5-traces/ ; for f in a b c; do x=.planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-$f; if [ -f "$x.log" ]; then echo "gate-$f: $(grep -c '100% tests passed, 0 tests failed' $x.log) trailer, $(test -f $x.xml && grep -c '<failure' $x.xml) failures, $(test -f $x.xml && grep -cE 'name=\"(SameBurnContentionUsesOneCanonicalSlotAndExactMint|LateContenderAndPassiveRecipientRemainReceiveOnly|RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce|PublisherLossAfterPersistenceUsesDeterministicFailover)\"' $x.xml) cases"; fi; done ; ls -t ~/Library/Logs/DiagnosticReports/*.ips 2>/dev/null | head -1 ; grep -c "12-22" .planning/STATE.md</automated>
  </verify>
  <done>Either the three-consecutive-serial-pass standard is finally met on the final build with zero new crash reports and per-run case-name proof in durable committed XML — or the series stopped at the first strike with an honest preserved record. Never a reroll, never a softened outcome.</done>
</task>

<task type="auto">
  <name>Task 3: Sibling matrix including cert-fallback on the same build generation, then the honest STATE.md finalization</name>
  <files>.planning/phases/12-multi-node-finality-fault-proof/round5-traces/sibling-matrix.log, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md gap 1 missing-item 3 (why cert-fallback rides this matrix: no recorded run on the round-4+ build generation; smoke/lifecycle/ownership were re-verified fresh in round 4 but this build generation adds the WR-06 clamp and the Option A test edits)
  - .planning/phases/12-multi-node-finality-fault-proof/12-20-PLAN.md Task 2 (the matrix protocol: one serial ctest alternation, recorded either way — a gate strike does not invalidate matrix evidence)
  - .planning/STATE.md (the 12-14 blocker entry and the Session Continuity block this task finalizes)
  </read_first>
  <action>
  Execute the sibling matrix serially on the SAME build generation as the gate series (no rebuild between Task 2 and this task): ctest --test-dir build/OSX/Release -R '^(multi_node_finality_fault_compatibility_smoke_test|consensus_pending_lifecycle_test|transaction_manager_certificate_fallback_test|multi_node_finality_fault_process_ownership_test)$' --output-on-failure 2>&1 | tee .planning/phases/12-multi-node-finality-fault-proof/round5-traces/sibling-matrix.log — port preflight first (lsof 54601-54634 empty; the fixed-port 40001 constraint in base_crdt_test means this alternation MUST stay serial, never parallel). Expect four Passed entries (historical: ownership ~0.34s, smoke ~6.5s, cert-fallback ~32s, lifecycle ~43s).

  Finalize STATE.md per the actual outcome, never aspirationally: on a passed gate series, the 12-14 evidence-gap blocker entry is closed with citations to the round5-traces artifacts and Phase 12's round-5 scope is recorded complete; on a withheld entry (Task 1 STOP) the blocker stays open with the failed clause named; on a gate strike (Task 2 STOP) the blocker stays open with the strike attributed and the matrix outcome recorded alongside (a strike does not invalidate matrix evidence — record it either way, honestly). Update the Session Continuity block to the actual end state. REQUIREMENTS.md is deliberately untouched: TEST-01..TEST-06 have been marked Complete since 12-03 (git-verified in round 4); the outstanding obligation was always the evidence gate, not the semantics. Commit sibling-matrix.log and STATE.md with a docs(12-22) message.
  </action>
  <acceptance_criteria>
  - round5-traces/sibling-matrix.log exists, was produced on the same build generation as the gate series (no rebuild in between — the mtime epochs recorded in Task 1 still describe the binaries), and contains four ctest Passed entries including transaction_manager_certificate_fallback_test (its first recorded green run on the round-4+ generation) — or the failing sibling's output is preserved verbatim with the failure attributed in STATE.md.
  - STATE.md's 12-14 evidence-gap blocker entry reflects the actual end state: closed-with-citations, withheld-at-entry, or open-with-attributed-strike — the three branches are mutually exclusive and the written one matches the on-disk artifacts exactly.
  - Session Continuity updated; REQUIREMENTS.md shows no modification from this plan (git diff is empty for it).
  - No /tmp paths anywhere in the round-5 evidence; all artifacts committed.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && grep -cE "multi_node_finality_fault_compatibility_smoke_test|consensus_pending_lifecycle_test|transaction_manager_certificate_fallback_test|multi_node_finality_fault_process_ownership_test" .planning/phases/12-multi-node-finality-fault-proof/round5-traces/sibling-matrix.log ; grep -c "Passed" .planning/phases/12-multi-node-finality-fault-proof/round5-traces/sibling-matrix.log ; git diff --name-only -- .planning/REQUIREMENTS.md ; grep -c "12-22" .planning/STATE.md ; git status --short | head -10</automated>
  </verify>
  <done>The sibling matrix closes the cert-fallback coverage hole on this build generation, and STATE.md finalizes the round honestly on whichever branch the evidence dictates — the phase's record matches the artifacts, exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate runs -> loopback libp2p hosts (ports 54601-54634) | the full suite exercises real sockets; a stale port holder turns a clean failure into a false strike — hence the preflight |
| evidence chain (ctest logs / xunit XML / crash reports) -> the phase record | the artifacts ARE the proof; any substitution, late copy, or synthesis corrupts the no-reroll evidence standard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-22-01 | Repudiation (no-reroll integrity) | the three-run series | mitigate | STOP-on-first-strike is mechanical: after a strike, later-run logs must not exist; both branches are acceptance-checked; STATE.md records pass or fail without softening |
| T-12-22-02 | Information spoofing (environment) | load/port contamination producing false strikes | mitigate | port preflight before every run; actual 1-min load recorded per log header; never proceed into the 12-12 contamination regime; a hot run is recorded, never retried |
| T-12-22-03 | Repudiation (crash-freedom) | the zero-new-crash assertion | mitigate | crash-baseline.txt recorded BEFORE run A with the newest .ips identity; per-run re-check; a new .ips fails the series honestly on the crash-freedom clause |
| T-12-22-04 | Repudiation (per-run evidence substitution) | the overwritten xunit file | mitigate | the XML copy is taken immediately after each run, before the next run can overwrite it; a missing copy on a crash/timeout is recorded as strike evidence, never synthesized; the four-case assertion runs on gate-{a,b,c}.xml, not the shared build-tree file; the three copies must be distinct files |
| T-12-22-05 | Tampering (gate re-entry) | the gate-entry check | mitigate | the clause-by-clause check runs on direct reads/greps before run A; a failed clause forces the STOP record; the 12-17/12-20 records stay untouched (git-verified); graduation evidence is verified on disk, never trusted from a summary |
| T-12-22-06 | Tampering (requirements record) | REQUIREMENTS.md | mitigate | deliberately untouched: TEST-01..06 are already marked Complete and the gate is an evidence obligation, not a semantics change — an empty git diff for the file is an acceptance criterion |
| T-12-22-SC | Tampering | package installs | accept | no package-manager installs occur in this plan |
</threat_model>

<verification>
- Gate-entry: GO with all clauses cited and baseline+preflight in place, or the honest STOP with the failed clause named — no middle state.
- Series: three trailers + three distinct per-run XMLs with all four case names green and zero <failure>, zero new crash reports — or the preserved strike with later runs absent.
- Gap 2 closure evidence named explicitly: the RestartAtVote and PublisherLoss testcase entries green inside all three gate XMLs.
- Sibling matrix: four serial Passed entries including cert-fallback on this build generation.
- STATE.md: the 12-14 blocker resolved per the actual branch; Session Continuity current; REQUIREMENTS.md diff-empty.
- Everything committed under .planning/phases/12-multi-node-finality-fault-proof/round5-traces/ and STATE.md; zero /tmp references.
</verification>

<success_criteria>
- VERIFICATION gap 1 (truth 11) closes on the PASSED branch: the three-consecutive-serial-pass standard met with per-run case-name XML proof and zero new crash reports.
- VERIFICATION gap 2 (truth 13) closes with it: restart exact-once and publisher-loss green as a stable property inside all three per-run XMLs.
- The cert-fallback sibling coverage hole (no run on the round-4+ generation) is closed on the same build generation.
- On any honest-stop branch, the record matches the artifacts exactly and the no-reroll budget discipline is demonstrably intact (missing artifacts are absent, not synthesized).
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-22-SUMMARY.md` when done
</output>
