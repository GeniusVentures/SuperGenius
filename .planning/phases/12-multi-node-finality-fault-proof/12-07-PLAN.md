---
phase: 12-multi-node-finality-fault-proof
plan: "07"
type: tdd
wave: 7
depends_on: ["12-06"]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - src/account/TransactionManager.hpp
  - src/account/TransactionManager.cpp
  - .planning/phases/12-multi-node-finality-fault-proof/12-07-SUMMARY.md
  - .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/run-1.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/run-2.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/run-3.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/serial-suite-run-1.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/serial-suite-run-2.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/serial-suite-run-3.log
autonomous: true
gap_closure: true
requirements: [TEST-04]
must_haves:
  truths:
    - "A fresh real-socket restart failure is classified at the first existing Mint recovery boundary using passive, friend-scoped snapshots only."
    - "Per D-14, no production behavior changes unless the same broken boundary fails in at least two independently started real-socket processes."
    - "Per D-15, if a repair is authorized, three fresh targeted restart passes are required; three normal serial full-suite CTest runs are logged, and repair verification succeeds only if every required suite passes."
    - "Per D-16, a late-contender or publisher-loss failure after the TEST-04 proof is captured as a diagnostic-only blocked handoff for separately scoped work, not repaired or treated as a Task 2 crash."
  artifacts:
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "Passive Mint-boundary diagnostic snapshots, reproduction gate, and any observer-only regression."
    - path: "src/account/TransactionManager.cpp"
      provides: "Existing exact-Mint, UTXO-before-marker, marker, and tracked-state boundaries, changed only if the twice-reproduced diagnosis locates the defect there."
    - path: "src/blockchain/Consensus.cpp"
      provides: "Existing committed certificate readback and certificate-work-journal boundary, changed only if the twice-reproduced diagnosis locates the defect there."
    - path: ".planning/phases/12-multi-node-finality-fault-proof/12-07-SUMMARY.md"
      provides: "Three independent run results with stable trace paths, permitted first-boundary/state/error records, exact branch status, and repair authorization for Task 2."
  key_links:
    - from: "src/blockchain/Consensus.cpp"
      to: "src/account/TransactionManager.cpp"
      via: "committed certificate readback and registered nonce/Mint consumer dispatch"
      pattern: "RecoverPendingCertificateWork|ProcessCommittedCertificate"
    - from: "src/account/TransactionManager.cpp"
      to: "local RocksDB /bridge/executed marker"
      via: "existing UTXO effects followed by PersistBridgeExecutedMarker"
      pattern: "ParseTransaction.*PersistBridgeExecutedMarker"
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "multi_node_finality_fault_test CTest"
      via: "three fresh focused process invocations and three normal RUN_SERIAL suite invocations"
      pattern: "RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce"
---

<objective>
Diagnose the fresh-process Mint-marker restart failure at its first existing recovery boundary, and conditionally repair only a twice-reproduced one-boundary defect.

Purpose: Close TEST-04 only when evidence proves a stable Mint-boundary defect without changing the Phase 8–11 finality protocol or broadening into harness reliability work.
Output: Passive structured snapshots and either a narrowly tested repair with six passing executions, or a blocked structured diagnosis with no behavioral code change.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-durable-one-vote-finality/09-CONTEXT.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-CONTEXT.md
@.planning/phases/11-convergent-certificate-consumption-mint-recovery/11-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md
@.planning/phases/12-multi-node-finality-fault-proof/12-PATTERNS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW-FIX.md
@.planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-07-HANDOFF.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add passive Mint-boundary snapshots and establish the twice-reproduction gate</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, src/account/TransactionManager.hpp, src/account/TransactionManager.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-07-SUMMARY.md, .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/run-1.log, .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/run-2.log, .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/run-3.log</files>
  <read_first>
    - .planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md — D-11 through D-16 are locked: this is a restart/Mint-marker diagnosis only, snapshots are passive and friend-scoped, and late/publisher remain diagnostic-only.
    - .planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md and 12-07-HANDOFF.md — retain the valid-topology fresh-process evidence and do not reinterpret late/publisher failures as a production root cause.
    - test/src/blockchain/multi_node_finality_fault_test.cpp — extend only the existing restart scenario and `MultiNodeFinalityFaultTestAccess`; preserve its public `CreateProposal`/`SubmitProposal`, real `AddPeers`/PubSub, same-root restart, existing Mint-boundary driver, and enabled late/publisher tests.
    - src/blockchain/Consensus.cpp and src/blockchain/Consensus.hpp — inspect the existing committed certificate readback, exact handler dispatch, certificate-work journal, and test friendship; do not alter certificate authority, release, publication, retry, or scheduling semantics.
    - src/blockchain/Consensus.hpp and `Consensus.cpp` — apply the mapped existing friend/read-only seam: extend only a private `MultiNodeFinalityFaultTestAccess`-style friend surface and existing durable readback observation, never a handler, publisher, timer, or CRDT writer.
    - src/account/TransactionManager.cpp and src/account/TransactionManager.hpp — inspect the existing exact transaction lookup/binding, tracked-state, UTXO/outpoint, `PersistBridgeExecutedMarker`, and current fault-test friendship. Apply the mapped `CertificateFallbackTestAccess` seam and the existing Mint completion path; do not add a public API or a test failure injector.
    - test/src/blockchain/multi_node_finality_fault_test.cpp — apply the existing `FinalityFaultNetwork` fixture cleanup/restart pattern: retain the peer root, stop/join dependent runtime objects in their established order, then recreate from the same root without changing topology timing.
    - .planning/phases/09-durable-one-vote-finality/09-CONTEXT.md, 10-CONTEXT.md, and 11-CONTEXT.md — preserve the exact direct local active vote, CRDT certificate authority, receiver-no-write rule, exact Mint binding, UTXO-before-marker ordering, and no successor certificate re-advertisement.
  </read_first>
  <behavior>
    - Per D-11 and D-12, each snapshot contains only canonical identifiers, state enums/booleans, normalized error code, and monotonically increasing sequence number. It records no serialized certificate or transaction payload, RocksDB key, key material, or raw database value.
    - Per D-12, passive snapshots occur at the existing certificate durable-readback; exact transaction lookup/binding; UTXO/outpoint inspection; local bridge-marker read and write-result; certificate-work-journal; and tracked-transaction-state boundaries. They identify the first boundary whose state contradicts the existing exact-once contract.
    - Per D-13, snapshots do not arm/release a barrier, pause, retry, reorder, inject a failure, write/remove a CRDT or RocksDB value, start a second store, or change the existing real restart scenario. The established stop/recreate at the Mint boundary remains the sole fault driver.
    - Each independently started focused process emits one `P12_MINT_MARKER_DIAG` record with run identifier, first broken boundary, state, normalized error code, and sequence only. It must never print payloads or keys.
    - The reproduction gate is closed unless at least two of exactly three fresh, complete real-socket process launches fail at the identical first boundary with the same state/error classification. A passing run, a different boundary, or an invalid/pre-topology failure cannot authorize Task 2.
  </behavior>
  <action>Before editing any source, test, trace, or planning artifact, capture the exact current commit with `git rev-parse HEAD` and initialize `12-07-SUMMARY.md` with the single immutable line `pre_task1_commit_sha=&lt;40-character-SHA&gt;`. This is the pre-Task-1 baseline for every later additions-only audit; never replace it with a later `HEAD` or amend it after edits. Per D-11 through D-16, add a small test-owned `MintRecoverySnapshot`/collector behind the already-friend-scoped `MultiNodeFinalityFaultTestAccess`, explicitly following the mapped Consensus existing friend/read-only seam. It may read existing private state but exposes no production method and cannot mutate protocol state. At the ordered existing boundaries, append snapshots containing: canonical slot identifier and certificate identity/status after durable certificate readback; exact winning transaction identifier plus binding-result state/error after the existing lookup; expected winner outpoint identifier/count/state after the existing UTXO inspection; marker-present state plus normalized existing read/write result after the local `/bridge/executed/&lt;chain&gt;:&lt;burn&gt;` operations without recording that key; certificate-work-journal status; and tracked-transaction status. Record a monotonically increasing sequence number for each sample. Do not capture a certificate/transaction serialization, raw RocksDB bytes, a key string, account material, or any payload. Reuse the test's current production-route restart and its existing post-effects/pre-marker stop/recreate boundary exactly as it is, applying the mapped `FinalityFaultNetwork` fixture cleanup/restart order while retaining the peer root; do not add or arm any new pause/barrier.

If an existing component lacks a passive read-only observation needed for those fields, add only a private friend accessor in the current `ConsensusManager` existing friend/read-only seam or the mapped `TransactionManager::CertificateFallbackTestAccess`-style seam at the existing Mint completion path. Its implementation may translate existing state to the permitted identifier/state/error/sequence schema but must not call `Handle*`, `CertificateReceived`, `OnConsensusCertificate`, `FilterCertificate`, `GlobalDB::Put*`, `PutConvergentImmutable`, `remove`, timer/round forcing, or any retry/dispatch/publish operation. Do not modify `UTXOManager`, CMake, test timeouts, or any production public header/API.

Run exactly three fresh complete processes of `FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce`, waiting for prior process exit before the next launch. Persist each full process log at `.planning/phases/12-multi-node-finality-fault-proof/12-07-traces/run-{1,2,3}.log`. Classify each execution as `pass`, `invalid-topology`, or `failure` with its first one of `certificate-readback`, `exact-transaction-binding`, `utxo-outpoint`, `bridge-marker-read`, `bridge-marker-write`, or `journal-tracked-state`; include only state/error/sequence as supplemental fields. Before leaving this task, create `12-07-SUMMARY.md` and validate that it contains, for all three independent runs: one `P12_MINT_MARKER_RUN` line with process exit result and that stable relative trace path, then one verbatim permitted `P12_MINT_MARKER_DIAG` record with the first boundary/state/error/sequence. If two failures have the exact same boundary/state/error classification, record exactly `repair_authorization=one-boundary` and `status: authorized`, then permit Task 2. Otherwise record exactly `repair_authorization=none` and `status: blocked`, make no Task 2 source change, and stop Phase 12 as BLOCKED without treating this documented no-repair outcome as an implementation failure. Do not add retries, sleeps, timeout changes, fixture timing changes, or speculative repairs. Late-contender and publisher-loss stay enabled for later full-suite evidence but are not diagnosed or repaired here.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; p12_phase_dir='.planning/phases/12-multi-node-finality-fault-proof' &amp;&amp; mkdir -p "${p12_phase_dir}/12-07-traces" &amp;&amp; p12_summary="${p12_phase_dir}/12-07-SUMMARY.md" &amp;&amp; p12_base="$(sed -n 's/^pre_task1_commit_sha=//p' "$p12_summary")" &amp;&amp; test "$(printf '%s' "$p12_base" | rg -x '[0-9a-f]{40}' | wc -l | tr -d '[:space:]')" -eq 1 &amp;&amp; test "$p12_base" = "$(git rev-parse "$p12_base")" &amp;&amp; p12_diag_pattern='^P12_MINT_MARKER_DIAG run=[123] outcome=(pass|invalid-topology|failure) boundary=(none|certificate-readback|exact-transaction-binding|utxo-outpoint|bridge-marker-read|bridge-marker-write|journal-tracked-state) state=[^[:space:]]+ error=[^[:space:]]+ sequence=[0-9]+$' &amp;&amp; for p12_run in 1 2 3; do if pgrep -f 'multi_node_finality_fault_test' &gt;/dev/null; then exit 1; fi; P12_07_RUN="$p12_run" build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' &gt;"${p12_phase_dir}/12-07-traces/run-${p12_run}.log" 2&gt;&amp;1; p12_exit=$?; test "$p12_exit" -ge 0 || exit 1; test "$(rg -x "$p12_diag_pattern" "${p12_phase_dir}/12-07-traces/run-${p12_run}.log" | wc -l | tr -d '[:space:]')" -eq 1 || exit 1; done &amp;&amp; test -f "$p12_summary" &amp;&amp; test "$(rg -x "$p12_diag_pattern" "$p12_summary" | wc -l | tr -d '[:space:]')" -eq 3 &amp;&amp; test "$(rg -x 'P12_MINT_MARKER_RUN run=[123] process_exit=-?[0-9]+ trace=\.planning/phases/12-multi-node-finality-fault-proof/12-07-traces/run-[123]\.log' "$p12_summary" | wc -l | tr -d '[:space:]')" -eq 3 &amp;&amp; p12_matching_failures="$(rg -x "$p12_diag_pattern" "$p12_summary" | awk '$3=="outcome=failure" { print $4, $5, $6 }' | sort | uniq -c | awk '$1 &gt;= 2 { print $1 }' | head -n 1)"; if test -n "$p12_matching_failures"; then rg -x 'repair_authorization=one-boundary' "$p12_summary" &amp;&amp; rg -x 'status: authorized' "$p12_summary"; else rg -x 'repair_authorization=none' "$p12_summary" &amp;&amp; rg -x 'status: blocked' "$p12_summary"; fi</automated>
  </verify>
  <acceptance_criteria>Each fresh process creates exactly one permitted structured record with no payload/key content and a first-boundary classification based on passive snapshots of the required certificate, transaction/binding, UTXO/outpoint, marker, journal, and tracked-state boundaries. Task 2 is authorized only by at least two independently started failures sharing the exact boundary/state/error classification. Any other 3-run outcome is a terminal blocked handoff with `repair_authorization=none` and no behavioral code change.</acceptance_criteria>
  <done>The plan has either deterministic one-boundary evidence that authorizes Task 2 RED/GREEN work or a successful structured blocked handoff. The trace has not changed finality, transport, CRDT, persistence, timer, retry, or fixture-fault behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conditionally repair the one twice-reproduced Mint boundary and prove serial recovery</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, src/account/TransactionManager.hpp, src/account/TransactionManager.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-07-SUMMARY.md, .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/serial-suite-run-1.log, .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/serial-suite-run-2.log, .planning/phases/12-multi-node-finality-fault-proof/12-07-traces/serial-suite-run-3.log</files>
  <read_first>
    - .planning/phases/12-multi-node-finality-fault-proof/12-07-SUMMARY.md — consume and revalidate its immutable `pre_task1_commit_sha`, all three `P12_MINT_MARKER_RUN`/`P12_MINT_MARKER_DIAG` records, stable trace paths, `status`, and `repair_authorization`; Task 2 is forbidden unless two records have one identical first-boundary/state/error classification and `repair_authorization=one-boundary`.
    - .planning/phases/12-multi-node-finality-fault-proof/12-07-HANDOFF.md — preserve public ingress, real transport, CRDT authority, registered Mint ingress, exact Phase-9 local vote, and no successor re-advertisement.
    - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md — TEST-04 is the scoped gap; TEST-01/02/03/05/06 are preserved evidence, not newly fixed claims.
    - src/blockchain/Consensus.cpp and src/account/TransactionManager.cpp — locate only the exact diagnosed boundary before editing; retain every surrounding validation, journal, ordering, and error propagation rule. Use the mapped Consensus existing friend/read-only seam, `TransactionManager::CertificateFallbackTestAccess`/Mint completion path, and `FinalityFaultNetwork` fixture cleanup/restart pattern rather than inventing another control route.
    - .planning/phases/09-durable-one-vote-finality/09-CONTEXT.md, 10-CONTEXT.md, and 11-CONTEXT.md — preserve all established vote, certificate-authority, exact-binding, recovery, UTXO-before-marker, and idempotency invariants.
  </read_first>
  <behavior>
    - RED: a regression isolates the Task 1 boundary classification while the unchanged production-route restart creates all real data. It fails before the repair and asserts the required passive identifier/state/error sequence rather than a timing surrogate.
    - GREEN: modify the smallest single existing boundary selected by the two matching diagnostics. If every durable production state is correct and only an observer/fixture reported the wrong state, change only `multi_node_finality_fault_test.cpp`; do not modify production code.
    - Production repair is allowed only when the two matching diagnostics demonstrate that the existing boundary, rather than the observer/fixture, is wrong. It must retain normal certificate readback to exact transaction binding to idempotent UTXO/outpoint handling to marker completion through the existing journal/tracked-state recovery path.
    - After any authorized repair, three independently started focused TEST-04 processes pass. Capture each normal serial full `multi_node_finality_fault_test` CTest run in a stable log with its exit status. The all-required-suites-pass branch alone may record repair verification as successful.
    - Per D-16, if the focused TEST-04 proof passes but a serial full suite reports late-contender and/or publisher-loss failure, append each failing scenario's stable trace path, CTest exit status, and `d16-diagnostic-only` classification to the summary; make no change for those scenarios and end Phase 12 BLOCKED pending separately scoped work. This permitted diagnostic-only result is not a Task 2 crash.
  </behavior>
  <action>First consume and validate Task 1's `12-07-SUMMARY.md`, including its sole `pre_task1_commit_sha=&lt;40-character-SHA&gt;` line, all three independent run results, stable trace paths, permitted diagnostic records, exact `status`, and `repair_authorization`. Resolve that stored SHA with `git rev-parse` before any Task 2 edit; it is immutable and remains the TEST-06 additions-only baseline even if Task 1 or Task 2 creates commits. If fewer than two failures share the exact first boundary/state/error classification, or the summary says `repair_authorization=none`, do not edit any file in this task: preserve the three-record handoff, keep exactly `status: blocked`, and stop Phase 12 BLOCKED successfully. This branch is a documented no-repair outcome, not a failed implementation. Do not convert an inconclusive process into a retry, increase a timeout, add sleeps, relax assertions, alter CMake, change port/topology timing, or make a test-only workaround.

When—and only when—the summary establishes `repair_authorization=one-boundary`, write the smallest RED regression in the existing restart test for that one classified boundary. Keep `CreateProposal`/`SubmitProposal`, real `AddPeers`/PubSub, CRDT readback, registered Mint ingress, and the mapped `FinalityFaultNetwork` same-root cleanup/restart lifecycle as the sole data/fault route. If the snapshots show the certificate, exact transaction binding, UTXO/outpoint, marker, journal, and tracked state were already correct, repair only the passive observer/fixture in `multi_node_finality_fault_test.cpp` through the mapped Consensus existing friend/read-only seam. Otherwise change only the production file that owns the demonstrated boundary: committed certificate readback/journal in `Consensus.cpp`, or exact transaction/UTXO/marker/tracked completion in `TransactionManager.cpp` through its mapped `CertificateFallbackTestAccess`-style private seam and existing Mint completion path. Do not edit both production boundaries, add a new journal/API, move the UTXO-before-marker order, or broaden recovery.

Preserve all protocol invariants: the public proposal APIs and real `AddPeers`/PubSub route remain active; CRDT `/cert/&lt;canonical-slot&gt;` remains authoritative; passive recipients never write a certificate; the Phase-9 exact local direct RocksDB vote remains the only vote recovery record and releases only after matching durable certificate acceptance; and a publisher never re-advertises a successor certificate. Do not call direct handlers/receivers, direct CRDT/active-vote writes, mock transport, forced timers/rounds, failure injection, pause/retry/reorder code, assertion weakening, sleeps, timeout changes, or CMake changes.

After the three targeted TEST-04 processes pass, run three normal serial `ctest --test-dir build/OSX/Release --output-on-failure --timeout 300 -R '^multi_node_finality_fault_test$'` invocations without `|| exit`. Persist each complete CTest stdout/stderr log as `12-07-traces/serial-suite-run-{1,2,3}.log`, capture each actual CTest exit status, and append one exact `P12_SERIAL_SUITE_RUN run=&lt;N&gt; ctest_exit=&lt;status&gt; trace=.planning/phases/12-multi-node-finality-fault-proof/12-07-traces/serial-suite-run-&lt;N&gt;.log` line to the summary. For every log in which the GTest result identifies a `LateContender` and/or `PublisherLoss` failure, append one `P12_D16_DIAGNOSTIC run=&lt;N&gt; scenario=&lt;late-contender|publisher-loss&gt; ctest_exit=&lt;status&gt; classification=d16-diagnostic-only trace=&lt;stable-relative-log-path&gt;` line per failed scenario. Do not modify those scenarios, their assertions, or their production paths; they do not authorize a retry, repair, or expanded claim. If all three focused TEST-04 runs passed and every nonzero serial-suite exit is solely attributable to one or both of those D-16 scenarios, append `repair_verification=blocked-d16-diagnostic-only`, `status: blocked`, and `phase_disposition=blocked-pending-separately-scoped-d16-work`; end Phase 12 BLOCKED successfully rather than crashing this task. If a serial log identifies any other failing scenario, preserve its log and fail verification normally. Only when all three serial CTest exits are zero may the summary append `repair_verification=successful` and `status: verified`.

Use the immutable `pre_task1_commit_sha` captured before Task 1—not mutable `HEAD`—as the TEST-06 route-audit baseline. Inspect only added (`+`) lines from `git diff --no-ext-diff -U0 &lt;pre_task1_commit_sha&gt; -- test/src/blockchain/multi_node_finality_fault_test.cpp`. Reject only newly added direct ingress, direct persistence/active-vote writes, timing/round forcing, failure-injection, sleep, mock, or timeout behavior; existing production handlers and read-only active-vote accessors are intentionally outside this additions-only gate.</action>
  <verify>
    <automated>p12_phase_dir='.planning/phases/12-multi-node-finality-fault-proof'; p12_summary="${p12_phase_dir}/12-07-SUMMARY.md"; p12_diag_pattern='^P12_MINT_MARKER_DIAG run=[123] outcome=(pass|invalid-topology|failure) boundary=(none|certificate-readback|exact-transaction-binding|utxo-outpoint|bridge-marker-read|bridge-marker-write|journal-tracked-state) state=[^[:space:]]+ error=[^[:space:]]+ sequence=[0-9]+$'; test -f "$p12_summary" || exit 1; p12_base="$(sed -n 's/^pre_task1_commit_sha=//p' "$p12_summary")"; test "$(printf '%s' "$p12_base" | rg -x '[0-9a-f]{40}' | wc -l | tr -d '[:space:]')" -eq 1 || exit 1; test "$p12_base" = "$(git rev-parse "$p12_base")" || exit 1; for p12_run in 1 2 3; do test -f "${p12_phase_dir}/12-07-traces/run-${p12_run}.log" || exit 1; test "$(rg -x "$p12_diag_pattern" "${p12_phase_dir}/12-07-traces/run-${p12_run}.log" | wc -l | tr -d '[:space:]')" -eq 1 || exit 1; done; test "$(rg -x "$p12_diag_pattern" "$p12_summary" | wc -l | tr -d '[:space:]')" -eq 3 || exit 1; test "$(rg -x 'P12_MINT_MARKER_RUN run=[123] process_exit=-?[0-9]+ trace=\.planning/phases/12-multi-node-finality-fault-proof/12-07-traces/run-[123]\.log' "$p12_summary" | wc -l | tr -d '[:space:]')" -eq 3 || exit 1; p12_matching_failures="$(rg -x "$p12_diag_pattern" "$p12_summary" | awk '$3=="outcome=failure" { print $4, $5, $6 }' | sort | uniq -c | awk '$1 &gt;= 2 { print $1 }' | head -n 1)"; if git diff --no-ext-diff -U0 "$p12_base" -- test/src/blockchain/multi_node_finality_fault_test.cpp | sed -n '/^+[^+]/p' | rg -n '(->|\.)[[:space:]]*(Handle[A-Za-z]*|OnConsensusMessage|CertificateReceived|OnConsensusCertificate|FilterCertificate)[[:space:]]*\(|GlobalDB::Put|PutConvergentImmutable[[:space:]]*\(|ActiveVoteStorageKey[[:space:]]*\(|Set[A-Za-z]*(Failure|Hook|Barrier)[A-Za-z]*[[:space:]]*\(|Force[A-Za-z]*(Timer|Due|Round)[[:space:]]*\(|sleep_for|sleep_until|mock|CTEST.*TIMEOUT'; then exit 1; fi; if test -z "$p12_matching_failures"; then rg -x 'repair_authorization=none' "$p12_summary" &amp;&amp; rg -x 'status: blocked' "$p12_summary"; else rg -x 'repair_authorization=one-boundary' "$p12_summary" &amp;&amp; rg -x 'status: authorized' "$p12_summary" || exit 1; cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 || exit 1; for p12_targeted_run in 1 2 3; do P12_07_RUN="targeted-${p12_targeted_run}" build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' || exit 1; done; p12_d16_failure=0; p12_unexpected_suite_failure=0; for p12_suite_run in 1 2 3; do p12_suite_log="${p12_phase_dir}/12-07-traces/serial-suite-run-${p12_suite_run}.log"; ctest --test-dir build/OSX/Release --output-on-failure --timeout 300 -R '^multi_node_finality_fault_test$' &gt;"$p12_suite_log" 2&gt;&amp;1; p12_suite_exit=$?; printf 'P12_SERIAL_SUITE_RUN run=%s ctest_exit=%s trace=%s\n' "$p12_suite_run" "$p12_suite_exit" "${p12_phase_dir}/12-07-traces/serial-suite-run-${p12_suite_run}.log" &gt;&gt;"$p12_summary"; if test "$p12_suite_exit" -ne 0; then p12_matched_d16=0; if rg -q '^\[ *FAILED *\] FinalityFaultNetwork\..*LateContender' "$p12_suite_log"; then printf 'P12_D16_DIAGNOSTIC run=%s scenario=late-contender ctest_exit=%s classification=d16-diagnostic-only trace=%s\n' "$p12_suite_run" "$p12_suite_exit" "${p12_phase_dir}/12-07-traces/serial-suite-run-${p12_suite_run}.log" &gt;&gt;"$p12_summary"; p12_d16_failure=1; p12_matched_d16=1; fi; if rg -q '^\[ *FAILED *\] FinalityFaultNetwork\..*PublisherLoss' "$p12_suite_log"; then printf 'P12_D16_DIAGNOSTIC run=%s scenario=publisher-loss ctest_exit=%s classification=d16-diagnostic-only trace=%s\n' "$p12_suite_run" "$p12_suite_exit" "${p12_phase_dir}/12-07-traces/serial-suite-run-${p12_suite_run}.log" &gt;&gt;"$p12_summary"; p12_d16_failure=1; p12_matched_d16=1; fi; if rg '^\[ *FAILED *\] FinalityFaultNetwork\.' "$p12_suite_log" | rg -v '(LateContender|PublisherLoss)' &gt;/dev/null || test "$p12_matched_d16" -eq 0; then p12_unexpected_suite_failure=1; fi; fi; done; test "$p12_unexpected_suite_failure" -eq 0 || exit 1; if test "$p12_d16_failure" -eq 1; then printf '%s\n' 'repair_verification=blocked-d16-diagnostic-only' 'status: blocked' 'phase_disposition=blocked-pending-separately-scoped-d16-work' &gt;&gt;"$p12_summary"; rg -x 'repair_verification=blocked-d16-diagnostic-only' "$p12_summary" &amp;&amp; rg -x 'status: blocked' "$p12_summary" &amp;&amp; rg -x 'phase_disposition=blocked-pending-separately-scoped-d16-work' "$p12_summary"; else test "$(rg -x 'P12_SERIAL_SUITE_RUN run=[123] ctest_exit=0 trace=\.planning/phases/12-multi-node-finality-fault-proof/12-07-traces/serial-suite-run-[123]\.log' "$p12_summary" | wc -l | tr -d '[:space:]')" -eq 3 || exit 1; printf '%s\n' 'repair_verification=successful' 'status: verified' &gt;&gt;"$p12_summary"; rg -x 'repair_verification=successful' "$p12_summary" &amp;&amp; rg -x 'status: verified' "$p12_summary"; fi; fi</automated>
  </verify>
  <acceptance_criteria>If the twice-reproduction gate is closed, Task 2 produces no code change and leaves Phase 12 blocked with its structured diagnostic. If it is open, one RED regression and one smallest boundary repair (or observer/fixture-only correction where durable state was correct) produce three fresh targeted TEST-04 passes and three captured normal serial full-suite CTest results. Repair verification succeeds only if all three full suites pass. Per D-16, late-contender and/or publisher-loss failure after the focused proof instead produces the exact trace/status/classification records and a separately scoped blocked handoff, with no change or claim for TEST-01/02/03/05/06.</acceptance_criteria>
  <done>TEST-04 has a twice-evidenced, one-boundary result: either an evidence-backed smallest repair proves exact-once Mint-marker recovery across all six required executions, a D-16 diagnostic-only serial-suite result is explicitly blocked for separately scoped work, or no repair is made because the reproduction gate remained closed. No finality protocol invariant or test-route constraint has been weakened.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Required behavior | Plan coverage | Status |
|--------|----|-------------------|---------------|--------|
| GOAL | Phase 12 | Production-path finality proves safe restart recovery. | Tasks 1–2 diagnose/conditionally prove Mint-boundary recovery without altering protocol. | COVERED |
| REQ | TEST-01 | Same-burn contention remains one slot/certificate/winner. | Serial suite remains enabled; no new fixed claim. | PRESERVED |
| REQ | TEST-02 | Late contender cannot create a second usable vote/certificate. | Serial suite remains enabled; its outcome is recorded only. | PRESERVED |
| REQ | TEST-03 | Passive recipient remains receive-only. | Serial suite remains enabled; no receiver write path changes. | PRESERVED |
| REQ | TEST-04 | Vote/certificate/Mint-boundary restarts retain exact vote and avoid duplicate Mint. | Tasks 1–2 are the only scope: passive diagnosis, twice-reproduction gate, RED/GREEN, and six required passes. | COVERED |
| REQ | TEST-05 | Publisher loss retains persist-before-advertise/non-conflicting recovery. | Serial suite remains enabled; its outcome is recorded only. | PRESERVED |
| REQ | TEST-06 | Tests use production PubSub, CRDT, persistence, and Mint ingress. | The immutable `pre_task1_commit_sha`, captured before any Task 1 edit, drives the `git diff -U0 &lt;base&gt;` audit of test-file additions; it rejects new direct ingress/writes/forcing while permitting existing handlers and read-only active-vote accessors. | PRESERVED |
| RESEARCH | Durable recovery pattern/pitfalls | Reuse UTXO-before-marker, journal-driven recovery, bounded waits, and no direct handlers. | Passive snapshots and conditional one-boundary repair only. | COVERED |
| CONTEXT | D-01–D-10 | Four-peer real-transport, durable-root suite and enabled bounded scenarios. | Existing harness/scenarios are retained; no topology/CMake/timing alteration. | PRESERVED |
| CONTEXT | D-11 | Restrict 12-07 to fresh-process restart/Mint-marker diagnosis. | Tasks 1–2 exclude late/publisher/harness reliability repair. | COVERED |
| CONTEXT | D-12 | Passive friend snapshots at every listed recovery boundary without payload/key capture. | Task 1 schema and collector. | COVERED |
| CONTEXT | D-13 | No pause/retry/reorder/failure injection. | Task 1 uses existing driver; Task 2 forbids them. | COVERED |
| CONTEXT | D-14 | Require two independently started matching failures before RED/GREEN; observer-only if durable state correct. | Task 1 gate and Task 2 conditional branches. | COVERED |
| CONTEXT | D-15 | Three fresh targeted and three normal serial passes after repair; otherwise blocked. | Task 2 records all three serial CTest runs; only three zero exits verify the repair, while any allowed diagnostic outcome is blocked. | COVERED |
| CONTEXT | D-16 | Leave late/publisher enabled, record but do not act. | Task 2 captures each serial log and CTest exit; after passing TEST-04, a late/publisher failure is appended with exact trace/status/`d16-diagnostic-only` classification, then blocked for separately scoped work without a change. | COVERED |
| CONTEXT | Deferred bridge startup/RPC wiring | Must not enter this closure. | No task covers it. | EXCLUDED |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed CRDT certificate → exact Mint lookup | A durable certificate must dispatch only its exact bound Mint through the registered consumer. |
| Mint UTXO/outpoint effect → local bridge marker | A restart can expose incomplete durable suffix state; recovery must neither duplicate the effect nor lose marker completion. |
| test observer → production finality | Friend access sees internal state and must not become a public API, protocol writer, timing controller, or failure injector. |
| real peer lifecycle → restarted root | Stop/recreate must preserve the existing direct vote, CRDT authority, journal, and marker semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-19 | T/D | Certificate readback, journal, and Mint dispatch | mitigate | Task 1 snapshots durable state and error outcome in order; Task 2 changes only the twice-evidenced faulty boundary. |
| T-12-20 | T | UTXO/outpoint and `/bridge/executed` completion | mitigate | Preserve UTXO-before-marker order, observe marker read/write outcome without key/value capture, and require three targeted restart passes. |
| T-12-21 | E/T | `MultiNodeFinalityFaultTestAccess` | mitigate | Private friend-only passive schema; no production API, handler, CRDT/RocksDB write, timer force, pause, retry, or injection operation. |
| T-12-22 | S/T/R | Test route and evidence | mitigate | Real `CreateProposal`/`SubmitProposal`, `AddPeers`/PubSub, CRDT, and registered Mint ingress; independently started logs carry stable state/error/sequence evidence. |
| T-12-23 | T | Phase-9 vote / Phase-10 certificate publication | mitigate | Explicit preservation gate rejects active-vote changes, receiver writes, authority changes, retry changes, and successor certificate re-advertisement. |
| T-12-SC | Tampering | C++ source/test changes | mitigate | No dependency/package/CMake change; TDD RED/GREEN gate and six real-socket executions constrain any authorized edit. |
</threat_model>

<verification>
Task 1 is a hard evidence gate. Its three fresh process records must produce two identical failures before Task 2 may write a RED regression or change production behavior. If the gate remains closed, the executor writes the structured blocked handoff only and stops. If it opens, run exactly three fresh focused TEST-04 processes and capture three ordinary serial CTest executions of the full registered Phase-12 target with their logs and exit statuses. Only six passing executions record repair verification as successful. Per D-16, if the focused TEST-04 proof passes but a serial suite identifies late-contender and/or publisher-loss failure, append the exact trace/status/classification, make no change for those scenarios, and finish BLOCKED pending separately scoped work without treating that diagnostic-only result as a task crash.
</verification>

<success_criteria>
1. The first broken Mint recovery transition is identified from permitted passive snapshots, not timeout, payload/key logging, or a test-controlled fault.
2. No repair occurs without two independently started real-socket failures with the same boundary/state/error classification; absence of that evidence leaves Phase 12 blocked.
3. An authorized repair is limited to the exact one boundary, or to the observer/fixture when durable state is correct, and preserves all Phase 9–11 protocol invariants.
4. After an authorized repair, three fresh focused TEST-04 runs pass and three normal serial full-suite CTest logs/exit statuses are captured with late/publisher tests still enabled; only three zero CTest exits verify repair success, while a D-16-only failure is recorded and blocked for separately scoped work.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-07-SUMMARY.md` when done. It begins with the immutable pre-Task-1 commit SHA. If the Task 1 gate is closed, the summary must contain the three structured records, trace locations, `repair_authorization=none`, and `status: blocked`; do not modify production or fixture behavior after that stop. If a D-16-only serial-suite failure follows a passing TEST-04 proof, it must additionally contain each `P12_D16_DIAGNOSTIC` trace/status/classification record plus `repair_verification=blocked-d16-diagnostic-only` and the separately scoped blocked disposition.
</output>
