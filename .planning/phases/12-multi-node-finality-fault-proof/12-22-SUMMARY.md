---
phase: 12-multi-node-finality-fault-proof
plan: 22
subsystem: evidence-gate/gate-series
tags: [evidence-gate, no-reroll, gate-entry-go, series-failed, publisher-loss-child-readiness, stop-on-first-strike, sibling-matrix, cert-fallback, honest-record]
requires:
  - "12-21 (graduated Option A attributed fix: round5-traces/option-a-restart-{1,2,3}.log all green)"
  - "12-20 (gate-entry clause structure and round-5 re-entry contract)"
  - "12-15 verdict table (PublisherLoss child-readiness characterization, Verdict 3)"
provides:
  - "The round-5 gate verdict: gate-entry GO on all three clauses, series FAILED at run B on the pre-declared PublisherLoss child-readiness strike, STOP-on-first-strike honored, no reroll"
  - "Sibling matrix 4/4 green on the gate build generation, including cert-fallback's first recorded green run on the round-4+ generation (coverage hole closed)"
  - "Full-suite evidence that the graduated Option A repair holds: all four FinalityFaultNetwork scenario cases green inside BOTH executed gate runs (gap 2 partial evidence)"
  - "The routed developer decision: repair PublisherLoss child-readiness under D-25 vs. re-examine the gate standard vs. accept-and-close-on-amended-evidence"
affects:
  - "The 12-14 evidence-gap blocker (stays OPEN; next decision routed)"
  - "Phase 12 round-6 planning (whatever branch the developer selects)"
tech-stack:
  added: []
  patterns:
    - "per-run xunit copy taken immediately after each ctest run (the shared build-tree XML is regenerated by every run)"
    - "crash-report baseline recorded before run A with newest-.ips identity; per-run re-check"
key-files:
  created:
    - .planning/phases/12-multi-node-finality-fault-proof/round5-traces/crash-baseline.txt
    - .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-a.log
    - .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-a.xml
    - .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-b.log
    - .planning/phases/12-multi-node-finality-fault-proof/round5-traces/gate-b.xml
    - .planning/phases/12-multi-node-finality-fault-proof/round5-traces/sibling-matrix.log
  modified:
    - .planning/STATE.md
decisions:
  - "Run C was never started after run B's strike — STOP-on-first-strike is mechanical (T-12-22-01): a PublisherLoss child-readiness strike is NOT forgiven mid-series because gate-eligibility was an entry condition, not a pass condition"
  - "The matrix ran after the strike exactly as Task 3 prescribes — a gate strike does not invalidate sibling-matrix evidence"
  - "REQUIREMENTS.md deliberately untouched (T-12-22-06): TEST-01..TEST-06 are already Complete since 12-03; the outstanding obligation was always the evidence gate, not the semantics"
metrics:
  duration: 15m
  completed: 2026-09-03T17:43:34Z
---

# Phase 12 Plan 22: Round-5 Evidence Gate Re-attempt Summary

**SERIES FAILED AT RUN B on the pre-declared PublisherLoss child-readiness strike — the gate-entry check passed all three clauses (GO), run A passed clean 214.81s with all four scenario cases green in its per-run XML, run B struck at the exact 12-15 Verdict 3 child-readiness boundary (54264 ms), STOP-on-first-strike fired, run C was never started, and the sibling matrix still closed 4/4 green on the same build generation including cert-fallback's first recorded run. The 12-14 evidence-gap blocker stays OPEN with the next decision routed; the round-5 no-reroll budget was spent honestly on a legitimate re-entry. This is the plan's own prescribed faithful branch — never a reroll, never a softened outcome.**

## Gate-Entry Check (Task 1) — GO, all three clauses cited

Every input verified by direct read/grep this session, never trusted from a summary.

### Clause (a) — a graduated attributed fix carries: PASS

- `round5-traces/option-a-restart-1.log`, `-2.log`, `-3.log` each verified on disk: exactly one `[       OK ] FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce` line per file (grep count 1 each), zero `Timed out waiting for condition` lines, zero `[  FAILED  ]` lines. Wall times 55.01s/55.66s/54.98s at pre-run loads 1.33/1.23/1.27 (12-21's graduation series, commits 97bb5f6d + 0e99efa3 + cad2109d).
- 12-21 ended in GRADUATION, not its STOP — the clause-(a) input the round-5 contract requires.

### Clause (b) — build currency: PASS

- All four phase targets rebuilt (`cmake --build ... --target multi_node_finality_fault_test --target multi_node_finality_fault_compatibility_smoke_test --target consensus_pending_lifecycle_test --target transaction_manager_certificate_fallback_test`, 100%; multi_node was already current — its binary postdates every round-5 source).
- Scoped mtime assert (per T-12-20-05: exactly these four binaries vs the two round-5 sources, never directory-wide): sources `multi_node_finality_fault_test.cpp` epoch 1788455874 and `graphsync_dagsyncer.cpp` epoch 1788455931 (newest; `consensus_pending_lifecycle_test.cpp` 1788441494 is the excluded round-4 edit) vs binaries `multi_node_finality_fault_test` 1788455947, `smoke` 1788456574, `lifecycle` 1788456576, `cert-fallback` 1788456580 — all strictly newer. Epochs recorded in `round5-traces/crash-baseline.txt`.

### Clause (c) — no live-unrepaired signature beyond the pre-declared gate-eligible residual: PASS

- Both Option A waits present in the restart-mint block: `multi_node_finality_fault_test.cpp:2512` (retention wait on second/third/passive before `RestartPeer`) and `:2538` (consensus-topic readiness gate >= 2 peers on every peer between `ConnectPeers` and the 12-19 re-publication) — placement verified by direct read.
- SameBurn's four-HasBridgeMarker predicate intact (:2090-2096 region: CheckCertificateForSlot + MintEffects on all four peers + HasBridgeMarker on first/second/third/passive inside the unchanged 20s bound).
- CR-03 intact: `consensus_pending_lifecycle_test.cpp` `registry.reset()` count exactly 2.
- The residual quote, per the plan, BEFORE any run record: **PublisherLoss child-readiness stands "characterized, NOT repaired (12-08 / D-18 / D-19 / D-25 discipline upheld)" at ~18%/run (12-15 Verdict 3): both preserved strikes share the SAME first-failing readiness boundary — `evidence->Origin() = "child-writer-probe-or-nonqualifying"` (expected "real-socket-publisher-loss"), followed by `Classification = "invalid_or_partial_blocked"` and `CountWeight = 0`, with heterogeneous child-death modes — "a launched scenario child failed to produce a qualifying terminal evidence record; the death mode is heterogeneous." This is the ONE residual the gate declares eligible without a new decision; a strike from it mid-series is NOT forgiven (entry condition, not pass condition).**

- Crash baseline written BEFORE run A (`round5-traces/crash-baseline.txt`): 2026-09-03 14:29:59 -0300, 1-min load 1.54, newest .ips `processing_core_gating_test-2026-09-02-175810.ips` (unrelated binary, yesterday; newest multi_node .ips still 2026-09-02-125541; zero today). Port preflight 54601-54634 empty. **Gate-entry outcome: GO** (STATE.md dated entry, commit 240769d2).

## The Three-Run Series (Task 2) — FAILED at run B, STOP-ON-FIRST-STRIKE

| Run | Pre-run 1-min load | Preflight | Outcome | Wall | Log / XML |
|-----|--------------------|-----------|---------|------|-----------|
| A | 1.42 | empty | **Passed** — "100% tests passed, 0 tests failed" | 214.81s | gate-a.log / gate-a.xml (failures="0", 13 tests, time=214.677, ts 14:31:23.850) |
| B | 1.38 | empty | **FAILED** — 1 test struck | 232.49s | gate-b.log / gate-b.xml (failures="1", time=232.387, ts 14:35:27.546) |
| C | — | — | **NEVER STARTED** (STOP honored) | — | intentionally absent |

- Run A assertions all green: trailer present; all four exact case-name testcase entries (classname FinalityFaultNetwork) with zero `<failure` elements and root `failures="0"`; crash-freedom held (newest .ips unchanged).
- Run B: the xunit copy was taken immediately after the run (root `failures="1"`), then the series stopped. NO log or XML exists for run C — absence is the prescribed outcome, verified, never synthesized.
- The two XML copies are distinct files (time 214.677 vs 232.387; differing timestamps) — per-run captures, not one file copied twice.

### Strike Attribution — by name, from the log lines and XML failure bodies

**PublisherLoss child-readiness (the pre-declared gate-eligible residual; NOT forgiven).**

- Striking test: `PublisherObserverProcessEvidenceCollector.RealSocketPublisherLossOnlyQualifiesWhenTwoRunsMatch` — FAILED 54264 ms (gate-b.log:79-122; 7 failure elements in its XML testcase body; the ONLY failed test of 13 — log :6963-6964).
- The boundary is EXACTLY 12-15 Verdict 3's first-failing boundary: `evidence->Origin()` = `"child-writer-probe-or-nonqualifying"` vs expected `"real-socket-publisher-loss"` (test :1889), `Classification` = `"invalid_or_partial_blocked"` (:1890), `CountWeight` = 0 (:1891), `ChildStatus` = `"normal-exit-nonzero"` (death mode matching 12-15 strike 1; :1892), Boundary/State/Error empty (:1895-:1897).
- NOT RestartAtVote and NOT the scenario cases: all four `FinalityFaultNetwork` case-name testcase entries are green inside gate-b.xml (status="run", zero failure children) — including `RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce` and `PublisherLossAfterPersistenceUsesDeterministicFailover`. Zero `Timed out waiting for condition` lines in either log; zero AssertSingleDurableMint/AssertOneLiveMintEffect failures — the graduated Option A repair held in BOTH full-suite runs, and exact-once safety held throughout.
- Crash-freedom held across both runs: zero new `~/Library/Logs/DiagnosticReports/*.ips` (newest still `processing_core_gating_test-2026-09-02-175810.ips`).
- Honest consequences recorded in STATE.md (commit 7c4003c8): the 12-14 evidence-gap blocker REMAINS OPEN (1 pass / 1 strike), the round-5 no-reroll budget is SPENT, and the next decision is routed to the developer: (a) fund a scoped PublisherLoss child-readiness repair under the D-25 discipline (this strike is the first fully-attributed fresh failure on the round-5 build — 12-15's two preserved strikes were on the pre-fix control build order — so a scoped plan could count it toward the two-fresh-failures authorization evidence or reproduce once more per the discipline's own terms), then re-gate under a new attributed fix; (b) re-examine the gate standard itself (whether the collector-level `RealSocketPublisherLossOnlyQualifiesWhenTwoRunsMatch` test belongs in the gated full-suite pass condition versus the four FinalityFaultNetwork scenario cases that are the truth-11/truth-13 subject) — a standard change is the developer's call, not the executor's; (c) accept the residual as documented and close on amended evidence.

## Sibling Matrix (Task 3) — PASSED 4/4 on the same build generation

Serial ctest alternation, port preflight empty, NO rebuild between the gate series and the matrix (binary epochs re-verified identical to Task 1's record). `round5-traces/sibling-matrix.log`, total 80.84s, trailer "100% tests passed, 0 tests failed out of 4":

| CTest target | Result |
|--------------|--------|
| transaction_manager_certificate_fallback_test | **Passed 32.10s — its FIRST recorded green run on the round-4+ build generation** (VERIFICATION gap 1 missing-item 3 coverage hole closed) |
| consensus_pending_lifecycle_test | Passed 41.93s |
| multi_node_finality_fault_compatibility_smoke_test | Passed 6.48s |
| multi_node_finality_fault_process_ownership_test | Passed 0.32s |

Zero new crash reports. A gate strike does not invalidate matrix evidence — recorded alongside the strike exactly as Task 3 prescribes (commit 11dc939e).

## Must-Have Truth Status

| Truth | Status |
|-------|--------|
| Gate-entry rule re-evaluated on the record BEFORE any run | **MET (GO branch)** — all three clauses passed on direct evidence with citations; crash-baseline.txt predates gate-a.log; the only live residual beyond the repair was the pre-declared gate-eligible PublisherLoss child-readiness, quoted above before any run record |
| Three consecutive serial passes — OR honest stop at first strike | **MET via the strike branch** — run A green with per-run case-name proof; run B struck at the attributed PublisherLoss child-readiness boundary; every log preserved verbatim; the strike attributed by name; run C never started; no reroll; STATE.md carries the FAILED record unsoftened |
| Sibling matrix passes serially on the same build generation | **MET** — 4/4 Passed including cert-fallback's first run on this generation |
| STATE.md resolves the 12-14 blocker honestly | **MET (open-branch)** — recorded still-open with the strike attributed by name and the next decision routed; REQUIREMENTS.md untouched (empty git diff verified; TEST-01..TEST-06 already Complete since 12-03) |

VERIFICATION gap 1 (truth 11, the three-consecutive-pass standard) remains OPEN — 1 pass / 1 strike this round. Gap 2 (truth 13) gained strong partial evidence: RestartAtVote and PublisherLoss scenario entries green inside BOTH executed full-suite XMLs, with the Option A repair holding in both — but the stable-property demonstration requires the completed series and therefore rides the next round. Gap 1's cert-fallback missing-item IS closed (matrix above).

## Deviations from Plan

None. The series-FAILED branch is the plan's own prescribed outcome ("either branch is plan-faithful"), executed exactly as written: STOP-on-first-strike fired on run B's first assertion failure, later-run artifacts do not exist, the strike is attributed by name, STATE.md records the failure without softening, and the matrix ran per Task 3's rule that a strike does not invalidate matrix evidence.

## Auth Gates

None.

## Known Stubs

None — no code was written; this is an evidence artifact.

## Threat Flags

None. T-12-22-01 (no-reroll integrity) honored: gate-c.log/gate-c.xml verified absent after the strike. T-12-22-02 (environment): preflight before every run (all empty), actual 1-min loads 1.42/1.38 recorded in the log headers — never near the 12-12 contamination regime. T-12-22-03 (crash-freedom): baseline recorded before run A; newest .ips identity unchanged after every run. T-12-22-04 (evidence substitution): each XML copied immediately after its run, before the next; the two copies are distinct files; the four-case assertion ran on the copies, not the shared build-tree file. T-12-22-05 (gate re-entry tampering): the clause-by-clause check ran on direct reads before run A; 12-17/12-20 records untouched (git-verified: this plan's commits touch only round5-traces/ + STATE.md + this SUMMARY). T-12-22-06 (requirements record): REQUIREMENTS.md git diff empty.

## Self-Check: PASSED

- Created files exist on disk: crash-baseline.txt, gate-a.log, gate-a.xml, gate-b.log, gate-b.xml, sibling-matrix.log under round5-traces/ — FOUND each (ls verified).
- Task commits verified in git log: 240769d2 (Task 1, gate-entry GO + baseline), 7c4003c8 (Task 2, series-FAILED evidence + STATE record), 11dc939e (Task 3, sibling matrix + STATE finalization).
- Withheld-artifact proof: gate-c.log and gate-c.xml verified absent (STOP-on-first-strike); zero /tmp paths anywhere in the round-5 evidence.
- REQUIREMENTS.md untouched: `git diff --name-only -- .planning/REQUIREMENTS.md` is empty.
