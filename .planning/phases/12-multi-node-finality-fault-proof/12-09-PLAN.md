---
phase: 12-multi-node-finality-fault-proof
plan: "09"
type: execute
wave: 9
depends_on: ["12-08"]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
  - .planning/phases/12-multi-node-finality-fault-proof/12-09-SUMMARY.md
  - .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-run-1.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-run-2.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-run-3.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-repair-run-1.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-repair-run-2.log
  - .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-repair-run-3.log
autonomous: true
gap_closure: true
requirements: [TEST-05, TEST-06]
must_haves:
  truths:
    - "Per D-21 through D-24, every publisher-readiness observation that can enter the gate has matching process-bound start and terminal records, a run token, schema version, executable path/size/modification time, process id, normal GTest completion, and proof that all four scenario-owned peer runtime handles were released before terminal emission."
    - "Per D-22 and D-23, invalid, mismatched, old-format, and partial records remain retained evidence but cannot count as a pass, a failure, or repair authorization; test-managed partial runs carry a distinct incomplete terminal record."
    - "Per D-25 and D-28, repair is considered only after two fully attributed completed real-socket failures share the same observer-lifecycle boundary, state, and error, and any repair is confined to the test observer ownership/output path."
    - "Per D-26, three fully attributed completed publisher-loss passes close this observer-attribution investigation without repair while Phase 12 remains blocked on its separately proven late-contender, restart, and publisher-proof gaps."
    - "Per TEST-06 and D-29, one synchronized test-owned writer emits and flushes structured diagnostics without changing production logging, finality, CRDT, PubSub, Mint ingress, topology, peer lifecycle outside the observer, retries, waits, timeouts, or CMake."
    - "Per D-22 through D-27, every complete-pass, complete-failure, invalid/partial-blocked, and tooling-attribution-rebuild branch leaves a valid terminal report; an authorized repair retains both its initial and distinct post-repair three-process evidence."
  artifacts:
    - path: test/src/blockchain/multi_node_finality_fault_test.cpp
      provides: "A scenario-owned, read-only publisher-readiness observer with process-bound start/incomplete/terminal records and an explicit post-release terminal path."
    - path: .planning/phases/12-multi-node-finality-fault-proof/12-09-SUMMARY.md
      provides: "Attribution classifications, retained invalid/partial evidence, repair authority decision, tooling-attribution branch, and terminal Phase-12 scope disposition."
    - path: .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-run-1.log
      provides: "Complete stdout/stderr for the first fresh attributed focused process."
    - path: .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-run-2.log
      provides: "Complete stdout/stderr for the second fresh attributed focused process."
    - path: .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-run-3.log
      provides: "Complete stdout/stderr for the third fresh attributed focused process."
    - path: .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-repair-run-1.log
      provides: "Complete stdout/stderr for post-repair attributed process one when D-25 authorizes a repair."
    - path: .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-repair-run-2.log
      provides: "Complete stdout/stderr for post-repair attributed process two when D-25 authorizes a repair."
    - path: .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-repair-run-3.log
      provides: "Complete stdout/stderr for post-repair attributed process three when D-25 authorizes a repair."
  key_links:
    - from: "PublisherLossAfterPersistenceUsesDeterministicFailover"
      to: "scenario-owned observer terminal path"
      via: "explicit StopNetwork/release proof before EmitTerminal, never an observer destructor"
      pattern: "StopNetwork.*EmitTerminal|EmitTerminal.*peer_release"
    - from: "P12_PUBLISHER_OBSERVER_START"
      to: "P12_PUBLISHER_OBSERVER_TERMINAL"
      via: "identical run token, schema version, process id, and executable path/size/mtime fingerprint"
      pattern: "run_token=.*schema=.*exe_path=.*exe_size=.*exe_mtime=.*pid="
    - from: "observer-attribution-run-*.log"
      to: "12-09-SUMMARY.md gate classification"
      via: "matching fingerprint plus normal GTest completion plus matching complete terminal record"
      pattern: "P12_PUBLISHER_OBSERVER_(START|TERMINAL)|\\[  PASSED  \\] 1 test"
    - from: "observer-attribution-repair-run-{1,2,3}.log"
      to: "12-09-SUMMARY.md post-repair classification"
      via: "a fresh, distinct three-process gate after the only authorized observer ownership/output repair"
      pattern: "P12_PUBLISHER_OBSERVER_(START|TERMINAL)|P12_PUBLISHER_OBSERVER_TERMINAL_REPORT branch=repair"
---

<objective>
Attribute the mixed publisher-readiness observer output to a specific test process and executable, then evaluate only fully attributed observer-lifecycle evidence.

Purpose: Close the observer-output attribution gate created by Plan 12-08 without turning a stale, partial, or foreign record into a finality/fixture repair.
Output: A process-bound test-only observer and retained three-run evidence that either authorizes an observer-only repair, records tooling attribution plus a clean rebuilt gate, or closes this narrow investigation with no repair.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md
@.planning/phases/12-multi-node-finality-fault-proof/12-PATTERNS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VALIDATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-08-PLAN.md
@.planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md
@test/src/blockchain/multi_node_finality_fault_test.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace destructor diagnostics with a process-bound, scenario-owned observer protocol</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-09-SUMMARY.md</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp — read `Peer::Stop`, `StopNetwork`, `ReadinessDiagnostics`, `ConnectAndWaitForPeers`, and `PublisherLossAfterPersistenceUsesDeterministicFailover` in one pass; preserve the existing public topology predicate and every finality assertion.
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp — use its explicit, test-owned `Close`/PubSub-stop cleanup and post-cleanup expectation pattern as the mapped analog; do not import its direct lifecycle access or consensus test seams into this real-socket fixture.
    - .planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md — D-21 through D-29 are locked; D-01 through D-20 remain non-regression boundaries.
    - .planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md — retain, rather than rewrite, the mixed post-review records that this plan attributes.
  </read_first>
  <behavior>
    - A focused run with `P12_09_RUN_TOKEN` emits one synchronized, flushed `P12_PUBLISHER_OBSERVER_START` record before its readiness operation and exactly one matching terminal record after the scenario has explicitly released all four test-owned peer runtime handles.
    - Both records contain the same non-empty run token, fixed observer schema version, process id, and executable identity made from resolved executable path, file size, and modification time; the records expose no serialized certificate, transaction, private key, or other secret payload.
    - The complete publisher scenario runs inside one scenario-owned, returnable control-flow envelope: unchanged fatal assertions return only from that envelope, then one outer epilogue always calls `StopNetwork`, proves release, and emits exactly one complete or incomplete matching terminal before `TestBody` returns.
    - A ready scenario emits `outcome=pass terminal=complete peer_release=all-four-runtime-handles-released boundary=none state=ready error=none`; a classified readiness failure that reaches the outer epilogue emits `outcome=failure terminal=complete` with its observed first observer-lifecycle boundary/state/error, while an unclassifiable or unreleased test-managed exit emits the distinct `outcome=incomplete terminal=incomplete` record before the outer function returns.
    - The observer itself holds read-only references/snapshots only, does not own or stop a peer, PubSub, CRDT, consensus, transaction, I/O, root, or shutdown object, and its destructor emits nothing.
  </behavior>
  <action>Per D-21, introduce a test-local `PublisherReadinessObserver` protocol that obtains the running executable through a portable test-binary location API, resolves its path, reads only its size and modification time, and reads the required run token from `P12_09_RUN_TOKEN`. Fail closed into an invalid observer state if any fingerprint component cannot be obtained; never substitute a guessed path or a serialized protocol payload. Define the fixed schema version and include `pid` with the required `run_token`, `schema`, `exe_path`, `exe_size`, and `exe_mtime` fields in both a start header and terminal record.

Per D-29, route all P12 observer output through one test-owned mutex-protected writer that emits one newline-delimited structured record with `std::flush`. Keep normal GTest and teardown output intact; do not redirect, suppress, or alter production logging. Replace `ReadinessDiagnostics` destructor output with explicit `EmitStart`, `MarkReady`, `MarkFailure`, `EmitTerminal`, and `EmitIncomplete` operations. The observer may snapshot the existing public readiness fields only (identity, `IsStarted`, host connectedness, consensus mesh, and test-owned root/I/O lifecycle state); it must not call `AddPeers`, publish, write CRDT data, alter a wait, or control shutdown.

Per D-23, D-24, and D-28, make `PublisherLossAfterPersistenceUsesDeterministicFailover` own the diagnostic lifecycle. Before any Task-1 source change, capture `git rev-parse HEAD` as `P12_09_START_BASELINE=<sha>` in `12-09-SUMMARY.md`; Task 2 must use that exact recorded SHA rather than a historical or post-Task-1 baseline. Before the existing `ConnectPeers` call, construct the observer with read-only access and emit its start header. Put the complete existing post-start scenario body inside one scenario-owned lambda/control-flow envelope whose fatal GTest assertions remain textually byte-for-byte and therefore return only from that envelope, never from the outer `TestBody`. The outer epilogue is the sole return path after `EmitStart`: it calls existing `StopNetwork` once, checks from test-owned `Peer` members that all four peers released consensus, blockchain, transaction manager, GlobalDB, PubSub, I/O, and any joinable I/O thread, then uses a one-shot terminal guard to emit exactly one matching terminal. Emit `outcome=failure terminal=complete` only after release when the observer classified the first readiness boundary; otherwise use the distinct incomplete terminal, also after the cleanup attempt, with its release/proof reason. Use non-fatal reporting in this epilogue so cleanup and the one terminal cannot be bypassed. Do not emit a terminal from an RAII destructor and do not inspect protocol internals to prove release. Preserve the exact `ConnectAndWaitForPeers`, `ConnectPeers`, `PeersFormConnectedTopology`, selected-publisher barrier, fault sequence, peer membership, public `SubmitProposal` ingress, retries, timeouts, and all certificate/Mint assertions byte-for-byte except the narrow scenario-owned envelope/observer calls required to reach the explicit terminal path.

Use RED then GREEN: first make the focused GTest fail because it lacks a matching process-bound start/terminal pair or emits terminal before release; then implement the observer protocol until the test passes. Commit the observer-only implementation as `test(12-09): attribute publisher observer output`.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; p12_test='build/OSX/Release/test_bin/multi_node_finality_fault_test' &amp;&amp; p12_token="p12-09-contract-$$" &amp;&amp; P12_09_RUN_TOKEN="$p12_token" "$p12_test" --gtest_filter='FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover' &gt;/tmp/p12-09-observer-contract.log 2&gt;&amp;1 &amp;&amp; test "$(rg -c "^P12_PUBLISHER_OBSERVER_START run_token=$p12_token schema=p12-observer-v[0-9]+ pid=[0-9]+ exe_path=[^[:space:]]+ exe_size=[0-9]+ exe_mtime=-?[0-9]+$" /tmp/p12-09-observer-contract.log)" -eq 1 &amp;&amp; test "$(rg -c "^P12_PUBLISHER_OBSERVER_TERMINAL run_token=$p12_token schema=p12-observer-v[0-9]+ pid=[0-9]+ exe_path=[^[:space:]]+ exe_size=[0-9]+ exe_mtime=-?[0-9]+ outcome=pass terminal=complete peer_release=all-four-runtime-handles-released boundary=none state=ready error=none$" /tmp/p12-09-observer-contract.log)" -eq 1 &amp;&amp; ! rg -q 'P12_PUBLISHER_READINESS_DIAG' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
    <automated>p12_file='test/src/blockchain/multi_node_finality_fault_test.cpp'; ! rg -n 'P12_PUBLISHER_OBSERVER_TERMINAL' "$p12_file" | rg '~PublisherReadinessObserver|~ReadinessDiagnostics' &amp;&amp; ! rg -n 'P12_PUBLISHER_OBSERVER.*(AddPeers|SubmitProposal|Publish|CertificateReceived|OnConsensusCertificate|GlobalDB::Put|PutConvergentImmutable|Handle[A-Za-z]*|Force[A-Za-z]*(Timer|Due|Round)|sleep_for|sleep_until|retry|TIMEOUT|timeout)' "$p12_file" &amp;&amp; git diff --check -- "$p12_file"</automated>
  </verify>
  <acceptance_criteria>The publisher-loss scenario emits a start and exactly one explicit terminal record with an identical process-bound fingerprint even when any existing post-start fatal assertion fails: that assertion returns from the scenario-owned envelope, and the outer epilogue releases all peers and emits exactly one complete or incomplete terminal before `TestBody` returns. Normal ready completion is distinct from test-managed incomplete completion; invalid fingerprint acquisition produces invalid evidence rather than a pass or failure. `12-09-SUMMARY.md` retains the Plan 12-09 start SHA. The only source surface is read-only observer ownership/output code in the named test file.</acceptance_criteria>
  <done>D-21 through D-24, D-28, and D-29 have an executable test-owned observer contract without changing any finality, CRDT, PubSub, Mint, topology, peer lifecycle outside that observer path, retry, timeout, CMake, or production logging behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the fully attributed evidence gate and conditionally repair only observer ownership/output</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-09-SUMMARY.md, .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-run-1.log, .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-run-2.log, .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-run-3.log, .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-repair-run-1.log, .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-repair-run-2.log, .planning/phases/12-multi-node-finality-fault-proof/12-09-traces/observer-attribution-repair-run-3.log</files>
  <read_first>
    - .planning/phases/12-multi-node-finality-fault-proof/12-08-SUMMARY.md — identify the retained two complete and one mixed old-format canonical records; do not replace or weaken them.
    - .planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md — apply D-22 through D-27 exactly, especially the invalid-evidence, two-failure authorization, three-pass closure, and foreign-process rebuild branches.
    - test/src/blockchain/multi_node_finality_fault_test.cpp — use the Task-1 record grammar and confirm that the observer, rather than peer/finality code, owns the output path.
  </read_first>
  <behavior>
    - Only a trace containing one matching start record, one matching complete terminal record, and the normal GTest one-test completion footer is classified `fully_attributed_complete` and can enter the pass/failure/repair count; a pass additionally requires exit status zero, while a completed failure carries the normal GTest failure result.
    - Any old-format line, missing field, fingerprint mismatch, duplicate terminal, absent terminal, non-zero exit, or missing GTest footer is retained in `12-09-SUMMARY.md` as `invalid` or `partial` and has count weight zero.
    - Two `fully_attributed_complete` failures with the same first observer-lifecycle `boundary`, `state`, and `error` are the sole authorization to write a RED regression then a GREEN repair, and that repair changes only observer ownership/output code.
    - Three `fully_attributed_complete` passes set `observer_attribution_disposition=closed-no-repair` and retain `PHASE_12_STATUS=BLOCKED` with the separately proven Phase-12 gaps named; they do not claim TEST-05 complete.
    - A fast test-local record classifier covers complete-pass, complete-failure, invalid/partial, and directly proven foreign-process/binary inputs before the real-socket gate; every resulting evidence disposition writes one machine-checkable `P12_PUBLISHER_OBSERVER_TERMINAL_REPORT` summary line rather than causing the evidence task to abort.
  </behavior>
  <action>First add a pure, test-local `ClassifyPublisherObserverRecord` helper plus focused GTest cases in `multi_node_finality_fault_test.cpp`. Its input is the captured process exit, GTest footer, start/terminal multiplicity and fingerprints, terminal fields, and direct provenance; its result is one of `fully_attributed_complete_pass`, `fully_attributed_complete_failure`, `invalid_or_partial_blocked`, or `tooling_attribution_rebuild`. RED must demonstrate each classification before GREEN code exists, then the focused classifier test must pass without opening sockets. This classifier is evidence-only: it does not alter the scenario, observer state, peer ownership, or production code.

Build the target and create/append `12-09-SUMMARY.md` plus three complete initial trace files. Require the `P12_09_START_BASELINE=<sha>` captured by Task 1 before any Plan 12-09 source change; abort only if that provenance record itself is absent or malformed. First classify the Plan-12-08 retained records without editing them: old-format or fingerprint-free records are `invalid` evidence with count weight zero; preserve their relative trace path, observed line, and reason. When reliable wrapper/provenance evidence proves that an old-format record came from a different process id or executable fingerprint, write `tooling_attribution=foreign-process-or-binary` with that direct cause, perform `cmake --build build/OSX/Release --target multi_node_finality_fault_test --clean-first --parallel 4`, retain the initial evidence, and restart the new three-run observer gate from run 1. Do not infer a foreign process from a missing fingerprint alone, do not discard the trace, and do not change the finality fixture. This is the D-27 tooling branch, not a retry or a repair.

For each fresh run, wait until no prior `multi_node_finality_fault_test` process remains, generate a distinct `P12_09_RUN_TOKEN`, launch only `FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover`, and capture all stdout/stderr into its numbered trace. Append one run manifest and one classification to the 12-09 summary. A record has `fully_attributed_complete` status only if the trace has exactly one matching start record and one matching terminal record with identical `run_token`, `schema`, `pid`, `exe_path`, `exe_size`, and `exe_mtime`, the terminal has `terminal=complete`, `peer_release=all-four-runtime-handles-released`, and explicit `boundary`, `state`, and `error` fields, and the normal GTest footer reports one completed test. Classify it as a pass only when `outcome=pass` and exit status is zero; classify it as a completed failure only when `outcome=failure` and the normal GTest failure result is present. Keep every invalid or partial trace and write an explicit zero-count reason; never rerun it in place or treat it as a pass/failure.

Per D-25, aggregate only `fully_attributed_complete` failure records. Authorize repair solely when at least two real-socket processes match exactly on `boundary`, `state`, and `error`, and that triple identifies an observer-lifecycle defect. Before a repair, make the matching observer-lifecycle regression fail; then make the smallest GREEN change only in `PublisherReadinessObserver`/the scenario-owned explicit output path. The observer remains read-only and cannot own peer, PubSub, I/O, root, or shutdown work. Do not modify consensus, CRDT, PubSub, Mint/transaction logic, peer/topology/lifecycle behavior outside this observer, test fault ordering, waits, retries, timeouts, CMake, or production logging. Rebuild and run a fresh three-process attributed gate after the repair, retaining its stdout/stderr as `observer-attribution-repair-run-1.log` through `observer-attribution-repair-run-3.log` without overwriting the initial evidence. If its required records do not all pass, retain both evidence sets, write a valid `branch=repair-blocked` terminal report, set `PHASE_12_STATUS=BLOCKED`, and stop without further repair.

Per D-26, if three fully attributed publisher-loss runs pass, set `repair_authorization=none`, `observer_attribution_disposition=closed-no-repair`, and `PHASE_12_STATUS=BLOCKED`; name the separate late-contender, three-boundary restart, and full publisher-loss proof gaps from `12-VERIFICATION.md`. Do not claim TEST-05 solved. In every terminal branch, including complete-pass closure, complete-failure repair authorization/blocking, invalid/partial blocking, and tooling-attribution rebuild, append exactly one `P12_PUBLISHER_OBSERVER_TERMINAL_REPORT` line containing `branch`, `gate_status`, `repair_authorization`, `observer_attribution_disposition`, `PHASE_12_STATUS`, `initial_trace_set`, and, when applicable, `repair_trace_set` or `tooling_attribution`. A valid blocked report is the expected outcome for invalid/partial or unresolved failure evidence; do not make the task fail merely because evidence does not authorize repair or closure. In every terminal branch, run the additions-only static gate using the recorded Plan 12-09 start baseline and aggregate committed (`<baseline>..HEAD`), staged, and unstaged diffs: only the named test file, `12-09-SUMMARY.md`, and all six 12-09 trace logs may differ; reject direct handler/author/receiver calls, direct CRDT writes, certificate/publisher logic, forced rounds, sleeps, topology changes, retry/timeout changes, CMake edits, production-source edits, and payload/secret diagnostic fields. Commit only the evidence/authorized observer result as `test(12-09): gate publisher observer attribution`.</action>
  <verify>
    <automated>p12_dir='.planning/phases/12-multi-node-finality-fault-proof'; p12_test='build/OSX/Release/test_bin/multi_node_finality_fault_test'; cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; mkdir -p "$p12_dir/12-09-traces" &amp;&amp; for p12_run in 1 2 3; do pgrep -f 'multi_node_finality_fault_test' &gt;/dev/null &amp;&amp; exit 1; p12_token="p12-09-$p12_run-$(date +%s)-$$"; P12_09_RUN_TOKEN="$p12_token" "$p12_test" --gtest_filter='FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover' &gt;"$p12_dir/12-09-traces/observer-attribution-run-$p12_run.log" 2&gt;&amp;1; p12_exit=$?; printf 'P12_PUBLISHER_OBSERVER_RUN run=%s run_token=%s process_exit=%s trace=%s\n' "$p12_run" "$p12_token" "$p12_exit" "$p12_dir/12-09-traces/observer-attribution-run-$p12_run.log" &gt;&gt; "$p12_dir/12-09-SUMMARY.md"; test -s "$p12_dir/12-09-traces/observer-attribution-run-$p12_run.log" || exit 1; done</automated>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='PublisherObserverRecordClassifier.*'</automated>
    <automated>p12_dir='.planning/phases/12-multi-node-finality-fault-proof'; p12_summary="$p12_dir/12-09-SUMMARY.md"; p12_branch="$(sed -n 's/^P12_PUBLISHER_OBSERVER_TERMINAL_REPORT branch=\\([^ ]*\\).*/\\1/p' "$p12_summary" | tail -1)"; case "$p12_branch" in complete-pass-closed|complete-failure-blocked|invalid-or-partial-blocked|tooling-attribution-rebuild|repair-passed|repair-blocked) ;; *) exit 1;; esac; rg -q "^P12_PUBLISHER_OBSERVER_TERMINAL_REPORT branch=$p12_branch gate_status=[^ ]+ repair_authorization=[^ ]+ observer_attribution_disposition=[^ ]+ PHASE_12_STATUS=BLOCKED initial_trace_set=[^ ]+" "$p12_summary"; case "$p12_branch" in complete-pass-closed|complete-failure-blocked|repair-passed|repair-blocked) for p12_run in 1 2 3; do p12_log="$p12_dir/12-09-traces/observer-attribution-run-$p12_run.log"; test -s "$p12_log" &amp;&amp; test "$(rg -c '^P12_PUBLISHER_OBSERVER_START ' "$p12_log")" -eq 1 &amp;&amp; test "$(rg -c '^P12_PUBLISHER_OBSERVER_TERMINAL .*terminal=complete peer_release=all-four-runtime-handles-released ' "$p12_log")" -eq 1 &amp;&amp; rg -q '^\\[==========\\] 1 test from 1 test suite ran\\.' "$p12_log" || exit 1; done;; invalid-or-partial-blocked) rg -q '^P12_PUBLISHER_OBSERVER_CLASSIFICATION .*status=(invalid|partial).*count_weight=0' "$p12_summary";; tooling-attribution-rebuild) rg -q '^P12_PUBLISHER_OBSERVER_TERMINAL_REPORT .*tooling_attribution=foreign-process-or-binary ' "$p12_summary" &amp;&amp; rg -q '^P12_09_CLEAN_REBUILD=completed ' "$p12_summary";; esac; if rg -q '^P12_PUBLISHER_OBSERVER_TERMINAL_REPORT .*repair_trace_set=' "$p12_summary"; then for p12_run in 1 2 3; do p12_log="$p12_dir/12-09-traces/observer-attribution-repair-run-$p12_run.log"; test -s "$p12_log" &amp;&amp; test "$(rg -c '^P12_PUBLISHER_OBSERVER_START ' "$p12_log")" -eq 1 &amp;&amp; test "$(rg -c '^P12_PUBLISHER_OBSERVER_TERMINAL .*terminal=complete peer_release=all-four-runtime-handles-released ' "$p12_log")" -eq 1 &amp;&amp; rg -q '^\\[==========\\] 1 test from 1 test suite ran\\.' "$p12_log" || exit 1; done; fi</automated>
    <automated>p12_dir='.planning/phases/12-multi-node-finality-fault-proof'; p12_summary="$p12_dir/12-09-SUMMARY.md"; p12_base="$(sed -n 's/^P12_09_START_BASELINE=\\([0-9a-f][0-9a-f]*\\)$/\\1/p' "$p12_summary" | head -1)"; test -n "$p12_base" &amp;&amp; git rev-parse --verify "$p12_base^{commit}" &amp;&amp; p12_allowed='^(test/src/blockchain/multi_node_finality_fault_test\.cpp|\.planning/phases/12-multi-node-finality-fault-proof/12-09-(SUMMARY\.md|traces/observer-attribution-(repair-)?run-[123]\.log))$'; p12_changed="$( { git diff --name-only "$p12_base"..HEAD; git diff --cached --name-only; git diff --name-only; } | sort -u )"; test -z "$(printf '%s\n' "$p12_changed" | rg -v '^$' | rg -v "$p12_allowed")" &amp;&amp; ! { git diff --unified=0 "$p12_base"..HEAD -- test/src/blockchain/multi_node_finality_fault_test.cpp; git diff --cached --unified=0 -- test/src/blockchain/multi_node_finality_fault_test.cpp; git diff --unified=0 -- test/src/blockchain/multi_node_finality_fault_test.cpp; } | rg '^\+[^+].*(AddPeers|ConnectPeers|PeersFormConnectedTopology|SubmitProposal|Publish|SubmitCertificate|CertificateReceived|OnConsensusCertificate|GlobalDB::Put|PutConvergentImmutable|Handle[A-Za-z]*|Force[A-Za-z]*(Timer|Due|Round)|sleep_for|sleep_until|retry|TIMEOUT|timeout)' &amp;&amp; ! rg -n 'P12_PUBLISHER_OBSERVER.*(Serialize|private.?key|certificate.*payload|transaction.*payload)' test/src/blockchain/multi_node_finality_fault_test.cpp &amp;&amp; git diff --check "$p12_base"..HEAD &amp;&amp; git diff --cached --check &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>The fast classifier proves the record-classification contract before real sockets run. The summary retains every old, invalid, partial, and foreign-process record with a zero-count attribution classification and ends each valid evidence branch with a machine-checkable terminal report rather than a task failure. Only matching fully attributed complete records participate in the gate. Two matching complete observer-lifecycle failures are the only repair authorization and any resulting change is observer ownership/output only; an authorized repair retains its distinct `observer-attribution-repair-run-{1,2,3}.log` set as well as the initial evidence. Three complete passes close this sub-investigation without repair and explicitly keep Phase 12 blocked on separate gaps. A directly proven foreign old-format producer triggers a clean target rebuild and a fresh three-run gate, never a fixture/finality change. The static gate compares against the recorded Plan 12-09 start SHA and aggregates committed, staged, and unstaged diffs.</acceptance_criteria>
  <done>D-22 through D-27 are enforced by retained evidence and executable gate rules. TEST-05/TEST-06 remain protected: the work either produces tightly attributed observer-only evidence or a narrowly authorized observer repair, never a consensus/finality/topology repair or an unsupported completion claim.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Required behavior | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 12 | Preserve production-path finality proof while resolving the observer attribution needed to judge publisher-readiness evidence. | 12-09 Tasks 1–2 | COVERED | This plan closes only the attribution sub-investigation, never the separately blocked finality proof. |
| REQ | TEST-05 | Publisher-loss evidence is trustworthy only when process ownership and normal completion are attributable. | 12-09 Tasks 1–2 | COVERED | The gate does not mark TEST-05 complete merely from readiness passes. |
| REQ | TEST-06 | Diagnostics remain production-route observation, not a local-author shortcut or production behavior change. | 12-09 Tasks 1–2 | COVERED | Static additions-only gate rejects protected routes and files. |
| RESEARCH | — | Reuse real GossipPubSub/GlobalDB and bounded existing predicates; no sleeps, mocks, direct handlers, or direct datastore writes. | 12-09 Tasks 1–2 | COVERED | Observer reads only existing public/test-owned lifecycle state and preserves the original scenario. |
| RESEARCH | — | `AddPeers` readiness is asynchronous and must be observed, not assumed. | 12-09 Task 1 | COVERED | The existing predicate remains unchanged; output is attributed after the fact. |
| CONTEXT | D-01–D-20 | Existing transport, durable-boundary, publisher-loss, and no-repair constraints remain non-regression boundaries. | 12-09 Tasks 1–2 | COVERED | No protected finality/test-fixture behavior is modified outside observer ownership/output. |
| CONTEXT | D-21 | Start and terminal records carry one matching process-bound fingerprint. | 12-09 Task 1 | COVERED | Run token, schema, pid, resolved executable path, size, and mtime are mandatory. |
| CONTEXT | D-22 | Mismatched fingerprints remain invalid and excluded. | 12-09 Task 2 | COVERED | Summary classifications retain zero-count invalid evidence. |
| CONTEXT | D-23 | Count only matching fingerprint + normal GTest completion + matching terminal; partial is distinct. | 12-09 Tasks 1–2 | COVERED | Complete/incomplete terminals and strict trace validator implement the rule. |
| CONTEXT | D-24 | Terminal follows scenario-owned release of all four peers and is not RAII output. | 12-09 Task 1 | COVERED | Explicit StopNetwork/release proof precedes terminal emission. |
| CONTEXT | D-25 | Two matching fully attributed complete lifecycle failures authorize repair. | 12-09 Task 2 | COVERED | Exact boundary/state/error triple is the only repair condition. |
| CONTEXT | D-26 | Three fully attributed passes close this scope without repair but do not complete Phase 12. | 12-09 Task 2 | COVERED | Required terminal disposition preserves separate gaps. |
| CONTEXT | D-27 | Foreign old-format process/binary evidence causes tooling attribution, clean rebuild, and fresh gate. | 12-09 Task 2 | COVERED | Direct provenance is required; uncertain old output remains invalid, retained evidence. |
| CONTEXT | D-28 | Repair is test-observer ownership/output only and scenario owns lifecycle. | 12-09 Tasks 1–2 | COVERED | Observer remains read-only and cannot own shutdown. |
| CONTEXT | D-29 | One synchronized test-owned flushed writer; no production-log suppression/change. | 12-09 Task 1 | COVERED | Single writer and static check enforce output ownership. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process/executable → observer evidence | A stale or foreign binary can emit plausible readiness text that must not authorize an observer or fixture repair. |
| scenario-owned peer handles → terminal output | A terminal emitted before runtime release can falsely claim teardown completion. |
| test observer → production finality route | Diagnostic code must not become a peer owner, transport controller, publisher, receiver, CRDT writer, or Mint driver. |
| trace contents → planning evidence | Diagnostic output can leak payloads or allow invalid/partial output to be mistaken for a completed test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-25 | T/R | observer start/terminal records | mitigate | D-21–D-23 require a matching run token, schema, pid, executable path/size/mtime, normal GTest footer, and one terminal before a record counts. |
| T-12-26 | D/T | peer teardown attribution | mitigate | D-24 requires scenario-owned release of all four runtime handles before explicit terminal output; incomplete records remain non-authorizing. |
| T-12-27 | T/E | test observer ownership/output path | mitigate | D-25 and D-28 restrict any repair to observer output/ownership after two matching complete failures; static gate rejects finality, PubSub, CRDT, Mint, topology, waits, retries, CMake, and production edits. |
| T-12-28 | R/T | old-format trace provenance | mitigate | D-22 and D-27 retain untrusted records with zero count; directly proven foreign producer records tooling attribution, forces a clean target rebuild, then restarts the three-run gate. |
| T-12-29 | I | structured diagnostic output | mitigate | D-29 permits only process/lifecycle metadata and public readiness facts; static scan rejects serialized certificate/transaction payloads and key material. |
| T-12-SC | Tampering | test/build artifacts | mitigate | No package installation occurs; committed, staged, and unstaged additions-only scans plus `git diff --check` block unauthorized source/config changes. |
</threat_model>

<verification>
1. Build the existing `multi_node_finality_fault_test` target; do not reconfigure CMake or alter its timeout.
2. Verify one focused contract run emits a matching process-bound start and explicit post-release terminal record without destructor output.
3. Classify retained Plan-12-08 records as invalid/partial unless their process/executable provenance is directly proven; retain every trace and zero-count reason.
4. Execute three fresh sequential real-socket publisher-loss processes and count only fully attributed complete records with normal GTest completion.
5. Authorize a RED/GREEN observer-only repair only after two exact matching complete lifecycle failures; otherwise record the no-repair branch. Three fully attributed passes close this scope but retain the Phase-12 blocker.
6. Enforce source/file/sensitive-output gates across committed, staged, and unstaged changes.
</verification>

<success_criteria>
- Every gate-eligible run has a matching start/terminal fingerprint, normal GTest completion, a complete terminal status, and post-release proof for all four test-owned peer runtime handles.
- Invalid, stale, partial, and foreign-process evidence is retained but has zero authorization/count weight; a proven foreign old-format producer triggers a clean rebuild and fresh three-run gate.
- Only two fully attributed matching observer-lifecycle failures can authorize a repair, and that repair is limited to test-observer ownership/output code.
- Three fully attributed publisher-loss passes close this attribution scope with no repair while Phase 12 stays blocked on separate verification gaps.
- No finality, consensus, CRDT, PubSub, Mint/transaction, topology, peer lifecycle outside the observer, retry, timeout, CMake, or production-logging change is introduced; diagnostics expose no secret or serialized protocol payload.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-09-SUMMARY.md` when done.
</output>
