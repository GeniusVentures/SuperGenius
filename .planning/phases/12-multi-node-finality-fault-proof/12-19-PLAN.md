---
phase: 12-multi-node-finality-fault-proof
plan: 19
type: execute
wave: 19
depends_on: ["12-18"]
files_modified:
  - .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-skip.md
  - test/src/blockchain/multi_node_finality_fault_test.cpp
  - .planning/STATE.md
  - .planning/phases/12-multi-node-finality-fault-proof/round4-traces/
autonomous: true
gap_closure: true
requirements: [TEST-03, TEST-04]
must_haves:
  truths:
    - "The developer directive's conditional fallback is resolved honestly: either a recorded skip (hypothesis was proven in 12-18, no repair needed) or a post-restart certificate re-publication / surviving-replica repair exists and is focused-verified"
    - "If the repair branch runs: three consecutive focused RestartAtVote passes on the repaired build, logs preserved at the durable repo-relative path, with recipients still receive-only (CERT-02 preserved) and no duplicate mint (TEST-04 semantics unchanged)"
    - "Whichever branch runs, 12-20's gate has an unambiguous attributed fix to carry (12-18's seam wiring or this plan's repair)"
  artifacts:
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-skip.md"
      provides: "the skip record when the hypothesis was proven (directive: 'only if it is disproven fall back')"
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "when the repair branch runs: the post-restart certificate re-advertisement through the public SubmitCertificate route after RestartPeer + ConnectPeers, or the recorded justified alternative"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-{1,2,3}.log"
      provides: "when the repair branch runs: the three focused verification runs"
  key_links:
    - from: "12-19 Task 1 branch decision"
      to: "round4-traces/hypothesis-verdict.md verdict token"
      via: "exact string match on PROVEN_BLACKLIST_MASKING or DISPROVEN"
      pattern: "PROVEN_BLACKLIST_MASKING|DISPROVEN"
    - from: "the repaired restart block (repair branch)"
      to: "ConsensusManager::SubmitCertificate (src/blockchain/Consensus.hpp:481)"
      via: "public-ingress re-advertisement of the durably-held unchanged certificate by a peer that holds it, after ConnectPeers"
      pattern: "SubmitCertificate"
---

<objective>
Round-4 gap closure, part 2 (of 3): resolve the conditional tail of the developer's 2026-09-03 directive — "The scoped plan should test this hypothesis first (cheap: seam + re-run focused RestartAtVote), and only if it is disproven fall back to post-restart certificate re-publication / surviving-replica serving as the repair."

Purpose: VERIFICATION gaps 1 and 2 (three-pass evidence gate; restart-case stability) both ride on the RestartAtVote residual. 12-18 tests the blacklist boot-window masking hypothesis; this plan either records the justified skip (hypothesis proven — the seam wiring is already the attributed fix) or implements the directive's named fallback repair and focused-verifies it. Either way, 12-20's gate re-attempt gets exactly one unambiguous attributed fix to carry under the no-reroll rule.

Output: round4-traces/fallback-skip.md (skip branch), or a repair in test/src/blockchain/multi_node_finality_fault_test.cpp (repair branch) plus three focused RestartAtVote passes at .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-{1,2,3}.log and a STATE.md record. All evidence at durable repo-relative paths, never /tmp.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-17-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-18-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/round4-traces/hypothesis-verdict.md

Authority for this plan's repair branch: the developer's NO-GO directive names the fallback explicitly, so the disproof evidence from 12-18 (focused failure(s) WITH the seam active, preserved logs) IS the required reproduction — no separate twice-reproduced gate applies. Scope discipline still binds (STATE.md decisions): change the smallest surface that makes the restart converge through real ingress; never relax a wait bound, add retries, or compensate with timing; recipients stay receive-only.

Inviolable requirements constraints (REQUIREMENTS.md / CONTEXT.md D-03):
- CERT-02: at most the deterministic protocol-selected publisher writes the authoritative certificate CRDT key; PubSub receipt never grants a peer write authority. The re-advertisement shape pushes the unchanged certificate through the public ingress/validation route (SubmitCertificate), which is receive-path processing, not a CRDT write grant — same authority reasoning as the recorded Phase 12 proposal precedent (STATE.md decision: "Re-advertise unchanged signed proposals through public SubmitProposal after AddPeers because offline GossipPubSub broadcasts are not replayed. — Preserves isolated initial submission and real production ingress.").
- CERT-05: a byte-identical certificate replay is harmless (idempotent acceptance path); a different certificate for an occupied slot fails closed.
- Phase 11 embedded-fallback decision (STATE.md): "Certificate-first processing selects tracked state, exact CRDT evidence, then only a validated embedded fallback" — a re-advertised certificate carrying its embedded content can converge recipients even when the graphsync CID fetch of the original DAG node stays blocked, which is why re-publication is robust against BOTH skip mechanisms (blacklist and the 180s cid_failures_ recency window).

<interfaces>
From .planning/phases/12-multi-node-finality-fault-proof/round4-traces/hypothesis-verdict.md (created by 12-18):
  verdict token: exactly "PROVEN_BLACKLIST_MASKING" or "DISPROVEN" — this plan's branch key.

From src/blockchain/Consensus.hpp:
  outcome::result<void> SubmitCertificate( const Certificate &certificate );  // :481, public ingress route (validates, orders by lower serialized SHA-256, publishes)
  outcome::result<Certificate> GetCertificateBySlot( const std::string &slot_key ) const;  // :509, the certificate retrieval getter this test already uses

From test/src/blockchain/multi_node_finality_fault_test.cpp:
  RestartPeer( peer )          // :1040 — stop + recreate from same root/port/keypair
  ConnectPeers( Peers( network ) )  // :1139 — source->pubsub->AddPeers( { target->pubsub->GetInterfaceAddress() } ) for every ordered pair
  RestartAtVote test :2318+    // three restart blocks: restart-vote @54601, restart-certificate @54611 (accepted-certificate barrier on second at :2404, "surviving peers retained" wait :2409-2414, RestartPeer(network.second) at :2416), mint-boundary block @54621
  MultiNodeFinalityFaultTestAccess::ArmAcceptedCertificateBarrier / AcceptedCertificateBarrierEntered  // durable-readback barrier seam used by the failing block

From 12-15-SUMMARY.md Verdict 2 (mechanism the repair targets): pre-restart exactly ONE /cert/mint-v2 callback (on the old peer); after restart, fetchers hit "CANNOT_CONNECT. Blacklisting peer and trying fallback" on the reused host identity, then "No usable route candidates left for CID"; zero cert callbacks in the 25s window; GossipPubSub does not replay pre-link publications and no recovery path re-publishes a completed certificate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read 12-18's hypothesis verdict and take the branch (skip record or activate the fallback repair)</name>
  <files>.planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-skip.md, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/round4-traces/hypothesis-verdict.md (the verdict token and its log citations)
  - .planning/phases/12-multi-node-finality-fault-proof/12-18-SUMMARY.md
  - .planning/phases/12-multi-node-finality-fault-proof/12-17-SUMMARY.md (the verbatim directive: the fallback is conditional on disproof)
  - .planning/STATE.md (Blockers/Concerns and the round-4 records)
  </read_first>
  <action>
  Read the verdict token in round4-traces/hypothesis-verdict.md and branch:

  SKIP branch (token is PROVEN_BLACKLIST_MASKING): create .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-skip.md recording: the verdict token, that the developer directive's fallback condition ("only if it is disproven") is NOT met, that 12-18's seam wiring inside RestartAtVote is the attributed fix 12-20's gate carries, and a citation of the three focused-pass logs. Append a dated 12-19 entry to STATE.md recording the skip and that no source change was made by this plan. Task 2 is then NOT executed — mark it N/A with the skip citation in this plan's SUMMARY and stop.

  REPAIR branch (token is DISPROVEN): do not create fallback-skip.md; record the entry condition in the SUMMARY (which log lines persisted, at which file:line, from hypothesis-verdict.md) and proceed to Task 2. If the verdict token is missing, ambiguous, or contradicts 12-18-SUMMARY.md, STOP and record the inconsistency honestly in STATE.md — do not guess the branch.
  </action>
  <acceptance_criteria>
  - Either round4-traces/fallback-skip.md exists AND contains the string PROVEN_BLACKLIST_MASKING AND Task 2 made no source change, OR hypothesis-verdict.md contains DISPROVEN AND Task 2 executed with its focused logs present.
  - The branch decision is recorded in STATE.md (dated 12-19 entry) on either path.
  - No branch is taken on an ambiguous verdict: a missing/contradictory token results in an honest STOP record, not a guess.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && (test -f .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-skip.md && grep -c "PROVEN_BLACKLIST_MASKING" .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-skip.md || grep -c "DISPROVEN" .planning/phases/12-multi-node-finality-fault-proof/round4-traces/hypothesis-verdict.md) && grep -c "12-19" .planning/STATE.md</automated>
  </verify>
  <done>The directive's conditional is resolved by an explicit, recorded branch keyed on the verdict token — skip record or repair activation — with no ambiguity carried forward.</done>
</task>

<task type="auto">
  <name>Task 2 (entry condition: verdict DISPROVEN): implement post-restart certificate re-publication through public ingress and focused-verify three consecutive RestartAtVote passes</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, .planning/STATE.md, .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-1.log, .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-2.log, .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-3.log</files>
  <read_first>
  - test/src/blockchain/multi_node_finality_fault_test.cpp lines 2384-2520 (the restart-certificate and mint-boundary blocks of RestartAtVote, including ArmAcceptedCertificateBarrier at :2404 and RestartPeer(network.second) at :2416) and lines 1132-1146 (ConnectPeers at :1139)
  - .planning/phases/12-multi-node-finality-fault-proof/round4-traces/hypothesis-verdict.md (which skip mechanism persisted and which CID fetch dead-ended)
  - src/blockchain/Consensus.hpp line 481 (SubmitCertificate) and line 509 (GetCertificateBySlot), plus the SubmitProposal re-advertisement sites in the test file (:2346, :2361, :2405, :2463 — the recorded precedent pattern this repair mirrors)
  - src/blockchain/Consensus.cpp — the SubmitCertificate implementation (grep "SubmitCertificate" for the definition; the STATE decision records its lower-serialized-SHA-256 ordering) and the certificate recovery readback path around :3623-3715
  - .planning/phases/12-multi-node-finality-fault-proof/12-15-SUMMARY.md Verdict 2 (the no-re-publication gap this repair closes)
  </read_first>
  <action>
  ENTRY CONDITION: 12-18's verdict token is DISPROVEN. If Task 1 took the skip branch, do not execute this task.

  Step 1 — locate the missing link from evidence: from hypothesis-verdict.md's citations, determine which CID fetch dead-ends after the restart (certificate CID vs registry/DAG CID) and which peers durably hold the accepted certificate at the restart boundary (the accepted-certificate barrier proves second's durable readback; the :2409-2414 wait proves first/third/passive retained it). Record this determination in the SUMMARY with log citations.

  Step 2 — implement the preferred shape (test-driven public-route re-advertisement, mirroring the recorded SubmitProposal precedent): in the affected restart block of RestartAtVote, immediately after RestartPeer(...) and the subsequent ConnectPeers( Peers( network ) ) call, have a peer that durably holds the accepted certificate re-advertise the UNCHANGED certificate through the public ingress — retrieve the certificate via the existing public query surface already used in this test (GetCertificateBySlot, Consensus.hpp:509 — the outcome::result&lt;Certificate&gt; getter; CheckCertificateForSlot at :515 is the bool predicate used inside the wait predicates, not a retrieval) and submit it through first-or-survivor consensus SubmitCertificate (Consensus.hpp:481). The certificate must be byte-identical to the durable record (CERT-05 idempotent replay); recipients process it through receive-only validation (CERT-02 preserved — no test code writes the CRDT certificate key); add a comment citing both the developer directive's fallback name and the STATE.md SubmitProposal re-advertisement precedent. Do not touch any wait bound, barrier, or timeout.

  Step 3 — justified alternative, only if the preferred shape cannot work: if (and only if) the durable holder's public route rejects the resubmission or the evidence shows no surviving peer holds the certificate content at all (pure surviving-replica gap), implement the smallest scoped change that makes a surviving holder serve or re-publish the content, keeping CERT-02 inviolable (recipients never write the cert key; only the deterministic publisher authority path writes). Record in the SUMMARY exactly which shape was chosen, why the preferred shape was insufficient (with the failing assertion or log line), and the blast radius of what changed. Do not broaden into TransactionManager, CRDT merge behavior, or publisher selection.

  Step 4 — focused verification: rebuild multi_node_finality_fault_test, port preflight (lsof -nP -iTCP:54601-54634 -sTCP:LISTEN empty), then three consecutive focused runs at 1-min load below 2, logs preserved at round4-traces/fallback-restart-{1,2,3}.log: ./build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' 2>&1 | tee .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-1.log (then -2, -3). All three must show the gtest OK line for the RestartAtVote case. A failure is preserved honestly and attributed — it is a diagnosis finding, not a reroll candidate; if any run fails, record it and STOP (do not iterate repairs beyond the directive's named shapes inside this plan; route a new decision instead).

  Step 5 — STATE.md: append a dated 12-19 entry recording the repair shape chosen, the three focused outcomes, and that this repair is the attributed fix 12-20's gate carries. Update Session Continuity.
  </action>
  <acceptance_criteria>
  - The re-advertisement (or justified alternative) is present in test/src/blockchain/multi_node_finality_fault_test.cpp with the precedent-citing comment; grep -c "SubmitCertificate" on the file increased by at least 1 relative to pre-12-19 (or the SUMMARY records the alternative shape with its exact diff surface).
  - No wait bound, barrier arm/release, or timeout constant in the file changed relative to 12-18's state (git diff shows changes confined to the re-advertisement block and its comment).
  - No test code writes the CRDT certificate key: grep for PutConvergentImmutable in the test file returns the same count as before this plan (zero new write sites).
  - round4-traces/fallback-restart-{1,2,3}.log all exist at the durable path and each contains "[       OK ] FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce" and zero "Timed out waiting for condition" lines.
  - STATE.md carries the dated 12-19 repair record naming the attributed fix for 12-20.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && cmake --build build/OSX/Release --target multi_node_finality_fault_test 2>&1 | tail -3 && lsof -nP -iTCP:54601-54634 -sTCP:LISTEN; ./build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' 2>&1 | tee .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-1.log | tail -3 && grep -c "Timed out waiting for condition" .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-1.log</automated>
  </verify>
  <done>The directive's fallback repair exists in the smallest shape the evidence supports, focused-verified 3/3 with durable logs, recipients remain receive-only, and 12-20's gate carries exactly one new attributed fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → certificate ingress | the re-advertisement submits protocol data through the public validation route; must not fabricate or mutate certificate content |
| conditional branch → evidence record | the branch key is a file token; a wrong or guessed branch silently skips a developer-directed repair |
| repair → protocol authority (CERT-02/CERT-05) | re-publication must not grant write authority or admit competing certificate contents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-19-01 | Elevation of privilege | test-driven SubmitCertificate re-advertisement | mitigate | byte-identical certificate only (retrieved from the durable record via GetCertificateBySlot, never constructed); public ingress route subjects it to the existing validation and lower-serialized-SHA-256 ordering; acceptance criterion: no new PutConvergentImmutable or CRDT-write site in test code (CERT-02) |
| T-12-19-02 | Tampering (protocol semantics) | the justified-alternative branch | mitigate | alternative only with recorded evidence that the public route cannot express the repair; CERT-02 inviolable; blast radius confined to the named surfaces; no wait-bound or timeout relaxation (git diff gate in acceptance criteria) |
| T-12-19-03 | Repudiation (branch integrity) | Task 1 branch decision | mitigate | exact-token branch key; ambiguous/missing token forces an honest STOP record instead of a guess; both branches write a durable record (fallback-skip.md or SUMMARY entry condition) |
| T-12-19-04 | Repudiation (evidence loss) | focused verification logs | mitigate | all logs at repo-relative round4-traces/, committed with phase docs; failures preserved, never rerolled |
| T-12-19-05 | DoS (test environment) | port contention during focused runs | mitigate | serial runs only, lsof preflight, 1-min load below 2 per run |
</threat_model>

<verification>
1. Task 1: branch resolved by exact token; skip record or entry-condition record present; STATE.md updated on both paths.
2. Task 2 (repair branch only): rebuild clean; 3/3 focused RestartAtVote passes at durable paths with zero wait-timeout lines; no new CRDT-write site in test code; wait bounds untouched.
3. Multi-source coverage: VERIFICATION gap 1 missing-item 2 (the conditional fallback) is either N/A-by-skip-record or implemented and focused-verified; gap 2 rides the same closure path; requirements TEST-03 (no recipient stall on unavailable CID — re-publication converges without the dead route) and TEST-04 (restart exact-once preserved) are exercised by the focused runs.
</verification>

<success_criteria>
- The developer directive's conditional tail is resolved explicitly: a cited skip record, or a smallest-shape repair with 3/3 focused passes.
- Exactly one attributed fix (12-18's seam wiring OR this plan's repair) is named for 12-20's gate re-entry.
- CERT-02/CERT-05 invariants hold; no timing relaxation anywhere.
- No /tmp evidence; all artifacts under .planning/phases/12-multi-node-finality-fault-proof/round4-traces/.
</success_criteria>

<output>
Create .planning/phases/12-multi-node-finality-fault-proof/12-19-SUMMARY.md when done. Record: the branch taken and its key evidence, the repair shape (if run) with the choice justification and log citations, the three focused-run outcomes, durable paths, and the single attributed fix 12-20 carries.
</output>
