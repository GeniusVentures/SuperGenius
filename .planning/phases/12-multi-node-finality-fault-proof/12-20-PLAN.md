---
phase: 12-multi-node-finality-fault-proof
plan: 20
type: execute
wave: 20
depends_on: ["12-18", "12-19"]
files_modified:
  - .planning/STATE.md
  - .planning/phases/12-multi-node-finality-fault-proof/round4-traces/
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]
must_haves:
  truths:
    - "The gate-entry rule is re-evaluated on the record before any run: a new attributed fix from round 4 exists and is focused-verified, and the only live gate-eligible residual is the pre-declared PublisherLoss child-readiness intermittence (~18%/run, funded by the developer without a go/no-go)"
    - "Three consecutive serial full multi_node_finality_fault_test runs execute on the final build with per-run 1-min load below 2, zero new crash reports against the recorded baseline, and all four fault-scenario cases green by name in every log — OR the series stops at the first strike with every log preserved honestly and no reroll"
    - "The sibling matrix (smoke, lifecycle, cert-fallback, ownership) passes serially on the same build generation"
    - "STATE.md resolves the 12-14 evidence-gap blocker honestly: closed with citations on a passed series, or recorded as still-open with the strike attributed on a failed series"
  artifacts:
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round4-traces/crash-baseline.txt"
      provides: "the pre-series crash-report baseline (newest .ips identity and timestamp) enabling the zero-new-crash assertion"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-a.log"
      provides: "gate run A: ctest full-suite output with the four scenario case names each PASSED"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-b.log"
      provides: "gate run B"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-c.log"
      provides: "gate run C"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round4-traces/sibling-matrix.log"
      provides: "the four sibling suites' serial results on the same build"
  key_links:
    - from: "12-20 gate-entry check"
      to: "the round-4 attributed fix (12-18 seam wiring with verdict PROVEN_BLACKLIST_MASKING, or 12-19's repair)"
      via: "verification that its focused evidence exists at round4-traces/ before run A"
      pattern: "restart-focused-[123]\\.log|fallback-restart-[123]\\.log"
    - from: "each gate log"
      to: "the four fault-scenario cases"
      via: "gtest PASSED lines matching each case name"
      pattern: "\\[       OK \\] FinalityFaultNetwork\\.(SameBurnContentionUsesOneCanonicalSlotAndExactMint|LateContenderAndPassiveRecipientRemainReceiveOnly|RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce|PublisherLossAfterPersistenceUsesDeterministicFailover)"
---

<objective>
Round-4 gap closure, part 3 (of 3): re-attempt the three-consecutive-serial-pass evidence gate (the 12-14/12-12 standard) on the final build carrying the round-4 attributed fix, with the sibling suite matrix and an honest STATE.md resolution of the 12-14 evidence-gap blocker. This plan supersedes 12-17's withheld gate re-attempt; 12-17's own record stays untouched.

Purpose: VERIFICATION gap 1 (three-pass evidence standard — truth 11) is the phase's remaining goal-blocking evidence obligation; gap 2 (restart/publisher-loss as a stable property — truth 13) rides the same runs. The no-reroll rule admits gate re-entry ONLY through the new attributed fix produced by 12-18/12-19. Evidence durability is mandatory: every artifact lives under .planning/phases/12-multi-node-finality-fault-proof/round4-traces/ and is committed — the 2026-09-03 07:52 reboot wiped every /tmp log, so anything in /tmp does not exist as evidence.

Output: crash baseline marker; gate runs A/B/C (or the honest stop at the first strike); per-run and per-log assertions; sibling matrix; STATE.md resolution; committed evidence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-14-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-15-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-17-PLAN.md
@.planning/phases/12-multi-node-finality-fault-proof/12-17-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-18-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-19-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/round4-traces/hypothesis-verdict.md

Standing discipline this gate inherits (12-14/12-15/12-17 precedent):
- No-reroll: this series is the round's single evidence spend for the gate standard. Any single strike fails the series; on a strike, STOP — do not run the remaining planned runs, do not rerun for a better outcome.
- Load discipline: 1-min load below 2 before every run (uptime); wait for external spikes to drain rather than running hot; a run started at load 2+ is contamination, not evidence.
- Crash-freedom: baseline the newest crash report before run A; after each run assert no new .ips for the phase binaries.
- Honesty: failures are preserved verbatim with attribution; STATE.md never softens an outcome; the only residual that is gate-eligible WITHOUT a new developer decision is the PublisherLoss child-readiness intermittence (~18%/run, characterized in 12-15 Verdict 3 and funded by the developer when the gate was first approved).

Command facts (12-15/12-16 precedent): the full-suite serial invocation is ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure (CTest TIMEOUT 300s; a healthy full pass is around 211s). Port preflight before the first run: lsof -nP -iTCP:54601-54634 -sTCP:LISTEN must be empty. Sibling targets: multi_node_finality_fault_compatibility_smoke_test, consensus_pending_lifecycle_test, transaction_manager_certificate_fallback_test, multi_node_finality_fault_process_ownership_test. Crash reports: ~/Library/Logs/DiagnosticReports/*.ips.

<interfaces>
Inputs this plan consumes (all created by 12-18/12-19):
- round4-traces/hypothesis-verdict.md — the branch key (PROVEN_BLACKLIST_MASKING or DISPROVEN).
- The attributed fix: either 12-18's seam wiring in RestartAtVote (verdict PROVEN_BLACKLIST_MASKING; focused evidence restart-focused-{1,2,3}.log, all OK) or 12-19's repair (verdict DISPROVEN; focused evidence fallback-restart-{1,2,3}.log, all OK).
- round4-traces/lifecycle-fixed.log — CR-03's lifecycle serial pass on the fixed build.
- STATE.md's round-4 dated entries.

Four fault-scenario case names that must appear PASSED in every gate log:
- FinalityFaultNetwork.SameBurnContentionUsesOneCanonicalSlotAndExactMint
- FinalityFaultNetwork.LateContenderAndPassiveRecipientRemainReceiveOnly
- FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce
- FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate-entry re-check, crash baseline, then the three-consecutive-serial-pass series with STOP-on-first-strike</name>
  <files>.planning/phases/12-multi-node-finality-fault-proof/round4-traces/crash-baseline.txt, .planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-a.log, .planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-b.log, .planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-c.log, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/round4-traces/hypothesis-verdict.md (the verdict token)
  - .planning/phases/12-multi-node-finality-fault-proof/12-18-SUMMARY.md and 12-19-SUMMARY.md (which attributed fix exists and its focused evidence)
  - .planning/phases/12-multi-node-finality-fault-proof/12-15-SUMMARY.md (the verdict table consulted at gate entry, including Verdict 3's gate-eligible child-readiness characterization)
  - .planning/phases/12-multi-node-finality-fault-proof/12-17-SUMMARY.md (the gate-entry rule this re-attempt must satisfy; the withheld-series record being superseded)
  - .planning/phases/12-multi-node-finality-fault-proof/12-14-SUMMARY.md (the no-reroll and load precedent)
  - .planning/STATE.md (the 12-14 evidence-gap blocker and round-4 entries)
  </read_first>
  <action>
  Step 1 — gate-entry re-check (before ANY run; this is the no-reroll re-entry condition):
  (a) Confirm exactly one round-4 attributed fix exists and its focused evidence is on disk: if hypothesis-verdict.md says PROVEN_BLACKLIST_MASKING, restart-focused-1/2/3.log must all contain the RestartAtVote OK line and 12-19 recorded the skip; if DISPROVEN, fallback-restart-1/2/3.log must all contain the OK line from 12-19's repair.
  (b) Confirm the build is current with all round-4 sources (binaries in build/OSX/Release/test_bin/ newer than the newest source file among src/crdt/graphsync_dagsyncer.{hpp,cpp} and the three test files).
  (c) Confirm no OTHER live-unrepaired signature exists beyond the pre-declared gate-eligible PublisherLoss child-readiness residual (consult 12-15's verdict table and every round-4 log; SameBurn is repaired and source-verified; RestartAtVote is covered by the attributed fix; CR-03 is closed).
  If any of (a)-(c) fails: do NOT start the series — write the gate-entry STOP record into STATE.md (dated 12-20) with the failed clause, and end this task. This mirrors 12-17's own STOP branch.

  Step 2 — crash baseline: write round4-traces/crash-baseline.txt recording date/time, the 1-min load, and the newest crash report identity: the output of ls -t ~/Library/Logs/DiagnosticReports/*.ips | head -1 (or an explicit "none exist" if the directory has no .ips).

  Step 3 — the series. For each of runs A, B, C (logs gate-a.log, gate-b.log, gate-c.log under round4-traces/):
  - Record 1-min load via uptime; it MUST be below 2 — if above, wait and re-check (do not run hot).
  - Port preflight before run A: lsof -nP -iTCP:54601-54634 -sTCP:LISTEN must return empty (terminate stale holders and re-check if not).
  - Execute: ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure 2>&1 | tee .planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-a.log (expect around 211s on a healthy pass; the CTest budget is 300s).
  - Immediately assert on the log: it ends with "100% tests passed, 0 tests failed", AND each of the four fault-scenario case names appears with a PASSED/OK gtest line.
  - Immediately assert crash-freedom: ls -t ~/Library/Logs/DiagnosticReports/*.ips | head -1 matches the baseline identity from Step 2 (no new .ips; if a new one exists, append its name to the log record — the series has failed honestly on the crash-freedom clause).
  - STOP-ON-FIRST-STRIKE: if ANY assertion fails (test failure, timeout, missing case name, new crash report) — do not start the next run. Preserve the failing log verbatim, attribute the striking signature from its log lines (SameBurn/LateContender/RestartAtVote/PublisherLoss-child-readiness/unexpected-new), write the honest series-FAILED record into STATE.md (the 12-14 blocker remains open; no reroll; route the next decision), and end this task after recording.
  Only on a fully green run proceed to the next letter.

  Step 4 — on three green runs: write the series-PASSED record into STATE.md: the three log paths with durations and pre-run loads, the crash-baseline identity with zero new reports, and that the 12-14 evidence-gap blocker is closed under the no-reroll rule carrying the round-4 attributed fix. Update the Session Continuity block.
  </action>
  <acceptance_criteria>
  - round4-traces/crash-baseline.txt exists and predates gate-a.log.
  - PASSED series: gate-a.log, gate-b.log, gate-c.log all exist under the phase directory; each ends with "100% tests passed, 0 tests failed"; each contains all four fault-scenario case names with OK/PASSED lines; no new .ips appeared after the baseline; STATE.md records series PASSED and closes the 12-14 blocker with citations.
  - FAILED series (either outcome is plan-faithful): the striking run's log is preserved verbatim, runs after the strike do not exist, the strike is attributed by name in STATE.md, the 12-14 blocker is recorded as still open, and no reroll occurred.
  - Every run's pre-run 1-min load is recorded and was below 2.
  - No gate artifact references /tmp.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && ls .planning/phases/12-multi-node-finality-fault-proof/round4-traces/crash-baseline.txt && for f in a b c; do test -f .planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-$f.log && grep -c "100% tests passed, 0 tests failed" .planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-$f.log && grep -c "SameBurnContentionUsesOneCanonicalSlotAndExactMint\|LateContenderAndPassiveRecipientRemainReceiveOnly\|RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce\|PublisherLossAfterPersistenceUsesDeterministicFailover" .planning/phases/12-multi-node-finality-fault-proof/round4-traces/gate-$f.log; done; true</automated>
  </verify>
  <done>Either the three-consecutive-serial-pass standard is met on the final build with zero new crash reports and durable committed evidence, or the series stopped at the first strike with an honest preserved record — never a reroll, never a softened outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Sibling suite matrix on the same build and the honest STATE.md resolution</name>
  <files>.planning/phases/12-multi-node-finality-fault-proof/round4-traces/sibling-matrix.log, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/12-16-SUMMARY.md (the prior sibling-matrix precedent and target list)
  - .planning/STATE.md (Current Position, Blockers/Concerns — the 12-14 entry and Task 1's new gate record)
  - .planning/ROADMAP.md (the Phase 12 plan list this round completes)
  </read_first>
  <action>
  Run the four sibling suites serially on the same build generation (no -j; consensus_pending_lifecycle_test has no RUN_SERIAL property and shares fixed ports with other CRDTFixture binaries), at 1-min load below 2, preserving the combined output at the durable path:
  ctest --test-dir build/OSX/Release -R '^(multi_node_finality_fault_compatibility_smoke_test|consensus_pending_lifecycle_test|transaction_manager_certificate_fallback_test|multi_node_finality_fault_process_ownership_test)$' --output-on-failure 2>&1 | tee .planning/phases/12-multi-node-finality-fault-proof/round4-traces/sibling-matrix.log
  Expected durations from the 12-16 matrix: smoke around 6.4s, lifecycle around 44s, cert-fallback around 32s, ownership under 1s. All four must end Passed (100% tests passed, 0 tests failed overall).

  Then finalize STATE.md per Task 1's outcome:
  - Series PASSED: resolve the 12-14 evidence-gap blocker (record it closed, citing gate-a/b/c.log, the crash baseline, and sibling-matrix.log), update Current Position (Phase 12 round-4 complete, plan counters), and update Session Continuity.
  - Series FAILED at the strike: the blocker stays open exactly as Task 1 recorded; add only the sibling-matrix outcome line (a strike does not invalidate the matrix evidence — record it either way, honestly).
  Do not rewrite historical notes; append dated entries only.

  Finally commit the round-4 evidence with the phase documentation (git add .planning/phases/12-multi-node-finality-fault-proof/round4-traces/ and the STATE/plan files) so the evidence survives reboots by living in the repository — the entire point of the durable-path constraint.
  </action>
  <acceptance_criteria>
  - round4-traces/sibling-matrix.log exists and ends with "100% tests passed, 0 tests failed"; it contains a Passed line for each of the four sibling targets.
  - STATE.md: on a passed series, the 12-14 evidence-gap blocker entry is resolved with citations to round4-traces/ artifacts; on a failed series it remains open with the strike attributed. Current Position reflects the actual end state (no aspirational claims).
  - git status shows round4-traces/ staged/committed with the phase docs (no untracked evidence left behind).
  - No sibling artifact references /tmp.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && ctest --test-dir build/OSX/Release -R '^(multi_node_finality_fault_compatibility_smoke_test|consensus_pending_lifecycle_test|transaction_manager_certificate_fallback_test|multi_node_finality_fault_process_ownership_test)$' --output-on-failure 2>&1 | tee .planning/phases/12-multi-node-finality-fault-proof/round4-traces/sibling-matrix.log | tail -4 && git add .planning/phases/12-multi-node-finality-fault-proof/round4-traces .planning/STATE.md && git status --short | head -10</automated>
  </verify>
  <done>The sibling matrix is green on the same build with a durable log, STATE.md tells the truth about the gate outcome in every branch, and all round-4 evidence is committed to the repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate-entry rule → evidence record | re-entry into the single-shot series is legitimate only through the round-4 attributed fix |
| run environment → result validity | load and port contamination can fake a failure; a hot run is not evidence |
| crash-report directory → crash-freedom claim | the zero-new-crash assertion depends on an honestly recorded baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-20-01 | Repudiation (no-reroll integrity) | the three-run series | mitigate | STOP-on-first-strike is mechanical: remaining runs must not exist after a strike; every log preserved verbatim; STATE.md records pass or fail without softening; acceptance criteria check both branches |
| T-12-20-02 | Information spoofing (environment) | load/port contamination producing false failures | mitigate | 1-min load below 2 recorded before every run; lsof port preflight before run A; contamination never silently retried — a hot run is recorded, not rerun |
| T-12-20-03 | Repudiation (crash-freedom) | the zero-new-crash assertion | mitigate | crash-baseline.txt recorded BEFORE run A with the newest .ips identity; per-run re-check; a new .ips fails the series honestly |
| T-12-20-04 | Repudiation (evidence durability) | gate evidence artifacts | mitigate | every artifact under repo-relative round4-traces/, committed with the phase docs in Task 2; nothing in /tmp counts as evidence |
| T-12-20-05 | Tampering (gate re-entry) | gate-entry re-check | mitigate | clause-by-clause check (attributed fix on disk, build current, no other live signature) recorded before run A; any failed clause forces the STOP branch exactly like 12-17 |
</threat_model>

<verification>
1. Task 1: gate-entry record exists before any run; crash baseline predates run A; three durable gate logs with all four case names green and zero new crash reports — or the honest stop record with no post-strike runs.
2. Task 2: sibling matrix 100% passed on the same build; STATE.md resolves or honestly holds the 12-14 blocker; evidence committed.
3. Multi-source coverage: VERIFICATION gap 1 missing-items 3 and 4 (the re-attempted gate under the no-reroll rule; durable-path evidence) are this plan's whole purpose; gap 2's truth (restart exact-once and publisher-loss green inside all gate logs) is asserted per-log by the four-case check; requirements TEST-01 through TEST-06 are collectively proven by the series (each scenario case maps to its requirement).
</verification>

<success_criteria>
- The evidence standard is either MET (three consecutive serial full passes, zero new crash reports, four cases green per log, sibling matrix green, blocker closed with citations) or HONESTLY UNMET (series stopped at the first strike, preserved logs, attributed signature, blocker open, zero rerolls).
- All round-4 evidence lives in the repository at .planning/phases/12-multi-node-finality-fault-proof/round4-traces/ and survives machine restarts.
- 12-17's own record was not modified; this plan supersedes its gate re-attempt.
</success_criteria>

<output>
Create .planning/phases/12-multi-node-finality-fault-proof/12-20-SUMMARY.md when done. Record: the gate-entry clause evaluation, the crash baseline identity, each run's outcome with duration/pre-run load/log path, the strike attribution if the series failed (which signature, which log lines), the sibling-matrix results, and the final honest state of the 12-14 evidence-gap blocker.
</output>
