---
phase: 12-multi-node-finality-fault-proof
plan: "05"
type: execute
wave: 5
depends_on: [12-04]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
autonomous: true
gap_closure: true
requirements: [TEST-04]
must_haves:
  truths:
    - "A validator's one canonical-slot active-vote record remains readable from its direct local RocksDB store across a real same-root StopPeer/recreate until a matching, durably accepted CRDT certificate exists."
    - "The vote-boundary restart subcase proves the original proposal ID is recovered at the same canonical slot before any later proposal, vote, certificate, or Mint success is used as evidence."
    - "The clean serial real-socket multi_node_finality_fault_test CTest target passes all named Phase 12 scenarios without a direct handler, direct CRDT write, forced timer, sleep, mock transport, or successor certificate re-advertisement."
  artifacts:
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "Deterministic, stage-labelled same-root active-vote persistence diagnosis and the strict TEST-04 regression proof"
      contains: "RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce"
    - path: "src/blockchain/Consensus.cpp"
      provides: "Only the diagnosed active-vote lifecycle correction, if production behavior is the root cause"
      contains: "RecoverActiveVotes"
  key_links:
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "ConsensusManager::ActiveVoteStorageKey and the peer's direct RocksDB datastore"
      via: "read-only raw key/bytes snapshots before shutdown, during teardown, before manager construction, and after recovery"
      pattern: "ActiveVote.*Snapshot|DurableActiveVoteProposalId"
    - from: "src/blockchain/Consensus.cpp"
      to: "direct local /consensus/vote/<slot> record"
      via: "PersistOrLoadExactActiveVote, RecoverActiveVotes, and ReleaseActiveVoteForAcceptedSlot"
      pattern: "PersistOrLoadExactActiveVote|RecoverActiveVotes|ReleaseActiveVoteForAcceptedSlot"
---

<objective>
Close the remaining TEST-04 gap: a pre-broadcast, locally durable active vote disappears after the vote owner's real same-root restart.

Purpose: Establish exactly whether teardown deletes the direct local record, recovery reads the wrong scope/key, or the fault fixture observes a different durable store; repair only that demonstrated cause while preserving Phase 9 one-vote finality.
Output: A diagnostic-first, TDD-backed active-vote restart regression and a passing serial real-socket Phase 12 target.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md
@.planning/phases/12-multi-node-finality-fault-proof/12-PATTERNS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-01-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-02-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-03-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-04-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md
@.planning/phases/09-durable-one-vote-finality/09-CONTEXT.md
@.planning/phases/09-durable-one-vote-finality/09-01-SUMMARY.md
@.planning/phases/09-durable-one-vote-finality/09-02-SUMMARY.md
@src/blockchain/Consensus.hpp
@src/blockchain/Consensus.cpp
@test/src/blockchain/multi_node_finality_fault_test.cpp
@test/src/blockchain/consensus_pending_lifecycle_test.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the same-root active-vote loss deterministic and classify its exact lifecycle boundary</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp</files>
  <read_first>
    - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md — TEST-04 is red because lines 915-921 see the stored proposal ID before StopPeer and nullopt after reopening the unchanged root; do not replace this assertion with later Mint success.
    - .planning/phases/09-durable-one-vote-finality/09-CONTEXT.md — Phase 9 D-05 through D-10 require one direct local RocksDB record, exact-vote-only recovery/re-announcement, expiry lock retention, and deletion only after durable same-slot certificate acceptance.
    - src/blockchain/Consensus.cpp — read ConsensusManager::Close, ActiveVoteStorageKey, PersistOrLoadExactActiveVote, RecoverActiveVotes, HasAcceptedCertificateForSlot, and ReleaseActiveVoteForAcceptedSlot as one lifecycle; ReleaseActiveVoteForAcceptedSlot is currently the only intended direct-record removal path.
    - test/src/blockchain/multi_node_finality_fault_test.cpp — read FinalityFaultNetwork::Peer::Stop, StartPeer, RestartPeer, MultiNodeFinalityFaultTestAccess::DurableActiveVoteProposalId, the fault barriers, and the first subcase of RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce.
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp — preserve the existing deterministic active-vote/restart coverage and use its access style only for observation; it is not a Phase 12 transport driver.
  </read_first>
  <behavior>
    - RED: add the standalone `TEST_F(FinalityFaultNetwork, ActiveVoteRestartDiagnosticClassifiesLifecycleBoundary)` diagnostic. The exact gtest name is required because Task 1 runs this one test as three separate complete processes; each run intentionally exercises the same-root active-vote boundary with the first validator disconnected after registry persistence, drives only public CreateProposal/SubmitProposal, and fails at the first labelled stage where its exact direct-local record changes or recovery differs.
    - The diagnostic records the canonical slot, exact storage key, and a byte-presence/digest snapshot: after successful pre-broadcast persistence; after ConsensusManager::Close and before runtime ownership is released; after old manager ownership is released while its existing db handle remains open; after the same root's GlobalDB is reopened but before Blockchain/ConsensusManager construction; and after RecoverActiveVotes completes.
    - The diagnostic separately observes whether ReleaseActiveVoteForAcceptedSlot was entered or succeeded for this slot, and proves no matching accepted `/cert/<slot>` exists at each pre-restart stage. It therefore distinguishes: erroneous certificate-driven release/shutdown deletion; root/key/store-lifecycle loss; and an intact raw record missed by recovery/decoding/scope.
    - Each RED failure must emit exactly one machine-searchable label before failing. Its full value must match `ACTIVE_VOTE_RED stage=(after-pre-broadcast-persistence|after-consensus-manager-close|after-manager-ownership-release|after-same-root-globaldb-reopen-before-manager|after-recover-active-votes) classification=(release-without-certificate|record-lost|recovery-missed-record)`: use one of those lifecycle stages and one of those mutually exclusive classifications, never a free-form token. Task 1 may proceed only after the three completed processes all fail and emit the identical validated stage/classification label; a later certificate or Mint must not make this diagnostic pass.
    - GREEN is not allowed in Task 1: retain the verifier's strict pre/post-restart active-vote proposal-ID equality in the existing TEST-04 subcase and report the deterministic classified failure.
  </behavior>
  <action>Per D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-09, and Phase 9 D-05 through D-10, extend the existing four-peer fixture only with read-only stage evidence. Keep all four real local peers and their independent on-disk roots; for this diagnosis, leave the selected vote owner disconnected after StoreRegistry so no peer can turn the pre-broadcast fault into certificate finality. Arm the existing post-persistence barrier, submit through the existing public API, and use bounded conditions only. Add a test-local ActiveVoteLifecycleSnapshot owned by Peer (root path, canonical slot, storage key, raw-record presence plus stable byte digest, decoded proposal ID, accepted-certificate status, recovered in-memory proposal ID, and release-attempt/result counters). Refactor StartPeer just enough to capture a direct datastore read after GlobalDB opens and before Blockchain::New creates the manager; refactor Peer::Stop just enough to take read-only snapshots after consensus close and after manager destruction while its existing db handle is still alive, before db reset. Do not reopen a second store, copy data, write a key, alter StopPeer ordering except for these observations, or delete the root. If necessary, add only friend-scoped counter/getter access in Consensus.hpp/.cpp: increment the counter at the existing ReleaseActiveVoteForAcceptedSlot boundary and expose current recovered active_votes_ membership. This instrumentation must neither select, publish, validate, remove, retry, or synthesize protocol data. Add the standalone `TEST_F(FinalityFaultNetwork, ActiveVoteRestartDiagnosticClassifiesLifecycleBoundary)`—not a helper folded into TEST-04—with assertions that the exact same canonical key and raw record survive each stage and that manager recovery exposes the same proposal ID. At its first violated assertion, print exactly one label matching the closed lifecycle-stage/classification enum `ACTIVE_VOTE_RED stage=(after-pre-broadcast-persistence|after-consensus-manager-close|after-manager-ownership-release|after-same-root-globaldb-reopen-before-manager|after-recover-active-votes) classification=(release-without-certificate|record-lost|recovery-missed-record)`, where the mutually exclusive classification is derived from the observed snapshots. Run the named test in three separate full-process invocations, collect each invocation's label, and require all three to be nonzero failures with exactly one valid label each and the identical validated stage/classification pair before Task 2 may alter production code. Retain TEST-04's strict pre/post-restart active-vote proposal-ID equality. Do not change production lifecycle semantics in this task.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; active_vote_log_base="$(mktemp -t active-vote-red.XXXXXX)" &amp;&amp; active_vote_labels="${active_vote_log_base}.labels" &amp;&amp; active_vote_valid_label_pattern='ACTIVE_VOTE_RED stage=(after-pre-broadcast-persistence|after-consensus-manager-close|after-manager-ownership-release|after-same-root-globaldb-reopen-before-manager|after-recover-active-votes) classification=(release-without-certificate|record-lost|recovery-missed-record)' &amp;&amp; active_vote_any_label_pattern='ACTIVE_VOTE_RED stage=[^[:space:]]+ classification=[^[:space:]]+' &amp;&amp; for active_vote_run in 1 2 3; do build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.ActiveVoteRestartDiagnosticClassifiesLifecycleBoundary' &gt;"${active_vote_log_base}.${active_vote_run}" 2&gt;&amp;1; active_vote_status=$?; test "$active_vote_status" -ne 0 || exit 1; active_vote_any_label_count="$(rg -x "$active_vote_any_label_pattern" "${active_vote_log_base}.${active_vote_run}" | wc -l | tr -d '[:space:]')"; active_vote_valid_label_count="$(rg -x "$active_vote_valid_label_pattern" "${active_vote_log_base}.${active_vote_run}" | wc -l | tr -d '[:space:]')"; test "$active_vote_any_label_count" -eq 1 || exit 1; test "$active_vote_valid_label_count" -eq 1 || exit 1; rg -x "$active_vote_valid_label_pattern" "${active_vote_log_base}.${active_vote_run}" &gt;&gt; "$active_vote_labels"; done &amp;&amp; test "$(wc -l &lt; "$active_vote_labels")" -eq 3 &amp;&amp; test "$(sort -u "$active_vote_labels" | wc -l)" -eq 1</automated>
  </verify>
  <done>All three separate, complete invocations of `FinalityFaultNetwork.ActiveVoteRestartDiagnosticClassifiesLifecycleBoundary` fail with exactly one identical, whitelist-valid `ACTIVE_VOTE_RED` label: its stage is one of `after-pre-broadcast-persistence`, `after-consensus-manager-close`, `after-manager-ownership-release`, `after-same-root-globaldb-reopen-before-manager`, or `after-recover-active-votes`; its classification is exactly one of `release-without-certificate`, `record-lost`, or `recovery-missed-record`. The result identifies whether release was entered without a matching durably accepted certificate, a direct record was lost during the named same-root lifecycle stage, or the direct record survived but RecoverActiveVotes missed/rejected it. The original TEST-04 pre/post proposal-ID assertion remains intact and no production finality behavior has been changed merely to observe it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair only the diagnosed active-vote boundary and prove strict recovery over real sockets</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp</files>
  <read_first>
    - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md — the required closure is recoverability of the persisted same-slot active vote plus a passing clean serial CTest, not an eventual successful Mint.
    - Task 1's committed diagnostic evidence in test/src/blockchain/multi_node_finality_fault_test.cpp — use its stage classification; do not speculate or apply more than one root-cause branch.
    - src/blockchain/Consensus.cpp — keep PersistOrLoadExactActiveVote's byte-identical collision rule, RecoverActiveVotes' signature/key validation and deadline handling, and ReleaseActiveVoteForAcceptedSlot's durable certificate gate intact except where the Task 1 classification proves the bug.
    - .planning/phases/09-durable-one-vote-finality/09-01-SUMMARY.md and 09-02-SUMMARY.md — preserve exact signed-vote recovery, no replacement vote, expiry lock retention, and release only after committed validated same-slot certificate finality.
    - .planning/phases/12-multi-node-finality-fault-proof/12-03-SUMMARY.md and 12-04-SUMMARY.md — preserve stop-aware post-durability barriers, durable-state Mint proof, CRDT certificate authority, best-effort PubSub cleanup, and no successor certificate re-advertisement.
  </read_first>
  <behavior>
    - RED: retain Task 1's failing classified case and add/update the smallest regression that proves its specific cause, including the original same-slot direct-record assertion immediately after RestartPeer and before reconnect/re-submission.
    - GREEN: if the raw record is lost while teardown runs without a matching durable certificate, fix only that shutdown/storage lifecycle ownership boundary; if the raw record survives but recovery misses it, fix only the demonstrated prefix/key/decode/recovery boundary; if the fixture changes roots or observes the wrong datastore, fix only the fixture lifecycle. Do not make any production edit unless the diagnostic rules out the fixture and proves it is required.
    - After GREEN, the recreated manager reads and decodes the same canonical-slot record, re-announces only the stored signed vote through normal production work when still before its absolute deadline, and never permits a different vote. A matching durable accepted CRDT certificate remains the sole reason to remove the direct local record.
  </behavior>
  <action>Use Task 1's single classified result to make the smallest TDD repair. Preserve generic Consensus behavior and Phase 9 invariants: the active vote remains a direct local RocksDB record under `/consensus/vote/<canonical-slot>`, byte-distinct replacement records are rejected, recovery verifies the stored slot/proposal/vote/signature and may re-announce only those exact bytes before the recorded deadline, expiry retains the lock, and ReleaseActiveVoteForAcceptedSlot may remove it only after existing durable accepted CRDT certificate readback and validation for that same slot. Do not add a CRDT active-vote record, do not use PubSub as vote durability, and do not clear the record because the process is stopping. If the diagnosis identifies the test lifecycle/root rather than Consensus, restrict the GREEN edit to `multi_node_finality_fault_test.cpp`; if it identifies recovery scoping or an erroneous removal, restrict Consensus.hpp/.cpp to the named faulty branch and retain the diagnostic observer as a regression guard. Keep the original restart assertion at its current logical boundary—immediately after same-root RestartPeer and before ConnectPeers or the public unchanged-proposal re-submission—then let the existing real AddPeers and public SubmitProposal route prove finality. Do not call Handle*, OnConsensusMessage, CertificateReceived, OnConsensusCertificate, a direct GlobalDB Put/remove API from test code, forced round/timer methods, mocks, sleeps, or a successor certificate re-advertisement. Retain publisher-loss behavior unchanged: CRDT certificate is authoritative and PubSub is only best-effort cleanup.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test consensus_pending_lifecycle_test --parallel 4 &amp;&amp; build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' &amp;&amp; build/OSX/Release/test_bin/consensus_pending_lifecycle_test --gtest_filter='*ActiveVote*:*Restart*:*Certificate*' &amp;&amp; ctest --test-dir build/OSX/Release --output-on-failure --timeout 300 -R '^multi_node_finality_fault_test$' &amp;&amp; ! rg -n 'ConsensusPendingLifecycleTestAccess::(Handle|CertificateReceived|WriteLiveCertificate|Force)|\bHandle[A-Za-z]*\(|\bOnConsensusMessage\(|\bCertificateReceived\(|\bOnConsensusCertificate\(|\bGlobalDB::Put|\bPutConvergentImmutable\(|\bremove\(.*ActiveVote|sleep_for|sleep_until|Force[A-Za-z]*(Timer|Due|Round)|mock' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
  </verify>
  <done>TEST-04's vote-boundary restart keeps the exact pre-broadcast active-vote proposal ID readable from the direct local RocksDB record and recovered at the identical canonical slot before reconnecting or producing a Mint; the other two restart subcases remain exact-once durable proofs; the clean serial real-socket Phase 12 target passes; and Phase 9 focused active-vote regressions stay green without weakening certificate authority or adding test shortcuts.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Required behavior | Plan | Status | Notes |
|--------|----|-------------------|------|--------|-------|
| GOAL | Phase 12 | Production-path finality remains safe and live through restart, without losing canonical one-vote safety. | 12-05 Tasks 1-2 | COVERED | Diagnoses and closes the remaining vote-boundary restart truth. |
| REQ | TEST-01 | Same-burn contention produces one canonical winning certificate. | 12-02 | COVERED | Passing and deliberately preserved. |
| REQ | TEST-02 | Late contender cannot obtain a second usable vote/certificate. | 12-02, 12-05 Task 2 | COVERED | Repair must retain Phase 9 no-replacement semantics. |
| REQ | TEST-03 | Passive PubSub recipient is receive-only. | 12-02 | COVERED | Passing and untouched. |
| REQ | TEST-04 | Vote, certificate, and Mint-boundary restarts preserve original vote and avoid duplicate Mint. | 12-05 Tasks 1-2 | COVERED | The sole verifier failure; strict active-vote recovery precedes Mint evidence. |
| REQ | TEST-05 | Publisher loss preserves persist-before-advertise without conflict. | 12-03, 12-04 | COVERED | Passing; no successor notification/re-advertisement may be added. |
| REQ | TEST-06 | Tests use production PubSub, CRDT, RocksDB, and Mint ingress. | 12-01, 12-05 Tasks 1-2 | COVERED | Static gate and real-socket CTest enforce the route. |
| RESEARCH | Pattern 1/2/3 | Persistent peer roots; post-durability barriers; durable readback over in-memory state. | 12-05 Tasks 1-2 | COVERED | Adds stage-labelled direct datastore readbacks without writing or mocking. |
| RESEARCH | Pitfalls 1-5 | No private handlers, no assumed reconnect, no root deletion, correct durable boundary, no memory-only proof. | 12-05 Tasks 1-2 | COVERED | Diagnosis checks concrete stages and the source gate rejects shortcuts. |
| CONTEXT | D-01, D-02 | Four real peers with independent retained RocksDB roots. | 12-05 Task 1 | COVERED | Diagnostic retains the existing component harness and same-root recreation. |
| CONTEXT | D-03, D-04 | Production ingress/real lifecycle disorder, not direct delivery or mock transport. | 12-05 Tasks 1-2 | COVERED | Uses disconnected lifecycle, AddPeers, and public SubmitProposal only. |
| CONTEXT | D-05 | Certificate persistence-before-notification publisher-loss boundary. | 12-03, 12-04, 12-05 Task 2 | COVERED | Preserved; no publisher-loss change is in scope. |
| CONTEXT | D-06, D-07 | Restart at actual durable boundary with read-only observation. | 12-05 Tasks 1-2 | COVERED | Classifying snapshots/counters are post-action observers only. |
| CONTEXT | D-08, D-09, D-10 | Named enabled bounded tests with per-node durable proof. | 12-05 Task 2 | COVERED | Runs the registered serial CTest and retains durable Mint assertions. |
| CONTEXT | Deferred bridge startup/RPC wiring | Do not broaden into unrelated startup/RPC work. | — | EXCLUDED | Explicitly deferred and outside this closure. |
| USER LOCK | Direct local active-vote invariant | Retain direct local vote from pre-broadcast persistence until matching durably accepted CRDT certificate; CRDT is authoritative and PubSub cleanup only. | 12-05 Tasks 1-2 | COVERED | Explicitly preserved in RED/GREEN criteria and repair boundaries. |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public proposal ingress → direct local active-vote record | Untrusted/ordered network work must not create a second usable vote or bypass direct local durability. |
| active peer runtime → same-root recreated runtime | Teardown/reconstruction can lose or mis-scope state that is supposed to remain local and durable. |
| accepted CRDT certificate → direct-vote release | Only a matching durably read-back authoritative certificate may unlock/delete the local vote. |
| test harness → production finality path | Diagnostic access can observe evidence but must not manufacture messages, authority, timers, or persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-15 | T/E | `ReleaseActiveVoteForAcceptedSlot` | mitigate | Task 1 records release entry/result with matching durable certificate status; Task 2 preserves validated same-slot CRDT readback as the only removal path. |
| T-12-16 | T/D | `Peer::Stop` / `StartPeer` and RocksDB root lifecycle | mitigate | Stage-labelled read-only snapshots prove the exact key/value survives close, ownership release, and same-root reopen; no root deletion or replacement is allowed. |
| T-12-17 | T | `RecoverActiveVotes` | mitigate | Require exact key, decode/signature validity, and recovered in-memory proposal ID to agree before reconnecting or re-submitting data. |
| T-12-18 | S/T/R | Phase 12 test driver | mitigate | Public SubmitProposal/AddPeers and real local services only; source gate rejects handlers, receiver calls, CRDT writes/removals, forced timers, mocks, and sleeps. |
| T-12-SC | Tampering | C++ source/test changes | mitigate | No package installation; TDD RED/GREEN evidence plus Phase 9 focused tests and serial CTest guard the safety contract. |
</threat_model>

<verification>
Task 1 establishes the deterministic classified RED evidence before any behavioral repair. Task 2 first passes the focused TEST-04 restart scenario and Phase 9 active-vote lifecycle coverage, then runs the registered clean serial real-socket `multi_node_finality_fault_test` CTest target with its 300-second bound. The static gate is mandatory in Task 2. Do not treat a later Mint result, a different active vote, or an eventual certificate as proof that the immediate post-restart direct-record assertion passed.
</verification>

<success_criteria>
1. The diagnostic identifies the concrete persistence/recovery/lifecycle boundary responsible for the record loss using same-root raw readback and no matching accepted certificate; it is deterministic across three focused repetitions.
2. The only GREEN repair corresponds to that diagnosis and keeps the direct local exact vote present/recovered at the same slot immediately after restart.
3. The reconstructed manager may publish/recover only its exact stored signed vote; no different usable vote can be created, including after deadline expiry.
4. The registered serial `multi_node_finality_fault_test` CTest target passes all scenarios while TEST-05 remains CRDT-authoritative with best-effort PubSub cleanup and no successor certificate re-advertisement.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-05-SUMMARY.md` when done. Do not modify or overwrite 12-01 through 12-04 plans, summaries, verification artifacts, ROADMAP.md, or REQUIREMENTS.md as part of this planning task.
</output>
