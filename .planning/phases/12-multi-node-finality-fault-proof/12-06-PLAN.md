---
phase: 12-multi-node-finality-fault-proof
plan: "06"
type: execute
wave: 6
depends_on: [12-05]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
  - .planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md
  - .planning/phases/12-multi-node-finality-fault-proof/12-07-HANDOFF.md
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]
must_haves:
  truths:
    - "Every red Phase-12 scenario has a committed 12-06-DIAGNOSIS.md matrix containing three fresh-process and three exact ordered-prefix executions, topology validity, failure phase, and one exhaustive terminal classification."
    - "If a failure is sequence-dependent, the four-peer fixture restores all owned lifecycle state and the normal serial real-socket target proves TEST-02 through TEST-05 without changing finality protocol behavior."
    - "If a failure reproduces alone after valid topology is observed, the evidence identifies a production-path defect without concealing it through fixture timing or assertion changes; a separate production-fix plan is then required."
    - "The passing contention and production-route audit regressions remain real PubSub/CRDT/RocksDB/Mint proofs, and the Phase-9 active-vote contract remains intact."
  artifacts:
    - path: ".planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md"
      provides: "Versioned deterministic six-run matrix and exactly one terminal label per red scenario: prefix-contamination, fresh-production, pre-topology-failure, or inconclusive"
      contains: "P12_HARNESS_CLASSIFICATION"
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "RAII-safe lifecycle instrumentation plus fresh-versus-prefix reproducibility diagnostics for the existing Phase-12 scenarios"
      contains: "P12_HARNESS_STATE"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/12-07-HANDOFF.md"
      provides: "Blocking, evidence-complete handoff when any scenario is not proven prefix contamination"
      contains: "P12_HARNESS_CLASSIFICATION"
  key_links:
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "GossipPubSub::Start, GlobalDB::New, Peer::Stop, GeniusAccount::Get/SetSecureStorageFactory"
      via: "phase-labelled read-only diagnostic events and scoped restoration"
      pattern: "P12_HARNESS_(STATE|EXCEPTION|CLASSIFICATION)"
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "ConsensusManager::SubmitProposal and GetCertificateBySlot"
      via: "the unchanged public ingress and durable assertion route"
      pattern: "SubmitProposal"
---

<objective>
Diagnose and, only when the evidence proves cross-test contamination, repair Phase 12 harness lifecycle reliability without weakening the production finality proof.

Purpose: The latest verifier found three red behaviors only in one fresh serial invocation: late/passive reopened-store recovery times out, the complete restart scenario throws during fixture construction, and publisher loss times out before topology readiness. The focused active-vote lifecycle diagnostic and prior focused scenarios passed. Determine whether each red result is an individual production-path failure or process/fixture contamination before selecting any fix.
Output: Executable TDD diagnostics, a retained failure-classification matrix, and a fixture-only isolation repair when (and only when) the matrix proves sequence dependence. A fresh-process production reproduction is an explicit stop condition for a separately planned minimal production fix, not permission to tune the harness around it.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md
@.planning/phases/12-multi-node-finality-fault-proof/12-PATTERNS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-03-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-04-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-05-SUMMARY.md
@.planning/phases/09-durable-one-vote-finality/09-CONTEXT.md
@.planning/phases/09-durable-one-vote-finality/09-VERIFICATION.md
@test/src/blockchain/multi_node_finality_fault_test.cpp
@test/src/blockchain/CMakeLists.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Produce an executable and versioned fresh-versus-prefix diagnosis before changing behavior</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp — retain the existing `Peer::StartPeer`, `Peer::Stop`, `RestartPeer`, `ConnectAndWaitForPeers`, `LateContenderAndPassiveRecipientRemainReceiveOnly`, `RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce`, and `PublisherLossAfterPersistenceUsesDeterministicFailover` public-route scenarios.
    - src/account/GeniusAccount.hpp and src/account/GeniusAccount.cpp — use the existing `GetSecureStorageFactory` and `SetSecureStorageFactory` APIs to observe and restore test-process factory state; do not change their production implementation.
    - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md — the target facts are the line-968 late recovery timeout, fixture-construction unknown exception, and line-520 publisher topology timeout.
    - .planning/phases/09-durable-one-vote-finality/09-VERIFICATION.md — active votes are direct local `/consensus/vote/<canonical-slot>` records; exact-vote recovery and release-after-durable-accepted-certificate behavior must remain unchanged.
  </read_first>
  <behavior>
    - RED: a lifecycle diagnostic test fails until every peer construction and teardown phase emits parseable state, exception, and factory-restoration evidence; a missing listener, datastore, thread join, or restored factory is a test failure rather than an opaque timeout.
    - Each event uses exactly one of these labels: `P12_HARNESS_STATE`, `P12_HARNESS_EXCEPTION`, or `P12_HARNESS_CLASSIFICATION`.
    - `P12_HARNESS_STATE` identifies run mode (`fresh` or `prefix`), scenario, peer, lifecycle phase, port, root, PubSub started/listener identity, host identity, consensus-topic peer count, `GlobalDB`/datastore presence, RocksDB-root existence, `io_context::stopped`, I/O-thread joinability, and whether the global secure-storage factory was installed/restored.
    - The committed `12-06-DIAGNOSIS.md` has one deterministic matrix row for every `(late|restart|publisher) × (fresh|exact-prefix) × (1|2|3)` execution. Each row records the exact ordered GTest filter, exit result, topology validity (`valid`, `invalid`, or `not-reached`), exact failure phase (or `none`), the trace location, and the full diagnostic label emitted by the test process.
    - `P12_HARNESS_EXCEPTION` identifies run mode, scenario, peer and construction/teardown phase plus `std::exception::what()` or `unknown`; it is emitted before the case reports the failure. The document ends with exactly one full `P12_HARNESS_CLASSIFICATION classification=<outcome> scenario=<scenario> terminal_outcome=<outcome> failure_phase=<phase> fresh_trace=<path> topology_evidence=<path>` label for each red scenario; `<outcome>` is only `prefix-contamination`, `fresh-production`, `pre-topology-failure`, or `inconclusive`.
    - Classification is exhaustive and mutually exclusive, in this precedence order: `pre-topology-failure` when any fresh execution fails during start/constructor or before valid topology is proven; otherwise `fresh-production` when any fresh execution fails after the matrix proves valid topology; otherwise `prefix-contamination` only when all three fresh executions pass and all three exact-prefix executions reproduce the same failure phase; otherwise `inconclusive` (including mixed fresh/prefix results, inconsistent prefix phases, or a prefix that does not reproduce three times). No other terminal label is allowed.
    - A scoped fixture-owned secure-storage-factory guard captures the previous factory, installs the existing `MemorySecureStorage` factory at scenario entry, and restores the captured factory on all normal, fatal-assertion, and exception unwinds. Peer RAII cleanup releases all armed barriers, stops services in the existing order, joins its I/O thread before releasing PubSub/GlobalDB ownership, and records state both before and after each ownership transition.
    - The new diagnostic test and all existing scenarios continue to drive proposals/votes/certificates/Mint only by public `CreateProposal`/`SubmitProposal`, real `AddPeers`, real PubSub, CRDT durable readback, and registered Mint ingress.
  </behavior>
  <action>Per D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-09, and D-10, first write a failing GTest for a fixture-owned diagnostic/RAII helper, then implement only that test support in `multi_node_finality_fault_test.cpp`. Replace the repeated unscoped `SetSecureStorageFactory` calls with one scoped guard local to each test body (or a fixture setup/teardown guard with equivalent per-case construction), preserving the memory-storage behavior while restoring the exact prior factory at case exit. Instrument `StartPeer` around keypair acquisition, PubSub creation/start, `GlobalDB::New`, `db->Start`, I/O-thread launch, account creation, UTXO loading, Blockchain creation, TransactionManager creation, and Consensus acquisition; catch `std::exception` and unknown exceptions long enough to emit `P12_HARNESS_EXCEPTION` with the concrete phase, then rethrow/fail normally. Instrument `Peer::Stop`, `RestartPeer`, `StopNetwork`, and failed partial-start destruction before and after transactions, Blockchain/Consensus close, barrier release, I/O stop/join, PubSub stop, GlobalDB/datastore release, and factory restoration. State snapshots are diagnostic/read-only: record pointers/booleans, root existence, public host/listener/connectedness/topic state, and normal datastore accessibility, but never add a test-side protocol write. Add a named `HarnessLifecycleDiagnosticsCoverStartFailureStopAndFactoryRestoration` regression that starts/stops a component peer, exercises a deliberately harmless partial-start cleanup path if one can be constructed through existing constructors, and asserts the trace has the required start/stop/factory labels. Run the three fresh and three exact ordered-prefix processes per scenario, then write their complete deterministic matrix to the versioned `12-06-DIAGNOSIS.md`; do not leave the result only in `/tmp` logs. Apply the stated precedence rules and write exactly one full allowed `P12_HARNESS_CLASSIFICATION` terminal label per scenario. Do not catch-and-ignore failures, mutate production `GeniusAccount`, `Consensus`, `TransactionManager`, `GlobalDB`, or PubSub code, reuse fixed sleeps, raise any timeout, retry blindly, invoke handlers, write CRDT/active-vote data directly, force rounds/timers, or use mocks. Keep the existing focused active-vote `ACTIVE_VOTE_RED` diagnostic unchanged as a separate Phase-9 contract guard.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test consensus_pending_lifecycle_test --parallel 4 &amp;&amp; p12_diag='.planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md' &amp;&amp; p12_bin='build/OSX/Release/test_bin/multi_node_finality_fault_test' &amp;&amp; p12_late='FinalityFaultNetwork.LateContenderAndPassiveRecipientRemainReceiveOnly' &amp;&amp; p12_restart='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' &amp;&amp; p12_publisher='FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover' &amp;&amp; p12_prefix_late='FinalityFaultNetwork.ProductionRouteAuditUsesOnlyPubSubCrdtPersistenceAndMintIngress:FinalityFaultNetwork.SameBurnContentionUsesOneCanonicalSlotAndExactMint:FinalityFaultNetwork.LateContenderAndPassiveRecipientRemainReceiveOnly' &amp;&amp; p12_prefix_restart="${p12_prefix_late}:FinalityFaultNetwork.ActiveVoteRestartDiagnosticClassifiesLifecycleBoundary:${p12_restart}" &amp;&amp; p12_prefix_publisher="${p12_prefix_restart}:${p12_publisher}" &amp;&amp; for p12_case in late restart publisher; do eval "p12_filter=\${p12_${p12_case}}"; for p12_run in 1 2 3; do P12_HARNESS_RUN="fresh-${p12_case}-${p12_run}" "${p12_bin}" --gtest_filter="${p12_filter}" --gtest_brief=1 || true; done; eval "p12_filter=\${p12_prefix_${p12_case}}"; for p12_run in 1 2 3; do P12_HARNESS_RUN="exact-prefix-${p12_case}-${p12_run}" "${p12_bin}" --gtest_filter="${p12_filter}" --gtest_brief=1 || true; done; done &amp;&amp; "${p12_bin}" --gtest_filter='FinalityFaultNetwork.HarnessLifecycleDiagnosticsCoverStartFailureStopAndFactoryRestoration:FinalityFaultNetwork.ActiveVoteRestartDiagnosticClassifiesLifecycleBoundary' --gtest_brief=1 &amp;&amp; test -f "${p12_diag}" &amp;&amp; for p12_case in late restart publisher; do test "$(rg -c "^\\| ${p12_case} \\| (fresh|exact-prefix) \\| [123] \\|" "${p12_diag}")" -eq 6 || exit 1; test "$(rg -c "^P12_HARNESS_CLASSIFICATION classification=(prefix-contamination|fresh-production|pre-topology-failure|inconclusive) scenario=${p12_case} terminal_outcome=(prefix-contamination|fresh-production|pre-topology-failure|inconclusive) failure_phase=.+ fresh_trace=.+ topology_evidence=.+$" "${p12_diag}")" -eq 1 || exit 1; done &amp;&amp; test "$(rg -c '^P12_HARNESS_CLASSIFICATION classification=' "${p12_diag}")" -eq 3 &amp;&amp; ! rg -n 'classification=(not-reproduced|unknown|pass)|terminal_outcome=(not-reproduced|unknown|pass)' "${p12_diag}" &amp;&amp; ! rg -n 'ConsensusPendingLifecycleTestAccess::(Handle|CertificateReceived|WriteLiveCertificate|Force)|\bHandle[A-Za-z]*\(|\bOnConsensusMessage\(|\bCertificateReceived\(|\bOnConsensusCertificate\(|\bGlobalDB::Put|\bPutConvergentImmutable\(|sleep_for|sleep_until|Force[A-Za-z]*(Timer|Due|Round)|mock' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
  </verify>
  <done>`12-06-DIAGNOSIS.md` is a retained, versioned six-run matrix for each red scenario, with an exact filter, topology validity, failure phase, trace, and one allowed terminal label per scenario; the automated command validates this artifact rather than temporary logs. The focused lifecycle/factory diagnostic and Phase-9 active-vote diagnostic pass. Every failed invocation names its concrete startup/teardown/exception/topology/RocksDB/thread/factory state instead of only reporting a timed-out predicate or unknown exception. No production protocol behavior, timeout value, original scenario assertion, or prohibited route changes in this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair only all-prefix contamination; otherwise block Phase 12 with an explicit 12-07 handoff</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md, .planning/phases/12-multi-node-finality-fault-proof/12-07-HANDOFF.md</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp — use Task 1's diagnostic trace, existing RAII `Peer` lifecycle, and the exact original predicates at lines around 520 and 968; retain all scenario public ingress and durable assertions.
    - test/src/blockchain/CMakeLists.txt — the normal target already has `TIMEOUT 300` and `RUN_SERIAL TRUE`; do not modify this registration, increase this timeout, or weaken the single full-target proof.
    - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md — TEST-02/03, TEST-04, and TEST-05 are blocked only by the observed serial target failures; TEST-01 and TEST-06 are passing regressions to preserve.
    - .planning/phases/12-multi-node-finality-fault-proof/12-REVIEW-FIX.md — prior RAII cleanup, readiness barriers, and serial CTest registration were deliberate safeguards; evolve them only with Task 1 evidence.
    - .planning/phases/09-durable-one-vote-finality/09-CONTEXT.md — retain one direct local exact-vote record per canonical slot, exact replay only, canonical slot semantics, and release only after durable same-slot accepted-certificate processing.
  </read_first>
  <behavior>
    - A fixture-only repair is permitted only when every scenario's retained terminal label is `prefix-contamination`: all three fresh runs passed and all three corresponding exact-prefix runs reproduced the same failed phase. The only eligible edits are fixture-owned cleanup/isolation/resource ownership identified by `P12_HARNESS_STATE`/`P12_HARNESS_EXCEPTION`: guaranteed barrier release, shutdown on all partial-start/fatal paths, I/O-thread join-before-dependent-release, exact global-factory restoration, and intentional restart-root retention.
    - The fixture-only repair passes three repeated fresh runs and three corresponding exact-prefix runs for every previously red scenario, then one unchanged normal serial `multi_node_finality_fault_test` CTest invocation containing all named scenarios.
    - If any scenario's terminal label is `fresh-production`, `pre-topology-failure`, or `inconclusive`, make no repair, tuning, retry policy, CMake registration change, assertion change, or production edit. Write `12-07-HANDOFF.md` and stop Phase 12 blocked. Each `fresh-production` handoff entry includes its scenario, exact failed phase, all fresh trace paths, valid-topology evidence, and the preserved Phase-9/Phase-12 invariants; `pre-topology-failure` and `inconclusive` entries state why no production-fix authorization exists.
  </behavior>
  <action>Use Task 1's retained matrix as a hard, per-scenario branch, not a heuristic. Only if all three terminal labels are `prefix-contamination`, write a failing ordered-prefix regression first and make the smallest fixture-only GREEN edit at the evidenced ownership boundary: ensure every `StartPeer` early return is RAII-cleaned; make barrier release unconditional before service stop; complete transaction/Blockchain/Consensus close and I/O stop/join before PubSub/GlobalDB destruction; preserve/reuse the same RocksDB root only for the intended restart; and restore the exact saved secure-storage factory after each case. Do not modify `CMakeLists.txt`; retain the existing normal serial target unchanged. Rerun the exact original assertions—late contender has no second usable vote/certificate and passive write attempts stay zero; complete restart preserves original vote plus exact one winner/no duplicate Mint; publisher loss proves persistence-before-notification and CRDT-authoritative cleanup without successor certificate re-advertisement. If any terminal label is `fresh-production`, `pre-topology-failure`, or `inconclusive`, stop without editing any fixture or production behavior. Create the versioned `12-07-HANDOFF.md` with a top-level `P12_HARNESS_CLASSIFICATION` entry for every non-prefix scenario. Each `fresh-production` entry is one complete line containing scenario, exact failed phase, all three fresh trace paths, valid-listener/peer-count/RocksDB/thread/factory evidence, and the invariant list `public-CreateProposal-SubmitProposal,real-AddPeers-PubSub,CRDT-durable-certificate-authority,registered-Mint-ingress,Phase-9-exact-local-vote-per-canonical-slot,no-successor-certificate-readvertisement`. State `PHASE_12_STATUS=BLOCKED` and that no 12-07 production edit is authorized until a separately scoped minimal TDD `12-07-PLAN.md` consumes this handoff. For `pre-topology-failure`/`inconclusive`, record the precise missing or mixed evidence and explicitly prohibit both fixture and production repair until a follow-up diagnosis plan is approved. Do not add sleeps, retry loops, relaxed predicates, direct receive/author handlers, direct CRDT or active-vote writes, forced timers/rounds, mocks, a new finality record, an assertion bypass, a CMake change, or a production source edit.</action>
  <verify>
    <automated>p12_diag='.planning/phases/12-multi-node-finality-fault-proof/12-06-DIAGNOSIS.md' &amp;&amp; p12_handoff='.planning/phases/12-multi-node-finality-fault-proof/12-07-HANDOFF.md' &amp;&amp; p12_labels="$(rg -o 'classification=(prefix-contamination|fresh-production|pre-topology-failure|inconclusive)' "${p12_diag}" | sort -u)" &amp;&amp; if test "${p12_labels}" = 'classification=prefix-contamination'; then cmake --build build/OSX/Release --target multi_node_finality_fault_test consensus_pending_lifecycle_test consensus_slot_key_test --parallel 4 &amp;&amp; p12_bin='build/OSX/Release/test_bin/multi_node_finality_fault_test' &amp;&amp; p12_late='FinalityFaultNetwork.LateContenderAndPassiveRecipientRemainReceiveOnly' &amp;&amp; p12_restart='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' &amp;&amp; p12_publisher='FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover' &amp;&amp; p12_prefix_late='FinalityFaultNetwork.ProductionRouteAuditUsesOnlyPubSubCrdtPersistenceAndMintIngress:FinalityFaultNetwork.SameBurnContentionUsesOneCanonicalSlotAndExactMint:FinalityFaultNetwork.LateContenderAndPassiveRecipientRemainReceiveOnly' &amp;&amp; p12_prefix_restart="${p12_prefix_late}:FinalityFaultNetwork.ActiveVoteRestartDiagnosticClassifiesLifecycleBoundary:${p12_restart}" &amp;&amp; p12_prefix_publisher="${p12_prefix_restart}:${p12_publisher}" &amp;&amp; for p12_case in late restart publisher; do eval "p12_filter=\${p12_${p12_case}}"; for p12_run in 1 2 3; do "${p12_bin}" --gtest_filter="${p12_filter}" --gtest_brief=1 || exit 1; done; eval "p12_filter=\${p12_prefix_${p12_case}}"; for p12_run in 1 2 3; do "${p12_bin}" --gtest_filter="${p12_filter}" --gtest_brief=1 || exit 1; done; done &amp;&amp; ctest --test-dir build/OSX/Release --output-on-failure --timeout 300 -R '^multi_node_finality_fault_test$' &amp;&amp; build/OSX/Release/test_bin/consensus_pending_lifecycle_test --gtest_filter='*ActiveVote*:*Restart*:*Certificate*' &amp;&amp; build/OSX/Release/test_bin/consensus_slot_key_test --gtest_brief=1 &amp;&amp; ! rg -n 'ConsensusPendingLifecycleTestAccess::(Handle|CertificateReceived|WriteLiveCertificate|Force)|\bHandle[A-Za-z]*\(|\bOnConsensusMessage\(|\bCertificateReceived\(|\bOnConsensusCertificate\(|\bGlobalDB::Put|\bPutConvergentImmutable\(|sleep_for|sleep_until|Force[A-Za-z]*(Timer|Due|Round)|mock' test/src/blockchain/multi_node_finality_fault_test.cpp; else test -f "${p12_handoff}" &amp;&amp; test -z "$(git diff --name-only HEAD -- src)" &amp;&amp; rg -n '^PHASE_12_STATUS=BLOCKED$' "${p12_handoff}" &amp;&amp; for p12_case in late restart publisher; do if rg -q "^P12_HARNESS_CLASSIFICATION classification=fresh-production scenario=${p12_case} " "${p12_diag}"; then rg -n "^P12_HARNESS_CLASSIFICATION classification=fresh-production scenario=${p12_case} terminal_outcome=fresh-production failure_phase=.+ fresh_trace=.+ fresh_trace=.+ fresh_trace=.+ topology_evidence=valid-.+ invariants=public-CreateProposal-SubmitProposal,real-AddPeers-PubSub,CRDT-durable-certificate-authority,registered-Mint-ingress,Phase-9-exact-local-vote-per-canonical-slot,no-successor-certificate-readvertisement$" "${p12_handoff}" || exit 1; elif rg -q "^P12_HARNESS_CLASSIFICATION classification=(pre-topology-failure|inconclusive) scenario=${p12_case} " "${p12_diag}"; then rg -n "^P12_HARNESS_CLASSIFICATION classification=(pre-topology-failure|inconclusive) scenario=${p12_case} .+ repair_authorization=none$" "${p12_handoff}" || exit 1; fi; done &amp;&amp; printf 'P12_HARNESS_STOP phase=12 reason=non-prefix-terminal-outcome\n' &gt;&amp;2 &amp;&amp; exit 42; fi</automated>
  </verify>
  <done>If and only if all three terminal labels are `prefix-contamination`, the fixture-only repair makes every focused fresh and exact-prefix repeat pass, then the unchanged normal serial real-socket target passes all scenarios with TEST-01/06 preserved and TEST-02–05 complete. If any scenario is `fresh-production`, `pre-topology-failure`, or `inconclusive`, Phase 12 stops blocked with `12-07-HANDOFF.md`; for `fresh-production` that handoff contains the scenario, exact phase, fresh traces, valid-topology evidence, and every preserved invariant, while the other outcomes explicitly prohibit speculative repair. In neither branch are original assertions weakened or protocol safeguards bypassed.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Required behavior | Plan | Status | Notes |
|--------|----|-------------------|------|--------|-------|
| GOAL | Phase 12 | Production-path finality remains safe and live through contention, propagation disorder, publisher loss, and restart. | 12-06 Tasks 1–2 | COVERED | The retained matrix proves whether each red suite failure is harness lifetime or real protocol behavior; fixture repair is allowed only when every label is prefix-contamination. |
| REQ | TEST-01 | Same-burn contention remains one canonical slot/certificate/winner. | 12-06 Task 2 | COVERED | Preserved in every ordered-prefix and final serial invocation. |
| REQ | TEST-02 | A late contender cannot obtain a second usable vote/certificate. | 12-06 Tasks 1–2 | COVERED | Fresh and audit→contention→late repetitions retain all original no-second-vote/certificate assertions. |
| REQ | TEST-03 | Recipient remains receive-only and avoids self-CID stall. | 12-06 Tasks 1–2 | COVERED | Late/passive diagnostic captures receiver topology/readback state; zero authority writes stays required. |
| REQ | TEST-04 | Three restart boundaries preserve vote and prevent duplicate Mint. | 12-06 Tasks 1–2 | COVERED | Fresh and ordered-prefix restart runs distinguish constructor/teardown contamination from an individual durable-recovery defect. |
| REQ | TEST-05 | Publisher loss proves persistence-before-advertisement and non-conflicting recovery. | 12-06 Tasks 1–2 | COVERED | Fresh and full-prefix runs capture topology readiness and retain CRDT-authoritative/no-successor-advertisement assertions. |
| REQ | TEST-06 | Real PubSub, CRDT, RocksDB, and Mint ingress only. | 12-06 Tasks 1–2 | COVERED | Static source gate and original production-route audit remain mandatory. |
| RESEARCH | — | Event-driven bounded predicates; no sleeps, mock transport, or direct ingress. | 12-06 Tasks 1–2 | COVERED | Diagnostics observe public/runtime state and use existing bounded waits only. |
| RESEARCH | — | Harness owns four real peers, independent roots, and stop/recreate lifecycle. | 12-06 Tasks 1–2 | COVERED | Per-peer lifecycle state is the diagnostic subject; roots remain unchanged for intentional restart. |
| CONTEXT | D-01, D-02 | Dedicated four-peer harness with independent persistent RocksDB roots. | 12-06 Tasks 1–2 | COVERED | No substitute fixture or fresh-root restart is permitted. |
| CONTEXT | D-03, D-04 | Normal ingress plus real connectivity/lifecycle fault control. | 12-06 Tasks 1–2 | COVERED | Test changes may observe, start, stop, and connect only. |
| CONTEXT | D-05, D-06, D-07 | Durable-boundary barriers and read-only instrumentation. | 12-06 Tasks 1–2 | COVERED | Teardown diagnostics release barriers; observers cannot influence finality. |
| CONTEXT | D-08, D-09, D-10 | Named enabled bounded scenarios and per-node durable proof. | 12-06 Tasks 1–2 | COVERED | Existing CTest remains enabled/bounded; no timeout increase, assertion relaxation, or ephemeral-only proof. |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-case lifetime → process-global secure-storage factory | A prior case can influence a later peer constructor unless the exact prior factory is restored. |
| peer shutdown → PubSub/GlobalDB/RocksDB/I/O thread | Incorrect ownership order can leave listeners, locks, I/O work, or datastore handles alive into a later case. |
| diagnostic fixture → consensus/CRDT/Mint route | Diagnostic code must observe state without becoming a protocol author, delivery path, timer authority, or datastore writer. |
| fresh-process red result → production finality code | A real production failure must not be hidden by test-only retries, timing changes, or weaker assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17 | T/D | `Peer::StartPeer` / `Peer::Stop` resource lifecycle | mitigate | State/exception labels record each acquisition/release; scoped cleanup releases barriers, joins I/O before dependent teardown, and retains only intentional restart roots. |
| T-12-18 | T/E | `GeniusAccount` secure-storage factory | mitigate | Fixture-owned guard captures/restores the existing factory on normal and exceptional paths; diagnostics prove installed/restored transitions without production API changes. |
| T-12-19 | R/D | suite reliability classification | mitigate | The committed deterministic six-run matrix per scenario produces exactly one allowed terminal label; only all-prefix contamination can authorize fixture repair, and no repeat is treated as a fix. |
| T-12-20 | T/E | finality test driver | mitigate | Preserve public PubSub/CRDT/Mint ingress and static-ban handlers, direct CRDT writes, forced timers, mocks, and sleeps. |
| T-12-21 | T | Phase-9 active vote and Phase-10/11 certificate recovery | mitigate | Production edits are forbidden in this plan unless a later separately planned fresh-production fix exists; the active-vote and slot-key regressions remain mandatory. |
| T-12-SC | Tampering | C++ test/CMake changes | mitigate | No package install; RED/GREEN diagnostics, repeated real-socket evidence, source gate, and normal serial CTest protect the contract. |
</threat_model>

<verification>
Task 1 produces and validates the committed executable diagnostic matrix first: three separate fresh processes and three exact ordered-prefix processes for each of late/passive, complete restart, and publisher loss. Each scenario has exactly one terminal outcome: `pre-topology-failure` for a fresh start/constructor/before-topology failure; otherwise `fresh-production` for a valid-topology fresh failure; otherwise `prefix-contamination` only when all fresh runs pass and all exact-prefix runs reproduce the same phase; otherwise `inconclusive`. Task 2 may make a fixture-only repair only when all scenario outcomes are `prefix-contamination`, then must pass the same six repetitions per scenario plus the unchanged normal complete serial CTest. Any other outcome stops Phase 12 blocked with a 12-07 handoff; it never authorizes a timeout increase, fixture workaround, speculative retry/tuning, or an unplanned production edit.
</verification>

<success_criteria>
1. The versioned `12-06-DIAGNOSIS.md` contains the six-run matrix and exactly one allowed full `P12_HARNESS_CLASSIFICATION` terminal label for each originally red scenario, with concrete startup/exception/listener/RocksDB/thread/factory lifecycle evidence.
2. A sequence-dependent defect is fixed only in fixture ownership/isolation when, and only when, every scenario is classified `prefix-contamination`; all three fresh and all three matching exact-prefix invocations pass before the unchanged normal serial CTest is accepted.
3. TEST-02, TEST-03, TEST-04, and TEST-05 pass together in the normal serial real-socket target, while TEST-01 and TEST-06 remain passing production-route regressions, only on the all-prefix-contamination branch.
4. No timeout is raised; no sleep, blind retry, mock, forced timer, direct handler/CRDT write, successor certificate re-advertisement, assertion weakening, CMake registration change, or protocol shortcut is introduced.
5. Any `fresh-production`, `pre-topology-failure`, or `inconclusive` result blocks Phase 12 and emits a versioned 12-07 handoff. Every `fresh-production` entry has scenario, exact failed phase, fresh trace, valid-topology evidence, and preserved invariants; no repair is attempted.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-06-SUMMARY.md` when the all-prefix-contamination branch completes. If any Task 1 terminal classification is `fresh-production`, `pre-topology-failure`, or `inconclusive`, create `12-07-HANDOFF.md`, create no fixture/production behavior change, and stop Phase 12 blocked. A subsequent `12-07-PLAN.md` is permitted only for an evidence-complete `fresh-production` handoff; the other outcomes require a diagnosis follow-up rather than speculative repair.
</output>
