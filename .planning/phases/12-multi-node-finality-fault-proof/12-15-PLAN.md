---
phase: 12-multi-node-finality-fault-proof
plan: 15
type: execute
wave: 15
depends_on: ["12-14"]
files_modified:
  - src/account/TransactionManager.cpp
  - src/blockchain/Consensus.cpp
  - test/src/blockchain/multi_node_finality_fault_test.cpp
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-04]

must_haves:
  truths:
    - "Every intermittent full-suite failure signature (SameBurn marker, RestartAtVote block-3 reconvergence, PublisherLoss child-readiness) has a written attribution verdict backed by preserved-log line citations — no signature left as 'undiagnosed ambient flakiness'"
    - "The SameBurn wait predicate covers the exact durable boundary it asserts (bridge marker present on all four peers), eliminating the confirmed check-then-act gap between multi_node_finality_fault_test.cpp:2088-2094 and the :2098 assertion"
    - "TransactionManager::Stop() and ConsensusManager::Close() notify each condition variable while holding its paired mutex, so a barrier waiter can never miss the shutdown wakeup (12-REVIEW.md WR-02 contract violation closed)"
    - "Focused SameBurnContention and RestartAtVote runs pass three consecutive times each on the stabilized build; one full serial run is executed with its outcome preserved honestly (no rerolls)"
  artifacts:
    - path: "src/account/TransactionManager.cpp"
      provides: "Stop() notifies fault_test_cv_ under fault_test_mutex_ and cv_ under cv_mutex_, each in a tight scope"
      contains: "fault_test_mutex_"
    - path: "src/blockchain/Consensus.cpp"
      provides: "Close() notifies timer_cv_ under timer_mutex_ and fault_test_cv_ under fault_test_mutex_, tight scopes before the join section"
      contains: "timer_mutex_"
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "SameBurn first convergence wait includes HasBridgeMarker for all four peers, matching the existing pattern at :2111-2112"
      contains: "HasBridgeMarker"
  key_links:
    - from: "src/account/TransactionManager.cpp (Stop)"
      to: "fault_test_cv_ / cv_"
      via: "std::lock_guard on fault_test_mutex_ / cv_mutex_ around each notify_all"
      pattern: "lock_guard\\s+\\w+\\(\\s*fault_test_mutex_\\s*\\)"
    - from: "src/blockchain/Consensus.cpp (Close)"
      to: "timer_cv_ / fault_test_cv_"
      via: "std::lock_guard on timer_mutex_ / fault_test_mutex_ around each notify_all, before the close_mutex_ section"
      pattern: "lock_guard\\s+\\w+\\(\\s*timer_mutex_\\s*\\)"
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp (SameBurn first wait)"
      to: "PersistBridgeExecutedMarker durable state"
      via: "HasBridgeMarker( first/second/third/passive, *winner ) terms inside the ASSERT_WAIT_FOR_CONDITION at :2088-2094"
      pattern: "HasBridgeMarker\\( passive, \\*winner \\)"
---

<objective>
Close VERIFICATION gaps 1 and 2 (12-VERIFICATION.md frontmatter, status: partial/partial): the three-consecutive-serial-pass evidence standard is unmet because the full multi_node_finality_fault_test suite fails ~40-60% per run from three intermittent signatures that postdate review fixes 2b1a8e47..caf34458 — SameBurnContention HasBridgeMarker (:2081/:2098), RestartAtVote block-3 certificate reconvergence (:2471-2476), and PublisherLoss collector child-readiness (:1871-1877).

Purpose: Phase 12 is the final v3.0 phase; a regression suite that fails ~60% of the time cannot distinguish a real finality regression from ambient flake, so the phase goal ( trustworthy production-path regression proof ) is not deliverable without stabilizing this suite. The round is funded by the developer's explicit option-(a) decision on the 12-VERIFICATION escalation gate.

Output: A per-signature attribution record built from the 11 preserved evidence logs; the two authorized stabilization fixes (SameBurn wait-predicate closure, WR-02 notify-under-paired-mutex in Stop()/Close()); focused and one-full-run verification with honestly preserved outcomes. The formal three-consecutive-pass gate runs afterwards in plan 12-17 on the final build (after 12-16's teardown propagation lands).

Planner's diagnostic groundwork (verified against the logs and source during planning — the executor must re-confirm, not re-derive):
1. SameBurn signature is a confirmed check-then-act gap: in /tmp/p12_14_full_c.log the failing peer is 4b514df6; its winner confirmation "ChangeTransactionState: Tracking entry confirmed tx=2f42ca23..." appears ~25 log lines AFTER the :2098 assertion block (assertion window-line 410, confirmation window-line 435, both at 13:40:42) — the wait at :2088-2094 requires MintEffects==1 (incremented at TransactionManager.cpp:5518, BEFORE the marker write at :5532) and the passive certificate, but NOT the bridge marker, so the test can assert :2098 before the marker write lands. The in-file pattern at :2104-2113 (the post-restart wait) already includes HasBridgeMarker for all four peers — the first wait's omission is an inconsistency, not a design choice.
2. RestartAtVote block-3 signature (CheckCertificateForSlot false on three peers per the :1260 dump, 25s timeout) is NOT yet attributed: canonical C's GlobalDB open/shutdown timeline (blocks 1-2 restarts at 13:41:24-34) shows prompt 1-second teardowns, so the WR-02 parked-barrier-hold hypothesis (missed wakeup keeps the old peer's TransactionManager/GlobalDB alive up to 30s across RestartPeer's same-root reopen) is neither confirmed nor excluded for the block-3 restart specifically — Task 1 builds that timeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md
@.planning/phases/12-multi-node-finality-fault-proof/deferred-items.md
@.planning/phases/12-multi-node-finality-fault-proof/12-14-SUMMARY.md

<interfaces>
<!-- Current code the executor will modify. Read each file for full context. -->

1. src/account/TransactionManager.cpp:314-323 — current Stop() (the WR-02 defect):
   void TransactionManager::Stop()
   {
       if ( stopped_.exchange( true ) ) { return; }
       cv_.notify_all();
       fault_test_cv_.notify_all();
   }
   Paired mutexes (TransactionManager.hpp:574-584): fault_test_mutex_ guards fault_test_cv_;
   cv_mutex_ guards cv_. Barrier waiter: EnterFinalityFaultBarrier (TransactionManager.cpp:2111-2126)
   takes fault_test_mutex_ and waits on fault_test_cv_ up to 30s with predicate
   `released || !armed || stopped_.load()` — a waiter between predicate evaluation and CV
   block misses an unlocked notify and parks the full 30s. The parked certificate-handler
   thread holds the last strong TransactionManager reference via the weak_ptr lock at
   TransactionManager.cpp:128-149, keeping globaldb_m (the old peer's GlobalDB on the same
   RocksDB root) alive across RestartPeer.

2. src/blockchain/Consensus.cpp:155-181 — current Close() head (the WR-02 defect):
   void ConsensusManager::Close()
   {
       stop_timer_.store( true );
       timer_cv_.notify_all();
       fault_test_cv_.notify_all();
       std::thread timer;
       { std::lock_guard lock( close_mutex_ ); ... timer = std::move( round_timer_ ); }
       timer.join();
   }
   Paired mutexes (Consensus.hpp:1000-1022): fault_test_mutex_ with fault_test_cv_;
   timer_mutex_ with timer_cv_. Barrier waiter: ConsensusManager::EnterFinalityFaultBarrier
   (Consensus.cpp:1343-1356), same 30s class. Deadlock safety verified during planning: the
   round-timer thread (Consensus.cpp:192-254) calls Close() only via the self-teardown path
   AFTER unlocking timer_mutex_ at :231 (its hook-access lock at :235-237 is scoped), so tight
   notify scopes in Close() cannot self-deadlock; fault_test_mutex_ is only taken by Publish's
   counter (:277) and friend test accessors, none of which call Close()/Stop().

3. test/src/blockchain/multi_node_finality_fault_test.cpp:2088-2094 — SameBurn first wait
   (checks CheckCertificateForSlot on passive + MintEffects==1 on all four peers; omits
   HasBridgeMarker) followed by the :2095-2099 loop asserting HasOnlyWinnerOutput and
   HasBridgeMarker. Winner is already determined at :2076-2078. The correct in-file pattern
   to mirror is the second wait at :2104-2113 (all four CheckCertificateForSlot +
   HasOnlyWinnerOutput + HasBridgeMarker terms).

4. Marker key contract (both sides verified): PersistBridgeExecutedMarker writes
   "/bridge/executed/" + GetChainId() + ":" + dag_st.uncle_hash()
   (TransactionManager.cpp:2101-2107, constants at TransactionManager.hpp:599-600);
   HasBridgeMarker (multi_node_finality_fault_test.cpp:1229-1234) reads the identical key
   from peer.db — a confirmed marker is always observable through HasBridgeMarker.

5. Preserved evidence logs (all serial full runs, verified present):
   - Failing: /tmp/p12_14_full_c.log (SameBurn :2098 + RestartAtVote :2476),
     /tmp/p12_14_full_a_rerun_failed.log (RestartAtVote),
     /tmp/p12_14_full_b_load_failed.log, /tmp/p12_14_full_b_load_contaminated.log (load 19 — context only),
     /tmp/p12_14_ab_neworder_4.log (RestartAtVote),
     /tmp/p12_14_ab_oldorder_{1,2,3}.log (pre-fix control: 2 assertion fails + 2 SIGSEGV)
   - Passing: /tmp/p12_14_full_a.log, /tmp/p12_14_full_b.log, /tmp/p12_14_ab_neworder_5.log (ctest summaries only)
   - Attribution ledger per signature: deferred-items.md (SameBurn struck 12:27/12:47/13:42;
     RestartAtVote struck a_rerun/b_load x2/neworder_4/canonical C; child-readiness 3/11 runs)

6. Useful log anchors (verified in canonical C): GlobalDB "Opening database <path>",
   "~GlobalDB CALLED with count N on <path>", "GlobalDB shutdown start/finished",
   "~TransactionManager: Metrics" (one line per manager destruction, timestamped),
   "Tracking entry confirmed tx=", "Standalone validator confirmed tx",
   "did not consume every burn input" (WR-07 warning), "Timed out waiting for condition".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attribute every failure signature from the preserved evidence (no code changes)</name>
  <files>(no file modifications — forensic analysis recorded in the SUMMARY)</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/deferred-items.md (attribution ledger: which run struck which signature)
  - .planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md (WR-02 mechanism statement)
  - test/src/blockchain/multi_node_finality_fault_test.cpp (lines 1229-1273 helpers, 2040-2114 SameBurn, 2440-2492 RestartAtVote, 1861-1890 collector test)
  - src/account/TransactionManager.cpp (lines 128-155 weak_ptr handler, 314-330 Stop, 2111-2126 barrier, 5487-5550 confirm path)
  </read_first>
  <action>
  Build a written attribution verdict for each of the three signatures from the preserved failing logs. Use targeted grep/awk extraction per test window (delimit with the "[ RUN      ]" and "[  FAILED  ]"/"[       OK ]" lines), never full-log reads (logs are ~860KB).

  A. SameBurn marker signature (failing runs: /tmp/p12_14_full_c.log 13:40:42, /tmp/p12_14_full_b_load_failed.log 12:27-12:30, /tmp/p12_14_ab_oldorder_*.log whichever struck at 12:45-12:47). For EACH failing instance:
     1. Identify the failing peer address prefix from the gtest failure dump context and the surrounding log slice.
     2. Establish the relative order of (a) the ":2098 Failure" block and (b) that peer's "Tracking entry confirmed tx=<winner>" or "Standalone validator confirmed tx=<winner>" line, plus any intervening "did not consume every burn input" warning.
     3. Confirm or refute the check-then-act verdict: the wait at :2088-2094 passed (MintEffects==1 on all four peers) while the winner's PersistBridgeExecutedMarker had not yet landed on the failing peer; the confirmation lands within the same second, after the assertion.
     4. Also check the second failing assertion variant ":2081" noted in deferred-items if present in any log.
     Authorization gate for Task 2's predicate fix: the check-then-act ordering must be confirmed in at least 2 independent failing runs. (Planner already confirmed it in canonical C; one more instance suffices.)

  B. RestartAtVote block-3 signature (failing runs: canonical C 13:42:53, /tmp/p12_14_full_a_rerun_failed.log, /tmp/p12_14_full_b_load_failed.log, /tmp/p12_14_ab_neworder_4.log; passing references: canonical A/B logs are summary-only — use the green focused runs' behavior as the contrast). For EACH failing run:
     1. Locate the block-3 restart window: the last "Opening database .../restart-mint-validator-one/rocksdb" (or the test's "restart-mint" root names) pair for peer `first` before the "Timed out waiting for condition: recreated Mint peer repaired its marker" line at :2476.
     2. Extract the teardown timeline of the OLD peer-one instance: its "~GlobalDB CALLED"/"shutdown finished" lines and its "~TransactionManager: Metrics" line, with timestamps, relative to the new instance's "Opening database" on the same root.
     3. Score the WR-02 parked-hold hypothesis: POSITIVE if the old peer's TransactionManager metrics line or GlobalDB shutdown lands more than ~5 seconds after the new "Opening database" on the same root (missed wakeup parks the barrier waiter up to 30s); NEGATIVE if teardown completes within ~1-2s (as blocks 1-2 restarts do in canonical C at 13:41:24-34).
     4. For the three peers reported false at :1260 (CheckCertificateForSlot dump): extract their post-restart "/cert/" CRDTCallbackManager callback lines and "Received certificate"/consensus ingress logs — did the certificate for the slot EVER arrive on those peers in the failing run, and how late?
     5. Record the measured convergence latency distribution in passing full runs if extractable (green runs are summary-only — if nothing is extractable, state that and rely on the focused-run contrast).
     Verdict required: either WR-02-parked-hold is evidenced in >= 2 failing runs, or it is excluded and the residual mechanism is characterized (cert re-publication latency after restart / load sensitivity), each with log citations.

  C. PublisherLoss child-readiness signature (3 strikes across /tmp/p12_14_ab_oldorder_{1,2,3}.log, /tmp/p12_14_ab_neworder_{4,5}.log, canonical series — identify which from the gtest FAILED lines for RealSocketPublisherLossOnlyQualifiesWhenTwoRunsMatch). Characterize only: extract the child readiness failure detail (:1871-1877 vicinity) from each strike and confirm whether all strikes share the same first-failing readiness predicate. Per the standing 12-08 discipline (STATE.md) and D-18/D-19/D-25, this signature's intermittence stands unless two fully attributed fresh runs fail at the same first readiness boundary — do NOT repair it in this plan; record the characterization for the evidence gate's risk assessment in 12-17.

  D. Write the attribution verdicts (per signature: mechanism, evidence citations with log filename + timestamp/line, confidence, repair authorization decision) into the 12-15-SUMMARY.md Findings section at plan end. If any verdict contradicts the two authorized repairs below (for example: SameBurn's marker NEVER lands in some failing run, meaning a real durability defect rather than a race), STOP — do not apply the contradicted repair; record the contradiction and report back in the SUMMARY.
  </action>
  <verify>
    <automated>grep -c "RUN.*SameBurnContention\|RUN.*RestartAtVote" /tmp/p12_14_full_c.log /tmp/p12_14_full_a_rerun_failed.log /tmp/p12_14_full_b_load_failed.log /tmp/p12_14_ab_neworder_4.log</automated>
  </verify>
  <acceptance_criteria>
  - SUMMARY contains a "Signature attribution" section with three named verdicts (SameBurn / RestartAtVote / child-readiness), each citing at least one preserved log filename plus a timestamp or log-line number
  - SameBurn verdict states, for at least 2 failing runs, the measured relative order of the :2098 failure block vs the failing peer's winner-confirmation line
  - RestartAtVote verdict states the old-peer teardown latency relative to the same-root reopen for at least 2 failing runs, and names the peers lacking the certificate with their last observed cert-ingress timestamp
  - No source file was modified during this task (git status shows only .planning changes or nothing)
  </acceptance_criteria>
  <done>Every signature has an evidence-backed attribution verdict; the two Task-2 repairs are each explicitly authorized or explicitly withheld with reasons; contradictions (if any) are recorded instead of repaired.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the authorized stabilizations — SameBurn wait-predicate closure and WR-02 notify-under-paired-mutex</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, src/account/TransactionManager.cpp, src/blockchain/Consensus.cpp</files>
  <read_first>
  - test/src/blockchain/multi_node_finality_fault_test.cpp (lines 2085-2114: both SameBurn waits, to mirror the existing :2104-2113 pattern)
  - src/account/TransactionManager.cpp (lines 314-330 current Stop; lines 2111-2126 the barrier it must wake; header TransactionManager.hpp lines 570-590 for the mutex/CV pairing)
  - src/blockchain/Consensus.cpp (lines 149-181 current Close; lines 183-254 the round-timer thread that proves tight-scope safety)
  - .planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md (WR-02 and its exact prescribed fix shape)
  </read_first>
  <action>
  Apply exactly the repairs Task 1 authorized. If Task 1 withheld one, skip it and record why.

  REPAIR 1 (authorized iff Task 1 confirmed the check-then-act ordering in >= 2 failing runs) — same file, same pattern as its sibling:
  In test/src/blockchain/multi_node_finality_fault_test.cpp, extend the ASSERT_WAIT_FOR_CONDITION predicate at :2088-2094 (SameBurnContention, "passive peer received and recovered the exact canonical winner") by appending four terms: HasBridgeMarker( first, *winner ) && HasBridgeMarker( second, *winner ) && HasBridgeMarker( third, *winner ) && HasBridgeMarker( passive, *winner ) — mirroring lines :2111-2112 exactly. Keep the existing MintEffects==1 and passive CheckCertificateForSlot terms and the 20-second bound unchanged. Change nothing else in the test.

  REPAIR 2 (WR-02, authorized iff Task 1 found the parked-hold signature in >= 2 failing runs OR excluded all competing mechanisms while confirming the source-level contract violation):
  a) src/account/TransactionManager.cpp Stop() (:314-323): after the stopped_.exchange(true) idempotence guard, replace the two bare notifies with two individually scoped lock-guarded notifies — a std::lock_guard on fault_test_mutex_ around fault_test_cv_.notify_all(), and a separate std::lock_guard on cv_mutex_ around cv_.notify_all(). Each lock_guard must open and close inside its own braced scope; the two scopes must not nest each other, must not be held simultaneously, and must not wrap anything except the single notify call. No other statement of Stop() changes.
  b) src/blockchain/Consensus.cpp Close() (:155-159): replace the two bare notifies with two individually scoped lock-guarded notifies — std::lock_guard on timer_mutex_ around timer_cv_.notify_all(), and std::lock_guard on fault_test_mutex_ around fault_test_cv_.notify_all() — both completing BEFORE the existing `std::thread timer;` / close_mutex_ section. Never hold either mutex across the round_timer_ join. No other statement of Close() changes; the self-teardown detach path (:169-175) stays untouched.
  Rationale to carry into the commit message: a CV waiter that evaluated its predicate but has not yet blocked loses an unlocked notify (missed wakeup); holding the paired mutex during store+notify serializes them with the waiter's predicate check. The 30-second barrier waiters are EnterFinalityFaultBarrier in both classes.

  Do NOT: alter any timeout, add retries, change the barrier wait durations, modify EnterFinalityFaultBarrier, touch the mint retry path (TransactionManager.cpp:5487-5550), or make any RestartAtVote test change — if Task 1 attributed RestartAtVote to something other than the parked hold and WR-02, record it and leave the suite alone; a wait-bound change requires its own evidence-backed decision, not this plan.
  </action>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && grep -n "HasBridgeMarker( passive, \*winner )" test/src/blockchain/multi_node_finality_fault_test.cpp | head -3 && sed -n '314,330p' src/account/TransactionManager.cpp && sed -n '155,168p' src/blockchain/Consensus.cpp && cmake --build build/OSX/Release --target multi_node_finality_fault_test consensus_pending_lifecycle_test transaction_manager_certificate_fallback_test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - The SameBurn first wait predicate contains four HasBridgeMarker terms including HasBridgeMarker( passive, *winner ); its timeout is still std::chrono::seconds( 20 ); the second wait at :2104-2113 is byte-identical to before
  - TransactionManager::Stop() contains lock_guard scopes over fault_test_mutex_ and cv_mutex_ and no bare notify_all outside those scopes
  - ConsensusManager::Close() contains lock_guard scopes over timer_mutex_ and fault_test_mutex_ completing before the close_mutex_ block; timer.join() executes with neither mutex held
  - git diff touches only the three files listed and only the regions described
  - The two rebuild targets compile without errors
  </acceptance_criteria>
  <done>Both authorized repairs are in, minimal-diff, compile clean; any withheld repair is documented with its Task-1 reason in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Focused verification plus one honest full-serial confirmation run</name>
  <files>(no file modifications — verification runs; outcomes recorded in the SUMMARY)</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/12-14-SUMMARY.md (load discipline precedent: 1-min load 1.0-1.5, serial only, honest preservation)
  - test/src/blockchain/CMakeLists.txt (target names and serial CTest properties)
  </read_first>
  <action>
  1. Rebuild the affected test targets (multi_node_finality_fault_test; the library change also relinks lifecycle/cert-fallback binaries — rebuild them too).
  2. Focused gate: run the two previously-flaky cases three consecutive times each, serially, recording 1-min load before each run (uptime). Invoke the test binary directly with gtest filters (ctest -R cannot select individual GTest cases; the runner wrapper is only required by the full CTest registration): ./build/OSX/Release/test/src/blockchain/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.SameBurnContentionUsesOneCanonicalSlotAndExactMint' (3 consecutive runs, logs /tmp/p12_15_sameburn_{1,2,3}.log) and the same with --gtest_filter='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' (3 runs, /tmp/p12_15_restart_{1,2,3}.log). All 6 must pass; a focused failure is a diagnosis finding, not a reroll candidate — preserve it.
  3. Regression check on the WR-02-touched binaries: one run each of consensus_pending_lifecycle_test and transaction_manager_certificate_fallback_test (serial). Both must pass.
  4. One full serial confirmation run of the whole suite: ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure | tee /tmp/p12_15_confirm_1.log, at 1-min load below 2 (wait for external spikes to drain per the 12-14 deviation-3 precedent). Preserve the outcome whatever it is — do NOT rerun for a better result. A pass here is encouraging context for 12-17's formal gate; a failure feeds the attribution record (identify which signature struck and whether it matches Task 1's verdicts).
  5. Record in the SUMMARY: per-run pass/fail, load at run time, elapsed seconds, and which (if any) signature struck in step 4, cross-referenced against Task 1's verdict.
  </action>
  <verify>
    <automated>grep -c "Passed" /tmp/p12_15_confirm_1.log; ls -la /tmp/p12_15_confirm_1.log</automated>
  </verify>
  <acceptance_criteria>
  - 3/3 focused SameBurn runs and 3/3 focused RestartAtVote runs passed (logs preserved under /tmp/p12_15_*)
  - consensus_pending_lifecycle_test and transaction_manager_certificate_fallback_test each passed once on the WR-02 build
  - /tmp/p12_15_confirm_1.log exists and contains exactly one full-suite outcome (Passed or a preserved FAILED listing), with the load at run time recorded in the SUMMARY
  - No test was rerun to replace a recorded failure (the SUMMARY shows every executed run)
  </acceptance_criteria>
  <done>Focused reliability demonstrated on the stabilized build; suite-level behavior honestly sampled once; all outcomes and load conditions recorded for 12-17's formal gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | No new input surface, protocol message, or network path; changes are a CV-notification locking contract inside shutdown paths, a test wait predicate, and diagnostics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-15-01 | DoS (deadlock) | TransactionManager::Stop / ConsensusManager::Close notify-under-lock | mitigate | Each notify runs in its own tight std::lock_guard scope; the two scopes never nest or overlap; neither mutex is held across round_timer_.join(); verified the round-timer thread only reaches Close() after releasing timer_mutex_ (Consensus.cpp:231), and fault_test_mutex_/cv_mutex_ holders never call Stop()/Close(); focused lifecycle + cert-fallback + 6 focused fault runs exercise the paths |
| T-12-15-02 | Tampering (evidence integrity) | full-run confirmation in Task 3 | mitigate | Single recorded run at load < 2, outcome preserved verbatim (12-08/12-12/12-14 no-reroll discipline); formal gate deferred to 12-17 on the final build |
| T-12-15-03 | DoS (masked defect) | SameBurn wait-predicate extension | mitigate | The predicate waits on the exact durable boundary already asserted at :2098 via the existing HasBridgeMarker helper (same key as PersistBridgeExecutedMarker); no timeout increase; a genuinely failing marker still fails the wait after 20s |
| T-12-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs; C++ rebuild of existing targets only |
</threat_model>

<verification>
- Task 1 verdicts exist for all three signatures with preserved-log citations; repairs applied only where authorized.
- Structural: grep shows the four HasBridgeMarker terms in the first SameBurn wait; lock_guard scopes wrap every notify in Stop()/Close(); diffs confined to the three files.
- Behavioral: 3/3 focused runs per previously-flaky case; lifecycle and cert-fallback green; one full-serial run preserved honestly.
- Discipline: no timeout widened, no retry added, no barrier duration changed, no rerolled evidence.
</verification>

<success_criteria>
- VERIFICATION gaps 1 and 2 move from "undiagnosed flakiness" to "attributed and stabilized or explicitly escalated with evidence": every signature has a verdict, the two named suspects are either fixed with focused proof or exonerated with citations, and the suite's remaining per-run risk is quantified for the 12-17 gate (including the child-readiness characterization).
- No finality semantics, timeouts, retry policy, or barrier behavior changed — the phase's evidence discipline (reproduction before repair, no rerolls) is visibly honored in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-15-SUMMARY.md` when done
</output>
