---
phase: 12-multi-node-finality-fault-proof
plan: "03"
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
autonomous: true
requirements: [TEST-04, TEST-05]
must_haves:
  truths:
    - "Restarting at durable vote, accepted-certificate, and post-Mint/pre-marker boundaries preserves the original vote and completes one exact Mint without duplicate outputs or markers."
    - "A selected publisher can be stopped after durable certificate persistence but before notification, and ordinary deterministic failover converges without a conflicting slot record."
  artifacts:
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "Restart-boundary and publisher-loss/failover scenarios"
      contains: "RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce"
  key_links:
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "ConsensusManager durable vote/certificate barriers"
      via: "real peer stop/recreate after an observed post-durability boundary"
      pattern: "RestartPeer"
    - from: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      to: "ConsensusManager immutable certificate barrier"
      via: "stop selected publisher after persistence and before normal notification"
      pattern: "authoritative.*certificate"
---

<objective>
Complete the production-path fault proof with durable-boundary restart and publisher-loss/failover regressions.

Purpose: Demonstrate that the established protocol stays both safe and live when persistent local state outlives the runtime that was processing it.
Output: Named TEST-04 and TEST-05 scenarios on the normal Phase 12 CTest target.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md
@.planning/phases/12-multi-node-finality-fault-proof/12-PATTERNS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VALIDATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-01-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove restart recovery at vote, certificate, and Mint durable boundaries</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp — extend the existing persistent fixture and use its exact-winner durable assertion helper for all three restart subcases.
    - .planning/phases/12-multi-node-finality-fault-proof/12-01-SUMMARY.md — source-of-truth for the actual post-durability barriers and `StopPeer`/`RestartPeer` lifecycle.
    - .planning/phases/12-multi-node-finality-fault-proof/12-02-SUMMARY.md — source-of-truth for public real-route scenario setup and anti-shortcut discipline.
    - .planning/phases/09-durable-one-vote-finality/09-01-SUMMARY.md — source-of-truth for exact persisted-vote recovery and no-revote lock semantics.
    - .planning/phases/11-convergent-certificate-consumption-mint-recovery/11-03-SUMMARY.md — source-of-truth for UTXO-before-marker recovery ordering and existing journal ownership.
    - test/testutil/wait_condition.hpp — mandatory source-of-truth for bounded waits and teardown-safe barriers.
  </read_first>
  <behavior>
    - Stopping/recreating after durable vote persistence causes only the original stored vote to be recovered or re-announced, never a replacement vote.
    - Stopping/recreating after accepted certificate durable readback preserves normal recovery/handler processing and does not lose or duplicate finality.
    - Stopping/recreating after successful idempotent Mint effects but before bridge-marker persistence repairs the marker through normal recovery without a second UTXO/Mint effect.
  </behavior>
  <action>Per D-02, D-03, D-04, D-06, D-08, D-09, and D-10, add the named `RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce` case with three independently initialized subcases so no assertion depends on test order. In the vote subcase, arm the existing post-persistence/pre-publication barrier, wait by named bounded predicate for entry, stop and recreate the peer from its unchanged root, reconnect it through real `AddPeers`, and prove only the exact durable vote can be recovered/announced before normal finality. In the certificate subcase, enter the existing post-committed-readback/pre-handler barrier, stop/recreate the peer, reconnect through real topology, and prove durable certificate recovery dispatches the exact winner once. In the Mint subcase, enter the post-`ParseTransaction`/pre-marker barrier after UTXO effects are durable, stop/recreate from the same root, then let normal certificate work recovery complete the bridge marker while the Mint counter and UTXO/outpoints remain exactly one. For every subcase assert canonical `/cert/&lt;slot&gt;`, original-vote preservation when applicable, exact-winner/no-loser effects, no duplicate UTXO, and one durable bridge marker after reopening. Barriers must always be released during teardown; use no sleeps, unbounded waits, forced timers, direct handler calls, direct `GlobalDB::Put*`, mock transport, or fabricated protocol data.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure &amp;&amp; ! rg -n 'ConsensusPendingLifecycleTestAccess::(Handle|CertificateReceived|WriteLiveCertificate|Force)|\bHandle[A-Za-z]*\(|\bCertificateReceived\(|\bOnConsensusCertificate\(|\bGlobalDB::Put|\bPutConvergentImmutable\(|sleep_for|sleep_until|Force[A-Za-z]*(Timer|Due|Round)' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
  </verify>
  <acceptance_criteria>The named TEST-04 regression has three actual stop/recreate durable-boundary subcases and proves, through reopened per-peer state, an unchanged exact vote where relevant plus exactly one winner Mint, zero losing Mint effects, zero duplicate UTXO, and one bridge marker.</acceptance_criteria>
  <done>All three restart boundaries are covered by a bounded real-lifecycle test that passes via the normal CTest target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove persistence-before-advertisement and deterministic publisher failover</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp — extend the current fixture and keep publisher-loss assertions self-contained.
    - .planning/phases/12-multi-node-finality-fault-proof/12-01-SUMMARY.md — source-of-truth for the post-immutable-write/pre-notification observer/barrier and counter semantics.
    - .planning/phases/12-multi-node-finality-fault-proof/12-02-SUMMARY.md — source-of-truth for real public-ingress and passive-recipient scenario patterns.
    - .planning/phases/10-authoritative-slot-certificate-publication/10-02-SUMMARY.md — source-of-truth for selected-publisher authority, durable write-before-PubSub, and normal failover ownership.
    - test/src/crdt/globaldb_integration.cpp — source-of-truth for real stop/recreate and AddPeers reconnection; do not copy sleep synchronization.
    - test/testutil/wait_condition.hpp — mandatory source-of-truth for bounded barrier and convergence predicates.
  </read_first>
  <behavior>
    - The selected publisher's durable `/cert/<slot>` exists while its notification-publication counter is zero.
    - Stopping that real publisher at this boundary and allowing ordinary membership/round behavior to proceed produces no conflicting immutable slot record.
    - All restarted/reconnected peers converge through normal notification/readback and apply the exact winner once.
  </behavior>
  <action>Per D-04, D-05, D-07, D-08, D-09, and D-10, add `PublisherLossAfterPersistenceUsesDeterministicFailover`. Establish the real validator mesh and drive an ordinary winning proposal/certificate flow. Arm only the selected publisher's existing post-successful-`PutConvergentImmutable`/pre-normal-`Publish` barrier; wait with a named bounded predicate for durable success, assert `/cert/&lt;slot&gt;` is already readable and that node's certificate-notification-publication count is zero, then stop that peer without releasing it. Continue only through ordinary real peer lifecycle, round/authority behavior, and `AddPeers` reconnection; the test must not select a successor, inject a certificate, force a timer, or write a CRDT certificate. Assert the observed successor behavior does not replace or conflict with the durable immutable certificate, all peers converge on the same exact certificate/winner after recovery, the original write happened before any notification, and every available/restarted peer passes winner-only UTXO, no loser, one Mint-effect, and one marker durable checks. Ensure all barriers are released on teardown and all waits remain bounded condition waits without sleeps.</action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure &amp;&amp; ctest --test-dir build/OSX/Release -R '^(multi_node_finality_fault_test|consensus_pending_lifecycle_test|transaction_manager_certificate_fallback_test)$' --output-on-failure &amp;&amp; ! rg -n 'ConsensusPendingLifecycleTestAccess::(Handle|CertificateReceived|WriteLiveCertificate|Force)|\bHandle[A-Za-z]*\(|\bCertificateReceived\(|\bOnConsensusCertificate\(|\bGlobalDB::Put|\bPutConvergentImmutable\(|sleep_for|sleep_until|Force[A-Za-z]*(Timer|Due|Round)' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
  </verify>
  <acceptance_criteria>The named TEST-05 case proves a durable authoritative record while notification is still zero, models loss with a genuine stopped/recreated peer, and proves normal deterministic recovery/failover cannot create conflicting slot authority or duplicate any exact Mint effect. The final wave focused suite and anti-shortcut gate pass.</acceptance_criteria>
  <done>Publisher loss after durability and before advertising is covered by an automated real-route failover regression with no conflicting certificate record.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - D-02/D-06: The three restart cases reuse each peer's persistent root at the actual durable boundaries.
    - D-05/D-07: Publisher loss is held only after successful immutable persistence and observed without altering publication authority.
    - D-03/D-04/D-09: Faults use real lifecycle/connectivity and bounded condition waits, not local shortcuts or sleeps.
    - D-08/D-10: Both final named cases prove durable per-node one-winner/no-duplicate outcomes.
  </truths>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable state → recreated peer | Crash/restart may expose a partial but valid sequence that recovery must complete exactly once. |
| selected publisher → successor | Loss after storage must not let a successor overwrite or forge certificate authority. |
| test barrier → production continuation | A barrier can only pause after a completed action and must release safely during teardown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | T/D | vote/certificate/Mint restart boundaries | mitigate | Stop/recreate exact peer roots after post-durability barriers and assert reopened active vote, certificate, UTXO, marker, and counter outcomes. |
| T-12-09 | T/E | selected publisher / failover | mitigate | Freeze only after successful immutable write and before unchanged Publish; stop real publisher and let normal protocol authority recover without test-selected successor or CRDT write. |
| T-12-10 | D | barrier/IO teardown | mitigate | All waits are bounded predicates; teardown releases barriers then stops/join runtime objects in the fixture's defined order. |
| T-12-11 | R/T | finality test route | mitigate | Per-scenario source gate forbids direct handlers, `GlobalDB::Put*`, forced timers, mock transport, and sleep-driven timing. |
</threat_model>

<verification>
After each task, build and run the normal `multi_node_finality_fault_test` CTest target plus its anti-shortcut source gate. Complete the phase only after the target and its two focused predecessor targets pass, then run the full normal CTest suite as the Phase 12 gate defined in `12-VALIDATION.md`.
</verification>

<success_criteria>
1. Restart at each designated durable boundary preserves the original vote and produces no duplicated Mint, UTXO, or bridge marker.
2. Publisher loss proves immutable persistence occurred before notification and that normal deterministic recovery never creates a conflicting certificate record.
3. The completed target remains normal CTest with a timeout no greater than 300 seconds; the full CTest suite is green.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-03-SUMMARY.md` when done.
</output>
