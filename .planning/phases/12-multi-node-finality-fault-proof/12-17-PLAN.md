---
phase: 12-multi-node-finality-fault-proof
plan: 17
type: execute
wave: 17
depends_on: ["12-15", "12-16"]
files_modified:
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]

must_haves:
  truths:
    - "Three consecutive serial full multi_node_finality_fault_test runs pass on the final build (12-15 stabilization + 12-16 teardown propagation + everything before them) — the phase's own 12-12/12-14 evidence standard, closing gap 1's missing item"
    - "Zero new crash reports for any phase test binary across the entire gate window (multi_node_finality_fault_test, compatibility smoke, lifecycle, cert-fallback), checked against a fresh pre-gate marker"
    - "Restart exact-once (RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce), publisher loss (PublisherLossAfterPersistenceUsesDeterministicFailover), same-burn contention, and late-contender cases are green inside ALL three gate logs — gap 2's 'still pass as a stable property' restored"
    - "The sibling suite matrix (lifecycle, compatibility smoke, cert-fallback, process ownership) passes fresh on the same final build — gap 3 closure demonstrated at suite level, not only in source order"
  artifacts:
    - path: "/tmp/p12_17_triple_a.log"
      provides: "Gate run A — full serial suite outcome"
    - path: "/tmp/p12_17_triple_b.log"
      provides: "Gate run B — full serial suite outcome"
    - path: "/tmp/p12_17_triple_c.log"
      provides: "Gate run C — full serial suite outcome"
    - path: ".planning/STATE.md"
      provides: "Blocker resolution record: the 12-14 evidence-gap note updated with the round-3 outcome (whatever it honestly is)"
      contains: "12-17"
  key_links:
    - from: "the three gate logs"
      to: "13 GTest cases of multi_node_finality_fault_test"
      via: "'[  PASSED  ]' with 13 tests and zero '[  FAILED  ]' lines in each log"
      pattern: "PASSED.*13"
    - from: "~/Library/Logs/DiagnosticReports"
      to: "the gate window"
      via: "find -newer /tmp/p12_17_ips_baseline filtered to the four phase binary names returns exactly zero"
      pattern: "NO_NEW_CRASH_REPORTS"
---

<objective>
Re-run the evidence gate that 12-14 could not close: three consecutive serial full multi_node_finality_fault_test passes with zero new crash reports, executed on the FINAL build generation (after 12-15's attribution-backed stabilization and 12-16's teardown-invariant propagation), plus the full sibling-suite matrix — the closing evidence for VERIFICATION gaps 1, 2, and 3 in one honest series.

Purpose: the developer funded this round (option a) precisely because the phase's evidence standard was unmet (best 2/3; suite green 3/8) and Phase 12 is the final v3.0 phase with no later phase to inherit the work. This plan converts the repairs into the phase's own certified evidence standard; it authorizes NO new repair — a gate failure is a preserved finding routed back to attribution, never a reroll.

Output: /tmp/p12_17_triple_{a,b,c}.log (three consecutive serial full-suite passes), crash-report absence proof against a fresh marker, fresh sibling-suite passes, and a STATE.md record resolving the 12-14 evidence-gap blocker note with the honest outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/deferred-items.md
@.planning/phases/12-multi-node-finality-fault-proof/12-15-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-16-SUMMARY.md

<interfaces>
<!-- Gate mechanics inherited from the 12-12/12-14 precedent. All commands verified against this repo. -->

1. Build/test convention: build tree is build/OSX/Release; the suite is registered serial with
   ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure
   (CMakeLists.txt:120-123: RUN_SERIAL TRUE, RESOURCE_LOCK phase12_real_socket_ports, TEST_LAUNCHER
   multi_node_finality_fault_runner, suite TIMEOUT). Never run with ctest -j.

2. Load discipline (12-12/12-14 precedent, STATE.md blockers record the load-19 contamination):
   record 1-min load (uptime) before every run; wait for external CPU spikes to drain until
   1-min load is below 2 before starting a gate run. A run executed above load 2 is labeled
   contaminated in the record and does not count toward the series (it is preserved, not deleted).

3. Crash-report gate: macOS DiagnosticReports; fresh marker before run A; post-series assertion
   filters to the four phase binary names (multi_node_finality_fault_test,
   multi_node_finality_fault_compatibility_smoke_test, consensus_pending_lifecycle_test,
   transaction_manager_certificate_fallback_test).

4. No-reroll rule (12-08/12-12/12-14): if any run in the series fails, the series result is
   FAILED — preserve the log, attribute the striking signature against 12-15's verdict table,
   and report back. Rerunning until a lucky triplet launders the evidence and is prohibited.
   The gate may only be re-attempted with a NEW attributed fix, never by repetition.

5. Expected passing set per full run: 13 GTests (11 PublisherObserverProcessEvidenceCollector
   cases + the FinalityFaultNetwork scenarios incl. the four fault scenarios and the
   production-route audit); a normal full run takes ~215-220s.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three consecutive serial full-suite gate with crash-report absence proof</name>
  <files>(no source modifications — evidence runs logged to /tmp)</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/12-15-SUMMARY.md (attribution verdicts and residual-risk assessment, incl. the child-readiness characterization)
  - .planning/phases/12-multi-node-finality-fault-proof/12-16-SUMMARY.md (propagated teardown verification and its full-suite sample)
  - .planning/phases/12-multi-node-finality-fault-proof/12-14-SUMMARY.md (no-reroll and load discipline precedent)
  </read_first>
  <action>
  1. Rebuild every phase test target from current sources and verify no source is newer than its test binary (the 12-VERIFICATION freshness check): cmake --build build/OSX/Release --target multi_node_finality_fault_test multi_node_finality_fault_compatibility_smoke_test consensus_pending_lifecycle_test transaction_manager_certificate_fallback_test multi_node_finality_fault_process_ownership_test.
  2. Crash baseline: ls -la ~/Library/Logs/DiagnosticReports/ | grep -E 'multi_node_finality_fault|consensus_pending_lifecycle|transaction_manager_certificate_fallback' (record the newest pre-existing .ips in the SUMMARY), then touch /tmp/p12_17_ips_baseline.
  3. Run the full suite THREE consecutive serial times, one after another with no other test/build activity in between (record 1-min load before each): ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure | tee /tmp/p12_17_triple_{a,b,c}.log. Each run must end with the suite Passed line and no [  FAILED  ] entries.
  4. Crash absence assertion: find ~/Library/Logs/DiagnosticReports -newer /tmp/p12_17_ips_baseline -name '*.ips' | grep -E 'multi_node_finality_fault|consensus_pending_lifecycle|transaction_manager_certificate_fallback' | wc -l must output 0.
  5. Inside each of the three logs confirm the four fault-scenario cases are green by name: SameBurnContentionUsesOneCanonicalSlotAndExactMint, LateContenderAndPassiveRecipientRemainReceiveOnly, RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce, PublisherLossAfterPersistenceUsesDeterministicFailover (plus the production-route audit case).
  6. IF ANY RUN FAILS: stop the series, preserve the failing log verbatim, attribute the striking signature against 12-15's verdict table (known residual: PublisherLoss child-readiness intermittence, which stands under the 12-08 discipline unless two fresh attributed failures authorize observer-only repair), record the attribution, and report the gate as NOT MET with the evidence. Do not rerun.
  </action>
  <verify>
    <automated>grep -c "Passed" /tmp/p12_17_triple_a.log /tmp/p12_17_triple_b.log /tmp/p12_17_triple_c.log && for f in /tmp/p12_17_triple_{a,b,c}.log; do grep -c "FAILED" $f; done && [ "$(find ~/Library/Logs/DiagnosticReports -newer /tmp/p12_17_ips_baseline -name '*.ips' 2>/dev/null | grep -Ec 'multi_node_finality_fault|consensus_pending_lifecycle|transaction_manager_certificate_fallback')" -eq 0 ] && echo NO_NEW_CRASH_REPORTS</automated>
  </verify>
  <acceptance_criteria>
  - All three logs exist; each shows the suite Passed with zero FAILED lines and the four fault-scenario case names each appearing with an OK result
  - The crash-report pipeline outputs exactly 0 and the command echoes NO_NEW_CRASH_REPORTS
  - Per-run 1-min load values are recorded in the SUMMARY and every gate run started below load 2 (any contaminated run is labeled and excluded, never deleted)
  - Exactly three gate runs exist — no fourth "replacement" run (SUMMARY enumerates every executed run)
  </acceptance_criteria>
  <done>The 12-12/12-14 three-consecutive-serial-pass standard is met on the final build with crash-freedom proven, or the failure is preserved and attributed without any reroll.</done>
</task>

<task type="auto">
  <name>Task 2: Sibling suite matrix on the final build and STATE.md resolution record</name>
  <files>.planning/STATE.md</files>
  <read_first>
  - .planning/STATE.md (Blockers/Concerns section — the "Phase 12 Plan 12-14 evidence gap" note this task resolves)
  - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md (gaps 2 and 3 truth statements these runs evidence)
  </read_first>
  <action>
  1. Run the sibling suites serially on the same final build, once each, load recorded: ctest --test-dir build/OSX/Release -R '^consensus_pending_lifecycle_test$' --output-on-failure; then -R '^multi_node_finality_fault_compatibility_smoke_test$'; then -R '^transaction_manager_certificate_fallback_test$'; then -R '^multi_node_finality_fault_process_ownership_test$'. All four must pass — this demonstrates gap 3's closure at suite level (propagated teardown in the shared fixture and smoke target) and gap 2's review-fix invariants on the same generation as the gate.
  2. Update .planning/STATE.md Blockers/Concerns: append a dated resolution note to the "Phase 12 Plan 12-14 evidence gap" entry recording the round-3 outcome honestly — which fixes landed (12-15 SameBurn predicate + WR-02 notify-under-mutex with attribution citations; 12-16 CR-01/CR-02/WR-01 propagation), the gate result from Task 1 (three-pass series dates, log paths, load conditions, crash-report count), and any residual intermittence (child-readiness) with its standing discipline reference. If the gate did NOT pass, the note says so explicitly and Phase 12 remains blocked pending the next developer decision — do not soften a failed gate.
  3. Make no other STATE.md changes (do not flip milestone status, do not remove historical blocker notes, do not alter the Deferred Items rows — the four production follow-ups stay deferred).
  </action>
  <verify>
    <automated>grep -n "12-17" .planning/STATE.md | head -3 && grep -c "Deferred" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
  - All four sibling ctest runs report Passed with their durations and load recorded in the SUMMARY
  - STATE.md contains a 12-17 resolution note under Blockers/Concerns citing the three gate log paths and the crash-report result, and its wording matches the actual Task 1 outcome (pass or preserved failure)
  - The four pre-existing Deferred Items rows and the milestone status fields are unchanged (grep count of "Deferred" unchanged from before)
  </acceptance_criteria>
  <done>The complete final-build evidence set (three-pass gate + four sibling suites + crash absence) is recorded in STATE.md with an honest resolution of the 12-14 evidence-gap blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Evidence execution and documentation only; no source, protocol, or configuration changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17-01 | Repudiation (evidence integrity) | the three-pass gate | mitigate | Exactly three enumerated runs, load conditions recorded per run, logs preserved verbatim under /tmp/p12_17_triple_*.log, no-reroll rule enforced (any failure stops the series), crash baseline marker captured before run A |
| T-12-17-02 | Tampering | STATE.md resolution note | mitigate | Note must cite log paths and match the actual gate outcome; historical blocker notes and Deferred Items rows remain untouched (grep-count check) |
| T-12-17-03 | DoS (environment contamination) | gate runs under external load | mitigate | 1-min load recorded before every run; runs above load 2 labeled contaminated and excluded per the 12-14 deviation-3 precedent — never silently kept in the series |
| T-12-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs; rebuild and execution of existing targets only |
</threat_model>

<verification>
- Three consecutive serial full-suite passes at recorded load < 2, four fault-scenario cases green by name in every log.
- Zero new .ips crash reports across all four phase binary names versus the pre-gate marker.
- Four sibling suites green on the identical build generation.
- STATE.md carries the honest 12-17 resolution; deferred rows intact.
</verification>

<success_criteria>
- VERIFICATION gap 1's evidence standard finally met on the current build generation: three consecutive serial passes with crash-freedom — the record the phase's own 12-12 discipline demands.
- Gap 2 closed as a stable property (restart exact-once and publisher-loss green across all three gate runs, not just in isolation).
- Gap 3 closure evidenced at suite level on the final build.
- The gate is reported honestly in every outcome branch: pass closes the phase's evidence debt; a preserved failure routes back to attribution with no reroll, leaving Phase 12's true state visible for the developer's next decision.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-17-SUMMARY.md` when done
</output>
