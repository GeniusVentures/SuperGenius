---
phase: 12-multi-node-finality-fault-proof
plan: 21
type: execute
wave: 21
depends_on: ["12-19", "12-20"]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
  - src/crdt/impl/graphsync_dagsyncer.cpp
  - .planning/STATE.md
  - .planning/phases/12-multi-node-finality-fault-proof/round5-traces/
autonomous: true
gap_closure: true
requirements: [TEST-03, TEST-04]
must_haves:
  truths:
    - "Option A is implemented verbatim in RestartAtVote's restart-mint block as the composed repair: a pre-restart CheckCertificateForSlot retention wait on second/third/passive (making a true surviving replica) AND the post-restart re-advertisement delayed until recipient gossip-topic readiness is observable (every peer holding >= 2 consensus-topic peers before the existing SubmitCertificate re-publication fires) — test-file only, no existing wait bound relaxed, no timeout bumped"
    - "The composed repair graduates to an attributed fix: three consecutive focused RestartAtVote runs ALL green on the repaired build (the 12-20 clause-(a) standard — the 12-19 precedent proves 1 pass / 1 fail graduates nothing), logs durable in-repo at round5-traces/, never /tmp — or the honest STOP at the first failing run with the failing leg named by its distinct wait message and a new decision routed"
    - "getBackoffTimeout's exponent is clamped at both branches (failures < 16 ? failures : 16) — no failure count produces UB — with production behavior identical for every reachable failure count and the test override path unchanged (WR-06 closed; IN-08 explicitly left in place: the round-5 production-change budget is the clamp alone)"
    - "CERT-02 preserved (PutConvergentImmutable count in the test file stays 0), CERT-05 preserved (the re-advertised certificate still comes from GetCertificateBySlot), and exact-once safety holds across every focused run (failures, if any, are missing-convergence, never duplicate mint)"
  artifacts:
    - path: "test/src/blockchain/multi_node_finality_fault_test.cpp"
      provides: "in the restart-mint block: (1) a new bounded wait between EXPECT_FALSE(HasBridgeMarker(network.first,*winner)) and RestartPeer(network.first) requiring CheckCertificateForSlot(slot) on second/third/passive at std::chrono::seconds(20) with the message 'surviving peers retained the accepted certificate before publisher restart'; (2) a new bounded wait between ConnectPeers(Peers(network)) and the existing 12-19 re-publication comment requiring getPeerCount(ConsensusTopic(peer->consensus)) >= 2 for every peer at std::chrono::seconds(10) with the message 'consensus-topic mesh re-formed with at least two topic peers on every peer before certificate re-advertisement'"
    - path: "src/crdt/impl/graphsync_dagsyncer.cpp"
      provides: "both getBackoffTimeout branches computing base_ms * ( 1ULL << exponent ) with exponent clamped at 16 (const uint64_t exponent = failures < 16 ? failures : 16;) — exactly two occurrences of the shift expression, both clamped"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-1.log"
      provides: "focused graduation run 1 with pre-run load header and the gtest OK line for RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-2.log"
      provides: "focused graduation run 2 (OK line) — or the honestly preserved first failure that STOPs the series with run 3 intentionally absent"
    - path: ".planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-3.log"
      provides: "focused graduation run 3 (OK line) — present only on the all-green path; intentionally absent after an honest STOP"
    - path: ".planning/STATE.md"
      provides: "dated 12-21 entry recording the composed Option A shape, the WR-06 clamp (and the IN-08 left-in-place scope note), and the three focused outcomes with the attributed verdict"
  key_links:
    - from: "restart-mint block pre-restart retention wait (new)"
      to: "ConsensusManager::CheckCertificateForSlot (src/blockchain/Consensus.hpp:515)"
      via: "the same public bool predicate block 2's wait uses at multi_node_finality_fault_test.cpp:2428-2435, applied to second/third/passive before RestartPeer(network.first)"
      pattern: "surviving peers retained the accepted certificate before publisher restart"
    - from: "restart-mint block readiness gate (new)"
      to: "GossipPubSub getPeerCount on the consensus topic"
      via: "the same observability already used at test :580 and :1107 (PeersFormConnectedTopology), raised from >= 1 to >= 2 per peer"
      pattern: "getPeerCount\\( sgns::MultiNodeFinalityFaultTestAccess::ConsensusTopic\\("
    - from: "readiness gate (new)"
      to: "the existing 12-19 re-publication block (GetCertificateBySlot :2512 / SubmitCertificate :2514)"
      via: "gate passes first, re-publication executes immediately after, unchanged"
      pattern: "consensus-topic mesh re-formed with at least two topic peers on every peer"
    - from: "getBackoffTimeout both branches"
      to: "the clamped exponent"
      via: "the shift expression uses the clamped local; std::min(timeout, max_ms) return preserved; the override short-circuit (:828-832 region) untouched"
      pattern: "1ULL << exponent"
---

<objective>
Round-5 gap closure, part 1 of 2: implement the developer's Option A decision verbatim (2026-09-03, recorded at STATE.md:143 / 12-19-SUMMARY.md "Decision Received", commit 633d6ff1) as the composed repair for the RestartAtVote block-3 reconvergence race, close the WR-06 review rider inside the same build generation, and graduate the repair to an attributed fix with all-green focused runs.

Purpose: VERIFICATION gaps 1 and 2 (truth 11 evidence gate; truth 13 restart-case stability) both ride on the same residual — 12-19's single-shot re-publication is necessary-but-insufficient (1 pass / 1 fail) because two candidate mechanisms remain: the gossip-mesh race (ConnectPeers proves libp2p connectivity, not topic grafts; GossipPubSub does not replay) and the CRDT DAG CID dependency (route_count=1: only the restarted publisher served the certificate CID). Option A's two named sub-shapes attack exactly these two mechanisms, so this plan implements BOTH, composed: the pre-restart CheckCertificateForSlot wait creates >= 3 surviving CID providers before the boot-window blacklist can erase the restarted publisher's route (mechanism 2), and the readiness gate delays the re-advertisement until the topic mesh is provably connected (mechanism 1). With four peers, requiring >= 2 consensus-topic peers on EVERY peer forces a connected mesh — a disconnected component of size k allows maximum degree k-1, so k=2 caps degree at 1 and any 3+1 split leaves the singleton at degree 0; the predicate therefore cannot hold on a partitioned mesh. If the composition still fails, each new wait has a distinct message, so the failing leg is attributed by the log line alone — the honest verdict per step the round-5 context requires.

Composition rationale (why both sub-shapes, not either/or): the developer's decision names the two shapes disjunctively and each independently addresses one of the two attributed mechanisms; implementing both stays strictly inside the named shapes (no third shape is invented), and 12-19's evidence shows the shapes fill different holes (block 3 had NO proven pre-restart holder, and the re-publication raced the mesh). The graduation requirement is unchanged either way: three consecutive focused passes, all green.

Output: the two new bounded waits in test/src/blockchain/multi_node_finality_fault_test.cpp; the WR-06 clamp in src/crdt/impl/graphsync_dagsyncer.cpp; three focused RestartAtVote runs at .planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-{1,2,3}.log (all green, or the honest STOP with run 3 absent); a dated STATE.md entry. All evidence in-repo, never /tmp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md
@.planning/phases/12-multi-node-finality-fault-proof/12-19-SUMMARY.md
@.planning/phases/12-multi-node-finality-fault-proof/12-20-SUMMARY.md

<interfaces>
Contracts and anchors the executor needs — extracted from current source. Use these directly; no codebase exploration needed.

From test/src/blockchain/multi_node_finality_fault_test.cpp — the wait macro shape used everywhere in this file (example: block 2's retention wait at :2428-2435):

```cpp
ASSERT_WAIT_FOR_CONDITION( [&] {
    return network.first.consensus->CheckCertificateForSlot( slot ) &&
           network.third.consensus->CheckCertificateForSlot( slot ) &&
           network.passive.consensus->CheckCertificateForSlot( slot );
}, std::chrono::seconds( 20 ),
                           "surviving peers retained the accepted certificate before receiver restart", nullptr );
```

Topic-peer observability precedent (PeersFormConnectedTopology, :1100-1110 — the readiness gate reuses this accessor, threshold raised 1 -> 2):

```cpp
peer->pubsub->getPeerCount( sgns::MultiNodeFinalityFaultTestAccess::ConsensusTopic( peer->consensus ) ) < 1
```

The restart-mint block's current post-restart sequence (anchors for insertion B): RestartPeer( network.first ) -> ASSERT_TRUE( network.first.consensus ) -> ConnectPeers( Peers( network ) ) -> the comment starting "2026-09-03 developer directive fallback (round-4 gap closure):" -> the 12-19 re-publication block (GetCertificateBySlot :2512, SubmitCertificate :2514) -> the 25s wait "recreated Mint peer repaired its marker through normal certificate recovery".

From src/crdt/impl/graphsync_dagsyncer.cpp — getBackoffTimeout's two shift sites (anchor by content: exactly two occurrences of the expression base_ms * ( 1ULL << failures ); cited as :842/:852 by VERIFICATION and :843/:853 by 12-REVIEW — line numbers disagree by one, so locate by grep, not by line):

```cpp
uint64_t timeout = base_ms * ( 1ULL << failures );
return std::min( timeout, max_ms );
```

The override short-circuit at the top of getBackoffTimeout (do NOT touch it — RestartAtVote's 100 ms seam depends on it) and the WR-06 fix shape from 12-REVIEW.md:

```cpp
const uint64_t exponent = failures < 16 ? failures : 16;
uint64_t timeout = base_ms * ( 1ULL << exponent );
return std::min( timeout, max_ms );
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Option A verbatim in the restart-mint block — pre-restart surviving-replica wait + readiness-gated re-advertisement</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp</files>
  <read_first>
  - test/src/blockchain/multi_node_finality_fault_test.cpp lines 2319-2545 (the full RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce body: block 1 restart-vote, block 2 restart-certificate including the retention-wait precedent at :2428-2435, block 3 restart-mint including the 12-19 re-publication block at :2494-2515)
  - test/src/blockchain/multi_node_finality_fault_test.cpp lines 1100-1146 (PeersFormConnectedTopology and ConnectAndWaitForPeers — the getPeerCount/ConsensusTopic observability precedent and the >= 1 threshold the readiness gate raises)
  - .planning/phases/12-multi-node-finality-fault-proof/12-19-SUMMARY.md (the two candidate mechanisms; the evidence that block 3 has no pre-restart retention wait; the Option A verbatim decision record)
  - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md gap 1 missing-item 1 (the Option A verbatim requirement and the test-file-only / no-bound-relaxed constraints)
  </read_first>
  <action>
  Edit ONLY the restart-mint block (the third block, opened by StartNetwork( "restart-mint", 54621 )). Two insertions, nothing else in the file changes.

  INSERTION A (Option A sub-shape "add a pre-restart CheckCertificateForSlot wait on second/third peers first (making a true surviving replica)"): between the line EXPECT_FALSE( HasBridgeMarker( network.first, *winner ) ); and the line RestartPeer( network.first );, add one ASSERT_WAIT_FOR_CONDITION whose predicate requires network.second.consensus->CheckCertificateForSlot( slot ) AND network.third.consensus->CheckCertificateForSlot( slot ) AND network.passive.consensus->CheckCertificateForSlot( slot ) — mirror block 2's wait at :2428-2435 verbatim in shape (same [&] lambda, same std::chrono::seconds( 20 ) bound, nullptr trailing arg) but with the message "surviving peers retained the accepted certificate before publisher restart" (distinct from block 2's "...before receiver restart" so a timeout names the failing leg unambiguously). Including passive alongside the named second/third follows the block-2 precedent, which waits on every non-restarted peer; second/third are the developer's named minimum. Precede it with a comment citing: the developer Option A decision (2026-09-03, STATE.md:143, commit 633d6ff1) and its named shape; the 12-19 attribution that route_count=1 meant only the restarted publisher served the certificate CID and no pre-restart holder was proven in this block; and that this wait both proves and creates the surviving replicas (the 20s window runs while first is still alive and routable, so recipients' CRDT CID fetches from first succeed even when the gossip publication raced the still-forming mesh — PutConvergentImmutable's consensus_datastore_topic_ broadcast and the certificate Publish both fire before the mint-effects barrier pauses first, per src/blockchain/Consensus.cpp SubmitCertificate :2141-2152).

  INSERTION B (Option A sub-shape "delay the re-advertisement until recipient gossip-topic readiness is observable"): between the line ConnectPeers( Peers( network ) ) (the call immediately following RestartPeer( network.first )) and the existing comment line beginning "2026-09-03 developer directive fallback (round-4 gap closure):", add one ASSERT_WAIT_FOR_CONDITION whose predicate is true only when EVERY peer returned by Peers( network ) satisfies peer->pubsub->getPeerCount( sgns::MultiNodeFinalityFaultTestAccess::ConsensusTopic( peer->consensus ) ) >= 2 (express the all-peers check with a range-for or std::all_of over Peers( network ) — both patterns exist in this file). Bound: std::chrono::seconds( 10 ) — a NEW bound for a NEW wait; this is not a relaxation of any existing bound (the developer's constraint forbids relaxing existing bounds, and this plan changes none). Message: "consensus-topic mesh re-formed with at least two topic peers on every peer before certificate re-advertisement", nullptr trailing arg. Precede it with a comment citing the Option A decision's named shape and the connectivity argument: with four peers, minimum topic-degree 2 on every peer forces a connected mesh (a disconnected component of size k permits maximum degree k-1: a 2+2 split caps degree at 1, a 3+1 split leaves the singleton at degree 0), so the predicate holding means the re-published ConsensusMessage can reach every recipient; ConnectPeers only proves >= 1 topic peer plus libp2p links, which is exactly the race 12-19 attributed. The existing 12-19 re-publication block and its comment stay byte-identical and execute immediately after the gate passes.

  Do NOT: touch any existing bound (the 15s barrier waits, the 20s waits, the 25s waits, the 5s topology wait), touch any Arm*/Release/Barrier call, add any PutConvergentImmutable site, add any second SubmitCertificate call site, modify blocks 1 or 2, or change production code in this task.

  Guard verification before committing: git diff -U0 test/src/blockchain/multi_node_finality_fault_test.cpp contains ZERO lines starting with a single minus matching chrono::seconds, Barrier, Arm, or Release (insertions only); grep -c "PutConvergentImmutable" on the file returns 0; grep -c "SubmitCertificate(" call sites unchanged (exactly one call, at the existing :2514 site, plus the existing comment citation); grep -c of the two new wait messages returns 1 each; cmake --build build/OSX/Release --target multi_node_finality_fault_test completes 100% with no warnings on the file.
  </action>
  <acceptance_criteria>
  - The restart-mint block contains a new wait between EXPECT_FALSE( HasBridgeMarker( network.first, *winner ) ); and RestartPeer( network.first ); whose predicate calls CheckCertificateForSlot( slot ) on network.second, network.third, and network.passive, bounded at std::chrono::seconds( 20 ), with the exact message "surviving peers retained the accepted certificate before publisher restart".
  - The restart-mint block contains a new wait between ConnectPeers( Peers( network ) ) and the 12-19 re-publication comment, requiring getPeerCount( ConsensusTopic( peer->consensus ) ) >= 2 for every peer in Peers( network ), bounded at std::chrono::seconds( 10 ), with the exact message "consensus-topic mesh re-formed with at least two topic peers on every peer before certificate re-advertisement".
  - git diff -U0 on the file shows no deleted line matching chrono::seconds, Barrier, Arm, or Release (no existing bound, barrier, or timeout touched — insertions only).
  - grep -c "PutConvergentImmutable" test/src/blockchain/multi_node_finality_fault_test.cpp returns 0 (CERT-02 preserved).
  - Exactly one SubmitCertificate call site exists in the file (unchanged from 12-19), and it sits textually after the new readiness-gate wait.
  - Blocks 1 and 2 of RestartAtVote are untouched: grep -c "surviving peers retained the accepted certificate before receiver restart" still returns 1 and its surrounding lines are outside the diff.
  - Build: cmake --build build/OSX/Release --target multi_node_finality_fault_test exits 0 with no new warnings on the file.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && git diff -U0 test/src/blockchain/multi_node_finality_fault_test.cpp | grep -cE "^-[^-]" | grep -x 0 ; grep -c "surviving peers retained the accepted certificate before publisher restart" test/src/blockchain/multi_node_finality_fault_test.cpp ; grep -c "consensus-topic mesh re-formed with at least two topic peers on every peer" test/src/blockchain/multi_node_finality_fault_test.cpp ; grep -c "PutConvergentImmutable" test/src/blockchain/multi_node_finality_fault_test.cpp | grep -x 0 ; cmake --build build/OSX/Release --target multi_node_finality_fault_test 2>&1 | tail -2</automated>
  </verify>
  <done>Option A is present in source exactly as the developer named it — both sub-shapes, composed, in the restart-mint block only — with no existing wait bound relaxed, CERT-02/CERT-05 preserved, and a clean build.</done>
</task>

<task type="auto">
  <name>Task 2: Clamp WR-06's unbounded exponent in both getBackoffTimeout branches (the only production change of round 5)</name>
  <files>src/crdt/impl/graphsync_dagsyncer.cpp</files>
  <read_first>
  - src/crdt/impl/graphsync_dagsyncer.cpp — the getBackoffTimeout definition and both shift sites (grep "1ULL << failures"; exactly two occurrences; do not trust the cited line numbers :842/:852 vs :843/:853 — anchor by content), plus the override short-circuit at the top of the function and the AddToBlackList failure-increment region
  - .planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md WR-06 (the exact 3-line fix and the unbounded-failures rationale) and IN-08 (the dead TIMEOUT_SECONDS/MAX_FAILURES constants this plan deliberately does NOT touch)
  - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md gap 3 (the rider scope: clamp at both branches, clamp >= 16 preserves behavior)
  </read_first>
  <action>
  In BOTH branches of getBackoffTimeout (ever-connected: base 5000 / max 30000; never-connected: base 10000 / max 1800000), replace the direct shift with the clamped-exponent form from 12-REVIEW.md WR-06: declare const uint64_t exponent = failures < 16 ? failures : 16; then compute uint64_t timeout = base_ms * ( 1ULL << exponent ); keeping the existing return std::min( timeout, max_ms ); unchanged. Add one short comment at the first site citing WR-06 (12-REVIEW.md round 5): failures increments without bound (AddToBlackList), entries are never erased, and resets happen only on successful connection, so 1ULL << failures is UB at failures >= 64; the caps are reached by 2^6 and 2^8, so clamping the exponent at 16 preserves every reachable behavior. Do NOT touch: the override short-circuit at the top of the function (RestartAtVote's 100 ms seam depends on it), the constants, the std::min returns, the seconds-based cid_failures_ subsystem, or anything else in the file. Do NOT resolve IN-08 (the dead TIMEOUT_SECONDS/MAX_FAILURES at graphsync_dagsyncer.hpp:142-143 stay): the round-5 production-change budget is the WR-06 clamp alone per the gap-closure directive — record that scope note in the SUMMARY (IN-08 remains open by scope, not by merit). Then rebuild: cmake --build build/OSX/Release --target multi_node_finality_fault_test (the target pulls in the crdt library containing this file).
  </action>
  <acceptance_criteria>
  - grep -c "1ULL << failures" src/crdt/impl/graphsync_dagsyncer.cpp returns 0 and grep -c "1ULL << exponent" returns 2 (both branches clamped).
  - The clamp line const uint64_t exponent = failures < 16 ? failures : 16; appears exactly twice in getBackoffTimeout.
  - The override short-circuit is untouched: grep -c "blacklist_backoff_override_ms_for_test_.load" still returns 1 in the file, textually before the branches.
  - The constants are unchanged: grep -cE "5000|30000|10000|1800000" in getBackoffTimeout matches the pre-edit values at the same sites.
  - git diff on the file shows additions of the clamp/exponent lines and the comment only — no deleted production line other than the two replaced shift-expression lines.
  - Build exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && grep -c "1ULL << failures" src/crdt/impl/graphsync_dagsyncer.cpp | grep -x 0 && grep -c "1ULL << exponent" src/crdt/impl/graphsync_dagsyncer.cpp | grep -x 2 && grep -c "failures < 16 ? failures : 16" src/crdt/impl/graphsync_dagsyncer.cpp | grep -x 2 && cmake --build build/OSX/Release --target multi_node_finality_fault_test 2>&1 | tail -2</automated>
  </verify>
  <done>WR-06 is closed: no failure count can produce UB in getBackoffTimeout, production behavior is identical for every reachable failure count (caps by 2^6/2^8, clamp at 16), the test seam is untouched, and IN-08 is explicitly left for the record.</done>
</task>

<task type="auto">
  <name>Task 3: Graduate the composed repair to an attributed fix — three consecutive focused RestartAtVote runs, all green or honest STOP</name>
  <files>.planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-1.log, .planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-2.log, .planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-3.log, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/12-20-SUMMARY.md "Round-5 Re-Entry Requirements" (the graduation standard this task satisfies: all-green focused runs, clause-(a) standard, 1 pass / 1 fail graduates nothing)
  - .planning/phases/12-multi-node-finality-fault-proof/round4-traces/fallback-restart-2.log around the cited lines :2214/:2218/:2458/:2874/:2985 (the failure signature and its log-line anatomy — what a strike looks like if the composition does not hold)
  - .planning/phases/12-multi-node-finality-fault-proof/12-19-SUMMARY.md "Focused Verification" (the load-header format, port preflight, and the STOP-clause precedent this task mirrors)
  - The edited restart-mint block from Tasks 1-2 (the three distinct wait messages: the two new ones plus "recreated Mint peer repaired its marker through normal certificate recovery" — the failing message names the failing leg)
  </read_first>
  <action>
  Create .planning/phases/12-multi-node-finality-fault-proof/round5-traces/ (round 5 gets its own directory — round4-traces/ stays frozen as the honest round-4 record; nothing is overwritten). Graduation series, strictly serial:

  Before run 1: port preflight lsof -nP -iTCP:54601-54634 -sTCP:LISTEN must be empty (terminate stale holders and re-check if not). Load discipline per the 12-18/12-19 precedent: prefer 1-min load below 2 (poll up to ~10 minutes if a drainable spike); if the sustained external plateau persists, proceed ONLY at actual load comfortably below the 12-12 contamination regime (15-20 on 8 cores) and record the actual 1-min load in each log header — never proceed into contamination-range load, never silently retry a completed run.

  Each run: ./build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' 2>&1 | tee .planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-N.log (N = 1, 2, 3), with a fresh port preflight before each. Expect roughly 55s on a healthy pass (the historical green wall time; failures historically ran ~98s via the wait cascades).

  Graduation criterion: ALL THREE runs contain the line "[       OK ] FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce" and zero "Timed out waiting for condition" lines. All green = the repair is the attributed fix 12-22's gate carries.

  STOP-ON-FIRST-STRIKE (the 12-19 rule, restated as this plan's own): if ANY run fails, preserve the log verbatim, attribute the failing leg from its wait-timeout message (retention wait = the pre-restart convergence leg never formed a surviving replica; readiness wait = the topic mesh never re-formed; marker wait = both gates passed and the residual is downstream), do NOT start the next run (option-a-restart-3.log must not exist after a run-2 strike), do NOT iterate the repair beyond Option A's named shapes inside this plan, write the honest STOP record, and route a new decision. A failing graduation also means 12-22's gate-entry check must withhold again — that is the correct outcome, not a plan failure.

  STATE.md: append a dated 12-21 entry recording the composed Option A shape (both sub-shapes, with the connectivity argument for the >= 2 threshold), the WR-06 clamp (with the IN-08 left-in-place scope note), each focused run's outcome with wall time and pre-run load, and the verdict: graduated attributed fix or honest STOP with the failing leg named. Update the Session Continuity block. Commit everything (test file, production clamp, round5-traces/, STATE.md) with a feat(12-21) message.
  </action>
  <acceptance_criteria>
  - round5-traces/option-a-restart-1.log, -2.log, -3.log all exist, each carrying a pre-run load header, each containing "[       OK ] FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce", and zero "Timed out waiting for condition" lines — OR the honest-STOP branch holds: the striking run's log is preserved verbatim with its failing wait message identified by name, no log exists for any run after the strike, and the STOP is recorded in STATE.md.
  - Zero /tmp paths anywhere in the round-5 evidence; every log committed under .planning/phases/12-multi-node-finality-fault-proof/round5-traces/.
  - No duplicate-mint failure in any run (no AssertSingleDurableMint or AssertOneLiveMintEffect failure lines — exact-once safety must hold in every outcome, matching all five round-4 runs).
  - STATE.md carries a dated 12-21 entry naming: the composed shape, the WR-06 clamp with the IN-08 scope note, the three outcomes with loads and wall times, and the graduate-or-STOP verdict.
  - On the all-green branch only: the entry states this repair is the attributed fix the 12-22 gate re-attempt carries, per 12-20's re-entry contract.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && for n in 1 2 3; do f=.planning/phases/12-multi-node-finality-fault-proof/round5-traces/option-a-restart-$n.log; if [ -f "$f" ]; then echo "run $n: $(grep -c '\[       OK \] FinalityFaultNetwork.RestartAtVoteCertificateAndMintDurableBoundariesRecoversExactlyOnce' $f) OK, $(grep -c 'Timed out waiting for condition' $f) timeouts, $(grep -c 'FAILED' $f) FAILED"; fi; done; grep -c "12-21" .planning/STATE.md</automated>
  </verify>
  <done>Either the composed Option A repair is a graduated attributed fix (3/3 focused green, durable logs, zero wait timeouts, zero safety violations) or it is honestly stopped at the first strike with the failing leg attributed and a decision routed — never a reroll, never a softened outcome, and 12-22's gate-entry check receives an unambiguous input either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> loopback libp2p hosts | the four real peers communicate over real local sockets; no untrusted external input crosses into the suite |
| test code -> production ingress APIs | test code drives CheckCertificateForSlot / GetCertificateBySlot / SubmitCertificate / SetBlacklistBackoffTimeoutForTest — public surfaces only; CERT-02 forbids test-side CRDT writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-21-01 | Tampering (scope) | the test-file edit | mitigate | diff gate is mechanical: zero deleted lines matching chrono::seconds/Barrier/Arm/Release; PutConvergentImmutable count stays 0; exactly one SubmitCertificate call site; both new waits carry Option A citations |
| T-12-21-02 | Repudiation (evidence) | focused graduation logs | mitigate | all logs at repo-relative round5-traces/, committed with the phase docs; failures preserved verbatim; after a strike, later-run logs must not exist; nothing in /tmp counts |
| T-12-21-03 | Information spoofing (environment) | load/port contamination during focused runs | mitigate | lsof preflight before every run; actual 1-min load recorded per log header; never proceed into the 12-12 contamination regime; a hot run is recorded, never silently retried |
| T-12-21-04 | Tampering (authority) | CERT-02/CERT-05 | mitigate | zero new CRDT-write sites; the re-advertised certificate still comes from GetCertificateBySlot (durable bytes, never constructed); recipients remain receive-only through SubmitCertificate validation |
| T-12-21-05 | Tampering (UB clamp correctness) | getBackoffTimeout | mitigate | clamp at 16 is above both cap exponents (2^6, 2^8) so every reachable duration is bit-identical; override short-circuit untouched; acceptance greps pin the clamp at exactly two sites |
| T-12-21-SC | Tampering | package installs | accept | no package-manager installs occur in this plan (C++ project, existing deps only) |
</threat_model>

<verification>
- Option A verbatim: both named sub-shapes present in the restart-mint block only, test-file change confined to the two waits plus comments (Task 1 acceptance greps).
- No bound relaxed: git diff -U0 shows insertions only for the guarded patterns; all pre-existing bounds (15s/20s/25s/5s) intact.
- WR-06: both shift sites clamped, override path untouched, build clean (Task 2 acceptance greps).
- Graduation: three durable focused logs all green with zero wait-timeout lines, or the honest STOP with the striking leg named and later runs absent (Task 3 acceptance criteria).
- Safety invariants across every run: no duplicate mint, no new CRDT-write site, no new crash reports (check ls -t ~/Library/Logs/DiagnosticReports/*.ips against the pre-round-4 newest, 2026-09-02-125541, when recording STATE.md).
</verification>

<success_criteria>
- VERIFICATION gap 1 missing-items 1-2 are satisfied at the focused-evidence level: Option A implemented verbatim (composed) and graduated all-green — or the honest STOP record exists with the failing leg attributed and a decision routed.
- VERIFICATION gap 3 (WR-06) is closed by the clamp; IN-08 explicitly recorded as left in place by scope.
- Every artifact lives under .planning/phases/12-multi-node-finality-fault-proof/round5-traces/ or the two source files, committed; nothing in /tmp.
- 12-22's gate-entry clause (a) input is unambiguous: either three all-green logs exist or they do not.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-21-SUMMARY.md` when done
</output>
