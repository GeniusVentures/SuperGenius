---
phase: 12-multi-node-finality-fault-proof
plan: 16
type: execute
wave: 16
depends_on: ["12-15"]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_compatibility_smoke_test.cpp
  - test/testutil/storage/base_crdt_test.cpp
  - test/src/blockchain/consensus_pending_lifecycle_test.cpp
autonomous: true
gap_closure: true
requirements: [TEST-06]

must_haves:
  truths:
    - "ComponentPeer::Stop in the compatibility smoke test releases the GlobalDB host co-owners (db, account) BEFORE pubsub->Stop(), mirroring the 12-14 Peer::Stop invariant (12-REVIEW.md CR-01 closed)"
    - "~CRDTFixture releases db_ BEFORE pubs_->Stop(), so every CRDTFixture-derived binary inherits the invariant (12-REVIEW.md CR-02 closed)"
    - "Both multi-validator teardown loops in consensus_pending_lifecycle_test reset manager/db/account before node.pubsub->Stop() (12-REVIEW.md WR-01 same-class sites closed — the lifecycle target is a phase proof artifact, so the gap-3 truth 'holds across ALL phase proof artifacts' is satisfied)"
    - "All affected suites pass on the propagated build: compatibility smoke, consensus_pending_lifecycle_test, transaction_manager_certificate_fallback_test, process ownership, plus one full serial multi_node_finality_fault_test run with zero new crash reports"
  artifacts:
    - path: "test/src/blockchain/multi_node_finality_fault_compatibility_smoke_test.cpp"
      provides: "ComponentPeer::Stop with db/account release before pubsub->Stop and the invariant comment"
      contains: "db.reset()"
    - path: "test/testutil/storage/base_crdt_test.cpp"
      provides: "~CRDTFixture with db_.reset() before pubs_->Stop(), cleanup remove_all unchanged"
      contains: "db_.reset()"
    - path: "test/src/blockchain/consensus_pending_lifecycle_test.cpp"
      provides: "Both teardown loops reset manager/db/account before pubsub->Stop()"
      contains: "node.db.reset()"
  key_links:
    - from: "test/src/blockchain/multi_node_finality_fault_compatibility_smoke_test.cpp (ComponentPeer::Stop)"
      to: "GossipPubSub::Stop"
      via: "db.reset() and account.reset() textually before pubsub->Stop()"
      pattern: "db\\.reset\\(\\);\\s*\\n\\s*account\\.reset\\(\\);"
    - from: "test/testutil/storage/base_crdt_test.cpp (~CRDTFixture)"
      to: "GossipPubSub::Stop"
      via: "db_.reset() textually before pubs_->Stop()"
      pattern: "db_\\.reset\\(\\);"
    - from: "test/src/blockchain/consensus_pending_lifecycle_test.cpp (teardown loops)"
      to: "GossipPubSub::Stop"
      via: "node.manager.reset(); node.db.reset(); node.account.reset(); before node.pubsub->Stop() in both loops"
      pattern: "node\\.db\\.reset\\(\\);"
---

<objective>
Close VERIFICATION gap 3 (12-VERIFICATION.md frontmatter, status: failed): the Peer::Stop teardown-order invariant that 12-14 established (asio io_context outlives every I/O object; GlobalDB's graphsync chain co-owns the libp2p host wired from pubsub->GetHost(), so db/account must be released BEFORE pubsub->Stop()) was not propagated to the sibling teardown sites. 12-REVIEW.md CR-01/CR-02 specify the exact fixes; WR-01 names the two same-class sites in the lifecycle test. Both files were already modified this phase (base_crdt_test.cpp by 12-13, the phase established the invariant by 12-14), so these are small mechanical diffs inside already-modified files — the same review-Critical-to-fix pattern the prior round followed (2b1a8e47, 0fb45d5b, caf34458), not a STATE.md deferral.

Purpose: the exact mechanism that invalidated UAT round 2 (teardown SIGSEGV in kqueue_reactor::deregister_descriptor via ~TcpConnection when ~BasicHost deregisters leftover connections from a freed reactor) remains live in two in-scope phase artifacts — the TEST-06 smoke target and the shared CRDTFixture inherited by 7 binaries including consensus_pending_lifecycle_test and transaction_manager_certificate_fallback_test — leaving gap 3's truth ("the invariant holds across ALL phase proof artifacts") failed.

Output: Three teardown sites reordered to the 12-14 invariant with the full affected-suite matrix green and zero new crash reports; WR-01's two loops included because the lifecycle target is one of the two UAT round-2 full targets and a phase proof artifact, so excluding it would re-open the same gap on the next verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md
@.planning/phases/12-multi-node-finality-fault-proof/12-14-SUMMARY.md

<interfaces>
<!-- The reference implementation and the three sites to mirror it into. All verified in current source during planning. -->

1. REFERENCE (do not modify) — FinalityFaultNetwork::Peer::Stop, multi_node_finality_fault_test.cpp:377-411:
   statement order is: transactions->Stop()/reset; blockchain->Stop(); consensus->Close(); both Snapshot(db,...) while db alive; consensus.reset(); blockchain.reset(); io->stop(); io_thread.join(); db.reset(); account.reset(); pubsub->Stop(); pubsub.reset(); io.reset() — with the block comment documenting the invariant (lines 389-405). Reuse that comment's terms for the propagated sites (a short form citing Peer::Stop is enough).

2. SITE 1 — ComponentPeer::Stop, multi_node_finality_fault_compatibility_smoke_test.cpp:265-279. Current order:
   transactions stop/reset; blockchain->Stop(); consensus.reset(); blockchain.reset(); io->stop(); io_thread.join(); pubsub->Stop() (line 274); db.reset() (275); pubsub.reset() (276); account.reset() (277); io.reset() (278).
   The co-ownership chain is identical to the fault test: StartPeer wires graphsync::Network from peer.pubsub->GetHost() (line 313) into GlobalDB::New (lines 315-321), and ConnectPeers (lines 395/401) creates real cross-peer TCP connections.
   12-REVIEW.md CR-01 gives the exact replacement body.

3. SITE 2 — ~CRDTFixture, test/testutil/storage/base_crdt_test.cpp:107-126. Current order:
   if (pubs_) pubs_->Stop() (line 111); db_.reset() (113); pubs_.reset(); io_.reset(); then the try/catch fs::remove_all(keypair_path_)/remove_all(db_path_) cleanup (lines 117-125, KEEP BYTE-IDENTICAL and LAST).
   The fixture constructs the same chain: graphsync::Network from pubs_->GetHost() (lines 90-91) into GlobalDB::New (line 93); pubs_->Start(40001, {pubs_->GetLocalAddress()}) (line 102) bootstraps a self-connection.
   12-REVIEW.md CR-02 gives the exact replacement body. Construction side (lines 55-105, the 12-13 pid+counter paths and reap) is OUT of scope — do not touch it.

4. SITE 3 — the two multi-validator teardown loops, consensus_pending_lifecycle_test.cpp ~1326-1330 and ~2021-2025. Current body of both loops:
   sgns::ConsensusPendingLifecycleTestAccess::Close( node.manager ); node.pubsub->Stop();
   MultiValidatorNode (struct at :626-635) has members: path, io, pubsub, db, account, registry, manager. node.db is the GlobalDB co-owner; node.account holds db-backed handles. 12-REVIEW.md WR-01 fix: add node.manager.reset(); node.db.reset(); node.account.reset(); inside each loop before node.pubsub->Stop(). The existing node.manager.reset() inside the vote-recovery block at :1994 is a different (mid-test reconstruction) site — leave it alone.

5. Why WR-01 is in scope: gap 3's truth is "the invariant holds across ALL phase proof artifacts, not just the fault test"; consensus_pending_lifecycle_test is a UAT round-2 full target and a phase proof artifact with the identical violation class (review-supplied fix). Excluded on purpose: WR-03 (CMake Linux gate), WR-04 (port 40001 concurrency), IN-01..IN-05 — review Info/operational items outside all three VERIFICATION gaps; they stay recorded in 12-REVIEW.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror the 12-14 teardown order in ComponentPeer::Stop (CR-01) and ~CRDTFixture (CR-02)</name>
  <files>test/src/blockchain/multi_node_finality_fault_compatibility_smoke_test.cpp, test/testutil/storage/base_crdt_test.cpp</files>
  <read_first>
  - test/src/blockchain/multi_node_finality_fault_test.cpp (lines 377-411: the reference Peer::Stop and its invariant comment — the pattern to mirror)
  - test/src/blockchain/multi_node_finality_fault_compatibility_smoke_test.cpp (lines 255-330: ComponentPeer::Stop, StartPeer's host wiring at :313-321, ConnectPeers usage)
  - test/testutil/storage/base_crdt_test.cpp (whole file: construction at :55-105, destructor at :107-126 — construction must stay untouched)
  - .planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md (CR-01/CR-02 exact fix bodies and the crash mechanism)
  </read_first>
  <action>
  SITE 1 — rewrite ComponentPeer::Stop (multi_node_finality_fault_compatibility_smoke_test.cpp:265-279) to this statement order, keeping every existing null guard and noexcept: transactions->Stop(); transactions.reset(); blockchain->Stop(); consensus.reset(); blockchain.reset(); io->stop(); io_thread.join(); db.reset(); account.reset(); pubsub->Stop(); pubsub.reset(); io.reset(). That is: move db.reset() and account.reset() from lines 275/277 up to immediately after the io_thread join, so pubsub->Stop() becomes the FINAL host release. Add a short comment above the moved pair citing the Peer::Stop invariant (asio io_context must outlive every I/O object; GlobalDB's graphsync chain co-owns the host wired from pubsub->GetHost() at StartPeer:313, so db/account are released before pubsub->Stop() — see multi_node_finality_fault_test.cpp:389-405).

  SITE 2 — rewrite ~CRDTFixture (base_crdt_test.cpp:107-126) so the body becomes: db_.reset(); if ( pubs_ ) pubs_->Stop(); pubs_.reset(); io_.reset(); — followed by the existing try/catch fs::remove_all( keypair_path_ ) / fs::remove_all( db_path_ ) block kept byte-identical and last. Add the same short invariant comment above db_.reset() (the fixture wires graphsync::Network from pubs_->GetHost() at :90-91 into GlobalDB::New at :93, and Start(40001, {GetLocalAddress()}) at :102 creates a self-connection).

  Do NOT touch: ComponentPeer construction/StartPeer/ConnectPeers, the CRDTFixture constructor (the 12-13 pid+counter paths and reap), the FSFixture base, or any other function in either file. No timeout, retry, or behavior change — ordering only.
  </action>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && sed -n '265,285p' test/src/blockchain/multi_node_finality_fault_compatibility_smoke_test.cpp && sed -n '107,130p' test/testutil/storage/base_crdt_test.cpp && cmake --build build/OSX/Release --target multi_node_finality_fault_compatibility_smoke_test transaction_manager_certificate_fallback_test consensus_pending_lifecycle_test 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
  - In ComponentPeer::Stop, db.reset() and account.reset() both appear after the io_thread.join() line and before the pubsub->Stop() line; the invariant comment mentions io_context and precedes the pair
  - In ~CRDTFixture, db_.reset() is the first release, pubs_->Stop() follows it, and the fs::remove_all try/catch block is unchanged in content and position (last)
  - The CRDTFixture constructor and ComponentPeer::StartPeer are byte-identical to HEAD (git diff shows only the two Stop/destructor regions plus comments)
  - All three rebuilt targets compile without errors
  </acceptance_criteria>
  <done>Both CR sites carry the 12-14 teardown order with the invariant documented; builds clean; diffs confined to the two teardown regions.</done>
</task>

<task type="auto">
  <name>Task 2: Reset manager/db/account before pubsub->Stop() in both lifecycle teardown loops (WR-01)</name>
  <files>test/src/blockchain/consensus_pending_lifecycle_test.cpp</files>
  <read_first>
  - test/src/blockchain/consensus_pending_lifecycle_test.cpp (lines 626-660: MultiValidatorNode members; lines ~1320-1332 and ~2015-2030: the two teardown loops; line ~1994: the mid-test node.manager.reset() reconstruction site that must NOT be touched)
  - .planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md (WR-01 fix statement)
  </read_first>
  <action>
  In both multi-validator teardown loops (the two `for ( auto &node : nodes )` loops whose bodies are `sgns::ConsensusPendingLifecycleTestAccess::Close( node.manager ); node.pubsub->Stop();`, currently around lines 1326-1330 and 2021-2025), insert three statements between the Close call and node.pubsub->Stop(): node.manager.reset(); node.db.reset(); node.account.reset(); — with a one-line comment citing the Peer::Stop teardown invariant (db/account co-owners of the pubsub host must be released before pubsub->Stop()). Keep the Close call first (it joins the round timer) and node.pubsub->Stop() last, exactly as the review's WR-01 fix specifies. Make no other change in the file; in particular leave the :1994 mid-test reconstruction and every other test body untouched.
  </action>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SuperGenius && grep -n -B1 -A4 "ConsensusPendingLifecycleTestAccess::Close( node.manager )" test/src/blockchain/consensus_pending_lifecycle_test.cpp && cmake --build build/OSX/Release --target consensus_pending_lifecycle_test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
  - Both teardown loops contain node.manager.reset(); node.db.reset(); node.account.reset(); in that order, after the Close call and before node.pubsub->Stop() (grep shows exactly two such occurrences)
  - The loop at :1994's mid-test node.manager.reset() (vote-recovery reconstruction) is unchanged
  - git diff for this file shows only the two loop bodies plus comments
  - consensus_pending_lifecycle_test compiles without errors
  </acceptance_criteria>
  <done>Both same-class lifecycle teardown sites release the host co-owners before stopping the transport; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Affected-suite verification matrix with crash-report baseline</name>
  <files>(no file modifications — verification runs; outcomes recorded in the SUMMARY)</files>
  <read_first>
  - .planning/phases/12-multi-node-finality-fault-proof/12-14-SUMMARY.md (crash-marker and load-discipline precedent)
  - test/src/blockchain/CMakeLists.txt (target names, RUN_SERIAL/RESOURCE_LOCK properties)
  </read_first>
  <action>
  1. Capture the crash baseline BEFORE any run: ls -la ~/Library/Logs/DiagnosticReports/ | grep -E 'multi_node_finality_fault|consensus_pending_lifecycle|transaction_manager_certificate_fallback' and touch /tmp/p12_16_ips_baseline.
  2. Run the affected suites serially (never ctest -j), each once, recording 1-min load:
     - ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_compatibility_smoke_test$' --output-on-failure (CR-01 target)
     - ctest --test-dir build/OSX/Release -R '^consensus_pending_lifecycle_test$' --output-on-failure (WR-01 target + CR-02 inherited)
     - ctest --test-dir build/OSX/Release -R '^transaction_manager_certificate_fallback_test$' --output-on-failure (CR-02 inherited)
     - ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_process_ownership_test$' --output-on-failure (TEST-06 ownership, regression check)
  3. One full serial multi_node_finality_fault_test run on this build: ctest --test-dir build/OSX/Release -R '^multi_node_finality_fault_test$' --output-on-failure | tee /tmp/p12_16_full_1.log at 1-min load below 2; preserve the outcome honestly (no rerolls — 12-15's Task 1 verdicts explain any residual signature; the formal three-pass gate belongs to 12-17).
  4. Assert zero new crash reports across all four binary names: find ~/Library/Logs/DiagnosticReports -newer /tmp/p12_16_ips_baseline -name '*.ips' | grep -E 'multi_node_finality_fault|consensus_pending_lifecycle|transaction_manager_certificate_fallback' | wc -l must output 0.
  5. Record per-run results, load, and elapsed seconds in the SUMMARY.
  </action>
  <verify>
    <automated>[ "$(find ~/Library/Logs/DiagnosticReports -newer /tmp/p12_16_ips_baseline -name '*.ips' 2>/dev/null | grep -Ec 'multi_node_finality_fault|consensus_pending_lifecycle|transaction_manager_certificate_fallback' )" -eq 0 ] && echo NO_NEW_CRASH_REPORTS; grep -c "Passed" /tmp/p12_16_full_1.log</automated>
  </verify>
  <acceptance_criteria>
  - All four suite runs report Passed (compatibility smoke, lifecycle, cert-fallback, process ownership)
  - /tmp/p12_16_full_1.log exists with its single honest outcome recorded in the SUMMARY alongside the run's load
  - The crash-report find|grep|wc pipeline outputs exactly 0 and the check echoes NO_NEW_CRASH_REPORTS
  - No run was rerun to replace a recorded failure
  </acceptance_criteria>
  <done>Every binary inheriting the propagated teardown order passes on this build with zero new crash reports; the full-suite sample is preserved for 12-17's gate context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Test-fixture/shared-test-base teardown ordering only; no production code, protocol behavior, input parsing, or network surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16-01 | DoS (teardown hang) | ~CRDTFixture early db_.reset() | mitigate | GlobalDB::ShutdownNow stops the broadcaster and closes the datastore synchronously while pubs_ still holds the object alive (verified in 12-14's T-12-14-01 assessment of the identical Peer::Stop reorder); lifecycle + cert-fallback + smoke green runs demonstrate no hang |
| T-12-16-02 | Tampering (evidence integrity) | Task 3 crash gate | mitigate | Pre-run baseline marker, post-run find -newer assertion across all affected binary names, honest single-run preservation per the 12-08/12-12/12-14 discipline |
| T-12-16-03 | DoS (regression) | lifecycle loop manager.reset() before db.reset() | mitigate | Close(node.manager) runs first and joins the round timer; ConsensusManager::Close is idempotent on the destructor's second call (early return when round_timer_ is not joinable); lifecycle suite green run is the proof |
| T-12-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs; C++ rebuild of existing test targets only |
</threat_model>

<verification>
- Structural: all three sites show co-owner release before pubsub/Stop ordering (key_link patterns greppable); construction paths and the :1994 reconstruction site byte-identical.
- Behavioral: smoke 6-7s-class pass, lifecycle ~44s pass, cert-fallback full pass, ownership ~0.3s pass, one full fault-suite run preserved.
- Crash gate: zero new .ips reports for any affected binary versus /tmp/p12_16_ips_baseline.
</verification>

<success_criteria>
- VERIFICATION gap 3 (status: failed) closes: CR-01 and CR-02 fixes applied exactly as 12-REVIEW.md specifies, the same-class WR-01 sites in the lifecycle phase-proof artifact propagated in the same mechanical pass, and every affected binary green with zero new crash reports — the teardown invariant now holds across ALL phase proof artifacts.
- No production code, timeouts, waits, or fixture construction behavior changed.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-16-SUMMARY.md` when done
</output>
