---
phase: 12-multi-node-finality-fault-proof
plan: "10"
type: tdd
wave: 10
depends_on: ["12-09"]
files_modified:
  - test/src/blockchain/multi_node_finality_fault_test.cpp
  - .planning/phases/12-multi-node-finality-fault-proof/12-10-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [TEST-05, TEST-06]
must_haves:
  truths:
    - "Per D-21 through D-24 and CR-01, a test-owned parent process launches the built focused GTest child, captures its combined stdout/stderr, derives ExpectedProcess from that child, and evaluates the child's serialized observer records, GTest footer, and exit status rather than any hand-authored fixture strings."
    - "Per D-22, D-23, and CR-01, malformed or missing records, a mismatched fingerprint, an incomplete/missing GTest completion footer, abnormal child termination, or a missing/incomplete terminal remain persisted zero-weight invalid evidence and cannot authorize repair."
    - "Per D-25, D-28, and CR-02, repair_authorization accepts only opaque collector provenance minted after parent capture, child wait-status normalization, and parent fingerprint validation; two independently launched, fully attributed real-socket PublisherLoss children with the same allowlisted boundary/state/error are required, and no observer, topology, or protocol repair is applied by this plan."
    - "Per D-29 and CR-02, the real observer write path sends a framed terminal write/flush outcome over the dedicated inherited parent-control pipe; the parent binds every frame to its child PID/run token, and terminal stream failure remains incomplete zero-weight evidence rather than a manufactured eligible terminal."
    - "Phase 9–11 one-vote, canonical `/cert/<slot>` authority, receiver-no-write, persist-before-notify, exact certificate binding, and UTXO-before-marker/at-most-once Mint invariants remain unchanged; the plan is test-only and does not change consensus, CRDT, PubSub, Mint, topology, peer lifecycle, waits, retries, timeouts, CMake, or production logging."
  artifacts:
    - path: test/src/blockchain/multi_node_finality_fault_test.cpp
      provides: "Test-owned focused-child launcher, combined-output collector, opaque collector-provenance authorization boundary, framed parent-control outcome channel, persisted two-run decision, and post-baseline scope manifest."
    - path: .planning/phases/12-multi-node-finality-fault-proof/12-10-SUMMARY.md
      provides: "Captured-evidence classifications and a no-repair repair_authorization report that preserves invalid evidence and the Phase 12 blocker."
  key_links:
    - from: "PublisherObserverProcessEvidenceCollector"
      to: "PublisherObserverEvidenceEvaluator::Evaluate"
      via: "parent-owned pipe capture, launched-child wait status, normalized lines, and ExpectedProcess constructed from the child PID plus statted executable identity"
      pattern: "fork|exec|waitpid|ExpectedProcess|Evaluate"
    - from: "PublisherReadinessObserver::Write"
      to: "PublisherObserverProcessEvidenceCollector parent-control pipe"
      via: "fixed 66-byte P12 observer-control v1 outcome frame carrying the parent-issued run token, child PID, sequence, write_result, flush_result, and terminal disposition; malformed, duplicate, foreign, partial, or absent terminal-status frames fail closed"
      pattern: "P12_OBSERVER_CONTROL_V1|write_result|flush_result|terminal_disposition"
    - from: "PublisherObserverProcessEvidenceCollector::CollectedEvidence"
      to: "repair_authorization"
      via: "private-constructor provenance token minted only after the collector has captured EOF, normalized waitpid, validated the parent-derived fingerprint, GTest footer, and control frame"
      pattern: "CollectedEvidence|IsObserverRepairAuthorized|repair_authorization"
---

<objective>
Close only CR-01 and CR-02 by making the existing publisher-observer evidence gate consume actual focused-child process output and by making eligible observer-output triples emit-able through the real observer writer.

Purpose: A process fingerprint has value only when the parent that launched the focused test captures and evaluates the child's complete serialized output. This plan turns the currently synthetic classifier into a test-owned process-level evidence collector without changing the finality test fixture or production behavior.
Output: Captured-child integration coverage, fail-closed evidence classification, and a no-repair authorization report for the existing observer-only scope.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md
@.planning/phases/12-multi-node-finality-fault-proof/12-RESEARCH.md
@.planning/phases/12-multi-node-finality-fault-proof/12-PATTERNS.md
@.planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md
@.planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md
@.planning/phases/12-multi-node-finality-fault-proof/12-09-PLAN.md
@.planning/phases/12-multi-node-finality-fault-proof/12-09-SUMMARY.md
@test/src/blockchain/multi_node_finality_fault_test.cpp
@test/src/blockchain/multi_node_finality_fault_compatibility_smoke_test.cpp
@test/src/blockchain/consensus_pending_lifecycle_test.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN the captured-child evidence contract and actual observer writer outcomes</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-10-SUMMARY.md</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp: PublisherReadinessObserver, PublisherObserverEvidenceEvaluator, IsObserverRepairAuthorized, and PublisherLossAfterPersistenceUsesDeterministicFailover
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp: `MultiValidatorSameSlotMintContentionPersistsOneHonestQuorumWinner` cleanup loop; reuse its explicit scenario-owner shutdown order (`Close(manager)`, `pubsub->Stop()`, then unregister the slot handler) rather than putting peer or shutdown ownership in the observer/collector
    - .planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md: CR-01 and CR-02
    - .planning/phases/12-multi-node-finality-fault-proof/12-CONTEXT.md: D-21 through D-29
    - .planning/phases/12-multi-node-finality-fault-proof/12-09-SUMMARY.md: current no-repair evidence and known output grammar
  </read_first>
  <behavior>
    - RED 1: a parent test launching the built target as a focused GTest child fails until its collector captures both stdout and stderr, splits the exact bytes into lines, observes EOF, normalizes `waitpid`, constructs `ExpectedProcess` from the child's actual PID and statted resolved executable, and passes those values to `Evaluate`.
    - RED 2: authorization does not compile for a generic `PublisherObserverEvidenceEvaluator::Record`, parsed record, lifecycle triple, or hand-authored line. It accepts only a non-forgeable `CollectedEvidence` value whose private constructor/factory is confined to the process collector after capture, wait status, GTest footer, parent fingerprint, and control-frame validation.
    - RED 3: a normal captured child with matching START/TERMINAL records, one normal GTest completion footer, ordinary exit, and exactly one valid parent-control result frame is fully attributed; each malformed/duplicate/foreign/missing frame, malformed record, missing/mismatched fingerprint, missing footer, signal/abnormal exit, or absent/incomplete terminal is invalid_or_partial_blocked with count weight zero.
    - RED 4: child-only writer probes exercise the observer's actual `write-failed`, `stream-closed`, and terminal-stream-failure paths and their exact control-frame bytes, but their resulting records are explicitly non-qualifying codec/negative evidence and cannot be supplied to repair authorization.
    - RED 5: only two independently launched children filtered to `FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover`, each using the existing real-socket scenario and distinct parent-issued token/PID, can form the positive D-25 authorization fixture when both are fully attributed and carry the same allowlisted observer-output triple. A terminal write/flush failure sends a valid frame identifying it as terminal-unrecorded, while its absent/incomplete terminal remains zero-weight invalid evidence; it must never be converted into an eligible complete terminal.
  </behavior>
  <action>
    Before changing any repository file, make a read-only execution-lifetime scope manifest in a unique mode-0700 `/private/tmp/p12-10-scope-*` directory; do not call `git stash`, `git commit`, `git add`, `git restore`, or any command that mutates the index or worktree. Record `P12_10_START_HEAD=$(git rev-parse HEAD)` only as provenance, then create NUL-delimited manifests from the union of `git diff --name-only -z` and `git diff --cached --name-only -z` (pre-existing dirty tracked paths) and `git ls-files --others --exclude-standard -z` (pre-existing untracked paths). For every baseline dirty tracked path, record its stage-0 index mode and OID from `git ls-files -s -z`, its working-tree lstat mode and type, and a byte-for-byte copy (or the exact link target for a symlink). For every baseline untracked path, record its relative name, lstat mode/type, and byte-for-byte copy or link target. Use record IDs that are hashes of the NUL-safe path bytes so paths containing whitespace or newlines cannot collide. Keep the complete manifest and copies only in `/private/tmp`; after the scope check, append only `P12_10_SCOPE_MANIFEST=<path>` and `P12_10_SCOPE_MANIFEST_SHA256=<digest>` to `12-10-SUMMARY.md`. This baseline is non-mutating and must succeed with user-owned `.planning/config.json` and `12-VERIFICATION.md` already staged, unstaged, or untracked; preserve them untouched. Task 2 must compare the full baseline index mode/OID and worktree mode/type/content before accepting any Plan-12-10 edit.

    In `multi_node_finality_fault_test.cpp`, add only test-owned C++ process orchestration that launches an explicit focused child filter. Before `fork`, generate an exact 64-character lowercase-hex run token encoding 32 raw bytes, resolve/stat the executable, and build the child `execve` environment by preserving the inherited environment while setting both `P12_10_RUN_TOKEN=<token>` and `P12_OBSERVER_CONTROL_FD=<numeric-control-write-fd>`. Use one nonblocking data pipe whose child write end is duplicated to both stdout and stderr and one nonblocking parent-control pipe; the child closes both read ends, closes the original data write FD after `dup2`, retains only the control write FD across `exec`, and marks every other inherited pipe FD close-on-exec. The parent closes both write ends immediately after `fork`, retains only the read ends, and never derives expected identity from captured record text.

    Define `P12_OBSERVER_CONTROL_V1` as exactly one fixed, network-byte-order 66-byte frame: ASCII magic `P12OCF01` (8 bytes), version `uint16_be(1)`, payload length `uint32_be(52)`, 32 raw token bytes decoded from `P12_10_RUN_TOKEN`, `uint64_be(child_pid)`, `uint64_be(monotonic_sequence)`, and one-byte `record_kind`, `write_result`, `flush_result`, and `terminal_disposition` enums. Accept no extension, padding, alternate byte order, or frame larger than 66 bytes; a control stream may contain exactly one terminal-result frame. Drive collection with `poll` (or `select`) before any blocking `waitpid`: for every ready data/control read end, repeatedly read until `EAGAIN`, accumulate exact bytes, and mark that channel closed only on EOF; retry `EINTR`. Continue draining both data and control channels to EOF even after the child exits, then reap with `waitpid` and reject control bytes that remain partial at EOF. Parse a control frame only after control EOF; reject short, oversized, malformed, duplicate, foreign, nonmatching PID/run-token, non-monotonic-sequence, or nonterminal-result frames, and reject a successful terminal disposition without a matching serialized terminal. Normalize `waitpid` into normal exit, nonzero exit, and abnormal signal status before calling the evaluator.

    Extend the evaluator input/model so a completed GTest footer is recognized from real GTest output, not only synthetic one-line fixtures, and so malformed/missing records, missing footer, abnormal status, fingerprint mismatch, duplicate records, missing terminal, invalid/missing control frame, and `terminal=incomplete` always return retained zero-weight invalid evidence. Keep `tooling_attribution_rebuild` only for a complete record that is structurally valid yet conflicts with the parent-derived `ExpectedProcess`. Keep parsed fields and generic `Record` values non-authorizing: define `PublisherObserverProcessEvidenceCollector::CollectedEvidence` as an opaque provenance type with a private constructor accessible only to the collector. Mint it only after the collector has observed data/control EOF, validated the exact parent-derived fingerprint and one normal GTest footer, normalized a normal child status, and paired the frame with the captured terminal. Expose read-only report fields (classification, count weight, triple, child status, footer/control disposition) but no public constructor, conversion, or factory from `Record`, strings, or triples.

    Replace literal-string-only repair authorization tests with integration-level parent tests whose fixtures are captured output from independently launched child processes. Add child-only focused GTests selected by the runner that exercise the real `PublisherReadinessObserver` output path with narrowly scoped test environment modes for `write-failed`, `stream-closed`, and terminal-stream-failure; they may affect only the test observer writer result, must be unset by default, and must not touch network/finality code. `Write` must synchronously attempt the selected test-owned stream write and flush, emit its actual result to the inherited control FD using the fixed frame even when the terminal stream operation fails, and never claim a complete terminal that did not flush. These child-only writer probes are codec and negative-evidence coverage only: mark their `CollectedEvidence` non-authorizing by origin and prove they cannot enter the D-25 two-run decision. Permit a complete observer-output terminal carrying an allowlisted triple only when the real writer observed the corresponding pre-terminal output failure, the terminal write/flush succeeded, and the parent validated the matching frame. Exercise that potentially qualifying path only by launching the existing real-socket `PublisherLossAfterPersistenceUsesDeterministicFailover` GTest as the focused child with distinct tokens/PIDs; otherwise return incomplete zero-weight evidence. The child must not add mocks, direct handlers, direct datastore writes, forced timers, sleeps, altered waits, retry logic, topology operations, or scenario ownership changes.

    Keep `PublisherLossAfterPersistenceUsesDeterministicFailover` scenario-owned lifecycle and its observer read-only per D-24/D-28. Reuse the `consensus_pending_lifecycle_test.cpp` explicit owner cleanup order: the scenario releases its own peer/runtime handles, then closes managers/stops PubSub and unregisters its handler; the collector observes only already-emitted output and never closes, stops, unregisters, joins, or retains any fixture runtime object. Add captured-child tests that launch that exact real-socket focused scenario through the same collector, proving actual START/TERMINAL/GTest/exit evaluation. The only D-25 candidates are two independently launched real-socket scenario children (distinct tokens/PIDs); their decision remains `none` unless both are fully attributed and share one allowlisted observer-output triple. Cite D-21, D-22, D-23, D-24, D-25, D-28, and D-29 in code-level test names/comments only where needed for traceability; do not change D-01 through D-20 behavior or Phase 9–11 invariants.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='PublisherObserverProcessEvidenceCollector.*:PublisherObserverProcessChild.*' --gtest_brief=1</automated>
    <automated>rg -q 'class CollectedEvidence' test/src/blockchain/multi_node_finality_fault_test.cpp &amp;&amp; rg -q 'P12_OBSERVER_CONTROL_V1' test/src/blockchain/multi_node_finality_fault_test.cpp &amp;&amp; ! rg -U 'IsObserverRepairAuthorized\s*\([^)]*Record' test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
    <automated>build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='PublisherObserverProcessEvidenceCollector.RealSocketPublisherLossOnlyQualifiesWhenTwoRunsMatch:PublisherObserverProcessEvidenceCollector.WriterProbeEvidenceNeverQualifies' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>CR-01 is closed in executable form: only opaque `CollectedEvidence` minted by a parent that actually launched the child can reach authorization; creation requires its independently observed PID/executable fingerprint, combined output EOF, one normal GTest footer, normal wait status, and validated control frame. CR-02 is closed in executable form: each allowlisted triple is emitted by the real observer writer under a child-controlled writer outcome reported through the dedicated framed parent-control pipe, while failed terminal writes are incomplete zero-weight evidence. Synthetic strings may remain only for parser-malformation unit coverage; no generic `Record`, parsed record, literal, or lifecycle triple can construct `CollectedEvidence` or prove repair authorization.</acceptance_criteria>
  <done>Captured child integration tests first fail, then pass after the collector and writer outcome path exist; actual focused publisher-loss child output is evaluated through the collector without any topology/finality repair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist a two-run evaluator-only no-repair decision and enforce the additions-only safety gate</name>
  <files>test/src/blockchain/multi_node_finality_fault_test.cpp, .planning/phases/12-multi-node-finality-fault-proof/12-10-SUMMARY.md</files>
  <read_first>
    - test/src/blockchain/multi_node_finality_fault_test.cpp: completed Task-1 collector, evaluator, writer outcome protocol, and existing finality scenario boundaries
    - .planning/phases/12-multi-node-finality-fault-proof/12-09-SUMMARY.md: existing complete attributed readiness failures and no observer repair conclusion
    - .planning/phases/12-multi-node-finality-fault-proof/12-REVIEW.md: required real-output authorization and emitability fixes
    - .planning/phases/12-multi-node-finality-fault-proof/12-VERIFICATION.md: separate late-contender, restart, and publisher-loss proof gaps that this plan must not repair
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp: `MultiValidatorSameSlotMintContentionPersistsOneHonestQuorumWinner` explicit manager/PubSub/handler cleanup order that keeps runtime ownership with the scenario rather than its observer
  </read_first>
  <behavior>
    - RED 1: authorization fails closed when either of two opaque collector-provenance values is invalid, partial, foreign/mismatched, lacks a validated parent-control frame, is non-eligible, or has a different lifecycle triple; compile-time coverage proves a plain evaluator `Record` cannot be passed to the authorization function.
    - RED 2: exactly two fully attributed `CollectedEvidence` values from independently launched real-socket `FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover` children with distinct parent-issued run tokens/PIDs and the same real allowlisted triple set `repair_authorization=observer-output-only`; the plan executes no repair branch even when this value is true.
    - RED 3: child-only writer-probe evidence, even with a matching allowlisted triple and valid control frame, reports `repair_authorization=none`; it is a negative/codec test and cannot be used as either D-25 qualifying run.
    - RED 4: each collected real-socket publisher-loss child result reports `repair_authorization=none` unless the pair of opaque, real-socket scenario values alone meets the two-record observer-lifecycle rule; a readiness/topology failure never becomes an eligible observer triple.
  </behavior>
  <action>
    Add a test-owned evidence-decision helper whose only input is `std::array<PublisherObserverProcessEvidenceCollector::CollectedEvidence, 2>` returned by the process collector. Make `IsObserverRepairAuthorized` accept that same opaque type, not `PublisherObserverEvidenceEvaluator::Record`; keep the `CollectedEvidence` constructor/factory private to the collector and add compile-time coverage that raw strings, parsed records, hand-built triples, and plain evaluator records are not invocable. Encode the evidence origin in the private collector-minted value: only a completed real-socket child whose focused filter is exactly `FinalityFaultNetwork.PublisherLossAfterPersistenceUsesDeterministicFailover` is D-25-qualifying; every `PublisherObserverProcessChild.*` writer probe is permanently non-qualifying even if it has a matching allowlisted triple. Persist each opaque evidence value's origin/filter, classification, count weight, expected-versus-observed process identity disposition, lifecycle triple, child status, GTest-footer status, control-frame validation status, parent-observed PID/run token, and authorization outcome to the Plan-12-10 summary report. Use two separately launched qualifying real-socket scenario children with distinct run tokens/PIDs for the positive eligible case, and use writer probes only for negative frame/terminal coverage alongside failed terminal writes, partial output, missing/malformed/foreign control frames, fingerprint mismatch, abnormal exit, and mismatched triples.

    After the focused collector suite, independently launch exactly two fresh real-socket `PublisherLossAfterPersistenceUsesDeterministicFailover` children through the collector using distinct parent tokens/PIDs; this is the D-25 pair, not a retry/timing repair. Record both classifications and their pair decision as `repair_authorization=none` unless those two qualifying collector values actually meet the matching eligible rule. Do not run child-only writer probes as substitutes, change the scenario, change `ConnectAndWaitForPeers`, tune any timeout, or apply an observer/topology/finality fix. The terminal summary must explicitly say `PHASE_12_STATUS=BLOCKED`, list CR-01/CR-02 as evidence-collector closure only, preserve the distinct remaining `12-VERIFICATION.md` gaps, and state that any authorized observer-output repair belongs to a separately scoped future plan.

    Before final verification, replay the Task-1 NUL-safe scope manifest without mutating the worktree: for every pre-baseline dirty tracked path, compare the recorded stage-0 index mode/OID with fresh `git ls-files -s -z`, and compare its current working-tree lstat mode/type plus regular-file bytes or symlink target to the saved value; for every pre-baseline untracked path compare the same lstat mode/type/content tuple. Fail rather than absorb a user modification, including an already-dirty `.planning/config.json` or `12-VERIFICATION.md`. Compute the current dirty-tracked and untracked NUL-safe sets afresh, subtract the baseline sets, and reject every post-baseline tracked or untracked path except the declared test source and `12-10-SUMMARY.md`; a baseline dirty path cannot be an allowed Plan-12-10 edit. Scan the zero-context patch of the allowed source against its baseline clean state and every post-baseline untracked code-like file (including an untracked file that attempts to use an allowed-looking name) for `Handle*`, `CertificateReceived`, `OnConsensusCertificate`, `SubmitCertificate`, `GlobalDB::Put`, `PutConvergentImmutable`, direct peer/topology manipulation (`AddPeers`, `ConnectPeers`, `ConnectAndWaitForPeers`, `StopPeer`, `RestartPeer`), forced timer/round operations, `sleep_for`/`sleep_until`, mock transport, retry/timeout changes, production file paths, and protocol payload/key output. Append machine-readable `P12_10_SCOPE_AUDIT` with `baseline_index_mode_oid=passed`, `baseline_worktree_mode_type_content=passed`, `baseline_untracked_mode_type_content=passed`, `post_baseline_paths=passed`, and `scan=passed`, followed by escaped NUL-manifest digest and changed-path hashes, to the summary. Run whitespace checks only over the permitted source patch and the newly created summary; do not use a diff against a stash or against `HEAD`, because either would treat the user's already-dirty files as Plan-12-10 changes. Do not edit CMake, production sources, trace archives, `.planning/config.json`, or `12-VERIFICATION.md`.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target multi_node_finality_fault_test --parallel 4 &amp;&amp; build/OSX/Release/test_bin/multi_node_finality_fault_test --gtest_filter='PublisherObserverProcessEvidenceCollector.*' --gtest_brief=1</automated>
    <automated>p12_summary='.planning/phases/12-multi-node-finality-fault-proof/12-10-SUMMARY.md'; p12_scope="$(sed -n 's|^P12_10_SCOPE_MANIFEST=\(/private/tmp/p12-10-scope-[^[:space:]]*\)$|\1|p' "$p12_summary" | head -1)"; test -d "$p12_scope" -a -f "$p12_scope/dirty-tracked-paths.z" -a -f "$p12_scope/untracked-paths.z" -a -f "$p12_scope/manifest.z"; rg -q '^P12_10_SCOPE_MANIFEST_SHA256=[0-9a-f]{64}$' "$p12_summary"; rg -q '^P12_10_SCOPE_AUDIT .*baseline_index_mode_oid=passed .*baseline_worktree_mode_type_content=passed .*baseline_untracked_mode_type_content=passed .*post_baseline_paths=passed .*scan=passed$' "$p12_summary"; git diff --check -- test/src/blockchain/multi_node_finality_fault_test.cpp</automated>
    <automated>rg -q '^P12_10_EVIDENCE_REPORT .*repair_authorization=(none|observer-output-only) .*PHASE_12_STATUS=BLOCKED' .planning/phases/12-multi-node-finality-fault-proof/12-10-SUMMARY.md &amp;&amp; rg -q '^P12_10_REAL_SOCKET_EVALUATION .*repair_authorization=none ' .planning/phases/12-multi-node-finality-fault-proof/12-10-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>Only opaque, collector-minted evidence with parent-validated capture provenance can cross the authorization boundary. The report distinguishes zero-weight invalid evidence from eligible observer-output failures, demonstrates both allowlisted triples through actual writer output, and records the real-socket branch without repairing it. The post-baseline gate proves every originally dirty staged, unstaged, and untracked user path remains byte-identical; it scans only Plan-12-10 additions, including newly untracked paths, and proves this closure adds no direct protocol shortcut, lifecycle/topology/wait/retry/timeout alteration, payload exposure, CMake change, production edit, or mutation of user-owned planning files.</acceptance_criteria>
  <done>CR-01 and CR-02 have focused process-level regression coverage and a machine-checkable evidence decision; Phase 12 remains blocked pending the existing finality proof gaps, with no repair applied.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Required behavior | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 12 | Preserve production-path finality proof while closing only observer evidence enforcement. | Tasks 1–2 no-repair collector/evidence branch | COVERED |
| REQ | TEST-05 | Publisher-loss evidence must remain attributable without changing publisher/failover behavior. | Task 1 real-socket capture; Task 2 no-repair report | COVERED |
| REQ | TEST-06 | Diagnostics must use real test process output and avoid local-author shortcuts. | Tasks 1–2 process runner plus additions-only scan | COVERED |
| RESEARCH | no mocks/direct handlers/writes/sleeps | Reuse built GTest and existing real-socket route; observe only. | Tasks 1–2 | COVERED |
| CONTEXT | D-21 | Parent verifies process-bound start/terminal identity. | Task 1 | COVERED |
| CONTEXT | D-22 | Mismatches remain zero-weight invalid evidence. | Tasks 1–2 | COVERED |
| CONTEXT | D-23 | Require matching fingerprint, normal GTest completion, and terminal; partial is excluded. | Task 1 | COVERED |
| CONTEXT | D-24 | Scenario retains post-release terminal ownership. | Task 1 | COVERED |
| CONTEXT | D-25 | Only two matching completed real-socket PublisherLoss observer lifecycle failures authorize repair; child-only writer probes cannot qualify. | Tasks 1–2 | COVERED |
| CONTEXT | D-26 | Passes/no-repair do not complete Phase 12. | Task 2 | COVERED |
| CONTEXT | D-27 | Mismatched attributable output becomes tooling attribution and remains fail-closed. | Task 1 | COVERED |
| CONTEXT | D-28 | Observer remains read-only; any repair is future-scoped and observer-output-only. | Tasks 1–2 | COVERED |
| CONTEXT | D-29 | Synchronized writer exposes real write/flush outcome without logging changes. | Task 1 | COVERED |
| REVIEW | CR-01 | Evaluate actual child output and use only returned Records for authorization. | Tasks 1–2 | COVERED |
| REVIEW | CR-02 | Make allowed triples real-writer emit-able; classify terminal failures incomplete. | Task 1 | COVERED |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| parent evidence collector → focused child | Child output, PID, executable fingerprint, and exit state are untrusted until the parent derives and cross-checks them. |
| observer writer → captured terminal | Failed write/flush must not turn an absent terminal into eligible evidence. |
| test collector → finality fixture | Test-only process code must not operate transport, consensus, CRDT, Mint, or peer lifecycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-30 | T/R | child observer records | mitigate | D-21–D-23 require parent-derived ExpectedProcess, exact START/TERMINAL match, real GTest footer, normalized wait status, and one PID/run-token-bound control frame before a nonzero weight; the parent drains data and control to EOF before parsing/wait-status attribution. |
| T-12-31 | T/D | terminal write/flush failure | mitigate | D-29/CR-02 require the inherited `P12_OBSERVER_CONTROL_FD`, parent-delivered `P12_10_RUN_TOKEN`, and exact 66-byte big-endian `P12OCF01` v1 frame; partial, malformed, foreign, duplicate, or terminal-failure frames produce incomplete zero-weight evidence. |
| T-12-32 | E/T | repair authorization | mitigate | D-25/D-28 permit only collector-minted opaque `CollectedEvidence`, whose private construction follows all parent validations; this plan records but does not apply a repair. |
| T-12-33 | T/E | test additions and pre-existing dirty worktree | mitigate | A non-mutating NUL-safe `/private/tmp` manifest proves each baseline dirty tracked path's index mode/OID and worktree mode/type/content plus every baseline untracked tuple are unchanged; only post-baseline paths enter the shortcut/scope scan. |
| T-12-SC | Tampering | dependencies/build | accept | No package installation or CMake registration change is in scope; existing built target only. |
</threat_model>

<verification>
1. Build only the existing `multi_node_finality_fault_test` target.
2. Run captured-child integration tests for matching output, each allowlisted real writer failure, terminal-stream failure through the dedicated control pipe, malformed/foreign control frames, fingerprint mismatch, abnormal exit, and mismatched triples.
3. Compile-test the opaque authorization boundary: generic evaluator records, parsed data, literals, and hand-built triples cannot call `IsObserverRepairAuthorized`.
4. Run exactly two independently launched existing publisher-loss GTests as real-socket children through the collector; only that pair may be evaluated for D-25, while child-only writer probes remain negative/codec tests.
5. Replay the non-mutating pre-task staged/unstaged/untracked manifest, then scan only post-baseline paths and run whitespace checks on the permitted source patch/new summary.
</verification>

<success_criteria>
- CR-01: actual serialized focused-child output, actual footer, actual child status, and parent-validated control outcome reach `Evaluate`; only collector-minted opaque evidence reaches repair authorization.
- CR-02: eligible lifecycle triples arise from the observer writer's controlled real output path and dedicated parent-control FD; failed terminal recording is invalid, not simulated complete output.
- No repair, topology/finality behavior, source registration, or user-owned planning artifact changes occur outside the declared test/summary files; pre-existing staged, unstaged, and untracked worktree content is proven byte-identical.
- The report preserves Phase 12's separately blocked late-contender, restart, and publisher-loss proof gaps.
</success_criteria>

<output>
Create `.planning/phases/12-multi-node-finality-fault-proof/12-10-SUMMARY.md` when done.
</output>
