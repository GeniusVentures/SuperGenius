---
phase: 15-private-networks-consume-privatenetworkid-identity-and-bind
plan: 07
type: execute
wave: 4
depends_on: ["15-05", "15-06"]  # 15-05: exclusive src/account/GeniusNode.cpp ownership (wave 4 = max(3,2)+1)
files_modified:
  - src/blockchain/ValidatorRegistry.hpp
  - src/blockchain/ValidatorRegistry.cpp
  - src/blockchain/impl/Blockchain.hpp
  - src/blockchain/impl/Blockchain.cpp
  - src/account/GeniusNode.cpp
  - test/src/blockchain/validator_registry_scope_test.cpp
  - test/src/blockchain/CMakeLists.txt
autonomous: true
requirements: [D-09, PNET-VAL]

must_haves:
  truths:
    - "ValidatorRegistry identifiers (registry key, gossip topic, CID key) are instance-scoped: a private-network instance uses /<privateNetworkId>-suffixed strings"
    - "The public instance's strings are byte-identical to the previous static constants (gnus-validator-registry, gnus-validator-registry, gnus-validator-registry-cid)"
    - "Two registries with different scopes read/write disjoint CRDT keys and gossip topics — consensus never merges across networks"
    - "ValidatorRegistry does not authorize NetworkRegistry updates (SecureCRDT authorization remains with the selected PeerRegistry)"
  artifacts:
    - path: "src/blockchain/ValidatorRegistry.hpp"
      provides: "instance-scoped registry_key_/validator_topic_/registry_cid_key_ accessors + scope ctor arg"
      contains: "network_scope"
    - path: "src/blockchain/impl/Blockchain.cpp"
      provides: "scope-aware ValidatorRegistry creation at the single site (line ~122)"
      contains: "network_scope"
    - path: "test/src/blockchain/validator_registry_scope_test.cpp"
      provides: "scope isolation + public byte-stability tests"
      min_lines: 60
  key_links:
    - from: "src/account/GeniusNode.cpp INITIALIZING_BLOCKCHAIN"
      to: "Blockchain::New"
      via: "passes private_network_id_ as the network scope"
      pattern: "Blockchain::New"
    - from: "src/blockchain/impl/Blockchain.cpp"
      to: "ValidatorRegistry::New"
      via: "scope forwarded to the registry at the creation site"
      pattern: "ValidatorRegistry::New"
---

<objective>
Give every private network its own ValidatorRegistry state and quorum (D-09) by converting the static `RegistryKey()/ValidatorTopic()/RegistryCidKey()` identifiers into instance state parameterized by network scope, with the public instance byte-stable, and threading the node's scope through `Blockchain::New` to the single registry creation site.

Purpose: Two ValidatorRegistry instances sharing one CRDT key and gossip topic would silently merge the networks' consensus (structural isolation, not just a second object).
Output: Scope-parameterized ValidatorRegistry, scope-aware Blockchain/GossipNode wiring, isolation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-CONTEXT.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-RESEARCH.md (Pitfall 7)
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-PATTERNS.md (section: ValidatorRegistry + Blockchain)
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-06-SUMMARY.md

<interfaces>
<!-- Verified in working tree -->

From src/blockchain/ValidatorRegistry.hpp (386-407):
```cpp
static constexpr std::string_view RegistryKey()    { return "gnus-validator-registry"; }
static constexpr std::string_view ValidatorTopic() { return "gnus-validator-registry"; }
static constexpr std::string_view RegistryCidKey() { return "gnus-validator-registry-cid"; }
```

From src/blockchain/ValidatorRegistry.hpp (145-153) — append the scope arg at the END to keep every existing call site compiling:
```cpp
static std::shared_ptr<ValidatorRegistry> New( std::shared_ptr<crdt::GlobalDB> db,
                                               uint64_t quorum_numerator, uint64_t quorum_denominator,
                                               WeightConfig weight_config, std::string genesis_authority,
                                               BlockRequestMethod block_request_method,
                                               InitCallback init_callback = nullptr );
```

From src/blockchain/impl/Blockchain.cpp:122 — the single creation site:
```cpp
instance->validator_registry_ = ValidatorRegistry::New( instance->db_, 2, 3,
    ValidatorRegistry::WeightConfig{}, GetAuthorizedFullNodeAddress(), /*block_request_method*/, /*init_callback*/ );
```

From src/account/GeniusNode.cpp:665 (INITIALIZING_BLOCKCHAIN): `blockchain_ = Blockchain::New( tx_globaldb_, account_, pubsub_, /*callback*/ ... );`

Default-arg precedent: TrustedPeerRegistry.hpp:123-128 (defaulted base_key argument).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instance-scope ValidatorRegistry identifiers</name>
  <files>src/blockchain/ValidatorRegistry.hpp, src/blockchain/ValidatorRegistry.cpp</files>
  <read_first>
  - src/blockchain/ValidatorRegistry.hpp (whole file — statics 386-407, New 145-153, member layout)
  - src/blockchain/ValidatorRegistry.cpp (every internal use of RegistryKey()/ValidatorTopic()/RegistryCidKey() — locate with grep before editing)
  </read_first>
  <action>
  Add to `ValidatorRegistry::New` a trailing defaulted parameter `std::string network_scope = ""` and thread it into the constructor. Store private members `std::string registry_key_; std::string validator_topic_; std::string registry_cid_key_;` initialized as: empty scope -> exactly the current constant values; non-empty -> constant value + "/" + network_scope (i.e. `gnus-validator-registry/<id>`, `gnus-validator-registry/<id>`, `gnus-validator-registry-cid/<id>`). Add const instance accessors `RegistryKeyValue()`, `ValidatorTopicValue()`, `RegistryCidKeyValue()` returning the members. KEEP the three static constexpr functions unchanged (they remain the byte-stable public values and the defaults). Replace every internal use of the static calls inside `ValidatorRegistry.cpp` (CRDT key construction, topic subscribe/publish, CID persistence) with the instance members — grep for `RegistryKey()`, `ValidatorTopic()`, `RegistryCidKey()` and route each through the members. Do not touch quorum math, weight handling, or the vote/certificate machinery, and do not add any NetworkRegistry/SecureCRDT authorization coupling (D-09: ValidatorRegistry supplies consensus only for its own scope).
  </action>
  <verify>
    <automated>grep -q "network_scope" src/blockchain/ValidatorRegistry.hpp &amp;&amp; ninja -C build/OSX/Release blockchain_genesis validator_registry &amp;&amp; ctest --test-dir build/OSX/Release -R "blockchain|validator|genesis|multi_account" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - The three static constexpr functions still return the exact original strings (diff shows them untouched)
  - `grep -c "registry_key_\|validator_topic_\|registry_cid_key_" src/blockchain/ValidatorRegistry.hpp` >= 3
  - Internal cpp uses read the instance members; a grep of ValidatorRegistry.cpp shows no remaining data-path call to the bare statics (statics may survive only as default initializers)
  - Full blockchain/validator/genesis/multi_account suites pass — public behavior regression-free
  </acceptance_criteria>
  <done>Registry identifiers are per-instance scope state; public instance byte-stable; consensus machinery untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Scope threading through Blockchain and GeniusNode</name>
  <files>src/blockchain/impl/Blockchain.hpp, src/blockchain/impl/Blockchain.cpp, src/account/GeniusNode.cpp</files>
  <read_first>
  - src/blockchain/impl/Blockchain.hpp (New signature)
  - src/blockchain/impl/Blockchain.cpp (New body + ValidatorRegistry::New site ~122; grep for other Blockchain::New callers repo-wide before changing the signature)
  - src/account/GeniusNode.cpp (INITIALIZING_BLOCKCHAIN ~660-680)
  </read_first>
  <action>
  Add a trailing defaulted `std::string network_scope = ""` parameter to `Blockchain::New`, store it on the instance, and forward it as the last argument of the `ValidatorRegistry::New` call at Blockchain.cpp:122. Grep the repo for all `Blockchain::New` call sites and confirm each still compiles unchanged (defaulted arg — public nodes and tests get the empty scope automatically). In `GeniusNode.cpp` INITIALIZING_BLOCKCHAIN (~665), pass `private_network_id_` as the trailing scope argument so a private node's blockchain (and thus its ValidatorRegistry) is scoped to its network. Do not construct multiple simultaneous registries in one node — one scope per node from config is this phase's wiring; the isolation guarantee is what the tests pin.
  </action>
  <verify>
    <automated>grep -q "network_scope" src/blockchain/impl/Blockchain.cpp &amp;&amp; grep -A6 "Blockchain::New" src/account/GeniusNode.cpp | grep -q "private_network_id_" &amp;&amp; ninja -C build/OSX/Release geniusnode &amp;&amp; ctest --test-dir build/OSX/Release -R "startup|node|blockchain" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `Blockchain::New` carries the defaulted scope parameter; all other call sites compile without edits (verify via full build)
  - The GeniusNode call site passes `private_network_id_`
  - startup/node/blockchain suites green — public nodes unchanged
  </acceptance_criteria>
  <done>A private node's validator consensus runs on scoped key/topic/cid strings end to end.</done>
</task>

<task type="auto">
  <name>Task 3: Scope isolation tests</name>
  <files>test/src/blockchain/validator_registry_scope_test.cpp, test/src/blockchain/CMakeLists.txt</files>
  <read_first>
  - test/src/blockchain/ (existing tests + CMakeLists — fixture and link patterns)
  - src/blockchain/ValidatorRegistry.hpp (post-Task-1 accessors + New signature)
  </read_first>
  <action>
  Create `test/src/blockchain/validator_registry_scope_test.cpp` and register it in `test/src/blockchain/CMakeLists.txt`. Cases (pure identifier-level, no consensus round needed): (1) `PublicScopeByteStable` — a default-constructed-scope registry exposes exactly `gnus-validator-registry`, `gnus-validator-registry`, `gnus-validator-registry-cid` via the instance accessors, equal to the retained static constants. (2) `PrivateScopeSuffixed` — scope "0xabcd..." yields each identifier as constant + "/" + scope, and none equals the public value. (3) `DisjointScopes` — two different scopes produce three-way disjoint identifier sets across {public, scopeA, scopeB} for all three identifier kinds. (4) `StaticDefaultsPreserved` — the static constexpr functions still return the original literals (compile-time comparable with `std::string_view` equality against the literals in the test).
  </action>
  <verify>
    <automated>ninja -C build/OSX/Release validator_registry_scope_test &amp;&amp; ctest --test-dir build/OSX/Release -R validator_registry_scope --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - All four cases pass; public identifiers pinned to exact literals
  - `ctest -R validator_registry_scope` exits 0 and the wider blockchain suites remain green
  </acceptance_criteria>
  <done>D-09's structural isolation (disjoint keys/topics per scope, public byte-stable) is test-pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scoped consensus data -> public consensus data | Validator records/quorum cross networks only if identifiers collide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-22 | Elevation of Privilege | cross-network consensus merge | mitigate | Instance-scoped identifiers + three-way disjointness test (Pitfall 7) |
| T-15-23 | Tampering | public registry identifier drift | mitigate | Static constants retained + byte-stability test against literals |
| T-15-24 | Elevation of Privilege | ValidatorRegistry authorizing NetworkRegistry | mitigate | No authorization coupling added; ValidatorRegistry remains consensus-only (D-09) |
| T-15-SC | Tampering | package installs | accept | No registry packages; in-repo only |
</threat_model>

<verification>
- `ctest --test-dir build/OSX/Release -R "validator_registry_scope|blockchain|genesis|startup" --output-on-failure` exits 0
- `git diff src/blockchain/ValidatorRegistry.hpp` shows the three static constexpr bodies unchanged
</verification>

<success_criteria>
- Per-network validator state/quorum with byte-stable public registry
- Single creation site scope-aware; all existing call sites compile unchanged
- Isolation pinned by tests
</success_criteria>

<output>
Create `.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-07-SUMMARY.md` when done.
</output>
