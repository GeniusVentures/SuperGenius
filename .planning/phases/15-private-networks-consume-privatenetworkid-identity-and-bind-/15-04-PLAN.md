---
phase: 15-private-networks-consume-privatenetworkid-identity-and-bind
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - 3rdparty/ipfs-pubsub/src/ipfs_pubsub/deny_list_connection_gater.hpp
  - 3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp
  - 3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp
  - 3rdparty/ipfs-pubsub/test/src/gossip_pubsub_test.cpp
autonomous: true
requirements: [D-07, D-11, PNET-GATE]

must_haves:
  truths:
    - "The vendored DenyListConnectionGater supports an injectable membership allow-list predicate consulted at every peer-aware intercept stage"
    - "A peer on the deny list is rejected even if the allow-list would admit it (deny wins)"
    - "GossipPubSub exposes the allow-list to applications before Start, alongside the existing pnet constructor"
    - "The extended vendored library is reinstalled into /Users/henriqueklein/gnus/3rdparty/build/OSX/Release so SGNUS plans (15-05, 15-08) compile against it"
  artifacts:
    - path: "3rdparty/ipfs-pubsub/src/ipfs_pubsub/deny_list_connection_gater.hpp"
      provides: "SetMembershipAllowList / ClearMembershipAllowList"
      contains: "SetMembershipAllowList"
    - path: "3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp"
      provides: "GossipPubSub::SetMembershipAllowList forwarding API"
      contains: "SetMembershipAllowList"
  key_links:
    - from: "3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp"
      to: "deny_list_connection_gater.hpp"
      via: "GossipPubSub::SetMembershipAllowList forwards to m_connection_gater"
      pattern: "m_connection_gater->SetMembershipAllowList"
    - from: "deny_list_connection_gater.hpp interceptSecured"
      to: "membership predicate"
      via: "peer-aware stages reject when predicate returns false"
      pattern: "GATER_REJECTED_SECURED"
---

<objective>
Extend the vendored `3rdparty/ipfs-pubsub` fork (branch `dev_pnets`) with an injectable allow-list membership predicate on the connection gater and surface it through `GossipPubSub` — the missing piece that makes D-07's NetworkRegistry authorization enforceable at connection upgrade (the installed gater is deny-only and not injectable).

Purpose: The GossipPubSub host (15-05) and the processing host (15-08) both need a positive-membership decision hook; deny-list emulation of an allow-list is impossible without enumerating the universe of peers.
Output: Extended vendored gater + GossipPubSub API, rebuilt and reinstalled into the shared 3rdparty install tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-RESEARCH.md (Pitfall 2, Architecture Patterns, Code Examples: gater skeleton)
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-PATTERNS.md (section: membership gater + gossip_pubsub surfacing)

<interfaces>
<!-- Verified against /Users/henriqueklein/gnus/3rdparty/build/OSX/Release (installed) and the vendored source -->

From 3rdparty/ipfs-pubsub/src/ipfs_pubsub/deny_list_connection_gater.hpp (installed copy identical):
```cpp
class DenyListConnectionGater final : public libp2p::network::ConnectionGater
{
public:
    void BlockPeer( const libp2p::peer::PeerId &peer );      // mutex-guarded set insert
    // interceptAccept: success() — peer unknown at raw stage; checked later at interceptSecured
    // interceptSecured: IsPeerBlocked -> GATER_REJECTED_SECURED else success()
};
```

From 3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp (Init, lines 255-277):
```cpp
void GossipPubSub::Init( std::optional<libp2p::crypto::KeyPair> keyPair, std::optional<std::string> networkKey )
{
    m_connection_gater = std::make_shared<DenyListConnectionGater>();
    if ( networkKey ) {
        auto injector = MakeCustomHostInjector( std::move( keyPair ), m_connection_gater,
                                                libp2p::injector::usePrivateNetwork( *networkKey ) );
        InitHostFromInjector( std::move( injector ) );
    } else { /* same without pnet */ }
}
```
`MakeCustomHostInjector` (lines 142-190) already binds `useSecurityAdaptors<Noise>()` and the gater via `di::bind<network::ConnectionGater>()`.

libp2p::network::ConnectionGater (installed network/connection_gater.hpp): five pure-virtual intercepts returning `outcome::result<void>`; error codes in `libp2p::network::ConnectionGaterError`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Membership allow-list predicate on DenyListConnectionGater</name>
  <files>3rdparty/ipfs-pubsub/src/ipfs_pubsub/deny_list_connection_gater.hpp</files>
  <read_first>
  - 3rdparty/ipfs-pubsub/src/ipfs_pubsub/deny_list_connection_gater.hpp (whole file — class shape, mutex discipline, intercept ordering)
  - /Users/henriqueklein/gnus/3rdparty/build/OSX/Release/libp2p/include/libp2p/network/connection_gater.hpp (interface + error codes)
  </read_first>
  <action>
  In the vendored working copy at `/Users/henriqueklein/gnus/3rdparty/ipfs-pubsub` (branch `dev_pnets` — verify with `git -C /Users/henriqueklein/gnus/3rdparty/ipfs-pubsub branch --show-current`), extend `DenyListConnectionGater` with: member `std::function<bool( const libp2p::peer::PeerId & )> m_membership_allow_list;` guarded by the existing `m_mutex`, and public methods `void SetMembershipAllowList( std::function<bool( const libp2p::peer::PeerId & )> allow_list )` and `void ClearMembershipAllowList()`. Semantics: when no allow-list is set, behavior is byte-identical to today (public mode). When set, every peer-aware stage — `interceptPeerDial`, `interceptSecured`, `interceptUpgraded` — returns the appropriate `libp2p::network::ConnectionGaterError::GATER_REJECTED_*` when the predicate returns false for the remote peer, and `interceptAccept` continues to return success (peer identity is not yet known at the raw stage — same ordering the deny list uses). Deny-list verdicts always win over the allow-list: a blocked peer is rejected even if the predicate would admit it. Never invoke the predicate while holding any other lock than `m_mutex`, and copy the function under the lock before invoking it so a concurrent Set cannot tear the call.
  </action>
  <verify>
    <automated>grep -q "SetMembershipAllowList" 3rdparty/ipfs-pubsub/src/ipfs_pubsub/deny_list_connection_gater.hpp &amp;&amp; test $(grep -c "m_membership_allow_list" 3rdparty/ipfs-pubsub/src/ipfs_pubsub/deny_list_connection_gater.hpp) -ge 5 &amp;&amp; git -C /Users/henriqueklein/gnus/3rdparty/ipfs-pubsub diff --stat -- src/ipfs_pubsub/deny_list_connection_gater.hpp | grep -q deny_list_connection_gater</automated>
  </verify>
  <acceptance_criteria>
  - `deny_list_connection_gater.hpp` contains `SetMembershipAllowList`, `ClearMembershipAllowList`
  - `interceptAccept` still returns `libp2p::outcome::success()` unconditionally (grep the method body)
  - All three peer-aware intercepts contain a membership rejection path returning a `GATER_REJECTED_*` code
  - Deny-list check ordering precedes the allow-list check in `interceptSecured`/`interceptUpgraded`
  </acceptance_criteria>
  <done>The gater expresses positive membership authorization in addition to runtime deny-listing, with deny winning and raw-stage pass-through preserved.</done>
  <note>Compile gate deferral (deliberate): this header is consumed by SGNUS only through the installed copy at /Users/henriqueklein/gnus/3rdparty/build/OSX/Release, which Task 2 rebuilds and reinstalls — no compile probe can observe Task 1's edit until then. The structural count gate above (>= 5 predicate references: declaration, setter, clear, three intercept paths) is the strongest pre-rebuild signal; the functional compile/test gate fires at the start of Task 2 before install.</note>
</task>

<task type="auto">
  <name>Task 2: GossipPubSub surface + rebuild/reinstall vendored library</name>
  <files>3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp, 3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp, 3rdparty/ipfs-pubsub/test/src/gossip_pubsub_test.cpp</files>
  <read_first>
  - 3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp (whole file — constructor set, BlockPeer forwarding at 119-124)
  - 3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp (BlockPeer/UnblockPeer forwarding 227-253, Init 255-277)
  - 3rdparty/ipfs-pubsub/test/src/gossip_pubsub_test.cpp (existing test fixtures/link set)
  - 3rdparty/ipfs-pubsub/CMakeLists.txt (install rules)
  </read_first>
  <action>
  Add to `GossipPubSub` a public `void SetMembershipAllowList( std::function<bool( const libp2p::peer::PeerId & )> allow_list )` that forwards to `m_connection_gater->SetMembershipAllowList(...)` — mirroring the existing `BlockPeer` forwarding — plus a `ClearMembershipAllowList()` forwarder. Document that it must be called after construction and before `Start(...)`, and that in private-network mode the constructor's eager `PskValidationError` contract is unchanged. Add a vendored unit test in `test/src/gossip_pubsub_test.cpp`: two GossipPubSub instances on interface addresses (loopback dialing is denied by default — use `GetInterfaceAddress()`); node A gets an allow-list admitting only node B's PeerId; assert A and B mesh and exchange a published message; then a third node C (admitted by nothing) fails to mesh with A within a bounded grace window using the existing wait-condition helpers in that test file. Then rebuild and reinstall the vendored library into the shared install tree: locate the build directory whose CMakeCache references install prefix `/Users/henriqueklein/gnus/3rdparty/build/OSX/Release` (search `/Users/henriqueklein/gnus/3rdparty/ipfs-pubsub/build*`; if none exists, configure a fresh Release Ninja build with `-DCMAKE_INSTALL_PREFIX=/Users/henriqueklein/gnus/3rdparty/build/OSX/Release/ipfs-pubsub` matching the existing install layout), run the vendored tests, then `cmake --install` (or `ninja install`). Do not touch the libp2p checkout.
  </action>
  <verify>
    <automated>grep -q "SetMembershipAllowList" /Users/henriqueklein/gnus/3rdparty/build/OSX/Release/ipfs-pubsub/include/ipfs_pubsub/gossip_pubsub.hpp &amp;&amp; grep -q "SetMembershipAllowList" /Users/henriqueklein/gnus/3rdparty/build/OSX/Release/ipfs-pubsub/include/ipfs_pubsub/deny_list_connection_gater.hpp</automated>
  </verify>
  <acceptance_criteria>
  - Installed headers at `/Users/henriqueklein/gnus/3rdparty/build/OSX/Release/ipfs-pubsub/include/ipfs_pubsub/` contain `SetMembershipAllowList` in both `gossip_pubsub.hpp` and `deny_list_connection_gater.hpp`
  - Vendored test suite (including the new allow-list case) passes before install
  - A compile probe against the install succeeds: a translation unit including `ipfs_pubsub/gossip_pubsub.hpp` and referencing `&GossipPubSub::SetMembershipAllowList` compiles with the include path `-I/Users/henriqueklein/gnus/3rdparty/build/OSX/Release/ipfs-pubsub/include` (plus its transitive libp2p include)
  - SGNUS still configures/builds against the refreshed install: `ninja -C build/OSX/Release pubsub_counts_test` exits 0
  </acceptance_criteria>
  <done>The allow-list enforcement surface exists in the vendored fork, is test-covered there, and is installed where SGNUS Wave 3+ plans consume it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote peer -> connection upgrade | Unauthenticated-at-raw-stage peers reach gater checks after the security handshake reveals PeerId |
| app -> gater predicate | The membership predicate is injected application state (NetworkRegistry cache in 15-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11 | Elevation of Privilege | gater allow-list | mitigate | Positive-membership predicate at all three peer-aware stages; raw-stage accept is safe because interceptSecured re-checks post-handshake |
| T-15-12 | Tampering | concurrent predicate swap | mitigate | Copy std::function under m_mutex before invocation; deny-list verdicts take precedence |
| T-15-13 | Denial of Service | predicate exceptions | mitigate | Document + test that the predicate must be total; wrap invocation so an exception cannot take down the upgrade path (catch and reject) |
| T-15-14 | Spoofing | Plaintext security | mitigate | GossipPubSub path is already Noise-only via MakeCustomHostInjector (useSecurityAdaptors<Noise>) — unchanged by this plan, verified by existing build |
| T-15-SC | Tampering | package installs | accept | No registry packages; vendored first-party fork on dev_pnets, changes committed to that checkout |
</threat_model>

<verification>
- Installed headers contain the new API; vendored tests green; SGNUS `pubsub_counts_test` still builds against the refreshed install
- `git -C /Users/henriqueklein/gnus/3rdparty/ipfs-pubsub status` shows only the three intended files modified
</verification>

<success_criteria>
- D-07's enforcement hook exists in the fork and is installable-by-SGNUS
- Public-mode behavior byte-identical (no allow-list set = today's semantics)
- Pnet eager-throw contract preserved
</success_criteria>

<output>
Create `.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-04-SUMMARY.md` when done. Record the vendored commit hash and the install location for Plans 15-05/15-08.
</output>
