---
phase: 15-private-networks-consume-privatenetworkid-identity-and-bind
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/peerregistry/PeerRegistry.hpp
  - src/peerregistry/CMakeLists.txt
  - src/CMakeLists.txt
  - src/securecrdt/SecureCrdtRegistry.hpp
  - src/trustedpeer/TrustedPeerRegistry.hpp
  - src/trustedpeer/TrustedPeerRegistry.cpp
  - src/trustedpeer/CMakeLists.txt
  - test/src/peerregistry/peer_registry_test.cpp
  - test/src/peerregistry/CMakeLists.txt
  - test/src/CMakeLists.txt
autonomous: true
requirements: [D-04, D-05, PNET-REG]

must_haves:
  truths:
    - "A PeerRegistry abstraction exists that resolves the current authorized peer set and quorum from cached state only"
    - "TrustedPeerRegistry implements PeerRegistry with zero change to its quorum/cache logic, and remains the single global root trust domain (no per-network instances)"
    - "A SecureCrdtRegistryEntry can carry an explicit association to a PeerRegistry instance, and resolving a registered key derives its signer set from that registry"
    - "Existing securecrdt and trustedpeer tests pass unchanged (regex contract and TPR behavior byte-compatible)"
  artifacts:
    - path: "src/peerregistry/PeerRegistry.hpp"
      provides: "PeerRegistry interface"
      contains: "class PeerRegistry"
    - path: "src/securecrdt/SecureCrdtRegistry.hpp"
      provides: "per-entry PeerRegistry association"
      contains: "peer_registry"
    - path: "test/src/peerregistry/peer_registry_test.cpp"
      provides: "association resolution tests"
      min_lines: 60
  key_links:
    - from: "src/trustedpeer/TrustedPeerRegistry.hpp"
      to: "src/peerregistry/PeerRegistry.hpp"
      via: "public inheritance + override"
      pattern: "public sgns::peerregistry::PeerRegistry"
    - from: "src/securecrdt/SecureCrdtRegistry.hpp"
      to: "PeerRegistry"
      via: "SecureCrdtRegistryEntry::peer_registry shared_ptr member (forward-declared to avoid include cycle)"
      pattern: "peer_registry"
---

<objective>
Introduce the explicit `PeerRegistry` base abstraction (D-04), adapt `TrustedPeerRegistry` onto it without logic changes (D-05), and give `SecureCrdtRegistryEntry` an explicit per-key registry association so SecureCrdt policy no longer implicitly assumes TrustedPeerRegistry.

Purpose: This is the interface contract Plans 15-03 (NetworkRegistry) and 15-05 (gater membership source) build against; defining it first prevents downstream executors from scavenger-hunting the authorization contract.
Output: New `src/peerregistry` module, evolved `SecureCrdtRegistry`, adapted `TrustedPeerRegistry`, association unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-CONTEXT.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-RESEARCH.md (Pattern 4, PeerRegistry sketch)
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-PATTERNS.md (section: PeerRegistry.hpp)

<interfaces>
<!-- Verified in working tree -->

From src/securecrdt/SecureCrdtRegistry.hpp (lines 33-59):
```cpp
struct SignerSetSnapshot
{
    std::vector<std::string> signer_set;
    uint64_t                 required_signatures = 0;
};
using SignerSetSource =
    std::function<outcome::result<SignerSetSnapshot>( const std::string &base_key )>;
struct SecureCrdtRegistryEntry
{
    std::string                                       key_pattern;
    SignerSetSource                                   signer_set_source;
    std::function<std::shared_ptr<ISignedCRDTData>()> make_instance;
    std::regex                                        compiled_pattern;
    const void                                       *owner_token = nullptr;
};
```

From src/trustedpeer/TrustedPeerRegistry.hpp:
```cpp
class TrustedPeerRegistry : public std::enable_shared_from_this<TrustedPeerRegistry>
{
public:
    TrustedPeerRegistry( std::shared_ptr<sgns::securecrdt::SecureCrdt> secure_crdt,
                         std::vector<std::string>                      genesis_peers,
                         std::string                                   bootstrapper_address,
                         uint64_t                                      quorum_threshold,
                         sgns::crdt::HierarchicalKey                   base_key );
    static outcome::result<std::shared_ptr<TrustedPeerRegistry>> New( ...,
        sgns::crdt::HierarchicalKey base_key = sgns::crdt::HierarchicalKey( "trusted-peer-registry" ) );
    std::vector<std::string> GetCurrentPeers() const;               // line 175
    outcome::result<sgns::securecrdt::SignerSetSnapshot> ResolveSignerSet() const;  // line 204
    void RegisterSignerSetSource();                                  // line 194
    void Unregister();                                               // line 187
private:
    sgns::crdt::HierarchicalKey base_key_;                           // line 207
};
```

From src/securecrdt/SecureCrdtRegistry.hpp Register (regex contract — MUST stay byte-compatible):
```cpp
entry.compiled_pattern = std::regex( "/?" + key_pattern + "(/sig/[^/]+)?" );
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the PeerRegistry interface module</name>
  <files>src/peerregistry/PeerRegistry.hpp, src/peerregistry/CMakeLists.txt, src/CMakeLists.txt</files>
  <read_first>
  - src/securecrdt/SecureCrdtRegistry.hpp (SignerSetSnapshot/SignerSetSource types to reuse)
  - src/securecrdt/ISignedCRDTData.hpp (header-only interface style: include guard, doxygen @file/@brief/@date, namespace)
  - src/securecrdt/QuorumThresholdValidation.hpp (header-only precedent)
  - src/crdt/hierarchical_key.hpp (HierarchicalKey)
  - src/CMakeLists.txt
  </read_first>
  <action>
  Create `src/peerregistry/PeerRegistry.hpp` in namespace `sgns::peerregistry`: an abstract class `PeerRegistry` with a virtual destructor and exactly three pure-virtual methods — `CurrentSignerSet()` returning `outcome::result<sgns::securecrdt::SignerSetSnapshot>` const-qualified, `GetCurrentPeers()` returning `std::vector<std::string>` const-qualified, and `BaseKey()` returning `sgns::crdt::HierarchicalKey` const-qualified. Document on `CurrentSignerSet` that implementations must resolve from cached state only and never re-enter the SecureCrdt quorum-read path (re-entrancy guard). Also provide an inline free function `MakeRegistrySignerSetSource( std::shared_ptr<PeerRegistry> registry )` returning a `securecrdt::SignerSetSource` lambda that captures the shared_ptr and returns `registry->CurrentSignerSet()` — this helper lives here (not in SecureCrdtRegistry.hpp) so SecureCrdtRegistry.hpp needs only a forward declaration, avoiding an include cycle. Create `src/peerregistry/CMakeLists.txt` exposing it as an INTERFACE library named `peerregistry` that includes the parents of its source dir and links `securecrdt` PUBLIC (mirror how header-only pieces are exposed in `src/securecrdt/CMakeLists.txt`; if that file declares a normal library, still use INTERFACE here — the headers have no TU). Register the module by adding `add_subdirectory(peerregistry)` to `src/CMakeLists.txt` after the `securecrdt` line.
  </action>
  <verify>
    <automated>grep -q "add_subdirectory(peerregistry)" src/CMakeLists.txt &amp;&amp; grep -q "class PeerRegistry" src/peerregistry/PeerRegistry.hpp &amp;&amp; ninja -C build/OSX/Release securecrdt trustedpeer</automated>
  </verify>
  <acceptance_criteria>
  - `src/peerregistry/PeerRegistry.hpp` declares `class PeerRegistry` with the three pure-virtual methods and `MakeRegistrySignerSetSource`
  - `grep -c "CurrentSignerSet\|GetCurrentPeers\|BaseKey" src/peerregistry/PeerRegistry.hpp` >= 3
  - `src/CMakeLists.txt` contains `add_subdirectory(peerregistry)`
  - `ninja -C build/OSX/Release securecrdt trustedpeer` exits 0 (module wires into the build without breaking targets)
  </acceptance_criteria>
  <done>Header-only PeerRegistry abstraction compiled into the build graph, reusing securecrdt value types.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit PeerRegistry association to SecureCrdtRegistryEntry</name>
  <files>src/securecrdt/SecureCrdtRegistry.hpp</files>
  <read_first>
  - src/securecrdt/SecureCrdtRegistry.hpp (whole file — Register/UnregisterIf/Resolve)
  - src/peerregistry/PeerRegistry.hpp (the interface this association targets)
  </read_first>
  <action>
  Forward-declare `namespace sgns::peerregistry { class PeerRegistry; }` near the top of the securecrdt namespace area (the complete type is not needed here — that is why the adaptation helper lives in PeerRegistry.hpp, avoiding an include cycle). Add to `SecureCrdtRegistryEntry` a member `std::shared_ptr<sgns::peerregistry::PeerRegistry> peer_registry;` defaulting to null, with a doc comment naming D-04 (shared_ptr per the BurnConfig ownership precedent — discretion exercised as shared, not weak). `SecureCrdtRegistry::Register` stores the member verbatim onto the map's copy of the entry and leaves an explicitly provided `signer_set_source` untouched; entries that want registry-derived sources build them with `peerregistry::MakeRegistrySignerSetSource` (Task 1) and may also set `peer_registry` so `Resolve()` callers can see which authority owns the key. Do NOT change the regex compilation line, `UnregisterIf` token semantics, or `Resolve` matching order.
  </action>
  <verify>
    <automated>grep -q "peer_registry" src/securecrdt/SecureCrdtRegistry.hpp &amp;&amp; ninja -C build/OSX/Release securecrdt &amp;&amp; ctest --test-dir build/OSX/Release -R securecrdt --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `SecureCrdtRegistryEntry` has the `peer_registry` shared_ptr member with a doc comment naming D-04
  - The regex line remains exactly `std::regex( "/?" + key_pattern + "(/sig/[^/]+)?" )` (byte-compatible)
  - All pre-existing securecrdt tests pass unchanged
  </acceptance_criteria>
  <done>Policy entries carry an explicit, optional PeerRegistry authority; existing behavior and regex contract untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Adapt TrustedPeerRegistry onto PeerRegistry + association tests</name>
  <files>src/trustedpeer/TrustedPeerRegistry.hpp, src/trustedpeer/TrustedPeerRegistry.cpp, src/trustedpeer/CMakeLists.txt, test/src/peerregistry/peer_registry_test.cpp, test/src/peerregistry/CMakeLists.txt, test/src/CMakeLists.txt</files>
  <read_first>
  - src/trustedpeer/TrustedPeerRegistry.hpp (whole file — class shape, GetCurrentPeers 175, ResolveSignerSet 204, RegisterSignerSetSource 194)
  - src/trustedpeer/TrustedPeerRegistry.cpp (RegisterSignerSetSource 160-178, ResolveSignerSet 180-188)
  - src/peerregistry/PeerRegistry.hpp
  - src/trustedpeer/CMakeLists.txt
  - test/src/securecrdt/securecrdt_registry_test.cpp (registry test fixture pattern)
  - test/src/securecrdt/securecrdt_test_node.hpp (MakeSecureCrdtTestNode helper)
  - test/src/trustedpeer/CMakeLists.txt (addtest + link set)
  - test/src/CMakeLists.txt
  </read_first>
  <action>
  Make `TrustedPeerRegistry` publicly inherit `sgns::peerregistry::PeerRegistry` alongside `std::enable_shared_from_this<TrustedPeerRegistry>` (D-05: it remains ONE global root — no new instances are created anywhere in this task). Add `override` to the existing `GetCurrentPeers()`; add two thin new methods that only forward: `CurrentSignerSet()` returning `ResolveSignerSet()` and `BaseKey()` returning `base_key_`. No changes to cache, quorum, or lifecycle logic. In `RegisterSignerSetSource` (cpp 160-178), additionally set `entry.peer_registry = shared_from_this();` before `SecureCrdtRegistry::Register(...)` so the global registry's own policy entry records its authority. Link `peerregistry` in `src/trustedpeer/CMakeLists.txt` (PUBLIC alongside `securecrdt`). Create `test/src/peerregistry/peer_registry_test.cpp` with a fixture building a `SecureCrdt` over `MakeSecureCrdtTestNode("peerregistry")` (mirror `test/src/trustedpeer/trustedpeerregistry_genesis_test.cpp` SetUp/TearDown including explicit `Unregister()`); tests: (1) after `TrustedPeerRegistry::New` + `RegisterSignerSetSource`, `SecureCrdtRegistry::Resolve("trusted-peer-registry")` returns an entry whose `peer_registry` is non-null and whose `signer_set_source` resolves the genesis cached set; (2) `Resolve("trusted-peer-registry/sig/<addr>")` resolves to the same authority (child-key shape preserved); (3) a hand-registered entry built with `MakeRegistrySignerSetSource(tpr)` returns the same snapshot as `tpr->CurrentSignerSet()`; (4) `Unregister()` removes the association (Resolve returns nullopt). Register the suite via new `test/src/peerregistry/CMakeLists.txt` (copy the trustedpeer test CMakeLists link set, linking `trustedpeer securecrdt peerregistry`) and `add_subdirectory(peerregistry)` in `test/src/CMakeLists.txt`.
  </action>
  <verify>
    <automated>ninja -C build/OSX/Release peer_registry_test &amp;&amp; ctest --test-dir build/OSX/Release -R "peer_registry|trustedpeer|securecrdt" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `grep -q "public sgns::peerregistry::PeerRegistry" src/trustedpeer/TrustedPeerRegistry.hpp`
  - `grep -q "entry.peer_registry = shared_from_this()" src/trustedpeer/TrustedPeerRegistry.cpp`
  - `peer_registry_test` binary exists, runs, and all its cases pass
  - Full `trustedpeer` and `securecrdt` suites pass — zero regression (D-05 preserved)
  </acceptance_criteria>
  <done>The root registry is a PeerRegistry, its policy entry self-associates, and the per-key authorization contract is test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRDT key -> policy resolution | A registered key pattern selects the signing authority; a wrong association would let an unauthorized registry sign values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Elevation of Privilege | SecureCrdtRegistryEntry association | mitigate | Explicit `peer_registry` member + tests asserting resolution returns the registered authority for base and sig-child keys |
| T-15-05 | Tampering | Register/Resolve regex contract | mitigate | Regex string kept byte-identical; securecrdt suite green proves no matching drift |
| T-15-06 | Spoofing | TrustedPeerRegistry adaptation | mitigate | Forwarding-only overrides; no logic changes; trustedpeer suite unchanged (D-05) |
| T-15-SC | Tampering | package installs | accept | No registry packages; first-party vendored only |
</threat_model>

<verification>
- `ctest --test-dir build/OSX/Release -R "peer_registry|trustedpeer|securecrdt" --output-on-failure` exits 0
- `git diff src/securecrdt/SecureCrdtRegistry.hpp` shows the regex line untouched
</verification>

<success_criteria>
- PeerRegistry abstraction exists and is the contract NetworkRegistry (15-03) implements
- TPR is the sole implementer; global root semantics intact
- Association mechanism test-covered, existing suites green
</success_criteria>

<output>
Create `.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-02-SUMMARY.md` when done.
</output>
