---
phase: 15-private-networks-consume-privatenetworkid-identity-and-bind
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build/OSX/Release/CMakeCache.txt
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - example/node_test/network_config.json
  - test/src/account/network_config_private_network_test.cpp
  - test/src/account/CMakeLists.txt
autonomous: false
requirements: [D-01, D-02, D-10, PNET-CFG]

must_haves:
  truths:
    - "The SGNUS Release build tree compiles against the dev_pnets vendored install at /Users/henriqueklein/gnus/3rdparty/build/OSX/Release and the existing pnet pubsub test passes"
    - "A node whose network_config.json contains a valid private_network_id starts with that identity retained and logged (public id only, never network_key)"
    - "A node whose network_config.json omits private_network_id behaves exactly as before (public node)"
    - "A malformed private_network_id (wrong length, non-hex, or all-zero) fails configuration load with an error log instead of starting"
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "NetworkSettings.private_network_id + network_bootstrap_peers members, GeniusNode::private_network_id_ member"
      contains: "private_network_id"
    - path: "src/account/GeniusNode.cpp"
      provides: "LoadNetworkConfig parsing + validation, WriteNetworkConfig emit, InitNetwork retention"
      contains: "private_network_id"
    - path: "test/src/account/network_config_private_network_test.cpp"
      provides: "Config parse/validate/round-trip tests"
      min_lines: 80
  key_links:
    - from: "src/account/GeniusNode.cpp LoadNetworkConfig"
      to: "NetworkSettings.private_network_id"
      via: "read(\"private_network_id\", ...) sibling of network_key read at line 1277"
      pattern: "read\\( \"private_network_id\""
    - from: "src/account/GeniusNode.cpp InitNetwork"
      to: "GeniusNode::private_network_id_"
      via: "retention beside network_key_ assignment (lines 1508-1513)"
      pattern: "private_network_id_ = settings\\.private_network_id"
---

<objective>
Establish the phase's build environment against the dev_pnets vendored install, gate on the D-10 dependency decision, and add the distinct public private-network identity (`private_network_id`) to `network_config.json` parsing — the config surface every later plan consumes.

Purpose: D-01/D-02 make `network_config.json` the offline-provisioned source of both the public identity and the secret pnet key; nothing else in the phase can compile or be tested until the build points at the vendored pnet/gater install, and D-10 requires an explicit go/no-go before implementation proceeds.
Output: Reconfigured `build/OSX/Release`, `private_network_id` (+ `network_bootstrap_peers`) config plumbing in GeniusNode, example config extension, config unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-CONTEXT.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-RESEARCH.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-PATTERNS.md

Commit 0515def3 already landed `network_key` parsing, pnet-mode `StartPubSub`, and `BlockPeer` plumbing. This plan adds the sibling public-identity key only — do not re-implement any `network_key` behavior.

<interfaces>
<!-- Verified in working tree at branch gsd/phase-15-... -->

From src/account/GeniusNode.hpp (NetworkSettings, ~line 1038-1047):
```cpp
struct ... NetworkSettings {
    ...
    std::string network_key;              ///< "network_key" pnet PSK; empty = public network.
};
```

From src/account/GeniusNode.hpp (~954-955):
```cpp
/// Private-network (pnet) PSK from network_config.json ("network_key"); empty = public network.
std::string network_key_;
```

From src/account/GeniusNode.cpp LoadNetworkConfig optional-key reader (~1244-1277):
```cpp
auto read = [&]( const char *key, auto &out ) { /* FindMember + type-checked assign */ };
read( "network_key", settings.network_key );   // line 1277
```

From src/account/GeniusNode.cpp InitNetwork retention (~1508-1513):
```cpp
network_key_ = settings.network_key;
if ( !network_key_.empty() ) { /* log private-network mode enabled */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconfigure SGNUS Release build against the dev_pnets 3rdparty install</name>
  <files>build/OSX/Release/CMakeCache.txt</files>
  <read_first>
  - .planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-RESEARCH.md (sections: Environment Availability, Pitfall 10)
  - build/CommonCompilerOptions.cmake (THIRDPARTY_DIR / THIRDPARTY_BUILD_DIR resolution, lines 156-166)
  </read_first>
  <action>
  The existing `build/OSX/Release` was configured 2026-08-24 against the old keyless sibling `thirdparty` checkout. Re-run cmake in `build/OSX/Release` preserving the cached generator and build type, overriding only the thirdparty location: pass `-DTHIRDPARTY_DIR=/Users/henriqueklein/gnus/3rdparty` (do NOT also set THIRDPARTY_BUILD_DIR; CommonCompilerOptions derives it when the install tree exists). Then build the `pubsub_counts_test` target and run the suite. Do not delete or reconfigure `build/OSX/Debug` in this task.
  </action>
  <verify>
    <automated>grep -q "THIRDPARTY_DIR:PATH=/Users/henriqueklein/gnus/3rdparty" build/OSX/Release/CMakeCache.txt &amp;&amp; ninja -C build/OSX/Release pubsub_counts_test &amp;&amp; ctest --test-dir build/OSX/Release -R pubsub_counts --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `build/OSX/Release/CMakeCache.txt` contains `THIRDPARTY_DIR:PATH=/Users/henriqueklein/gnus/3rdparty`
  - `ninja -C build/OSX/Release pubsub_counts_test` exits 0 — proving `libp2p/security/pnet/psk.hpp` and `ipfs_pubsub/deny_list_connection_gater.hpp` resolve from the dev_pnets install
  - `ctest -R pubsub_counts` exits 0 including `PubsubCounts.PnetIsolationAndGaterBlocking` (the commit-0515def3 two-node PSK test)
  </acceptance_criteria>
  <done>Release build tree is unambiguously bound to the dev_pnets vendored install and the pre-existing pnet test is green on it.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: D-10 dependency gate — confirm implementation base and identity encoding</name>
  <decision>
  (a) Proceed with Phase 15 implementation on the current branch base, given `GeniusVentures/libp2p#10` is OPEN but branch `dev_pnets` (libp2p b28eed2, ipfs-pubsub 3294d41) is fully implemented and installed locally, and the `gsd/phase-13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production` branch is NOT merged into this branch (verified via `git merge-base --is-ancestor` — returns false). (b) Confirm the `private_network_id` JSON encoding: 0x-prefixed hex of exactly 32 bytes (66 chars), all-zero rejected, absent = public — versus decimal uint256.
  </decision>
  <context>
  D-10 locks "wait for libp2p#10 and the trusted-peer closeout branch to land". Research (assumptions A1/A3) flags both as owner confirmations. Plans 15-02..15-08 modify `TrustedPeerRegistry` and `GeniusNode`; if the closeout branch lands later, a rebase will be needed — the later plans keep their TPR changes additive (interface inheritance only) precisely to minimize that risk.
  </context>
  <options>
    <option id="proceed-current-base">
      <name>Proceed on current branch base (recommended)</name>
      <pros>No waiting; dev_pnets is installed and tested; TPR changes in this phase are additive</pros>
      <cons>Later rebase onto the closeout branch when it lands</cons>
    </option>
    <option id="merge-closeout-first">
      <name>Merge/rebase the closeout branch first</name>
      <pros>Eliminates D-10 ambiguity entirely</pros>
      <cons>Blocks the phase on an unreviewed branch; merge conflicts resolved outside this phase's scope</cons>
    </option>
    <option id="wait">
      <name>Wait for both upstreams to land formally</name>
      <pros>Strictest D-10 reading</pros>
      <cons>Indefinite stall; local deliverable already satisfies the technical dependency</cons>
    </option>
  </options>
  <action>
  Blocking decision gate implementing D-10: present the verified dependency state (libp2p#10 OPEN with dev_pnets installed at b28eed2/3294d41; closeout branch NOT an ancestor of HEAD) and collect the owner's choice of implementation base plus the private_network_id encoding before any Wave 2+ plan executes. Record the selected option and encoding verbatim in the plan SUMMARY.
  </action>
  <resume-signal>Select: proceed-current-base, merge-closeout-first, or wait — plus encoding choice: 0x-hex-32B (recommended) or decimal-uint256</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add private_network_id (+ network_bootstrap_peers) config identity</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, example/node_test/network_config.json, test/src/account/network_config_private_network_test.cpp, test/src/account/CMakeLists.txt</files>
  <read_first>
  - src/account/GeniusNode.hpp (NetworkSettings struct ~1038-1047; private members ~950-960)
  - src/account/GeniusNode.cpp (WriteNetworkConfig escaped emit 167-212; LoadNetworkConfig reader 1244-1320; InitNetwork retention 1488-1525; StartPubSub 1384-1452)
  - test/src/account/network_config_precedence_test.cpp (helpers MakeDevConfig/MakeTempDir/UseMemorySecureStorage and the WriteNetworkConfig -> GeniusNode::New scene pattern)
  - test/src/account/CMakeLists.txt
  - .planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-PATTERNS.md (section: GeniusNode config identity)
  </read_first>
  <action>
  Per D-01/D-02: add the public identity as a sibling of the existing secret key, never conflating them. In `NetworkSettings`: add `std::string private_network_id;` with doc comment "public Ed25519 identity from the license NFT; empty = public network" and `std::vector<std::string> network_bootstrap_peers;` with doc comment "offline-provisioned initial NetworkRegistry membership (libp2p PeerId base58 strings); consumed when private_network_id is set". In `GeniusNode` private members beside `network_key_` (~955): `std::string private_network_id_;` and `std::vector<std::string> network_bootstrap_peers_;`. In `LoadNetworkConfig`: `read( "private_network_id", settings.private_network_id );` immediately beside the `network_key` read (line 1277), plus an array read for `network_bootstrap_peers` following the same FindMember/type-check pattern. Add validation after the reads: when `private_network_id` is non-empty it must match 0x-prefixed hex of exactly 32 bytes (66 characters, regex `^0x[0-9a-fA-F]{64}$`) and must not be all-zero (`0x` followed by 64 `0` characters); on violation log via `node_logger_->error` including the reason (never log `network_key`) and fail the load the same way other fatal config divergences fail (mirror the warn-on-ill-typed divergence precedent at lines 1304-1319 but escalate to failure — malformed identity must not start a node). In `InitNetwork` (~1508-1513): retain `private_network_id_` and `network_bootstrap_peers_`, and when non-empty log `private-network identity: {private_network_id_}` — the public id only. In `WriteNetworkConfig`: emit `private_network_id` (plain string, no escaping needed for hex) and the `network_bootstrap_peers` array mirroring the `network_key` emit block at 187-209. Extend `example/node_test/network_config.json` with `"private_network_id": ""` and an empty `"network_bootstrap_peers": []` (placeholder-only values; no real keys). Create `test/src/account/network_config_private_network_test.cpp` using the precedence-test helpers and register it in `test/src/account/CMakeLists.txt` via the existing `addtest` idiom.
  </action>
  <verify>
    <automated>ninja -C build/OSX/Release network_config_private_network_test &amp;&amp; ctest --test-dir build/OSX/Release -R network_config_private_network --output-on-failure &amp;&amp; ctest --test-dir build/OSX/Release -R network_config --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c 'private_network_id' src/account/GeniusNode.cpp` is >= 4 (read, validate, retain, emit)
  - Absent key: `GeniusNode::New` succeeds and `private_network_id_` remains empty (public node, existing tests unaffected)
  - Valid id (e.g. `0x` + 64 non-zero hex chars): parsed, retained, round-trips through `WriteNetworkConfig` -> `LoadNetworkConfig` distinct from `network_key`
  - Malformed ids (63 hex chars, non-hex chars, all-zero) each produce a configuration-load failure with an error log — no node start
  - `example/node_test/network_config.json` contains `"private_network_id": ""` and no real secret material
  - Pre-existing `network_config` account tests still pass (no regression to `network_key` handling)
  </acceptance_criteria>
  <done>The distinct public identity is offline-provisioned via network_config.json, validated at load, retained by GeniusNode, and covered by unit tests; public-node behavior is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> node | Offline-provisioned `network_config.json` content crosses into node startup; malformed values must fail closed |
| node -> logs | Identity/secret material must not leak into log output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | LoadNetworkConfig private_network_id parsing | mitigate | Reject non-66-char, non-hex, all-zero ids at load; fail node start (V5 input validation) |
| T-15-02 | Information Disclosure | InitNetwork logging | mitigate | Log public id only; never log `network_key` bytes (D-03 hygiene) |
| T-15-03 | Tampering | example/node_test/network_config.json | mitigate | Placeholder-only values; no production keys committed |
| T-15-SC | Tampering | package installs | accept | No registry (npm/pip/cargo) packages introduced; all dependencies are first-party vendored checkouts verified by git remote/branch |
</threat_model>

<verification>
- `ctest --test-dir build/OSX/Release -R "pubsub_counts|network_config" --output-on-failure` exits 0
- `git diff example/node_test/network_config.json` contains no secret-looking material
</verification>

<success_criteria>
- Build tree bound to dev_pnets install; existing pnet test green on it
- D-10 decision recorded (option selected + encoding choice) in the plan SUMMARY before Plans 15-02+ execute
- `private_network_id` distinct-from-`network_key` config surface with validation and tests
</success_criteria>

<output>
Create `.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-01-SUMMARY.md` when done. Record the Task 2 checkpoint decision verbatim in the SUMMARY — later plans assume it.
</output>
