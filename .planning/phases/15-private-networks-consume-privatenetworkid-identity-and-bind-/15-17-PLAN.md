---
phase: 15-private-networks-consume-privatenetworkid-identity-and-bind-
plan: 17
type: execute
wave: 1
depends_on: []
gap_closure: true
gaps: [CR-C2-01, WR-C2-01]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - src/securecrdt/SecureCrdt.cpp
  - test/src/account/private_network_registry_binding_test.cpp
  - test/src/networkregistry/network_registry_test.cpp
autonomous: true
requirements: [D-06, D-07, PNET-NETREG, PNET-GATE]

must_haves:
  truths:
    - "On every policy-stack failure path that leaves the GlobalDB running (BurnConfig failure, SecureCrdt::RegisterFilters failure, NetworkRegistry::New failure in INITIALIZING_TRANSACTIONS), a private node's broadcaster still carries a membership filter — an explicit deny-all — so no same-PSK peer can merge raw unsealed CRDT deltas into the stalled node's durable datastore (CR-C2-01 closed)"
    - "A SecureCrdt element filter whose owner weak_ptr has expired REJECTS the element (empty replacement vector) instead of accepting it — the expiry policy now mirrors the candidate filter's reject-on-expiry (WR-C2-01 closed)"
    - "The full-destruction route (ShutdownForDestruction -> ShutdownNow three lines later) still clears the filter — TeardownClearsBroadcasterMembershipFilter stays green"
    - "Public nodes keep byte-identical raw behavior: the teardown clear branch remains a no-op on public nodes (no filter ever installed)"
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "ShutdownNodePolicyServices(bool global_db_shutdown_follows = false) — gated teardown semantics"
      contains: "global_db_shutdown_follows"
    - path: "src/account/GeniusNode.cpp"
      provides: "deny-all-on-private teardown branch using the proven fail-closed helper"
      contains: "MakeBootstrapMembershipFilter( {} )"
    - path: "src/securecrdt/SecureCrdt.cpp"
      provides: "element-filter lambda rejects on weak_self expiry"
      contains: "return std::vector<sgns::crdt::pb::Element>{};"
    - path: "test/src/account/private_network_registry_binding_test.cpp"
      provides: "stalled-node filter regression inside PrivateNodeWithoutBootstrapMembershipFailsClosed"
      contains: "BroadcasterMembershipFilterInstalled"
    - path: "test/src/networkregistry/network_registry_test.cpp"
      provides: "ElementFilterRejectsOnSecureCrdtExpiry — behavioral reject-on-expiry proof"
      contains: "ElementFilterRejectsOnSecureCrdtExpiry"
  key_links:
    - from: "GeniusNode.cpp failure paths (:1084, :1094, :1137)"
      to: "PubSubBroadcasterExt::SetMembershipFilter(MakeBootstrapMembershipFilter({}))"
      via: "ShutdownNodePolicyServices() default parameter on a !private_network_id_.empty() node"
      pattern: "MakeBootstrapMembershipFilter\\( \\{\\} \\)"
    - from: "GeniusNode::ShutdownForDestruction (:2461)"
      to: "PubSubBroadcasterExt::ClearMembershipFilter"
      via: "ShutdownNodePolicyServices(true) immediately before tx_globaldb_->ShutdownNow() (:2464)"
      pattern: "global_db_shutdown_follows=*/ true"
    - from: "SecureCrdt::RegisterFilters element lambda"
      to: "expired-owner policy"
      via: "!weak_self.lock() -> return std::vector<sgns::crdt::pb::Element>{} (reject)"
      pattern: "weak_self.lock"
---

<objective>
Close the sole remaining cycle-3 blocker CR-C2-01 (15-REVERIFICATION-2.md, truth 7 "fail-closed
throughout" FAILED) and its folded warning WR-C2-01: on every policy-stack failure path in
INITIALIZING_TRANSACTIONS, ShutdownNodePolicyServices unconditionally calls ClearMembershipFilter()
(GeniusNode.cpp:2376) on a STILL-LIVE GlobalDB (tx_globaldb_ runs indefinitely, PubSub live, topics
subscribed, node parked in INITIALIZING_TRANSACTIONS) — restoring public pass-through ingest while
the error log at :1129-1136 claims "failing closed". Compounding: :2401 secure_crdt_.reset() while
tx_globaldb_ lives exposes SecureCrdt element filters that fail OPEN on weak_ptr expiry
(SecureCrdt.cpp:686 returns std::nullopt = accept; the candidate filter at :695-703 correctly
returns {} = reject — contradictory policies).

Fix per the report's remediation shape: (1) deny-all-on-private instead of clear — the teardown
never restores public pass-through ingest on a private node whose GlobalDB is still live, with the
clear gated to the destruction route where ShutdownNow() follows; (2) mirror the candidate filter's
reject-on-expiry in the element lambda; (3) regressions: the stalled node keeps a filter installed,
and an expired-owner element filter rejects unsigned remote elements.

Purpose: the fail-closed clause of the owner-sanctioned posture (authenticated membership gates at
all four inbound surfaces) becomes true end-to-end — the exact adversary the posture targets (any
same-PSK peer, raw unsealed publishes) loses the unauthenticated durable-write primitive into a
stalled private node's datastore.
Output: gated teardown semantics in GeniusNode, reject-on-expiry in SecureCrdt, two behavioral
regressions, zero regressions across the phase-15 battery.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-REVERIFICATION-2.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-REVIEW-CYCLE2.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-15-SUMMARY.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-16-SUMMARY.md

## Code facts (verified at HEAD d55401c8, 2026-09-03)

- ShutdownNodePolicyServices (src/account/GeniusNode.cpp:2363-2403): resets
  trust_startup_controller_, then at :2374-2377 unconditionally
  tx_globaldb_->GetBroadcaster()->ClearMembershipFilter(); later resets network_registry_,
  burn_config_, trusted_peer_registry_, and secure_crdt_ (:2401). The comment block at :2369-2373
  rationalizes the clear with "the weak_ptr-backed predicate would deny-all anyway" — that
  rationale is exactly what CR-C2-01 disproved (clearing removes even the deny-all predicate) and
  must be rewritten.
- Exactly four call sites (grep-verified): :1084 (BurnConfig::New failure), :1094
  (SecureCrdt::RegisterFilters failure), :1137 (NetworkRegistry::New failure) — all three return
  immediately after, leaving tx_globaldb_ running indefinitely; and :2461 inside
  ShutdownForDestruction, where tx_globaldb_->ShutdownNow() follows at :2464. Declaration:
  src/account/GeniusNode.hpp:1321 (no parameters).
- On all three failure paths the currently-installed broadcaster filter is the interim
  MakeBootstrapMembershipFilter(network_bootstrap_peers_) installed at :774-784 (the
  registry-backed filter at :1149-1155 installs only after NetworkRegistry::New succeeds). The
  report blesses either "explicit deny-all SetMembershipFilter" or "leave the interim installed" —
  this plan uses the explicit deny-all (stricter: with the policy stack gone, membership authority
  is gone; also uniform regardless of which filter was installed).
- MakeBootstrapMembershipFilter(const std::vector<std::string> &bootstrap_peer_ids) in
  src/networkregistry/NetworkMembershipFilter.hpp: empty set denies ALL (fail-closed, unit-proven
  by BootstrapMembershipFilterSemantics). GeniusNode.cpp already includes this header (used at
  :779). Broadcaster API: SetMembershipFilter(std::function<bool(const PeerId &)>),
  HasMembershipFilter(), ClearMembershipFilter() (pubsub_broadcaster_ext.hpp:126-140).
- SecureCrdt::RegisterFilters element-filter lambda (src/securecrdt/SecureCrdt.cpp:679-687):
  returns strong->FilterSecureCrdtUpdate(entry, element) when weak_self.lock() succeeds, else
  `return std::nullopt;` at :686 (nullopt = ACCEPT/pass-through). The candidate-domain lambda right
  below (:695-703) returns `std::vector<sgns::crdt::pb::Element>{}` on the same expiry (REJECT).
  Datastore filter contract: nullopt = accept unmodified; returned vector = replacement (empty =
  drop). SecureCrdt has NO user-declared destructor (grep-verified) — element filters stay
  registered in the GlobalDB datastore after the SecureCrdt object dies; only explicit
  UnregisterFiltersFor removes a pattern (G-WR-01, NetworkRegistry-only today).
- test/src/account/private_network_registry_binding_test.cpp:
  PrivateNodeWithoutBootstrapMembershipFailsClosed (:197-222) drives the exact CR-C2-01 path
  (empty network_bootstrap_peers -> NetworkRegistry::New fails below the strict-majority floor),
  polls 15s, asserts state == INITIALIZING_TRANSACTIONS and NetworkRegistry(node) == nullptr —
  but nothing about the broadcaster. TeardownClearsBroadcasterMembershipFilter (:241+) drives the
  REAL destruction route and asserts HasMembershipFilter() goes true -> false — this pins the
  destruction-route clear and MUST stay green. Test accessors in
  test/testutil/genius_node_test_access.hpp: BroadcasterMembershipFilterInstalled(node) (:68),
  BroadcasterOf(node) (:81).
- test/src/networkregistry/network_registry_test.cpp fixture: SetUp (:102-127) builds node_ =
  sgns::test::securecrdt::MakeSecureCrdtTestNode("networkregistry") then secure_crdt_ =
  std::make_shared<sgns::securecrdt::SecureCrdt>(node_->db, "networkregistry-test") and calls
  secure_crdt_->RegisterFilters() with an EMPTY registry (only AddListenTopic happens).
  SecureCrdtTestNode (test/src/securecrdt/securecrdt_test_node.hpp:75+) holds ONLY pubsub + db —
  it does NOT hold the SecureCrdt. The attacker two-datastore pattern to mirror:
  IngestFilterCoversLateRegisteredNetworkRegistryPattern (:577-645) and TeardownRemovesIngestFilter
  (:656+) — MakeSecureCrdtTestNode attacker, AddBroadcastTopic + AddListenTopic
  ("networkregistry-test"), node_->pubsub->AddPeers({attacker->pubsub->GetInterfaceAddress()}),
  waitForCondition on connection counts >= 1, then attacker tx->Put(branch_key, garbage) +
  tx->Put(probe_key, probe_value) + tx->Commit({"networkregistry-test"}), control-replicates
  assert, branch-never-lands assert (negated waitForCondition 500ms + Get has_error).
- SecureCrdtRegistryEntry (src/securecrdt/SecureCrdtRegistry.hpp:78-97): default-constructible
  (owner_token = nullptr, peer_registry = nullptr). Registry().Register(key_pattern, entry) sets
  key_pattern and compiles "/?" + key_pattern + "(/sig/[^/]+)?". A bare entry with null
  peer_registry pins NOTHING — resetting the fixture's secure_crdt_ expires weak_self while
  node_->db keeps the registered filter (this is why the expiry test must NOT use MakeRegistry:
  a NetworkRegistry entry capture pins secure_crdt_ via the filter lambda's by-value entry).
- Build/test targets (verified in build/OSX/Release): ninja targets genius_node, securecrdt,
  networkregistry, network_registry_test, private_network_registry_binding_test; matching ctest
  names.

## Do NOT touch (binding residual dispositions)

- 15-04 vendored gater: permanently skipped per deferred-items.md §3 (app-layer posture stands).
- WR-05 shared_from_this pinning (deferred per 15-11); WR-C2-02/WR-C2-03/WR-C2-04 (latent warnings,
  not this cycle); IN-02/IN-03 (optional hardening); full multi-process GeniusNode E2E (stays
  recommended-manual, blocked by pre-existing no-genesis READY-stall base failures per
  deferred-items.md §1).
- The interim install at :774-784, the registry-backed install at :1149-1155, and the failure-path
  log texts at :1082-1084 / :1092-1094 / :1129-1137 stay unchanged — the "failing closed" log claim
  becomes TRUE once this plan lands.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deny-all-on-private teardown + stalled-node filter regression</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/account/private_network_registry_binding_test.cpp</files>
  <read_first>
  - src/account/GeniusNode.cpp:2363-2403 (ShutdownNodePolicyServices full body incl. the :2369-2373 comment to rewrite), :1075-1140 (the three failure paths and the interim/registry install sites), :2455-2465 (ShutdownForDestruction call site + ShutdownNow)
  - src/account/GeniusNode.hpp:1315-1325 (declaration)
  - src/networkregistry/NetworkMembershipFilter.hpp (MakeBootstrapMembershipFilter signature + empty-set deny-all semantics)
  - test/src/account/private_network_registry_binding_test.cpp:191-280 (PrivateNodeWithoutBootstrapMembershipFailsClosed to extend; TeardownClearsBroadcasterMembershipFilter that must stay green)
  - test/testutil/genius_node_test_access.hpp:58-95 (BroadcasterMembershipFilterInstalled / BroadcasterOf accessors)
  </read_first>
  <action>
  CR-C2-01 fix (report missing item 1). Change the GeniusNode.hpp:1321 declaration to
  ShutdownNodePolicyServices( bool global_db_shutdown_follows = false ). In the
  GeniusNode.cpp definition, take the parameter and replace the unconditional broadcaster block
  at :2374-2377 with: when tx_globaldb_ && tx_globaldb_->GetBroadcaster() — if
  ( !private_network_id_.empty() && !global_db_shutdown_follows ), call
  tx_globaldb_->GetBroadcaster()->SetMembershipFilter(
  sgns::networkregistry::MakeBootstrapMembershipFilter( {} ) ) and log a
  node_logger_->warn stating that policy-stack teardown on private network {private_network_id_}
  set gossip ingest to deny-all because the GlobalDB remains live (CR-C2-01 fail-closed; log the
  PUBLIC id only per D-03, never key material); else call ClearMembershipFilter() as today
  (public-node no-op; destruction route). Rewrite the :2369-2373 comment block: the old
  "weak_ptr-backed predicate would deny-all anyway" rationale is the disproven one — the new
  comment states the fail-closed teardown contract: a private node whose policy stack is going
  away must never fall back to public pass-through ingest while its GlobalDB is still live (the
  three failure paths at :1084/:1094/:1137 park the node in INITIALIZING_TRANSACTIONS with PubSub
  running and topics subscribed), and only the destruction route (ShutdownNow three lines later)
  restores the raw state. Update the single call site inside ShutdownForDestruction (:2461) to
  ShutdownNodePolicyServices( /*global_db_shutdown_follows=*/ true ); leave the three failure-path
  call sites on the default (no edit needed). Do NOT touch :774-784, :1149-1155, or the failure
  log texts.
  Regression (report missing item 3): in private_network_registry_binding_test.cpp, inside
  PrivateNodeWithoutBootstrapMembershipFailsClosed, after the existing null-registry assertion
  (:220-221) and before the closing brace, add
  EXPECT_TRUE( GeniusNodeTestAccess::BroadcasterMembershipFilterInstalled( node) ) with a failure
  message naming CR-C2-01 (the policy-stack failure path must not clear the membership filter on
  a still-live GlobalDB; the stalled node's ingest stays deny-all). Mutation evidence for the
  SUMMARY: temporarily restore the unconditional ClearMembershipFilter (revert the branch), rerun
  the scene, confirm the new EXPECT fails, then restore the fix.
  </action>
  <acceptance_criteria>
  - grep -n "global_db_shutdown_follows" src/account/GeniusNode.hpp returns the declaration;
    grep -c "global_db_shutdown_follows=*/ true" src/account/GeniusNode.cpp returns exactly 1
    (the ShutdownForDestruction call site).
  - grep -n "MakeBootstrapMembershipFilter( {} )" src/account/GeniusNode.cpp returns exactly 1
    match (the new deny-all teardown branch; the interim install at :779 passes
    network_bootstrap_peers_, not {}).
  - grep -c "ClearMembershipFilter()" src/account/GeniusNode.cpp returns exactly 1 (now inside
    the else branch — public/destruction route only).
  - The rewritten comment in ShutdownNodePolicyServices no longer contains the phrase
    "would deny-all anyway" (grep -c "would deny-all anyway" src/account/GeniusNode.cpp == 0).
  - test/src/account/private_network_registry_binding_test.cpp:
    PrivateNodeWithoutBootstrapMembershipFailsClosed body contains BroadcasterMembershipFilterInstalled
    (grep within the :197-222 scene), and the suite passes ALL scenes including
    TeardownClearsBroadcasterMembershipFilter (destruction-route clear preserved).
  - ninja -C build/OSX/Release genius_node private_network_registry_binding_test succeeds.
  - Mutation evidence (or the recorded failure output of the reverted-branch run) in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SGNUS && ninja -C build/OSX/Release genius_node private_network_registry_binding_test && ctest -R "^private_network_registry_binding_test$" --test-dir build/OSX/Release --output-on-failure</automated>
  </verify>
  <done>Every policy-stack failure path leaves a deny-all filter installed on a private node's still-live GlobalDB; the destruction route still clears; the stalled-node scene proves it; mutation-verified non-vacuous.</done>
</task>

<task type="auto">
  <name>Task 2: SecureCrdt element-filter reject-on-expiry + behavioral proof</name>
  <files>src/securecrdt/SecureCrdt.cpp, test/src/networkregistry/network_registry_test.cpp</files>
  <read_first>
  - src/securecrdt/SecureCrdt.cpp:666-721 (RegisterFilters: element lambda :679-687 with the nullopt return at :686; candidate lambda :695-703 with the empty-vector return; UnregisterFiltersFor)
  - src/securecrdt/SecureCrdtRegistry.hpp:78-137 (SecureCrdtRegistryEntry defaults; Register signature and pattern compilation)
  - test/src/networkregistry/network_registry_test.cpp:95-145 (fixture SetUp — node_, secure_crdt_, first RegisterFilters with empty registry; MakeRegistry to AVOID), :577-645 (IngestFilterCoversLateRegisteredNetworkRegistryPattern — the attacker pattern to mirror), :656-740 (TeardownRemovesIngestFilter — phase A/B assert shape)
  - test/src/securecrdt/securecrdt_test_node.hpp:70-150 (SecureCrdtTestNode holds pubsub + db only; MakeSecureCrdtTestNode)
  </read_first>
  <action>
  WR-C2-01 fix (report missing item 2, standalone — also covers the healthy-shutdown live window
  :2461-2464 and the TPR-failure path :1070). In SecureCrdt::RegisterFilters' element-filter
  lambda (src/securecrdt/SecureCrdt.cpp:679-687), replace the expired-owner return at :686 —
  return std::nullopt (ACCEPT) — with return std::vector<sgns::crdt::pb::Element>{} (REJECT),
  making it byte-identical in policy to the candidate lambda's expiry return at :702. Add a
  comment on the branch mirroring the candidate filter's: an expired policy owner (weak_self)
  fail-closes its branches on a GlobalDB that keeps running — the owner's FilterSecureCrdtUpdate
  can no longer run, so the element is dropped rather than passed through unfiltered
  (WR-C2-01 / CR-C2-01).
  Regression: add TEST_F( NetworkRegistryTest, ElementFilterRejectsOnSecureCrdtExpiry ) to
  network_registry_test.cpp (new numbered section comment; cite WR-C2-01/CR-C2-01). Do NOT call
  MakeRegistry (its NetworkRegistry entry capture pins secure_crdt_ via the filter lambda's
  by-value entry — weak_self would never expire; the fixture SetUp's secure_crdt_ and bare-entry
  approach avoids all pinning). Steps: (1) default-construct a
  sgns::securecrdt::SecureCrdtRegistryEntry (null peer_registry, null owner_token are fine for
  the expiry path — the lambda never consults the entry when weak_self is expired) and register
  it via secure_crdt_->Registry().Register( "wrc201-expiry-branch", entry ) — the key contains no
  regex specials; (2) assert secure_crdt_->RegisterFilters() returns true (SetUp's first call ran
  with an empty registry, so this call installs the element filter for the new pattern — the
  15-09 re-run idempotence); (3) build the attacker exactly like
  IngestFilterCoversLateRegisteredNetworkRegistryPattern (:588-603): MakeSecureCrdtTestNode,
  AddBroadcastTopic + AddListenTopic( "networkregistry-test" ), AddPeers, waitForCondition on
  both hosts' connection counts >= 1 within 10s; (4) expire the owner:
  secure_crdt_.reset() — nothing else holds a strong reference (SecureCrdtTestNode holds only
  pubsub + db; the bare entry pins nothing; SecureCrdt has no destructor that removes filters),
  so weak_self expires while node_->db keeps the registered filter; verify the fixture TearDown
  tolerates secure_crdt_ == nullptr while reading it; (5) attacker commits ONE delta carrying an
  unsigned garbage element under crdt::HierarchicalKey( "wrc201-expiry-branch" ) (base::Buffer
  with a non-membership string) plus a benign control element under
  crdt::HierarchicalKey( "wrc201-control" ), committed to { "networkregistry-test" }; (6) assert
  the control replicates (waitForCondition node_->db->Get( control ) within 10s — sync path
  works) AND the branch element never lands (negated waitForCondition 500ms +
  node_->db->Get( branch_key ) has_error) — mirroring :634-639; (7) attacker teardown:
  attacker->db->ShutdownNow() then attacker.reset(). Mutation evidence for the SUMMARY: revert
  :686 to return std::nullopt, rerun, confirm the branch element lands and the test fails, then
  restore the fix.
  </action>
  <acceptance_criteria>
  - grep -n "return std::vector<sgns::crdt::pb::Element>{};" src/securecrdt/SecureCrdt.cpp
    returns exactly 2 matches (the element lambda's new expiry return + the candidate lambda's
    existing one at ~:702).
  - grep -c "return std::nullopt;" src/securecrdt/SecureCrdt.cpp within the RegisterFilters
    element lambda is 0 (the function region :666-709 contains no nullopt return; record the
    region inspection in the SUMMARY).
  - test/src/networkregistry/network_registry_test.cpp contains
    ElementFilterRejectsOnSecureCrdtExpiry (visible in ./test_bin/network_registry_test
    --gtest_list_tests after build) and the suite passes ALL cases (>= 19: the 18 from 15-16 plus
    this one).
  - The new test does NOT call MakeRegistry and contains secure_crdt_.reset() before the
    attacker commit (source-order pinned in the SUMMARY).
  - ninja -C build/OSX/Release securecrdt networkregistry network_registry_test succeeds.
  - Mutation evidence (reverted-:686 run failing) recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/henriqueklein/gnus/SGNUS && ninja -C build/OSX/Release securecrdt networkregistry network_registry_test && ctest -R "^network_registry_test$" --test-dir build/OSX/Release --output-on-failure</automated>
  </verify>
  <done>An expired SecureCrdt owner rejects unsigned remote elements on its registered branches instead of accepting them; proven behaviorally against a live attacker datastore; mutation-verified non-vacuous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| same-PSK peer -> stalled private node's live GlobalDB gossip ingest | The CR-C2-01 exposure: node parked in INITIALIZING_TRANSACTIONS with PubSub running and topics subscribed after a policy-stack failure |
| remote CRDT delta -> datastore element filter with expired owner | WR-C2-01: filter lambda still registered after secure_crdt_.reset() on a live GlobalDB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-17-01 | Tampering | ShutdownNodePolicyServices broadcaster block (GeniusNode.cpp:2374-2377) | mitigate | Private node + GlobalDB still live -> SetMembershipFilter(MakeBootstrapMembershipFilter( {} )) deny-all instead of ClearMembershipFilter; same-PSK raw unsealed publishes can no longer merge CRDT deltas/CID announces into the stalled node's durable datastore |
| T-15-17-02 | Tampering | SecureCrdt::RegisterFilters element lambda (SecureCrdt.cpp:686) | mitigate | Reject-on-expiry: return std::vector<sgns::crdt::pb::Element>{} when weak_self expired, mirroring the candidate filter — expired-owner branches (network-registry/<id>, trust, burn-config) deny unsigned remote elements on live GlobalDBs (covers the stalled path :2401, the healthy-shutdown window :2461-2464, and the TPR-failure path :1070) |
| T-15-17-03 | Tampering | Destruction-route clear window (:2461 clear before :2464 ShutdownNow) | accept | Three-line window before the DB stops; behavior pinned by TeardownClearsBroadcasterMembershipFilter; the report explicitly keeps/gates the clear for this route |
| T-15-17-04 | DoS | Deny-all blocks all ingest on the stalled node | accept | The node is parked in INITIALIZING_TRANSACTIONS and never reaches READY by design (15-05 fail-closed posture); recovery is operator restart; stricter is the contract |
| T-15-17-05 | Information disclosure | New teardown warn log | mitigate | Logs the PUBLIC private_network_id_ only (D-03) — never network_key_ or peer key material |
| T-15-17-06 | Tampering | Regression vacuity | mitigate | Both regressions mutation-verified: reverting the deny-all branch fails the stalled-node EXPECT; reverting :686 to nullopt fails the expiry scene — evidence required in the SUMMARY |
| T-15-17-SC | Tampering | package installs | mitigate | No new packages; SGNUS-side changes only |
</threat_model>

<verification>
- Task batteries: ctest -R "^private_network_registry_binding_test$" and ctest -R
  "^network_registry_test$" (both with --output-on-failure against rebuilt binaries).
- Zero-regression battery (the 15-REVERIFICATION-2 spot checks): ctest -R
  "^(network_membership_filter_test|network_registry_test|private_network_registry_binding_test|network_config_private_network_test|securecrdt_registry_test|securecrdt_quorum_gate_test|burnconfig_test|trustedpeerregistry_quorum_test)$"
  --test-dir build/OSX/Release --output-on-failure (8/8 expected).
- Source assertions from both acceptance-criteria blocks recorded in the SUMMARY with line
  numbers (branch guard, single clear site, single true call site, 2 empty-vector expiry returns).
- Mutation evidence for both regressions recorded in the SUMMARY.
</verification>

<success_criteria>
- CR-C2-01 closed: no code path on a private node restores public pass-through ingest while its
  GlobalDB is still live — truth 7 of 15-REVERIFICATION-2 flips to VERIFIED (10/10 expected on
  re-verification).
- WR-C2-01 closed standalone: element-filter and candidate-filter expiry policies agree
  (reject).
- TeardownClearsBroadcasterMembershipFilter and the public-node scenes stay green (public paths
  byte-identical; destruction route unchanged).
- All 8 battery suites green; both new/extended regressions mutation-verified non-vacuous.
</success_criteria>

<output>
Create .planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-17-SUMMARY.md when done
</output>
