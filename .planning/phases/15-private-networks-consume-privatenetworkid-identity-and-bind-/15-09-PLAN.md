---
phase: 15-private-networks-consume-privatenetworkid-identity-and-bind
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/networkregistry/NetworkRegistry.hpp
  - src/networkregistry/NetworkRegistry.cpp
  - test/src/networkregistry/network_registry_test.cpp
autonomous: true
gap_closure: true
requirements: [D-06, PNET-NETREG]

must_haves:
  truths:
    - "After the first network-registry CRDT change notification, the refresh thread runs TryConfirm once per notification and then returns to waiting (no continuous spin)"
    - "Constructing a second NetworkRegistry for an already-registered network id fails with an error AND leaves the first registry fully functional"
    - "Once a NetworkRegistry is constructed, network-registry/<id> CRDT elements receive the SecureCrdt ingest filter (unsigned writes into that branch are rejected)"
  artifacts:
    - path: "src/networkregistry/NetworkRegistry.cpp"
      provides: "drain-once refresh loop, non-destructive duplicate check, post-New filter re-registration"
      contains: "refresh_pending_.store( false"
    - path: "test/src/networkregistry/network_registry_test.cpp"
      provides: "regression tests for WR-02/WR-03/WR-04"
  key_links:
    - from: "NetworkRegistry::New"
      to: "SecureCrdt::RegisterFilters"
      via: "re-run at the end of successful New"
      pattern: "RegisterFilters\\(\\)"
---

<objective>
Close VERIFICATION.md gaps 7-9 (review findings WR-02, WR-03, WR-04 — all code-verified unfixed):

- WR-02: `refresh_pending_` is set (NetworkRegistry.cpp:417) and read in the wait predicate (:445) but
  never cleared — after the first change notification the refresh loop busy-spins (TryConfirm +
  GlobalDB scans at 100% CPU). This becomes LIVE in-node the moment 15-12 wires `tx_globaldb_` as
  the trailing `global_db` argument, so it must be fixed first.
- WR-03: `NetworkRegistry::New` maps `SecureCrdtRegistry::Register`'s "replaced" false to
  `std::errc::file_exists` (:365-367); the destroyed duplicate's `~NetworkRegistry` → `Unregister()`
  then removes the newly inserted entry, leaving the FIRST live registry with no policy entry (all
  subsequent writes fail UNREGISTERED_KEY — the live registry is bricked).
- WR-04: `RegisterFilters()` runs at GeniusNode.cpp:1037 BEFORE `NetworkRegistry::New` at :1059
  (and the controller path has the same ordering via TrustStartupController.cpp:160), so the
  `network-registry/<id>` pattern gets NO ingest filter — remote membership payloads/sig children
  are accepted into the datastore without canonical-signer verification (griefing/reset vector).

Purpose: the NetworkRegistry becomes the live membership authority for app-layer enforcement
(15-11/15-12); its lifecycle must be correct and its CRDT branch ingest-filtered before that.
Output: hardened NetworkRegistry + 3 regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-VERIFICATION.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-REVIEW.md
@src/networkregistry/NetworkRegistry.hpp
@src/networkregistry/NetworkRegistry.cpp
@test/src/networkregistry/network_registry_test.cpp

<interfaces>
From src/securecrdt/SecureCrdtRegistry.hpp (use these directly):
```cpp
// Resolves `key` against all registered patterns; nullopt when unregistered.
std::optional<SecureCrdtRegistryEntry> Resolve( const std::string &key ) const;
std::vector<SecureCrdtRegistryEntry> AllEntries() const;
void UnregisterIf( const std::string &key_pattern, const void *expected_token );
```
From src/securecrdt/SecureCrdt.hpp:
```cpp
bool RegisterFilters();  // snapshots AllEntries(), installs one element filter per pattern; re-runnable
```
Relevant NetworkRegistry.cpp regions (verified this planning pass):
- New: :311-373 (floor checks :330-353; make_shared :355; RegisterSignerSetSource :364-367; callback :368-371)
- RegisterCrdtChangeCallback: :398-435 (sets refresh_pending_ at :417)
- RefreshLoop: :437-460 (wait predicate :442-447 reads refresh_pending_ but nothing ever clears it)
- Unregister: :579-601
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden NetworkRegistry lifecycle (WR-02 drain-once, WR-03 non-destructive duplicate, WR-04 filter re-registration)</name>
  <files>src/networkregistry/NetworkRegistry.hpp, src/networkregistry/NetworkRegistry.cpp</files>
  <read_first>
    - src/networkregistry/NetworkRegistry.cpp (entire file — all three fixes land in New/RefreshLoop)
    - src/networkregistry/NetworkRegistry.hpp (member declarations ~:370-391, New docs ~:196-228)
    - src/securecrdt/SecureCrdtRegistry.hpp (Resolve/UnregisterIf/AllEntries contract)
    - .planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-REVIEW.md (WR-02/WR-03/WR-04 fix sketches)
  </read_first>
  <action>
    Three surgical fixes, all inside NetworkRegistry (no vendored trees, no new deps):

    1. WR-02 — drain-once refresh semantics in `RefreshLoop` (NetworkRegistry.cpp:437-460).
       After the `refresh_stopping_` early-return check (:448-451) and BEFORE calling `TryConfirm()`
       (:454), clear the consumed notification: `refresh_pending_.store( false, std::memory_order_release );`
       (while still holding no lock — the current code unlocks at :452 before TryConfirm; keep that
       ordering). A notification arriving DURING TryConfirm re-sets the flag, so the next loop
       iteration wakes and processes it — no lost refresh, no spin. Do NOT add retry semantics
       (re-setting the flag after a `success(false)` TryConfirm would resurrect the spin).
       Observability seam for Task 2: add member `std::atomic<uint64_t> refresh_attempts_{ 0 };`
       beside `refresh_pending_` (hpp ~:388), increment it immediately before every TryConfirm call
       in RefreshLoop, and expose `uint64_t RefreshAttemptsForTesting() const` (public, documented
       as a test seam) returning the counter load.

    2. WR-03 — non-destructive duplicate check in `New` (:311-373). Before the `std::make_shared`
       call (:355), resolve-first: if `secure_crdt_->Registry().Resolve( DefaultBaseKey( private_network_id ).GetKey() )`
       returns a value, log an error naming the base key (public id only — never key material) and
       return `outcome::failure( std::errc::address_in_use )` WITHOUT having registered anything —
       the live entry is untouched and no instance is created. Keep the existing
       `RegisterSignerSetSource()` failure mapping at :364-367 but change its error code from
       `std::errc::file_exists` to `std::errc::address_in_use` (file_exists is misleading per WR-03)
       and update the hpp doc comment on `New` (~:215-218) to document `address_in_use` as the
       duplicate-registration failure alongside the floor errors.

    3. WR-04 — re-register ingest filters at the end of a successful `New`. After
       `RegisterCrdtChangeCallback()` (:368-371) and before `return instance`, run
       `if ( !instance->secure_crdt_->RegisterFilters() )` → on false, log an error and return
       `outcome::failure( std::errc::io_error )` (the instance is destroyed; its Unregister removes
       only its own token-matched entry — non-destructive). On success, log at info that ingest
       filters were re-registered for the network-registry branch. This covers BOTH wiring paths
       (direct and TrustStartupController) because construction always funnels through `New`.
       RegisterFilters() is re-runnable: it re-snapshots AllEntries() (now including
       network-registry/<id>) and installs the per-pattern element filters (SecureCrdt.cpp:653-696).

    Constraints: no behavior change to ResolveSignerSet/TryConfirm/Unregister semantics; no
    modification of anything under 3rdparty/ or thirdparty/.
  </action>
  <verify>
    <automated>ninja -C build/OSX/Release networkregistry &amp;&amp; grep -c "refresh_pending_.store( false" src/networkregistry/NetworkRegistry.cpp &amp;&amp; grep -c "address_in_use" src/networkregistry/NetworkRegistry.cpp &amp;&amp; grep -c "RegisterFilters" src/networkregistry/NetworkRegistry.cpp</automated>
  </verify>
  <acceptance_criteria>
    - RefreshLoop contains exactly one `refresh_pending_.store( false` between the stopping check and the TryConfirm call (source assertion: grep -n "refresh_pending_.store( false" src/networkregistry/NetworkRegistry.cpp returns 1 in RefreshLoop)
    - `std::errc::address_in_use` returned by the duplicate pre-check BEFORE any make_shared/Register call (source assertion: the Resolve() check appears above the make_shared line in New)
    - NetworkRegistry::New ends with a RegisterFilters() re-run whose failure fails New (source assertion: grep -n "RegisterFilters" src/networkregistry/NetworkRegistry.cpp shows a call inside New after RegisterCrdtChangeCallback)
    - RefreshAttemptsForTesting() declared in hpp and defined in cpp; refresh_attempts_ incremented once per TryConfirm call in RefreshLoop
    - `ninja networkregistry` builds clean
  </acceptance_criteria>
  <done>All three review findings fixed at the source level with the exact semantics above; networkregistry target compiles</done>
</task>

<task type="auto">
  <name>Task 2: Regression tests for the three lifecycle fixes</name>
  <files>test/src/networkregistry/network_registry_test.cpp</files>
  <read_first>
    - test/src/networkregistry/network_registry_test.cpp (entire file — fixture at :96-135, CacheRefreshViaCrdtChangeCallback at :439+, NeverConfirmsWithin negative-window helper at :70-80)
    - src/networkregistry/NetworkRegistry.hpp (New signature, DefaultBaseKey, RefreshAttemptsForTesting)
    - test/src/crdt/crdt_datastore_test.cpp:300-345 (element-filter observable pattern: rejected element → HasKey false)
  </read_first>
  <action>
    Add three TEST_F cases to NetworkRegistryTest (reuse the existing fixture: MakeSecureCrdtTestNode,
    SecureCrdt + RegisterFilters, 3-peer TPR; kPrivateNetworkId constant at :50):

    1. `DuplicateNewDoesNotClobberLiveRegistry` (WR-03): construct registry A with the fixture's
       initial peers (kInitialPeers). Call `NetworkRegistry::New` a second time with the same
       kPrivateNetworkId → assert the result `has_error()` (address_in_use). Then prove registry A
       is still live: `SeedBootstrap(kInitialPeers)` (or ProposeMembershipChange) on A must NOT
       fail with UNREGISTERED_KEY — assert `has_value()`. Pre-fix this call fails because A's
       policy entry was destroyed by the duplicate's teardown. Also assert
       `secure_crdt_->Registry().Resolve(NetworkRegistry::DefaultBaseKey(kPrivateNetworkId).GetKey())`
       still returns a value.

    2. `RefreshLoopDrainsOnceWithoutSpinning` (WR-02): build the CacheRefreshViaCrdtChangeCallback
       scenario (registry with global_db = node_->db so the refresh thread runs), drive the
       change→confirm sequence as that test does, then wait a bounded grace window
       (std::chrono::milliseconds(500), the NeverConfirmsWithin-style negative-window pattern —
       no bare sleep_for outside an assertion loop) and assert `RefreshAttemptsForTesting()`
       grew by at most 1 during the window. Pre-fix the counter climbs continuously (busy-spin).
       Also assert the refresh still WORKS: after a second element arrival the counter increases
       (clearing the flag did not lose notifications).

    3. `IngestFilterCoversLateRegisteredNetworkRegistryPattern` (WR-04): with the fixture's
       call order (RegisterFilters BEFORE NetworkRegistry::New — exactly the production wiring
       order), construct the registry, then attempt to write an UNSIGNED element under the
       registry branch key (`NetworkRegistry::DefaultBaseKey(kPrivateNetworkId).GetKey()`) with
       arbitrary non-empty bytes through `node_->db` (GlobalDB Put/transaction — match how
       GlobalDB is used in test/src/graphsync/pubsub_graphsync_test.cpp:227-247). Assert the
       element does NOT become retrievable (Get on that key fails) — the SecureCrdt ingest
       filter rejected the unsigned write. If a local Put on this base demonstrably does not
       traverse element filters (verify empirically first), prove the same truth with the
       two-datastore sync pattern from test/src/crdt/crdt_datastore_test.cpp:300-345 instead;
       the required observable is identical: an unsigned element under network-registry/<id>
       never lands.
  </action>
  <verify>
    <automated>ctest -R network_registry_test --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest -R network_registry_test` passes 9/9 cases (6 pre-existing + DuplicateNewDoesNotClobberLiveRegistry + RefreshLoopDrainsOnceWithoutSpinning + IngestFilterCoversLateRegisteredNetworkRegistryPattern)
    - DuplicateNew test asserts both the error return AND a subsequent successful ProposeValue/SeedBootstrap on the first registry (the UNREGISTERED_KEY regression is what it pins)
    - Refresh test asserts a bounded attempt count (≤1 growth in the grace window) AND that a second notification still increments attempts (no lost wakeup)
    - Ingest test asserts a Get/HasKey-style negative on the network-registry/<id> key for an unsigned write
  </acceptance_criteria>
  <done>All three gaps have a green regression test proving the fixed behavior; no pre-existing network_registry_test case regressed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote peer → CRDT datastore ingest | gossip-replicated elements under network-registry/<id> cross here (WR-04) |
| library consumer → NetworkRegistry::New | duplicate construction attempts (WR-03) |
| datastore callback → refresh thread | internal handoff (WR-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09-01 | Tampering | network-registry/<id> ingest path | mitigate | WR-04: RegisterFilters() re-run inside New so unsigned/under-signed membership payloads and sig children are rejected at datastore ingest (same coverage trusted-peer-registry/burn-config get) |
| T-15-09-02 | DoS | RefreshLoop thread | mitigate | WR-02: drain-once flag clearing ends the permanent 100%-CPU spin + GlobalDB scan loop; required before 15-12 wires the live refresh in-node |
| T-15-09-03 | Tampering | NetworkRegistry::New duplicate path | mitigate | WR-03: resolve-first pre-check returns address_in_use without registering; the live registry's policy entry is never destroyed |
| T-15-09-04 | Information disclosure | logging | accept | error logs name only the base key / public id; key material never logged (existing D-03 posture, unchanged) |
| T-15-09-SC | Tampering | package installs | accept | no new packages; no vendored tree modifications in this plan |
</threat_model>

<verification>
- ninja networkregistry builds clean
- ctest -R network_registry_test → 9/9 PASS
- Source assertions: drain-once store, address_in_use pre-check, RegisterFilters re-run all greppable in NetworkRegistry.cpp
- Regression guard: ctest -R "trustedpeerregistry|securecrdt|burnconfig" still green (shared SecureCrdt machinery untouched)
</verification>

<success_criteria>
- VERIFICATION gaps 7, 8, 9 each have a green regression test and a source-level fix
- No behavior change for public nodes (NetworkRegistry is only constructed for provisioned private networks)
- WR-02 fixed BEFORE 15-12 wires tx_globaldb_ (dependency recorded in 15-12's depends_on)
</success_criteria>

<output>
Create `.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-09-SUMMARY.md` when done
</output>
