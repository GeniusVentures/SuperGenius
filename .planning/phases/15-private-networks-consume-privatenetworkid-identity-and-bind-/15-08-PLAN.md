---
phase: 15-private-networks-consume-privatenetworkid-identity-and-bind
plan: 08
type: execute
wave: 5
depends_on: ["15-05"]
files_modified:
  - src/processing/impl/processing_core_impl.hpp
  - src/processing/impl/processing_core_impl.cpp
  - src/account/GeniusNode.cpp
  - test/src/processing/processing_core_gating_test.cpp
  - test/src/processing/CMakeLists.txt
autonomous: true
requirements: [D-11, PNET-PROC]

must_haves:
  truths:
    - "The processing host built per ProcessSubTask binds the same private-network enforcement as the gossip host: Noise-only security, pnet when a network key is configured, and the membership gater"
    - "The processing host never offers Plaintext security in any mode"
    - "An invalid network key fails host construction as a caught error (mapped to a ProcessingCoreImpl error), never an uncaught exception or a half-configured host"
    - "A public node's processing path behaves exactly as before (default arguments reproduce the current construction)"
  artifacts:
    - path: "src/processing/impl/processing_core_impl.cpp"
      provides: "MakeGatedHostInjector helper + per-subtask bindings"
      contains: "useSecurityAdaptors"
    - path: "src/processing/impl/processing_core_impl.hpp"
      provides: "network-key/membership construction params + PNET_INITIALIZATION_ERROR"
      contains: "network_key"
    - path: "test/src/processing/processing_core_gating_test.cpp"
      provides: "injector gating + eager-fail tests"
      min_lines: 60
  key_links:
    - from: "src/account/GeniusNode.cpp InitProcessingModules"
      to: "ProcessingCoreImpl::New"
      via: "passes network_key_ + NetworkRegistry membership predicate"
      pattern: "ProcessingCoreImpl::New"
    - from: "src/processing/impl/processing_core_impl.cpp"
      to: "vendored DenyListConnectionGater"
      via: "gater instance + di::bind<network::ConnectionGater> override in the host injector"
      pattern: "ConnectionGater"
---

<objective>
Close the second host-construction path (D-11): `ProcessingCoreImpl` builds an independent libp2p host per `ProcessSubTask` — give it the identical Noise-only + pnet + membership-gater binding set the gossip host has, threaded through its construction from GeniusNode.

Purpose: Securing only the GossipPubSub host leaves a private-network bypass through the processing host, which today uses default Plaintext+Noise with no gater and no pnet.
Output: Gated processing host construction, GeniusNode threading, gating tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-CONTEXT.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-RESEARCH.md (Pitfalls 4, 8; Pattern 1; injector API lines)
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-PATTERNS.md (section: processing_core_impl)
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-05-SUMMARY.md

<interfaces>
<!-- Verified in working tree + installed vendored headers -->

Current anti-pattern at src/processing/impl/processing_core_impl.cpp:92-98 (inside ProcessSubTask):
```cpp
auto injector = libp2p::injector::makeHostInjector(
    libp2p::injector::makeKademliaInjector( libp2p::injector::useKademliaConfig( kademlia_config ) ) );
```
(Default security = Plaintext + Noise; no gater; no pnet.)

From src/processing/impl/processing_core_impl.cpp:33-42:
```cpp
std::shared_ptr<ProcessingCoreImpl> ProcessingCoreImpl::New(
    std::shared_ptr<ProcessingTaskQueue> task_queue, uint32_t maximalProcessingSubTaskCount, TokenID tokenID );
```
Call site: src/account/GeniusNode.cpp:1643 — `ProcessingCoreImpl::New( task_queue_, 1, dev_config_.TokenID )`.

Target binding set (proven in 3rdparty/ipfs-pubsub gossip_pubsub.cpp:173-189 MakeCustomHostInjector):
useSecurityAdaptors<libp2p::security::Noise>() (drops Plaintext) + di::bind<network::ConnectionGater>().TEMPLATE_TO(gater)[di::override] + usePrivateNetwork(key) in pnet mode.

Installed injector API (3rdparty/build/OSX/Release/libp2p/include/libp2p/injector/network_injector.hpp):
useSecurityAdaptors line 208; useConnectionGater line 254; useAllowLoopbackDial line 275; usePrivateNetwork(std::string_view) line 308 (throws PskValidationError eagerly); usePrivateNetwork(Psk) line 341 (exception-free).

From 15-04: DenyListConnectionGater::SetMembershipAllowList(std::function<bool(const PeerId&)>).
From 15-05: GeniusNode::network_registry_ (membership source), network_key_.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gated processing-host construction</name>
  <files>src/processing/impl/processing_core_impl.hpp, src/processing/impl/processing_core_impl.cpp, src/account/GeniusNode.cpp</files>
  <read_first>
  - src/processing/impl/processing_core_impl.hpp (whole file — Error enum, members)
  - src/processing/impl/processing_core_impl.cpp (New/ctor 33-53, ProcessSubTask host construction 85-105, Error ToString mapping at top)
  - /Users/henriqueklein/gnus/3rdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp (MakeCustomHostInjector 142-190 — the binding set to replicate)
  - /Users/henriqueklein/gnus/3rdparty/build/OSX/Release/libp2p/include/libp2p/injector/network_injector.hpp (exact signatures, lines listed above)
  - src/account/GeniusNode.cpp (InitProcessingModules ~1638-1655)
  </read_first>
  <action>
  Extend `ProcessingCoreImpl::New` and the constructor with two trailing defaulted parameters: `std::string network_key = ""` and `std::function<bool( const libp2p::peer::PeerId & )> membership_allow_list = nullptr`; store both as members plus a member `std::shared_ptr<sgns::ipfs_pubsub::DenyListConnectionGater> connection_gater_` constructed in the constructor (with the allow-list applied via `SetMembershipAllowList` when provided). Add an enum value `PNET_INITIALIZATION_ERROR` to the Error enum with a ToString entry "Private-network initialization error". Extract the host construction in `ProcessSubTask` (cpp ~92-98) into a testable static helper declared in the header: `static auto MakeGatedHostInjector( const std::string &network_key, const std::shared_ptr<sgns::ipfs_pubsub::DenyListConnectionGater> &gater, libp2p::protocol::kademlia::Config kademlia_config )` which composes `libp2p::injector::makeHostInjector<di::extension::shared_config>( makeKademliaInjector<di::extension::shared_config>( useKademliaConfig( kademlia_config ) ), libp2p::injector::useSecurityAdaptors<libp2p::security::Noise>(), di::bind<libp2p::network::ConnectionGater>().TEMPLATE_TO( gater )[di::override] )` and, only when `network_key` is non-empty, appends `libp2p::injector::usePrivateNetwork( network_key )`. In `ProcessSubTask`, call the helper inside a try/catch for `std::exception` (PskValidationError arrives as one): on catch, log via the existing logger and return `Error::PNET_INITIALIZATION_ERROR` after `DecProcessingSubTaskCount()` — never proceed with a half-configured host. Keep the kademlia config exactly as today. Update the `GeniusNode.cpp` `InitProcessingModules` call site to pass `network_key_` and — when `network_registry_` exists — a membership predicate mirroring Plan 15-05's (weak capture, `GetCurrentPeers()` contains `remote_peer.toBase58()`, false when registry null); a public node passes the defaults and gets today's construction semantics plus Noise-only security.
  </action>
  <verify>
    <automated>grep -q "useSecurityAdaptors" src/processing/impl/processing_core_impl.cpp &amp;&amp; grep -q "PNET_INITIALIZATION_ERROR" src/processing/impl/processing_core_impl.hpp &amp;&amp; ninja -C build/OSX/Release processing geniusnode &amp;&amp; ctest --test-dir build/OSX/Release -R processing --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "Plaintext" src/processing/impl/processing_core_impl.cpp` returns 0 (no Plaintext adaptor anywhere)
  - `MakeGatedHostInjector` binds Noise-only security, the gater override, and pnet only for non-empty keys (grep shows all three bindings)
  - The try/catch maps pnet failures to `PNET_INITIALIZATION_ERROR` and decrements the in-flight count before returning
  - The GeniusNode call site passes `network_key_` and the registry-backed predicate
  - Existing processing tests pass with default arguments (public path regression-free)
  </acceptance_criteria>
  <done>Both host construction paths enforce the same private-network policy; the processing host is Noise-only by construction.</done>
</task>

<task type="auto">
  <name>Task 2: Processing-host gating tests</name>
  <files>test/src/processing/processing_core_gating_test.cpp, test/src/processing/CMakeLists.txt</files>
  <read_first>
  - src/processing/impl/processing_core_impl.hpp (MakeGatedHostInjector + Error enum)
  - test/src/processing/ (existing tests + CMakeLists — link set including ipfs-pubsub if needed; mirror test/src/pubsub_counts/CMakeLists.txt for libp2p/ipfs-pubsub link lines)
  - test/src/pubsub_counts/pubsub_counts.cpp (valid SWARM_KEY_PNET literal for the positive case)
  </read_first>
  <action>
  Create `test/src/processing/processing_core_gating_test.cpp` and register it in `test/src/processing/CMakeLists.txt` (link `ipfs-pubsub` plus the libp2p targets the pubsub_counts CMakeLists uses). Cases against `ProcessingCoreImpl::MakeGatedHostInjector`: (1) `EmptyKeyBuildsPublicHost` — empty network key + a fresh DenyListConnectionGater builds an injector that creates a Host (injector.create on the host type) without throwing. (2) `ValidKeyBuildsPnetHost` — the well-known SWARM_KEY_PNET literal from pubsub_counts builds successfully. (3) `InvalidKeyFailsEagerly` — a malformed key string (e.g. "/key/swarm/psk/1.0.0/\n/base16/\nZZZZ\n") throws `std::exception` (assert EXPECT_THROW with the processing caller's catch contract in mind). (4) `GaterMembershipRejects` — construct a DenyListConnectionGater with an allow-list admitting exactly one known PeerId and assert `interceptSecured( true, admitted_peer, addr )` succeeds while `interceptSecured( true, other_peer, addr )` returns an error (this pins the D-11 layer at the unit level without a live network). Build any needed PeerId fixtures with the vendored crypto providers the same way existing tests generate keypairs.
  </action>
  <verify>
    <automated>ninja -C build/OSX/Release processing_core_gating_test &amp;&amp; ctest --test-dir build/OSX/Release -R processing_core_gating --output-on-failure &amp;&amp; ctest --test-dir build/OSX/Release -R processing --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - All four cases pass; the invalid-key case proves the eager-throw contract the caller catches
  - The gater case proves membership rejection at `interceptSecured` for the processing host's gater instance
  - Full processing suite green (public defaults regression-free)
  </acceptance_criteria>
  <done>D-11 is verified: no Plaintext offer, pnet honored, membership enforced, and failures surface as errors rather than exceptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote peer -> processing host | Per-subtask host performs model/result exchange; previously offered unauthenticated Plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-25 | Spoofing | Plaintext security on processing host | mitigate | useSecurityAdaptors<Noise> binding; grep gate asserts no Plaintext remains (D-11) |
| T-15-26 | Elevation of Privilege | ungated private-network bypass via processing host | mitigate | Same pnet + membership gater bindings as gossip host; D-11 both-paths rule |
| T-15-27 | Denial of Service | uncaught PskValidationError from per-subtask injector | mitigate | try/catch -> PNET_INITIALIZATION_ERROR + count decrement; eager-throw test |
| T-15-SC | Tampering | package installs | accept | No registry packages; first-party vendored only |
</threat_model>

<verification>
- `ctest --test-dir build/OSX/Release -R processing --output-on-failure` exits 0
- `grep -c "Plaintext" src/processing/impl/processing_core_impl.cpp` returns 0
</verification>

<success_criteria>
- Processing host Noise-only, pnet-capable, membership-gated
- Public processing path unchanged under default arguments
- Error contract (no leaked exceptions) test-proven
</success_criteria>

<output>
Create `.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-08-SUMMARY.md` when done.
</output>
