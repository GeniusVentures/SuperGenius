---
phase: 15-private-networks-consume-privatenetworkid-identity-and-bind
plan: 03
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - src/networkregistry/NetworkRegistry.hpp
  - src/networkregistry/NetworkRegistry.cpp
  - src/networkregistry/CMakeLists.txt
  - src/CMakeLists.txt
  - test/src/networkregistry/network_registry_test.cpp
  - test/src/networkregistry/CMakeLists.txt
  - test/src/CMakeLists.txt
autonomous: true
requirements: [D-03, D-05, D-06, PNET-NETREG]

must_haves:
  truths:
    - "A NetworkRegistry exists per privateNetworkId, implemented on SecureCRDT/ISignedCRDTData exactly like TrustedPeerRegistry (no bespoke signature protocol)"
    - "Its bootstrap record confirms only with a majority of the global TrustedPeerRegistry's current peers; under-signed bootstrap never confirms"
    - "After confirmation, membership changes require the network's own cached-peer quorum; a single peer can never admit itself"
    - "Serialized registry records contain only non-secret metadata (peer list + key version/fingerprint) — never raw pnet key material"
    - "Signer-set resolution reads cached state only (never re-enters ReadIfQuorum)"
  artifacts:
    - path: "src/networkregistry/NetworkRegistry.hpp"
      provides: "NetworkRegistry + NetworkMembershipPayload (ISignedCRDTData)"
      contains: "class NetworkRegistry"
    - path: "src/networkregistry/NetworkRegistry.cpp"
      provides: "bootstrap-from-TPR lifecycle, cached self-governance, TryConfirm"
      min_lines: 150
    - path: "test/src/networkregistry/network_registry_test.cpp"
      provides: "bootstrap quorum, self-governance, unilateral-admission, secret-exclusion tests"
      min_lines: 120
  key_links:
    - from: "src/networkregistry/NetworkRegistry.cpp"
      to: "src/trustedpeer/TrustedPeerRegistry.hpp"
      via: "bootstrap authority: TPR current peers + TPR-majority threshold"
      pattern: "TrustedPeerRegistry"
    - from: "src/networkregistry/NetworkRegistry.cpp"
      to: "src/securecrdt/SecureCrdt.hpp"
      via: "propose/sign/TryConfirm via ReadIfQuorum"
      pattern: "ReadIfQuorum"
    - from: "src/networkregistry/NetworkRegistry.cpp"
      to: "src/securecrdt/QuorumThresholdValidation.hpp"
      via: "double floor validation at New"
      pattern: "ValidateQuorumThreshold"
---

<objective>
Build the SecureCRDT-backed `NetworkRegistry` child trust domain (D-06): a per-`privateNetworkId` membership registry whose bootstrap record is signed by a majority of the global TrustedPeerRegistry and which afterwards governs itself from its own cached peer set and quorum.

Purpose: This registry is the D-07 allow-list source consumed by Plan 15-05's connection gater and holds only non-secret pnet metadata (D-03).
Output: New `src/networkregistry` module + quorum/isolation test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-CONTEXT.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-PATTERNS.md (section: NetworkRegistry.{hpp,cpp} — copy targets with line numbers)
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-RESEARCH.md (Pattern 3, Pitfall 9, Shared Pattern: quorum floor)

<interfaces>
<!-- Verified in working tree; produced by 15-02 -->

From src/peerregistry/PeerRegistry.hpp (created in 15-02):
```cpp
class PeerRegistry {
public:
    virtual ~PeerRegistry() = default;
    virtual outcome::result<securecrdt::SignerSetSnapshot> CurrentSignerSet() const = 0;
    virtual std::vector<std::string> GetCurrentPeers() const = 0;
    virtual crdt::HierarchicalKey BaseKey() const = 0;
};
```

From src/trustedpeer/TrustedPeerRegistry.hpp (authoritative template — copy the lifecycle):
```cpp
static outcome::result<std::shared_ptr<TrustedPeerRegistry>> New(
    std::shared_ptr<sgns::securecrdt::SecureCrdt> secure_crdt,
    std::vector<std::string>                      genesis_peers,
    std::string                                   bootstrapper_address,
    uint64_t                                      quorum_threshold,
    sgns::crdt::HierarchicalKey base_key = sgns::crdt::HierarchicalKey( "trusted-peer-registry" ) );
// ResolveSignerSet (cpp:180-188): cached-only — {bootstrapper,1} pre-confirm, {cached_peers_,threshold} post
// TryConfirm (cpp:230-265): ReadIfQuorum -> FromBytes -> Verify -> Apply -> overwrite cache + set confirmed
// RegisterSignerSetSource (cpp:160-178): weak_self capture + owner_token + SecureCrdtRegistry::Register
```

From src/securecrdt/QuorumThresholdValidation.hpp:
```cpp
inline outcome::result<void> ValidateQuorumThreshold( uint64_t threshold, size_t signer_set_size )
{ /* floor = (N*51+99)/100; below -> QUORUM_THRESHOLD_BELOW_FLOOR */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NetworkRegistry core module</name>
  <files>src/networkregistry/NetworkRegistry.hpp, src/networkregistry/NetworkRegistry.cpp, src/networkregistry/CMakeLists.txt, src/CMakeLists.txt</files>
  <read_first>
  - src/trustedpeer/TrustedPeerRegistry.hpp (whole file — class shape to mirror)
  - src/trustedpeer/TrustedPeerRegistry.cpp (whole file — payload Verify 73-114, New 120-158, RegisterSignerSetSource 160-178, ResolveSignerSet 180-188, TryConfirm 230-265)
  - src/peerregistry/PeerRegistry.hpp (interface to implement)
  - src/securecrdt/SecureCrdt.hpp (propose/sign/ReadIfQuorum API)
  - src/securecrdt/QuorumThresholdValidation.hpp
  - src/account/BurnConfig.hpp (RegisterNewElementCallback cache-refresh pattern, doc block 100-127)
  - src/trustedpeer/CMakeLists.txt
  - src/CMakeLists.txt
  </read_first>
  <action>
  Copy the TrustedPeerRegistry skeleton and swap the authority per D-06. Header `src/networkregistry/NetworkRegistry.hpp`, namespace `sgns::networkregistry`: co-locate payload class `NetworkMembershipPayload : public sgns::securecrdt::ISignedCRDTData` with fields `std::vector<std::string> network_peers` (entries are libp2p PeerId base58 strings so the connection gater can compare directly), `uint32_t pnet_key_version = 1`, and `std::string pnet_key_fingerprint` (short hex, optional) — NOTHING else may be serialized (D-03: raw key never stored). `SerializeToBytes`/`FromBytes` via an explicit field layout of your choosing; `Verify()` is structural-only exactly like TrustedPeerRegistry.cpp:73-114 — non-empty peer list, no duplicate entries, each entry non-empty and starting with `Qm` or `12D3KooW` (valid PeerId multihash prefixes) — never diff against cached state. `Apply()` overwrites the owning registry's cached membership. Class `NetworkRegistry : public std::enable_shared_from_this<NetworkRegistry>, public sgns::peerregistry::PeerRegistry` with factory `static outcome::result<std::shared_ptr<NetworkRegistry>> New( std::shared_ptr<sgns::securecrdt::SecureCrdt> secure_crdt, std::shared_ptr<sgns::trustedpeer::TrustedPeerRegistry> global_trusted_peers, std::string private_network_id, std::vector<std::string> initial_network_peers, uint64_t network_quorum_threshold )` plus `static sgns::crdt::HierarchicalKey DefaultBaseKey( const std::string &private_network_id )` returning `sgns::crdt::HierarchicalKey( "network-registry" ).ChildString( private_network_id )` (literal per-network base key — one registration per active network, no defaulted collision-prone arg). `New` validates the quorum floor TWICE with `securecrdt::ValidateQuorumThreshold`: once for the bootstrap threshold computed as `(global_tpr_set_size * 51 + 99) / 100` over the TPR's current `GetCurrentPeers().size()`, and once for `network_quorum_threshold` over `initial_network_peers.size()`; either failure returns `QUORUM_THRESHOLD_BELOW_FLOOR` with no instance. The constructor caches `initial_network_peers` immediately (TPR genesis-cache pattern) and snapshots the TPR bootstrap authority. Methods mirroring TPR: `SeedBootstrap( const std::vector<std::string> &initial_network_peers )` (proposes the payload through SecureCrdt; does not self-sign — TPR member nodes add signatures through the standard propose/sign flow), `TryConfirm()` (ReadIfQuorum -> FromBytes -> Verify -> Apply -> unique lock, overwrite `cached_network_peers_`, set `bootstrap_confirmed_`), `RegisterSignerSetSource()` (entry with weak_self capture, `make_instance` returning a `NetworkMembershipPayload`, `owner_token = &registry_token_`, registered under the base key), `Unregister()`, and PeerRegistry overrides. `CurrentSignerSet()`/internal `ResolveSignerSet()` is cached-only under `mutable std::shared_mutex cache_mutex_`: pre-confirmation it returns the TPR snapshot `{ global_trusted_peers_->GetCurrentPeers(), tpr_majority_threshold_ }`; post-confirmation `{ cached_network_peers_, network_quorum_threshold_ }` — it must NEVER call `ReadIfQuorum` (Pitfall 9). Add a CRDT change callback (BurnConfig `RegisterNewElementCallback` pattern) that triggers `TryConfirm()` so later quorum-signed membership changes refresh the cache. Logger `sgns::base::createLogger( "networkregistry" )`. CMakeLists mirrors `src/trustedpeer/CMakeLists.txt` (library `networkregistry`, links `securecrdt peerregistry trustedpeer` as needed, `supergenius_install(networkregistry)`); add `add_subdirectory(networkregistry)` to `src/CMakeLists.txt`.
  </action>
  <verify>
    <automated>grep -q "class NetworkRegistry" src/networkregistry/NetworkRegistry.hpp &amp;&amp; grep -q "ValidateQuorumThreshold" src/networkregistry/NetworkRegistry.cpp &amp;&amp; ninja -C build/OSX/Release networkregistry</automated>
  </verify>
  <acceptance_criteria>
  - `src/networkregistry/NetworkRegistry.hpp` contains `NetworkMembershipPayload`, `SeedBootstrap`, `TryConfirm`, `DefaultBaseKey`
  - `NetworkMembershipPayload` serializable fields are exactly: network_peers, pnet_key_version, pnet_key_fingerprint (grep confirms no key/password/secret field names)
  - `NetworkRegistry.cpp` calls `ValidateQuorumThreshold` at least twice (bootstrap + self-governance floors)
  - `ResolveSignerSet`/`CurrentSignerSet` body contains no `ReadIfQuorum` call (re-entrancy guard)
  - `ninja -C build/OSX/Release networkregistry` exits 0
  </acceptance_criteria>
  <done>NetworkRegistry compiles with the TPR-lifecycle (bootstrap-from-TPR-majority, cached self-governance, double quorum floor) and secret-free payload.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap/self-governance/secret-exclusion tests</name>
  <files>test/src/networkregistry/network_registry_test.cpp, test/src/networkregistry/CMakeLists.txt, test/src/CMakeLists.txt</files>
  <read_first>
  - test/src/trustedpeer/trustedpeerregistry_genesis_test.cpp (whole file — fixture, precomputed-signature helpers, `SignatureOverMismatchedPayloadNeverConfirms` shape)
  - test/src/securecrdt/securecrdt_test_node.hpp (MakeSecureCrdtTestNode)
  - test/src/securecrdt/securecrdt_test.cpp (propose/sign/quorum test mechanics)
  - src/networkregistry/NetworkRegistry.hpp (the unit under test)
  - test/src/trustedpeer/CMakeLists.txt (link set to copy)
  - test/src/CMakeLists.txt
  </read_first>
  <action>
  Create `test/src/networkregistry/network_registry_test.cpp` with the trustedpeer genesis-test fixture shape (SetUp: `MakeSecureCrdtTestNode("networkregistry")`, `SecureCrdt` on a `"networkregistry-test"` topic, `RegisterFilters()`; TearDown: explicit `Unregister()` then reset in reverse dependency order). Build a real `TrustedPeerRegistry` with a 3-peer genesis set as the global authority, then exercise: (1) `BootstrapUnderTprMajorityConfirms` — SeedBootstrap + signatures from only 1 of 3 TPR peers -> `TryConfirm()` returns false and `GetCurrentPeers()` still equals the initial set; with 2 of 3 TPR-peer signatures (the ceil(0.51*3) majority) -> `TryConfirm()` returns true and the cache reflects the record. (2) `SelfGovernanceAfterConfirm` — post-confirmation, propose a membership change signed by fewer than the network quorum -> never confirms; signed by quorum of cached network peers -> confirms and cache updates. (3) `SinglePeerCannotAdmitItself` — a membership change adding a new peer signed only by that new peer (and not by the quorum) never confirms; membership unchanged. (4) `NoRawKeyMaterialInRecords` — serialize the proposed payload bytes and assert the test network_key string (embed a distinctive sentinel like a fixed 32-byte test PSK) does not appear as a substring; assert the payload JSON/binary contains the version/fingerprint fields but no key bytes. (5) `UnconfirmedResolveUsesTprSnapshot` — before confirmation `CurrentSignerSet()` returns the TPR peer set with the TPR-majority required count. Use `ASSERT_WAIT_FOR_CONDITION`/`test::assertWaitForCondition` from `test/testutil/wait_condition.hpp` — no raw `sleep_for`. Register via new `test/src/networkregistry/CMakeLists.txt` (addtest + link `networkregistry trustedpeer securecrdt` + include dir `${CMAKE_CURRENT_SOURCE_DIR}/..`) and `add_subdirectory(networkregistry)` in `test/src/CMakeLists.txt`.
  </action>
  <verify>
    <automated>ninja -C build/OSX/Release network_registry_test &amp;&amp; ctest --test-dir build/OSX/Release -R network_registry --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `network_registry_test` binary builds and all five cases pass
  - The under-signed-bootstrap and self-admission cases assert BOTH the negative (TryConfirm false / membership unchanged) within a bounded wait window
  - `grep -c "sleep_for" test/src/networkregistry/network_registry_test.cpp` returns 0
  - `ctest -R network_registry` exits 0
  </acceptance_criteria>
  <done>D-06's trust lifecycle and D-03's secret exclusion are proven by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRDT replicated records -> registry cache | Quorum-signed membership records cross from replicated storage into the authorization cache |
| registry cache -> connection gater (consumed in 15-05) | Membership decisions gate peer authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Elevation of Privilege | NetworkRegistry bootstrap | mitigate | Bootstrap confirms only at TPR-majority; under-signed case test-proven to never confirm |
| T-15-08 | Elevation of Privilege | post-confirm membership updates | mitigate | Own-quorum + ValidateQuorumThreshold majority floor; unilateral self-admission test-proven rejected |
| T-15-09 | Information Disclosure | NetworkMembershipPayload | mitigate | Only version/fingerprint metadata; test asserts sentinel key bytes absent from serialized records (D-03) |
| T-15-10 | Denial of Service | ResolveSignerSet re-entrancy | mitigate | Cached-only resolution under shared_mutex; no ReadIfQuorum on the signer-set path |
| T-15-SC | Tampering | package installs | accept | No registry packages; in-repo + first-party vendored only |
</threat_model>

<verification>
- `ctest --test-dir build/OSX/Release -R "network_registry|trustedpeer|securecrdt" --output-on-failure` exits 0
- `grep -c "network_key" src/networkregistry/NetworkRegistry.hpp` is 0 (no secret field in the payload header)
</verification>

<success_criteria>
- NetworkRegistry is a SecureCRDT consumer (no parallel signature protocol) with per-network base keys
- TPR-majority bootstrap and self-governance both floor-checked and test-covered
- Payload is metadata-only by construction and by test
</success_criteria>

<output>
Create `.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-03-SUMMARY.md` when done.
</output>
