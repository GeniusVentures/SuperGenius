---
phase: 15-private-networks-consume-privatenetworkid-identity-and-bind
plan: 05
type: execute
wave: 3
depends_on: ["15-03", "15-04"]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/pubsub_counts/pubsub_counts.cpp
autonomous: true
requirements: [D-06, D-07, PNET-GATE, PNET-NETREG]

must_haves:
  truths:
    - "A node configured with private_network_id constructs a NetworkRegistry at startup (after TrustedPeerRegistry) and feeds its cached membership as the GossipPubSub allow-list"
    - "Two same-PSK peers that are in each other's membership allow-list mesh and exchange messages"
    - "A same-PSK peer NOT in the allow-list is rejected at connection upgrade and never meshes"
    - "In private mode with no/empty membership the gater fails closed (rejects) rather than failing open"
    - "A public node (no private_network_id) constructs no NetworkRegistry and its pubsub behavior is unchanged"
  artifacts:
    - path: "src/account/GeniusNode.cpp"
      provides: "NetworkRegistry construction + allow-list binding in StartPubSub"
      contains: "NetworkRegistry::New"
    - path: "test/src/pubsub_counts/pubsub_counts.cpp"
      provides: "PrivateNetworkMembershipGating test"
      contains: "PrivateNetworkMembershipGating"
  key_links:
    - from: "src/account/GeniusNode.cpp StartPubSub"
      to: "GossipPubSub::SetMembershipAllowList"
      via: "predicate over NetworkRegistry::GetCurrentPeers (PeerId base58 compare)"
      pattern: "SetMembershipAllowList"
    - from: "src/account/GeniusNode.cpp INITIALIZING_TRANSACTIONS"
      to: "src/networkregistry/NetworkRegistry.hpp"
      via: "NetworkRegistry::New(secure_crdt_, trusted_peer_registry_, private_network_id_, ...)"
      pattern: "NetworkRegistry::New"
---

<objective>
Bind the private-network enforcement to the GossipPubSub host (D-07, D-11 gossip half): GeniusNode constructs the NetworkRegistry in the INITIALIZING_TRANSACTIONS quorum-trio path and passes its cached membership as the gossip host's allow-list, with fail-closed semantics — proven by two-node integration tests.

Purpose: pnet proves possession of the private credential; this plan adds the second, independent layer that decides whether that peer is authorized for the selected private network.
Output: Wired GeniusNode + membership gating integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-CONTEXT.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-RESEARCH.md (Architecture diagram, Pitfalls 1/4/9, Open Question 4)
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-PATTERNS.md (sections: GeniusNode pnet-mode construction, test fixtures)
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-01-SUMMARY.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-03-SUMMARY.md
@.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-04-SUMMARY.md

<interfaces>
<!-- Verified in working tree + produced by 15-01/15-03/15-04 -->

From src/account/GeniusNode.cpp INITIALIZING_TRANSACTIONS (lines 735-767):
```cpp
secure_crdt_ = std::make_shared<sgns::securecrdt::SecureCrdt>( tx_globaldb_, quorum_topic );
auto tpr_result = sgns::trustedpeer::TrustedPeerRegistry::New( secure_crdt_, /*genesis peers*/ ... );
trusted_peer_registry_ = tpr_result.value();
// BurnConfig::New( secure_crdt_, ..., trusted_peer_registry_, ... ) follows
```

From src/account/GeniusNode.hpp (884-890): destructor-ordering note — SecureCrdt declared first, then trusted_peer_registry_ (their destructors call Unregister() which needs SecureCrdt alive). NetworkRegistry must join this ordering.

From src/account/GeniusNode.cpp StartPubSub (1384-1452): pnet-mode ctor in try/catch; fail-lambda tears down pubsub_ on any later failure.

From 15-03: sgns::networkregistry::NetworkRegistry::New( secure_crdt, global_trusted_peers, private_network_id, initial_network_peers, network_quorum_threshold ); GetCurrentPeers() returns cached PeerId-base58 strings.

From 15-04: void GossipPubSub::SetMembershipAllowList( std::function<bool( const libp2p::peer::PeerId & )> ) — call after construction, before Start.

From test/src/pubsub_counts/pubsub_counts.cpp: SWARM_KEY_PNET/SWARM_KEY_OUTSIDE constants (38-45), PnetIsolationAndGaterBlocking (140-261) — Start(0,{}), AddPeers({other->GetInterfaceAddress()}), grace-window negative-assertion loop (235-252).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GeniusNode NetworkRegistry construction + gossip allow-list binding</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp</files>
  <read_first>
  - src/account/GeniusNode.hpp (forward decls ~60-70; quorum-trio members + destructor note 884-895; private_network_id_ from 15-01)
  - src/account/GeniusNode.cpp (INITIALIZING_TRANSACTIONS 735-767; StartPubSub 1384-1452; teardown Unregister 1780-1800)
  - src/networkregistry/NetworkRegistry.hpp (15-03 output: New signature, GetCurrentPeers, Unregister)
  - src/securecrdt/QuorumThresholdValidation.hpp (floor formula for default threshold)
  </read_first>
  <action>
  In `GeniusNode.hpp`: forward-declare `namespace sgns::networkregistry { class NetworkRegistry; }` beside the existing securecrdt/trustedpeer forward declarations, and add member `std::shared_ptr<sgns::networkregistry::NetworkRegistry> network_registry_;` immediately AFTER `trusted_peer_registry_` (it must be destroyed BEFORE secure_crdt_, joining the existing destructor-ordering chain — its destructor also calls Unregister()). In `GeniusNode.cpp` INITIALIZING_TRANSACTIONS case (735-767): after `trusted_peer_registry_` is successfully constructed AND when `private_network_id_` is non-empty, call `sgns::networkregistry::NetworkRegistry::New( secure_crdt_, trusted_peer_registry_, private_network_id_, network_bootstrap_peers_, default_threshold )` where `default_threshold` is the ValidateQuorumThreshold floor over `network_bootstrap_peers_.size()` (i.e. `(N*51+99)/100`); on failure log via `node_logger_->error` and fail the state transition the same way the TPR failure path does. In `StartPubSub` (1384-1452): inside the private-mode branch (network_key non-empty), after the pnet-constructor try/catch succeeds and BEFORE `Start` is invoked, call `pubsub_->SetMembershipAllowList( membership_predicate )` where the predicate is a lambda capturing `this` weakly: it returns false when `network_registry_` is null or the registry's `GetCurrentPeers()` does not contain `remote_peer.toBase58()` (fail-closed per D-07 and the 15-04 gater semantics — an unconfirmed/empty registry rejects everyone in private mode; log one warning when private mode runs with an empty membership so operators can diagnose provisioning). Log the association at info level using the PUBLIC `private_network_id_` only — never `network_key_`. In the `~GeniusNode` teardown region (1780-1800): mirror the trustedpeer block — `if ( network_registry_ ) { network_registry_->Unregister(); } network_registry_.reset();` ordered before the trusted_peer_registry_ reset. Do not alter the public-mode branch: no `private_network_id_` means no NetworkRegistry and no allow-list call, leaving public behavior byte-identical.
  </action>
  <verify>
    <automated>grep -q "NetworkRegistry::New" src/account/GeniusNode.cpp &amp;&amp; grep -q "SetMembershipAllowList" src/account/GeniusNode.cpp &amp;&amp; ninja -C build/OSX/Release geniusnode &amp;&amp; ctest --test-dir build/OSX/Release -R "trustedpeer|securecrdt|burn" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "network_registry_" src/account/GeniusNode.cpp` >= 4 (construct, bind, teardown-unregister, reset)
  - The allow-list predicate compares against `GetCurrentPeers()` output and returns false on null registry (fail-closed) — grep shows the null check
  - `network_registry_` member is declared after `trusted_peer_registry_` in GeniusNode.hpp
  - Public-mode branch untouched: `git diff src/account/GeniusNode.cpp` shows the StartPubSub public path unchanged
  - Quorum-trio regression suites (trustedpeer, securecrdt) green
  </acceptance_criteria>
  <done>The gossip host of a private node is gated by NetworkRegistry membership on top of pnet, with fail-closed empty-registry semantics and unchanged public behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Two-node membership gating integration tests</name>
  <files>test/src/pubsub_counts/pubsub_counts.cpp</files>
  <read_first>
  - test/src/pubsub_counts/pubsub_counts.cpp (whole file — SWARM_KEY constants 38-45, PnetIsolationAndGaterBlocking 140-261 including the grace-window loop 235-252, GenerateKeyPair/MakeGossipConfig helpers, GetInterfaceAddress usage)
  - test/testutil/wait_condition.hpp (wait-condition macros — mandatory, no sleep_for)
  </read_first>
  <action>
  Add `TEST_F( PubsubCounts, PrivateNetworkMembershipGating )` reusing the file's existing fixtures verbatim: same `SWARM_KEY_PNET` for inside nodes, `GenerateKeyPair()`, `MakeGossipConfig()`, `Start( 0, {} )` then `AddPeers( { other->GetInterfaceAddress() } )` (interface addresses — loopback dialing is denied by default). Scenario: nodes A and B constructed with the pnet constructor; A is given `SetMembershipAllowList` admitting exactly B's PeerId (obtain it the same way the existing test obtains the blocked peer's id around line 179), B admits A. Assert A-B mesh and a published message on a shared topic is received on both sides via the wait-condition macros. Negative 1: node C with the SAME PSK but not admitted by A's allow-list — assert A and C never become connected within the existing grace-window pattern (copy the loop shape from lines 235-252). Negative 2 (fail-closed): node D with the same PSK whose allow-list predicate admits nothing (empty membership) — assert D meshes with nobody in the window. Runtime admission: after the negative window, extend A's allow-list to also admit C (re-call `SetMembershipAllowList` with the widened set) and assert A-C subsequently connect within the wait window — proving the gater consults live registry state, not a startup snapshot.
  </action>
  <verify>
    <automated>ninja -C build/OSX/Release pubsub_counts_test &amp;&amp; ctest --test-dir build/OSX/Release -R pubsub_counts --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `PrivateNetworkMembershipGating` case exists and passes alongside the pre-existing `PnetIsolationAndGaterBlocking` (different-PSK rejection still green — regression intact)
  - The same-PSK-not-in-allow-list negative assertion uses the grace-window loop, not a fixed sleep; `grep -c "sleep_for" test/src/pubsub_counts/pubsub_counts.cpp` does not increase
  - The runtime-admission step demonstrates a peer connecting only after the allow-list widens
  - `ctest -R pubsub_counts` exits 0
  </acceptance_criteria>
  <done>D-07's two independent layers are integration-proven: pnet excludes wrong-credential peers, membership excludes unauthorized-but-correct-credential peers, and admission is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| same-PSK remote peer -> gossip host | Correct network credential but possibly unauthorized peer; allow-list decides |
| registry cache -> gater predicate | Authorization state crosses from CRDT-governed cache into the connection path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | Elevation of Privilege | valid-PSK unauthorized peer | mitigate | Membership allow-list bound in private mode; same-PSK-not-admitted test-proven rejected (D-07) |
| T-15-16 | Elevation of Privilege | unconfirmed/empty registry | mitigate | Fail-closed predicate (false on null/empty); Negative 2 test |
| T-15-17 | Information Disclosure | startup logging | mitigate | Log public id and membership size only; never network_key bytes |
| T-15-18 | Tampering | allow-list wiring omission | mitigate | grep gate asserts SetMembershipAllowList present in StartPubSub private branch; public branch diff-clean |
| T-15-SC | Tampering | package installs | accept | No registry packages; first-party vendored only |
</threat_model>

<verification>
- `ctest --test-dir build/OSX/Release -R "pubsub_counts|trustedpeer|securecrdt" --output-on-failure` exits 0
- `git diff src/account/GeniusNode.cpp` shows no change inside the public-mode StartPubSub branch
</verification>

<success_criteria>
- Private node: pnet + membership allow-list both active on the gossip host
- Fail-closed empty-registry semantics tested
- Public node path byte-identical; all regression suites green
</success_criteria>

<output>
Create `.planning/phases/15-private-networks-consume-privatenetworkid-identity-and-bind-/15-05-SUMMARY.md` when done.
</output>
