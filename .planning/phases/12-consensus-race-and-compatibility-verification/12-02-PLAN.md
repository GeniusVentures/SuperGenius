---
phase: 12-consensus-race-and-compatibility-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - test/src/blockchain/consensus_finality_race_test.cpp
  - test/src/blockchain/CMakeLists.txt
autonomous: true
requirements:
  - TEST-02
  - TEST-05
must_haves:
  truths:
    - "D-06: The exact PubSub HandleCertificate-before-CRDT/application interleaving pauses after authority is established and before reservation/application work."
    - "D-07: Every interleaving is controlled by a condition-variable barrier or owned recovery trigger, never scheduling luck or sleep."
    - "D-08: While paused, slot lookup returns the winner, a competitor cannot gain a usable signature or certificate, identical delivery is idempotent, and a conflict is rejected and recorded."
    - "D-12: Cross-component interleaving coverage lives in the dedicated consensus_finality_race_test target; owning subsystem suites remain separate."
    - "D-19: Local, PubSub, committed-CRDT recovery, and restart recovery ingress preserve one application and one deduplicated conflict identity."
  artifacts:
    - path: src/blockchain/Consensus.cpp
      provides: Deterministic authority-established stage after authoritative finality and before application
    - path: test/src/blockchain/consensus_finality_race_test.cpp
      provides: Exact application-gap, identical-ingress, and conflict-ingress integration proof
    - path: test/src/blockchain/CMakeLists.txt
      provides: Registered consensus_finality_race_test CTest target
  key_links:
    - from: ConsensusManager::HandleCertificate
      to: authoritative slot/index and FinalizedPendingApplication
      via: shared FinalizeSlot path before authority-established barrier
    - from: authority-established barrier
      to: competing proposal/vote/certificate attempts
      via: friend-only controlled deadline and real same-slot mint subject
    - from: CRDT CertificateReceived journal
      to: committed certificate recovery
      via: RecoverPendingCertificateWork after the authoritative pair is readable
    - from: all ingress paths
      to: one process/application lease and one conflict record
      via: shared slot-keyed finalization state machine
---

<objective>
Reproduce the original certificate-before-CRDT/application race deterministically and prove identical/conflicting certificates are safe through every external ingress.

Purpose: Demonstrate that authoritative slot finality closes the second-certificate gap before transaction application completes.
Output: A lock-safe authority barrier and a dedicated real-manager/real-store integration target.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-CONTEXT.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-RESEARCH.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-PATTERNS.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-VALIDATION.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-01-PLAN.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-VERIFICATION.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated finality-race harness and exact authority barrier</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, test/src/blockchain/consensus_finality_race_test.cpp, test/src/blockchain/CMakeLists.txt</files>
  <read_first>
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - src/blockchain/ConsensusStateStore.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - test/src/blockchain/consensus_finalization_test.cpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - test/src/blockchain/consensus_certificate_store_test.cpp
    - test/src/blockchain/CMakeLists.txt
    - test/testutil/wait_condition.hpp
  </read_first>
  <action>
Add an `authority-established` call to the existing private `finalization_stage_observer_` immediately after `FinalizeSlot` verifies/persists the canonical slot/index pair and installs `restored_final_slots_` plus `FinalizedPendingApplication`, but before `FinalizeBurnReservation`, `PutPendingProcess`, publication, handler invocation, or cleanup. Invoke it outside `proposals_mutex_`. Preserve the Plan 12-01 structured authority trace; this stage is a deterministic control point, not a second public event system.

Create `consensus_finality_race_test.cpp` with a real `BaseCRDTTest`/RocksDB fixture, real validator registry/account, canonical mint-v2 subject and burn outpoint, valid competing proposals/certificates for the same slot, a condition-variable `DeterministicBarrier`, RAII workers, and a `ConsensusFinalityRaceTestAccess` friend. The accessor may install the stage observer, call the actual `HandleCertificate` PubSub handler, drive `CertificateReceived`/`RecoverPendingCertificateWork`, inspect slot lifecycle/votes/process/conflicts, and control the existing deadline clock; it must not bypass canonical validation or synthesize authority records directly.

Register `consensus_finality_race_test` in `test/src/blockchain/CMakeLists.txt` with `blockchain_genesis`, `genius_node_test`, `rapidjson`, and `base_crdt_test`, matching existing finalization targets. Add a discoverability test that constructs and closes the full fixture cleanly.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_finality_race_test -j2 &amp;&amp; test "$(ctest --test-dir build/OSX/Release -N -R '^consensus_finality_race_test$' | sed -n 's/^Total Tests: //p')" -eq 1 &amp;&amp; build/OSX/Release/test_bin/consensus_finality_race_test --gtest_filter='*HarnessIsDiscoverable' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - `authority-established` occurs only after slot lookup can return the exact authoritative certificate and lifecycle blocks signing.
    - The observer is invoked with no consensus/store/handler lock held and before all burn/process/application work.
    - The dedicated target uses real consensus validation, state store, CRDT work journal, and canonical mint subjects.
    - The new CTest target is discovered exactly once and the harness closes without leaked workers or hooks.
    - No public test API, mock consensus, sleep-based race, or direct fabricated certificate-store authority is introduced.
  </acceptance_criteria>
  <done>The exact finality boundary can be paused deterministically in a dedicated real-path integration target.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the application gap and every certificate ingress contract</name>
  <files>test/src/blockchain/consensus_finality_race_test.cpp</files>
  <read_first>
    - test/src/blockchain/consensus_finality_race_test.cpp
    - test/src/blockchain/consensus_finalization_test.cpp
    - test/src/blockchain/consensus_certificate_store_test.cpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - src/blockchain/Consensus.cpp
    - src/blockchain/Consensus.hpp
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-CONTEXT.md
  </read_first>
  <action>
Add `HandleCertificateBeforeCrdtApplicationBlocksCompetingCertificate`. Start one worker through the actual PubSub `HandleCertificate` path and pause it at `authority-established`. While paused, assert `GetCertificateBySlotId(slot)` returns the exact winner, `CheckCertificateForSubject` reports the slot finalized for the competitor, and slot lifecycle is `FinalizedPendingApplication`. Submit/admit the competitor and drive its deadline/votes through real methods; assert signer/publication counters do not create a distinct usable signature and `CreateCertificate`/`SubmitCertificate` cannot establish a competitor. Then feed the identical canonical slot/index pair through the CRDT filter/callback journal, release application, recover committed work, and prove one process reaches the exact terminal result once.

Add `IdenticalCertificateAllIngressPathsApplyAndCleanupOnce`. Deliver byte-identical canonical certificate input through local `SubmitCertificate`, PubSub `HandleCertificate`, committed CRDT callback/recovery, and same-datastore manager restart recovery. Exact replay may be observed/published repeatedly where production allows, but application handler, durable completion, cleanup, and dependency wake each occur at most once and authority bytes/digest never change.

Add `ConflictingCertificateAllIngressPathsPreserveWinnerAndDeduplicateEvidence`. After establishing the winner, deliver a structurally valid different certificate for the same slot through every reachable ingress. Each route must reject/preserve the winner, never publish or apply the conflict, keep the winning process/application exact, and update one canonical sorted digest-pair record. Assert one unique-pair metric, unchanged authoritative bytes, expected source bitset/observation count, and no second certificate/winner.

All workers and barriers must use RAII release/join. Do not use `sleep_for`, detached threads, log text, or `FinalizeSlot` as the sole ingress under test.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_finality_race_test consensus_finalization_test consensus_burn_reservation_test consensus_certificate_store_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_finality_race_test --gtest_list_tests | grep -Ec 'HandleCertificateBeforeCrdtApplicationBlocksCompetingCertificate|IdenticalCertificateAllIngressPathsApplyAndCleanupOnce|ConflictingCertificateAllIngressPathsPreserveWinnerAndDeduplicateEvidence')" -eq 3 &amp;&amp; build/OSX/Release/test_bin/consensus_finality_race_test --gtest_brief=1 &amp;&amp; ctest --test-dir build/OSX/Release -R '^(consensus_finalization_test|consensus_burn_reservation_test|consensus_certificate_store_test)$' --output-on-failure --no-tests=error &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - All three exact regression names are discovered and pass without sleeps.
    - During the paused gap, slot lookup returns the winner and no competitor obtains a distinct published vote or certificate.
    - Local, PubSub, committed-CRDT recovery, and restart recovery apply and clean the identical winner once.
    - Every conflicting ingress preserves byte-identical authority and one application while accumulating one deduplicated conflict identity.
    - Tests invoke actual external handlers/recovery entry points; direct finalizer calls are fixture support only.
    - Existing finalization, burn-reservation, and certificate-store targets remain green.
  </acceptance_criteria>
  <done>The historical ordering gap and all duplicate/conflict ingress paths are proven safe against a second certificate or application.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
## ASVS L1 Trust Boundaries

| Boundary | Risk |
|---|---|
| PubSub certificate handling to CRDT merge/application | A second proposal can exploit the time between certificate observation and replicated application |
| Authoritative slot storage to local lifecycle | A pause at the wrong stage can falsely claim protection before authority exists |
| Multiple external ingress paths to one finalizer | Duplicate or conflicting delivery can apply twice, overwrite authority, or fragment conflict evidence |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-12-01 | High | Denial of Service / Tampering | Authority barrier runs outside locks and every worker is RAII-owned/released |
| T-12-03 | High | Elevation of Privilege | Test proves authoritative lookup and finalized lifecycle before competitor injection |
| T-12-04 | High | Tampering / Repudiation | Every external ingress preserves original bytes, applies once, and records one canonical conflict pair |
| T-12-07 | Medium | Denial of Service | Static clocks/hooks are reset and manager restart uses exact same-path durable ownership |

All High threats require the dedicated target to pass before the 11-node race or full-suite plans execute.
</threat_model>

<verification>
- `consensus_finality_race_test` is registered exactly once and all named cases pass.
- Existing finalization, burn reservation, and certificate storage suites pass.
- No test uses scheduling luck, sleeps, public hooks, or internal-finalizer-only coverage.
- `git diff --check` passes.
</verification>

<success_criteria>
- The certificate-before-application gap cannot admit a second usable vote or certificate.
- Identical certificate delivery is once-only across every ingress and restart.
- Conflicts preserve the winner and produce one actionable deduplicated record.
</success_criteria>
