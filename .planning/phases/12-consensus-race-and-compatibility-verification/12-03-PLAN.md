---
phase: 12-consensus-race-and-compatibility-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/src/blockchain/consensus_vote_journal_test.cpp
  - test/src/blockchain/consensus_certificate_store_test.cpp
  - test/src/blockchain/certificate_compatibility_test.cpp
  - test/src/account/transaction_manager_pending_lifecycle_test.cpp
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
must_haves:
  truths:
    - "D-07: Vote-deadline and restart tests use controlled clocks and predicates, never sleep-based correctness."
    - "D-09: Restart reconstruction destroys the original manager and opens a new manager against the same durable database before replay and conflict attempts."
    - "D-10: A better candidate arriving before the controlled deadline may win; after a durable vote is published, the validator cannot sign a different target for that slot."
    - "D-18: Malformed certificate-index data produces typed corruption, preserves existing authority, and supplies a useful diagnostic without exact log-text assertions."
    - "D-20: Real previous-nonce and producer-UTXO transaction consumers resolve authoritative certificates through the slot index and preserve their prior semantics."
  artifacts:
    - path: test/src/blockchain/consensus_vote_journal_test.cpp
      provides: Durable restart conflict and controlled candidate-deadline regression proof
    - path: test/src/blockchain/consensus_certificate_store_test.cpp
      provides: Typed corruption and immutable authority compatibility proof without sleeps
    - path: test/src/blockchain/certificate_compatibility_test.cpp
      provides: Legacy/new certificate lookup compatibility coverage
    - path: test/src/account/transaction_manager_pending_lifecycle_test.cpp
      provides: Real nonce-chain and producer-UTXO consumer coverage through slot-index lookup
  key_links:
    - from: durable validator vote journal
      to: reconstructed ConsensusManager
      via: same RocksDB path and exact stored vote bytes
    - from: controlled candidate deadline
      to: signer/publication counts
      via: before-deadline replacement followed by after-publication conflicting proposal
    - from: certificate slot index
      to: previous-nonce and producer-UTXO consumers
      via: TransactionManager dependency resolution using the authoritative certificate
---

<objective>
Close the focused durability and compatibility proof for one-vote-per-slot finality.

Purpose: Prove that a real restart cannot forget a vote lock, deadline ordering cannot create a second signature, corrupted index data cannot mutate authority, and existing transaction consumers still resolve certificates correctly.
Output: Focused deterministic regressions in the owning subsystem suites.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-CONTEXT.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-RESEARCH.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-PATTERNS.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-VALIDATION.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-VERIFICATION.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove reconstructed vote locks and deadline ordering</name>
  <files>test/src/blockchain/consensus_vote_journal_test.cpp</files>
  <read_first>
    - test/src/blockchain/consensus_vote_journal_test.cpp
    - src/blockchain/Consensus.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/blockchain/ConsensusStateStore.hpp
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-CONTEXT.md
  </read_first>
  <action>
Add `RestartedVoteLockRejectsCompetingSameSlotProposalWithoutResigning`. Use the suite's real temporary RocksDB path and signer/publication recorder. Drive manager A to persist and publish one vote, retain the exact serialized envelope for replay comparison, close and destroy manager A, then construct manager B with the same validator identity and database. Verify recovery republishes only the exact durable bytes. Submit a different valid proposal for the same canonical slot, advance the controlled clock through its deadline, and assert no second signer invocation, no distinct outbound vote tuple, and no replacement of the durable journal entry.

Add `BetterCandidateBeforeDeadlineWinsButAfterPublicationCannotResign` or strengthen the existing equivalent under that exact discoverable name. Submit candidate A, inject strictly better candidate B before the controlled deadline, and prove only B is signed when the deadline is advanced. After B is durably published, inject a still-better distinct candidate C for the same slot and advance again; prove the signer/publication counts remain one and the stored target/digest remains B. Compare canonical slot/proposal identities rather than timing or log text.

Use the existing controlled clock, explicit deadline driver, deterministic callbacks, and RAII manager shutdown. Do not keep manager A alive during reconstruction, copy in-memory vote state into manager B, delete the journal, or use `sleep_for`.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_vote_journal_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_vote_journal_test --gtest_list_tests | grep -Ec 'RestartedVoteLockRejectsCompetingSameSlotProposalWithoutResigning|BetterCandidateBeforeDeadlineWinsButAfterPublicationCannotResign')" -eq 2 &amp;&amp; build/OSX/Release/test_bin/consensus_vote_journal_test --gtest_filter='*RestartedVoteLockRejectsCompetingSameSlotProposalWithoutResigning:*BetterCandidateBeforeDeadlineWinsButAfterPublicationCannotResign' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - Both exact regression names are discovered once and pass.
    - The original manager is closed and destroyed before the replacement opens the same database.
    - Restart replay bytes equal the original durable envelope and do not invoke the signer again.
    - A distinct same-slot proposal after restart cannot produce a new signature, digest, publication target, or journal replacement.
    - Before-deadline candidate replacement selects the best candidate once; after-publication replacement cannot re-sign.
    - No correctness assertion depends on wall-clock sleeps or scheduling luck.
  </acceptance_criteria>
  <done>Durable vote ownership survives true reconstruction and controlled deadline ordering cannot create an equivocation.</done>
</task>

<task type="auto">
  <name>Task 2: Close index corruption and real-consumer compatibility coverage</name>
  <files>test/src/blockchain/consensus_certificate_store_test.cpp, test/src/blockchain/certificate_compatibility_test.cpp, test/src/account/transaction_manager_pending_lifecycle_test.cpp</files>
  <read_first>
    - test/src/blockchain/consensus_certificate_store_test.cpp
    - test/src/blockchain/certificate_compatibility_test.cpp
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/blockchain/Consensus.cpp
    - src/account/TransactionManager.cpp
    - src/account/TransactionManager.hpp
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-RESEARCH.md
  </read_first>
  <action>
Audit certificate-store tests that currently use `sleep_for` to infer absence. Replace each correctness sleep with an existing completion callback, state predicate, controlled recovery call, or bounded `wait_condition` that can terminate immediately when the expected invariant is observable. Add or strengthen typed-corruption coverage so malformed slot-index bytes return the project's typed corruption result/error, leave the previously authoritative certificate bytes and lookup result unchanged, and expose a structured/status diagnostic suitable for debugging without asserting exact `spdlog` wording.

Run and, only where an assertion is missing, strengthen legacy/new compatibility coverage to prove lookup by canonical slot resolves both supported certificate encodings without overwriting immutable authority. Preserve on-disk compatibility; do not rewrite production formats merely to simplify tests.

Run the existing real consumer regressions `PreviousNonceCertificateLookupPreservesConsumerSemantics` and `ProducerUTXOCertificateLookupPreservesConsumerSemantics`. Confirm they construct actual transaction dependencies and enter `TransactionManager` certificate resolution through the slot index. If either test bypasses that path or only probes the store directly, minimally refactor the test fixture—not production behavior—so it exercises the real consumer and asserts its prior ready/pending/rejection semantics remain unchanged.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_certificate_store_test certificate_compatibility_test transaction_manager_pending_lifecycle_test -j2 &amp;&amp; build/OSX/Release/test_bin/consensus_certificate_store_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/certificate_compatibility_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_filter='*PreviousNonceCertificateLookupPreservesConsumerSemantics:*ProducerUTXOCertificateLookupPreservesConsumerSemantics' --gtest_brief=1 &amp;&amp; ! rg -n 'sleep_for' test/src/blockchain/consensus_certificate_store_test.cpp &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Certificate-store correctness tests contain no `sleep_for` and pass deterministically.
    - Corrupt index input returns typed corruption, preserves the authoritative certificate byte-for-byte, and yields a useful non-log diagnostic.
    - Legacy and current supported certificate encodings remain discoverable through canonical lookup without authority replacement.
    - Both named TransactionManager consumer tests are discovered and pass through the real slot-index resolution path.
    - Previous-nonce and producer-UTXO ready/pending/rejection behavior is unchanged.
  </acceptance_criteria>
  <done>The durable certificate index remains immutable under corruption and compatible with every real transaction consumer.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
## ASVS L1 Trust Boundaries

| Boundary | Risk |
|---|---|
| Durable vote journal to reconstructed manager | Restart may lose the one-vote lock or re-sign replayed content |
| Candidate deadline to signer | Ordering around a deadline may authorize two proposal targets |
| Untrusted persisted index bytes to authority lookup | Corruption may overwrite authority, masquerade as absence, or crash recovery |
| Certificate index to transaction consumers | Compatibility changes may alter nonce-chain or UTXO dependency semantics |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-12-02 | High | Spoofing / Repudiation | True manager reconstruction and exact outbound-byte comparison prove replay without re-signing |
| T-12-04 | High | Tampering | Typed corruption tests preserve byte-identical authority and reject malformed index state |
| T-12-07 | High | Denial of Service | Controlled clocks, predicates, and deterministic shutdown replace sleep-based ordering |

Every High threat is blocked by its focused owning-subsystem target before full-suite closure.
</threat_model>

<verification>
- Both restart/deadline regressions pass using a controlled clock and one durable database.
- Certificate-store and compatibility suites pass with typed corruption and immutable authority.
- Both real TransactionManager consumer regressions pass through canonical slot-index lookup.
- No certificate-store correctness test uses `sleep_for`; `git diff --check` passes.
</verification>

<success_criteria>
- A validator cannot sign two targets across deadline changes or process reconstruction.
- Persisted corruption cannot mutate or hide an existing authoritative certificate.
- Certificate lookup remains compatible for prior encodings and real nonce/UTXO consumers.
</success_criteria>
