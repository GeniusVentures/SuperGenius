---
phase: 12-consensus-race-and-compatibility-verification
plan: 05
type: execute
wave: 3
depends_on:
  - 12-02
  - 12-03
  - 12-04
files_modified:
  - .planning/phases/12-consensus-race-and-compatibility-verification/12-FULL-SUITE-REPORT.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
must_haves:
  truths:
    - "D-14: The real 11-node race is mandatory and passes in isolated CTest execution."
    - "D-16: The entire configured repository CTest inventory runs without a phase regex, including unrelated and external-integration tests."
    - "D-17: Every available prerequisite-backed integration runs; unavailable prerequisites are recorded as explicit reviewed skips rather than silently omitted."
    - "No failure, timeout, crash, not-run test, or unreviewed skip is reported as successful closure."
  artifacts:
    - path: .planning/phases/12-consensus-race-and-compatibility-verification/12-FULL-SUITE-REPORT.md
      provides: Reproducible build identity, dynamic inventory, focused/isolated/full-suite results, and reviewed prerequisite skips
  key_links:
    - from: Phase 12 focused targets
      to: isolated long race
      via: all owning-subsystem regressions pass before the expensive network gate
    - from: ctest dynamic inventory
      to: full repository execution
      via: unfiltered `ctest --output-on-failure --no-tests=error`
    - from: external-integration skip
      to: prerequisite evidence
      via: explicit environment/tool/service check and reviewer disposition in the report
---

<objective>
Close Phase 12 with an auditable focused, isolated-race, and full-repository verification run.

Purpose: Ensure the consensus fix is not merely locally plausible but compatible with the whole configured test inventory, while making unavailable external prerequisites visible and reviewable.
Output: A committed Phase 12 full-suite report with zero hidden failures or skips.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-CONTEXT.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-RESEARCH.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-VALIDATION.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-02-PLAN.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-03-PLAN.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run focused regressions and the isolated real race</name>
  <files>.planning/phases/12-consensus-race-and-compatibility-verification/12-FULL-SUITE-REPORT.md</files>
  <read_first>
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-VALIDATION.md
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-02-PLAN.md
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-03-PLAN.md
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-04-PLAN.md
    - test/src/blockchain/CMakeLists.txt
    - test/src/bridge_race/CMakeLists.txt
    - build/OSX/CMakeLists.txt
  </read_first>
  <action>
Create `12-FULL-SUITE-REPORT.md` from a fresh verification run. Record repository commit/worktree state, generator/build configuration, compiler, build directory, command lines, start/end timestamps, and prerequisite environment facts without exposing secrets. Build every Phase 12 owning target and run the focused consensus finalization, finality race, vote journal, burn reservation, certificate store, compatibility, and TransactionManager lifecycle tests. Require every named Phase 12 regression from Plans 01–04 to be discovered and pass.

Run `bridge_race_single_burn_test` alone with `ctest -R '^bridge_race_single_burn_test$' --output-on-failure --no-tests=error -j1`. Record duration, configured timeout, readiness/proposal/vote/authority summary, and whether teardown completed. If any focused or isolated test fails, crashes, times out, is not discovered, or leaves the fixture unable to tear down, stop and report the exact blocker; do not proceed to characterize the full suite as passing.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_finalization_test consensus_finality_race_test consensus_vote_journal_test consensus_burn_reservation_test consensus_certificate_store_test certificate_compatibility_test transaction_manager_pending_lifecycle_test bridge_race_single_burn_test -j2 &amp;&amp; ctest --test-dir build/OSX/Release -R '^(consensus_finalization_test|consensus_finality_race_test|consensus_vote_journal_test|consensus_burn_reservation_test|consensus_certificate_store_test|certificate_compatibility_test|transaction_manager_pending_lifecycle_test)$' --output-on-failure --no-tests=error -j2 &amp;&amp; ctest --test-dir build/OSX/Release -R '^bridge_race_single_burn_test$' --output-on-failure --no-tests=error -j1</automated>
  </verify>
  <acceptance_criteria>
    - Every Phase 12 focused regression is discovered and passes.
    - The real 11-node test passes alone as a mandatory CTest with clean teardown.
    - Report metadata is sufficient to reproduce the build and commands without recording secrets.
    - Any failure/not-run/timeout aborts successful closure and is recorded accurately.
  </acceptance_criteria>
  <done>All focused invariants and the production-realistic one-burn race pass before repository-wide execution.</done>
</task>

<task type="auto">
  <name>Task 2: Execute and account for the complete configured repository suite</name>
  <files>.planning/phases/12-consensus-race-and-compatibility-verification/12-FULL-SUITE-REPORT.md</files>
  <read_first>
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-FULL-SUITE-REPORT.md
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-RESEARCH.md
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-VALIDATION.md
    - build/OSX/Release/CTestTestfile.cmake
    - build/OSX/CMakeLists.txt
  </read_first>
  <action>
Capture the configured inventory dynamically with `ctest --test-dir build/OSX/Release -N`; do not hardcode the current test count. Enumerate test names and labels into the report or attach a concise generated inventory section. Check known external prerequisites identified by research—live bridge RPC/contract environment, Anvil, Cast, fork RPC, and any test-declared service/tool requirement—and classify each as available or unavailable without printing credential values.

Run the entire configured inventory with exactly one unfiltered CTest invocation: `ctest --test-dir build/OSX/Release --output-on-failure --no-tests=error -j2`. Do not use a phase regex, exclude regex, manual test list, or success-only rerun as the reported full-suite result. Every available prerequisite-backed test must execute and pass. For a genuinely unavailable prerequisite, record the test name, prerequisite check, CTest result/skip mechanism, why it is unavailable, and an explicit reviewed-skip disposition; a missing executable, crash, timeout, fixture/setup failure, `Not Run`, or accidental filter is not an acceptable skip.

Complete the report with inventory count, passed/failed/not-run/skipped counts, durations, failed output references, and a requirement matrix for TEST-01 through TEST-06 pointing to the exact focused and full-suite evidence. Successful Phase 12 closure requires zero failures, zero timeouts/crashes, zero `Not Run`, and zero unreviewed skips. Do not fix unrelated suite failures under this reporting task; report them as blockers for a scoped follow-up decision.
  </action>
  <verify>
    <automated>ctest --test-dir build/OSX/Release -N &amp;&amp; ctest --test-dir build/OSX/Release --output-on-failure --no-tests=error -j2 &amp;&amp; test -s .planning/phases/12-consensus-race-and-compatibility-verification/12-FULL-SUITE-REPORT.md &amp;&amp; rg -q 'TEST-01' .planning/phases/12-consensus-race-and-compatibility-verification/12-FULL-SUITE-REPORT.md &amp;&amp; rg -q 'TEST-06' .planning/phases/12-consensus-race-and-compatibility-verification/12-FULL-SUITE-REPORT.md &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The report derives its inventory count from the current configured `ctest -N` output.
    - One unfiltered CTest command executes the entire repository inventory, including unrelated and external-integration tests.
    - All available-prerequisite tests pass; every unavailable-prerequisite skip has concrete evidence and explicit reviewed disposition.
    - The reported successful state has zero failures, crashes, timeouts, `Not Run`, or unreviewed skips.
    - TEST-01 through TEST-06 each map to named focused evidence plus the full-suite result.
  </acceptance_criteria>
  <done>The complete configured repository suite is accounted for with reproducible evidence and no hidden failure or skip.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
## ASVS L1 Trust Boundaries

| Boundary | Risk |
|---|---|
| Selected focused tests to repository-wide confidence | A narrow filter can hide regressions outside Phase 12 |
| External prerequisite detection to skip classification | Missing infrastructure can be mislabeled as a successful test |
| Raw test output to durable report | Secrets can leak or failures can be summarized inaccurately |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-12-05 | High | Repudiation | Dynamic inventory plus one unfiltered CTest run accounts for every configured test and outcome |
| T-12-06 | Medium | Information Disclosure | Report records prerequisite presence/status only and redacts all credential values |
| T-12-07 | High | Denial of Service / Repudiation | Timeouts, crashes, `Not Run`, and unavailable prerequisites are distinct blocking/reporting states, never silently accepted |

Every High threat blocks Phase 12 closure unless the recorded verification conditions are satisfied.
</threat_model>

<verification>
- All focused targets and the isolated mandatory bridge race pass before the full suite.
- Dynamic `ctest -N` inventory and one unfiltered full-suite invocation are captured.
- The report has zero hidden/unreviewed outcomes and maps TEST-01 through TEST-06 to evidence.
- `git diff --check` passes.
</verification>

<success_criteria>
- Focused and real-network regressions pass with clean teardown.
- Every configured repository test is executed or has an explicit evidence-backed reviewed prerequisite skip.
- Phase 12 has a reproducible audit artifact with no failure represented as success.
</success_criteria>
