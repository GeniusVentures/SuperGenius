---
phase: 12-consensus-race-and-compatibility-verification
plan: 04
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - test/src/bridge_race/bridge_race_fixture.hpp
  - test/src/bridge_race/bridge_race_single_burn_test.cpp
  - test/src/bridge_race/CMakeLists.txt
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "D-01: The test starts all 11 validators, checks every ConfigureRpcEndpoint result, waits until all report READY, then creates exactly one new burn."
    - "D-02: All 11 validators publish a local proposal for that same canonical mint slot."
    - "D-03: Every node converges on the same authoritative certificate and winner; exactly the winner is CONFIRMED and all losing proposals remain unconfirmed."
    - "D-04: For each validator and slot, at most one distinct usable vote target is published; byte-identical replay is not misclassified as a second vote."
    - "D-05: Any failure prints bounded per-node proposal, vote, authority, transaction-state, and readiness evidence."
    - "D-11/D-14/D-15: The mandatory long CTest runs a real 11-node, real PubSub/CRDT, production-realistic one-burn race in isolation."
  artifacts:
    - path: test/src/bridge_race/bridge_race_fixture.hpp
      provides: Friend-only per-manager trace collectors, predicate barriers, and bounded race snapshots
    - path: test/src/bridge_race/bridge_race_single_burn_test.cpp
      provides: Exact one-burn, 11-proposal, one-vote-per-validator, one-certificate convergence proof
    - path: test/src/bridge_race/CMakeLists.txt
      provides: Mandatory isolated long-test registration and evidence-based timeout
  key_links:
    - from: GeniusNode::GetTransactionManager
      to: each private ConsensusManager trace observer
      via: BridgeRaceConsensusTestAccess friendship from Plan 12-01
    - from: one burn receipt
      to: all local proposals
      via: canonical mint slot equality and structured LocalProposalPublished events
    - from: per-validator VotePublished events
      to: authoritative winner on every node
      via: canonical validator/slot/proposal/digest grouping and slot-index lookup
---

<objective>
Turn the historical bridge race into a direct, deterministic 11-validator consensus proof.

Purpose: Verify that one burn creates one contested slot, every validator participates, no validator produces usable votes for two targets, and the entire network converges on one certificate and one confirmed mint.
Output: A production-realistic mandatory long test with structured, bounded failure evidence.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-CONTEXT.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-RESEARCH.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-PATTERNS.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-VALIDATION.md
@.planning/phases/12-consensus-race-and-compatibility-verification/12-01-PLAN.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-VERIFICATION.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Equip the 11-node fixture with structured race evidence</name>
  <files>test/src/bridge_race/bridge_race_fixture.hpp</files>
  <read_first>
    - test/src/bridge_race/bridge_race_fixture.hpp
    - test/src/bridge_race/bridge_race_single_burn_test.cpp
    - src/account/GeniusNode.hpp
    - src/account/TransactionManager.hpp
    - src/blockchain/Blockchain.hpp
    - src/blockchain/Consensus.hpp
    - test/testutil/wait_condition.hpp
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-01-PLAN.md
  </read_first>
  <action>
Define the named `BridgeRaceConsensusTestAccess` already befriended in Plan 12-01. Starting from each node's existing public `GetTransactionManager()`, copy the existing shared ownership chain to its private `ConsensusManager` and install one per-manager trace callback. Keep all managers and observer cleanup RAII-owned by the fixture; do not retain raw pointers or add public getters.

Add a thread-safe bounded collector keyed by node/validator and typed event stage. Its snapshot must retain canonical slot, proposal, subject/winner, deterministic public digest, delivery source/stage, occurrence count, and first/last monotonic observation time while capping duplicate detail. Provide predicates for: all nodes READY, all 11 local proposals observed for one expected slot, every validator's distinct usable vote-target cardinality at most one, all nodes exposing the same authoritative certificate, exact transaction-state convergence, and a bounded stability window driven by counters/condition variables rather than sleep.

Provide a single bounded diagnostic renderer that reports endpoint configuration outcome, readiness, local proposal identities, distinct vote targets and replay counts, authority identity, winner/loser transaction states, application counters, and the last relevant event per node. Exclude private keys/raw signer inputs and do not parse logs. Ensure callbacks only copy event values under the collector mutex and signal a condition variable; they must not call back into node/consensus code.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target bridge_race_single_burn_test -j2 &amp;&amp; build/OSX/Release/test_bin/bridge_race_single_burn_test --gtest_list_tests | grep -q 'ExactlyOneCertificateForOneBurn'</automated>
  </verify>
  <acceptance_criteria>
    - All 11 managers install independent private observers through shared ownership and named friendship only.
    - Snapshot predicates distinguish distinct vote targets from byte-identical replay and are safe under concurrent callbacks.
    - Diagnostics are bounded, structured, per node, and contain no secret material or log parsing.
    - Fixture teardown always clears observers before manager/node destruction and joins every owned worker.
    - No production public API, global observer map, raw lifetime pointer, or sleep-based correctness helper is introduced.
  </acceptance_criteria>
  <done>The production-realistic fixture can directly observe and diagnose every consensus invariant needed by the race.</done>
</task>

<task type="auto">
  <name>Task 2: Assert one burn, eleven proposals, one vote each, and one winner</name>
  <files>test/src/bridge_race/bridge_race_single_burn_test.cpp, test/src/bridge_race/CMakeLists.txt</files>
  <read_first>
    - test/src/bridge_race/bridge_race_single_burn_test.cpp
    - test/src/bridge_race/bridge_race_fixture.hpp
    - test/src/bridge_race/CMakeLists.txt
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.cpp
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-CONTEXT.md
    - .planning/phases/12-consensus-race-and-compatibility-verification/12-RESEARCH.md
  </read_first>
  <action>
Rewrite `ExactlyOneCertificateForOneBurn` around predicate barriers. Create all 11 nodes, call `ConfigureRpcEndpoint` for every node and fail with the node index/result if any return is unsuccessful, then wait until every node reports READY. Install observers before triggering bridge ingestion. Record the pre-burn transaction/certificate baseline, create exactly one new on-chain burn, and require exactly one new canonical mint slot attributable to that receipt; historical backfill must not count as the test burn.

Wait until every one of the 11 nodes emits a local proposal whose canonical slot equals that one burn slot. Group successful `VotePublished` events by validator and slot, require every validator to participate, and assert each group has at most one distinct proposal/digest target while allowing repeated identical replay. Wait for all 11 nodes' slot lookup to return byte-identical authoritative certificate content and the same winning proposal/transaction. Assert exactly one winner transaction reaches CONFIRMED on every node, every observed losing proposal remains non-CONFIRMED everywhere, the expected destination balance changes once, and process/application counters reach one and remain unchanged across the fixture's event-driven bounded stability predicate.

On any failed predicate, append the fixture's one bounded structured snapshot. Replace every race-critical `sleep_for` with condition-variable/poll predicates with explicit deadlines. Keep only delays required by external process startup if they cannot be exposed as readiness; those delays must not prove a consensus assertion. Run the isolated test repeatedly enough during implementation to measure its maximum healthy duration, then retain or adjust the CTest timeout only from that evidence and document the observed margin in the plan summary. Keep the target mandatory—do not add a disabled label or default skip.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target bridge_race_single_burn_test -j2 &amp;&amp; ctest --test-dir build/OSX/Release -R '^bridge_race_single_burn_test$' --output-on-failure --no-tests=error -j1 &amp;&amp; ! rg -n 'sleep_for' test/src/bridge_race/bridge_race_single_burn_test.cpp &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Every endpoint configuration succeeds and all 11 nodes are READY before the sole new burn is created.
    - Exactly one new burn-derived slot is observed and all 11 validators publish a local proposal for it.
    - Every validator has exactly one distinct usable vote target for that slot; identical replay does not fail the test.
    - All 11 nodes expose byte-identical authority and the same winner; only that transaction is CONFIRMED and every loser is not.
    - Balance and once-only process/application counters prove one mint application and remain stable without `sleep_for`.
    - The isolated mandatory CTest passes within an evidence-based timeout and emits bounded structured diagnostics on failure.
  </acceptance_criteria>
  <done>The original failure scenario is a deterministic end-to-end proof of one certificate and one confirmed mint for one burn.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
## ASVS L1 Trust Boundaries

| Boundary | Risk |
|---|---|
| Real RPC readiness to burn creation | A burn created too early can be backfilled inconsistently or missed by validators |
| Concurrent validator publications to collector | Lost or misgrouped events can conceal equivocation or invent duplicates |
| PubSub/CRDT convergence to transaction state | Certificate agreement may be claimed before all nodes apply the same winner |
| Test teardown to shared network/storage resources | Leaked workers or observers can contaminate subsequent tests |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-12-01 | High | Denial of Service / Tampering | Lock-minimal per-manager collectors, predicate deadlines, and RAII teardown avoid observer-induced races |
| T-12-02 | High | Spoofing / Repudiation | Canonical validator/slot/proposal/digest grouping proves one distinct usable target per validator |
| T-12-05 | High | Repudiation | All 11 nodes must expose byte-identical authority plus winner/loser state and once-only application evidence |
| T-12-06 | Medium | Information Disclosure | Bounded snapshots include public consensus identity only and exclude signer secrets |

Every High threat is blocked by the isolated real-network regression before full-suite closure.
</threat_model>

<verification>
- `bridge_race_single_burn_test` is discovered once as a mandatory CTest and passes in isolation.
- Structured evidence proves all 11 proposals, one distinct vote target per validator, one authority, one confirmed winner, and no confirmed loser.
- The race test contains no correctness `sleep_for`; teardown is deterministic and `git diff --check` passes.
</verification>

<success_criteria>
- One post-readiness burn creates one contested slot and exactly one authoritative certificate.
- All 11 validators participate without signing two proposal targets.
- Every node converges to the same winner and applies it once with actionable bounded diagnostics on failure.
</success_criteria>
