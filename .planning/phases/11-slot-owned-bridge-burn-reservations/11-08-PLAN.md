---
phase: 11-slot-owned-bridge-burn-reservations
plan: 08
type: execute
wave: 7
depends_on:
  - 11-07
files_modified:
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - src/blockchain/ConsensusStateStore.hpp
  - src/blockchain/ConsensusStateStore.cpp
  - test/src/blockchain/consensus_burn_reservation_test.cpp
  - test/src/blockchain/consensus_vote_journal_test.cpp
  - test/src/blockchain/consensus_pending_lifecycle_test.cpp
  - test/src/blockchain/consensus_finalization_test.cpp
autonomous: true
requirements:
  - BURN-02
  - BURN-03
  - BURN-05
must_haves:
  truths:
    - D-15/D-16: an uncertified reservation releases automatically only at strict passage beyond the persisted candidate horizon and every durable vote acceptance horizon, with authoritative certificate lookup exactly NotFound.
    - D-17/D-19: safe deletion removes both durable keys and no history; a later contender creates a fresh random generation.
    - D-18: release atomically rechecks generation/state/certificate/vote/candidate evidence through the shared store and slot lifecycle; admission renewal and certificate finality defeat stale cleanup.
    - FinalizedPendingApplication, Consumed, SafetyError, lookup uncertainty, and equality with any protection horizon never release.
    - Shutdown owns, wakes, and drains reconciliation activity so no reservation mutation occurs after manager destruction.
  artifacts:
    - path: src/blockchain/Consensus.cpp
      provides: Owned timer/recovery reconciliation and whole-slot abandonment sequencing
    - path: src/blockchain/ConsensusStateStore.cpp
      provides: Expected-generation reciprocal conditional deletion under the shared reservation gate
    - path: test/src/blockchain/consensus_burn_reservation_test.cpp
      provides: Horizon, stale-release, ABA, admission/finality race, and shutdown matrix
  key_links:
    - from: consensus timer/recovery loop
      to: ConsensusStateStore conditional release
      via: exact certificate NotFound, durable vote record, candidate horizon/live recheck, and expected generation
---

<objective>
Release only provably abandoned uncertified reservation generations and close their concurrency and shutdown races.

Purpose: Return a burn to local availability at a deterministic safe horizon without allowing stale work to defeat admission, finality, or a recreated generation.
Output: Owned reconciliation, strict-horizon deletion, ABA-safe interleavings, and lifecycle-drain tests.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
This plan is limited to abandonment, release/admission/finality races, and shutdown lifecycle. Plan 11-09 owns the broad requirements audit and regression gates. Do not add the Phase 12 11-node race, mock RPC, parser fuzzing, or unrelated fixes.
</execution_note>

<context>
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-CONTEXT.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-RESEARCH.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile and conditionally abandon only provably dead generations</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, src/blockchain/ConsensusStateStore.hpp, src/blockchain/ConsensusStateStore.cpp, test/src/blockchain/consensus_burn_reservation_test.cpp, test/src/blockchain/consensus_vote_journal_test.cpp, test/src/blockchain/consensus_pending_lifecycle_test.cpp, test/src/blockchain/consensus_finalization_test.cpp</files>
  <read_first>
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - src/blockchain/ConsensusStateStore.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - test/src/blockchain/consensus_vote_journal_test.cpp
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp
    - test/src/blockchain/consensus_finalization_test.cpp
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-CONTEXT.md
    - .planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-CONTEXT.md
  </read_first>
  <action>
Add reservation reconciliation to the owned consensus timer/recovery loop beside candidate, vote, and certificate-processing work; create no detached or per-slot timer. For each due Reserved generation, serialize through the slot lifecycle, capture its generation, and outside broad proposal locking perform typed authoritative slot-certificate lookup. Certificate presence transitions to finality; anything other than exact NotFound fails closed. Read the exact durable validator vote, retire it only under Phase 10 rules, and require `now > acceptance_horizon_ms`; also require strict passage beyond the persisted maximum candidate horizon and verify no live admitted candidate extends protection. Equality remains reserved.

Reacquire and atomically recheck current generation, Reserved state, reciprocal index, certificate, vote, and candidate evidence before one-batch conditional deletion through the same store gate established in Plan 11-06. Admission may extend generation G or create fresh H after deletion; certificate finality wins; stale G cleanup cannot remove H. Delete both keys with no abandoned history, and allow a later proposal to receive a fresh random generation. Never delete FinalizedPendingApplication, Consumed, or SafetyError.

Extend Close/drain ownership for reconciliation. Add deterministic fixed-clock/barrier tests for equality and strict passage, candidate-only and vote-only protection, certificate and lookup/storage-error protection, missing ephemeral candidates with bounded release, release/admission extension, release/delete/recreate ABA, release/certificate finality, cleanup/finality, and shutdown while reconciliation is paused. Assert no post-destruction mutation; use no sleeps or detached workers.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_burn_reservation_test consensus_vote_journal_test consensus_pending_lifecycle_test consensus_finalization_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_list_tests | grep -Ec 'Race|Stale|ABA|Horizon|Abandon|Shutdown')" -gt 0 &amp;&amp; build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_filter='*Race*:*Stale*:*ABA*:*Horizon*:*Abandon*:*Shutdown*' --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/consensus_vote_journal_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/consensus_pending_lifecycle_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/consensus_finalization_test --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - At `now ==` any candidate/vote horizon the reservation remains; only strict passage plus exact certificate NotFound permits deletion.
    - Certificate presence or lookup/storage uncertainty never releases, and certificate finality wins every paused release race.
    - Stale cleanup cannot delete a renewed or recreated generation; safe deletion removes both keys and no history.
    - Missing candidates after restart eventually release when the deterministic horizon passes and no certificate/vote protection remains.
    - Close wakes and joins paused reconciliation, with no work after manager destruction.
  </acceptance_criteria>
  <done>Only a provably abandoned uncertified generation releases, with ABA, finality, and shutdown safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| Time/horizon evidence to release | Boundary errors can free a still-certifiable burn |
| Reconciliation to concurrent admission/finality | Stale work can delete current protection |
| Timer callbacks to object lifetime | Late cleanup can mutate storage after manager destruction |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-11-08-01 | High | Elevation of Privilege | Strict `now > horizon`, exact certificate NotFound, and durable vote checks gate release |
| T-11-08-02 | High | Tampering | Expected-generation reciprocal deletion under shared slot/store sequencing defeats stale and ABA cleanup |
| T-11-08-03 | High | Denial of Service | Bounded deterministic horizons release genuine orphans while finalized states remain permanent |
| T-11-08-04 | High | Denial of Service | Owned reconciliation activity is awakened and drained before destruction |
</threat_model>

<verification>
Run the guarded abandonment/race tests and direct Phase 10 lifecycle regressions; inspect strict comparison, exact NotFound handling, conditional deletion, and Close/drain; run `git diff --check`.
</verification>

<success_criteria>
- D-15 through D-19 are implemented with deterministic automatic release and no ABA/finality window.
- BURN-05 is directly exercised without pulling the full Phase 12 network race into scope.
</success_criteria>

<output>
Create `.planning/phases/11-slot-owned-bridge-burn-reservations/11-08-SUMMARY.md` when done.
</output>
