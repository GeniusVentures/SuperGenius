---
phase: 11-slot-owned-bridge-burn-reservations
plan: 10
type: execute
gap_closure: true
wave: 9
depends_on:
  - 11-09
files_modified:
  - src/blockchain/Consensus.cpp
  - src/account/TransactionManager.cpp
  - test/src/blockchain/consensus_burn_reservation_test.cpp
  - test/src/account/transaction_manager_pending_lifecycle_test.cpp
autonomous: true
requirements:
  - BURN-03
  - BURN-04
must_haves:
  truths:
    - "BURN-04: Applied or AlreadyApplied from a resource handler is advisory until an exact durable reread proves the same slot, outpoint, generation, certificate digest, proposal, and winner are CONSUMED."
    - "A missing, unreadable, or still-FINALIZED_PENDING_APPLICATION postcondition cannot mark the process COMPLETE, move the slot to Applied, run proposal cleanup, wake certificate dependencies, or mark recovery work done."
    - "A false success remains bound to the exact authoritative winner and is retryable across restart; no alternate winner is selected and BURN-03 protection is never released."
    - "With a finalized reservation handle, malformed or missing embedded mint data, deserialization failure, or transaction-hash mismatch is Irreconcilable, never Applied; legacy non-resource fallback behavior remains unchanged when no finalized handle exists."
    - "Normal first application and exact idempotent replay complete only after the atomic mint path has durably produced CONSUMED, and cleanup/dependency wake occur exactly once afterward."
  artifacts:
    - path: src/blockchain/Consensus.cpp
      provides: Exact identity-matched `CONSUMED` completion gate before process completion and every downstream success side effect
    - path: src/account/TransactionManager.cpp
      provides: Finalized-handle decode/deserialization/hash failures classified as irreconcilable certified-data contradictions
    - path: test/src/blockchain/consensus_burn_reservation_test.cpp
      provides: Integrated false-Applied, false-AlreadyApplied, retry/restart, exact-success, cleanup, and dependency-wake evidence
    - path: test/src/account/transaction_manager_pending_lifecycle_test.cpp
      provides: Finalized-handle malformed/hash-mismatch disposition coverage with legacy compatibility control
  key_links:
    - from: ConsensusManager::ProcessFinalizedCertificate handler disposition
      to: ConsensusStateStore::MarkComplete, SlotState::Applied, ClearProposalSlot, and WakePendingDependency
      via: exact `GetBurnReservation(slot_id)` reread requiring matching finality identity and `CONSUMED`
    - from: TransactionManager::OnConsensusCertificate finalized fallback
      to: ConsensusManager::ApplicationDisposition
      via: finalized-handle-aware decode/deserialization/hash classification
---

<objective>
Bind certified mint completion and cleanup to the exact durable `CONSUMED` postcondition.

Purpose: Eliminate the hollow-success path where a handler can report `Applied` while mint effects are absent and the reservation remains final-pending.
Output: A fail-closed completion gate, finalized-handle-safe disposition mapping, and deterministic retry/restart/cleanup evidence.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
This gap plan closes WR-01/BURN-04 only. Preserve certificate authority, the existing atomic mint batch, exact shared-store identity, and legacy handlers without burn resources. Do not add WR-03 handler replacement, Phase 12 races, mock RPC, startup simulation, or unrelated lifecycle changes.
</execution_note>

<context>
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-REVIEW.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-05-SUMMARY.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-06-SUMMARY.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate resource application completion on exact durable consumption</name>
  <files>src/blockchain/Consensus.cpp, test/src/blockchain/consensus_burn_reservation_test.cpp</files>
  <read_first>
    - src/blockchain/Consensus.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/ConsensusStateStore.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/account/TransactionManager.cpp
    - src/account/UTXOManager.cpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp
    - test/src/account/utxo_manager_test.cpp
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
  </read_first>
  <action>
In `ConsensusManager::ProcessFinalizedCertificate`, retain the immutable expected identity assembled before handler invocation: canonical `slot_id`, `BurnOutpoint` (`source_chain`, `burn_hash`, receipt-local `receipt_log_index`), reservation `generation`, authoritative certificate `digest`, `proposal_id`, and `winner_id`. For a resource-bearing mint, treat both `ApplicationDisposition::Applied` and `AlreadyApplied` only as a request to verify success. Reread through the exact live `state_store_->GetBurnReservation(slot_id)` after the handler returns and before `MarkComplete` or any downstream success action.

Allow the success branch only when the reread is present, strict, state `CONSUMED`, and every outpoint/generation/finality identity field equals the captured handle. A storage/unavailable reread, absent record, or identity-matched `FINALIZED_PENDING_APPLICATION` is retryable: call `RestorePending`, refresh `restored_processes_`, release `processing_slots_`, and return `PendingApplication` without `MarkComplete`, slot `Applied`, `ClearProposalSlot`, `WakePendingDependency`, or certificate-work completion. Route an identity-matched terminal safety record through the existing terminal path with no cleanup/wake; route any readable contradictory identity/illegal state through the irreconcilable safety path rather than accepting it. Do not impose a burn postcondition on legacy certificate handlers or subjects without `burn_reservation`.

Strengthen `SharedStoreApplicationHandlePrecedesHandlerAndCleanup` so its success handler uses `ApplyFinalizedReservationBatch` to stage/commit the exact `CONSUMED` transition before expecting cleanup. Add deterministic integrated tests named `FalseAppliedWithoutConsumedRemainsPendingWithoutCompletionCleanupOrWake`, `FalseAlreadyAppliedWithoutConsumedRetriesAfterRestart`, and `ExactConsumedPostconditionCompletesAndCleansExactlyOnce`. The false-success cases must assert the process remains `PENDING`, reservation remains exact `FINALIZED_PENDING_APPLICATION`, slot is not Applied, cleanup count is zero, a certificate dependency waiter is not woken, and restart invokes the same winner again. The exact-success case must assert durable `CONSUMED` before process `COMPLETE`, cleanup, and one dependency wake. Use the existing friend-only access surface and predicate/counter mechanisms; no sleeps or public hooks.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_burn_reservation_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_list_tests | grep -Ec 'FalseAppliedWithoutConsumedRemainsPendingWithoutCompletionCleanupOrWake|FalseAlreadyAppliedWithoutConsumedRetriesAfterRestart|ExactConsumedPostconditionCompletesAndCleansExactlyOnce')" -eq 3 &amp;&amp; build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_filter='*FalseAppliedWithoutConsumedRemainsPendingWithoutCompletionCleanupOrWake:*FalseAlreadyAppliedWithoutConsumedRetriesAfterRestart:*ExactConsumedPostconditionCompletesAndCleansExactlyOnce:*SharedStoreApplicationHandlePrecedesHandlerAndCleanup' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - `ProcessFinalizedCertificate` contains a post-handler durable reservation reread before the only resource-bearing `MarkComplete` path.
    - Source assertions compare state, slot/outpoint, generation, certificate digest, proposal ID, and winner ID before success.
    - All three exact test names are discovered once; false Applied and false AlreadyApplied leave process pending with zero cleanup/wake, while exact Consumed completes once.
    - Retry/restart calls the same certified winner and cannot select, release, or clean another proposal.
    - Legacy non-resource certificate success remains outside the new reservation postcondition.
  </acceptance_criteria>
  <done>Resource handler success cannot escape exact durable consumption or trigger premature completion side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Make finalized-handle payload failures irreconcilable and run closure gates</name>
  <files>src/account/TransactionManager.cpp, test/src/account/transaction_manager_pending_lifecycle_test.cpp</files>
  <read_first>
    - src/account/TransactionManager.cpp
    - src/account/TransactionManager.hpp
    - src/account/MintTransactionV2.cpp
    - src/account/GeniusTransaction.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - src/blockchain/ConsensusStateStore.hpp
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - test/src/account/utxo_manager_test.cpp
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
  </read_first>
  <action>
In `TransactionManager::OnConsensusCertificate`, make each standalone-certificate fallback branch aware of `finalized_handle`. If `DecodeNonceSubject` fails, the embedded transaction is absent, `DeSerializeEmbeddedTransaction` fails, or the reconstructed transaction fails `tx->GetHash() == tx_hash` / `CheckHash()`, return `ApplicationDisposition::Irreconcilable` whenever a finalized reservation handle is present. Preserve the existing compatibility `Applied` result only for the no-handle legacy/non-resource path. Keep storage, owner-dead, and unavailable dependencies retryable; do not convert operational failures into permanent safety errors.

Add exact tests `FinalizedHandleMalformedEmbeddedMintIsIrreconcilable`, `FinalizedHandleHashMismatchIsIrreconcilable`, and `LegacyFallbackWithoutFinalizedHandleRetainsCompatibility`. Exercise the production application callback or `OnConsensusCertificate` friend seam with a complete exact handle; do not merely test a copied classifier. For the two finalized cases, also drive the disposition through the focused consensus finalizer and assert terminal protection/no cleanup rather than `Applied`. Then run the focused account targets followed by the exact Phase 11 nine-target and Phase 10 eight-target guarded gates.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target transaction_manager_pending_lifecycle_test consensus_burn_reservation_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_list_tests | grep -Ec 'FinalizedHandleMalformedEmbeddedMintIsIrreconcilable|FinalizedHandleHashMismatchIsIrreconcilable|LegacyFallbackWithoutFinalizedHandleRetainsCompatibility')" -eq 3 &amp;&amp; build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_filter='*FinalizedHandleMalformedEmbeddedMintIsIrreconcilable:*FinalizedHandleHashMismatchIsIrreconcilable:*LegacyFallbackWithoutFinalizedHandleRetainsCompatibility' --gtest_brief=1 &amp;&amp; test "$(ctest --test-dir build/OSX/Release -N -R '^(consensus_burn_reservation_test|consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|transaction_manager_pending_lifecycle_test|utxo_manager_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test)$' | sed -n 's/^Total Tests: //p')" -eq 9 &amp;&amp; ctest --test-dir build/OSX/Release -R '^(consensus_burn_reservation_test|consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|transaction_manager_pending_lifecycle_test|utxo_manager_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test)$' --no-tests=error --output-on-failure &amp;&amp; test "$(ctest --test-dir build/OSX/Release -N -R '^(consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test|transaction_manager_pending_lifecycle_test|utxo_manager_test)$' | sed -n 's/^Total Tests: //p')" -eq 8 &amp;&amp; ctest --test-dir build/OSX/Release -R '^(consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test|transaction_manager_pending_lifecycle_test|utxo_manager_test)$' --no-tests=error --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Every finalized-handle decode, absent-embedded, deserialization, and hash-binding failure returns `Irreconcilable`; none returns `Applied`.
    - The no-finalized-handle compatibility control retains existing legacy behavior.
    - Malformed and hash-mismatched finalized mint tests reach durable protected terminal handling with zero proposal cleanup.
    - Exact guarded discovery reports 9 Phase 11 and 8 Phase 10 targets before both suites pass.
    - No WR-03 registration replacement, mock RPC, Phase 12 network race, or unrelated source change is introduced.
  </acceptance_criteria>
  <done>Invalid certified mint payloads cannot masquerade as successful application, and the full Phase 11/10 closure remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| Resource handler disposition to durable consensus completion | A buggy or stale handler can spoof `Applied` without consuming the exact burn |
| Certified embedded payload to TransactionManager fallback | Malformed or hash-substituted bytes can bypass mint application while finality is marked complete |
| Retry/restart to cleanup and dependency wake | Premature side effects can hide missing effects and stop exact-winner recovery |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-11-10-01 | High | Spoofing | BURN-04 completion requires an exact identity-matched durable `CONSUMED` reread, not handler assertion |
| T-11-10-02 | High | Tampering | Finalized-handle decode/deserialization/hash failures are irreconcilable and preserve certificate-bound protection |
| T-11-10-03 | High | Elevation of Privilege | MarkComplete, Applied lifecycle, cleanup, dependency wake, and work-journal completion all remain behind the postcondition |
| T-11-10-04 | Medium | Denial of Service | Storage/absence/final-pending failures remain retryable for the exact winner across restart instead of becoming false completion |
</threat_model>

<verification>
Run both nonzero/exact focused discovery guards, inspect the single resource-success branch from disposition through durable reread to completion, then run the exact Phase 11 nine-target and Phase 10 eight-target guarded gates and `git diff --check`.
</verification>

<success_criteria>
- BURN-04 has a mechanically enforced `CONSUMED` postcondition before every completion, cleanup, and dependency-wake side effect.
- False success and invalid finalized payloads remain fail-closed, exact-winner-bound, and restart-safe without releasing BURN-03 protection.
- Both exact regression closures pass and deferred/non-goal work remains absent.
</success_criteria>

<output>
Create `.planning/phases/11-slot-owned-bridge-burn-reservations/11-10-SUMMARY.md` when done.
</output>
