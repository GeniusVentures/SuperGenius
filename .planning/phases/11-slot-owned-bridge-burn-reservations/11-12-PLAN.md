---
phase: 11-slot-owned-bridge-burn-reservations
plan: 12
type: execute
gap_closure: true
wave: 11
depends_on:
  - 11-11
files_modified:
  - src/blockchain/Consensus.cpp
  - test/src/blockchain/consensus_burn_reservation_test.cpp
  - test/src/account/transaction_manager_pending_lifecycle_test.cpp
autonomous: true
requirements:
  - BURN-03
  - BURN-04
must_haves:
  truths:
    - "A live SAFETY_ERROR or CONSUMED_SAFETY_ERROR shortcut is idempotent only when the strict reservation outpoint, generation, certificate digest, proposal ID, and winner ID all match the authoritative process and currently normalized certificate."
    - "A same-slot terminal record with any identity mismatch fails closed with actionable slot, expected, and observed evidence; it is never returned as AlreadyFinalized and cannot mark certificate work done."
    - "A composed production-path test starts with a genuinely certified mint applied by the registered TransactionManager callback through UTXOManager, then corrupts a persisted mint artifact and proves restart recovery advances CONSUMED to CONSUMED_SAFETY_ERROR exactly once."
    - "After consumed-terminal recovery, duplicate certificate delivery, another restart, later handler registration, timer reconciliation, and recovery scheduling perform no handler retry, process completion, proposal cleanup, dependency wake, conditional release, remint, or alternate-winner application."
    - "BURN-03 reciprocal protection and BURN-04 physical consumption remain exact across corruption and every restart; WR-02 callback replacement, mock RPC, broad fuzzing, and Phase 12 race work remain out of scope."
  artifacts:
    - path: src/blockchain/Consensus.cpp
      provides: Exact authoritative terminal-identity gate before every live terminal shortcut and certificate-work completion decision
    - path: test/src/blockchain/consensus_burn_reservation_test.cpp
      provides: Live exact-idempotence and terminal-identity mismatch delivery coverage with actionable failure evidence
    - path: test/src/account/transaction_manager_pending_lifecycle_test.cpp
      provides: Composed certified mint, persisted-artifact corruption, production callback, restart recovery, and permanent suppression proof
  key_links:
    - from: ConsensusManager::ProcessFinalizedCertificate terminal reservation read
      to: authoritative ProcessRecord plus normalized certificate identity
      via: one exact slot/outpoint/generation/certificate-digest/proposal-ID/winner-ID comparison before AlreadyFinalized
    - from: ConsensusManager::FinalizeSlot certificate-work journal
      to: terminal live-delivery result
      via: mismatched terminal identity returning StorageFailure rather than AlreadyFinalized so MarkDone is unreachable
    - from: persisted bridge application/output/input corruption
      to: ConsensusStateStore::MarkBurnReservationSafetyError
      via: reopened UTXOManager state, registered TransactionManager application callback, and ProcessFinalizedCertificate recovery of the exact pending process
    - from: CONSUMED_SAFETY_ERROR restart restoration
      to: proposal cleanup, dependency wake, release, and remint suppression
      via: terminal lifecycle installation before recovered work scheduling and exact duplicate-delivery short-circuit
---

<objective>
Close the last Phase 11 verification gaps in live terminal identity handling and composed consumed-artifact recovery.

Purpose: Ensure terminal idempotence belongs only to the exact authoritative certificate and prove the full production recovery chain preserves a corrupted-but-consumed burn without retrying or reopening it.
Output: An exact live terminal guard plus deterministic production-path corruption/restart coverage.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
This gap plan closes only the two blocking findings in 11-VERIFICATION.md. Preserve Plans 11-01 through 11-11 and their summaries exactly. WR-02 stale weak callback registration is advisory and out of scope. Do not add callback replacement/unregistration, mock RPC, broad fuzzing, the 11-node race, or other Phase 12 work.
</execution_note>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-CONTEXT.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-REVIEW.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-10-PLAN.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-10-SUMMARY.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-11-PLAN.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Require exact authoritative identity for live terminal replay</name>
  <files>src/blockchain/Consensus.cpp, test/src/blockchain/consensus_burn_reservation_test.cpp</files>
  <read_first>
    - src/blockchain/Consensus.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/ConsensusStateStore.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/blockchain/impl/proto/ConsensusLocalState.proto
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-REVIEW.md
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-10-PLAN.md
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-11-PLAN.md
  </read_first>
  <action>
In `ConsensusManager::ProcessFinalizedCertificate`, derive the canonical mint outpoint and the expected terminal identity from the already-validated `ProcessRecord` plus the currently normalized authoritative certificate before examining `SAFETY_ERROR` or `CONSUMED_SAFETY_ERROR`. Reuse one narrow exact-identity predicate for the early terminal shortcut and the post-handler terminal reread. It must compare the strict reservation's slot ID, source chain, burn hash, receipt-local index, generation, certificate digest, proposal ID, and winner ID. The outpoint must equal the certificate-derived canonical outpoint; the digest/proposal/winner must equal both the process record and normalized certificate identity; the generation must remain exact through the store's reciprocal slot/outpoint reread rather than being accepted from an unchecked slot record.

Return `AlreadyFinalized` and install `SafetyViolation` only for an exact match. For a readable same-slot terminal record with any mismatch, emit an error/critical diagnostic containing the slot, terminal state, expected process/certificate digest-proposal-winner identity, and observed reservation identity, then return `StorageFailure` (or the existing typed fail-closed integrity result if the function is narrowly refactored to expose it). Do not call the application handler, do not mark the process complete, do not clean proposals or wake dependencies, and specifically do not let `FinalizeSlot` reach `certificate_work_journal_->MarkDone`. Preserve strict storage-read failures as `StorageFailure`; do not mutate or release the mismatched terminal record and do not invent a second certificate authority.

Add focused tests named `LiveTerminalExactIdentityDuplicateIsIdempotentWithoutHandlerCleanupOrWake` and `LiveTerminalIdentityMismatchFailsClosedAndCannotMarkCertificateWorkDone`. Exercise live `FinalizeSlot`/`ProcessFinalizedCertificate` delivery rather than startup-only validation. The exact case must cover both terminal enums across subcases or a typed parameter and prove zero handler/cleanup/wake calls. The mismatch matrix must cover readable finality mismatches for certificate digest, proposal ID, and winner ID, plus canonical outpoint and reciprocal-generation corruption that strict reading rejects before idempotence. Use only the existing friend-only durable-state seam. Every subcase must return non-success, perform no additional durable mutation, leave process/work pending, and capture actionable mismatch or strict-integrity evidence. Do not weaken strict startup checks or change terminal-state schema.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_burn_reservation_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_list_tests | grep -Ec 'LiveTerminalExactIdentityDuplicateIsIdempotentWithoutHandlerCleanupOrWake|LiveTerminalIdentityMismatchFailsClosedAndCannotMarkCertificateWorkDone')" -eq 2 &amp;&amp; build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_filter='*LiveTerminalExactIdentityDuplicateIsIdempotentWithoutHandlerCleanupOrWake:*LiveTerminalIdentityMismatchFailsClosedAndCannotMarkCertificateWorkDone' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - The only early terminal `AlreadyFinalized` branch is dominated by an exact outpoint, generation, certificate digest, proposal ID, and winner ID comparison against both process and certificate identity.
    - Every readable identity mismatch returns fail-closed without handler invocation, SafetyViolation idempotence, process completion, cleanup, dependency wake, or certificate-work `MarkDone`.
    - Diagnostics identify the slot and expected-versus-observed finality identity sufficiently to investigate the contradiction.
    - Both exact test names are discovered exactly once; exact duplicate delivery is idempotent, readable finality mismatches preserve byte-identical terminal and reciprocal records, and strict outpoint/generation corruption is rejected without further mutation.
    - No terminal schema, release path, callback-registration lifecycle, or Phase 12 behavior is changed.
  </acceptance_criteria>
  <done>Live terminal replay is idempotent only for the exact authoritative certificate and every mismatch remains visible, pending, and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove composed certified-mint corruption recovery through production managers</name>
  <files>test/src/account/transaction_manager_pending_lifecycle_test.cpp</files>
  <read_first>
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - test/src/account/utxo_manager_test.cpp
    - src/account/TransactionManager.cpp
    - src/account/TransactionManager.hpp
    - src/account/UTXOManager.cpp
    - src/account/UTXOManager.hpp
    - src/account/MintTransactionV2.cpp
    - src/blockchain/Blockchain.hpp
    - src/blockchain/impl/Blockchain.cpp
    - src/blockchain/Consensus.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/blockchain/ConsensusStateStore.hpp
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-11-PLAN.md
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-11-SUMMARY.md
  </read_first>
  <action>
Replace the missing composed evidence with one exact test named `CertifiedMintPersistedArtifactCorruptionRecoversToConsumedSafetyAndNeverRetries` in the existing CRDT/RocksDB-backed TransactionManager lifecycle fixture. Build a valid embedded `MintTransactionV2`, proposal, vote, and certificate; deliver it through the production consensus finalization path with the callback installed by `TransactionManager::New`. Prove the first delivery invokes the real TransactionManager handler and `UTXOManager::ApplyMintEffectsAtomically`, and verify in durable storage that the canonical bridge application, ordered winner output(s), consumed bridge input, reciprocal reservation index, and exact `CONSUMED` reservation committed together.

Use only the existing `TransactionManagerPendingLifecycleTestAccess` friendship to add a controlled test helper that removes one persisted canonical bridge-application artifact under the UTXO persistence/state lock order and exact key derived by `UTXOManager::MakeBridgeApplicationKey`; do not use `bridge_application_reader_`, a synthetic handler, a reservation-only batch, public production hooks, or mock RPC. Recreate the exact pending process/work seam for commit-before-completion using the existing private consensus/store friend access, retaining the authoritative certificate/process digest, proposal ID, winner ID, outpoint, and generation. The test seam may rewrite only the local process/work marker required to model the crash boundary; it must not synthesize consumption or terminal safety.

Close the first `TransactionManager`/`Blockchain`/consensus ownership graph, reconstruct production objects over the same RocksDB, reload UTXOs, and let later real handler registration drive `RecoverRestoredCertificateWork`. Count the production handler through the existing finalized-handle observer and assert exactly one recovery attempt. That attempt must observe the missing persisted application while the input/reservation are consumed, return `Irreconcilable`, and cause `ProcessFinalizedCertificate` to durably transition the exact record to `CONSUMED_SAFETY_ERROR` with unchanged slot, outpoint, generation, certificate digest, proposal ID, winner ID, and reciprocal index.

Then close and reconstruct again over the same path, register the real TransactionManager handler, trigger the owned reconciliation/recovery seam, and redeliver the exact certificate through live ingress. Assert the handler count remains one, the process is not falsely COMPLETE, any certificate dependency waiter remains unwoken, proposal cleanup evidence remains unchanged, the removed application stays absent, winner-output and consumed-input bytes do not change, no additional output/remint appears, conditional deletion and create/join both fail, and no alternate winner can apply. Use deterministic friend-only counters/predicates and existing reconstruction helpers; no sleeps beyond existing bounded fixture initialization, detached workers, registration replacement, or synthetic callback.

After the focused test passes, run the exact Phase 11 nine-target and Phase 10 eight-target guarded closures sequentially. The Phase 11 expression is exactly `consensus_burn_reservation_test|consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|transaction_manager_pending_lifecycle_test|utxo_manager_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test`; the Phase 10 compatibility expression is the same list without `consensus_burn_reservation_test`.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target transaction_manager_pending_lifecycle_test consensus_burn_reservation_test utxo_manager_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_list_tests | grep -Ec 'CertifiedMintPersistedArtifactCorruptionRecoversToConsumedSafetyAndNeverRetries')" -eq 1 &amp;&amp; build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_filter='*CertifiedMintPersistedArtifactCorruptionRecoversToConsumedSafetyAndNeverRetries' --gtest_brief=1 &amp;&amp; test "$(ctest --test-dir build/OSX/Release -N -R '^(consensus_burn_reservation_test|consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|transaction_manager_pending_lifecycle_test|utxo_manager_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test)$' | sed -n 's/^Total Tests: //p')" -eq 9 &amp;&amp; ctest --test-dir build/OSX/Release -R '^(consensus_burn_reservation_test|consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|transaction_manager_pending_lifecycle_test|utxo_manager_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test)$' --no-tests=error --output-on-failure &amp;&amp; test "$(ctest --test-dir build/OSX/Release -N -R '^(consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test|transaction_manager_pending_lifecycle_test|utxo_manager_test)$' | sed -n 's/^Total Tests: //p')" -eq 8 &amp;&amp; ctest --test-dir build/OSX/Release -R '^(consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test|transaction_manager_pending_lifecycle_test|utxo_manager_test)$' --no-tests=error --output-on-failure &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The named test is discovered exactly once and begins with a valid certificate delivered through production consensus, TransactionManager, and UTXOManager code—not a direct reservation transition or synthetic application handler.
    - The first certified application durably proves application/output/input artifacts and exact `CONSUMED`; the corruption seam physically removes one persisted artifact without reader mocking.
    - Same-path restart and real handler registration drive exactly one contradiction attempt through `UTXOManager -> TransactionManager -> ProcessFinalizedCertificate`, ending in exact durable `CONSUMED_SAFETY_ERROR`.
    - Another restart, recovery/reconciliation, later production-handler registration, and duplicate certificate ingress leave handler calls at one and produce zero cleanup, dependency wake, release, remint, or alternate-winner effects.
    - Reservation and reciprocal identity remain unchanged; the corrupted artifact stays absent and all remaining persisted input/output bytes remain unchanged.
    - Exact guarded discovery reports 9 Phase 11 and 8 Phase 10 targets before both suites pass; `git diff --check` passes.
    - WR-02 callback replacement, mock RPC, broad fuzzing, and Phase 12 race/compatibility expansion are absent.
  </acceptance_criteria>
  <done>The required composed production path proves consumed-artifact corruption becomes permanent exact terminal safety and can never reopen or reapply the burn.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
## ASVS L1 Trust Boundaries

| Boundary | Risk |
|---|---|
| Live terminal local state to authoritative process/certificate identity | Same-slot corrupted terminal data can spoof idempotence and falsely retire authoritative work |
| Persisted UTXO/application artifacts to production recovery classification | Partial durable corruption can be hidden by component-only tests or mistranslated across managers |
| Consumed-terminal restart to later callbacks and cleanup | Recovery ordering can retry, remint, release, wake, or clean before terminal protection suppresses work |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-11-12-01 | High | Spoofing | Terminal replay requires exact outpoint, generation, digest, proposal, and winner agreement with process and certificate before AlreadyFinalized |
| T-11-12-02 | High | Tampering | Persisted artifact deletion is driven through real UTXO/Transaction/consensus recovery and must preserve exact consumed-terminal and reciprocal evidence |
| T-11-12-03 | High | Elevation of Privilege | Identity mismatch cannot reach certificate-work MarkDone, handler success, cleanup, wake, release, or alternate-winner application |
| T-11-12-04 | High | Denial of Service | Exact terminal recovery suppresses futile retries while actionable mismatch diagnostics keep unrelated terminal corruption visible rather than silently complete |
| T-11-12-05 | Medium | Repudiation | Named live mismatch and composed same-path restart tests retain before/after identities, artifact state, counters, and diagnostic evidence |

All High threats are blocking: execution is incomplete until each mitigation has direct named-test evidence and both guarded regression closures pass.
</threat_model>

<verification>
Run both exact focused name/discovery guards, inspect the live terminal branch and composed production call chain, then run the exact Phase 11 nine-target closure, the Phase 10 eight-target compatibility closure, and `git diff --check` sequentially.
</verification>

<success_criteria>
- Exact terminal identity is mandatory before live AlreadyFinalized or certificate-work completion.
- The production UTXOManager/TransactionManager/consensus recovery chain converts persisted post-consumption corruption to exact non-retryable `CONSUMED_SAFETY_ERROR`.
- BURN-03 no-release and BURN-04 physical-consumption authority survive duplicate delivery and repeated restart without cleanup, wake, remint, or alternate winner.
- Every ASVS L1 High threat has blocking automated evidence, both exact guarded closures pass, and Phase 12 plus WR-02 scope remains untouched.
</success_criteria>

<output>
Create `.planning/phases/11-slot-owned-bridge-burn-reservations/11-12-SUMMARY.md` when done.
</output>
