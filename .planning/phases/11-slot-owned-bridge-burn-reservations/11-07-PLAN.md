---
phase: 11-slot-owned-bridge-burn-reservations
plan: 07
type: execute
wave: 6
depends_on:
  - 11-06
files_modified:
  - src/blockchain/ConsensusStateStore.hpp
  - src/blockchain/ConsensusStateStore.cpp
  - src/account/UTXOManager.hpp
  - src/account/UTXOManager.cpp
  - src/account/TransactionManager.hpp
  - src/account/TransactionManager.cpp
  - test/src/blockchain/consensus_burn_reservation_test.cpp
  - test/src/account/utxo_manager_test.cpp
  - test/src/account/transaction_manager_pending_lifecycle_test.cpp
autonomous: true
requirements:
  - BURN-03
  - BURN-04
must_haves:
  truths:
    - D-11: winner outputs, canonical bridge application record, physical bridge-input consumption, and reservation Consumed transition commit in one RocksDB batch or remain entirely absent.
    - Atomic application uses only Plan 11-06's exact shared store, immutable handle, datastore identity check, and single serialization gate; no second store, recursive gate, or post-commit reservation update exists.
    - A missing synthetic bridge input is materialized only from exact certified facts; a different application, winner, consumption, generation, or output set is irreconcilable.
    - Duplicate and restart replay return AlreadyApplied only after live verification of the exact slot, outpoint, generation, certificate, proposal, winner, ordered outputs, application record, consumed input, and Consumed reservation.
    - A deterministic competing writer cannot cross the shared store gate between finalized reread/check and the one batch commit.
  artifacts:
    - path: src/account/UTXOManager.cpp
      provides: One-batch certified mint outputs, application, bridge-input consumption, and reservation consumption
    - path: src/blockchain/ConsensusStateStore.cpp
      provides: Finalized-reservation verification and Consumed staging through the established shared gate
    - path: test/src/account/utxo_manager_test.cpp
      provides: Precommit failure, exact replay, materialization, and contradiction matrix
    - path: test/src/blockchain/consensus_burn_reservation_test.cpp
      provides: Deterministic blocked competing-writer and legal atomic-outcome coverage
  key_links:
    - from: TransactionManager::ApplyConfirmedMintV2
      to: UTXOManager::ApplyMintEffectsAtomically
      via: Plan 11-06 immutable FinalizedReservationApplicationHandle
    - from: UTXO atomic staging participant
      to: BurnReservationRecord::CONSUMED
      via: exact shared store gate and the same datastore batch as outputs/input/application
---

<objective>
Apply the certified mint and consume its durable reservation in one crash-atomic batch.

Purpose: Prevent partial mint effects, false replay success, or a competing reservation writer from crossing the finalized read-to-commit interval.
Output: UTXO batch participation, Consumed transition, exact replay/safety classification, and deterministic concurrency tests.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
Use the ownership, identity, handler, gate, and lock-order contracts completed by Plan 11-06. Do not redesign that plumbing, construct another store, or move abandonment/reconciliation into this plan.
</execution_note>

<context>
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-CONTEXT.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-RESEARCH.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-PATTERNS.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-06-PLAN.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-14-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage exact mint effects and reservation consumption in the shared batch</name>
  <files>src/blockchain/ConsensusStateStore.hpp, src/blockchain/ConsensusStateStore.cpp, src/account/UTXOManager.hpp, src/account/UTXOManager.cpp, src/account/TransactionManager.hpp, src/account/TransactionManager.cpp, test/src/blockchain/consensus_burn_reservation_test.cpp, test/src/account/utxo_manager_test.cpp, test/src/account/transaction_manager_pending_lifecycle_test.cpp</files>
  <read_first>
    - src/blockchain/ConsensusStateStore.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/account/UTXOManager.hpp
    - src/account/UTXOManager.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - src/storage/rocksdb/rocksdb.hpp
    - test/src/account/utxo_manager_test.cpp
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-06-PLAN.md
  </read_first>
  <action>
Implement the UTXO participant for Plan 11-06's shared-store batch API. Extend `AtomicMintEffectRequest` with the exact slot/outpoint/generation and authoritative certificate digest/proposal/winner identity from the immutable handle. Under the established gate and lock order, reread and validate FinalizedPendingApplication, stage ordered winner outputs, the canonical bridge application record, physical bridge-input consumption, and the reciprocal reservation Consumed records, then commit all artifacts in one RocksDB batch. Use unlocked store helpers inside the gate; make no second store call and write no reservation state after commit.

If the synthetic bridge input is absent on a certificate-only node, materialize it deterministically only from the certified canonical chain/burn/receipt descriptor in candidate state and consume it in that same batch. An existing mismatched input, application, output, winner, slot, outpoint, generation, certificate, proposal, or consumption is irreconcilable and feeds Plan 11-05's durable SafetyError disposition. Storage and injected precommit failures remain retryable and mutate neither durable artifacts nor in-memory UTXO maps.

On duplicate or restart replay, return AlreadyApplied only after verifying every live durable artifact: exact ordered outputs, canonical application record, consumed bridge input, reciprocal reservation state Consumed, and all immutable handle identities. Partial presence is never success. Preserve ordinary transaction reservation and existing UTXO atomic persistence behavior.

Extend the friend-only precommit fault stage and tests. Prove all four artifact groups are absent before/failure and present after commit; reopen after commit-before-processing-complete and observe exact AlreadyApplied without duplicate outputs; safely materialize a missing synthetic input; reject every different-winner/identity/artifact/datastore mismatch; and keep ordinary UTXO tests green. Add a deterministic barrier after reservation reread/check but before batch commit, start a finalize/release writer on the exact shared store, prove with predicates/counters that it remains blocked at the serialization gate, release application, and verify one legal atomic outcome. Use no sleep or detached work.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_burn_reservation_test utxo_manager_test transaction_manager_pending_lifecycle_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_list_tests | grep -Ec 'Application|Atomic|Consumed|CompetingWriter')" -gt 0 &amp;&amp; build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_filter='*Application*:*Atomic*:*Consumed*:*CompetingWriter*' --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/utxo_manager_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - One batch contains winner outputs, application record, bridge-input consumption, and reciprocal reservation Consumed state; no separate reservation commit exists.
    - Precommit failure leaves all persistent artifacts and in-memory candidate maps unchanged.
    - Replay verifies every exact artifact before AlreadyApplied; partial or different state returns irreconcilable rather than retry success.
    - Missing synthetic input is safely materialized only from the certified descriptor and consumed atomically.
    - The exact Plan 11-06 store gate spans reread/check/stage/commit and a deterministic paused test proves a competing writer cannot pass it.
    - Lock order remains store -> UTXO persistence -> UTXO state with no recursive store acquisition, sleep, or detached work.
  </acceptance_criteria>
  <done>Certified mint application and durable reservation consumption share one exact, replay-safe, serialized commit boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| Finalized reservation to UTXO persistence | Split commits can expose a consumed burn without its winner or an applied winner with reusable protection |
| Certificate-only facts to synthetic input | Untrusted reconstruction can consume the wrong outpoint |
| Durable replay state to AlreadyApplied | Partial or different-winner state can be mistaken for idempotent success |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-11-07-01 | High | Tampering | One shared-gate batch commits outputs, application, input consumption, and Consumed reservation together |
| T-11-07-02 | High | Spoofing | Synthetic input and all request identities derive from the immutable certified handle |
| T-11-07-03 | High | Elevation of Privilege | AlreadyApplied requires exact live verification of every durable artifact |
| T-11-07-04 | High | Denial of Service | Deterministic blocked-writer coverage and fixed lock order prove the read/check/commit interval cannot split or deadlock |
</threat_model>

<verification>
Run the guarded atomic/application tests and both account regressions; inspect the single batch, exact replay checks, and lock ordering; run `git diff --check`.
</verification>

<success_criteria>
- D-11 and BURN-04 have one crash-safe physical completion boundary.
- Atomic application preserves D-10, D-12, D-13, and D-14 and the exact shared-store architecture.
</success_criteria>

<output>
Create `.planning/phases/11-slot-owned-bridge-burn-reservations/11-07-SUMMARY.md` when done.
</output>
