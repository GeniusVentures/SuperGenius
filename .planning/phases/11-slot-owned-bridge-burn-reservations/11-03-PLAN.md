---
phase: 11-slot-owned-bridge-burn-reservations
plan: 03
type: execute
wave: 2
depends_on:
  - 11-02
files_modified:
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - src/blockchain/impl/Blockchain.cpp
  - test/src/blockchain/consensus_burn_reservation_test.cpp
  - test/src/blockchain/consensus_vote_journal_test.cpp
autonomous: true
requirements:
  - BURN-01
  - BURN-04
  - BURN-05
must_haves:
  truths:
    - D-01/D-03: reservation scan and reconciliation finish before subscription, timer, proposal admission, restored application work, or vote replay.
    - D-03: missing ephemeral candidates after restart are normal and neither abort startup nor immediately unlock a Reserved record.
    - D-04: malformed reciprocal records and reservation/certificate/vote contradictions abort construction before observable side effects.
    - Canonical nonce/mint slot derivation is available before ConsensusManager::RestoreLocalState in a real fresh process; restoration never depends on TransactionManager registration or stale static state.
    - D-10/D-12: authoritative certificates restore or synthesize exact FinalizedPendingApplication protection even before the transaction handler is registered.
  artifacts:
    - path: src/blockchain/Consensus.cpp
      provides: Fail-closed reservation restoration and reconciliation in startup ordering
    - path: src/blockchain/impl/Blockchain.cpp
      provides: Built-in canonical nonce/mint slot resolver installation before consensus construction
    - path: test/src/blockchain/consensus_burn_reservation_test.cpp
      provides: Fresh-process-like resolver, restart, reconciliation, and startup-order cases
  key_links:
    - from: Blockchain::New / ConsensusManager::New
      to: ConsensusManager::RestoreLocalState
      via: built-in resolver registration before construction and strict local-state scan before side effects
---

<objective>
Restore and reconcile durable burn reservations safely before live consensus activity.

Purpose: Eliminate the restart interval and static-handler ordering hazard that could make a protected burn appear under the wrong slot or become available.
Output: Production startup ordering, fail-closed reconciliation, and real-process-like restart tests.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-CONTEXT.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-RESEARCH.md
@.planning/phases/10-durable-vote-lock-and-finalization-state-machine/10-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make canonical mint slot resolution a startup prerequisite and reconcile reservations</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, src/blockchain/impl/Blockchain.cpp, test/src/blockchain/consensus_burn_reservation_test.cpp, test/src/blockchain/consensus_vote_journal_test.cpp</files>
  <read_first>
    - src/blockchain/impl/Blockchain.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - src/blockchain/ConsensusStateStore.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/account/TransactionManager.cpp
    - src/account/MintTransactionV2.hpp
    - src/account/MintTransactionV2.cpp
    - test/src/blockchain/consensus_vote_journal_test.cpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
  </read_first>
  <action>
Move or install the built-in nonce/mint slot resolver at a production construction point that executes before `ConsensusManager::New()` invokes `RestoreLocalState()`. Preserve single-registration/no-overwrite semantics and generic fallback only for truly unregistered subject types. Do not depend on `TransactionManager::New()`, a local account, UTXO load, or prior-process static-map residue. Keep this narrowly to canonical handler ordering; do not add mock RPC or relayer startup infrastructure.

Extend `RestoreLocalState()` to strict-scan reciprocal burn records alongside votes/process/conflict/safety state and authoritative certificates. Recompute canonical slot/outpoint identity; cross-check finalized records against certificate digest/proposal/winner/burn descriptor; cross-check same-slot durable votes and their acceptance horizons without requiring candidate objects. If an authoritative mint certificate exists, create or restore FinalizedPendingApplication protection before application recovery even when no proposal or handler exists. Treat missing ephemeral candidates as expected. Abort `ConsensusManager::New()` before `subscribe`, filter registration, timer start, handler recovery, or vote replay for malformed/half-present records, slot mismatch, certificate mismatch, or contradictory vote identity. Storage unavailability remains typed and fail closed.

Add restart tests that explicitly reset handler registration state, reconstruct over the same RocksDB, observe startup events, and prove: Reserved survives without candidates; final certificate creates/upgrades protection before handlers; missing handler remains pending; candidate-horizon and active/equality vote horizons retain protection; corrupt/contradictory state returns null with zero observable startup side effects.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_burn_reservation_test consensus_vote_journal_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_list_tests | grep -Ec 'Restart|Reconcile|Startup|Horizon')" -gt 0 &amp;&amp; build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_filter='*Restart*:*Reconcile*:*Startup*:*Horizon*' --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/consensus_vote_journal_test --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - Production construction installs canonical mint slot derivation before `RestoreLocalState`; a fresh-process-like test passes after clearing static registrations.
    - Startup event assertions show reservation reconciliation precedes subscribe/filter/timer/replay/application activity.
    - A Reserved record with no ephemeral candidate remains protected and does not fail startup.
    - Certificate-backed state restores FinalizedPendingApplication before transaction handler registration; no alternate winner is selected.
    - Every malformed reciprocal or certificate/vote contradiction aborts construction with zero observed live side effects.
  </acceptance_criteria>
  <done>Real startup restores canonical reservation protection and fails closed before exposing consensus activity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| Static handler registry to fresh process startup | Missing resolver can derive the wrong mint slot during restoration |
| Durable records to live subscriptions/replay | Side effects before validation can vote or apply from corrupt state |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-11-03-01 | High | Spoofing | Built-in canonical mint resolver is installed before restore and rechecks the exact descriptor |
| T-11-03-02 | High | Elevation of Privilege | Fail-closed scan/reconciliation completes before all observable consensus side effects |
| T-11-03-03 | High | Tampering | Certificate, vote, slot, outpoint, and reciprocal-index relationships are cross-validated |
| T-11-03-04 | Medium | Denial of Service | Missing ephemeral candidates and handlers are normal recoverable states, not corruption |
</threat_model>

<verification>
Run the guarded restart/reconciliation tests and vote-journal regression; inspect `ConsensusManager::New()` event ordering and run `git diff --check`.
</verification>

<success_criteria>
- D-01, D-03, D-04, D-10, and D-12 hold across a genuine fresh-process-like reconstruction.
- No protected burn is temporarily Ready during restart.
</success_criteria>

<output>
Create `.planning/phases/11-slot-owned-bridge-burn-reservations/11-03-SUMMARY.md` when done.
</output>
