---
phase: 11-slot-owned-bridge-burn-reservations
plan: 11
type: execute
gap_closure: true
wave: 10
depends_on:
  - 11-10
files_modified:
  - src/blockchain/impl/proto/ConsensusLocalState.proto
  - src/blockchain/ConsensusStateStore.hpp
  - src/blockchain/ConsensusStateStore.cpp
  - src/blockchain/Consensus.cpp
  - test/src/blockchain/consensus_burn_reservation_test.cpp
  - test/src/account/transaction_manager_pending_lifecycle_test.cpp
  - test/src/account/utxo_manager_test.cpp
autonomous: true
requirements:
  - BURN-03
  - BURN-04
must_haves:
  truths:
    - "An identity-matched irreconcilable contradiction discovered after CONSUMED advances monotonically to an explicit consumed-terminal safety representation; it never regresses to final-pending, Reserved, or absent."
    - "The terminal record durably preserves that physical consumption preceded the contradiction, plus exact slot/outpoint/generation/certificate/proposal/winner identity, reciprocal outpoint protection, and critical diagnostic text."
    - "Identity mismatch, generation mismatch, different finality, or different diagnostic cannot mutate a consumed reservation or overwrite another winner; only exact idempotent terminal replay succeeds."
    - "Restart/recovery recognizes consumed-terminal safety before application scheduling, invokes no further transaction handler, performs no proposal cleanup or dependency wake, and never releases or remints the burn."
    - "BURN-04 remains certificate-authoritative and BURN-03 protection remains non-releasable even when application/output artifacts are missing or conflicting after physical consumption."
  artifacts:
    - path: src/blockchain/impl/proto/ConsensusLocalState.proto
      provides: Explicit `CONSUMED_SAFETY_ERROR` terminal state retaining the durable consumed fact
    - path: src/blockchain/ConsensusStateStore.cpp
      provides: Strict identity-matched `CONSUMED -> CONSUMED_SAFETY_ERROR` transition and reciprocal validation
    - path: src/blockchain/Consensus.cpp
      provides: Terminal consumed-safety recovery suppression with no cleanup, wake, release, or handler retry
    - path: test/src/blockchain/consensus_burn_reservation_test.cpp
      provides: Store transition plus end-to-end restart/recovery terminal evidence
    - path: test/src/account/transaction_manager_pending_lifecycle_test.cpp
      provides: Exact production application handler mapping of consumed artifact contradiction to irreconcilable
    - path: test/src/account/utxo_manager_test.cpp
      provides: Missing/conflicting post-consumption artifact detection feeding the terminal consensus path
  key_links:
    - from: UTXOManager consumed-artifact `state_not_recoverable`
      to: ConsensusStateStore consumed-terminal safety record
      via: TransactionManager `Irreconcilable` and ProcessFinalizedCertificate exact-identity safety transition
    - from: RestoreLocalState and RecoverRestoredCertificateWork
      to: terminal reservation state
      via: pre-scheduling recognition that suppresses exact-winner handler retry while preserving certificate finality
---

<objective>
Persist terminal safety for exact-winner contradictions discovered after physical burn consumption.

Purpose: Preserve the durable consumed fact and stop futile recovery without weakening finality, reciprocal protection, or no-release guarantees.
Output: An explicit consumed-terminal state, monotonic transition, recovery suppression, and end-to-end restart evidence.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
This gap plan closes WR-02 after Plan 11-10 establishes the completion postcondition. Do not weaken `CONSUMED`, delete the reciprocal index, mark a contradictory mint successfully applied, or release a finalized burn. Do not include WR-03, Phase 12, mock RPC, or broad corruption/fuzz infrastructure.
</execution_note>

<context>
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-REVIEW.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-07-SUMMARY.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-10-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a strict consumed-terminal safety transition</name>
  <files>src/blockchain/impl/proto/ConsensusLocalState.proto, src/blockchain/ConsensusStateStore.hpp, src/blockchain/ConsensusStateStore.cpp, test/src/blockchain/consensus_burn_reservation_test.cpp</files>
  <read_first>
    - src/blockchain/impl/proto/ConsensusLocalState.proto
    - src/blockchain/ConsensusStateStore.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - src/account/UTXOManager.cpp
    - src/account/TransactionManager.cpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - test/src/account/utxo_manager_test.cpp
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
  </read_first>
  <action>
Extend `BurnReservationRecord::State` with an explicit terminal `CONSUMED_SAFETY_ERROR` value rather than overwriting the fact of consumption with ordinary `SAFETY_ERROR`. Update strict validation so both safety states require nonempty diagnostic and exact finality identity, while `CONSUMED_SAFETY_ERROR` is accepted only as a finalized, consumed-derived, non-releasable terminal record. Preserve the existing schema version policy used by this clean v2 local state; do not add dual-read or migration behavior.

Change `ConsensusStateStore::MarkBurnReservationSafetyError` (or an equivalently narrow exact-identity API) under the existing `mutex_`: exact `FINALIZED_PENDING_APPLICATION` advances to `SAFETY_ERROR`; exact `CONSUMED` advances to `CONSUMED_SAFETY_ERROR`; either matching terminal state is idempotent only for the same diagnostic. Require exact slot, outpoint through reciprocal reread, expected generation, certificate digest, proposal ID, and winner ID before mutation. Write only the slot record while retaining the exact reciprocal outpoint index and generation. Mismatched identity/diagnostic returns typed conflict; storage failure leaves the prior terminal/consumed record intact. Ensure `PrepareConsumedBurnReservation`, `ApplyFinalizedReservationBatch`, `DeleteReservedBurnReservation`, create/join, and finalization cannot regress or delete either safety state.

Add exact store tests `ConsumedReservationContradictionTransitionsToConsumedTerminalSafety`, `ConsumedTerminalSafetyPreservesReciprocalIdentityAndRejectsRelease`, and `ConsumedTerminalSafetyRejectsMismatchedIdentityAndIsExactlyIdempotent`. Begin from a genuinely batch-committed `CONSUMED` record, not handcrafted state; assert the consumed-terminal enum, unchanged finality/outpoint/generation/index, diagnostic, strict scan/reopen success, no release, no re-consume, no create/join, and no mutation on mismatched identity or failed commit.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_burn_reservation_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_list_tests | grep -Ec 'ConsumedReservationContradictionTransitionsToConsumedTerminalSafety|ConsumedTerminalSafetyPreservesReciprocalIdentityAndRejectsRelease|ConsumedTerminalSafetyRejectsMismatchedIdentityAndIsExactlyIdempotent')" -eq 3 &amp;&amp; build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_filter='*ConsumedReservationContradictionTransitionsToConsumedTerminalSafety:*ConsumedTerminalSafetyPreservesReciprocalIdentityAndRejectsRelease:*ConsumedTerminalSafetyRejectsMismatchedIdentityAndIsExactlyIdempotent:*BurnReservationSafetyErrorCannotRegressOrRelease' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - The protobuf and strict validator distinguish consumed-terminal safety from pre-consumption SafetyError.
    - Only exact identity-matched `CONSUMED` reaches `CONSUMED_SAFETY_ERROR`; no other winner/generation/finality can mutate it.
    - Reciprocal outpoint record remains present and exact, and every release/recreate/reconsume path rejects the terminal state.
    - All three named tests are discovered exactly once and include same-path reopen plus failed-write preservation.
    - No CRDT replication, legacy migration, release escape hatch, or alternate-winner transition is added.
  </acceptance_criteria>
  <done>Post-consumption contradictions have a monotonic, identity-bound, durably consumed terminal representation.</done>
</task>

<task type="auto">
  <name>Task 2: Drive consumed artifact corruption through restart recovery and stop retries</name>
  <files>src/blockchain/Consensus.cpp, test/src/blockchain/consensus_burn_reservation_test.cpp, test/src/account/transaction_manager_pending_lifecycle_test.cpp, test/src/account/utxo_manager_test.cpp</files>
  <read_first>
    - src/blockchain/Consensus.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/ConsensusStateStore.hpp
    - src/blockchain/ConsensusStateStore.cpp
    - src/blockchain/impl/proto/ConsensusLocalState.proto
    - src/account/TransactionManager.cpp
    - src/account/TransactionManager.hpp
    - src/account/UTXOManager.cpp
    - src/account/UTXOManager.hpp
    - test/src/blockchain/consensus_burn_reservation_test.cpp
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
    - test/src/account/utxo_manager_test.cpp
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-10-PLAN.md
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-VERIFICATION.md
  </read_first>
  <action>
Update every Phase 11 terminal-state branch in `ConsensusManager` to recognize both `SAFETY_ERROR` and `CONSUMED_SAFETY_ERROR`. In `ProcessFinalizedCertificate`, an irreconcilable disposition from an exact consumed reservation must persist `CONSUMED_SAFETY_ERROR`, emit the critical diagnostic, release the processing lease, and return terminal `AlreadyFinalized` without `MarkComplete`, slot `Applied`, proposal cleanup, dependency wake, or work that can release the burn. In `RestoreLocalState`, retain the authoritative certificate/finality cross-check for the terminal record, install a terminal safety lifecycle before live side effects, and exclude its process from `RecoverRestoredCertificateWork` handler scheduling. Duplicate local/pubsub/CRDT/recovery delivery and later handler registration must short-circuit before invoking the application handler.

Extend `ConsumedApplicationRejectsDifferentWinnerIdentityAndArtifacts` with explicit missing canonical application record, missing winner output, conflicting ordered output, and consumed-input inconsistency cases, each proving `state_not_recoverable` with the reservation still `CONSUMED`. In `transaction_manager_pending_lifecycle_test`, add `ConsumedArtifactContradictionMapsToIrreconcilableThroughProductionHandler` using the registered production callback and exact finalized handle, not a copied classifier. In the focused consensus fixture add `ConsumedArtifactContradictionRecoveryPersistsTerminalSafetyAndStopsRetry`: perform a genuine certified atomic mint to `CONSUMED`, leave/recreate a pending processing seam representing commit-before-completion, corrupt one application artifact through friend-only raw storage access, close and reconstruct over the same RocksDB, register the real TransactionManager application handler, and run recovery. Assert exactly one contradiction-detection handler attempt, durable `CONSUMED_SAFETY_ERROR`, unchanged reciprocal/finality identity, critical terminal lifecycle, zero cleanup and dependency wake, no release/remint, and zero additional handler calls after another restart, handler registration, timer reconciliation, and duplicate certificate ingress.

Run the focused reservation, transaction lifecycle, and UTXO targets sequentially, then the exact Phase 11 nine-target and Phase 10 eight-target guarded closures.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_burn_reservation_test transaction_manager_pending_lifecycle_test utxo_manager_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_list_tests | grep -Ec 'ConsumedArtifactContradictionRecoveryPersistsTerminalSafetyAndStopsRetry')" -eq 1 &amp;&amp; test "$(build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_list_tests | grep -Ec 'ConsumedArtifactContradictionMapsToIrreconcilableThroughProductionHandler')" -eq 1 &amp;&amp; build/OSX/Release/test_bin/utxo_manager_test --gtest_filter='*ConsumedApplicationRejectsDifferentWinnerIdentityAndArtifacts' --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_filter='*ConsumedArtifactContradictionMapsToIrreconcilableThroughProductionHandler' --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_filter='*ConsumedArtifactContradictionRecoveryPersistsTerminalSafetyAndStopsRetry' --gtest_brief=1 &amp;&amp; test "$(ctest --test-dir build/OSX/Release -N -R '^(consensus_burn_reservation_test|consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|transaction_manager_pending_lifecycle_test|utxo_manager_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test)$' | sed -n 's/^Total Tests: //p')" -eq 9 &amp;&amp; ctest --test-dir build/OSX/Release -R '^(consensus_burn_reservation_test|consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|transaction_manager_pending_lifecycle_test|utxo_manager_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test)$' --no-tests=error --output-on-failure &amp;&amp; test "$(ctest --test-dir build/OSX/Release -N -R '^(consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test|transaction_manager_pending_lifecycle_test|utxo_manager_test)$' | sed -n 's/^Total Tests: //p')" -eq 8 &amp;&amp; ctest --test-dir build/OSX/Release -R '^(consensus_vote_journal_test|consensus_finalization_test|consensus_pending_lifecycle_test|consensus_certificate_store_test|certificate_compatibility_test|network_config_precedence_test|transaction_manager_pending_lifecycle_test|utxo_manager_test)$' --no-tests=error --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - The end-to-end test starts from real atomic `CONSUMED`, corrupts an applied artifact, and reaches `CONSUMED_SAFETY_ERROR` through UTXOManager -> TransactionManager -> ProcessFinalizedCertificate.
    - Same-path restart installs terminal safety before live recovery and produces no handler call after the first irreconcilable classification.
    - Cleanup, dependency wake, conditional release, remint, and alternate-winner counters remain zero across restart, timer reconciliation, and duplicate ingress.
    - Terminal state retains exact slot/outpoint/generation/certificate/proposal/winner and reciprocal index after every restart.
    - Exact guarded discovery reports 9 Phase 11 and 8 Phase 10 targets before both suites pass.
  </acceptance_criteria>
  <done>Consumed-artifact contradictions become durable terminal safety and never re-enter application, cleanup, or release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| Consumed UTXO/application artifacts to durable reservation state | Post-commit corruption can be retried forever or erase evidence that the burn was physically consumed |
| Irreconcilable handler result to terminal persistence | Identity mismatch could overwrite another winner or leave a retry loop |
| Restart recovery to terminal state | Reconstructed work can invoke handlers, cleanup, or release before recognizing permanent contradiction |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-11-11-01 | High | Tampering | Explicit `CONSUMED_SAFETY_ERROR` preserves BURN-04 consumption plus exact certificate/finality identity |
| T-11-11-02 | High | Elevation of Privilege | Exact generation/outpoint/finality checks and retained reciprocal index prevent alternate-winner mutation or reuse |
| T-11-11-03 | High | Denial of Service | Terminal-aware startup/recovery suppresses futile handler retries without releasing the burn |
| T-11-11-04 | High | Repudiation | Durable diagnostic, same-path restart, and end-to-end artifact-corruption evidence prove the terminal cause and no-cleanup behavior |
</threat_model>

<verification>
Run the exact focused test-name guards sequentially, inspect protobuf/state validation and every terminal-state branch in restore/finalization/reconciliation, then run both exact 9/8 regression closures and `git diff --check`.
</verification>

<success_criteria>
- Every exact-winner contradiction before or after physical consumption reaches the appropriate durable terminal safety state.
- Post-consumption terminal safety preserves the consumed fact, certificate authority, reciprocal protection, and BURN-03 no-release behavior across restart.
- No cleanup, dependency wake, handler retry, release, remint, or alternate winner occurs after terminal persistence.
- Exact Phase 11 and Phase 10 regression gates pass with deferred/non-goal scope excluded.
</success_criteria>

<output>
Create `.planning/phases/11-slot-owned-bridge-burn-reservations/11-11-SUMMARY.md` when done.
</output>
