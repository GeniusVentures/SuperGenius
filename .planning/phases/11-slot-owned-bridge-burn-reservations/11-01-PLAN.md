---
phase: 11-slot-owned-bridge-burn-reservations
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/src/blockchain/CMakeLists.txt
  - test/src/blockchain/consensus_burn_reservation_test.cpp
autonomous: true
requirements:
  - BURN-01
  - BURN-02
  - BURN-03
  - BURN-04
  - BURN-05
must_haves:
  truths:
    - D-01 through D-19 can be exercised through one focused real-RocksDB harness before production behavior is changed.
    - Tests use explicit clocks, scoped fault hooks, predicate barriers, and same-path restart reconstruction; they do not infer safety from sleeps.
    - The harness models node-local state only and does not introduce CRDT reservation publication, mock RPC startup, or the Phase 12 11-node race.
  artifacts:
    - path: test/src/blockchain/consensus_burn_reservation_test.cpp
      provides: Deterministic store, restart, admission, finality, application, abandonment, and ABA fixtures
    - path: test/src/blockchain/CMakeLists.txt
      provides: Active consensus_burn_reservation_test target
  key_links:
    - from: test/src/blockchain/CMakeLists.txt
      to: consensus_burn_reservation_test.cpp
      via: addtest with blockchain_genesis, genius_node_test, rapidjson, and base_crdt_test
---

<objective>
Create the focused test target and deterministic fixtures required by every Phase 11 behavior plan.

Purpose: Ensure durable reservation, restart, atomicity, and concurrency work has a fast non-flaky feedback loop before production implementation starts.
Output: An active `consensus_burn_reservation_test` binary with a baseline real-storage restart test and reusable scoped helpers.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
Wave 0 is infrastructure only. Do not implement reservation behavior or alter production APIs. Preserve user-owned dirty files. Do not add mock-RPC/startup simulation or the full multi-node race.
</execution_note>

<context>
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-CONTEXT.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-RESEARCH.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-PATTERNS.md
@.planning/phases/11-slot-owned-bridge-burn-reservations/11-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the focused burn-reservation harness</name>
  <files>test/src/blockchain/CMakeLists.txt, test/src/blockchain/consensus_burn_reservation_test.cpp</files>
  <read_first>
    - test/src/blockchain/CMakeLists.txt
    - test/src/blockchain/consensus_vote_journal_test.cpp
    - test/src/blockchain/consensus_finalization_test.cpp
    - test/src/blockchain/consensus_pending_lifecycle_test.cpp
    - test/src/account/utxo_manager_test.cpp
    - src/blockchain/Consensus.hpp
    - src/blockchain/ConsensusStateStore.hpp
    - src/account/UTXOManager.hpp
    - .planning/phases/11-slot-owned-bridge-burn-reservations/11-VALIDATION.md
  </read_first>
  <action>
Register `consensus_burn_reservation_test` with the same production and test libraries as the Phase 10 consensus fixtures. Create a fixture that owns a unique real RocksDB/GlobalDB path, can close and reconstruct storage over that exact path, provides fixed system/steady clocks, scoped hook reset, predicate barriers with RAII release/join, raw key inspection, and counters for subscribe, timer, replay, admission, handler, cleanup, and application stages. Keep future access friend-only and private; Wave 0 must compile against current APIs without adding production seams.

Add `ConsensusBurnReservationHarness.PersistentDatabaseAndBarrierReopenCleanly`: write/read an existing harmless local-store record through the supported fixture, close every owner, reopen the same path, and deterministically block/release/join a worker. Leave named helper groups for Store, Admission, Restart, Final, Application, Race, Stale, ABA, and Horizon cases. Do not add disabled placeholders, wall-clock sleeps, detached threads, or assertions of pre-Phase-11 unsafe behavior.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_burn_reservation_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_list_tests | grep -Ec 'PersistentDatabaseAndBarrierReopenCleanly')" -eq 1 &amp;&amp; build/OSX/Release/test_bin/consensus_burn_reservation_test --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - `test/src/blockchain/CMakeLists.txt` contains one active `addtest(consensus_burn_reservation_test ...)` entry with the required links.
    - `--gtest_list_tests` reports exactly one `PersistentDatabaseAndBarrierReopenCleanly` baseline and the binary passes.
    - The fixture reopens the identical database path and releases/joins its worker even when an assertion path exits early.
    - The new source contains no `sleep_for`, `.detach()`, public production test setter, CRDT reservation write, mock RPC, or multi-node fixture.
  </acceptance_criteria>
  <done>The focused Phase 11 binary builds and supplies deterministic persistent-storage and concurrency scaffolding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| Test fixture to persistent store | A fresh path can falsely claim restart durability |
| Test timing to production races | Sleep-based checks can miss stale-release and finality races |

## STRIDE Threat Register

| Threat ID | Severity | Category | Mitigation |
|---|---|---|---|
| T-11-01-01 | High | Tampering | Same-path real RocksDB reopen and raw-record helpers make persistence assertions genuine |
| T-11-01-02 | High | Repudiation | Nonzero named-test guards and explicit event counters preserve deterministic evidence |
| T-11-01-03 | Medium | Denial of Service | RAII barrier release, worker join, and hook reset prevent stranded test activity |
</threat_model>

<verification>
Run the focused build/test command and `git diff --check`; inspect the new test for sleeps, detached workers, and public test APIs.
</verification>

<success_criteria>
- Wave 0 supplies every missing target and deterministic fixture prerequisite in 11-VALIDATION.md.
- No production behavior or deferred Phase 12 infrastructure is implemented.
</success_criteria>

<output>
Create `.planning/phases/11-slot-owned-bridge-burn-reservations/11-01-SUMMARY.md` when done.
</output>
