---
phase: 09-canonical-slot-and-certificate-storage
plan: 13
type: execute
wave: 7
depends_on:
  - 09-05
  - 09-09
files_modified:
  - src/account/PublicChainInputValidator.cpp
  - test/src/account/public_chain_mint_validation_test.cpp
autonomous: true
gap_closure: true
requirements:
  - SLOT-03
  - SLOT-04
must_haves:
  truths:
    - A missing or failed receipt status contributes zero endpoint weight without vetoing later independent endpoints.
    - The mint succeeds when later endpoints independently provide at least 75 successful weight for the exact requested receipt and indexed burn.
    - Receipt hash, status, indexed log, ABI facts, and endpoint weight remain endpoint-local; no failing endpoint contributes success weight.
  artifacts:
    - path: src/account/PublicChainInputValidator.cpp
      provides: Endpoint-local failed/missing receipt status semantics
    - path: test/src/account/public_chain_mint_validation_test.cpp
      provides: Ordered disagreement regressions with later 75 successful weight
  key_links:
    - from: receipt status check
      to: weighted endpoint loop
      via: failed/missing status increments tried and continues rather than returning false
    - from: later successful endpoints
      to: kRequiredConsensusWeight
      via: only exact receipt/log matches add 50 plus 25 weight
---

<objective>
Restore the weighted endpoint policy for missing and failed receipt status responses.

Purpose: Prevent one stale or malicious endpoint from vetoing a mint that later independent endpoints prove with the configured 75-weight threshold.
Output: Endpoint-local status failure handling and ordered multi-endpoint disagreement tests.
Decisions: D-05.
Finding: WR-03.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
This plan is conceptually parallel with 09-10, 09-11, and 09-12. The repository setting `workflow.use_worktrees=false` serializes Wave 7 execution in the shared worktree.
Protected and unrelated ProofSystem, `.claude/`, `src/account/GeniusNode.cpp`, `log_bridge_race.txt`, `test_failed`, `supergenius_atomic_transaction_test/`, and generated `supergenius_crdt_*` paths must not be edited, removed, or staged.
</execution_note>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-VERIFICATION.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-REVIEW.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-05-SUMMARY.md

<interfaces>
`PublicChainInputValidator::VerifyPublicChainSmartContract` uses `kRequiredConsensusWeight = 75`.
Transport failure, parse failure, receipt-hash mismatch, and indexed-log mismatch currently increment `tried` and continue.
Missing or false `receipt->receipt.status` currently logs and returns false immediately.
`WeightedRpcEndpoint::consensus_weight` is accumulated only after exact indexed-log verification.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Treat missing/failed receipt status as endpoint-local disagreement</name>
  <files>src/account/PublicChainInputValidator.cpp, test/src/account/public_chain_mint_validation_test.cpp</files>
  <read_first>
    - src/account/PublicChainInputValidator.hpp
    - src/account/PublicChainInputValidator.cpp
    - evmrelay/include/eth/rpc_receipt_source.hpp
    - evmrelay/src/eth/json_rpc.cpp
    - test/src/account/public_chain_mint_validation_test.cpp
    - test/src/bridge_e2e/bridge_e2e_chainlist_test.cpp
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-REVIEW.md
  </read_first>
  <action>
In the endpoint loop of `VerifyPublicChainSmartContract`, replace the immediate false return for `!receipt.status.has_value()` or `!receipt.status.value()` with endpoint-local failure: log the URL and status class, increment `tried`, and continue. Do not add that endpoint's weight to `success_weight`. Preserve the existing ordering in which response parsing and exact `receipt->tx_hash == tx_hash_parsed` occur before status, and status success occurs before indexed-log/address/topic/ABI comparison. The function returns true only when later successful endpoint weight reaches `kRequiredConsensusWeight`; otherwise it reaches the existing insufficient-consensus false result.

Extend the receipt fixture so status can be `"0x0"` or omitted, and add a URL-keyed scripted transport factory that returns distinct receipts in configured endpoint order. Add `PublicChainMintValidationTest.FailedReceiptStatusIsEndpointLocalAndLaterWeightCanProveMint` with a first 25-weight endpoint returning status false, followed by a 50-weight exact valid endpoint and a 25-weight exact valid endpoint; assert true only after the later 75 weight. Run the same assertion with the first endpoint omitting status. Record call order/count so the test proves later endpoints were queried.

Add a negative control `PublicChainMintValidationTest.FailedReceiptStatusContributesNoWeight`: first endpoint has 50 weight but failed status, the only valid later endpoint has 25 weight, and verification returns false. Keep the existing receipt-hash mismatch and exact indexed-log tests green. Reuse `public_chain_mint_validation_test`; it is already registered, so make no CMake change.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target public_chain_mint_validation_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/public_chain_mint_validation_test --gtest_filter='PublicChainMintValidationTest.FailedReceiptStatusIsEndpointLocalAndLaterWeightCanProveMint:PublicChainMintValidationTest.FailedReceiptStatusContributesNoWeight' --gtest_list_tests | grep -Ec '^[[:space:]]+(FailedReceiptStatusIsEndpointLocalAndLaterWeightCanProveMint|FailedReceiptStatusContributesNoWeight)$')" -eq 2 &amp;&amp; build/OSX/Release/test_bin/public_chain_mint_validation_test --gtest_filter='PublicChainMintValidationTest.FailedReceiptStatusIsEndpointLocalAndLaterWeightCanProveMint:PublicChainMintValidationTest.FailedReceiptStatusContributesNoWeight' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - The missing/false status branch increments `tried` and continues; it contains no immediate `return false`.
    - Failed/missing-status endpoints never increment `success_weight`.
    - The positive test places the failed endpoint first, proves both later endpoints are called, and succeeds from later weight 50 plus 25.
    - The negative control proves failed 50 weight plus valid 25 weight remains below threshold and returns false.
    - Exact transaction-hash and indexed-log validation still precede any successful weight contribution.
    - The automated verification first proves both exact fully qualified status tests are listed with a nonzero expected count of two, then runs exactly those two tests.
  </acceptance_criteria>
  <done>Receipt status disagreement follows the same endpoint-local weighted policy as transport, parse, hash, and log disagreement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC endpoint → weighted proof | One provider may be stale, malicious, or return incomplete receipt state |
| Endpoint result → quorum accumulator | Failed status must contribute neither a veto nor positive weight |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-09-G13-01 | Medium | Denial of Service | Receipt status branch | mitigate | Continue to independent endpoints and let the 75-weight threshold decide |
| T-09-G13-02 | High | Spoofing / Tampering | Endpoint success weight | mitigate | Award weight only after exact tx hash, successful status, indexed log, and decoded fact equality |
</threat_model>

<verification>
Run the filtered status regressions, then the complete `public_chain_mint_validation_test` and adjacent `bridge_e2e_chainlist_test`. Run `git diff --check`.
</verification>

<success_criteria>
- Missing/failed status is one endpoint's zero-weight vote, not a global veto.
- Later independent exact receipts totaling at least 75 weight prove the mint.
- A failed endpoint's configured weight never helps meet the threshold.
- Existing exact source, receipt hash, and indexed burn facts remain fail closed.
</success_criteria>

<output>
Create `.planning/phases/09-canonical-slot-and-certificate-storage/09-13-SUMMARY.md` when done.
</output>
