---
phase: 09-canonical-slot-and-certificate-storage
plan: 09
type: execute
wave: 6
depends_on:
  - 09-05
  - 09-08
files_modified:
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - src/blockchain/Blockchain.hpp
  - src/account/TransactionManager.cpp
  - src/account/GeniusInputValidator.cpp
  - test/src/blockchain/certificate_compatibility_test.cpp
  - test/src/account/transaction_manager_pending_lifecycle_test.cpp
autonomous: true
gap_closure: true
requirements:
  - SLOT-01
  - SLOT-02
  - CERT-04
  - COMP-01
must_haves:
  truths:
    - Only underlying `storage::DatabaseError::NOT_FOUND` becomes `CertificateStoreError::NotFound`.
    - Datastore corruption becomes `IntegrityError`; I/O, timeout, shutdown, busy, and other operational failures become a distinct `StorageError`.
    - An index that exists but targets an absent slot is still `IntegrityError`, while operational slot-read failure propagates as `StorageError`.
    - The real previous-nonce and producer-UTXO consumers execute winning, absent, corrupt, and operational-error branches through Blockchain certificate lookup.
    - Previous-nonce absence alone becomes Pending; corruption or operational failure rejects. Producer lookup never accepts any missing or failed read.
  artifacts:
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - test/src/blockchain/certificate_compatibility_test.cpp
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
  key_links:
    - Both slot and hash lookup call one datastore-error mapping helper before relationship validation.
    - TransactionManager::EvaluateTransactionReplayProtection consumes the Blockchain hash wrapper and creates a dependency only for typed NotFound.
    - GeniusInputValidator::ValidateWitness consumes the same wrapper and fails closed for every non-success result.
---

<objective>
Finish typed certificate-read semantics and prove the real compatibility consumers.

Purpose: Prevent corruption or datastore outages from masquerading as normal certificate absence, and replace contract-only tests with executable consumer regressions.
Output: Precise storage error mapping, consumer-specific diagnostics/control flow, injectable read failures, and real previous-nonce/producer-witness test matrices.
Decisions: D-16, D-18, D-19, D-20.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-RESEARCH.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-VERIFICATION.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-REVIEW.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map certificate datastore reads to absence, integrity, and operational errors</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, test/src/blockchain/certificate_compatibility_test.cpp</files>
  <read_first>
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - src/crdt/globaldb/globaldb.hpp
    - src/crdt/globaldb/globaldb.cpp
    - src/storage/database_error.hpp
    - src/storage/database_error.cpp
    - test/src/blockchain/certificate_compatibility_test.cpp
    - test/src/blockchain/consensus_certificate_store_test.cpp
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-VERIFICATION.md
  </read_first>
  <action>
Add `CertificateStoreError::StorageError` with message `"Certificate store operational error"`. Add one private `MapCertificateReadError(std::error_code)` helper used by both `GetCertificateBySlotId` and `GetCertificateBySubjectHash`: `storage::DatabaseError::NOT_FOUND` maps to `NotFound`; `storage::DatabaseError::CORRUPTION` maps to `IntegrityError`; every other database error (`IO_ERROR`, `TIMED_OUT`, `SHUTDOWN_IN_PROGRESS`, `BUSY`, `TRY_AGAIN_`, `INCOMPLETE`, and all remaining non-NOT_FOUND codes) maps to `StorageError`. Unknown/non-database categories also map conservatively to `StorageError`. Log the exact key and underlying error before returning integrity/operational failures.

Keep relationship rules separate from raw reads. If the hash index read is true NOT_FOUND, return NotFound. If the index exists and `GetCertificateBySlotId` returns NotFound, convert it to IntegrityError because the index is dangling. Propagate IntegrityError and StorageError from slot lookup unchanged. Malformed index/payload/key relationships remain IntegrityError.

Add a private certificate-record read callable initialized to `db_->Get` and settable only through the existing `ConsensusManagerTestAccess` friend, so focused tests can inject an exact `storage::DatabaseError` without changing public production APIs. In `certificate_compatibility_test`, cover both slot-key and index-key reads for actual NOT_FOUND, injected CORRUPTION, and injected IO_ERROR; also cover an existing index whose slot read yields NOT_FOUND, CORRUPTION, and IO_ERROR. Assert respectively NotFound/IntegrityError/StorageError, IntegrityError/IntegrityError/StorageError. Keep malformed-record relationship cases green.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target certificate_compatibility_test -j2 &amp;&amp; build/OSX/Release/test_bin/certificate_compatibility_test --gtest_filter='*ReadError*:*NotFound*:*Corruption*:*Operational*:*DanglingIndex*' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - `CertificateStoreError` contains a distinct `StorageError` enumerator and error-category message.
    - Neither lookup API contains an unconditional `has_error() -> NotFound` mapping.
    - Only `storage::DatabaseError::NOT_FOUND` maps to NotFound; CORRUPTION and IO_ERROR tests produce different certificate errors.
    - An existing index plus missing authoritative slot returns IntegrityError, while the same path with injected IO_ERROR returns StorageError.
    - The injectable reader is private/friend-only and defaults to the real `GlobalDB::Get`.
  </acceptance_criteria>
  <done>Certificate lookup preserves true absence, corruption, and operational failure as distinct typed outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Execute real previous-nonce and producer-UTXO lookup branches</name>
  <files>src/blockchain/Blockchain.hpp, src/account/TransactionManager.cpp, src/account/GeniusInputValidator.cpp, test/src/account/transaction_manager_pending_lifecycle_test.cpp</files>
  <read_first>
    - src/blockchain/Blockchain.hpp
    - src/blockchain/impl/Blockchain.cpp
    - src/account/TransactionManager.cpp
    - src/account/TransactionManager.hpp
    - src/account/GeniusInputValidator.cpp
    - src/account/GeniusInputValidator.hpp
    - src/account/GeniusTransaction.hpp
    - src/account/TransferTransaction.hpp
    - src/blockchain/Consensus.hpp
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
    - test/src/account/transaction_manager_certificate_fallback_test.cpp
    - test/src/blockchain/certificate_compatibility_test.cpp
    - test/src/blockchain/consensus_subject_test.cpp
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-REVIEW.md
  </read_first>
  <action>
Update consumer diagnostics without weakening behavior. In `TransactionManager::EvaluateTransactionReplayProtection`, only NotFound creates `ValidationResult::Pending` with `PendingDependencyKey::Certificate(previous_hash)`; IntegrityError logs certificate corruption and returns Reject; StorageError logs operational unavailability and returns Reject without a dependency. In `GeniusInputValidator::ValidateWitness`, NotFound logs missing producer, IntegrityError logs corruption, and StorageError logs operational unavailability; all return false.

Extend `TransactionManagerPendingLifecycleTestAccess` to invoke the real private `EvaluateTransactionReplayProtection` and inspect its full `ValidationResult`. Add narrow test-only friend access in `Blockchain.hpp` only if needed to obtain its `ConsensusManager` for the existing injected-reader seam; do not add a public production test hook.

Build a CRDT-backed fixture with a real `Blockchain`, `TransactionManager`, canonical nonce slot handler, signed authoritative certificate/index, and a transfer whose nonce is predecessor nonce plus one and whose `previous_hash` is the winning transaction hash. Execute four previous-nonce cases through `EvaluateTransactionReplayProtection`: canonical winner returns Approve; absent index returns Pending with exactly that hash dependency; corrupt index/payload returns Reject; injected IO_ERROR returns Reject and no dependency.

In the same target, construct a real producer certificate whose nonce subject contains a `produced_outputs_root`, plus a consuming transaction/`UTXOWitness` whose `ConsumedInputProof` leaf and Merkle branch validate against that root. Invoke `GeniusInputValidator::ValidateWitness` directly with the real Blockchain. Run four producer cases: canonical winner returns true; absent index returns false; corrupt index/payload returns false; injected IO_ERROR returns false. These tests must invoke the named production consumers and may not substitute direct `GetCertificateBySubjectHash` calls or enum/message assertions.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target transaction_manager_pending_lifecycle_test certificate_compatibility_test -j2 &amp;&amp; build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_filter='*PreviousNonce*:*ProducerUTXO*' --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/certificate_compatibility_test --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - Previous-nonce tests call `TransactionManager::EvaluateTransactionReplayProtection` through its friend accessor for winner, absent, corrupt, and IO_ERROR states.
    - Only the absent previous certificate produces Pending, and its dependency type/value are Certificate and the exact previous hash.
    - Producer tests call `GeniusInputValidator::ValidateWitness` with a proof that succeeds when the canonical producer certificate exists.
    - The same producer proof fails for absent, corrupt, and IO_ERROR lookup outcomes.
    - No new test case satisfies COMP-01 by calling the Consensus/Blockchain lookup twice in place of the consumers.
  </acceptance_criteria>
  <done>The actual nonce-chain and producer-witness branches prove v2 hash-index compatibility and fail closed on corruption or operational read failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RocksDB/CRDT read result → certificate API | Error collapse can disguise corruption or outage as normal absence |
| Certificate API → previous-nonce admission | NotFound is retryable, but integrity/operational failures must not become pending |
| Certificate API → producer UTXO proof | A missing/corrupt producer certificate must never authorize an input |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-09-G09-01 | High | Tampering | Certificate read classification | resolved in-plan | CORRUPTION maps to IntegrityError, never NotFound |
| T-09-G09-02 | High | Elevation of Privilege / Tampering | Producer-UTXO validation | resolved in-plan | Real witness consumer rejects every non-success lookup |
| T-09-G09-03 | Medium | Denial of Service | Previous-nonce retry | resolved in-plan | Only true absence creates a dependency; outage/corruption reject with distinct diagnostics |
</threat_model>

<verification>
Run the complete compatibility and account consumer targets. Inspect filtered test names and assertions to confirm both production consumers, not only the lookup wrapper, execute all four outcome classes.
</verification>

<success_criteria>
- Raw datastore absence, corruption, and operational failure remain typed and distinguishable.
- Dangling index integrity is not weakened by raw NOT_FOUND mapping.
- The real previous-nonce consumer approves a winner, waits only on absence, and rejects corruption/outage.
- The real producer-witness consumer approves the valid producer proof and rejects absence, corruption, and outage.
- SLOT-01/SLOT-02 normal nonce semantics and CERT-04/COMP-01 verified-index semantics are covered by executable consumer paths.
</success_criteria>

<output>
Create `.planning/phases/09-canonical-slot-and-certificate-storage/09-09-SUMMARY.md` when done.
</output>
