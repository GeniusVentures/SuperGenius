---
phase: 09-canonical-slot-and-certificate-storage
plan: 04
type: execute
wave: 3
depends_on:
  - 09-03
files_modified:
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - src/blockchain/Blockchain.hpp
  - src/blockchain/impl/Blockchain.cpp
  - src/account/TransactionManager.cpp
  - src/account/GeniusInputValidator.cpp
  - test/src/blockchain/CMakeLists.txt
  - test/src/blockchain/certificate_compatibility_test.cpp
  - test/src/account/transaction_manager_pending_lifecycle_test.cpp
autonomous: true
requirements:
  - SLOT-01
  - CERT-04
  - COMP-01
must_haves:
  truths:
    - Slot lookup returns only a certificate whose embedded proposal derives to the requested slot.
    - Transaction-hash lookup follows the secondary index and verifies both index-to-slot and certificate-to-winning-hash links.
    - A losing transaction hash is NotFound, while a full losing subject observes that its slot is finalized and can retrieve the winner.
    - Existing previous-nonce and producer-UTXO consumers continue retrieving winners by transaction hash without knowing the storage layout.
  artifacts:
    - src/blockchain/Consensus.hpp
    - src/blockchain/Consensus.cpp
    - src/blockchain/Blockchain.hpp
    - test/src/blockchain/certificate_compatibility_test.cpp
  key_links:
    - GetCertificateBySubjectHash reads only the tx index, then delegates authoritative validation to GetCertificateBySlotId.
    - CheckCertificateForSubject(subject) derives the candidate slot directly instead of first deriving candidate transaction hash.
    - TransactionManager and GeniusInputValidator remain hash-lookup consumers through Blockchain wrappers.
---

<objective>
Expose verified certificate lookup semantics without breaking transaction-hash consumers.

Purpose: Separate authoritative finality identity from compatibility retrieval and ensure corruption is never hidden as absence.
Output: Typed slot/hash APIs, subject-slot finality checks, unchanged consumer seams, and focused compatibility tests.
Decisions: D-13, D-16, D-17, D-18, D-19, D-20.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-RESEARCH.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-PATTERNS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-03-SUMMARY.md
@src/blockchain/Consensus.hpp
@src/blockchain/Consensus.cpp
@src/blockchain/Blockchain.hpp
@src/blockchain/impl/Blockchain.cpp
@src/account/TransactionManager.cpp
@src/account/GeniusInputValidator.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed authoritative slot and verified transaction-hash lookups</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, src/blockchain/Blockchain.hpp, src/blockchain/impl/Blockchain.cpp, test/src/blockchain/CMakeLists.txt, test/src/blockchain/certificate_compatibility_test.cpp</files>
  <action>
Create the Wave 0 `certificate_compatibility_test` target using the CRDT-backed consensus fixture. Define repository-style typed certificate lookup errors that distinguish at minimum `NotFound` and `IntegrityError` (plus invalid input/conflict if shared with Plan 03).

Implement `GetCertificateBySlotId(slot_id)` to enforce canonical 64-lowercase-hex input, load `/cert/v2/slot/...`, parse and validate the complete certificate, re-derive its slot, and reject any mismatch. Reimplement `GetCertificateBySubjectHash(tx_hash)` to enforce canonical hash input, read `/cert/v2/tx/...`, validate the index slot value, call slot lookup, and verify the certificate's embedded winning subject hash exactly equals the requested hash. Do not scan certificate records or repair indexes.

Expose matching Blockchain wrappers. Tests cover success, absent slot/index as `NotFound`, malformed index, index to missing slot, wrong slot key, malformed certificate, and certificate winning-hash mismatch as `IntegrityError`.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target certificate_compatibility_test -j2 &amp;&amp; build/OSX/Release/test_bin/certificate_compatibility_test --gtest_filter='*SlotLookup*:*HashLookup*:*Integrity*:*NotFound*' --gtest_brief=1</automated>
  </verify>
  <done>Both lookup APIs verify every storage relationship and expose absence separately from corruption.</done>
</task>

<task type="auto">
  <name>Task 2: Make full-subject finality slot-aware and preserve hash consumer compatibility</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, src/blockchain/Blockchain.hpp, src/blockchain/impl/Blockchain.cpp, src/account/TransactionManager.cpp, src/account/GeniusInputValidator.cpp, test/src/blockchain/certificate_compatibility_test.cpp, test/src/account/transaction_manager_pending_lifecycle_test.cpp</files>
  <action>
Change `CheckCertificateForSubject(const Subject&amp;)` to derive the candidate's canonical slot directly and query the authoritative slot certificate. It returns finalized when the loaded winner occupies that slot even if the candidate transaction hash differs. Preserve the string/hash check as exact winning-hash semantics through `GetCertificateBySubjectHash`.

Audit the previous-nonce lookup in `TransactionManager` and producer-certificate lookup in `GeniusInputValidator`: keep them on the verified hash API and adapt only typed error handling so `NotFound` follows their existing pending/absent path while `IntegrityError` fails closed and is logged as corruption. Add fixtures proving a winning hash resolves, losing hash is `NotFound`, full losing subject sees the winner's finalized slot, previous-nonce retrieval succeeds, and producer-certificate retrieval succeeds without direct `/cert/` reads.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target certificate_compatibility_test transaction_manager_pending_lifecycle_test -j2 &amp;&amp; build/OSX/Release/test_bin/certificate_compatibility_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_brief=1</automated>
  </verify>
  <done>Full subjects observe slot finality while legacy callers retain exact winner-hash retrieval through a verified index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Datastore index → authoritative certificate | Secondary metadata must not redirect callers to an unrelated winner |
| Certificate API → transaction validation consumers | Corruption must not be collapsed into normal absence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | Slot/hash lookup chain | mitigate | Canonical key validation, re-derived slot and winner hash checks, typed integrity errors, and no scan repair |
| T-09-10 | Repudiation / Denial of Service | Compatibility consumers | mitigate | Preserve NotFound behavior but fail closed and log integrity corruption distinctly |
</threat_model>

<verification>
Run both focused targets and inspect the new compatibility tests for winner, loser, absent, and corrupt states. Confirm no account consumer reads `/cert/` directly.
</verification>

<success_criteria>
- Slot and transaction-hash APIs have distinct, verified semantics.
- Losing candidate hash lookup does not alias to the winner.
- Full-subject lookup recognizes authoritative winner finality for the shared slot.
- Previous-nonce and producer-UTXO paths pass against v2 storage.
- Integrity failures are visible and fail closed.
</success_criteria>

<output>
Create `.planning/phases/09-canonical-slot-and-certificate-storage/09-04-SUMMARY.md` when done.
</output>
