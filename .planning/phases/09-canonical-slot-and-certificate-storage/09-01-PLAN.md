---
phase: 09-canonical-slot-and-certificate-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/account/GeniusTransaction.hpp
  - src/account/GeniusTransaction.cpp
  - src/account/MintTransactionV2.hpp
  - src/account/MintTransactionV2.cpp
  - src/account/TransactionManager.cpp
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - test/src/blockchain/consensus_slot_key_test.cpp
autonomous: true
requirements:
  - SLOT-01
  - SLOT-02
  - SLOT-03
  - SLOT-04
must_haves:
  truths:
    - Every accepted consensus subject resolves to one lowercase 64-hex SHA-256 slot ID.
    - Normal transactions with the same canonical source address and nonce share one slot.
    - Every proposal for the same chain, burn hash, and receipt-local index shares one mint slot regardless of proposer or candidate-controlled fields.
    - Invalid or noncanonical slot inputs are rejected instead of falling back to proposal identity.
  artifacts:
    - src/account/GeniusTransaction.hpp
    - src/account/MintTransactionV2.cpp
    - src/blockchain/Consensus.cpp
    - test/src/blockchain/consensus_slot_key_test.cpp
  key_links:
    - TransactionManager nonce slot handler delegates embedded transactions to the canonical transaction slot API.
    - Consensus proposal admission, arbitration, and state maps consume the same result-returning canonical slot derivation.
    - Mint slot derivation reads source chain, first external input hash, and that input's output_idx_ only.
---

<objective>
Establish the canonical finality identity contract for all consensus subjects.

Purpose: Make every proposal that competes for the same account nonce or external burn enter one fixed-size finality domain, with strict rejection of ambiguous aliases.
Output: Canonical preimage/digest helpers, fail-closed consensus slot resolution, and adversarial slot-key tests.
Decisions: D-06, D-07, D-08, D-09, D-10.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-RESEARCH.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-PATTERNS.md
@src/account/GeniusTransaction.hpp
@src/account/MintTransactionV2.cpp
@src/account/TransactionManager.cpp
@src/blockchain/Consensus.hpp
@src/blockchain/Consensus.cpp
@test/src/blockchain/consensus_slot_key_test.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement strict canonical preimages and hashed transaction slot IDs</name>
  <files>src/account/GeniusTransaction.hpp, src/account/GeniusTransaction.cpp, src/account/MintTransactionV2.hpp, src/account/MintTransactionV2.cpp, test/src/blockchain/consensus_slot_key_test.cpp</files>
  <action>
Create a result-returning canonical slot seam that builds a readable preimage, validates canonical components, and returns SHA-256 as exactly 64 lowercase hexadecimal characters. Preserve normal transaction semantics by hashing the existing canonical source-address-plus-unsigned-decimal-nonce preimage. Define the mint preimage exactly as `mint-v2:&lt;source_chain_id&gt;:&lt;burn_tx_hash&gt;:&lt;receipt_log_index&gt;`, sourced from the serialized chain ID and the single external input's `txid_hash_` plus `output_idx_`. Reject missing/ambiguous inputs, leading-zero numeric aliases, signs/whitespace, hashes with `0x` or uppercase/non-hex text, and noncanonical addresses. Keep the readable preimage available for diagnostics. Do not include token, amount, destination, transaction hash, proposal ID, proposer address, or proposer nonce.

Rewrite `consensus_slot_key_test` expectations around hashed outputs. Cover deterministic normal slots, distinct normal nonces, the exact known mint preimage digest, same burn with different proposal/transaction/proposer/nonce/token/amount/destination producing the same digest, different chain/hash/receipt index producing different digests, and noncanonical/missing fields failing closed.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_slot_key_test -j2 &amp;&amp; build/OSX/Release/test_bin/consensus_slot_key_test --gtest_brief=1</automated>
  </verify>
  <done>Both normal and mint transactions expose strict deterministic slot digests, and tests prove candidate-controlled mint fields cannot split a burn slot.</done>
</task>

<task type="auto">
  <name>Task 2: Make consensus use one fail-closed slot derivation path</name>
  <files>src/account/TransactionManager.cpp, src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, test/src/blockchain/consensus_slot_key_test.cpp</files>
  <action>
Change the registered slot-handler contract and `ConsensusManager::GetSlotKey` path so canonical derivation failure is explicit (`outcome::result` or an equivalent typed invalid result). The nonce handler must deserialize an embedded transaction and use its canonical slot result; a valid non-embedded nonce subject uses the same normal address/nonce preimage. Remove recognized-subject fallbacks to proposal ID, subject hash, or nonce zero. Reject a proposal before insertion into `proposals_` or `slot_states_` when no valid slot exists, and propagate the same slot result through local submission, remote continuation, proposal restoration, and certificate derivation seams. Keep fallback subject identity only for subject types that have no registered domain handler, and hash their canonical subject identity to the fixed-size slot form.

Extend tests through the friend accessor to prove handler failures never create proposal-specific slots and that repeated calls across proposal IDs produce the same digest.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_slot_key_test consensus_pending_lifecycle_test -j2 &amp;&amp; build/OSX/Release/test_bin/consensus_slot_key_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/consensus_pending_lifecycle_test --gtest_brief=1</automated>
  </verify>
  <done>All consensus state that groups proposals obtains the same canonical slot ID, and invalid canonical input cannot enter arbitration under a fallback key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedded transaction → slot derivation | Network-provided transaction fields determine the finality domain |
| Domain slot result → consensus maps | A malformed or aliased identity could split arbitration state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing / Tampering | Canonical preimage and `GetSlotKey` | mitigate | Strict canonical parsing, exact domain preimages, SHA-256 digest, typed failure, and no recognized-subject fallback |
| T-09-07 | Denial of Service | Invalid subject admission | mitigate | Reject before inserting into proposal or slot maps; bounded fixed-size operational keys |
</threat_model>

<verification>
Run both focused targets, inspect failures for any readable `mint-v2:` operational key or proposal-ID fallback, and confirm all slot IDs match `^[0-9a-f]{64}$`.
</verification>

<success_criteria>
- All four SLOT requirements are represented by executable tests.
- Same-resource candidates collide even when every candidate-controlled field differs.
- Canonical aliases and incomplete mint identities fail before consensus state mutation.
- Normal transactions retain source-address-plus-nonce semantics through the hashed representation.
</success_criteria>

<output>
Create `.planning/phases/09-canonical-slot-and-certificate-storage/09-01-SUMMARY.md` when done.
</output>
