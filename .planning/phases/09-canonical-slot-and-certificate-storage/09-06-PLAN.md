---
phase: 09-canonical-slot-and-certificate-storage
plan: 06
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-04
files_modified:
  - evmrelay/include/eth/eth_receipt_source.hpp
  - evmrelay/src/eth/eth_receipt_source.cpp
  - src/watcher/impl/bridge_catchup_watcher.hpp
  - src/watcher/impl/bridge_catchup_watcher.cpp
  - test/src/account/bridge_event_identity_test.cpp
autonomous: true
gap_closure: true
requirements:
  - SLOT-03
  - SLOT-04
must_haves:
  truths:
    - A catch-up chunk is complete only when every enabled log query succeeds and every matching log resolves against its exact finalized receipt.
    - BurnProcessor observes no candidate from a chunk until all enabled v1/v2 queries and every receipt/index resolution for that chunk have succeeded.
    - Missing, unparseable, mismatched, ambiguous, or overflowing receipt-local identity leaves the chain cursor at the failed chunk start.
    - Receipt-local ordinal narrowing is checked through a wide-integer helper that can prove the uint32_t boundary without constructing billions of receipt entries.
    - A retry performs a fresh receipt request; a transient null receipt cached during one poll cannot suppress the next poll.
    - Once the dependency succeeds, the burn is delivered once with its canonical receipt-local index and the cursor advances exactly to chunk_end plus one.
  artifacts:
    - evmrelay/include/eth/eth_receipt_source.hpp
    - src/watcher/impl/bridge_catchup_watcher.cpp
    - test/src/account/bridge_event_identity_test.cpp
  key_links:
    - process_logs reports receipt-resolution failure to the owning chunk rather than counting it as a skipped burn.
    - process_logs appends decoded burn tuples to a chunk-local staging buffer; poll_once publishes that buffer only after the complete v1/v2 chunk succeeds.
    - poll_once commits last_block_per_chain_ only after the staged chunk is published and the complete v1/v2 chunk succeeds.
    - receipt_cache is scoped to one poll attempt and is rebuilt before a failed chunk is retried.
---

<objective>
Make bridge catch-up cursor advancement transactional with receipt-local identity resolution.

Purpose: Prevent a transient receipt failure from permanently skipping a historical burn.
Output: Fail-closed chunk status, retry-safe cursor semantics, fresh receipt resolution, and a deterministic scripted-transport regression.
Decisions: D-01, D-02, D-04.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-RESEARCH.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-VERIFICATION.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-REVIEW.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve the catch-up cursor until all receipt dependencies resolve</name>
  <files>evmrelay/include/eth/eth_receipt_source.hpp, evmrelay/src/eth/eth_receipt_source.cpp, src/watcher/impl/bridge_catchup_watcher.hpp, src/watcher/impl/bridge_catchup_watcher.cpp, test/src/account/bridge_event_identity_test.cpp</files>
  <read_first>
    - evmrelay/include/eth/eth_receipt_source.hpp
    - evmrelay/src/eth/eth_receipt_source.cpp
    - evmrelay/src/eth/CMakeLists.txt
    - src/watcher/impl/bridge_catchup_watcher.hpp
    - src/watcher/impl/bridge_catchup_watcher.cpp
    - evmrelay/include/eth/json_rpc.hpp
    - evmrelay/include/eth/rpc_receipt_source.hpp
    - evmrelay/src/eth/json_rpc.cpp
    - test/src/account/bridge_event_identity_test.cpp
    - test/src/bridge_e2e/bridge_anvil_catchup_e2e_test.cpp
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-VERIFICATION.md
  </read_first>
  <action>
Add a directly testable `eth::checked_receipt_log_ordinal(uint64_t ordinal) -> std::optional&lt;uint32_t&gt;` declaration/definition in the existing `eth_receipt_source.hpp/.cpp` compilation unit. Production accepts every ordinal through `UINT32_MAX` and returns `std::nullopt` for any larger value; do not add a test-only production limit. `BridgeCatchupWatcher` must convert the nonnegative `std::distance` result to `uint64_t`, pass it to this helper, and use the returned value as the only narrowing to the receipt-local index.

Replace the current “skip and return true” receipt-resolution branches in `BridgeCatchupWatcher::poll_once` with explicit chunk failure. `process_logs` must return failure when a matching `RpcLog` cannot obtain or parse a receipt; when receipt `tx_hash`, `block_hash`, or `block_number` differs from the log; when `log_indices.size()` differs from `receipt.logs.size()`; when the block-wide `log_index` is missing or duplicated; when `checked_receipt_log_ordinal` rejects the derived distance; or when the matching log cannot be ABI-decoded into the tuple required by `BurnProcessor`.

Introduce a chunk-local `StagedBurn` value containing the decoded ABI values, canonical no-prefix transaction hash, decimal chain ID, receipt-local `uint32_t` index, and v1/v2 family as needed for diagnostics. `process_logs` may only validate and append to `staged_burns`; it must never invoke `burn_processor_`. Maintain a chunk-local dedup set for `(tx_hash, receipt_log_index)`, checked against burns committed by earlier successful chunks, and merge it into the committed per-poll set only after this chunk validates. If any enabled query, receipt identity, index resolution, or decode fails, discard both the complete staging buffer and its chunk-local dedup additions so retry begins cleanly and no candidate from the failed chunk has been externally delivered.

Track v1 and v2 query/process results independently. A chunk succeeds only when the v1 query parses and stages successfully and, when v2 is enabled, the v2 query also parses and stages successfully. Only after those conditions hold, iterate `staged_burns` in deterministic observation order and invoke `BurnProcessor`; preserve the existing meaning of a false processor result as “already processed/skipped.” Any mandatory query or validation failure breaks the chunk loop before this publication sweep and without executing `from_block = chunk_to + 1`; `last_block_per_chain_[chain_id]` must remain the failed chunk start. Cancellation remains distinct and must not publish staged burns or mark success.

Keep receipt caching only inside one `poll_once` attempt (sharing one successful receipt across v1/v2 observations in that attempt). Do not persist a cached `std::nullopt` across polls. Keep `(tx_hash, receipt_log_index)` deduplication for successful resolution; insert into `seen_burns` only after the receipt identity is proven.

Add `BridgeCatchupWatcherTestAccess` as a narrowly scoped friend that performs one deterministic `poll_once` without starting a sleeping thread. In `bridge_event_identity_test`, add a scripted `JsonRpcTransport` with `start_block=10` and one-block chunks. Add an early-valid/late-receipt-failure case whose v1 result contains two burns: the first has a fully valid receipt and the later burn has a missing/unparseable receipt on poll one; assert poll one makes zero BurnProcessor calls and leaves `GetLastProcessedBlock(chain)==10`. On poll two return both valid receipts; assert fresh receipt calls occur, both distinct `(tx_hash,receipt_index)` tuples are delivered exactly once, and the cursor advances to 11.

Add a separate v1-success/v2-failure case: return one valid v1 burn and then fail or return an unparseable response for the enabled v2 query. Assert the failed poll publishes zero burns and preserves cursor 10; on retry return the same valid v1 response plus a valid v2 response, then assert each staged tuple is delivered exactly once and the cursor advances to 11. Keep table-driven transport cases for mismatched receipt transaction/block identity, unequal log arrays, and missing/duplicate block-wide log index; every case must publish zero burns and preserve cursor 10.

Directly unit-test `checked_receipt_log_ordinal` in `bridge_event_identity_test` with `uint64_t{0}`, `uint64_t{UINT32_MAX}`, and `uint64_t{UINT32_MAX} + 1`: the first two return exact `uint32_t` values and the last returns `std::nullopt`. This is the ordinal-overflow regression; do not construct or reserve a `ReceiptResult::log_indices` vector near `UINT32_MAX`. Reuse the existing `bridge_event_identity_test` target: `evmrelay/src/eth/CMakeLists.txt` already compiles `eth_receipt_source.cpp` and `test/src/account/CMakeLists.txt` already registers/links the test through `genius_node_test`, so no new CMake target or source-list change is required.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target bridge_event_identity_test bridge_anvil_catchup_e2e_test -j2 &amp;&amp; build/OSX/Release/test_bin/bridge_event_identity_test --gtest_filter='*Catchup*Receipt*:*Catchup*Cursor*:*Catchup*Retry*' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - No receipt-resolution failure branch in `process_logs` uses `continue` to let the chunk report success.
    - `process_logs` contains no `burn_processor_` call; all such calls occur in one publication sweep after successful enabled v1/v2 staging.
    - `from_block = chunk_to + 1` is reachable only after successful v1 processing and successful v2 processing when v2 is enabled.
    - The early-valid/late-receipt-failure regression observes zero BurnProcessor calls on the failed poll and exactly one call per distinct burn after the successful retry.
    - The v1-success/v2-failure regression observes zero BurnProcessor calls on the failed poll and exactly one call per v1/v2 tuple after retry.
    - Every receipt mismatch/ambiguity case asserts zero delivery and `GetLastProcessedBlock(chain)==start_block`.
    - `checked_receipt_log_ordinal(uint64_t)` is directly tested at `UINT32_MAX` and `UINT32_MAX + 1` without a billion-entry fixture, and production calls the same helper.
    - `bridge_event_identity_test` remains the explicit automated target; the existing `evmrelay/src/eth/CMakeLists.txt` entry for `eth_receipt_source.cpp` means no CMake edit is needed.
    - The existing multiple-burn and re-inclusion receipt-index tests remain green.
  </acceptance_criteria>
  <done>Catch-up retries the same chunk after any receipt-resolution failure and advances only after the canonical burn identity is delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC log response → receipt lookup | Logs may arrive before their receipt is available or may be inconsistent across endpoints |
| Receipt resolution → durable scan cursor | Treating missing proof as “skipped” permanently suppresses a legitimate burn |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-09-G06-01 | High | Tampering / Denial of Service | Catch-up receipt resolution | resolved in-plan | Any missing or inconsistent identity fails the chunk and preserves its cursor |
| T-09-G06-02 | High | Denial of Service | Receipt retry cache | resolved in-plan | Retry creates a fresh per-poll cache and re-fetches transient failures |
| T-09-G06-03 | Medium | Repudiation | Mixed v1/v2 chunk status | resolved in-plan | Require every enabled query family to succeed before cursor commit |
| T-09-G06-04 | Medium | Tampering / Denial of Service | Receipt-local ordinal narrowing | resolved in-plan | One uint64-to-uint32 checked helper is used in production and directly boundary-tested |
</threat_model>

<verification>
Run the focused scripted transport cases and the existing bridge identity suite. Build the Anvil target for compile compatibility; its environment-dependent runtime remains the phase verifier's separate follow-up.
</verification>

<success_criteria>
- A log-query success plus a later receipt failure publishes no earlier candidate and never advances the cursor.
- A successful v1 query followed by a failed v2 query publishes neither family.
- Retry does not retain transient receipt failure and eventually delivers the burn once.
- Receipt identity mismatches and unresolved ordinals are retried, not silently skipped.
- Same-transaction multiple burns retain distinct canonical receipt indexes.
</success_criteria>

<output>
Create `.planning/phases/09-canonical-slot-and-certificate-storage/09-06-SUMMARY.md` when done.
</output>
