---
phase: 09-canonical-slot-and-certificate-storage
plan: 10
type: execute
wave: 7
depends_on:
  - 09-06
  - 09-09
files_modified:
  - src/watcher/impl/bridge_catchup_watcher.hpp
  - src/watcher/impl/bridge_catchup_watcher.cpp
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - src/account/TransactionManager.hpp
  - src/account/TransactionManager.cpp
  - test/src/account/bridge_event_identity_test.cpp
  - test/src/account/transaction_manager_pending_lifecycle_test.cpp
  - test/src/bridge_e2e/bridge_anvil_catchup_e2e_test.cpp
  - test/src/startup/startup_wiring_test.cpp
autonomous: true
gap_closure: true
requirements:
  - SLOT-03
  - SLOT-04
must_haves:
  truths:
    - A catch-up chunk advances only after every staged burn returns Processed or has durable AlreadyHandled proof.
    - Retry and callback exceptions preserve the chunk cursor and leave its dedup tuples uncommitted for a later poll.
    - A reserved outpoint, unavailable node/account/transaction manager, failed MintTokens submission, and storage read failure are Retry outcomes, never duplicate proof.
    - Only a persisted executed-burn record or durable consumed outpoint can produce AlreadyHandled.
    - MintFunds treats only database NOT_FOUND as an absent executed-burn record and performs no UTXO, reservation, or queue mutation on corruption or operational read failure.
  artifacts:
    - path: src/watcher/impl/bridge_catchup_watcher.hpp
      provides: Explicit BurnProcessOutcome callback contract
    - path: src/watcher/impl/bridge_catchup_watcher.cpp
      provides: Publication commit barrier that preserves cursor and dedup state on Retry
    - path: src/account/TransactionManager.cpp
      provides: Typed durable executed-burn state read shared by MintFunds and catch-up classification
    - path: src/account/GeniusNode.hpp
      provides: Private production-used catch-up outcome classifier with friend-only test access
    - path: test/src/account/bridge_event_identity_test.cpp
      provides: Retry, exception, partial-publication, reservation, and failed-submission cursor regressions
    - path: test/src/bridge_e2e/bridge_anvil_catchup_e2e_test.cpp
      provides: Enum-compatible standalone watcher callback and E2E compile coverage
    - path: test/src/startup/startup_wiring_test.cpp
      provides: Enum-compatible startup cancellation callback and compile coverage
  key_links:
    - from: src/account/GeniusNode.cpp
      to: src/watcher/impl/bridge_catchup_watcher.hpp
      via: catch-up BurnProcessor returns BurnProcessOutcome instead of bool
    - from: src/account/GeniusNode.cpp
      to: src/account/TransactionManager.cpp
      via: production callback obtains durable burn state before returning AlreadyHandled
    - from: test/src/account/bridge_event_identity_test.cpp
      to: src/account/GeniusNode.hpp
      via: friend-only access executes the same private outcome classifier used by the production callback
    - from: src/watcher/impl/bridge_catchup_watcher.cpp
      to: last_block_per_chain_ and seen_burns
      via: both commit only after every staged callback has a non-Retry outcome
    - from: repository-wide BridgeCatchupWatcher caller audit
      to: all production and test callback constructions
      via: an exact source-search inventory plus compile coverage for every affected target
---

<objective>
Close the catch-up publication durability gap and the bridge replay-read fail-open warning.

Purpose: Prevent a transient mint-submission or storage failure from permanently skipping a historical burn, while allowing only durable completion evidence to advance the scan.
Output: Explicit publication outcomes, a typed durable burn-state query, fail-closed MintFunds replay reads, and executable cursor/non-mutation regressions.
Decisions: D-01, D-03, D-04, D-09, D-10.
Findings: CR-01 and WR-01.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<execution_note>
This plan is conceptually parallel with 09-11, 09-12, and 09-13 because their files do not overlap. The current project setting `workflow.use_worktrees=false` means the executor will serialize same-wave plans in the shared workspace.
Protected and unrelated ProofSystem, `.claude/`, `log_bridge_race.txt`, `test_failed`, `supergenius_atomic_transaction_test/`, and generated `supergenius_crdt_*` paths must not be edited, removed, or staged. In `src/account/GeniusNode.cpp`, preserve the existing logger-level hunk and stage only the plan-owned bridge callback hunk.
</execution_note>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-VERIFICATION.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-REVIEW.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-06-SUMMARY.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-09-SUMMARY.md

<interfaces>
Current callback: `BridgeCatchupWatcher::BurnProcessor` is `std::function<bool(decoded_values, tx_hash_hex, chain_id_str, uint32_t receipt_log_index)>`.
Current commit point: `bridge_catchup_watcher.cpp` inserts `chunk_seen_burns` into `seen_burns` and assigns `from_block = chunk_to + 1` after treating false and exceptions as skipped.
Current durable identity: `TransactionManager::MakeBridgeExecutedKey(chainid, transaction_hash, receipt_log_index)` returns `/bridge/executed/&lt;chain&gt;:&lt;hash&gt;:&lt;index&gt;`.
Current production callback: the lambda in `GeniusNode::InitializeAndStartBridge` checks consumed/reserved UTXO state and calls `MintTokens`.
Exhaustive current callback-bearing source inventory: `src/account/GeniusNode.cpp`, `test/src/account/bridge_event_identity_test.cpp`, `test/src/bridge_e2e/bridge_anvil_catchup_e2e_test.cpp`, and `test/src/startup/startup_wiring_test.cpp`. The latter two contain `MakeCountingBurnProcessor` and the startup cancellation callback respectively; all four must move atomically with the callback contract.
Current test linkage: `bridge_event_identity_test` links and force-loads `genius_node_test`, so it can execute a private `GeniusNode` classifier through a narrow friend accessor without adding a public API or CMake target.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish typed durable bridge state and make MintFunds fail closed</name>
  <files>src/account/TransactionManager.hpp, src/account/TransactionManager.cpp, test/src/account/transaction_manager_pending_lifecycle_test.cpp</files>
  <read_first>
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - src/account/UTXOManager.hpp
    - src/storage/database_error.hpp
    - test/src/account/transaction_manager_pending_lifecycle_test.cpp
    - test/src/account/CMakeLists.txt
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-REVIEW.md
  </read_first>
  <action>
Add `TransactionManager::BridgeBurnState { Available, Reserved, AlreadyHandled }` and a result-returning `GetBridgeBurnState(chainid, transaction_hash, receipt_log_index)` used immediately by `MintFunds` and available through the existing private `GeniusNode` friendship for Task 2. Validate the canonical identity once. Read `MakeBridgeExecutedKey(...)` through a private reader callable that defaults to the real datastore `get` and is settable only by `TransactionManagerPendingLifecycleTestAccess`. A present executed record returns AlreadyHandled; exact `storage::DatabaseError::NOT_FOUND` continues to UTXO state; `CORRUPTION`, `IO_ERROR`, timeout, shutdown, busy, unknown categories, or missing datastore return their typed storage failure. After a true durable miss, CONSUMED returns AlreadyHandled, RESERVED returns Reserved, and absent/ready returns Available.

Make `MintFunds` call this seam before `PutUTXO`, `ReserveUTXOs`, persistence construction side effects, or `EnqueueTransaction`. AlreadyHandled and Reserved preserve the existing duplicate/reservation rejection, Available proceeds, and every storage error returns failure immediately. Do not convert corruption/I/O to absence. The private reader injection must be reset to the real reader by default and must not become a public production test hook.

Extend `TransactionManagerPendingLifecycleTestAccess` with set/reset access for the private reader and direct access to `GetBridgeBurnState`. Add exactly these tests: `TransactionManagerPendingLifecycleTest.MintFundsExecutedReadErrorsFailClosedBeforeMutation`, with table rows for CORRUPTION and IO_ERROR that call the real `MintFunds` and assert the exact storage error, unchanged queue size, absent outpoint state, and no executed-key write; `TransactionManagerPendingLifecycleTest.MintFundsExecutedReadNotFoundAllowsIndexedMint`, which injects exact NOT_FOUND and proves one indexed mint is created; and `TransactionManagerPendingLifecycleTest.BridgeBurnStateDistinguishesReservedConsumedAndPersisted`, which proves RESERVED maps to Reserved while a CONSUMED outpoint or present executed key maps to AlreadyHandled without queue growth. Reset the reader to the real datastore after every injected row.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target transaction_manager_pending_lifecycle_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_filter='TransactionManagerPendingLifecycleTest.MintFundsExecutedReadErrorsFailClosedBeforeMutation:TransactionManagerPendingLifecycleTest.MintFundsExecutedReadNotFoundAllowsIndexedMint:TransactionManagerPendingLifecycleTest.BridgeBurnStateDistinguishesReservedConsumedAndPersisted' --gtest_list_tests | grep -Ec '^[[:space:]]+(MintFundsExecutedReadErrorsFailClosedBeforeMutation|MintFundsExecutedReadNotFoundAllowsIndexedMint|BridgeBurnStateDistinguishesReservedConsumedAndPersisted)$')" -eq 3 &amp;&amp; build/OSX/Release/test_bin/transaction_manager_pending_lifecycle_test --gtest_filter='TransactionManagerPendingLifecycleTest.MintFundsExecutedReadErrorsFailClosedBeforeMutation:TransactionManagerPendingLifecycleTest.MintFundsExecutedReadNotFoundAllowsIndexedMint:TransactionManagerPendingLifecycleTest.BridgeBurnStateDistinguishesReservedConsumedAndPersisted' --gtest_brief=1</automated>
  </verify>
  <acceptance_criteria>
    - The executed-burn read maps only `storage::DatabaseError::NOT_FOUND` to absence; CORRUPTION and IO_ERROR remain distinguishable failures.
    - `MintFunds` reaches no UTXO insertion, reservation, or enqueue call after a non-NOT_FOUND executed-record read error.
    - The injected reader is private/friend-only and defaults to the real datastore.
    - A persisted executed key and durable CONSUMED state report AlreadyHandled; RESERVED reports Reserved for the following task to classify as Retry.
    - The named corruption/I/O test asserts queue, outpoint, and executed-key non-mutation through the real `MintFunds`.
    - The automated verification proves all three concrete tests are listed before executing them, so a zero-match filter cannot pass this task.
  </acceptance_criteria>
  <done>The typed durable-state seam exists, is tested independently, and MintFunds fails closed before any mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Make every BurnProcessor caller use durable Processed, AlreadyHandled, or Retry outcomes</name>
  <files>src/watcher/impl/bridge_catchup_watcher.hpp, src/watcher/impl/bridge_catchup_watcher.cpp, src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/account/bridge_event_identity_test.cpp, test/src/bridge_e2e/bridge_anvil_catchup_e2e_test.cpp, test/src/startup/startup_wiring_test.cpp</files>
  <read_first>
    - src/watcher/impl/bridge_catchup_watcher.hpp
    - src/watcher/impl/bridge_catchup_watcher.cpp
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - test/src/account/bridge_event_identity_test.cpp
    - test/src/bridge_e2e/bridge_anvil_catchup_e2e_test.cpp
    - test/src/startup/startup_wiring_test.cpp
    - test/src/account/CMakeLists.txt
    - test/src/bridge_e2e/CMakeLists.txt
    - test/src/startup/CMakeLists.txt
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-VERIFICATION.md
    - .planning/phases/09-canonical-slot-and-certificate-storage/09-REVIEW.md
  </read_first>
  <action>
Define `enum class BurnProcessOutcome { Processed, AlreadyHandled, Retry }` beside `BridgeCatchupWatcher::BurnProcessor`, and change the callback result from `bool` to that enum. In the one post-validation publication sweep, count Processed as backfilled and AlreadyHandled as durably skipped. On Retry, stop publishing the chunk; on any `std::exception` or unknown exception from the callback, take the same Retry path. In every Retry case, do not insert `chunk_seen_burns` into `seen_burns`, do not execute `from_block = chunk_to + 1`, and leave `last_block_per_chain_[chain]` at the failed chunk start. A prior Processed callback in the same chunk does not permit cursor or dedup commit when a later burn retries.

Extract the production GeniusNode mapping into a private, production-used `ClassifyCatchupBurnOutcome` static method plus private facts type and `GeniusNodeCatchupTestAccess` friendship in `GeniusNode.hpp`; include the watcher contract where needed, but expose no new public API. The facts must represent account availability, transaction-manager availability, the typed `TransactionManager::BridgeBurnState` read or its error, and submission state `{NotAttempted, Succeeded, Failed}`. The classifier maps missing account, missing manager, durable-read error, Reserved, Available without successful submission, and failed submission to Retry; AlreadyHandled to AlreadyHandled; and Available plus successful submission to Processed. Every production callback return after parsing/hash validation must go through this classifier. Parse/event/hash failures, expired `weak_self`, and callback exceptions also return Retry. Before submission, call Task 1's `GetBridgeBurnState`; only its AlreadyHandled result may skip durably. If `MintTokens` fails after a race, re-read durable state and return AlreadyHandled only if that second read proves completion; otherwise return Retry.

Extend `CatchupHarness` so each staged burn can return a scripted `BurnProcessOutcome` or throw. Add real `poll_once` regressions named `BridgeCatchupPublicationOutcomeTest.RetryPreservesCursorAndDedup`, `BridgeCatchupPublicationOutcomeTest.ExceptionPreservesCursorAndDedup`, and `BridgeCatchupPublicationOutcomeTest.PartialProcessedThenRetryDoesNotCommitChunk`. Each test retries the same one-block script, asserts cursor 10 and no committed dedup after the failed poll, then returns Processed or AlreadyHandled and asserts cursor 11.

In the same suite, add `BridgeCatchupPublicationOutcomeTest.GeniusNodeClassifierMapsTransientStatesToRetry` and `BridgeCatchupPublicationOutcomeTest.GeniusNodeClassifierMapsSubmissionAndDurableCompletion`. Invoke the private production classifier through `GeniusNodeCatchupTestAccess`, not a duplicate test-only mapper. The first test must cover unavailable account, unavailable transaction manager, durable-read error, Reserved, NotAttempted, and failed submission; the second must cover successful submission as Processed plus persisted/consumed AlreadyHandled state as AlreadyHandled, including the post-failure durable re-read case.

Update every enum-contract caller atomically, using the repository-wide source inventory rather than assuming only the two previously known tests. Change `MakeCountingBurnProcessor` in `bridge_anvil_catchup_e2e_test.cpp`, the `CatchupHarness` callback in `bridge_event_identity_test.cpp`, and the `burn_processor` callback in `startup_wiring_test.cpp` to return `BurnProcessOutcome::Processed`; keep the production `GeniusNode` callback on the classifier mapping above and do not retain a boolean compatibility overload. Re-run the repository search in verification and require its exact four-file result so a newly discovered construction, assignment, or compatible invocation must be added to this task before it can pass. Preserve the user-owned logger-level hunk already present near `GeniusNode.cpp:971`; do not reformat or stage it. During execution, stage only the callback/classifier hunks owned by this plan. Do not include `ProofSystem`, `.claude/`, `src/account/log_bridge_race.txt`, `test_failed`, `supergenius_atomic_transaction_test/`, or generated `supergenius_crdt_*` directories.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target genius_node_test bridge_event_identity_test bridge_anvil_catchup_e2e_test startup_wiring_test -j2 &amp;&amp; test "$(build/OSX/Release/test_bin/bridge_event_identity_test --gtest_filter='BridgeCatchupPublicationOutcomeTest.RetryPreservesCursorAndDedup:BridgeCatchupPublicationOutcomeTest.ExceptionPreservesCursorAndDedup:BridgeCatchupPublicationOutcomeTest.PartialProcessedThenRetryDoesNotCommitChunk:BridgeCatchupPublicationOutcomeTest.GeniusNodeClassifierMapsTransientStatesToRetry:BridgeCatchupPublicationOutcomeTest.GeniusNodeClassifierMapsSubmissionAndDurableCompletion' --gtest_list_tests | grep -Ec '^[[:space:]]+(RetryPreservesCursorAndDedup|ExceptionPreservesCursorAndDedup|PartialProcessedThenRetryDoesNotCommitChunk|GeniusNodeClassifierMapsTransientStatesToRetry|GeniusNodeClassifierMapsSubmissionAndDurableCompletion)$')" -eq 5 &amp;&amp; build/OSX/Release/test_bin/bridge_event_identity_test --gtest_filter='BridgeCatchupPublicationOutcomeTest.RetryPreservesCursorAndDedup:BridgeCatchupPublicationOutcomeTest.ExceptionPreservesCursorAndDedup:BridgeCatchupPublicationOutcomeTest.PartialProcessedThenRetryDoesNotCommitChunk:BridgeCatchupPublicationOutcomeTest.GeniusNodeClassifierMapsTransientStatesToRetry:BridgeCatchupPublicationOutcomeTest.GeniusNodeClassifierMapsSubmissionAndDurableCompletion' --gtest_brief=1 &amp;&amp; test "$(rg -l --glob '*.{cpp,cc,cxx}' 'make_(shared|unique)&lt;[^&gt;]*BridgeCatchupWatcher|BridgeCatchupWatcher[[:space:]]+[A-Za-z_][A-Za-z0-9_]*[[:space:]]*\(|MakeCountingBurnProcessor|auto[[:space:]]+burn_processor[[:space:]]*=' src test evmrelay | sort | tr '\n' ' ')" = "src/account/GeniusNode.cpp test/src/account/bridge_event_identity_test.cpp test/src/bridge_e2e/bridge_anvil_catchup_e2e_test.cpp test/src/startup/startup_wiring_test.cpp "</automated>
  </verify>
  <acceptance_criteria>
    - `BridgeCatchupWatcher::BurnProcessor` returns `BurnProcessOutcome` and contains no boolean false-as-already-processed contract.
    - The publication sweep has explicit branches for Processed, AlreadyHandled, and Retry; exceptions take the Retry branch.
    - `seen_burns.insert(...)` and `from_block = chunk_to + 1` are unreachable after any staged callback returns Retry or throws.
    - The three cursor tests execute the real `poll_once` path and prove retry of the same chunk, not only enum values.
    - The two classifier tests execute the private method used by the production callback and cover every required unavailable, reserved, storage-error, failed-submission, successful-submission, and durable-completion mapping.
    - The production callback calls Task 1's typed durable-state seam and returns AlreadyHandled only for persisted execution or durable CONSUMED proof.
    - `MakeCountingBurnProcessor`, `CatchupHarness`, and the startup cancellation callback return Processed; automated verification builds `genius_node_test`, `bridge_event_identity_test`, `bridge_anvil_catchup_e2e_test`, and `startup_wiring_test`, so every inventoried callback-bearing translation unit is interface-compatible without requiring Anvil runtime availability.
    - The automated verification proves all five exact fully qualified publication tests are listed before executing exactly those five tests.
    - The repository source-audit command returns exactly `src/account/GeniusNode.cpp`, `test/src/account/bridge_event_identity_test.cpp`, `test/src/bridge_e2e/bridge_anvil_catchup_e2e_test.cpp`, and `test/src/startup/startup_wiring_test.cpp`; every returned file appears in this task's files/read-first/action and compile target coverage.
    - `git diff -- src/account/GeniusNode.cpp` retains the pre-existing logger-level changes unchanged, and the plan commit/staging excludes those lines.
  </acceptance_criteria>
  <done>Catch-up publication cannot advance after transient callback failure, production mapping is directly executable, and every callback caller compiles against the durable outcome contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catch-up callback → scan cursor | A transient local failure must not become irreversible external-event acknowledgement |
| Durable datastore → duplicate classification | Read errors must not masquerade as an absent or already-handled burn |
| UTXO reservation → catch-up publication | A transient in-flight reservation is not proof that the mint will finalize |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-09-G10-01 | High | Tampering / Denial of Service | `BridgeCatchupWatcher::poll_once` | mitigate | Explicit outcomes; Retry/exception preserve cursor and dedup state |
| T-09-G10-02 | High | Tampering | `TransactionManager::MintFunds` executed read | mitigate | Only NOT_FOUND permits mutation; corruption/I/O return before state changes |
| T-09-G10-03 | High | Repudiation / Tampering | `GeniusNode` duplicate classification | mitigate | AlreadyHandled requires persisted execution or durable consumed outpoint |
| T-09-G10-04 | Medium | Denial of Service | Reserved/in-flight burn | mitigate | Treat reservation as Retry and re-evaluate until durable completion or availability |
</threat_model>

<verification>
Run both complete focused binaries after the filtered slices. Confirm `bridge_event_identity_test` still passes receipt-resolution retry coverage and `transaction_manager_pending_lifecycle_test` still passes canonical mint and certificate-consumer coverage. Run `git diff --check` and audit the `GeniusNode.cpp` diff/staged scope.
</verification>

<success_criteria>
- False/exception-equivalent, unavailable, reservation, and failed-submission outcomes preserve the catch-up cursor.
- Only Processed or durable AlreadyHandled outcomes commit chunk dedup state and advance to `chunk_end + 1`.
- Executed-burn corruption and I/O failures cause no UTXO, reservation, executed-key, or queue mutation.
- Protected user-owned and unrelated paths remain outside the plan's staged changes.
</success_criteria>

<output>
Create `.planning/phases/09-canonical-slot-and-certificate-storage/09-10-SUMMARY.md` when done.
</output>
