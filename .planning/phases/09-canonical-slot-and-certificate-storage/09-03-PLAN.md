---
phase: 09-canonical-slot-and-certificate-storage
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/crdt/crdt_data_filter.hpp
  - src/crdt/impl/crdt_data_filter.cpp
  - src/crdt/crdt_datastore.hpp
  - src/crdt/impl/crdt_datastore.cpp
  - src/crdt/globaldb/globaldb.hpp
  - src/crdt/globaldb/globaldb.cpp
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - test/src/blockchain/CMakeLists.txt
  - test/src/blockchain/consensus_certificate_store_test.cpp
autonomous: true
requirements:
  - CERT-01
  - CERT-02
  - CERT-03
  - COMP-02
must_haves:
  truths:
    - One successful certificate publication makes its authoritative slot record and winning transaction index visible in the same CRDT delta.
    - A byte-identical replay is idempotent, while a different certificate for an occupied slot is rejected and diagnosed.
    - Replicated malformed, partial, or mismatched certificate/index pairs are rejected before merge.
    - Legacy transaction-keyed certificate state prevents consensus startup before background side effects begin.
  artifacts:
    - src/blockchain/Consensus.cpp
    - src/crdt/impl/crdt_data_filter.cpp
    - test/src/blockchain/consensus_certificate_store_test.cpp
  key_links:
    - SubmitCertificate derives slot and winner hash from the complete signed certificate and emits both v2 keys via batch Put.
    - A delta-aware CRDT filter validates the sibling key/value relationship before either element is merged.
    - Certificate work callbacks and recovery operate on authoritative slot keys only.
---

<objective>
Replace transaction-keyed certificate persistence with an atomic, validated, write-once v2 certificate/index store.

Purpose: Make the canonical slot the authoritative finality key while preserving the complete winning certificate and refusing incompatible or corrupt state.
Output: Pair-aware CRDT validation, atomic v2 writes, conflict semantics, focused storage tests, and legacy-state startup rejection.
Decisions: D-11, D-12, D-13, D-14, D-15.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-CONTEXT.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-RESEARCH.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-PATTERNS.md
@.planning/phases/09-canonical-slot-and-certificate-storage/09-01-SUMMARY.md
@src/blockchain/Consensus.hpp
@src/blockchain/Consensus.cpp
@src/crdt/atomic_transaction.hpp
@src/crdt/impl/atomic_transaction.cpp
@src/crdt/crdt_data_filter.hpp
@src/crdt/impl/crdt_data_filter.cpp
@src/crdt/globaldb/globaldb.hpp
@test/src/blockchain/consensus_pending_lifecycle_test.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold certificate-store tests and add pair-aware pre-merge validation</name>
  <files>src/crdt/crdt_data_filter.hpp, src/crdt/impl/crdt_data_filter.cpp, src/crdt/crdt_datastore.hpp, src/crdt/impl/crdt_datastore.cpp, src/crdt/globaldb/globaldb.hpp, src/crdt/globaldb/globaldb.cpp, test/src/blockchain/CMakeLists.txt, test/src/blockchain/consensus_certificate_store_test.cpp</files>
  <action>
Create the Wave 0 `consensus_certificate_store_test` target using the established `base_crdt_test` fixture and a focused ConsensusManager test accessor. Add a thread-safe delta-aware filter registration seam alongside the existing element filters. Its callback must inspect the complete incoming delta before per-element filtering and be able to reject the complete certificate/index pair atomically. Preserve existing filter behavior for unrelated namespaces.

Add storage-fixture cases for a valid two-element pair; missing slot or index sibling; extra/mismatched winning index; malformed slot/index key/value; invalid certificate bytes/signature; mutation of each security-relevant signed winning-proposal surface (subject, proposer/account identity, nonce, transaction hash, embedded transaction, registry, and votes); and a pair whose derived certificate slot or winner hash differs from its keys. Prove every rejection leaves neither logical v2 record visible. This infrastructure is internal—do not introduce a new dependency or a general transaction/CAS claim.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target crdt_atomic_transaction_test consensus_certificate_store_test -j2 &amp;&amp; build/OSX/Release/test_bin/crdt_atomic_transaction_test --gtest_brief=1 &amp;&amp; build/OSX/Release/test_bin/consensus_certificate_store_test --gtest_filter='*Pair*:*Delta*' --gtest_brief=1</automated>
  </verify>
  <done>Incoming v2 certificate data can be validated as one pair before merge, and tests prove partial or inconsistent pairs never become visible.</done>
</task>

<task type="auto">
  <name>Task 2: Publish authoritative slot certificates with write-once winning indexes</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, test/src/blockchain/consensus_certificate_store_test.cpp</files>
  <action>
Introduce `/cert/v2/slot/&lt;slot_id&gt;` and `/cert/v2/tx/&lt;winning_tx_hash&gt;` constants and exact regexes. Keep the certificate protobuf and signing bytes unchanged. In `SubmitCertificate`, validate the complete certificate, derive canonical slot ID and winning subject hash, serialize deterministically, and preflight any existing records. A byte-identical occupied slot with the same index is idempotent success; a differing occupied slot value, occupied winner index targeting another slot, or partial existing pair returns a typed conflict/integrity failure with slot plus both proposal IDs/hashes in diagnostics.

Use one `GlobalDB::Put(vector&lt;DataPair&gt;, topics)` operation for the serialized slot certificate and index value containing only the slot ID. Register the pair-aware v2 filter/callbacks so replicated writes enforce the same derivations. Process, journal, wake dependencies, and recover certificates from slot keys only; index callbacks must never execute transaction handlers. Preserve complete-certificate validation and registry processing.

Do not characterize local preflight as distributed compare-and-swap. If a conflicting valid pair is observed, reject/report it; Phase 10 prevents honest formation of concurrent competing certificates.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_certificate_store_test -j2 &amp;&amp; build/OSX/Release/test_bin/consensus_certificate_store_test --gtest_filter='*Submit*:*Replay*:*Conflict*:*Callback*:*Recovery*' --gtest_brief=1</automated>
  </verify>
  <done>Local and replicated certificate paths use one atomic v2 pair, exact replay is harmless, and differing occupied-slot data is rejected with actionable diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Fail startup on legacy certificate state before consensus side effects</name>
  <files>src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, test/src/blockchain/consensus_certificate_store_test.cpp</files>
  <action>
Add a startup compatibility check immediately after dependencies/work journal are available and before pubsub subscription, timer start, v2 filter/listener registration, topic listening, or recovery. Query `/cert/` and accept only exact v2 slot/index namespaces. On any `/cert/&lt;transaction_hash&gt;` legacy record, log a clear v2.0 protocol-state error containing the offending key and return `nullptr`; do not migrate, delete, dual-read, or scan-convert it.

Instrument the test fixture's pubsub/listener/timer seams as needed to prove legacy state returns null with zero startup side effects, while empty or v2-only state starts normally.
  </action>
  <verify>
    <automated>cmake --build build/OSX/Release --target consensus_certificate_store_test -j2 &amp;&amp; build/OSX/Release/test_bin/consensus_certificate_store_test --gtest_filter='*Legacy*:*Startup*' --gtest_brief=1</automated>
  </verify>
  <done>Incompatible legacy certificate state blocks startup clearly and cannot mix with v2 state or begin background consensus work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local certificate formation → CRDT publication | Derived storage keys must match the signed winning proposal |
| Replicated CRDT delta → local datastore | Remote data may be malformed, partial, or conflicting |
| Existing datastore → v2 consensus startup | Legacy records are incompatible protocol state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Certificate/index delta | mitigate | Complete-delta pair validation before merge plus one atomic batch Put |
| T-09-05 | Tampering / Repudiation | Occupied slot/index writes | mitigate | Byte-identical idempotency only, typed conflict/integrity errors, and slot/proposal diagnostics |
| T-09-06 | Elevation of Privilege / Tampering | Legacy certificate namespace | mitigate | Fail factory initialization before subscriptions, timers, filters, listeners, or recovery; no migration or dual read |
| T-09-09 | Denial of Service | CRDT filter registry | mitigate | Thread-safe snapshot pattern matching existing filter registries and namespace-scoped pair validation |
</threat_model>

<verification>
Run the complete focused certificate-store target and atomic transaction regression. Confirm its fixture checks logical visibility after rejected deltas and startup side-effect counts.
</verification>

<success_criteria>
- Valid certificate and winning index are emitted and received as one pair.
- Complete signed certificate semantics remain unchanged.
- Exact replay succeeds without duplicate handler application from index records.
- Differing or corrupt pairs fail closed with typed diagnostics.
- Legacy records stop startup before any background activity.
</success_criteria>

<output>
Create `.planning/phases/09-canonical-slot-and-certificate-storage/09-03-SUMMARY.md` when done.
</output>
