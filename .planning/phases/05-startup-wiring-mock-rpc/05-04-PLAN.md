---
phase: 05-startup-wiring-mock-rpc
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/account/UTXOManager.hpp
  - src/account/UTXOManager.cpp
autonomous: true
requirements: []

must_haves:
  truths:
    - "UTXO_RESERVED state exists in UTXOState enum between UTXO_READY and UTXO_CONSUMED (D-18)"
    - "UTXOType enum exists with UTXO_NORMAL and UTXO_BRIDGE values (D-19)"
    - "UTXOEntry struct has a type field of UTXOType (D-19)"
    - "All 8 foreign-address guards in UTXOManager.cpp are removed (D-17)"
    - "IsOutPointConsumed() returns false for RESERVED state (not yet consumed)"
    - "GetBalance() skips RESERVED UTXOs (same as existing reservation logic)"
    - "UTXO serialization/deserialization handles UTXO_RESERVED and UTXOType fields"
    - "Existing UTXO* unit tests pass unchanged after guard removal + enum additions"
  artifacts:
    - path: "src/account/UTXOManager.hpp"
      provides: "Updated UTXOState enum with UTXO_RESERVED, new UTXOType enum, UTXOEntry with type field"
      contains: "UTXO_RESERVED"
      contains: "enum class UTXOType"
      contains: "UTXOType  type"
    - path: "src/account/UTXOManager.cpp"
      provides: "Guard removal, RESERVED state handling in IsOutPointConsumed/GetBalance/SelectUTXOs"
  key_links:
    - from: "UTXOManager::PutUTXO()"
      to: "BridgeRelayer::OnWatchEvent()"
      via: "MintFunds calls PutUTXO with UTXOType::UTXO_BRIDGE"
      pattern: "PutUTXO.*UTXO_BRIDGE"
    - from: "UTXOManager::IsOutPointConsumed()"
      to: "TransactionManager::HasConfirmedInputConflict()"
      via: "Conflict detection queries UTXO state"
      pattern: "IsOutPointConsumed"
---

<objective>
Implement three UTXO subsystem changes required for bridge burn tracking: (1) Remove 8 foreign-address guards so all nodes store UTXOs for all peers (D-17), (2) Add UTXO_RESERVED lifecycle state for burn UTXOs during consensus voting (D-18), (3) Add UTXOType enum to distinguish bridge/burn UTXOs from regular UTXOs (D-19).

Purpose: Enable validators to track burn UTXOs from all peers for conflict detection, provide a RESERVED intermediate state so burn UTXOs are locked during consensus but not yet consumed, and mark burn UTXOs with a type flag for efficient startup catch-up scans.

Output: Updated UTXOManager with guard removal, new enum values, and type-tagged UTXO entries.
</objective>

<execution_context>
@/Users/henriqueklein/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-startup-wiring-mock-rpc/05-CONTEXT.md
@.planning/phases/05-startup-wiring-mock-rpc/05-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@src/account/UTXOManager.hpp
@src/account/UTXOManager.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UTXO_RESERVED state and UTXOType enum to UTXOManager header</name>
  <files>src/account/UTXOManager.hpp</files>
  <read_first>
    - src/account/UTXOManager.hpp — lines 51-72: current UTXOState enum (UTXO_READY, UTXO_CONSUMED), UTXOEntry struct (state, utxo, created_epoch, spent_epoch, spent_by_txid)
    - .planning/phases/05-startup-wiring-mock-rpc/05-RESEARCH.md — UTXOType Enum Addition (lines 429-451): exact enum design, UTXOEntry field addition
    - .planning/phases/05-startup-wiring-mock-rpc/05-CONTEXT.md — D-18: UTXO_RESERVED lifecycle (READY → RESERVED → CONSUMED), D-19: UTXOType field
    - .planning/codebase/CONVENTIONS.md — enum class conventions: `enum class UTXOState : uint8_t`, UPPER_CASE enumerators, PascalCase type names
  </read_first>
  <action>
    **D-18, D-19** — Extend UTXOManager header with new lifecycle state and type marker.

    1. **Add UTXO_RESERVED to UTXOState enum** (D-18):

    In `UTXOManager.hpp` at lines 51-55, insert `UTXO_RESERVED` between `UTXO_READY` and `UTXO_CONSUMED`:
    ```cpp
    enum class UTXOState : uint8_t
    {
        UTXO_READY,    ///< UTXO is unspent and available for use
        UTXO_RESERVED, ///< UTXO is reserved (burn UTXO with mint in consensus) — blocks local reuse, allows consensus voting
        UTXO_CONSUMED  ///< UTXO has been consumed by a transaction and is no longer available
    };
    ```
    Per RESEARCH.md line 433-438. The RESERVED state means: this burn UTXO has been detected and a mint has been initiated via MintFunds(). It should NOT be selected by SelectUTXOs() for local spending, but it should NOT be treated as consumed by IsOutPointConsumed() — consensus validators can still vote on the mint proposal.

    2. **Add UTXOType enum** (D-19):

    Add a new `enum class UTXOType : uint8_t` immediately after the UTXOState enum (around line 56):
    ```cpp
    enum class UTXOType : uint8_t
    {
        UTXO_NORMAL = 0, ///< Standard UTXO from local transfers/mints
        UTXO_BRIDGE = 1  ///< UTXO from cross-chain bridge burn (per D-19)
    };
    ```
    Per RESEARCH.md lines 440-443. Explicit values for serialization stability.

    3. **Add `type` field to UTXOEntry struct** (D-19):

    In the UTXOEntry struct (lines 65-72), add after the `state` field:
    ```cpp
    UTXOType  type{ UTXOType::UTXO_NORMAL }; ///< UTXO category — bridge burns flagged for catch-up scans
    ```
    Existing entries default to `UTXO_NORMAL` (backward compatible — all existing UTXOs were created before this field existed).

    4. **Check all UTXOEntry construction sites** in UTXOManager.cpp for any that use aggregate initialization with positional arguments — adding a field in the middle of the struct could break them. Search for `UTXOEntry{` in the .cpp file. If constructors use designated initializers (C++20) or positional init, adjust to account for the new field. If they use `UTXOEntry{ UTXOState::UTXO_READY, ... }`, note that adding a field between `state` and `utxo` changes positional order.

    **IMPORTANT**: The current UTXOEntry struct has fields in order: `state` (UTXOState), `utxo` (GeniusUTXO), `created_epoch` (uint64_t), `spent_epoch` (optional<uint64_t>), `spent_by_txid` (optional<Hash256>). Adding `type` after `state` and before `utxo` shifts all subsequent positional indices. Check ALL construction sites and update them.

    For example, `UTXOEntry{ UTXOState::UTXO_READY, new_utxo, 0, std::nullopt, std::nullopt }` at line 178 would need to become `UTXOEntry{ UTXOState::UTXO_READY, UTXOType::UTXO_NORMAL, new_utxo, 0, std::nullopt, std::nullopt }`.

    Alternatively, to minimize code churn, add `type` as the LAST field instead of after `state`. This way all existing positional initialization stays valid. The field default `UTXO_NORMAL` means all existing constructions implicitly get the correct type. Add `type` after `spent_by_txid` at line 71:
    ```cpp
    std::optional<base::Hash256> spent_by_txid;
    UTXOType  type{ UTXOType::UTXO_NORMAL }; ///< UTXO category
    ```
    This approach is safer — zero breakage of existing positional initialization.
  </action>
  <verify>
    <automated>cmake --build build --target utxo_manager 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: UTXOState enum contains UTXO_RESERVED between UTXO_READY and UTXO_CONSUMED
    - Source assertion: `enum class UTXOType : uint8_t` exists with UTXO_NORMAL = 0 and UTXO_BRIDGE = 1
    - Source assertion: UTXOEntry struct has `UTXOType type{ UTXOType::UTXO_NORMAL }` field (D-19)
    - Source assertion: UTXOEntry type field is the LAST field in the struct (minimizes positional init breakage)
    - Build: `cmake --build build --target utxo_manager` compiles without errors — all existing construction sites still valid
  </acceptance_criteria>
  <done>UTXOState enum extended with UTXO_RESERVED, new UTXOType enum with UTXO_BRIDGE marker, UTXOEntry struct updated with type field.</done>
</task>

<task type="auto">
  <name>Task 2: Remove 8 foreign-address guards and handle RESERVED state in UTXOManager.cpp</name>
  <files>src/account/UTXOManager.cpp</files>
  <read_first>
    - src/account/UTXOManager.cpp — 8 guard locations at lines 83, 124, 161, 191, 338, 573, 850, 931 (per RESEARCH.md and STATE.md)
    - src/account/UTXOManager.hpp — updated in Task 1: UTXOState with RESERVED, UTXOType enum, UTXOEntry with type field
    - .planning/phases/05-startup-wiring-mock-rpc/05-CONTEXT.md — D-17: remove all 8 guards, D-18: RESERVED state semantics
    - .planning/phases/05-startup-wiring-mock-rpc/05-RESEARCH.md — Pitfall #4 (lines 328-342): scope considerations for guard removal
    - src/account/TransactionManager.cpp — MintFunds() and PutUTXO call chain — to understand where burn UTXOs are inserted
  </read_first>
  <action>
    **D-17, D-18** — Remove all 8 foreign-address guards and implement UTXO_RESERVED state handling.

    **Part A: Guard removal (D-17):**

    Remove the `if ( !is_full_node_ && address != address_ )` guard at each of these 8 locations in UTXOManager.cpp:

    | Line | Method | Guard Pattern |
    |------|--------|---------------|
    | ~83 | GetUTXOs() | `if ( !is_full_node_ && address != address_ ) return {};` |
    | ~124 | GetUTXOsPaged() | `if ( !is_full_node_ && address != address_ ) return {};` |
    | ~161 | PutUTXO() | `if ( !is_full_node_ && address != address_ ) return false;` |
    | ~191 | RemoveUTXO() | `if ( !is_full_node_ && address != address_ ) return false;` |
    | ~338 | ComputeUTXOMerkleRoot() | `if ( !is_full_node_ && address != address_ ) return {};` |
    | ~573 | GetOutPointState() | `if ( !is_full_node_ && address != address_ ) return std::nullopt;` |
    | ~850 | BuildUTXORecordKey() | guard variant |
    | ~931 | another guard variant | guard variant |

    For each location:
    - Delete the `if` block entirely (the condition AND the return statement)
    - Keep the code that was after the guard — it now executes for all addresses
    - The `address_` member variable may become unused after guard removal — do NOT delete it (other code may reference it)

    Per D-17: "Remove the 8 `!is_full_node_ && address != address_` guards" — all 8 must be removed. The CONTEXT.md says "all nodes store UTXOs for all peers" — this enables validators to track burn UTXOs from other addresses for conflict detection.

    **⚠️ Pitfall #4 Warning (RESEARCH.md lines 328-342):** Verify that removing these guards does not break balance calculations. `GetBalance()` at line 99 already filters by address within its loop (`entry.utxo.GetOwnerAddress() == address`), so guard removal won't affect balance correctness — the address filter is applied at a different level.

    **Part B: UTXO_RESERVED state handling (D-18):**

    1. **ToProtoState / FromProtoState (lines 54-63):** Update serialization helpers. `UTXO_RESERVED` should serialize as a distinct protobuf state value. Since the current proto enum has only `UTXO_ENTRY_CONSUMED` (UTXO_CONSUMED → this value, UTXO_READY → default), the RESERVED state needs a proto mapping. Check the proto definition (`SGTransaction::UTXOEntryState`) — if there's no RESERVED enum value, map UTXO_RESERVED to the READY proto value (same as UTXO_READY). This is correct because RESERVED is an intermediate in-memory state — on deserialization, RESERVED UTXOs become READY and the catch-up scan will re-detect them as burns.

    Alternatively, if you can add a proto field, add `UTXO_ENTRY_RESERVED = 2` to the proto. But proto changes may have broader impact. Use the mapping approach: `UTXO_RESERVED` serializes same as `UTXO_READY`.

    2. **IsOutPointConsumed() (line 563):** Current implementation:
       ```
       return state.has_value() && state.value() == UTXOState::UTXO_CONSUMED;
       ```
       This already returns `false` for RESERVED (since it's not CONSUMED). No change needed — RESERVED UTXOs are NOT considered consumed. ✅

    3. **SelectUTXOs() + GetBalance() — skip RESERVED:** In the loops that iterate UTXOs and check `state != UTXOState::UTXO_READY` (lines 99, 140, 278, 304, 504, 596, 869, 1017), these already skip non-READY UTXOs. Since RESERVED is not READY, these loops will skip RESERVED UTXOs. No change needed. ✅

    4. **Add IsOutPointReserved() predicate** (optional but useful for Plan 05-05 catch-up scan):
       ```cpp
       bool UTXOManager::IsOutPointReserved(const base::Hash256 &utxo_id, uint32_t output_idx) const {
           auto state = GetOutPointState(utxo_id, output_idx);
           return state.has_value() && state.value() == UTXOState::UTXO_RESERVED;
       }
       ```
       Declare in header, implement in .cpp near IsOutPointConsumed(). This is needed by the startup catch-up scan (Plan 05-05) to find burn UTXOs currently in consensus.

    5. **PutUTXO should mark burns as UTXO_BRIDGE:** When MintFunds creates a burn UTXO, it calls PutUTXO(). The PutUTXO method should accept an optional UTXOType parameter (default UTXO_NORMAL). Add a new overload or default parameter:
       ```cpp
       bool PutUTXO(const GeniusUTXO &utxo, const std::string &address, UTXOType type = UTXOType::UTXO_NORMAL);
       ```
       Store `type` in the UTXOEntry. This enables the startup catch-up scan to find bridge UTXOs by filtering on `UTXOType::UTXO_BRIDGE`.
  </action>
  <verify>
    <automated>ctest --test-dir build -R UTXOManager -j4</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "!is_full_node_ && address != address_" src/account/UTXOManager.cpp` returns ZERO matches (all 8 guards removed)
    - Source assertion: ToProtoState() handles UTXO_RESERVED (maps to appropriate proto value, no crash on unknown enum)
    - Source assertion: IsOutPointConsumed() returns false for UTXO_RESERVED (existing logic already correct, verify with test)
    - Source assertion: IsOutPointReserved() method exists and returns true for UTXO_RESERVED state
    - Source assertion: PutUTXO() signature has optional UTXOType parameter (default UTXO_NORMAL)
    - Test command: `ctest --test-dir build -R UTXOManager -j4` — all existing UTXO tests pass
    - Behavior: PutUTXO with UTXOType::UTXO_BRIDGE stores type in UTXOEntry, retrievable via GetOutPointState + type lookup
    - Behavior: SelectUTXOs() does NOT select RESERVED UTXOs (skipped like CONSUMED)
  </acceptance_criteria>
  <done>8 guards removed, UTXO_RESERVED serialized and handled in lifecycle, IsOutPointReserved() predicate added, PutUTXO accepts UTXOType, all existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PutUTXO() caller → UTXOManager | UTXO data + type crosses from BridgeRelayer/MintFunds (trusted) to UTXOManager (trusted) — same process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Elevation of Privilege | Guard removal — PutUTXO() now writes foreign addresses | mitigate | Only MintFunds/BridgeRelayer path creates UTXOType::UTXO_BRIDGE entries for foreign addresses — audit PutUTXO callers to ensure no unauthorized foreign writes |
| T-05-09 | Denial of Service | Guard removal — unbounded UTXO storage | accept | Non-full nodes now store all peers' UTXOs — increased memory/disk usage. Mitigated by: burn UTXOs are finite (one per bridge event), and the guard removal is required for validator conflict detection. Monitor memory in testing. |
| T-05-10 | Information Disclosure | UTXOType field in serialized protobuf | accept | UTXOType is not sensitive — it's a category tag. Nodes already share UTXO data via CRDT. No additional disclosure risk. |
| T-05-SC | Tampering | npm/pip/cargo installs | N/A | No external packages installed by this plan |
</threat_model>

<verification>
Run: `ctest --test-dir build -R UTXOManager -j4`
All existing UTXOManager tests must pass.
Run: `grep -c "!is_full_node_ && address != address_" src/account/UTXOManager.cpp` must return 0.
Build must succeed: `cmake --build build --target supergenius_account`
</verification>

<success_criteria>
- All 8 foreign-address guards removed from UTXOManager.cpp
- UTXOState enum includes UTXO_RESERVED between READY and CONSUMED
- UTXOType enum exists with UTXO_BRIDGE value
- UTXOEntry includes type field (D-19)
- IsOutPointReserved() predicate available for catch-up scan
- PutUTXO() accepts optional UTXOType parameter
- All existing UTXOManager tests pass
- Serialization handles RESERVED state and UTXOType correctly
</success_criteria>

<output>
Create `.planning/phases/05-startup-wiring-mock-rpc/05-04-SUMMARY.md` when done
</output>
