---
phase: 05-startup-wiring-mock-rpc
plan: 05
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-03
  - 05-04
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - src/account/ChainRpcEndpointProvider.hpp
  - src/account/ChainRpcEndpointProvider.cpp
  - evmrelay/examples/chains_config.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "InitializeAndStartBridge() fires asynchronously from INITIALIZING_TRANSACTIONS state after transaction_manager_ is created (D-04)"
    - "InitializeRpcEndpoints() sources chain list from chains_config.json with bridge_contract_address — no hardcoded ChainIdMap (D-02, D-05)"
    - "BridgeRelayer::Start() only fires after RPC endpoints are ready — ordering guaranteed (D-04)"
    - "chains_config.json has optional bridge_contract_address on 8 chains: ethereum-mainnet, ethereum-sepolia, bnb-smart-chain, bnb-smart-chain-testnet, polygon-mainnet, polygon-amoy, base-mainnet, base-sepolia (D-03)"
    - "Chains without bridge_contract_address are skipped for bridge startup (D-02)"
    - "chains.json path resolved relative to binary directory — not CWD (Pitfall #3 fix)"
    - "API-key direct endpoints configurable via ChainRpcProviderConfig.direct_endpoints (D-06)"
    - "GenesisNode::PerformStartupCatchupScan() probes RPC for historical burns, matches against UTXO set, inserts missing burns as READY with UTXOType::UTXO_BRIDGE (D-20)"
    - "SGNS_E2E_REAL_RPC=1 env var enables real RPC transport opt-in for test binaries (D-15)"
    - "Bridge burn on Sepolia triggers MintFunds via BridgeRelayer when node starts (end-to-end wiring works)"
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "InitializeAndStartBridge() and PerformStartupCatchupScan() declarations"
      contains: "InitializeAndStartBridge"
      contains: "PerformStartupCatchupScan"
    - path: "src/account/GeniusNode.cpp"
      provides: "Async bridge init wiring in INITIALIZING_TRANSACTIONS, rewritten InitializeRpcEndpoints(), catch-up scan"
      contains: "boost::asio::post"
      contains: "bridge_relayer_->Start("
    - path: "evmrelay/examples/chains_config.json"
      provides: "bridge_contract_address field on 8 chain entries"
      contains: "bridge_contract_address"
  key_links:
    - from: "GeniusNode::StateTransition(INITIALIZING_TRANSACTIONS)"
      to: "BridgeRelayer::Start(vector<ChainContractPair>)"
      via: "async InitializeAndStartBridge()"
      pattern: "InitializeAndStartBridge"
    - from: "GeniusNode::InitializeRpcEndpoints()"
      to: "PublicChainInputValidator::SetRpcEndpoints()"
      via: "WeightedRpcEndpoint with bridge_contract_address + event_topic0"
      pattern: "bridge_contract_address.*event_topic0"
    - from: "chains_config.json"
      to: "GeniusNode::InitializeRpcEndpoints()"
      via: "JSON parse → bridge_contract_address per chain"
      pattern: "bridge_contract_address"
---

<objective>
Wire BridgeRelayer::Start() and InitializeRpcEndpoints() into GeniusNode's startup state machine so the burn→MintFunds pipeline actually runs in normal nodes. Add a startup catch-up scan that probes RPC for unprocessed bridged transactions after CRDT sync. Fix the CWD-relative chains.json path fragility. Update chains_config.json with bridge_contract_address for 8 deployed chains.

Purpose: Currently BridgeRelayer::Start() is never called and InitializeRpcEndpoints() has hardcoded chain data — the bridge pipeline is dead in production nodes. This plan activates it.

Output: GeniusNode with wired async bridge initialization, rewritten InitializeRpcEndpoints() sourcing from chains_config.json, catch-up scan for historical burns, and updated chain configuration.
</objective>

<execution_context>
@/Users/henriqueklein/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-startup-wiring-mock-rpc/05-CONTEXT.md
@.planning/phases/05-startup-wiring-mock-rpc/05-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp
@src/account/BridgeRelayer.hpp
@src/account/ChainRpcEndpointProvider.hpp
@src/account/ChainRpcEndpointProvider.cpp
@src/account/PublicChainInputValidator.hpp
@evmrelay/examples/chains_config.json
@evmrelay/examples/send_test_transactions.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bridge_contract_address to chains_config.json and extend ChainRpcEndpointProvider</name>
  <files>evmrelay/examples/chains_config.json, src/account/ChainRpcEndpointProvider.hpp, src/account/ChainRpcEndpointProvider.cpp</files>
  <read_first>
    - evmrelay/examples/chains_config.json — current format (656 lines), chain entries have fields: name, chain, chainId, rpc, etc. No bridge_contract_address currently.
    - evmrelay/examples/send_test_transactions.sh — gnus_contract() function (lines 91-103): contract addresses for all 8 chains
    - src/account/ChainRpcEndpointProvider.hpp — ChainRpcProviderConfig struct (lines 29-51): chains_json_path, direct_endpoints. Does NOT have bridge contract info.
    - src/account/ChainRpcEndpointProvider.cpp — Initialize() method (lines 23-129): constructs WeightedRpcEndpoint at line 82-83 with only url and weight, no bridge_contract_address or event_topic0
    - src/account/PublicChainInputValidator.hpp — WeightedRpcEndpoint struct (lines 26-32): has bridge_contract_address and event_topic0 fields, both empty by default
    - .planning/phases/05-startup-wiring-mock-rpc/05-CONTEXT.md — D-02, D-03, D-05
  </read_first>
  <action>
    **D-02, D-03, D-05** — Update configuration and provider to carry bridge contract info.

    **Part A: chains_config.json (D-02, D-03)**

    Add an optional `"bridge_contract_address"` field to 8 chain entries in `evmrelay/examples/chains_config.json`. The field is a JSON string (hex address). Only chains with deployed GNUS bridges get this field. Contract addresses from `send_test_transactions.sh` gnus_contract():

    | Config name | Contract Address |
    |---|---|
    | ethereum-mainnet | 0x614577036F0a024DBC1C88BA616b394DD65d105a |
    | ethereum-sepolia | 0x9af8050220D8C355CA3c6dC00a78B474cd3e3c70 |
    | bnb-smart-chain | 0x614577036F0a024DBC1C88BA616b394DD65d105a |
    | bnb-smart-chain-testnet | 0xeC20bDf2f9f77dc37Ee8313f719A3cbCFA0CD1eB |
    | polygon-mainnet | 0x127E47abA094a9a87D084a3a93732909Ff031419 |
    | polygon-amoy | 0xeC20bDf2f9f77dc37Ee8313f719A3cbCFA0CD1eB |
    | base-mainnet | 0x614577036F0a024DBC1C88BA616b394DD65d105a |
    | base-sepolia | 0xeC20bDf2f9f77dc37Ee8313f719A3cbCFA0CD1eB |

    In each chain entry object, add `"bridge_contract_address": "0x..."` as a sibling of `"name"`, `"chain"`, etc. Skipped chains (ethereum-holesky, ethereum-hoodi, gnosis-chain per D-03) do NOT get this field.

    Per D-02: "Chains without the field are skipped" — the absence of the field is the signal to skip.

    **Part B: ChainRpcEndpointProvider config (D-05)**

    Add per-chain bridge info to ChainRpcProviderConfig so it can be passed to WeightedRpcEndpoint:

    1. In `ChainRpcEndpointProvider.hpp`, add two new fields to `ChainRpcProviderConfig`:
       - `std::unordered_map<uint64_t, std::string> bridge_contract_addresses` — keyed by numeric chain_id, value is "0x..." contract address
       - `std::unordered_map<uint64_t, std::string> bridge_event_topic0` — keyed by numeric chain_id, value is "0x..." topic0 hex

    2. In `ChainRpcEndpointProvider.cpp`, in the Initialize() method, after constructing each WeightedRpcEndpoint (lines 82-83, 108), look up the chain_id in the new maps and set bridge_contract_address and event_topic0 if present:

       After line 83 (`{ ep.url_template, kPublicEndpointWeight }`), add:
       ```
       auto &ep_ref = endpoints_by_chain[ep.chain_id].back();
       if (auto it = config.bridge_contract_addresses.find(ep.chain_id); it != config.bridge_contract_addresses.end()) {
           ep_ref.bridge_contract_address = it->second;
       }
       if (auto it = config.bridge_event_topic0.find(ep.chain_id); it != config.bridge_event_topic0.end()) {
           ep_ref.event_topic0 = it->second;
       }
       ```
       Same for line 108 (direct endpoints push_back).

    **Event topic0 computation:** The event is `BridgeSourceBurned(address,uint256,uint256,uint256,uint256)`. The topic0 is keccak256 of this string. Use `eth::cli::event_registry().params_for(event_sig)` to get the hash, or compute it directly. The exact value is: `keccak256("BridgeSourceBurned(address,uint256,uint256,uint256,uint256)")`. Research this in the evmrelay codebase — `eth::cli::event_registry()` already computes topic0 for registered events. The BridgeSourceBurned event should be registered there, and its topic0 can be obtained.

    Verify that `eth::cli::event_registry().params_for("BridgeSourceBurned(address,uint256,uint256,uint256,uint256)")` returns valid params — check `evmrelay/src/eth/eth_watch_cli.cpp` or similar for how events are registered. Use `eth::compute_event_topic0(event_sig)` if available, or `eth::Hash256` from keccak256 of the event signature string.
  </action>
  <verify>
    <automated>cmake --build build --target supergenius_account 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "bridge_contract_address" evmrelay/examples/chains_config.json` returns 8 (one per chain with deployed bridge)
    - Source assertion: ChainRpcProviderConfig struct contains `bridge_contract_addresses` and `bridge_event_topic0` map fields
    - Source assertion: ChainRpcEndpointProvider::Initialize() sets bridge_contract_address on WeightedRpcEndpoint when config has it for that chain_id
    - Source assertion: ChainRpcEndpointProvider::Initialize() sets event_topic0 on WeightedRpcEndpoint when config has it for that chain_id
    - Source assertion: 8 chain entries in chains_config.json have correct contract addresses matching send_test_transactions.sh gnus_contract() values
    - Build: `cmake --build build --target supergenius_account` compiles with no errors
  </acceptance_criteria>
  <done>chains_config.json with bridge_contract_address for 8 chains, ChainRpcEndpointProvider extended to carry bridge contract + event_topic0 to WeightedRpcEndpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Wire InitializeAndStartBridge() + rewrite InitializeRpcEndpoints() in GeniusNode</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp</files>
  <read_first>
    - src/account/GeniusNode.hpp — lines 600-628: private members (bridge_relayer_, eth_watch_service_, transaction_manager_, dev_config_, node_logger_, io_), line 734: InitializeRpcEndpoints() declaration
    - src/account/GeniusNode.cpp — lines 403-434: INITIALIZING_TRANSACTIONS case in New() state machine, lines 1961-1986: current InitializeRpcEndpoints()
    - src/account/BridgeRelayer.hpp — updated in Plan 05-01: ChainContractPair struct, Start(vector<ChainContractPair>) signature
    - src/account/ChainRpcEndpointProvider.hpp — updated in Task 1: ChainRpcProviderConfig with bridge_contract_addresses and bridge_event_topic0
    - src/account/PublicChainInputValidator.hpp — from Plan 05-03: SetTransportFactory(), TransportFactory type
    - .planning/phases/05-startup-wiring-mock-rpc/05-RESEARCH.md — Pitfall #1 (lines 293-303): transaction_manager_ availability timing, Pitfall #2 (lines 304-312): EthWatchService initialization, Pitfall #3 (lines 314-327): CWD path fix
  </read_first>
  <action>
    **D-04, D-05, D-06, Pitfall #1, Pitfall #2, Pitfall #3** — Wire bridge startup into GeniusNode and rewrite RPC endpoint initialization.

    **Part A: Add InitializeAndStartBridge() method**

    In GeniusNode.hpp, add private method declaration (near line 734 where InitializeRpcEndpoints() is declared):
    ```cpp
    void InitializeAndStartBridge();
    ```

    In GeniusNode.cpp, implement InitializeAndStartBridge():
    - Launch via `boost::asio::post(io_, [weak_self = weak_from_this()] { ... })` — non-blocking async per D-04
    - Inside the async lambda:
      1. Lock weak_self, return if expired
      2. Call `InitializeRpcEndpoints()` — this populates RPC endpoints AND returns the list of (chain_name, contract_address) pairs
      3. After endpoints are ready, if bridge_relayer_ is non-null, call `bridge_relayer_->Start(chain_contract_pairs)` per D-04 ordering guarantee
      4. Log: "BridgeRelayer startup: watching N chains"
    - **Pitfall #2 (EthWatchService):** Check if `eth_watch_service_->initialize()` needs to be called before `watch_event()`. If so, add the initialize() call in the CREATING state where eth_watch_service_ is created (line 428), or in InitializeAndStartBridge() before Start(). Verify by checking EthWatchService API.

    **Part B: Wire into state machine (D-04)**

    In GeniusNode::New(), in the `case NodeState::INITIALIZING_TRANSACTIONS:` block (after line 434), add:
    ```cpp
    // Launch async bridge initialization — non-blocking
    // InitializeRpcEndpoints() + BridgeRelayer::Start() fire asynchronously
    InitializeAndStartBridge();
    ```
    This fires AFTER transaction_manager_ and bridge_relayer_ are created (lines 405-432). Per RESEARCH.md Pitfall #1: transaction_manager_ is available here, unlike in CREATING state. The state machine proceeds to INITIALIZING_PROCESSING without waiting — the async lambda handles its own lifecycle.

    **Part C: Rewrite InitializeRpcEndpoints() (D-02, D-05, D-06, Pitfall #3)**

    Replace the current hardcoded implementation (lines 1961-1986) with a new version that sources from chains_config.json and returns chain/contract pairs:

    New signature: change from `void InitializeRpcEndpoints()` to returning chain/contract pairs, or use an out-parameter. Simpler: have InitializeAndStartBridge() call a helper. Keep InitializeRpcEndpoints() as void but have it populate a member or return value used by the caller.

    Approach: InitializeRpcEndpoints() builds the chain/contract list and stores it. Or: make InitializeRpcEndpoints() return a `std::vector<ChainContractPair>`:

    ```cpp
    std::vector<ChainContractPair> GeniusNode::InitializeRpcEndpoints()
    ```

    Implementation:
    1. Guard: if !transaction_manager_, log warning and return empty vector.
    2. **Path fix (Pitfall #3):** Resolve chains.json path relative to binary directory using `boost::dll::program_location().parent_path() / "chains.json"` instead of `std::filesystem::current_path() / "chains.json"`. Fall back to CWD if binary path not available (e.g., when running from debugger). Check `dev_config_` for any overridable path setting.
    3. Read chains_config.json using `std::ifstream` + `std::istreambuf_iterator` (same pattern as ChainRpcEndpointProvider.cpp lines 49-59).
    4. Parse JSON with `boost::json::parse()`.
    5. Iterate chain entries. For each entry with a `"bridge_contract_address"` field (D-02: skip if absent):
       - Extract chain name (e.g., "ethereum-sepolia") and numeric chain_id (from "chainId" field)
       - Extract bridge_contract_address
       - Compute event_topic0 for `BridgeSourceBurned(address,uint256,uint256,uint256,uint256)`:
         Use `eth::cli::event_registry().params_for("BridgeSourceBurned(address,uint256,uint256,uint256,uint256)")` and extract the topic0 hash. If params_for returns valid params, the topic0 is embedded in them. Check the eth_watch_cli API for a direct `topic0()` accessor. If not available, compute keccak256 of the event signature string using a crypto utility.
       - Build `ChainRpcProviderConfig` with:
         - `chains_json_path` = resolved path (from step 2)
         - `direct_endpoints` = from `ChainRpcProviderConfig.direct_endpoints` (D-06, already supported)
         - `bridge_contract_addresses[chain_id]` = contract address
         - `bridge_event_topic0[chain_id]` = event topic0
       - Build `ChainIdMap` with `{chain_name → numeric_chain_id}` only for chains with bridge_contract_address
       - Create `ChainRpcEndpointProvider(chain_id_map)`, call `provider.Initialize(validator, config, logger)`
       - On success, add `ChainContractPair{chain_name, contract_address}` to the return vector
       - On failure, log warning and continue (best-effort per chain)
    6. Return the vector of ChainContractPair for BridgeRelayer::Start()

    **D-03 contract addresses for ChainIdMap:** The ChainIdMap maps config names to numeric chain IDs:
    - ethereum-mainnet → 1
    - ethereum-sepolia → 11155111
    - bnb-smart-chain → 56
    - bnb-smart-chain-testnet → 97
    - polygon-mainnet → 137
    - polygon-amoy → 80002
    - base-mainnet → 8453
    - base-sepolia → 84532

    **D-06 API-key endpoints:** The `direct_endpoints` map in ChainRpcProviderConfig is populated by the app layer (desktop launcher or mobile host) before GeniusNode construction. InitializeRpcEndpoints() should check if it's non-empty and log the count. Do NOT read API keys from environment variables or tracked files.

    7. Remove the hardcoded `ChainIdMap` (lines 1969-1973) — all chain data now sourced from chains_config.json.
  </action>
  <verify>
    <automated>cmake --build build --target genius_node 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: GeniusNode.hpp contains `void InitializeAndStartBridge()` private method declaration (or `void PerformStartupCatchupScan()` if separate)
    - Source assertion: GeniusNode.cpp INITIALIZING_TRANSACTIONS case calls InitializeAndStartBridge() (or launches the async function)
    - Source assertion: InitializeAndStartBridge() uses `boost::asio::post(io_, ...)` for async execution (D-04 non-blocking)
    - Source assertion: BridgeRelayer::Start() is called with the chain/contract pairs AFTER InitializeRpcEndpoints() completes (D-04 ordering)
    - Source assertion: InitializeRpcEndpoints() no longer has hardcoded ChainIdMap with 4 chains — sources from chains_config.json
    - Source assertion: chains.json path resolves relative to binary directory using `boost::dll::program_location().parent_path()`, not `std::filesystem::current_path()` (Pitfall #3)
    - Source assertion: InitializeRpcEndpoints() reads `bridge_contract_address` from chains_config.json entries
    - Source assertion: event_topic0 for BridgeSourceBurned is computed and passed to ChainRpcProviderConfig
    - Source assertion: `direct_endpoints` from ChainRpcProviderConfig is carried through to ChainRpcEndpointProvider (D-06)
    - Build: `cmake --build build --target genius_node` compiles successfully
  </acceptance_criteria>
  <done>GeniusNode wired with async bridge initialization: InitializeRpcEndpoints() sources from chains_config.json with bridge_contract_address, BridgeRelayer::Start() fires after endpoints ready, CWD path fixed to binary-relative.</done>
</task>

<task type="auto">
  <name>Task 3: Implement startup catch-up scan for unprocessed bridged transactions</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp</files>
  <read_first>
    - src/account/GeniusNode.cpp — updated in Task 2: INITIALIZING_TRANSACTIONS case, InitializeAndStartBridge()
    - src/account/TransactionManager.hpp — MintFunds(), GetLastMintTimestamp() or equivalent API for finding last processed mint
    - src/account/UTXOManager.hpp — updated in Plan 05-04: IsOutPointReserved(), UTXOType::UTXO_BRIDGE, PutUTXO with UTXOType
    - src/account/BridgeRelayer.hpp — from Plan 05-01: ChainContractPair struct
    - evmrelay/include/eth/json_rpc.hpp — eth_getLogs request construction or equivalent RPC query mechanism
    - evmrelay/include/eth/rpc_receipt_source.hpp — RpcReceiptSource for backfill queries
    - .planning/phases/05-startup-wiring-mock-rpc/05-CONTEXT.md — D-20: startup catch-up scan requirements
    - .planning/phases/05-startup-wiring-mock-rpc/05-RESEARCH.md — Pattern 3 (lines 236-254): catch-up scan integration, Open Question #4 (lines 529-534): scan depth/mechanism
  </read_first>
  <action>
    **D-20** — Implement startup catch-up scan that probes RPC for historical burns after CRDT sync.

    **Part A: Add PerformStartupCatchupScan() method**

    In GeniusNode.hpp, add private method declaration:
    ```cpp
    void PerformStartupCatchupScan();
    ```

    In GeniusNode.cpp, implement the method. Call it from InitializeAndStartBridge() (Task 2) after endpoints are ready and BridgeRelayer::Start() is called, OR from the TransactionManager state change callback when it reaches READY state (TransactionStateChanged at line 2005, case READY). The best timing: after CRDT sync is complete, which is indicated by TransactionManager reaching READY state. Add the call in `TransactionStateChanged()` case `TransactionManager::State::READY` (around line 2011).

    Actually, per RESEARCH.md Pattern 3: "After CRDT sync in INITIALIZING_TRANSACTIONS, a new method PerformStartupCatchupScan() queries RPC for historical burns." And the integration point is after transaction_manager_ is ready. The TransactionStateChanged callback at line 2011 fires when TransactionManager reaches READY — this is after CRDT sync. Add the call there, guarded by a flag so it only runs once:

    ```cpp
    case TransactionManager::State::READY:
        if (!catchup_scan_done_) {
            catchup_scan_done_ = true;
            boost::asio::post(io_, [weak_self = weak_from_this()] {
                if (auto self = weak_self.lock()) {
                    self->PerformStartupCatchupScan();
                }
            });
        }
    ```

    Add `bool catchup_scan_done_ = false` member to GeniusNode.hpp private section.

    **Part B: Implement PerformStartupCatchupScan() body**

    Per RESEARCH.md Code Examples (lines 453-479) and CONTEXT.md D-20:

    1. Guard: if !transaction_manager_ or !bridge_relayer_, return.

    2. **Get last mint timestamp:** Check TransactionManager API for a method like `GetLastMintTimestamp()` or `GetLastMintBlock()`. If no direct API exists, query the CRDT/GlobalDB for the most recent MintTransactionV2 by date. Store the timestamp/block_number as `last_mint_block`. If no prior mints exist, default to 0 (scan from chain genesis — but cap at max scan depth).

    3. **Scan depth (RESEARCH.md Open Question #4):** Default max depth = 10,000 blocks. Make configurable via `DevConfig_st` field `bridge_catchup_scan_depth` (add if not present). Default 10,000 blocks (~2 days on Ethereum mainnet, ~4 hours on Polygon).

    4. **For each chain with bridge_contract_address:**
       - Skip if contract_address is empty
       - Compute `from_block = max(last_mint_block - scan_depth, 0)`
       - Construct `eth_getLogs` request with:
         - `address` = bridge_contract_address
         - `topics[0]` = event_topic0 for `BridgeSourceBurned(address,uint256,uint256,uint256,uint256)`
         - `fromBlock` = hex(from_block)
         - `toBlock` = "latest"
       - Send RPC request via the transport factory (reuse the same RPC mechanism as VerifyPublicChainSmartContract) OR use `eth::rpc::RpcReceiptSource` for backfill capability
       - Parse response to extract burn event logs
       - For each burn event:
         a. Extract tx_hash from log
         b. Check if burn UTXO already exists: use `transaction_manager_->HasBurnUTXO(tx_hash)` or query UTXOManager for UTXOs with UTXOType::UTXO_BRIDGE matching this tx_hash
         c. If NOT found: insert as READY burn UTXO using `MintFunds()` or direct `PutUTXO()` with `UTXOType::UTXO_BRIDGE` and `UTXOState::UTXO_READY`
         d. If found but in RESERVED state: log "burn already in consensus, tx={}" — skip
         e. If found but CONSUMED: log "burn already processed" — skip

    5. **RPC timeout and error handling:** Each RPC call times out after 10 seconds. If RPC fails for a chain, log warning and continue to next chain (best-effort).

    6. **Logging:** Log "CatchUpScan: found N historical burns across M chains" at completion.

    **RPC transport for catch-up scan:** The catch-up scan needs its own RPC transport instance (not the validator's transport factory). Use a direct `RpcHttpTransport` construction for each chain's RPC endpoint, or use a shared RPC client. Since this runs during startup (not in validator hot path), a simple `RpcHttpTransport` per chain RPC URL is acceptable.

    For `eth_getLogs` RPC calls, check if evmrelay provides a helper: look for `eth::rpc::make_eth_getLogs_request()` or similar in `evmrelay/include/eth/json_rpc.hpp`. If not available, construct the JSON-RPC request manually using `boost::json::object`.

    **DevConfig_st field:** Add `uint64_t bridge_catchup_scan_depth = 10000` to `DevConfig_st` in GeniusNode.hpp (or wherever it's defined). This allows production configuration without recompilation.
  </action>
  <verify>
    <automated>cmake --build build --target genius_node 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: GeniusNode.hpp contains `void PerformStartupCatchupScan()` declaration
    - Source assertion: GeniusNode.hpp contains `bool catchup_scan_done_ = false` member
    - Source assertion: TransactionStateChanged() READY case calls PerformStartupCatchupScan() once via boost::asio::post
    - Source assertion: PerformStartupCatchupScan() iterates chains with bridge_contract_address, constructs eth_getLogs request
    - Source assertion: Missing burns inserted as UTXO_READY with UTXOType::UTXO_BRIDGE (D-20)
    - Source assertion: Already-processed burns (CONSUMED/RESERVED) are skipped
    - Source assertion: Scan depth capped at 10,000 blocks (configurable via DevConfig_st)
    - Source assertion: RPC failure on one chain does not block other chains (best-effort)
    - Build: `cmake --build build --target genius_node` compiles successfully
    - Behavior: Node startup logs "CatchUpScan: found N historical burns" when historical burns are detected
  </acceptance_criteria>
  <done>Startup catch-up scan: after CRDT sync, probes RPC for unprocessed burns, inserts missing burns as READY with UTXO_BRIDGE type, skips already-processed burns, best-effort across chains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chains_config.json → InitializeRpcEndpoints() | JSON file on disk parsed at startup — untrusted input |
| RPC response → PerformStartupCatchupScan() | Network data from external RPC endpoints — untrusted input |
| GeniusNode → BridgeRelayer::Start() | ChainContractPair vector crosses internal boundary — same process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Spoofing | chains_config.json bridge_contract_address | mitigate | Contract address format validated (0x + 40 hex chars); cross-check against known deployment addresses from send_test_transactions.sh during code review |
| T-05-12 | Tampering | chains_config.json modification | mitigate | Production deployment should set read-only permissions on chains_config.json; config hash can be validated at startup |
| T-05-13 | Denial of Service | Startup catch-up scan — unbounded RPC queries | mitigate | Scan depth capped at 10,000 blocks (configurable); each RPC call times out at 10s; best-effort per chain — failed queries don't block startup |
| T-05-14 | Elevation of Privilege | Injecting fake burns via catch-up scan | mitigate | Catch-up scan RPC responses are verified against bridge_contract_address + event_topic0 — fake events from wrong contract are rejected; only BridgeSourceBurned topic0 logs are processed |
| T-05-15 | Denial of Service | Malformed chains_config.json crash | mitigate | boost::json::parse() exceptions caught; malformed JSON logged and startup continues without bridge support (graceful degradation) |
| T-05-SC | Tampering | npm/pip/cargo installs | N/A | No external packages installed by this plan |
</threat_model>

<verification>
Run: `cmake --build build --target genius_node` — must compile cleanly.
Manually verify: start a node in test mode, check logs for "BridgeRelayer startup: watching N chains" and "CatchUpScan" entries.
</verification>

<success_criteria>
- InitializeAndStartBridge() fires asynchronously during INITIALIZING_TRANSACTIONS
- InitializeRpcEndpoints() sources chains from chains_config.json, not hardcoded
- BridgeRelayer::Start() called after RPC endpoints ready (D-04 ordering)
- chains_config.json has bridge_contract_address on 8 chains (D-03)
- chains.json path is binary-relative (Pitfall #3 fixed)
- Startup catch-up scan runs after CRDT sync (D-20)
- Missing burn UTXOs inserted as READY with UTXO_BRIDGE type
- Scan depth capped at 10,000 blocks (configurable)
- Best-effort across chains — one failure doesn't block others
- SGNS_E2E_REAL_RPC=1 env var supported for test opt-in (D-15)
- genius_node compiles and links successfully
</success_criteria>

<output>
Create `.planning/phases/05-startup-wiring-mock-rpc/05-05-SUMMARY.md` when done
</output>
