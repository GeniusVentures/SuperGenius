---
phase: 10-authoritative-slot-certificate-publication
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/crdt/globaldb/globaldb.hpp
  - src/crdt/globaldb/globaldb.cpp
  - src/crdt/crdt_datastore.hpp
  - src/crdt/impl/crdt_datastore.cpp
  - src/crdt/crdt_set.hpp
  - src/crdt/impl/crdt_set.cpp
  - test/src/crdt/crdt_datastore_test.cpp
  - test/src/crdt/globaldb_integration.cpp
autonomous: true
requirements: [CERT-04]
nyquist_compliant: true
must_haves:
  truths:
    - "An immutable authoritative-record write creates an empty key, accepts an identical replay, and deterministically retains the lowest SHA-256 lowercase-hex certificate encoding when different bytes contend."
    - "Two replicas that concurrently create different valid encodings at the same empty slot converge to the same lowest-hash bytes after replication in either synchronization order."
  artifacts:
    - path: src/crdt/globaldb/globaldb.hpp
      provides: Narrow GlobalDB immutable-write API returning an outcome result.
    - path: src/crdt/impl/crdt_set.cpp
      provides: Immutable-priority merge behavior that deterministically selects the lowest SHA-256 lowercase-hex value rather than local first-seen bytes.
    - path: test/src/crdt/crdt_datastore_test.cpp
      provides: Deterministic immutable-write/replay and local merge regressions.
    - path: test/src/crdt/globaldb_integration.cpp
      provides: Two-replica concurrent-write convergence regression through the actual GlobalDB replication path.
  key_links:
    - from: src/crdt/globaldb/globaldb.cpp
      to: src/crdt/impl/crdt_datastore.cpp
      via: GlobalDB immutable API delegates to an immutable CRDT delta creation path.
      pattern: "PutConvergentImmutable.*PutConvergentImmutableKey"
    - from: src/crdt/impl/crdt_datastore.cpp
      to: src/crdt/impl/crdt_set.cpp
      via: Explicit immutable priority survives delta application and makes every replica select the same SHA-256 lowercase-hex winner.
      pattern: "IMMUTABLE.*sha2_256.*hex_lower.*SetValue"
---

<objective>
Create the narrow convergent immutable CRDT write primitive needed to give an occupied authoritative certificate slot one deterministic value.

Purpose: implement D-04 at the storage layer by ordering concurrent certificate encodings with SHA-256 lowercase hex, rather than treating local first-seen state or a pre-read as a distributed compare-and-set.
Output: a no-dependency GlobalDB/CRDT convergent immutable-write path with unit and two-replica storage regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-CONTEXT.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-RESEARCH.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-VALIDATION.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-PATTERNS.md

<interfaces>
From `src/crdt/globaldb/globaldb.hpp`: `GlobalDB::Put(const HierarchicalKey &, const Buffer &, const std::unordered_set<std::string> &) -> outcome::result<CID>`.
From `src/crdt/crdt_datastore.hpp`: `CrdtDatastore::PutKey(...) -> outcome::result<CID>` builds and publishes an add delta.
From `src/crdt/crdt_set.hpp`: `CrdtSet::SetValue(..., uint64_t priority) -> outcome::result<void>` is called while applying local and received deltas; its immutable-priority branch must apply the same winner calculation in both paths.
From `src/crypto/hasher.hpp` and `src/base/hexutil.hpp`: use `crypto::sha2_256(serialized_certificate_bytes)` rendered by `base::hex_lower` as the D-04 ordering key.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify convergent immutable storage behavior with local and two-replica regressions</name>
  <files>test/src/crdt/crdt_datastore_test.cpp, test/src/crdt/globaldb_integration.cpp</files>
  <read_first>src/crdt/crdt_datastore.hpp; src/crdt/crdt_set.hpp; src/crdt/impl/crdt_set.cpp; test/src/crdt/crdt_datastore_test.cpp; test/src/crdt/globaldb_integration.cpp; src/crypto/hasher.hpp; src/base/hexutil.hpp</read_first>
  <behavior>
    - An immutable write to an absent key stores the supplied bytes.
    - Repeating an immutable write with byte-identical content succeeds and leaves the value unchanged.
    - For different serialized certificate bytes at the reserved immutable priority, the visible value is the value whose `base::hex_lower(crypto::sha2_256(bytes))` is lexicographically lowest, regardless of arrival order.
    - Two isolated GlobalDB replicas can write different valid-certificate byte fixtures to the same initially empty key, exchange state in both directions/orders, and each exposes the identical lowest-hash value without an overwrite race or local first-seen dependence.
  </behavior>
  <action>Add RED GoogleTest cases around the in-memory datastore fixture and the existing two-node `GlobalDBIntegrationTest` fixture. Feed the immutable path byte-identical and distinct serialized-certificate fixtures; calculate the expected D-04 winner in the test with `crypto::sha2_256` and `base::hex_lower`, never by insertion order. Keep the two nodes disconnected through their first writes, then synchronize A→B and B→A in separate test cases and assert both exposed values equal the same lowest-hash bytes. Keep ordinary `PutKey` behavior untouched; this documents D-04's special-purpose convergent primitive rather than a caller-side pre-read or a generic overwrite change.</action>
  <verify><automated>cmake --build build/OSX/Release --target crdt_datastore_test globaldb_integration_test consensus_pending_lifecycle_test consensus_slot_key_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R 'crdt_datastore_test|globaldb_integration_test|consensus_(slot_key|pending_lifecycle)_test' --output-on-failure</automated></verify>
  <acceptance_criteria>The new tests fail before the convergent API exists and prove two replicas converge on the same SHA-256 lowercase-hex winner despite opposite first-write and synchronization orders.</acceptance_criteria>
  <done>Storage behavior is expressed by deterministic tests for absence, identical replay, local merge ordering, and two-replica concurrent-first-write convergence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement a narrow convergent immutable CRDT delta and hash-ordered merge rule</name>
  <files>src/crdt/globaldb/globaldb.hpp, src/crdt/globaldb/globaldb.cpp, src/crdt/crdt_datastore.hpp, src/crdt/impl/crdt_datastore.cpp, src/crdt/crdt_set.hpp, src/crdt/impl/crdt_set.cpp</files>
  <read_first>test/src/crdt/crdt_datastore_test.cpp; src/crdt/globaldb/globaldb.hpp; src/crdt/globaldb/globaldb.cpp; src/crdt/crdt_datastore.hpp; src/crdt/impl/crdt_datastore.cpp; src/crdt/crdt_set.hpp; src/crdt/impl/crdt_set.cpp</read_first>
  <behavior>
    - The immutable path encodes an explicit reserved immutable priority in its replicated delta; ordinary `Put` keeps its current priority behavior.
    - At that immutable priority, an absent key is written, exact existing bytes are idempotent, and different serialized certificate bytes deterministically retain the lexicographically lowest `base::hex_lower(crypto::sha2_256(bytes))` value.
    - The same lowest-hash rule applies while receiving and merging deltas, so a lower-hash concurrent value replaces a higher-hash local provisional value and replicas converge; lower-priority ordinary updates cannot replace an immutable value.
  </behavior>
  <action>Introduce the named `GlobalDB::PutConvergentImmutable` → `CrdtDatastore::PutConvergentImmutableKey` → `CrdtSet` operation, using a private reserved immutable priority carried by the existing delta format so local and received updates share the rule. In `CrdtSet::SetValue`, handle that priority before its existing equal-priority replacement branch: exact bytes are replay; otherwise compute `crypto::sha2_256` over each serialized certificate byte sequence, render each digest through lowercase `base::hex_lower`, and persist only the lexicographically lowest value. A replica may provisionally hold its own empty-slot write, but a received lower-hash value must replace it; a received higher-hash value must not replace the current winner. This implements D-04 without local first-seen state, a pre-read CAS assumption, or a fail-open conflict outcome. Do not alter non-immutable callers, add a transaction system, or introduce a dependency.</action>
  <verify><automated>cmake --build build/OSX/Release --target crdt_datastore_test globaldb_integration_test consensus_pending_lifecycle_test consensus_slot_key_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R 'crdt_datastore_test|globaldb_integration_test|consensus_(slot_key|pending_lifecycle)_test' --output-on-failure &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>All Task 1 cases pass; the new API is the only path that requests immutable semantics, and a lower-hash concurrent payload replaces a higher-hash provisional value while the inverse never does.</acceptance_criteria>
  <done>An explicit replicated convergent immutable-write primitive is available for certificate authority and drives every replica to the same lowest-hash certificate bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| consensus publisher → CRDT delta | Certificate bytes become replicated persistent state. |
| remote CRDT delta → local CrdtSet | Untrusted peer-propagated bytes enter local merge behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-04 | Tampering | GlobalDB/CrdtSet immutable write | mitigate | D-04 reserved immutable deltas compare `sha2_256` lowercase-hex values, so local and remote application select one identical lowest-hash value rather than first-seen state. |
| T-10-09 | Denial of service | malformed/conflicting immutable delta | mitigate | Consensus admits only validated certificate bytes; the CRDT merge deterministically converges valid concurrent bytes and callers fail closed on unreadable/invalid authority. |
| T-10-SC | Tampering | package installs | mitigate | No package installs; use existing C++17, CMake, GoogleTest, RocksDB, and CRDT facilities only. |
</threat_model>

<verification>
Build the CRDT target plus the focused Phase 10 lifecycle/slot targets, run the listed CTest regex, and run `git diff --check`.
</verification>

<success_criteria>
- A caller has an explicit convergent immutable write result instead of read-then-write assumptions.
- Storage makes every replica select the same lowest SHA-256 lowercase-hex immutable value through local and replicated application.
- Existing ordinary CRDT `Put` semantics remain outside this special-purpose path.
</success_criteria>

<output>
Create `.planning/phases/10-authoritative-slot-certificate-publication/10-01-SUMMARY.md` when done.
</output>
