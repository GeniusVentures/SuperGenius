---
phase: 10-authoritative-slot-certificate-publication
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/crdt/globaldb/globaldb.hpp
  - src/crdt/globaldb/globaldb.cpp
  - src/crdt/crdt_datastore.hpp
  - src/crdt/impl/crdt_datastore.cpp
  - src/crdt/crdt_set.hpp
  - src/crdt/impl/crdt_set.cpp
  - test/src/crdt/crdt_datastore_test.cpp
autonomous: true
requirements: [CERT-04]
nyquist_compliant: true
must_haves:
  truths:
    - "An immutable authoritative-record write creates an empty key once, accepts an identical replay, and rejects different bytes without replacing the existing value."
    - "A replicated immutable write cannot replace a previously stored immutable value with different bytes."
  artifacts:
    - path: src/crdt/globaldb/globaldb.hpp
      provides: Narrow GlobalDB immutable-write API returning an outcome result.
    - path: src/crdt/impl/crdt_set.cpp
      provides: Immutable-priority merge behavior that preserves existing bytes and reports a conflict.
    - path: test/src/crdt/crdt_datastore_test.cpp
      provides: Deterministic immutable-write/replay/conflict regressions.
  key_links:
    - from: src/crdt/globaldb/globaldb.cpp
      to: src/crdt/impl/crdt_datastore.cpp
      via: GlobalDB immutable API delegates to an immutable CRDT delta creation path.
      pattern: "PutImmutable.*PutImmutableKey"
    - from: src/crdt/impl/crdt_datastore.cpp
      to: src/crdt/impl/crdt_set.cpp
      via: Explicit immutable priority survives delta application and selects non-overwrite handling.
      pattern: "IMMUTABLE.*SetValue"
---

<objective>
Create the narrow immutable CRDT write primitive needed to keep an occupied authoritative certificate slot from being overwritten by different content.

Purpose: close the D-04 collision-safety gap at the storage layer rather than treating a local pre-read as a distributed compare-and-set.
Output: a no-dependency GlobalDB/CRDT immutable-write path with focused storage regressions.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-CONTEXT.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-RESEARCH.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-VALIDATION.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-PATTERNS.md

<interfaces>
From `src/crdt/globaldb/globaldb.hpp`: `GlobalDB::Put(const HierarchicalKey &, const Buffer &, const std::unordered_set<std::string> &) -> outcome::result<CID>`.
From `src/crdt/crdt_datastore.hpp`: `CrdtDatastore::PutKey(...) -> outcome::result<CID>` builds and publishes an add delta.
From `src/crdt/crdt_set.hpp`: `CrdtSet::SetValue(..., uint64_t priority) -> outcome::result<void>` is called while applying local and received deltas.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify immutable-write storage behavior with deterministic CRDT tests</name>
  <files>test/src/crdt/crdt_datastore_test.cpp</files>
  <read_first>src/crdt/crdt_datastore.hpp; src/crdt/crdt_set.hpp; src/crdt/impl/crdt_set.cpp; test/src/crdt/crdt_datastore_test.cpp</read_first>
  <behavior>
    - An immutable write to an absent key stores the supplied bytes.
    - Repeating an immutable write with byte-identical content succeeds and leaves the value unchanged.
    - A different immutable payload for an occupied key reports a conflict and leaves the original value unchanged.
    - Applying a second immutable delta with different bytes follows the same non-overwrite rule, proving the rule is in the shared merge path rather than only a caller-side pre-read.
  </behavior>
  <action>Add focused GoogleTest cases around the existing in-memory CRDT datastore fixture for the new immutable API and incoming-delta merge path. Make the RED cases call the intended `GlobalDB`/`CrdtDatastore` immutable operation, seed an original value, and assert both its preserved bytes and an error on a conflicting payload. Keep ordinary `PutKey` behavior untouched; this test documents the D-04 special-purpose primitive rather than changing generic overwrite semantics.</action>
  <verify><automated>cmake --build build/OSX/Release --target crdt_datastore_test consensus_pending_lifecycle_test consensus_slot_key_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R 'crdt_datastore_test|consensus_(slot_key|pending_lifecycle)_test' --output-on-failure</automated></verify>
  <acceptance_criteria>The new tests fail before the immutable API exists and distinguish identical replay from different-content collision without relying on timing or a second process.</acceptance_criteria>
  <done>Storage behavior is expressed by deterministic tests for absence, identical replay, local collision, and received-delta collision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement a narrow immutable CRDT delta and no-overwrite merge rule</name>
  <files>src/crdt/globaldb/globaldb.hpp, src/crdt/globaldb/globaldb.cpp, src/crdt/crdt_datastore.hpp, src/crdt/impl/crdt_datastore.cpp, src/crdt/crdt_set.hpp, src/crdt/impl/crdt_set.cpp</files>
  <read_first>test/src/crdt/crdt_datastore_test.cpp; src/crdt/globaldb/globaldb.hpp; src/crdt/globaldb/globaldb.cpp; src/crdt/crdt_datastore.hpp; src/crdt/impl/crdt_datastore.cpp; src/crdt/crdt_set.hpp; src/crdt/impl/crdt_set.cpp</read_first>
  <behavior>
    - The immutable path encodes an explicit reserved immutable priority in its replicated delta; ordinary `Put` keeps its current priority behavior.
    - At that immutable priority, an absent key is written, exact existing bytes are idempotent, and different bytes return a conflict without replacing stored data.
    - Lower-priority ordinary updates cannot replace an immutable value; another immutable value with different bytes is also refused during merge.
  </behavior>
  <action>Introduce one explicitly named immutable write operation from `GlobalDB` through `CrdtDatastore` to `CrdtSet`, using a private reserved immutable priority carried by the existing delta format so local and received updates share the rule. In `CrdtSet`'s `SetValue` path, handle that priority before its existing equal-priority replacement branch: preserve exact bytes as replay, return a concrete conflict for different bytes, and never call the backing-store overwrite. This is the D-04 safety strategy: it is not a pre-read CAS and it deliberately preserves an existing immutable record even under a conflicting replicated update. Do not alter non-immutable callers, add a general transaction system, or introduce a dependency.</action>
  <verify><automated>cmake --build build/OSX/Release --target crdt_datastore_test consensus_pending_lifecycle_test consensus_slot_key_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R 'crdt_datastore_test|consensus_(slot_key|pending_lifecycle)_test' --output-on-failure &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>All Task 1 cases pass; the new API is the only path that requests immutable semantics, and a different immutable payload never invokes the existing backing-store replacement operation.</acceptance_criteria>
  <done>An explicit replicated immutable-write primitive is available for certificate authority, preserves first stored bytes, and surfaces conflicting content as an error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| consensus publisher → CRDT delta | Certificate bytes become replicated persistent state. |
| remote CRDT delta → local CrdtSet | Untrusted peer-propagated bytes enter local merge behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-04 | Tampering | GlobalDB/CrdtSet immutable write | mitigate | Reserved immutable delta priority preserves existing bytes, treats exact replay as idempotent, and rejects different bytes before backing-store replacement. |
| T-10-09 | Denial of service | malformed/conflicting immutable delta | mitigate | Return an error and retain original state; publication callers fail closed instead of retrying a conflicting write. |
| T-10-SC | Tampering | package installs | mitigate | No package installs; use existing C++17, CMake, GoogleTest, RocksDB, and CRDT facilities only. |
</threat_model>

<verification>
Build the CRDT target plus the focused Phase 10 lifecycle/slot targets, run the listed CTest regex, and run `git diff --check`.
</verification>

<success_criteria>
- A caller has an explicit immutable write result instead of read-then-write assumptions.
- Storage never overwrites an immutable value with different bytes through either local or replicated application.
- Existing ordinary CRDT `Put` semantics remain outside this special-purpose path.
</success_criteria>

<output>
Create `.planning/phases/10-authoritative-slot-certificate-publication/10-01-SUMMARY.md` when done.
</output>
