---
phase: 10-authoritative-slot-certificate-publication
plan: "02"
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - /Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp
  - /Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp
  - src/blockchain/Consensus.hpp
  - src/blockchain/Consensus.cpp
  - src/account/AccountMessenger.cpp
  - test/src/blockchain/consensus_pending_lifecycle_test.cpp
  - test/src/pubsub_counts/pubsub_counts.cpp
autonomous: true
requirements: [CERT-01, CERT-02, CERT-03, CERT-04]
nyquist_compliant: true
must_haves:
  truths:
    - "Only a current-round selected aggregator can durably create `/cert/<canonical-slot>`; a PubSub receiver and a non-selected validator never write it."
    - "A valid selected publisher persists the slot record before one best-effort full-certificate PubSub announcement."
    - "An identical certificate replay is harmless; concurrent valid encodings converge to the lowest SHA-256 lowercase-hex certificate bytes, while malformed or unreadable occupied values fail closed; an eligible later round can recover only with the same validated evidence after a determinate empty-slot result."
    - "A certificate notification failure returned by the production GossipPubSub transport is logged after durable persistence and is never retried."
    - "Publisher completion still waits for the existing callback plus committed recovery path."
  artifacts:
    - path: src/blockchain/Consensus.cpp
      provides: Authoritative slot-keyed publication, normal-round authority gate, hash-convergent collision handling, production publish-error logging, and slot-keyed ingress/recovery.
    - path: /Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp
      provides: Result-returning GossipPubSub publish contract that reports an attempted transport send failure rather than only queueing work.
    - path: src/account/AccountMessenger.cpp
      provides: Direct caller propagation of the result-returning PubSub contract.
    - path: test/src/blockchain/consensus_pending_lifecycle_test.cpp
      provides: Deterministic selected/non-selected/failover/order/collision/callback and actual publish-failure regressions.
    - path: test/src/pubsub_counts/pubsub_counts.cpp
      provides: Production PubSub result-contract regression for a real failed send state.
  key_links:
    - from: src/blockchain/Consensus.cpp
      to: src/crdt/globaldb/globaldb.cpp
      via: SubmitCertificate uses convergent immutable `/cert/<slot>` write before PubSub.
      pattern: "PutConvergentImmutable.*CERTIFICATE_BASE_PATH_KEY"
    - from: src/blockchain/Consensus.cpp
      to: /Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp
      via: ConsensusManager::Publish propagates the completed production transport result to SubmitCertificate.
      pattern: "pubsub_->Publish.*has_(error|failure)"
    - from: src/blockchain/Consensus.cpp
      to: src/blockchain/Consensus.cpp
      via: ProcessCertificates gates SubmitCertificate with GetAggregatorRole and committed recovery owns completion.
      pattern: "GetAggregatorRole.*SubmitCertificate|RecoverPendingCertificateWork"
---

<objective>
Move certificate publication and CRDT ingress to the authoritative `/cert/<canonical-slot>` record while preserving deterministic round rotation, durable-before-advertisement ordering, and post-commit recovery.

Purpose: make one protocol-selected publisher publish convergent generic slot authority and log an actual production notification failure without introducing a local-delivery shortcut, lease, or retry protocol.
Output: authoritative consensus publication/ingress behavior with deterministic lifecycle and production PubSub-result coverage.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-canonical-slot-certificate-binding/08-CONTEXT.md
@.planning/phases/09-durable-one-vote-finality/09-CONTEXT.md
@.planning/phases/09-durable-one-vote-finality/09-02-SUMMARY.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-CONTEXT.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-RESEARCH.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-VALIDATION.md
@.planning/phases/10-authoritative-slot-certificate-publication/10-PATTERNS.md

<interfaces>
From `Consensus.hpp`: `GetAggregatorRole(const Proposal &, const ValidatorRegistry::Registry &)`, `SubmitCertificate(const Certificate &)`, `FilterCertificate(const crdt::pb::Element &)`, `CertificateReceived(...)`, `RecoverPendingCertificateWork()`, and `GetExpectedCertificateSlotKey(const Certificate &)`. `Publish(const ConsensusMessage &)` returns `outcome::result<void>` and must propagate the production GossipPubSub result.
From `/Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp`: change `GossipPubSub::Publish(topic, payload)` from void to the smallest result/error contract whose result completes only after the scheduled send attempt can report success or failure.
From Plan 10-01: the named GlobalDB convergent immutable-write API makes local and received deltas select the same lowest `sha2_256` lowercase-hex certificate bytes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic authority, convergence, and production publish-result regressions</name>
  <files>test/src/blockchain/consensus_pending_lifecycle_test.cpp, test/src/pubsub_counts/pubsub_counts.cpp</files>
  <read_first>src/blockchain/Consensus.hpp; src/blockchain/Consensus.cpp; src/account/AccountMessenger.cpp; test/src/blockchain/consensus_pending_lifecycle_test.cpp; test/src/pubsub_counts/pubsub_counts.cpp; /Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp; .planning/phases/10-authoritative-slot-certificate-publication/10-VALIDATION.md</read_first>
  <behavior>
    - Current aggregator writes `/cert/<slot>` before exactly one full-certificate notification; a write error produces no notification.
    - A non-selected active validator and `HandleCertificate`/PubSub receipt cause no authoritative write or advertisement.
    - Empty slot accepts the selected certificate; identical replay is idempotent; a detected different valid encoding follows the Plan 10-01 lowest SHA-256 lowercase-hex winner, while malformed and unreadable occupied records fail closed without announcement.
    - A later normal-round successor uses the existing rotation and same exact validated certificate only after a determinate empty-slot check; indeterminate occupancy retains evidence for later rounds.
    - A real failed `GossipPubSub::Publish` result from a non-running or otherwise failed production transport state is propagated through `ConsensusManager::Publish`, captured as a `ConsensusManagerLogger()` error after one durable certificate write, and triggers no retry while the record remains available for recovery.
    - The publisher reaches completion only after callback plus committed readback recovery, matching a receiving node.
  </behavior>
  <action>Extend `ConsensusPendingLifecycleTestAccess` only with helpers to force round/aggregator state, seed/read canonical records, observe convergent-write versus announcement order, bind the fixture to a real GossipPubSub instance in a stopped/not-running state, and capture `ConsensusManagerLogger()` error output. Do not add a test-only publish failure seam: the certificate case must observe the actual result returned by `GossipPubSub::Publish` and assert its error log after the durable record is present. In `pubsub_counts`, add a direct transport contract regression that proves `Publish(topic, payload)` reports that real failed state rather than merely accepting work into a void queue. Use the lifecycle fixture's `MemorySecureStorage` before account construction and direct process/recovery seams rather than sleeps. Add actual signed-certificate tests for every authority behavior above, including legacy `/cert/<subject-hash>` rejection at CRDT filter ingress; assert that a lower-hash detected record is the only advertised authority. The PubSub case observes one failed attempt and durable recovery fallback only, never a retry loop.</action>
  <verify><automated>cmake --build build/OSX/Release --target consensus_pending_lifecycle_test consensus_slot_key_test pubsub_counts_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R 'consensus_(slot_key|pending_lifecycle)_test|pubsub_counts_test' --output-on-failure</automated></verify>
  <acceptance_criteria>Tests are deterministic, use real fixture storage/signatures and the production transport's non-running failure state, and initially fail against subject-hash publication, publish-before-write, receiver writing, callback-time completion, hash-order divergence, and a void/queue-only PubSub outcome.</acceptance_criteria>
  <done>The lifecycle and transport targets contain the Phase 10 authority, convergence, ordering, actual best-effort notification-failure, and recovery regressions required by CERT-01 through CERT-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement selected-publisher immutable publication and slot-keyed ingress/recovery</name>
  <files>/Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp, /Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp, src/blockchain/Consensus.hpp, src/blockchain/Consensus.cpp, src/account/AccountMessenger.cpp</files>
  <read_first>test/src/blockchain/consensus_pending_lifecycle_test.cpp; test/src/pubsub_counts/pubsub_counts.cpp; /Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.hpp; /Users/henriqueklein/gnus/thirdparty/ipfs-pubsub/src/ipfs_pubsub/gossip_pubsub.cpp; src/blockchain/Consensus.hpp; src/blockchain/Consensus.cpp; src/account/AccountMessenger.cpp; src/crdt/globaldb/globaldb.hpp; .planning/phases/10-authoritative-slot-certificate-publication/10-CONTEXT.md</read_first>
  <behavior>
    - `ProcessCertificates` preserves the existing proposal-derived `GetAggregatorRole` rotation; only `CurrentAggregator` calls the authoritative write path, while `ActiveButNotAggregator` retains quorum evidence without writing or advertising.
    - `SubmitCertificate` validates and serializes first, derives only `GetExpectedCertificateSlotKey(certificate)`, makes a determinate occupied-slot decision, persists bytes through the convergent immutable path, then sends the selected full certificate once on PubSub.
    - Same bytes are idempotent; different valid bytes select the lowest SHA-256 lowercase-hex certificate value, while malformed, unreadable, or unavailable occupancy returns failure/fails closed; a successful durable Put requires no readback-before-advertise.
    - `FilterCertificate`, accepted-slot fences, callback journal work, and committed recovery validate the `/cert/<slot>` key and retain Phase 9's post-commit completion boundary; publishing does not call a special completion shortcut or directly clear finality work.
    - `GossipPubSub::Publish` returns a production result that resolves after the scheduled transport attempt, `ConsensusManager::Publish` propagates it, and `SubmitCertificate` logs and returns an actual notification failure after durable persistence without retrying; `AccountMessenger` updates its direct call site to handle the same result contract.
  </behavior>
  <action>Replace the legacy certificate authority predicates in consensus publication, filter, accepted-slot scan, callback/recovery validation, and internal already-certified checks with one authoritative slot-key validator built on `GetExpectedCertificateSlotKey`. Implement D-01/D-02/D-03 by retaining the existing sorted-validator/current-round rotation and keeping `HandleCertificate` validation-only; it must never derive a write key or call persistence. Implement D-04 with Plan 10-01's convergent immutable write and explicit existing-value hash comparison: serialize a valid certificate, calculate `crypto::sha2_256` rendered by `base::hex_lower`, retain/advertise only the lower valid value when an occupied record is known, and rely on the shared merge rule for concurrent isolated writes. Never call a local pre-read a distributed CAS. Implement D-05 by completing the convergent immutable Put before constructing/sending the selected full `ConsensusMessage`; no readback is required before the send. Implement D-06/D-08 by changing production `GossipPubSub::Publish(topic, payload)` in the ipfs-pubsub source to return `outcome::result<void>` after the scheduled send attempt completes: reject a non-running service with a concrete `GossipPubSubError`, execute inline on the pubsub thread or post a completion promise and return its send/error result off-thread, and map the underlying attempt's exception/failure to that result. Propagate the result through `ConsensusManager::Publish`, log and return an actual failed certificate notification after persistence, and add no retry. Update the direct `AccountMessenger::SendAccountMessage` caller to propagate/log this result rather than discard it. Do not replace this with an injected ConsensusManager-only seam, a void handoff, a test double, or a change to message schemas/CRDT finality. Implement D-07 by leaving `CertificateReceived` as stalled journal notification and letting committed `RecoverPendingCertificateWork` drive callback, active-vote release, and proposal cleanup for the publisher and all peers. Implement D-09/D-10 by allowing later ordinary rounds to retry retained quorum evidence only after a determinate empty authoritative slot; unavailable/read-error state is not empty and must not clear or overwrite.</action>
  <verify><automated>cmake --build build/OSX/Release --target consensus_pending_lifecycle_test consensus_slot_key_test pubsub_counts_test --parallel 4 &amp;&amp; ctest --test-dir build/OSX/Release -R 'consensus_(slot_key|pending_lifecycle)_test|pubsub_counts_test' --output-on-failure &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>All Task 1 regressions pass; no normal consensus write uses `/cert/<subject-hash>`, no PubSub receiver writes, no notification precedes durable Put, concurrent valid bytes converge to one lowest-hash authority, and a real production notification failure is logged/returned exactly once without retry.</acceptance_criteria>
  <done>The generic slot record is the only consensus publication and ingress authority, with deterministic normal-round recovery, convergent collision resolution, actual production publish-failure logging, and no publisher-only completion behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| quorum state → publisher path | Local candidate evidence may produce a durable authority record only under protocol-selected authority. |
| PubSub message → consensus handler | Peer-delivered certificates are untrusted notifications, not write authority. |
| CRDT callback → recovery | Callback is pre-commit and cannot prove durable acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-01 | Spoofing | ProcessCertificates publisher selection | mitigate | D-01/D-02: gate the write path on existing `CurrentAggregator`; do not use delivery source or receiver state. |
| T-10-02 | Elevation of privilege | HandleCertificate/PubSub receipt | mitigate | Keep receipt keyless and validation-only; tests assert it never calls immutable Put. |
| T-10-03 | Tampering | SubmitCertificate ordering | mitigate | Validate, hash-select any known occupied value, and durable convergent immutable Put before a single full-certificate notification. |
| T-10-04 | Tampering | occupied `/cert/<slot>` | mitigate | D-04 uses Plan 10-01 SHA-256 lowercase-hex convergence for valid concurrent bytes; malformed/unreadable occupancy fails closed. |
| T-10-06 | Repudiation | best-effort notification | mitigate | D-06/D-08 use the production result-returning GossipPubSub contract; log a completed send failure and assert one attempt with no retry. |
| T-10-07 | Tampering | pre-commit CRDT callback | mitigate | Callback only journals stalled work; committed readback/recovery is the sole completion path. |
| T-10-SC | Tampering | package installs | mitigate | No package installs; use existing C++17/CMake/GoogleTest/RocksDB/CRDT/PubSub facilities. |
</threat_model>

<verification>
Run the Validation Strategy's focused build and CTest commands after each task, then inspect the diff for whitespace errors.
</verification>

<success_criteria>
- `/cert/<canonical-slot>` is the sole consensus certificate authority.
- Only a selected normal-round publisher writes it, it persists before best-effort advertising, and actual transport failure is logged without retry.
- All collision, receiver, failover, and callback boundary cases are deterministic lifecycle tests, including two-replica lowest-hash convergence from Plan 10-01.
</success_criteria>

<output>
Create `.planning/phases/10-authoritative-slot-certificate-publication/10-02-SUMMARY.md` when done.
</output>
