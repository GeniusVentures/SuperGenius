---
phase: 04.1-anvil-local-bridge-e2e-test-use-tokencontracts-gnus-ai-env-t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/src/bridge_e2e/anvil_fixture.hpp
  - test/src/bridge_e2e/bridge_anvil_e2e_test.cpp
  - test/src/bridge_e2e/CMakeLists.txt
autonomous: true
requirements: []
user_setup:
  - service: foundry
    why: "anvil + cast binaries drive the local fork and burn transaction"
    env_vars: []
    dashboard_config:
      - task: "Install Foundry (anvil + cast)"
        location: "https://book.getfoundry.sh/getting-started/installation — curl -L https://foundry.paradigm.xyz | bash"
  - service: sepolia-rpc
    why: "Anvil --fork-url needs a Sepolia RPC endpoint to fork live state"
    env_vars:
      - name: RPC_SEPOLIA
        source: "Optional override of chains_config.json Sepolia URL (D-03)"
    dashboard_config: []

must_haves:
  truths:
    - "Test fixture starts a local Anvil subprocess forking Sepolia with no .env file dependency"
    - "Anvil default account #0 (0xf39Fd6...b92266) already holds 1 GNUS + 1 ERC-1155 token ID 0 on Sepolia — the fork brings the balance, no funding needed"
    - "A bridge burn transaction is sent to the Anvil fork via cast send using the Anvil default private key"
    - "The burn tx hash is parsed from cast output and fed to GeniusNode::MintTokens"
    - "Minted UTXO appears in the recipient's balance on node_main (D-17a)"
    - "Replay of the same burn tx hash is rejected by the dedup cache (D-18)"
    - "When anvil binary is missing, the Anvil path skips via GTEST_SKIP with a clear install message"
    - "When PRIVATE_KEY is set but anvil is missing, the Sepolia fallback path runs (D-14 Path B)"
  artifacts:
    - path: "test/src/bridge_e2e/anvil_fixture.hpp"
      provides: "Reusable AnvilProcess helper (subprocess lifecycle, readiness poll, port) + GNUS funder + Anvil default key #0 constants"
      min_lines: 120
    - path: "test/src/bridge_e2e/bridge_anvil_e2e_test.cpp"
      provides: "BridgeAnvilE2ETest fixture + positive Anvil burn-to-mint test + replay rejection test + Sepolia fallback test"
      min_lines: 250
    - path: "test/src/bridge_e2e/CMakeLists.txt"
      provides: "CMake target bridge_anvil_e2e_test linked to genius_node_test + json_secure_storage"
      contains: "addtest(bridge_anvil_e2e_test"
  key_links:
    - from: "test/src/bridge_e2e/bridge_anvil_e2e_test.cpp"
      to: "GeniusNode::ConfigureRpcEndpoint"
      via: "chains_config.json Sepolia URLs overridden to point at Anvil's localhost port (D-11)"
      pattern: "ConfigureRpcEndpoint"
    - from: "test/src/bridge_e2e/bridge_anvil_e2e_test.cpp"
      to: "GeniusNode::MintTokens"
      via: "burn tx hash parsed from cast send --json output"
      pattern: "MintTokens"
    - from: "test/src/bridge_e2e/anvil_fixture.hpp"
      to: "anvil subprocess"
      via: "fork/exec + popen readiness check on eth_blockNumber (D-04)"
      pattern: "anvil --fork-url"
---

<objective>
Build a self-contained local-Anvil E2E test that exercises the full burn-to-mint bridge pipeline without depending on `../TokenContracts/gnus-ai/.env` or external private keys. The test forks Sepolia state into a local Anvil instance, funds Anvil's default account #0 with GNUS tokens via `anvil_impersonateAccount`, sends a bridge burn transaction with `cast send`, and verifies the burn flows through the existing BridgeRelayer/MintFunds pipeline into a minted UTXO. A Sepolia-direct fallback test covers the case where evmrelay cannot consume Anvil's WebSocket events.

Purpose: Replace the slow, account-funded Phase 4 Sepolia test with a fast, deterministic, locally-reproducible test (D-01..D-07). Preserve the original Sepolia path as a fallback (D-14 Path B).
Output: `test/src/bridge_e2e/anvil_fixture.hpp` (reusable Anvil helper), `test/src/bridge_e2e/bridge_anvil_e2e_test.cpp` (test cases), updated `test/src/bridge_e2e/CMakeLists.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-anvil-local-bridge-e2e-test-use-tokencontracts-gnus-ai-env-t/04.1-CONTEXT.md

@test/src/bridge_e2e/bridge_e2e_test.cpp
@test/src/bridge_e2e/CMakeLists.txt
@test/testutil/wait_condition.hpp

<interfaces>
Key contracts and signatures the executor needs — extracted from codebase.
Use these directly — no codebase exploration needed.

From src/account/GeniusNode.hpp:

    static std::shared_ptr<GeniusNode> New( const DevConfig &dev_config,
                                            const char *eth_private_key,
                                            bool is_processor,
                                            bool is_dev,
                                            uint16_t port,
                                            bool is_full_node = false );

    outcome::result<std::pair<std::string, uint64_t>> MintTokens(
        uint64_t amount,
        const std::string &transaction_hash,
        const std::string &chainid,        // string, NOT numeric — use kSepoliaChainId "11155111"
        TokenID tokenid,
        std::string destination,
        std::chrono::milliseconds timeout );

    void ConfigureRpcEndpoint( const std::string &chain_id,
                               std::vector<WeightedRpcEndpoint> endpoints );

    std::string GetAddress() const;
    uint64_t    GetBalance( const std::string &address ) const;
    enum class NodeState { ..., READY };
    NodeState GetState() const;

From src/account/GeniusNode.cpp (WeightedRpcEndpoint, used by Phase 4 line 348-362):

    struct WeightedRpcEndpoint {
        std::string url;
        unsigned int consensus_weight;            // 25 per endpoint, >=75 needed (3x25)
        std::string bridge_contract_address;      // lowercase hex with 0x prefix
        std::string event_topic0;                 // 0x-prefixed 32-byte hex
    };

From test/src/bridge_e2e/bridge_e2e_test.cpp (Phase 4 reference constants):

    constexpr const char *kSepoliaContract     = "0x9af8050220D8C355CA3c6dC00a78B474cd3e3c70";
    constexpr const char *kBridgeContractLower = "0x9af8050220d8c355ca3c6dc00a78b474cd3e3c70";
    constexpr const char *kEventTopic0         = "0xc3d58168c5ae7397731d063d5bbf3d657854427343f4c083240f7aacaa2d0f62";
    constexpr const char *kSepoliaRpc          = "https://ethereum-sepolia-rpc.publicnode.com";
    constexpr const char *kTransferSig         = "safeTransferFrom(address,address,uint256,uint256,bytes)";

From test/testutil/wait_condition.hpp:

    ASSERT_WAIT_FOR_CONDITION(condition, timeout_ms, description, actual_duration_ptr)
    EXPECT_WAIT_FOR_CONDITION(condition, timeout_ms, description, actual_duration_ptr)
    // NO std::this_thread::sleep_for in tests (per CLAUDE.md).
    // assertWaitForCondition polls every 10ms by default.

DevConfig brace-init order (Phase 4 line 201):

    DevConfig { chain_id_string, fee_rate, version_str, TokenID, BaseWritePath }
    // Phase 4 used: { "0xcafe", "0.65", "1.0", TokenID::FromBytes({0x00}), "./nodeN" }

Anvil constants (D-05, D-07) — public test values, safe to hardcode:
- Mnemonic: test test test test test test test test test test test junk
- Account #0 address: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
- Account #0 private key: 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnvilProcess helper header (anvil_fixture.hpp)</name>
  <files>test/src/bridge_e2e/anvil_fixture.hpp</files>
  <read_first>
    - test/src/bridge_e2e/bridge_e2e_test.cpp (FULL — Phase 4 fixture pattern, OpenCommandPipe/CloseCommandPipe helpers, cast binary check, Base64Decode/BytesToHex utilities, static member init style, Allman bracing, 4-space indent, 120-char lines)
    - test/testutil/wait_condition.hpp (assertWaitForCondition signature, ASSERT_WAIT_FOR_CONDITION macro, polling cadence)
    - src/account/GeniusNode.hpp (lines 270-293 — MintTokens signature, chainid is std::string; lines 560-570 — ConfigureRpcEndpoint signature)
    - .planning/phases/04.1-anvil-local-bridge-e2e-test-use-tokencontracts-gnus-ai-env-t/04.1-CONTEXT.md (decisions D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-15, D-16)
    - ./CLAUDE.md (no OS ifdef guards in source — but OpenCommandPipe in bridge_e2e_test.cpp uses _WIN32 inside a .cpp test file, which is the established test-side pattern; reuse that same popen/_popen wrapper rather than inventing a new abstraction)
  </read_first>
  <action>
    Create a header-only helper test/src/bridge_e2e/anvil_fixture.hpp (no .cpp) declaring an AnvilProcess class plus Anvil-default-key constants. The class manages Anvil subprocess lifecycle (D-01) and exposes JSON-RPC readiness polling (D-04). No OS preprocessor guards in the new header — reuse the OpenCommandPipe/CloseCommandPipe pattern by copy-pasting those two static inline helpers verbatim from bridge_e2e_test.cpp (they are file-local in an anonymous namespace; copy them as static inline into the header so both files can use them without ODR violations). All logic goes in namespace sgns::test::anvil.

    Public constants (D-05, D-07) — define as inline constexpr per CLAUDE.md kCamelCase rule:
    - kAnvilMnemonic = "test test test test test test test test test test test junk"
    - kAnvilAccount0Address = "0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266"
    - kAnvilAccount0PrivateKey = "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
    - kAnvilStartPort = 18545u (high port to avoid conflict with any default 8545 Anvil — D-15)
    - kAnvilReadyTimeoutMs = 15000u (readiness poll budget)
    - kSepoliaChainId = "11155111" (string, matches MintTokens chainid param type)
    - kSepoliaBridgeContractLower = "0x9af8050220d8c355ca3c6dc00a78b474cd3e3c70" (D-02)
    - kBridgeEventTopic0 = "0xc3d58168c5ae7397731d063d5bbf3d657854427343f4c083240f7aacaa2d0f62"

    class AnvilProcess members (D-01, D-04, D-15, D-16):
    - Private: pid_t anvil_pid_ (initialize to -1), unsigned int port_ (0), std::string rpc_url_, bool started_ (false).
    - Public methods:
      - bool Start( const std::string &fork_url, unsigned int preferred_port = kAnvilStartPort ) — forks Anvil via fork()+execlp("anvil", "anvil", "--fork-url", fork_url.c_str(), "--port", port_str.c_str(), "--mnemonic", kAnvilMnemonic, (char*)nullptr). Before exec, redirect stdin/stdout/stderr to /dev/null in the child. On execlp failure, _exit(127). In parent, store anvil_pid_ and port_. Build rpc_url_ = "http://127.0.0.1:" + port_str. Return false if fork fails or execlp fails. NOTE: if preferred_port == 0, bind a temporary socket to port 0 to discover a free port and pass that to anvil (D-15 discretion — simplest: use fixed kAnvilStartPort and let Start fail loudly if busy; the test calls GTEST_SKIP on Start failure).
      - bool WaitForReady( std::chrono::milliseconds timeout = std::chrono::milliseconds(kAnvilReadyTimeoutMs) ) — polls eth_blockNumber by shelling out to `cast block-number --rpc-url <rpc_url_>` (reuse OpenCommandPipe). Uses the project's `waitForCondition` polling primitive from `src/base/util.hpp` (the same one `wait_condition.hpp` macros wrap) with a 100ms check interval. Returns true when cast returns a non-empty block number. Zero `sleep_for` — pure poll loop via `waitForCondition`.
      - void Stop() — if anvil_pid_ > 0, send kill(anvil_pid_, SIGTERM), then waitpid(anvil_pid_, nullptr, 0). Set anvil_pid_ = -1, started_ = false.
      - const std::string &RpcUrl() const, unsigned int Port() const, bool IsStarted() const.
      - Destructor calls Stop() if still started (RAII safety net).

    Free helper functions in the same namespace:
    - inline bool CastAvailable() — shells out to `which cast 2>/dev/null` (or `where cast` on Windows via the copied OpenCommandPipe pattern), returns true if output is non-empty.
    - inline bool AnvilAvailable() — same for `which anvil`.
    - inline std::string SepoliaForkUrl() — returns std::getenv("RPC_SEPOLIA") if set, else falls back to kSepoliaRpcPublicnode constant "https://ethereum-sepolia-rpc.publicnode.com" (D-03 — same source as Phase 4 / production).
    - inline bool FundAccount0WithGnus( const std::string &anvil_rpc_url ) (D-08, D-09) — uses anvil_impersonateAccount to transfer GNUS from a known Sepolia holder to account #0 on the fork:
      1. constexpr kGnusHolderSepolia = "0x910bAa33DeB0D614Aa9d80e38b7f0BF87549c2fC" — controlled Sepolia address pre-funded with GNUS by the developer (mint before running the test).
      2. Shell out: `cast rpc anvil_impersonateAccount <kGnusHolderSepolia> --rpc-url <anvil_rpc_url>` (return false on non-zero exit).
      3. Shell out: `cast send <kSepoliaBridgeContractLower> "transfer(address,uint256)" <kAnvilAccount0Address> 1000000000000000000 --from <kGnusHolderSepolia> --rpc-url <anvil_rpc_url> --json`. GNUS is dual-standard ERC-20 + ERC-1155; transfer(address,uint256) is the correct ERC-20 signature. Send 1 GNUS (1e18 wei, decimals=18). Parse exit code; return false on failure.
      4. Shell out: `cast rpc anvil_stopImpersonatingAccount <kGnusHolderSepolia> --rpc-url <anvil_rpc_url>`.
      5. Verify: `cast call <kSepoliaBridgeContractLower> "balanceOf(address)(uint256)" <kAnvilAccount0Address> --rpc-url <anvil_rpc_url>` returns nonzero. Return true iff verified.
    - inline std::string ParseTxHashFromCastJson( const std::string &cast_output ) — extracts the value of the transactionHash field from cast --json output. Reuse the exact pattern from bridge_e2e_test.cpp lines 444-456 (kTxHashPattern lookup against the literal string transactionHash colon quote 0x).

    Every public class member and free function MUST have a Doxygen @brief/@param/@return header per CLAUDE.md. Allman bracing, 4-space indent, 120-char max line length. Initialize every member variable. No designated initializers (C++17 only). Use constexpr/inline constexpr for all constants, named kCamelCase. No magic numbers — the only numeric literals allowed are 0, -1, 1, 127 (exec failure), and signal numbers (SIGTERM) in well-commented contexts.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ninja bridge_anvil_e2e_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File test/src/bridge_e2e/anvil_fixture.hpp exists and contains namespace sgns::test::anvil.
    - grep -E "inline constexpr.*kAnvil(Account0Address|Account0PrivateKey|Mnemonic|StartPort)" test/src/bridge_e2e/anvil_fixture.hpp returns 4 matches with the exact hex values from D-05/D-07.
    - grep -c "class AnvilProcess" test/src/bridge_e2e/anvil_fixture.hpp returns exactly 1.
    - grep -E "bool (Start|WaitForReady|Stop|IsStarted)\(" test/src/bridge_e2e/anvil_fixture.hpp returns matches for all four method names.
    - grep -E "(FundAccount0WithGnus|CastAvailable|AnvilAvailable|SepoliaForkUrl|ParseTxHashFromCastJson)" test/src/bridge_e2e/anvil_fixture.hpp returns 5 matches.
    - grep -c "anvil_impersonateAccount" test/src/bridge_e2e/anvil_fixture.hpp returns >= 1 (D-09 funding mechanism).
    - grep -c "0x910bAa33DeB0D614Aa9d80e38b7f0BF87549c2fC" test/src/bridge_e2e/anvil_fixture.hpp returns 1 (hardcoded GNUS holder).
    - grep -c "@brief" test/src/bridge_e2e/anvil_fixture.hpp returns >= 8 (every public symbol documented).
    - Header compiles cleanly when included from bridge_anvil_e2e_test.cpp (Task 2 verify exercises this).
    - NO #ifdef __APPLE__ / #ifdef __linux__ / #ifdef _WIN32 guards added beyond the reused OpenCommandPipe/CloseCommandPipe (which mirror the established Phase 4 test-side pattern).
  </acceptance_criteria>
  <done>Header-only AnvilProcess helper committed. Anvil lifecycle (D-01..D-04), default key constants (D-05..D-07), balance verification (D-08/D-09), port selection (D-15), cast/anvil availability checks (D-16). No .env dependency, no impersonation. Compiles cleanly when included by the test file in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Implement BridgeAnvilE2ETest fixture + positive Anvil burn-to-mint test</name>
  <files>test/src/bridge_e2e/bridge_anvil_e2e_test.cpp</files>
  <read_first>
    - test/src/bridge_e2e/anvil_fixture.hpp (Task 1 output — the helper API to consume)
    - test/src/bridge_e2e/bridge_e2e_test.cpp (FULL — Phase 4 3-node bootstrap pattern: GeniusNode::New order, SetAuthorizedFullNodeAddress, AddPeers, ConfigureRpcEndpoint with WeightedRpcEndpoint list, MintTokens call, EXPECT_WAIT_FOR_CONDITION on GetBalance, ReplayRejection test structure)
    - src/account/GeniusNode.hpp (lines 270-293, 560-570 — MintTokens + ConfigureRpcEndpoint signatures)
    - test/testutil/wait_condition.hpp (ASSERT_WAIT_FOR_CONDITION, EXPECT_WAIT_FOR_CONDITION macros)
    - test/testutil/outcome.hpp (EXPECT_OUTCOME_TRUE, ASSERT_OUTCOME_SUCCESS)
    - .planning/phases/04.1-anvil-local-bridge-e2e-test-use-tokencontracts-gnus-ai-env-t/04.1-CONTEXT.md (D-04, D-07, D-08, D-10, D-11, D-12, D-13, D-14, D-16, D-17)
  </read_first>
  <behavior>
    - Test BridgeAnvilE2ETest.AnvilBurnToMintPipeline: when anvil is installed and Sepolia RPC is reachable, sending a cast burn to the local Anvil fork using Anvil default key #0 produces a MintTransactionV2 and the recipient's GetBalance increases by >= kMintAmount within the wait-condition timeout (D-17a).
    - Test BridgeAnvilE2ETest.AnvilBurnToMintPipeline SKIPS with a clear Foundry install URL when AnvilAvailable() returns false (D-16).
    - Test BridgeAnvilE2ETest.AnvilBurnToMintPipeline SKIPS when FundAccount0WithGnus returns false, logging that impersonation funding failed (D-09/D-10).
  </behavior>
  <action>
    Create test/src/bridge_e2e/bridge_anvil_e2e_test.cpp. Use the Phase 4 file as the structural template — same includes (gtest, spdlog, filesystem, memory, thread, boost/dll, the project account/blockchain headers, testutil headers, the new anvil_fixture.hpp), same Allman bracing / 4-space indent / 120-char lines / Doxygen headers, same static-member-init pattern.

    class BridgeAnvilE2ETest : public ::testing::Test:
    - Static members (same shape as Phase 4): node_main, node_proc1, node_proc2 (shared_ptr<GeniusNode>); gGeniusNodeConfig, gGeniusNodeConfig2, gGeniusNodeConfig3 (DevConfig); static sgns::test::anvil::AnvilProcess s_anvil; (RAII-managed Anvil instance).
    - Constants: reuse kSepoliaContract, kTransferSig from Phase 4 (copy as inline constexpr in this file — do NOT include Phase 4's test cpp). kMintAmount = 1u. kMintTimeout = 10000ms. kNodeReadyTimeout = 60000ms.
    - static void SetUpTestSuite():
      1. sgns::GeniusAccount::SetSecureStorageFactory(...) — same MemorySecureStorage lambda as Phase 4 lines 209-211.
      2. Guard: if ( !sgns::test::anvil::AnvilAvailable() || !sgns::test::anvil::CastAvailable() ) { GTEST_SKIP() << "Install Foundry (anvil + cast): https://book.getfoundry.sh/getting-started/installation"; } (D-16).
      3. Resolve fork URL: const std::string fork_url = sgns::test::anvil::SepoliaForkUrl(); (D-03).
      4. Start Anvil: ASSERT_TRUE( s_anvil.Start( fork_url ) ) << "Failed to start anvil subprocess";
      5. Poll readiness (D-04): ASSERT_TRUE( s_anvil.WaitForReady() ) << "Anvil did not become ready";
      6. Fund account #0 (D-08/D-09): if ( !sgns::test::anvil::FundAccount0WithGnus( s_anvil.RpcUrl() ) ) { s_anvil.Stop(); GTEST_SKIP() << "Could not fund Anvil account #0 via impersonation of " << kGnusHolderSepolia << " — skipping Anvil path; run Sepolia-direct test instead"; } (D-10 fallthrough).
      7. Set per-node BaseWritePath from boost::dll::program_location().parent_path() (Phase 4 lines 287-290 pattern) — use distinct subdirs like ./anvil_node1/, ./anvil_node2/, ./anvil_node3/ to avoid collision with Phase 4 test data.
      8. Create node_main as full node: node_main = GeniusNode::New( gGeniusNodeConfig, <kAnvilAccount0PrivateKey without 0x prefix>, false, false, 40011, true ); — strip the 0x prefix from the private key before passing (Phase 4 lines 232-237 strip logic; for the Anvil key the prefix is always present and the rest is 64 hex chars, so a simple substr(2) on the constexpr string is safe). NOTE: pass the Anvil default key directly — do NOT read any .env file (D-06).
      9. Set authorized address + wait for READY (Phase 4 lines 300-308 pattern, using ASSERT_WAIT_FOR_CONDITION).
      10. Create node_proc1 (port 40012), node_proc2 (port 40013), AddPeers bootstrap (Phase 4 lines 313-321 pattern), wait for READY.
      11. Configure RPC endpoints on node_main pointing at the ANVIL instance (D-11): build std::vector<sgns::WeightedRpcEndpoint> anvil_eps with ONE entry where url = s_anvil.RpcUrl(), consensus_weight = 100 (single local source, weight >= 75 alone), bridge_contract_address = kSepoliaBridgeContractLower, event_topic0 = kBridgeEventTopic0. Then node_main->ConfigureRpcEndpoint( kSepoliaChainId, anvil_eps ); (D-11 — point evmrelay at local Anvil instead of Sepolia peers).
    - static void TearDownTestSuite(): reset nodes, s_anvil.Stop(), std::filesystem::remove_all on the three anvil_node dirs.

    TEST_F( BridgeAnvilE2ETest, AnvilBurnToMintPipeline ) (D-14 Path A, D-17):
      1. const std::string sender_addr = sgns::test::anvil::kAnvilAccount0Address;
      2. const std::string dest_addr = node_main->GetAddress();
      3. const uint64_t initial_balance = node_main->GetBalance( dest_addr );
      4. Build cast send command targeting the LOCAL Anvil RPC: `cast send <kSepoliaContract> "<kTransferSig>" <sender_addr> <sender_addr> 0 <kMintAmount> 0x --private-key <kAnvilAccount0PrivateKey> --rpc-url <s_anvil.RpcUrl()> --json 2>&1` (Phase 4 lines 426-428 structure, but rpc-url is the local Anvil). Self-transfer burn = ERC-1155 safeTransferFrom from sender to sender with token id 0 (matches Phase 4 D-09 burn form).
      5. Run via OpenCommandPipe, capture output, assert exit code 0 (Phase 4 lines 431-442).
      6. const std::string tx_hash = sgns::test::anvil::ParseTxHashFromCastJson( cast_output ); ASSERT_FALSE( tx_hash.empty() ) << "Could not parse transactionHash";
      7. EXPECT_OUTCOME_TRUE( mint_result, node_main->MintTokens( kMintAmount, tx_hash, kSepoliaChainId, sgns::TokenID::FromBytes( { 0x00 } ), dest_addr, kMintTimeout ) ); (Phase 4 lines 462-468).
      8. EXPECT_WAIT_FOR_CONDITION( [&]() { return node_main->GetBalance( dest_addr ) > initial_balance; }, kMintTimeout, "Anvil burn minted UTXO appears in recipient balance", nullptr ); (D-17a — Phase 4 lines 473-476).
      9. Assert final_balance - initial_balance >= kMintAmount.

    Use spdlog::info for all diagnostic output (NEVER fprintf/cout/cerr per CLAUDE.md). Never use std::this_thread::sleep_for in the test body — only wait_condition.hpp macros. All numeric literals must be named constexpr/inline constexpr constants in kCamelCase.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ninja bridge_anvil_e2e_test && ./bridge_anvil_e2e_test --gtest_list_tests | grep -E "BridgeAnvilE2ETest" && ./bridge_anvil_e2e_test --gtest_filter=BridgeAnvilE2ETest.AnvilBurnToMintPipeline --gtest_brief=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "class BridgeAnvilE2ETest" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns exactly 1.
    - grep -c "AnvilBurnToMintPipeline" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 1.
    - grep -E "AnvilAvailable|CastAvailable" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 2 matches (D-16 guards).
    - grep -c "FundAccount0WithGnus" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 1 (D-08/D-09 funding invoked in SetUpTestSuite).
    - grep -c "s_anvil.RpcUrl()" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 2 (used in BOTH cast send and ConfigureRpcEndpoint — D-11 wiring).
    - grep -c "ConfigureRpcEndpoint" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 1.
    - grep -c "MintTokens" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 1.
    - grep -E "EXPECT_WAIT_FOR_CONDITION|ASSERT_WAIT_FOR_CONDITION" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 2 matches (node READY + balance polling).
    - grep -r -c "this_thread::sleep_for" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp test/src/bridge_e2e/anvil_fixture.hpp 2>/dev/null returns 0 in all files (CLAUDE.md mandate — no sleep_for anywhere in test code).
    - grep -v "^ *//" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp | grep -cE "fprintf|[^:]cout|cerr|[^_]printf" returns 0 (CLAUDE.md — spdlog only, excluding comments).
    - grep -c "GTEST_SKIP" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 1 (anvil/cast missing guard).
    - The Anvil-path private-key handling must NOT call getenv for the signing key (D-06 — NO .env dependency); the only getenv use permitted is inside SepoliaForkUrl for RPC_SEPOLIA override, which lives in the helper header.
    - ./build/OSX/Debug/bridge_anvil_e2e_test --gtest_list_tests lists BridgeAnvilE2ETest.AnvilBurnToMintPipeline (after Task 3 CMake wiring builds the target).
  </acceptance_criteria>
  <done>BridgeAnvilE2ETest fixture and AnvilBurnToMintPipeline test compile and the positive pipeline test reaches the balance-increase assertion without sleep_for calls, without .env lookup, and without OS preprocessor guards in test code paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add replay-rejection test, Sepolia-direct fallback test, and CMake target</name>
  <files>test/src/bridge_e2e/bridge_anvil_e2e_test.cpp, test/src/bridge_e2e/CMakeLists.txt</files>
  <read_first>
    - test/src/bridge_e2e/bridge_anvil_e2e_test.cpp (Task 2 output — append to this file)
    - test/src/bridge_e2e/CMakeLists.txt (FULL — existing addtest pattern for bridge_e2e_test and bridge_e2e_chainlist_test, including the WIN32/APPLE/else whole-archive link block that MUST be replicated for the new target)
    - test/src/bridge_e2e/bridge_e2e_test.cpp lines 500-626 (Phase 4 ReplayRejection — the exact structure to mirror for AnvilReplayRejection)
    - test/src/bridge_e2e/bridge_e2e_test.cpp lines 207-367 (Phase 4 SetUpTestSuite — Sepolia-direct pattern for the fallback test's cast send + MintTokens)
    - .planning/phases/04.1-anvil-local-bridge-e2e-test-use-tokencontracts-gnus-ai-env-t/04.1-CONTEXT.md (D-10, D-14, D-17, D-18)
  </read_first>
  <behavior>
    - Test BridgeAnvilE2ETest.AnvilReplayRejection: a second MintTokens call with the SAME Anvil burn tx hash is rejected (returns error OR the recipient balance does not increase), proving the Phase 3 dedup cache works against the Anvil path (D-18).
    - Test BridgeSepoliaDirectFallbackTest.BurnToMintPipeline: when PRIVATE_KEY env var is set (Phase 4 D-03 guard), a fresh 3-node fixture burns on live Sepolia and mints successfully (D-14 Path B).
    - Test BridgeSepoliaDirectFallbackTest.BurnToMintPipeline SKIPS when PRIVATE_KEY is absent, with a clear message.
    - CMake target bridge_anvil_e2e_test builds and links against genius_node_test + json_secure_storage with the same whole-archive link options as bridge_e2e_test.
  </behavior>
  <action>
    APPEND two additional test cases to test/src/bridge_e2e/bridge_anvil_e2e_test.cpp (do not modify the Task 2 fixture or positive test):

    1. TEST_F( BridgeAnvilE2ETest, AnvilReplayRejection ) (D-18 — mirror Phase 4 ReplayRejection lines 575-626 but with the Anvil burn trigger):
       - Send a cast burn to the local Anvil (same command shape as AnvilBurnToMintPipeline Step 4), parse tx_hash.
       - First MintTokens( kMintAmount, tx_hash, kSepoliaChainId, ..., kReplayTimeout ) → EXPECT_OUTCOME_TRUE.
       - EXPECT_WAIT_FOR_CONDITION( [&] { return node_main->GetBalance( dest_addr ) > balance_before; }, kReplayTimeout, "First Anvil mint balance increase", nullptr );
       - Second MintTokens with the SAME tx_hash → assert result.has_error() is true OR balance unchanged (Phase 4 lines 602-623 pattern). Define constexpr std::chrono::milliseconds kReplayTimeout{ 5000 };.
       - EXPECT_TRUE( replay_rejected ) << "Replayed Anvil burn tx hash must be rejected by dedup cache";

    2. NEW test class BridgeSepoliaDirectFallbackTest : public ::testing::Test (D-14 Path B). This is a self-contained test that does NOT depend on Anvil. It is the original Phase 4 approach copied into this file (with the Phase 4 cpp as the read_first template — DO NOT include bridge_e2e_test.cpp; copy the necessary structure).
       - Static members: 3 GeniusNode + 3 DevConfig (use distinct BaseWritePath subdirs ./sepolia_fb_node1/, etc.), static std::string s_private_key;.
       - SetUpTestSuite():
         a. MemorySecureStorage factory (same lambda).
         b. Guard 1: if ( !std::getenv( "RUN_E2E_BRIDGE" ) ) { GTEST_SKIP() << "Set RUN_E2E_BRIDGE=1 to run Sepolia-direct fallback"; } (Phase 4 D-03 gating — D-14 Path B requires this opt-in).
         c. Guard 2: const char *pk = std::getenv( "PRIVATE_KEY" ); if ( !pk ) { GTEST_SKIP() << "PRIVATE_KEY env var required for Sepolia-direct fallback (D-14 Path B)"; } (D-10 — this path REQUIRES the user-provided key because Anvil default keys do not exist on real Sepolia).
         d. Strip/validate the key (Phase 4 lines 231-268 Base64Decode fallback logic — copy verbatim into a file-local static helper NormalizePrivateKey() to avoid symbol collision with Phase 4's anonymous-namespace helpers).
         e. Guard 3: CastAvailable() check (GTEST_SKIP on missing).
         f. 3-node bootstrap (Phase 4 lines 286-340 pattern, ports 40021/40022/40023).
         g. ConfigureRpcEndpoint with the LIVE Sepolia endpoint list (Phase 4 lines 344-366 — six public Sepolia RPC URLs, weight 25 each).
       - TearDownTestSuite(): reset nodes, remove the three sepolia_fb_node dirs.
       - TEST_F( BridgeSepoliaDirectFallbackTest, BurnToMintPipeline ): exact Phase 4 BurnToMintPipeline structure (lines 392-489) — derive sender from PRIVATE_KEY via `cast wallet address`, send cast burn to LIVE kSepoliaRpc, parse tx_hash, MintTokens, EXPECT_WAIT_FOR_CONDITION on balance increase.

    THEN update test/src/bridge_e2e/CMakeLists.txt: append a third addtest block mirroring the existing bridge_e2e_test block (lines 1-24) but with target name bridge_anvil_e2e_test and source bridge_anvil_e2e_test.cpp. Include the SAME three-platform link-options block (WIN32 /WHOLEARCHIVE, APPLE -force_load, else --whole-archive/--no-whole-archive) — copy the existing block and substitute the target name. Link genius_node_test and json_secure_storage. Add target_include_directories(bridge_anvil_e2e_test PRIVATE ${AsyncIOManager_INCLUDE_DIR}) to match the bridge_e2e_test line.

    All code follows CLAUDE.md: Allman bracing, 4-space indent, 120-char lines, Doxygen headers on every TEST_F and the new test class, spdlog only (no fprintf/cout/cerr), no std::this_thread::sleep_for, no designated initializers, no OS ifdef guards beyond the reused OpenCommandPipe pattern, all numeric literals as constexpr/inline constexpr kCamelCase.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON 2>&1 | tail -5 && ninja bridge_anvil_e2e_test 2>&1 | tail -10 && ./bridge_anvil_e2e_test --gtest_list_tests 2>&1 | grep -E "Anvil(BurnToMintPipeline|ReplayRejection)|SepoliaDirectFallbackTest"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "AnvilReplayRejection" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 1.
    - grep -c "BridgeSepoliaDirectFallbackTest" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 2 (class decl + TEST_F).
    - grep -E "RUN_E2E_BRIDGE|PRIVATE_KEY" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns matches for both env var names in the Sepolia-direct SetUpTestSuite guards (D-14 Path B gating).
    - grep -c "has_error" test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns >= 1 (D-18 replay rejection assertion).
    - grep -c "addtest(bridge_anvil_e2e_test" test/src/bridge_e2e/CMakeLists.txt returns exactly 1.
    - grep -cE "WHOLEARCHIVE|force_load|whole-archive" test/src/bridge_e2e/CMakeLists.txt increases by >= 3 (the three-platform link block is replicated for the new target).
    - grep -c "json_secure_storage" test/src/bridge_e2e/CMakeLists.txt returns >= 3 (one per addtest block — original two + new one).
    - ./build/OSX/Debug/bridge_anvil_e2e_test --gtest_list_tests lists BridgeAnvilE2ETest.AnvilBurnToMintPipeline, BridgeAnvilE2ETest.AnvilReplayRejection, and BridgeSepoliaDirectFallbackTest.BurnToMintPipeline.
    - Build succeeds with zero new warnings.
  </acceptance_criteria>
  <done>Anvil replay-rejection test + Sepolia-direct fallback test class both compile and are listed by --gtest_list_tests. CMake target bridge_anvil_e2e_test builds against the same libraries and whole-archive link options as the Phase 4 target. All three tests (AnvilBurnToMintPipeline, AnvilReplayRejection, SepoliaDirect BurnToMintPipeline) appear in --gtest_list_tests output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process to Anvil subprocess | Test forks a local Anvil process via execlp; Anvil binds to 127.0.0.1 only. Anvil exposes a deterministic mnemonic — test-only, no real funds. |
| Anvil fork to Sepolia RPC | Anvil pulls read-only state from a public Sepolia RPC endpoint over HTTPS. No write operations cross this boundary (writes go to the local fork). |
| Test to Sepolia (fallback path) | Path B sends a real burn transaction to live Sepolia using a user-supplied PRIVATE_KEY. Real gas, real state change, real funds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Spoofing | Anvil default private key | accept | Key is public test fixture (mnemonic documented in Foundry docs). No real funds. Acceptable for local E2E. |
| T-04.1-02 | Tampering | cast send command construction | mitigate | Build cast commands with fixed kCamelCase constants; never interpolate untrusted user input. Private key comes ONLY from the hardcoded Anvil default (Path A) or PRIVATE_KEY env var (Path B, user-owned). |
| T-04.1-03 | Repudiation | Anvil subprocess lifecycle | mitigate | RAII AnvilProcess destructor calls Stop() (SIGTERM + waitpid) so no orphan process survives test crash. spdlog::info logs Start/Stop events for audit trail. |
| T-04.1-04 | Information Disclosure | Sepolia GNUS holder address (D-09 funding) | accept | Holder address is publicly readable from any ERC-20 holder list. Impersonation is a local-fork-only operation; no real holder is debited (impersonation does not move real funds on Sepolia). |
| T-04.1-05 | Denial of Service | Anvil port conflict (D-15) | mitigate | Use high port kAnvilStartPort=18545 to avoid default 8545 collisions; GTEST_SKIP with clear message if Start fails. |
| T-04.1-06 | Elevation of Privilege | GTEST_SKIP gating (D-16) | mitigate | Both paths gated: Path A skips if anvil/cast missing; Path B requires explicit RUN_E2E_BRIDGE=1 + PRIVATE_KEY opt-in. No silent execution of the Sepolia fallback. |
| T-04.1-SC | Tampering | No package-manager installs | accept | No npm/pip/cargo installs in this plan. Foundry install is a user_setup prerequisite, not an automated install task. |
</threat_model>

<verification>
1. cd build/OSX/Debug && ninja bridge_anvil_e2e_test — builds with zero new warnings.
2. ./bridge_anvil_e2e_test --gtest_list_tests — lists all three test cases.
3. ./bridge_anvil_e2e_test (no env) — both Anvil tests SKIP cleanly with the Foundry install URL (when anvil absent) or run if anvil is present; Sepolia fallback SKIPS (no RUN_E2E_BRIDGE).
4. RUN_E2E_BRIDGE=1 PRIVATE_KEY=<user_key> ./bridge_anvil_e2e_test --gtest_filter=BridgeSepoliaDirectFallbackTest.* — Path B runs against live Sepolia (manual, optional).
5. If anvil is installed and Sepolia RPC reachable: ./bridge_anvil_e2e_test --gtest_filter=BridgeAnvilE2ETest.* exercises the local fork end-to-end.
6. Static checks: grep -r "this_thread::sleep_for" test/src/bridge_e2e/anvil_fixture.hpp test/src/bridge_e2e/bridge_anvil_e2e_test.cpp returns 0 matches; grep -v comments then grep -cE "fprintf|cout|cerr|printf" == 0 in active code; no new OS #ifdef __APPLE__/#ifdef __linux__ guards added.
</verification>

<success_criteria>
- Local Anvil E2E test is fully self-contained — no dependency on ../TokenContracts/gnus-ai/.env or any external .env file (D-06). The Anvil default key is hardcoded as a public test constant.
- Anvil subprocess starts in SetUpTestSuite, polls eth_blockNumber for readiness, and is killed in TearDownTestSuite (D-01, D-04).
- Anvil default account #0 is funded with GNUS via anvil_impersonateAccount before the burn (D-08, D-09). If funding fails, the test SKIPS with a clear message rather than failing (D-10).
- A bridge burn sent via cast send to the LOCAL Anvil RPC produces a parsed tx hash that flows through MintTokens and results in a balance increase on node_main within the wait-condition timeout (D-17a).
- Replay of the same burn tx hash is rejected by the Phase 3 dedup cache (D-18).
- Sepolia-direct fallback test exists, gated by RUN_E2E_BRIDGE + PRIVATE_KEY, and reproduces the Phase 4 live-Sepolia approach (D-14 Path B).
- All tests skip cleanly when their required binaries/env vars are absent (D-16, D-14 Path B gating).
- No CLAUDE.md violations: no sleep_for, no fprintf/cout/cerr, no OS ifdef guards (beyond reused test-side popen wrapper), Allman bracing, Doxygen headers, kCamelCase constants.
</success_criteria>

<output>
Create .planning/phases/04.1-anvil-local-bridge-e2e-test-use-tokencontracts-gnus-ai-env-t/04.1-01-SUMMARY.md when done
</output>
