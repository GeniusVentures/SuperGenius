---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "12"
type: execute
wave: 12
depends_on: [14-11]
files_modified:
  - example/node_test/NodeExample.cpp
  - test/src/CMakeLists.txt
  - test/src/processing_multi/processing_multi_test.cpp
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py
autonomous: true
requirements: [D-03, D-04, D-11, D-16]
must_haves:
  truths:
    - "The node example and processing-multi test consume checked lifecycle-bound GeniusNode APIs without changing their functional processing or interactive-node behavior."
    - "Every expression in each omitted source is assigned exactly once to its actual CMake target and both targets rebuild before node-shim removal."
  artifacts:
    - path: ".planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv"
      provides: "Complete migrated 14-12 rows for the node_example and processing_multi_test source/target pairs"
      contains: "14-12"
    - path: "test/src/CMakeLists.txt"
      provides: "Configured parent-test wiring for the existing processing_multi_test target"
      contains: "add_subdirectory(processing_multi)"
  key_links:
    - from: "example/node_test/NodeExample.cpp"
      to: "node_example"
      via: "existing example/node_test CMake target and manifest row"
      pattern: "node_example"
    - from: "test/src/processing_multi/processing_multi_test.cpp"
      to: "processing_multi_test"
      via: "test/src parent CMake subdirectory wiring, existing processing_multi CMake target, and manifest row"
      pattern: "processing_multi_test"
---

<objective>
Migrate the two remaining account-bound GeniusNode caller source/target pairs after every existing caller partition and before final node-shim removal.

Purpose: Close the complete caller manifest without redesigning the interactive example or processing-multi test behavior, per D-03/D-04/D-11/D-16.
Output: Strictly owned and migrated rows for `NodeExample.cpp` → `node_example` and `processing_multi_test.cpp` → `processing_multi_test`, configured parent-test CMake wiring, fresh target builds, and preserved final-union evidence.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-11-SUMMARY.md
@example/node_test/NodeExample.cpp
@example/node_test/CMakeLists.txt
@test/src/CMakeLists.txt
@test/src/processing_multi/processing_multi_test.cpp
@test/src/processing_multi/CMakeLists.txt
</context>

<tasks>
<task type="auto">
  <name>Task 1: Lock the omitted example and processing-multi caller rows to their exact targets</name>
  <files>.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py</files>
  <read_first>
    - example/node_test/NodeExample.cpp
    - example/node_test/CMakeLists.txt
    - test/src/CMakeLists.txt
    - test/src/processing_multi/processing_multi_test.cpp
    - test/src/processing_multi/CMakeLists.txt
    - build/OSX/Release/compile_commands.json
  </read_first>
  <action>Rerun declaration-driven, comment/string-aware complete node API discovery. Assign every discovered account-bound expression from exactly `example/node_test/NodeExample.cpp` to `owner_plan=14-12`, `source=example/node_test/NodeExample.cpp`, and target `node_example`; assign every discovered account-bound expression from exactly `test/src/processing_multi/processing_multi_test.cpp` to `owner_plan=14-12`, `source=test/src/processing_multi/processing_multi_test.cpp`, and target `processing_multi_test`. Preserve the manifest header and require one resolved source/target owner per expression, no duplicate row, no unowned expression, and no owner outside 14-06 through 14-12. Classify each expression as active-ready, configured-bootstrap, lifecycle-selection, processing-generation, or internal-control; preserve the two source/target pairs in `--check-all-migrated` source and target unions. Treat the existing child `processing_multi` CMake file as target-definition evidence; Task 2 owns the single parent-test wiring required to configure that target.</action>
  <verify><automated>set -euo pipefail; p=.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle; python3 "$p/14-node-api-caller-audit.py" --check-current --inventory "$p/14-02-CALLER-INVENTORY.tsv" --compile-commands build/OSX/Release/compile_commands.json; python3 "$p/14-node-api-caller-audit.py" --check-owner 14-12 --inventory "$p/14-02-CALLER-INVENTORY.tsv" --sources example/node_test/NodeExample.cpp,test/src/processing_multi/processing_multi_test.cpp --required-targets node_example,processing_multi_test; python3 "$p/14-node-api-caller-audit.py" --check-assigned --inventory "$p/14-02-CALLER-INVENTORY.tsv" --owners 14-06,14-07,14-08,14-09,14-10,14-11,14-12 --require-exact-owner-per-expression-target --check-owner-source-partition</automated></verify>
  <done>Every account-bound caller expression in the two exact source/target pairs is classified, source-resolved, and exclusively owned by 14-12.</done>
</task>
<task type="auto">
  <name>Task 2: Wire the existing processing-multi target, migrate both caller contracts, and rebuild</name>
  <files>example/node_test/NodeExample.cpp, test/src/CMakeLists.txt, test/src/processing_multi/processing_multi_test.cpp, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv</files>
  <read_first>
    - build/OSX/CMakeLists.txt
    - test/src/CMakeLists.txt
    - test/src/processing_multi/CMakeLists.txt
    - build/OSX/Release/CMakeCache.txt
  </read_first>
  <action>Add the exact line `add_subdirectory(processing_multi)` to `test/src/CMakeLists.txt` immediately after the neighboring `add_subdirectory(processing)` entry, so the existing local `processing_multi_test` definition enters the configured test graph; do not change that target definition or redesign the test harness. Regenerate the existing macOS Release build and compile database with `cmake -S build/OSX -B build/OSX/Release -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE="$PWD/build/apple.toolchain.cmake" -DCMAKE_EXPORT_COMPILE_COMMANDS=ON`, then require configured target help to list `processing_multi_test`. Consume every 14-12 manifest row. Per D-03/D-04/D-11/D-16, unpack typed active-ready balances, mint/transfer/process-image results, processing status, and start/stop results before logging, arithmetic, control flow, or assertions; propagate or explicitly report exact `SWITCH_IN_PROGRESS` and `ACCOUNT_UNAVAILABLE` instead of treating default, empty, zero, `INVALID`, or `DISABLED` values as success. Preserve all node-example commands, prompts, logging intent, input parsing, and processing-multi fixture/test processing behavior. Do not redesign interactive commands, processing workflows, timing, node construction, bridge/trust ownership, selection/recovery behavior, or sources outside these two translation units plus the one parent CMake wiring line. Mark every 14-12 row migrated, then require `--check-all-migrated` to retain both exact sources and targets in the persisted union lists consumed by Plan 14-13 and Plan 14-15.</action>
  <verify><automated>set -euo pipefail; p=.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle; d="$(mktemp -d /tmp/phase14-12-union.XXXXXX)"; cmake -S build/OSX -B build/OSX/Release -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE="$PWD/build/apple.toolchain.cmake" -DCMAKE_EXPORT_COMPILE_COMMANDS=ON; cmake --build build/OSX/Release --target help | rg -q '(^|[[:space:]])processing_multi_test([[:space:]]|$)'; test -s build/OSX/Release/compile_commands.json; python3 "$p/14-node-api-caller-audit.py" --check-migrated --owner 14-12 --inventory "$p/14-02-CALLER-INVENTORY.tsv" --compile-commands build/OSX/Release/compile_commands.json; cmake --build build/OSX/Release --target node_example processing_multi_test -j8; python3 "$p/14-node-api-caller-audit.py" --check-all-migrated --inventory "$p/14-02-CALLER-INVENTORY.tsv" --compile-commands build/OSX/Release/compile_commands.json --targets-output "$d/targets" --sources-output "$d/sources"; rg -Fx 'example/node_test/NodeExample.cpp' "$d/sources"; rg -Fx 'test/src/processing_multi/processing_multi_test.cpp' "$d/sources"; rg -Fx 'node_example' "$d/targets"; rg -Fx 'processing_multi_test' "$d/targets"</automated></verify>
  <done>The parent test CMake graph configures the existing processing-multi target, both actual target owners compile freshly, all rows are migrated, and the complete final caller union retains both omitted source/target pairs.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Interactive/test caller → lifecycle result | Sentinel values could make an unavailable generation appear successful. |
| Caller source → configured CMake target | An unregistered processing-multi child target could evade the final shim-removal build. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-12-01 | Tampering / Information Disclosure | example and processing result handling | mitigate | Typed-result adaptation and exact lifecycle-error propagation. |
| T14-12-02 | Repudiation | processing-multi target coverage | mitigate | Parent `add_subdirectory(processing_multi)` wiring, reconfiguration/target-help assertion, strict manifest rows, fresh target builds, and persisted all-migrated union membership. |
| T14-SC | Tampering | package installs | accept | No package installation occurs. |
</threat_model>

<multi_source_coverage_audit>
## Multi-Source Coverage Audit
| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | complete account-bound caller migration | Omitted lifecycle-bound callers migrate before shim removal | 14-12 | COVERED | Exact two source/target pairs. |
| REQ | D-03/D-04/D-11/D-16 | Explicit switching/unavailable contracts and generation-owned status | 14-12 | COVERED | No sentinel success path. |
| RESEARCH | complete caller/target union | Manifest rows and final union include node_example and configured processing_multi_test | 14-12, 14-13, 14-15 | COVERED | Existing child target plus parent-test wiring are read-first evidence. |
| CONTEXT | Deferred processing/node-example redesign | Caller-contract adaptation plus one existing-target registration line only | NONE | EXCLUDED | Commands and processing behavior are preserved; no broader test-harness redesign. |
</multi_source_coverage_audit>

<verification>Audit and strict exact owner rows precede edits; parent CMake registration, reconfiguration, configured target-help, migrated audit, fresh builds, and all-migrated union membership follow.</verification>
<success_criteria>The two previously omitted caller partitions are exclusively owned, lifecycle-typed, freshly rebuilt, and represented in the final union before node shim removal; the configured test graph contains `processing_multi_test` through its parent CMake registration.</success_criteria>
<output>Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-12-SUMMARY.md` when done.</output>
