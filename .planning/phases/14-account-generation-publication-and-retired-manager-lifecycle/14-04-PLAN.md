---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "04"
type: execute
wave: 4
depends_on: [14-03]
files_modified: []
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16]
must_haves:
  truths:
    - "The exact sixteen Phase 14 semantic regressions implementing D-01 through D-16 each have one unique active source declaration, every GoogleTest parameter expansion is explicitly enumerated, and freshly linked XML equals that runtime name set with no failure, error, skip, not-run, disabled, missing, duplicate, or extra result."
    - "The strengthened concurrent snapshot regression contains no partial-owner continue escape and mechanically asserts complete account/manager/processing presence before inspecting a ready generation, per D-03, D-09, D-11, and D-16."
    - "The manager mutation inventory and selected-switch timeout/failure paths retain the exact D-05 through D-15 contracts: one admission gate, no post-close mutation, no accepted-work cancellation, no automatic retry, and no retired or failed generation republish."
    - "A fresh explicit build precedes all evidence, both complete scoped CTest targets pass with exact enumeration/JUnit equality, and the deterministic admission/drain/timeout/stale/cleanup/retirement/status subset passes five consecutive exact repetitions."
    - "ThreadSanitizer is reported PASS only for a build tree where both scoped targets' own test translation units and both final executables prove the same TSan instrumentation; otherwise the only sanitizer result is sanitizer_configured=0 sanitizer_status=NOT_RUN."
  artifacts:
    - path: ".planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-04-SUMMARY.md"
      provides: "Post-success exact source/list/XML, two-target CTest/JUnit, five-repeat, TSan, and cumulative threat evidence for D-01 through D-16"
  key_links:
    - from: "Plans 14-01 through 14-03 exact regression declarations"
      to: "multi_account_test and account_management_test --gtest_list_tests"
      via: "sixteen-name semantic equality with parameterized runtime expansion normalization"
      pattern: "semantic_names_exact=PASS|runtime_expansions_exact=PASS"
    - from: "fresh multi_account_test/account_management_test binaries"
      to: "focused GoogleTest XML and complete scoped CTest JUnit"
      via: "exact unique runtime-name and target-name equality"
      pattern: "focused_xml_names_exact=PASS|ctest_names_exact=PASS|junit_names_exact=PASS"
    - from: "both targets' own translation units"
      to: "both final target executables"
      via: "same-tree -fsanitize=thread compile proof plus link flag or TSan runtime import"
      pattern: "sanitizer_configured=0 sanitizer_status=NOT_RUN|sanitizer_status=PASS"
---

<objective>
Run the fail-fast exact Phase 14 closure gate after Plans 14-01 through 14-03: mechanically prove their source contracts, freshly build both scoped binaries, establish exact sixteen-semantic-case source/list/XML equality including parameterized expansions, run both complete CTest targets, repeat the deterministic lifecycle subset five times, and report TSan only when both exact targets are proven instrumented.

Purpose: D-01 through D-16 are concurrency and lifecycle guarantees whose evidence is invalid if a case is renamed, omitted, duplicated, disabled, selected with a zero-match filter, executed from a stale binary, hidden by count-only accounting, or inferred from an unrelated sanitizer configuration.
Output: Read-only structural and execution evidence plus `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-04-SUMMARY.md`, created only after the uninterrupted gate exits zero.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PLAN-OUTLINE.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-PLAN.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-PLAN.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-03-PLAN.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-03-SUMMARY.md
@test/src/multiaccount/multi_account_sync.cpp
@test/src/account/account_management_test.cpp
@test/src/multiaccount/CMakeLists.txt
@test/src/account/CMakeLists.txt
@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp
@src/account/TransactionManager.hpp
@src/account/TransactionManager.cpp
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-29-PLAN.md
@.planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-29-SUMMARY.md
</context>

<expected_inventory>
The authoritative semantic inventory is derived only from Plans 14-01 through 14-03. It contains exactly these sixteen suite/case contracts; a `TEST_P` declaration may expand to multiple runtime names, but every expansion must normalize back to exactly one of these semantic names and the complete listed expansion set must equal focused XML:

- `MultiAccountTest.AccountGenerationAdmissionBoundaryIsLinearizable`
- `MultiAccountTest.RetiredManagerRejectsEveryMutationEntryPoint`
- `MultiAccountTest.RetiredManagerDiagnosticsAreImmutable`
- `MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration`
- `MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization`
- `MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion`
- `MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle`
- `MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent`
- `MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation`
- `MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration`
- `MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage`
- `AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent`
- `AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap`
- `AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration`
- `AccountManagement.TimedOutSwitchRejectsRecoveryUntilRetireDrain`
- `AccountManagement.FailedSwitchRequiresExplicitRecovery`

The every-stage cleanup case must cover exactly `ACCOUNT_CONSTRUCTION`, `ACCOUNT_CONFIGURATION`, `BLOCKCHAIN_CONSTRUCTION`, `BLOCKCHAIN_START`, `TRANSACTION_MANAGER_CONSTRUCTION`, `TRANSACTION_MANAGER_START`, `PROCESSING_CONSTRUCTION`, and `PROCESSING_START`, whether implemented as one table-driven `TEST_F` body or as parameterized expansions. No other test is added to or substituted for this inventory.
</expected_inventory>

<tasks>

<task type="auto">
  <name>Task 1: Fail closed on weakened Phase 14 source contracts</name>
  <files>None; read-only structural verification of the production and test sources produced by Plans 14-01 through 14-03.</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-PLAN.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-PLAN.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-03-PLAN.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-03-SUMMARY.md
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
  </read_first>
  <action>Run one comment/string-aware, brace-aware structural verifier and stop immediately on its first failed assertion. Parse `TEST`, `TEST_F`, and `TEST_P` declarations in both scoped sources. Normalize one or more leading `DISABLED_` prefixes independently from suite and case names; require one and only one active source declaration for every semantic name in `<expected_inventory>`, no disabled counterpart, and no extra declaration in the Phase 14 `AccountGeneration*`, `RetiredManager*`, or named account-lifecycle families. Extract every exact Phase 14 body and reject `sleep_for`, `sleep_until`, `usleep`, `Sleep`, or `nanosleep`; deterministic cases must instead contain their Plan 14-01..14-03 named hook/barrier observations or explicit expected/observed variables asserted at the same semantic boundary. Do not use token-distance checks where a named expected value or delegated helper is explicitly asserted; resolve local named aliases and one level of fixture/helper delegation before failing.

In `ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent`, reject every `continue` statement, require a non-empty observation set, and trace assertions that account, manager, and processing presence are equal before any ready-owner address/generation assertion. Require zero stale callback side effects. In `AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage`, require exact equality with the eight-stage inventory above, not mere token presence or a lower-bound count.

Gate the manager mutation inventory from Plan 14-01. Require `TransferFunds`, `MintFunds`, `MigrationFunds`, `HoldEscrow`, `PayEscrow`, `AsyncPayEscrow`, and `SubmitTransaction` to reach the one `TryAdmit` contract before mutation, directly or through an explicitly extracted admitted helper; require both enqueue overloads to consume an existing `AdmittedOperation` and never acquire fresh admission; require background continuation paths to use `RequireAdmittedContinuation`; and require `RetiredManagerRejectsEveryMutationEntryPoint` to exercise all seven public surfaces plus post-close background ingress with unchanged counters/reservations/queue/ledger. Preserve typed `MANAGER_RETIRED`, immutable retirement diagnostics, idempotent terminal completion, and old-generation attribution identifiers.

Extract `HandleAccountSwitchDrainTimeout`, `HandleAccountSwitchDrainCompleted`, `BeginPendingAccountInitialization`, and `FailPendingAccountGeneration` rather than scanning arbitrary token proximity. The timeout path must not stop/cancel/synthetic-terminalize the retiring manager, construct/start replacement owners, call either retry helper, or publish ready. The selected initialization and failure paths must not call `ScheduleBlockchainRetry`, `ScheduleMigrationRetry`, or any generic retry/queued-selection helper; failure must not call `PublishReadyAccountGeneration` or assign a retiring/failed bundle to ready. Permit cancellation only of the switch deadline handle and permit `Stop` only for partial pending-owner cleanup—neither is evidence that accepted old work was cancelled. Require generation plus expected-owner guards, cleanup before `ACCOUNT_SWITCH_FAILED`, and no queued/retry target field. Do not modify production or test sources in this evidence plan; any mismatch returns to the owning implementation plan.</action>
  <verify>
    <automated>set -euo pipefail; python3 -c 'exec("""import pathlib,re
paths={"multi":pathlib.Path("test/src/multiaccount/multi_account_sync.cpp"),"account":pathlib.Path("test/src/account/account_management_test.cpp")}
sources={key:path.read_text(encoding="utf-8") for key,path in paths.items()}
expected={
"MultiAccountTest.AccountGenerationAdmissionBoundaryIsLinearizable","MultiAccountTest.RetiredManagerRejectsEveryMutationEntryPoint","MultiAccountTest.RetiredManagerDiagnosticsAreImmutable","MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration","MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization","MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion","MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle","MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent","MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation","MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration","MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage","AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent","AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap","AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration","AccountManagement.TimedOutSwitchRejectsRecoveryUntilRetireDrain","AccountManagement.FailedSwitchRequiresExplicitRecovery"}
noise=re.compile(r"//[^\\n]*|/\\*.*?\\*/|\\\"(?:\\\\.|[^\\\"\\\\])*\\\"|\\x27(?:\\\\.|[^\\x27\\\\])*\\x27",re.S)
def code(raw): return noise.sub(lambda m:re.sub(r"[^\\n]"," ",m.group()),raw)
def brace(raw,start):
    opening=raw.find("{",start); assert opening>=0,("opening brace",start)
    depth=0
    for token in re.finditer(r"//[^\\n]*|/\\*.*?\\*/|\\\"(?:\\\\.|[^\\\"\\\\])*\\\"|\\x27(?:\\\\.|[^\\x27\\\\])*\\x27|[{}]",raw[opening:],re.S):
        if token.group()=="{": depth+=1
        elif token.group()=="}":
            depth-=1
            if depth==0: return raw[opening:opening+token.end()]
    raise AssertionError("unbalanced body")
decl=[]; bodies={}
macro=re.compile(r"TEST(?:_F|_P)?\\s*\\(\\s*([A-Za-z_]\\w*)\\s*,\\s*([A-Za-z_]\\w*)\\s*\\)")
for key,raw in sources.items():
    for match in macro.finditer(code(raw)):
        suite,case=match.groups(); normalized_suite=re.sub(r"^(?:DISABLED_)+","",suite); normalized_case=re.sub(r"^(?:DISABLED_)+","",case); normalized=normalized_suite+"."+normalized_case; active=(suite==normalized_suite and case==normalized_case)
        decl.append((normalized,active,suite+"."+case,key));
        if active and normalized in expected: bodies[normalized]=brace(raw,match.end())
active=[n for n,on,raw,key in decl if on and n in expected]; disabled=[raw for n,on,raw,key in decl if not on and n in expected]
assert set(active)==expected and len(active)==len(expected),(sorted(expected-set(active)),sorted(set(active)-expected),active)
assert all(active.count(name)==1 for name in expected) and not disabled,(active,disabled)
families=[n for n,on,raw,key in decl if on and (n.startswith("MultiAccountTest.AccountGeneration") or n.startswith("MultiAccountTest.RetiredManager") or n.startswith("AccountManagement.SelectAccountReturnsGeneration") or n.startswith("AccountManagement.SwitchInProgress") or n.startswith("AccountManagement.ConfiguredIdentity") or n.startswith("AccountManagement.TimedOutSwitch") or n.startswith("AccountManagement.FailedSwitch"))]
assert set(families)==expected and len(families)==len(expected),(sorted(families),sorted(expected))
sleep_rx=re.compile(r"(?:std::this_thread::)?sleep_(?:for|until)\\s*\\(|\\busleep\\s*\\(|\\bnanosleep\\s*\\(|\\bSleep\\s*\\(")
for name,body in bodies.items(): assert not sleep_rx.search(code(body)),("sleep in exact Phase14 case",name)
snapshot=code(bodies["MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent"])
assert not re.search(r"\\bcontinue\\s*;",snapshot)
assert re.search(r"ASSERT_(?:FALSE|GT|GE|NE)\\s*\\([^;]*(?:observed|snapshot)",snapshot,re.S)
for owner in ("account","manager","processing"): assert re.search(r"observed\\.[A-Za-z_]*"+owner,snapshot,re.I),(owner,"presence observation missing")
presence_assertions="\\n".join(re.findall(r"(?:ASSERT|EXPECT)_(?:EQ|TRUE|FALSE)\\s*\\([^;]+",snapshot,re.S))
assignments=dict(re.findall(r"\\b(?:const\\s+)?(?:bool|auto)\\s+([A-Za-z_]\\w*)\\s*=\\s*([^;]+);",snapshot))
expanded=presence_assertions
for _ in range(3):
    for name,value in assignments.items(): expanded=re.sub(r"\\b"+re.escape(name)+r"\\b","("+value+")",expanded)
assert all(owner in expanded.lower() for owner in ("account","manager","processing")),expanded
assert re.search(r"stale_callback_side_effects[\\s\\S]*?(?:ASSERT|EXPECT)_EQ\\s*\\([^;]*0U?",snapshot)
stage_body=code(bodies["MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage"])
stages=["ACCOUNT_CONSTRUCTION","ACCOUNT_CONFIGURATION","BLOCKCHAIN_CONSTRUCTION","BLOCKCHAIN_START","TRANSACTION_MANAGER_CONSTRUCTION","TRANSACTION_MANAGER_START","PROCESSING_CONSTRUCTION","PROCESSING_START"]
assert all(stage_body.count(stage)>=1 for stage in stages),[stage for stage in stages if stage not in stage_body]
assert re.search(r"(?:expected_[A-Za-z_]*stages|all_[A-Za-z_]*stages|ReplacementInitializationStage)[\\s\\S]*?(?:ASSERT|EXPECT)_EQ",stage_body,re.I),"stage set/count equality missing"
tm_h=pathlib.Path("src/account/TransactionManager.hpp").read_text(encoding="utf-8"); tm=pathlib.Path("src/account/TransactionManager.cpp").read_text(encoding="utf-8"); gn_h=pathlib.Path("src/account/GeniusNode.hpp").read_text(encoding="utf-8"); gn=pathlib.Path("src/account/GeniusNode.cpp").read_text(encoding="utf-8")
enum_match=re.search(r"enum(?:\\s+class)?\\s+ReplacementInitializationStage\\s*\\{([^}]*)\\}",code(gn_h),re.S); assert enum_match,"ReplacementInitializationStage missing"
enum_values={token for token in re.findall(r"\\b([A-Z][A-Z0-9_]*)\\b",enum_match.group(1))}; assert enum_values==set(stages),(enum_values,set(stages))
assert all(token in tm_h+tm for token in ("MANAGER_RETIRED","ManagerLifecycle","AdmittedOperation","TryAdmit","RequireAdmittedContinuation","CompleteAdmittedOperation","RetirementSnapshot"))
def functions(raw,qualified):
    matches=list(re.finditer(r"\\b"+re.escape(qualified)+r"\\s*\\(",code(raw))); assert matches,("missing function",qualified)
    candidates=[]
    for match in matches:
        tail=code(raw[match.end():match.end()+800]); semi=tail.find(";"); opening=tail.find("{")
        if opening>=0 and (semi==-1 or opening.__lt__(semi)): candidates.append(code(brace(raw,match.end())))
    assert candidates,(qualified,"definition missing"); return candidates
def one_function(raw,qualified):
    found=functions(raw,qualified); assert len(found)==1,(qualified,len(found)); return found[0]
def body_reaches_admission(body,origin):
    if "TryAdmit" in body or "AdmittedOperation" in body: return True
    callees=re.findall(r"\\b([A-Za-z_]\\w*)\\s*\\(",body)
    for callee in callees:
        if callee==origin or not re.search(r"TransactionManager::"+re.escape(callee)+r"\\s*\\(",code(tm)): continue
        if any("TryAdmit" in helper for helper in functions(tm,"TransactionManager::"+callee)): return True
    return False
def reaches_admission(name):
    return all(body_reaches_admission(body,name) for body in functions(tm,"TransactionManager::"+name))
mutators=["TransferFunds","MintFunds","MigrationFunds","HoldEscrow","PayEscrow","AsyncPayEscrow","SubmitTransaction"]
assert all(reaches_admission(name) for name in mutators),[name for name in mutators if not reaches_admission(name)]
enqueue=functions(tm,"TransactionManager::EnqueueTransaction"); assert len(enqueue)==2,("enqueue overloads",len(enqueue)); assert all("AdmittedOperation" in body and "TryAdmit" not in body for body in enqueue)
def body_reaches_continuation(body,origin):
    if "RequireAdmittedContinuation" in body: return True
    for callee in re.findall(r"\\b([A-Za-z_]\\w*)\\s*\\(",body):
        if callee==origin or not re.search(r"TransactionManager::"+re.escape(callee)+r"\\s*\\(",code(tm)): continue
        if any("RequireAdmittedContinuation" in helper for helper in functions(tm,"TransactionManager::"+callee)): return True
    return False
for name in ("TickOnce","SendTransactionItem","OnConsensusCertificate","ChangeTransactionState"):
    assert all(body_reaches_continuation(body,name) for body in functions(tm,"TransactionManager::"+name)),(name,"continuation gate missing")
retired=code(bodies["MultiAccountTest.RetiredManagerRejectsEveryMutationEntryPoint"])
assert all(name in retired for name in mutators),[name for name in mutators if name not in retired]
assert all(token in retired for token in ("MANAGER_RETIRED","reservation","queue","ledger")),"retired side-effect inventory incomplete"
timeout=one_function(gn,"GeniusNode::HandleAccountSwitchDrainTimeout"); drained=one_function(gn,"GeniusNode::HandleAccountSwitchDrainCompleted"); begin=one_function(gn,"GeniusNode::BeginPendingAccountInitialization"); failure=one_function(gn,"GeniusNode::FailPendingAccountGeneration")
for label,body in (("timeout",timeout),("drain",drained),("selected-init",begin)):
    forbidden=[x for x in ("ScheduleBlockchainRetry","ScheduleMigrationRetry") if x in body]; assert not forbidden,(label,forbidden)
for token in ("PublishReadyAccountGeneration","CompleteAdmittedOperation","CancelPendingTransactionWaits","BeginPendingAccountInitialization","TransactionManager::New","GeniusAccount::NewFromPublicKey","Blockchain::New"):
    assert token not in timeout,("timeout escape",token)
assert not re.search(r"retir(?:ing|ed)[A-Za-z0-9_]*[\\s\\S]{0,200}?->\\s*Stop\\s*\\(",timeout,re.I)
assert "FailPendingAccountGeneration" in begin
assert not any(token in failure for token in ("ScheduleBlockchainRetry","ScheduleMigrationRetry","PublishReadyAccountGeneration"))
assert not re.search(r"ready[A-Za-z0-9_]*\\s*=\\s*(?:std::move\\s*\\()?\\s*(?:retir|fail)",failure,re.I)
assert re.search(r"pending[\\s\\S]*?(?:StopProcessing|ResetProcessingMembers|processing)[\\s\\S]*?Stop",failure,re.I)
assert re.search(r"ACCOUNT_UNAVAILABLE[\\s\\S]*?ACCOUNT_SWITCH_FAILED",failure)
assert not re.search(r"queued_(?:account|target|selection)|retry_(?:account|target|selection)",gn_h+gn,re.I)
for body_name,body in (("drain",drained),("selected-init",begin),("failure",failure)):
    assert "generation" in body.lower() and re.search(r"expected|\\.get\\s*\\(",body),(body_name,"generation/owner guard missing")
print("phase14_structural_contracts=PASS source_declarations_exact=PASS partial_snapshot_guard=PASS no_sleep_race_proof=PASS mutation_inventory=PASS timeout_failure_escape_guard=PASS")
""")'</automated>
  </verify>
  <acceptance_criteria>
    - All sixteen expected semantic declarations exist exactly once as active `TEST`/`TEST_F`/`TEST_P` declarations; every normalized disabled counterpart and every extra Phase 14-family declaration is rejected.
    - The existing partial-snapshot skip is gone, and the strengthened regression asserts complete owner-presence equality, non-empty observations, owner/generation consistency, and zero stale-callback side effects.
    - Every exact lifecycle case is free of sleep-based race proof and contains deterministic named hook/barrier or explicit expected/observed assertions for its claimed boundary.
    - The seven public mutation surfaces, enqueue handoff, and admitted continuations mechanically reach the Plan 14-01 admission/ledger contract; the retired mutation case observes unchanged mutation state.
    - Timeout and selected-failure paths cannot cancel accepted old work, start replacement after timeout, retry automatically, queue another selection, or publish a retired/failed bundle; cleanup-before-failure and generation/owner guards remain present.
  </acceptance_criteria>
  <done>The source tree still expresses every D-01..D-16 lifecycle contract and contains no structural escape that could let passing runtime counts conceal a weakened implementation or test.</done>
</task>

<task type="auto">
  <name>Task 2: Run fresh exact two-target, five-repeat, and honest TSan evidence</name>
  <files>.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-04-SUMMARY.md (create only after the complete chain exits zero)</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-03-SUMMARY.md
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
    - test/src/multiaccount/CMakeLists.txt
    - test/src/account/CMakeLists.txt
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-29-PLAN.md
    - .planning/phases/13-close-v1-1-trusted-peer-genesis-quorum-policy-and-production/13-29-SUMMARY.md
  </read_first>
  <action>After Task 1 passes, run one uninterrupted `set -euo pipefail` chain. Create a fresh temporary evidence directory, record the current source hashes, and explicitly build `multi_account_test` and `account_management_test`. Require both binaries to exist, to be at least as new as their own source/object inputs, and require a second explicit build check to report no pending relink before accepting evidence. Do not reuse prior XML or logs.

Parse fresh `--gtest_list_tests` output from both binaries. Normalize parameterized runtime names by stripping only the GoogleTest instantiation prefix before the fixture and the final parameter suffix after the case; independently normalize `DISABLED_` on suite and case. Require exact equality with the sixteen semantic names, at least one runtime expansion per semantic case, unique raw runtime names, and no disabled/unknown Phase 14 family. Persist the exact raw runtime names per binary and use those names—not wildcard or count-only filters—to run focused XML. Parse all suite and case status fields and require raw XML name equality with the persisted list, semantic normalization equality with the sixteen-name inventory, executed equals passed, and failure/error/skip/not-run/disabled/duplicate/extra/missing all equal zero. A successful process that executed zero or a proper subset is failure.

Enumerate CTest with exact regex `^(multi_account_test|account_management_test)$`; require exactly those two unique enabled names and one `Total Tests: 2`. Run those complete targets with `--output-junit`, then require the JUnit names to equal exactly those two and executed=passed=2 with zero failure/error/skip/not-run/disabled. Do not substitute Phase 13's twenty-five-target suite. Add another regression target only if a Plan 14-01..14-03 summary records a changed consumer outside these two targets; if that occurs, stop and revise this plan with the named consumer rather than silently widening the regex.

After the broad scoped gate is green, derive the raw runtime expansions for exactly this deterministic repeat subset and execute it five consecutive times with separately parsed XML: admission (`AccountGenerationAdmissionBoundaryIsLinearizable`), drain (`AccountGenerationDrainsBeforeReplacementInitialization`), timeout plus true late terminal (`AccountGenerationDrainTimeoutFailsClosedWithoutCancellation`, `AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration`), stale callback (`AccountGenerationRejectsStaleBlockchainCompletion`), every-stage cleanup (`AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage`), retired mutation and terminal attribution (`RetiredManagerRejectsEveryMutationEntryPoint`, `RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration`), and processing status (`AccountGenerationProcessingStatusTracksLifecycle`). Every repetition must equal the same persisted raw expansion set and pass completely; no retries of failed repetitions are permitted.

Search configured build trees for TSan eligibility only after Release evidence is green. A tree qualifies only when `compile_commands.json` proves `-fsanitize=thread` on `multi_account_sync.cpp` for `multi_account_test` and on `account_management_test.cpp` for `account_management_test`, and each exact final executable independently has `-fsanitize=thread` in its link command or imports the TSan runtime. Both targets must qualify in the same tree; flags on dependencies, another target, a cache variable, compiler capability test, fuzz target, or only one scoped target do not count. For each qualifying tree, freshly build both targets, repeat list/source semantic equality, run every Phase 14 raw expansion under TSan into XML, and require exact names and zero bad results. If there is no qualifying two-target tree, emit exactly `sanitizer_configured=0 sanitizer_status=NOT_RUN`; otherwise emit target-specific compile/link/runtime and exact-case PASS records followed by `sanitizer_status=PASS`. Never turn absence or an ineligible tree into PASS.

Only after structural, fresh-build, focused XML, complete CTest/JUnit, all five repetitions, and every eligible TSan run have exited zero, create `14-04-SUMMARY.md`. Record the temporary evidence path, source/binary hashes, exact sixteen semantic names, exact raw expansion names/counts, two CTest/JUnit names and counts, five labeled repeat results, exact sanitizer disposition/proofs, and a table mapping every HIGH threat from Plans 14-01 through 14-03 plus T14-E01..T14-E07 below to named passing evidence. Do not edit production, tests, CMake, prior plans/summaries, STATE, ROADMAP, REQUIREMENTS, CONTEXT, RESEARCH, PATTERNS, VALIDATION, or the outline. If any source/test changes after the chain starts, discard the temporary evidence and restart from Task 1 and the fresh build.</action>
  <verify>
    <automated>set -euo pipefail; evidence_dir="$(mktemp -d /tmp/phase14-04-evidence.XXXXXX)"; expected_semantic='MultiAccountTest.AccountGenerationAdmissionBoundaryIsLinearizable,MultiAccountTest.RetiredManagerRejectsEveryMutationEntryPoint,MultiAccountTest.RetiredManagerDiagnosticsAreImmutable,MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration,MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization,MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion,MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle,MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent,MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation,MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration,MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage,AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent,AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap,AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration,AccountManagement.TimedOutSwitchRejectsRecoveryUntilRetireDrain,AccountManagement.FailedSwitchRequiresExplicitRecovery'; repeat_semantic='MultiAccountTest.AccountGenerationAdmissionBoundaryIsLinearizable,MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization,MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation,MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration,MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion,MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage,MultiAccountTest.RetiredManagerRejectsEveryMutationEntryPoint,MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration,MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle'; shasum -a 256 src/account/GeniusNode.hpp src/account/GeniusNode.cpp src/account/TransactionManager.hpp src/account/TransactionManager.cpp test/src/multiaccount/multi_account_sync.cpp test/src/account/account_management_test.cpp &gt; "$evidence_dir/source-before.sha256"; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8 | tee "$evidence_dir/build.log"; test -x build/OSX/Release/test_bin/multi_account_test; test -x build/OSX/Release/test_bin/account_management_test; python3 -c 'import pathlib,sys
for binary,source,object_name in ((pathlib.Path("build/OSX/Release/test_bin/multi_account_test"),pathlib.Path("test/src/multiaccount/multi_account_sync.cpp"),"multi_account_sync.cpp"),(pathlib.Path("build/OSX/Release/test_bin/account_management_test"),pathlib.Path("test/src/account/account_management_test.cpp"),"account_management_test.cpp")):
 assert binary.stat().st_mtime_ns&gt;=source.stat().st_mtime_ns,(binary,"older than source")
 objects=[p for p in pathlib.Path("build/OSX/Release").glob("**/*") if p.is_file() and object_name in p.name and p.suffix in (".o",".obj")]
 assert objects,(binary,"own object missing")
 assert binary.stat().st_mtime_ns&gt;=max(p.stat().st_mtime_ns for p in objects),(binary,"older than own object")
print("fresh_target_artifacts=PASS")'; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8 | tee "$evidence_dir/build-confirm.log"; build/OSX/Release/test_bin/multi_account_test --gtest_list_tests &gt; "$evidence_dir/multi.list"; build/OSX/Release/test_bin/account_management_test --gtest_list_tests &gt; "$evidence_dir/account.list"; python3 -c 'exec("""import pathlib,re,sys
expected_list=sys.argv[1].split(","); expected=set(expected_list); assert len(expected_list)==len(expected)==16
macro=re.compile(r"TEST(?:_F|_P)?\\s*\\(\\s*([A-Za-z_]\\w*)\\s*,\\s*([A-Za-z_]\\w*)\\s*\\)")
noise=re.compile(r"//[^\\n]*|/\\*.*?\\*/|\\\"(?:\\\\.|[^\\\"\\\\])*\\\"|\\x27(?:\\\\.|[^\\x27\\\\])*\\x27",re.S)
declarations=[]; disabled=[]
for source_path in ("test/src/multiaccount/multi_account_sync.cpp","test/src/account/account_management_test.cpp"):
 source=noise.sub(lambda match:re.sub(r"[^\\n]"," ",match.group()),pathlib.Path(source_path).read_text(encoding="utf-8"))
 for suite,case in macro.findall(source):
  normalized_suite=re.sub(r"^(?:DISABLED_)+","",suite); normalized_case=re.sub(r"^(?:DISABLED_)+","",case); normalized=normalized_suite+"."+normalized_case
  if normalized in expected:
   if suite==normalized_suite and case==normalized_case: declarations.append(normalized)
   else: disabled.append(suite+"."+case)
assert len(declarations)==len(set(declarations))==16 and set(declarations)==expected and not disabled,(declarations,disabled)
def normalize(raw):
 suite,case=raw.split(".",1); suite=suite.split("/")[-1]; case=case.split("/",1)[0]; suite=re.sub(r"^(?:DISABLED_)+","",suite); case=re.sub(r"^(?:DISABLED_)+","",case); return suite+"."+case
def parse(path):
 suite=None; names=[]
 for raw in pathlib.Path(path).read_text(encoding="utf-8").splitlines():
  line=raw.strip()
  if not line or line.startswith("#"): continue
  if not raw[:1].isspace() and line.endswith("."): suite=line[:-1].split(" #",1)[0]; continue
  if suite is not None and raw[:1].isspace():
   case=line.split(" #",1)[0]; names.append(suite+"."+case)
 return names
all_names=[]
for label,path,prefix in (("multi",sys.argv[2],"MultiAccountTest."),("account",sys.argv[3],"AccountManagement.")):
 listed=parse(path); selected=[name for name in listed if normalize(name) in expected]
 normalized_selected=[normalize(name) for name in selected]; wanted={name for name in expected if name.startswith(prefix)}
 phase=[name for name in listed if normalize(name).startswith("MultiAccountTest.AccountGeneration") or normalize(name).startswith("MultiAccountTest.RetiredManager") or normalize(name).startswith("AccountManagement.SelectAccountReturnsGeneration") or normalize(name).startswith("AccountManagement.SwitchInProgress") or normalize(name).startswith("AccountManagement.ConfiguredIdentity") or normalize(name).startswith("AccountManagement.TimedOutSwitch") or normalize(name).startswith("AccountManagement.FailedSwitch")]
 assert len(selected)==len(set(selected)) and set(normalized_selected)==wanted and set(phase)==set(selected),(label,phase,selected,wanted)
 assert not any("DISABLED_" in name for name in selected),(label,selected)
 pathlib.Path(sys.argv[4],label+".runtime").write_text("\\n".join(sorted(selected))+"\\n",encoding="utf-8")
 pathlib.Path(sys.argv[4],label+".filter").write_text(":".join(sorted(selected)),encoding="utf-8")
 all_names.extend(selected)
normalized=[normalize(name) for name in all_names]
assert len(all_names)==len(set(all_names)) and set(normalized)==expected,(sorted(expected-set(normalized)),sorted(set(normalized)-expected),all_names)
assert all(normalized.count(name)&gt;=1 for name in expected)
pathlib.Path(sys.argv[4],"all.runtime").write_text("\\n".join(sorted(all_names))+"\\n",encoding="utf-8")
print(f"source_names_exact=PASS source_declarations=16 semantic_names_exact=PASS semantic_count=16 runtime_expansions_exact=PASS runtime_count={len(all_names)}")
""")' "$expected_semantic" "$evidence_dir/multi.list" "$evidence_dir/account.list" "$evidence_dir"; multi_filter="$(tr -d '\n' &lt; "$evidence_dir/multi.filter")"; account_filter="$(tr -d '\n' &lt; "$evidence_dir/account.filter")"; test -n "$multi_filter"; test -n "$account_filter"; build/OSX/Release/test_bin/multi_account_test --gtest_filter="$multi_filter" --gtest_output="xml:$evidence_dir/multi.xml"; build/OSX/Release/test_bin/account_management_test --gtest_filter="$account_filter" --gtest_output="xml:$evidence_dir/account.xml"; python3 -c 'exec("""import pathlib,re,sys,xml.etree.ElementTree as E
expected=set(pathlib.Path(sys.argv[1]).read_text(encoding="utf-8").splitlines()); names=[]; bad=[]
for path in sys.argv[2:]:
 root=E.parse(path).getroot()
 for node in root.iter():
  tag=node.tag.rsplit("}",1)[-1]
  if tag in ("testsuites","testsuite"):
   for attr in ("failures","errors","skipped","disabled"):
    if int(node.get(attr,"0")): bad.append((path,tag,attr,node.get(attr)))
 for case in root.iter("testcase"):
  full=case.get("classname")+"."+case.get("name"); names.append(full)
  if case.get("status","run").lower() in ("disabled","notrun","skipped") or any(child.tag.rsplit("}",1)[-1] in ("failure","error","skipped") for child in case): bad.append((path,full,"case"))
assert len(names)==len(set(names)) and set(names)==expected and not bad,(names,expected,bad)
print(f"focused_xml_names_exact=PASS focused_executed={len(names)} focused_passed={len(names)} focused_bad=0")
""")' "$evidence_dir/all.runtime" "$evidence_dir/multi.xml" "$evidence_dir/account.xml"; ctest --test-dir build/OSX/Release -N -V -R '^(multi_account_test|account_management_test)$' | tee "$evidence_dir/ctest-selection.txt"; python3 -c 'import pathlib,re,sys; text=pathlib.Path(sys.argv[1]).read_text(encoding="utf-8"); names=re.findall(r"^\\s*Test\\s+#\\d+:\\s+(\\S+?)(?:\\s+\\(Disabled\\))?\\s*$",text,re.M); totals=re.findall(r"^Total Tests:\\s*(\\d+)\\s*$",text,re.M); assert totals==["2"] and len(names)==len(set(names))==2 and set(names)=={"multi_account_test","account_management_test"} and "(Disabled)" not in text,(names,totals); print("ctest_names_exact=PASS selected=2 disabled=0")' "$evidence_dir/ctest-selection.txt"; ctest --test-dir build/OSX/Release --output-on-failure --output-junit "$evidence_dir/ctest.xml" -R '^(multi_account_test|account_management_test)$'; python3 -c 'import pathlib,sys,xml.etree.ElementTree as E; root=E.parse(sys.argv[1]).getroot(); cases=list(root.iter("testcase")); names=[case.get("name") for case in cases]; bad=[case.get("name") for case in cases if case.get("status","run").lower() in ("disabled","notrun","skipped") or any(child.tag.rsplit("}",1)[-1] in ("failure","error","skipped") for child in case)]; attrs=[(node.tag,attr,node.get(attr)) for node in root.iter() if node.tag.rsplit("}",1)[-1] in ("testsuites","testsuite") for attr in ("failures","errors","skipped","disabled") if int(node.get(attr,"0"))]; assert len(cases)==2 and len(set(names))==2 and set(names)=={"multi_account_test","account_management_test"} and not bad and not attrs,(names,bad,attrs); print("junit_names_exact=PASS executed=2 passed=2 failed=0 skipped=0 disabled=0")' "$evidence_dir/ctest.xml"; python3 -c 'import pathlib,re,sys; wanted=set(sys.argv[1].split(",")); runtime=pathlib.Path(sys.argv[2]).read_text(encoding="utf-8").splitlines(); normalize=lambda raw: re.sub(r"^(?:DISABLED_)+","",raw.split(".",1)[0].split("/")[-1])+"."+re.sub(r"^(?:DISABLED_)+","",raw.split(".",1)[1].split("/",1)[0]); selected=[name for name in runtime if normalize(name) in wanted]; assert set(map(normalize,selected))==wanted and len(selected)==len(set(selected)); pathlib.Path(sys.argv[3]).write_text(":".join(sorted(selected)),encoding="utf-8"); pathlib.Path(sys.argv[4]).write_text("\\n".join(sorted(selected))+"\\n",encoding="utf-8")' "$repeat_semantic" "$evidence_dir/multi.runtime" "$evidence_dir/repeat.filter" "$evidence_dir/repeat.runtime"; repeat_filter="$(tr -d '\n' &lt; "$evidence_dir/repeat.filter")"; test -n "$repeat_filter"; for i in 1 2 3 4 5; do build/OSX/Release/test_bin/multi_account_test --gtest_filter="$repeat_filter" --gtest_output="xml:$evidence_dir/repeat-$i.xml"; python3 -c 'import pathlib,sys,xml.etree.ElementTree as E; expected=set(pathlib.Path(sys.argv[1]).read_text(encoding="utf-8").splitlines()); root=E.parse(sys.argv[2]).getroot(); cases=list(root.iter("testcase")); names=[case.get("classname")+"."+case.get("name") for case in cases]; bad=[name for name,case in zip(names,cases) if case.get("status","run").lower() in ("disabled","notrun","skipped") or any(child.tag.rsplit("}",1)[-1] in ("failure","error","skipped") for child in case)]; assert len(names)==len(set(names)) and set(names)==expected and not bad,(names,expected,bad)' "$evidence_dir/repeat.runtime" "$evidence_dir/repeat-$i.xml"; printf 'deterministic_lifecycle_repeat_%s=PASS\n' "$i"; done | tee "$evidence_dir/repeats.txt"; : &gt; "$evidence_dir/tsan-eligible.tsv"; : &gt; "$evidence_dir/tsan-records.txt"; while IFS= read -r cache; do tsan_build="${cache%/CMakeCache.txt}"; python3 -c 'exec("""import json,pathlib,re,shlex,shutil,subprocess,sys
tree=pathlib.Path(sys.argv[1]); specs={"multi_account_test":"multi_account_sync.cpp","account_management_test":"account_management_test.cpp"}
cc=tree/"compile_commands.json"
if not cc.is_file(): raise SystemExit(0)
entries=json.loads(cc.read_text(encoding="utf-8"))
def command(entry): return entry.get("command") or " ".join(entry.get("arguments",[]))
def tsan(text):
 return any(token.strip(chr(34)+chr(39)).startswith("-fsanitize=") and "thread" in token.strip(chr(34)+chr(39)).split("=",1)[1].split(",") for token in shlex.split(text))
def runtime(target):
 binary=tree/"test_bin"/target
 if not binary.is_file(): return ""
 out=[]
 for argv in (("otool","-L",str(binary)),("ldd",str(binary)),("readelf","-d",str(binary)),("nm","-u",str(binary)),("strings",str(binary))):
  if shutil.which(argv[0]):
   run=subprocess.run(argv,text=True,capture_output=True); out.extend((run.stdout,run.stderr))
 return "\\n".join(out)
def links(target):
 files=list(tree.glob("**/CMakeFiles/"+target+".dir/link.txt"))
 if files: return [p.read_text(encoding="utf-8") for p in files]
 if (tree/"build.ninja").is_file() and shutil.which("ninja"):
  run=subprocess.run(["ninja","-C",str(tree),"-t","commands",target],text=True,capture_output=True,check=True); output=re.compile(r"(?:^|\\s)-o\\s+\\S*test_bin/"+re.escape(target)+r"(?:\\s|$)"); return [line for line in run.stdout.splitlines() if output.search(line)]
 return []
for target,own_source in specs.items():
 own=[command(entry) for entry in entries if pathlib.Path(str(entry.get("file",""))).name==own_source and ("CMakeFiles/"+target+".dir/") in (str(entry.get("output",""))+" "+command(entry))]
 if not own or not all(tsan(item) for item in own): raise SystemExit(0)
 if not any(tsan(item) for item in links(target)) and not re.search(r"(?:lib(?:clang_rt\\.)?tsan|__tsan_)",runtime(target),re.I): raise SystemExit(0)
print(str(tree)+"|thread")
""")' "$tsan_build" &gt;&gt; "$evidence_dir/tsan-eligible.tsv"; done &lt; &lt;(find build -name CMakeCache.txt -type f | sort); sort -u "$evidence_dir/tsan-eligible.tsv" -o "$evidence_dir/tsan-eligible.tsv"; tsan_index=0; while IFS='|' read -r tsan_build tsan_kind; do test -n "$tsan_build" || continue; tsan_index=$((tsan_index+1)); cmake --build "$tsan_build" --target multi_account_test account_management_test -j8; "$tsan_build/test_bin/multi_account_test" --gtest_list_tests &gt; "$evidence_dir/tsan-$tsan_index-multi.list"; "$tsan_build/test_bin/account_management_test" --gtest_list_tests &gt; "$evidence_dir/tsan-$tsan_index-account.list"; python3 -c 'exec("""import pathlib,re,sys
expected=set(sys.argv[1].split(","))
def normalize(raw):
 suite,case=raw.split(".",1); return re.sub(r"^(?:DISABLED_)+","",suite.split("/")[-1])+"."+re.sub(r"^(?:DISABLED_)+","",case.split("/",1)[0])
def parse(path):
 suite=None; out=[]
 for raw in pathlib.Path(path).read_text(encoding="utf-8").splitlines():
  line=raw.strip()
  if not line or line.startswith("#"): continue
  if not raw[:1].isspace() and line.endswith("."): suite=line[:-1].split(" #",1)[0]
  elif suite is not None and raw[:1].isspace(): out.append(suite+"."+line.split(" #",1)[0])
 return out
for label,path,prefix in (("multi",sys.argv[2],"MultiAccountTest."),("account",sys.argv[3],"AccountManagement.")):
 selected=[name for name in parse(path) if normalize(name) in expected]; normalized={normalize(name) for name in selected}; wanted={name for name in expected if name.startswith(prefix)}; assert normalized==wanted and len(selected)==len(set(selected)),(label,normalized,wanted); pathlib.Path(sys.argv[4],"tsan-"+sys.argv[5]+"-"+label+".runtime").write_text("\\n".join(sorted(selected))+"\\n",encoding="utf-8"); pathlib.Path(sys.argv[4],"tsan-"+sys.argv[5]+"-"+label+".filter").write_text(":".join(sorted(selected)),encoding="utf-8")
all_names=pathlib.Path(sys.argv[4],"tsan-"+sys.argv[5]+"-multi.runtime").read_text().splitlines()+pathlib.Path(sys.argv[4],"tsan-"+sys.argv[5]+"-account.runtime").read_text().splitlines()
assert len(all_names)==len(set(all_names)) and set(map(normalize,all_names))==expected,(all_names,expected)
""")' "$expected_semantic" "$evidence_dir/tsan-$tsan_index-multi.list" "$evidence_dir/tsan-$tsan_index-account.list" "$evidence_dir" "$tsan_index"; tsan_multi_filter="$(tr -d '\n' &lt; "$evidence_dir/tsan-$tsan_index-multi.filter")"; tsan_account_filter="$(tr -d '\n' &lt; "$evidence_dir/tsan-$tsan_index-account.filter")"; "$tsan_build/test_bin/multi_account_test" --gtest_filter="$tsan_multi_filter" --gtest_output="xml:$evidence_dir/tsan-$tsan_index-multi.xml"; "$tsan_build/test_bin/account_management_test" --gtest_filter="$tsan_account_filter" --gtest_output="xml:$evidence_dir/tsan-$tsan_index-account.xml"; python3 -c 'import pathlib,sys,xml.etree.ElementTree as E; expected=set(pathlib.Path(sys.argv[1]).read_text().splitlines()+pathlib.Path(sys.argv[2]).read_text().splitlines()); names=[]; bad=[]; [(names.append(case.get("classname")+"."+case.get("name")),bad.append(case.get("name")) if case.get("status","run").lower() in ("disabled","notrun","skipped") or any(child.tag.rsplit("}",1)[-1] in ("failure","error","skipped") for child in case) else None) for path in sys.argv[3:] for case in E.parse(path).getroot().iter("testcase")]; assert len(names)==len(set(names)) and set(names)==expected and not bad,(names,expected,bad); print("tsan_cases_exact=PASS")' "$evidence_dir/tsan-$tsan_index-multi.runtime" "$evidence_dir/tsan-$tsan_index-account.runtime" "$evidence_dir/tsan-$tsan_index-multi.xml" "$evidence_dir/tsan-$tsan_index-account.xml"; printf 'sanitizer_tree=%s sanitizer_kind=thread multi_account_test_compile=PASS multi_account_test_link_or_runtime=PASS account_management_test_compile=PASS account_management_test_link_or_runtime=PASS phase14_cases=PASS\n' "$tsan_build" | tee -a "$evidence_dir/tsan-records.txt"; done &lt; "$evidence_dir/tsan-eligible.tsv"; sanitizer_configured="$(wc -l &lt; "$evidence_dir/tsan-records.txt" | tr -d '[:space:]')"; if test "$sanitizer_configured" -eq 0; then printf 'sanitizer_configured=0 sanitizer_status=NOT_RUN\n' | tee "$evidence_dir/sanitizer-status.txt"; else python3 -c 'import pathlib,re,sys; lines=pathlib.Path(sys.argv[1]).read_text(encoding="utf-8").splitlines(); expected=int(sys.argv[2]); rx=re.compile(r"^sanitizer_tree=\\S+ sanitizer_kind=thread multi_account_test_compile=PASS multi_account_test_link_or_runtime=PASS account_management_test_compile=PASS account_management_test_link_or_runtime=PASS phase14_cases=PASS$"); assert len(lines)==expected and expected&gt;0 and all(rx.fullmatch(line) for line in lines); print(f"sanitizer_configured={expected} sanitizer_status=PASS sanitizer_records_exact=PASS")' "$evidence_dir/tsan-records.txt" "$sanitizer_configured" | tee "$evidence_dir/sanitizer-status.txt"; fi; shasum -a 256 src/account/GeniusNode.hpp src/account/GeniusNode.cpp src/account/TransactionManager.hpp src/account/TransactionManager.cpp test/src/multiaccount/multi_account_sync.cpp test/src/account/account_management_test.cpp &gt; "$evidence_dir/source-after.sha256"; cmp "$evidence_dir/source-before.sha256" "$evidence_dir/source-after.sha256"; shasum -a 256 build/OSX/Release/test_bin/multi_account_test build/OSX/Release/test_bin/account_management_test &gt; "$evidence_dir/binaries.sha256"; printf 'phase14_exact_gate=PASS evidence_dir=%s\n' "$evidence_dir"</automated>
  </verify>
  <acceptance_criteria>
    - The chain starts with a fresh explicit two-target build, current own-source/object-to-binary checks, fresh lists/XML/JUnit, and unchanged source hashes through completion; stale binaries or reused evidence cannot pass.
    - Source semantics and fresh GoogleTest listing equal exactly sixteen unique Phase 14 names, every parameterized expansion is persisted and run exactly once, and focused XML equals the raw expansion set with zero bad status.
    - CTest enumeration and JUnit equal exactly `multi_account_test` and `account_management_test`; both complete binaries pass and no Phase 13 twenty-five-target substitution is used.
    - Five consecutive deterministic lifecycle repetitions each execute the same exact raw expansion set covering admission, drain, timeout, stale callback, cleanup, retired mutation/terminal attribution, and processing status, with no failed-run retry.
    - Every same-tree two-target TSan-eligible configuration has target-own compile and target-final link/runtime proof plus exact Phase 14 XML PASS; no eligible configuration is reported only as `sanitizer_configured=0 sanitizer_status=NOT_RUN`.
    - `14-04-SUMMARY.md` is absent until every command above exits zero, then records exact evidence and named dispositions for every upstream and closure HIGH threat without changing source, tests, CMake, or prior artifacts.
  </acceptance_criteria>
  <done>D-01 through D-16 have one fail-fast, exact, fresh, repeated, sanitizer-honest evidence chain over the two complete Phase 14 regression binaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test source to GoogleTest registration | Macro declarations, disabled prefixes, and parameter instantiations determine whether the intended semantic regressions actually exist and enumerate. |
| Build graph to executable evidence | Source/object changes cross into the two final binaries; stale or partially linked executables cannot represent current lifecycle code. |
| GoogleTest list/filter to XML | Selection may silently match zero, omit expansions, or overmatch similarly named cases unless raw name sets are compared exactly. |
| CTest selection to JUnit | A broad regex or count-only check may substitute unrelated targets or hide a disabled/not-run scoped binary. |
| Build configuration to sanitizer claim | Cache flags or unrelated instrumented targets do not prove either scoped test translation unit or final executable uses TSan. |
| Deterministic hooks to repeated concurrency evidence | Timing sleeps, polling accidents, or retrying a failed run can misrepresent flaky lifecycle races as stable safety. |
| Gate completion to summary | Partial logs or a pre-authored summary can claim closure before the complete fail-fast chain succeeds. |

## STRIDE Threat Register — HIGH Findings Are Blocking

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan / Blocking Evidence |
|-----------|----------|----------|-----------|-------------|-------------------------------------|
| T14-E01 | Repudiation / Tampering | HIGH | Semantic test inventory | mitigate | Exact sixteen-name source/list normalization, one active declaration each, exact parameter expansion accounting, and no extra Phase 14-family declaration. |
| T14-E02 | Tampering / Repudiation | HIGH | Build-to-binary provenance | mitigate | Fresh explicit build, own source/object mtime checks, fresh hashes, no-pending-relink confirmation, and unchanged source hash through gate completion. |
| T14-E03 | Spoofing / Repudiation | HIGH | Disabled, skipped, or zero-match tests | mitigate | Independently normalize disabled suite/case prefixes and require exact raw list/XML sets plus zero bad suite/case status; counts alone never pass. |
| T14-E04 | Repudiation | HIGH | ThreadSanitizer evidence | mitigate | Same-tree target-own translation-unit proof and target-final link/runtime proof for both exact targets; absence is exactly NOT_RUN. |
| T14-E05 | Denial of Service / Repudiation | HIGH | Lifecycle concurrency regressions | mitigate | No sleep in exact cases, named deterministic boundaries, and five uninterrupted exact subset repetitions with no retry after failure. |
| T14-E06 | Tampering | HIGH | Regression boundary substitution | mitigate | Exact two-name CTest enumeration/JUnit equality preserves both complete scoped binaries; additional targets require an identified changed consumer and plan revision. |
| T14-E07 | Repudiation | HIGH | Closure evidence publication | mitigate | Summary creation is sequenced after every structural, focused, broad, repeat, hash, and sanitizer assertion exits zero; partial evidence is never summarized as closure. |
| T14-E-SC | Tampering | LOW | npm/pip/cargo supply chain | accept | This plan installs no package and changes no dependency or lockfile. |
</threat_model>

<multi_source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan(s) | Status | Evidence in 14-04 |
|--------|----|-----------------------|---------|--------|-------------------|
| GOAL | Phase 14 | Publish only complete ready generations, close old admission, preserve accepted terminal work, and make failed/retired generations unavailable and non-mutable | 14-01, 14-02, 14-03, 14-04 | COVERED | Exact sixteen-case source/list/XML evidence, complete two-target CTest, and five deterministic repetitions. |
| REQ | D-01, D-02 | Accepted asynchronous selection and generation-tagged ready/failure events | 14-02, 14-03, 14-04 | COVERED | `SelectAccountReturnsGenerationBeforeReadyEvent`, cleanup/failure, stale-callback, and exact event inventories. |
| REQ | D-03, D-04 | Exact switching errors and overlap rejection without cancellation/queue/latest-wins | 14-02, 14-03, 14-04 | COVERED | `SwitchInProgressRejectsAccountCallsAndOverlap`, timed-out overlap rejection, and no queued/retry source guard. |
| REQ | D-05, D-06 | Close admission before acceptance and allow only pre-close work to terminalize | 14-01, 14-02, 14-04 | COVERED | Admission-boundary race, drain case, seven-surface mutation inventory, and five repeats. |
| REQ | D-07, D-08 | True drain before replacement and bounded timeout without old-work cancellation | 14-02, 14-03, 14-04 | COVERED | Drain/timeout/late-terminal cases, timeout escape guard, exact XML, and five repeats. |
| REQ | D-09, D-10, D-11, D-12 | Cleanup-first unavailable failure and explicit-only recovery with no republish | 14-02, 14-03, 14-04 | COVERED | Every-stage cleanup, configured identity, explicit recovery, no-retry/no-ready source guards, and full account binary. |
| REQ | D-13, D-14, D-15 | Permanent retired mutation denial, immutable diagnostics, and old-generation terminal attribution | 14-01, 14-03, 14-04 | COVERED | Three retired-manager regressions, exact mutation/continuation gate, timeout-late terminal, and five repeats. |
| REQ | D-16 | Lifecycle-specific generation-owned processing status | 14-02, 14-04 | COVERED | Processing-status regression, complete-owner snapshot guard, and five repeats. |
| RESEARCH | Two coupled state machines | Manager admission/terminal ledger plus node ready/retiring/pending generation bundles | 14-01, 14-02, 14-03, 14-04 | COVERED | Task 1 gates the critical links without redesigning production behavior. |
| RESEARCH | Deterministic validation architecture | Named admission/reservation/enqueue/drain/timeout/stale/publication/cleanup hooks; no sleeps | 14-01, 14-02, 14-03, 14-04 | COVERED | Brace-aware no-sleep/semantic structural checks and repeated exact runtime subset. |
| RESEARCH | Exact final evidence | Fresh two-target build, source/list/XML equality, complete scoped CTest, five repeats, target-proven TSan or NOT_RUN | 14-04 | COVERED | Task 2 is the uninterrupted fail-fast realization of the research/validation gate. |
| VALIDATION | 14-W0-01..14-W0-04 | Acceptance/event, switching/overlap, admission, and drain regressions | 14-01, 14-02, 14-04 | COVERED | Exact semantic declarations and runtime/XML evidence. |
| VALIDATION | 14-W0-05..14-W0-08 | Timeout, every-stage failure, explicit recovery, and exhaustive retired mutation | 14-01, 14-03, 14-04 | COVERED | Exact semantic/expansion evidence plus repeated timeout/cleanup/mutation cases. |
| VALIDATION | 14-W0-09..14-W0-11 | Immutable diagnostics, old terminal attribution, processing status, coherent snapshots, and stale completion | 14-01, 14-02, 14-04 | COVERED | Exact cases, partial-snapshot structural gate, full binaries, and repeated terminal/status/stale cases. |
| HIGH | T14-01..T14-05 | Retained authority, admission race, terminal attribution, lock/drain DoS, and post-close ingress | 14-01, 14-04 | COVERED | Mutation/admission/continuation structural gate and named manager cases in focused/full/repeat evidence. |
| HIGH | T14-PP, T14-SC, T14-ID, T14-OR, T14-PR, T14-DR | Partial publication, stale callbacks, identity leakage, overlap/reentrancy, processing race, and premature initialization | 14-02, 14-04 | COVERED | Complete-owner/stale/identity/overlap/status/drain cases plus exact full-target evidence. |
| HIGH | T14-DOS, T14-FC, T14-PM, T14-PL, T14-DE, T14-IR | Drain timeout, false cancellation, timeout race, partial cleanup, duplicate failure, and implicit retry/republish | 14-03, 14-04 | COVERED | Timeout/late terminal/every-stage cleanup/recovery cases, escape guards, and five repetitions. |
| HIGH | T14-E01..T14-E07 | Omission, stale linkage, disabled/zero-match, false sanitizer claim, flaky concurrency, target substitution, and premature evidence | 14-04 | COVERED | Tasks 1-2 fail closed on each evidence-layer threat and summary is post-success only. |
| CONTEXT | Deferred bridge ownership | Provider/relayer/catch-up watcher publication redesign | NONE | EXCLUDED | The gate neither changes ownership nor adds bridge tests. |
| CONTEXT | Deferred trust refresh | Refresh coalescing/retry-timer lifetime | NONE | EXCLUDED | No trust-controller source or target enters the Phase 14 boundary. |
| CONTEXT | Deferred repository authority | Raw `AtomicTransaction`, GlobalDB, and CRDT capability redesign | NONE | EXCLUDED | Structural checks are limited to the existing account-manager admission contract. |
</multi_source_coverage_audit>

<verification>
Task 1 must pass before Task 2 starts. Task 2 is one uninterrupted, fail-fast chain from fresh source hashes and explicit build through exact focused XML, both complete CTest targets, five exact repetitions, eligible TSan runs or honest NOT_RUN, and final unchanged-source comparison. Any source/test change, missing or renamed semantic case, duplicate or disabled counterpart, unknown/missing parameter expansion, zero-match filter, bad XML/JUnit status, stale target artifact, nonzero complete target, failed repetition, or incomplete TSan proof invalidates all evidence and restarts from Task 1. `14-04-SUMMARY.md` is written only after this chain exits zero.
</verification>

<success_criteria>
- D-01 through D-16 have exact source/list/XML evidence from all sixteen semantic regressions, including every parameterized runtime expansion and zero disabled/skipped/not-run/failing/duplicate/missing/extra result.
- The source gate proves the partial-snapshot `continue` blind spot is removed, exact cases use deterministic synchronization rather than sleeps, the manager mutation inventory remains closed, and timeout/failure code contains no cancellation/retry/republish escape.
- Fresh `multi_account_test` and `account_management_test` binaries both pass completely through exact two-target CTest/JUnit accounting.
- The deterministic lifecycle subset passes five consecutive exact repetitions covering admission, drain, timeout, stale callback, cleanup, retired mutation/terminal attribution, and processing status.
- TSan is PASS only with same-tree, both-target own-translation-unit and final-link/runtime proof plus exact Phase 14 XML; otherwise status is exactly NOT_RUN.
- Every HIGH threat from Plans 14-01 through 14-03 and T14-E01..T14-E07 has named passing evidence in the post-success summary, with all deferred scope still excluded.
</success_criteria>

<output>
Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-04-SUMMARY.md` only after every Task 1 and Task 2 gate exits zero.
</output>
