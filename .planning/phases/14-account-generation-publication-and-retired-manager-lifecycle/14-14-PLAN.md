---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "14"
type: execute
wave: 14
depends_on: [14-13]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/multiaccount/multi_account_sync.cpp
  - test/src/account/account_management_test.cpp
autonomous: true
requirements: [D-04, D-08, D-09, D-10, D-11, D-12, D-15]
must_haves:
  truths:
    - "A drain timeout fails only the accepted switch, leaves the node unavailable, starts no replacement work, and does not cancel admitted old work, per D-08."
    - "One explicit recovery may be accepted after timeout but cannot initialize until true old-generation drain; a further overlap returns SWITCH_IN_PROGRESS, per D-04/D-10/D-12."
    - "Initialization-stage failure cleans every pending owner before one generation-tagged failure event, per D-09/D-12."
  artifacts:
    - path: "src/account/GeniusNode.cpp"
      provides: "Timeout-versus-drain arbitration, cleanup-before-failure, owner/generation guards, and explicit recovery gating"
      contains: "FailPendingAccountGeneration"
    - path: "test/src/multiaccount/multi_account_sync.cpp"
      provides: "Deterministic timeout, late-terminal, and stage-cleanup lifecycle regressions"
      contains: "AccountGenerationDrainTimeoutFailsClosedWithoutCancellation"
  key_links:
    - from: "timeout and real manager drain"
      to: "pending recovery initialization"
      via: "generation plus expected-owner guard; drain is the only initializer gate"
      pattern: "HandleAccountSwitchDrainTimeout|HandleRetiredGenerationDrained"
---
<objective>After final node-shim removal, add deterministic timeout, cleanup-first failure, late terminal attribution, and explicit drain-gated recovery per D-04/D-08/D-09/D-10/D-11/D-12/D-15.</objective>
<execution_context>@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-13-SUMMARY.md
@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp
@src/account/TransactionManager.hpp
@src/account/TransactionManager.cpp
@test/src/multiaccount/multi_account_sync.cpp
@test/src/account/account_management_test.cpp</context>
<tasks>
<task type="auto">
  <name>Task 1: RED five deterministic timeout, late-terminal, cleanup, and recovery cases</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp</files>
  <action>Add exactly five active regressions with one unique intentional RED assertion marker each: `AccountGenerationDrainTimeoutFailsClosedWithoutCancellation` `[PHASE14-14-RED-TIMEOUT]`; `AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration` `[PHASE14-14-RED-LATE-TERMINAL]`; `AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage` `[PHASE14-14-RED-STAGE-CLEANUP]`; `TimedOutSwitchAcceptsOneExplicitRecoveryBehindRetireDrain` `[PHASE14-14-RED-QUEUED-RECOVERY]`; and `FailedSwitchRequiresExplicitRecovery` `[PHASE14-14-RED-EXPLICIT-RECOVERY]`. Extend the Plan 14-02 test seam with default-empty manual hooks `capture_drain_timeout`, `before_replacement_stage`, `fail_replacement_stage`, `after_pending_cleanup`, `before_switch_failure_event`, and `after_retired_drain_following_timeout`. Define `ReplacementInitializationStage` with exactly eight named table parameters—account construction, account configuration, blockchain construction, blockchain start, transaction-manager construction, transaction-manager start, processing construction, and processing start—stored in the named `kPhase14StageCleanupCases` registry; the stage-cleanup case must execute and report all eight parameters. Use named barriers/futures only—no sleep, polling cadence, incidental network failure, null dependency, or production deadline wait.

For D-08/D-15, admit and block old durable work, accept G, manually fire G timeout, prove one G-tagged failure and `ACCOUNT_UNAVAILABLE`, zero replacement stage entries, no fabricated cancellation/terminalization, and continued strong old-runtime ownership; after releasing old work, prove its one real terminal result retains the old manager generation. For D-04/D-10/D-12, accept exactly one explicit recovery G+1 behind unresolved drain, prove its target-only pending state cannot initialize any replacement owner until true old drain, then reject a third selection as `SWITCH_IN_PROGRESS`. For every injected replacement stage failure, prove reverse-order cleanup and all pending owners released before the one G-tagged failure event; stale timeout/drain/stage callbacks must be inert. Prove neither timeout nor stage failure auto-retries, restores old ready state, or uses a retry-only API. Add declarations/hooks only to compile the RED tests, retaining current behavior so each case fails at its own marked assertion.</action>
  <verify><automated>set -euo pipefail; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; d="$(mktemp -d /tmp/phase14-14-red.XXXXXX)"; bins=(multi_account_test multi_account_test multi_account_test account_management_test account_management_test); specs=(MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage AccountManagement.TimedOutSwitchAcceptsOneExplicitRecoveryBehindRetireDrain AccountManagement.FailedSwitchRequiresExplicitRecovery); marks=('[PHASE14-14-RED-TIMEOUT]' '[PHASE14-14-RED-LATE-TERMINAL]' '[PHASE14-14-RED-STAGE-CLEANUP]' '[PHASE14-14-RED-QUEUED-RECOVERY]' '[PHASE14-14-RED-EXPLICIT-RECOVERY]'); for i in 1 2 3 4 5; do xml="$d/$i.xml"; out="$d/$i.out"; set +e; build/OSX/Release/test_bin/${bins[$i]} --gtest_filter="${specs[$i]}" --gtest_output="xml:$xml" >"$out" 2>&1; rc=$?; set -e; test "$rc" -eq 1; test -s "$xml"; python3 -c 'import sys,xml.etree.ElementTree as E; p,s,m=sys.argv[1:]; root=E.parse(p).getroot(); cs=list(root.iter("testcase")); assert len(cs)==1; c=cs[0]; assert c.get("classname")+"."+c.get("name")==s; assert c.get("status","run").lower()=="run" and c.get("result","completed").lower() not in ("skipped","suppressed","notrun","disabled"); bad=[x for x in root.iter() if x.tag.rsplit("}",1)[-1] in ("error","skipped")]; fs=[x for x in root.iter() if x.tag.rsplit("}",1)[-1]=="failure"]; assert len(fs)==1 and not bad; assert all((x.get(k,"0") in ("0","0.0") for k in ("disabled","skipped","errors") if k in x.attrib) for x in root.iter()); payload=(fs[0].get("message") or "")+(fs[0].text or ""); assert m in payload' "$xml" "${specs[$i]}" "${marks[$i]}"; rg -F -q "${marks[$i]}" "$out"; if rg -n 'unknown file: Failure|C\+\+ exception|Segmentation fault|Bus error|Abort trap|AddressSanitizer|ThreadSanitizer|runtime error:|SetUp\(|TearDown\(|[Tt]imed out|No tests ran' "$out"; then exit 1; fi; done; printf 'phase14_13_exact_red_failures=PASS evidence_dir=%s\n' "$d"</automated></verify>
  <acceptance_criteria>
    - Each of the five requested tests runs exactly once, exits with the normal GoogleTest assertion-failure code, writes nonempty XML containing exactly one failure and no skipped, disabled, not-run, or error result, and fails at its own required marker.
    - Every focused output contains its required marker and contains no unrelated fatal, signal, crash, timeout, exception, sanitizer, SetUp, TearDown, or no-test-run marker.
    - The stage-cleanup test executes exactly the eight declared replacement-stage table parameters without sleep-based race proof.
  </acceptance_criteria>
  <done>Five active tests fail only as normal, marker-proven GoogleTest assertion failures for the missing timeout, attribution, cleanup, or recovery behavior.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN bounded failure with real drain and one explicit recovery</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp</files>
  <behavior>
    - "Timeout before real drain -> one failed G, unavailable, no cancellation or replacement initialization."
    - "Real drain after timeout -> old admitted terminal event retains old generation; only accepted explicit recovery may then initialize."
    - "Every replacement-stage failure -> cleanup before exactly one generation-tagged failure; no automatic retry or old republish."
  </behavior>
  <action>Implement the finite configurable `AccountSwitchDrainTimeout` and manual test scheduler seam. Use the Plan 14-01 manager `CloseAdmission` drain callback and admitted-operation ledger unchanged: timeout changes only the accepted switch request from waiting to failed, retains the retiring runtime, and never cancels, stops, or synthetic-terminalizes old durable work. Serialize timeout versus drain under the lifecycle mutex with the accepted generation and exact retiring owner identity; release the mutex before hooks/events/stops. `HandleRetiredGenerationDrained` must be the only route that releases the old runtime and starts an already-accepted target-only recovery. Timeout failure releases the active request slot so exactly one new explicit `SelectAccount` can accept a greater generation; it cannot create an automatic request, and another overlap is `SWITCH_IN_PROGRESS`.

Centralize every selected-replacement error through `FailPendingAccountGeneration`: generation/owner guard it, stop/unregister/release partial pending owners in reverse order, publish unavailable, then emit one `ACCOUNT_SWITCH_FAILED` only after cleanup. Bypass generic blockchain/migration retry helpers on this path; do not call ready publication or assign retired/failed bundles to ready. Late timeout, drain, initialization, cleanup, and event callbacks validate their generation and expected owner identity so they cannot clean or advance a later recovery. Preserve D-11 typed unavailable APIs and D-15 old terminal attribution.</action>
  <verify><automated>set -euo pipefail; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation:MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration:MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage'; build/OSX/Release/test_bin/account_management_test --gtest_filter='AccountManagement.TimedOutSwitchAcceptsOneExplicitRecoveryBehindRetireDrain:AccountManagement.FailedSwitchRequiresExplicitRecovery'; python3 -c 'import pathlib; s=pathlib.Path("src/account/GeniusNode.cpp").read_text(); assert "FailPendingAccountGeneration" in s and "HandleAccountSwitchDrainTimeout" in s and "HandleRetiredGenerationDrained" in s; assert "ScheduleBlockchainRetry" not in s[s.find("FailPendingAccountGeneration"):s.find("FailPendingAccountGeneration")+6000]'</automated></verify>
  <done>Timeout/failure is deterministic, cleanup-first and non-cancelling; one explicit recovery is accepted but waits for true old drain before initialization.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Timeout callback → retiring runtime | Timeout must not claim cancellation or permit replacement mutation. |
| Failure cleanup → observer event | Observers must never see a partial replacement. |
| Recovery request → true drain | Accepted recovery cannot initialize before old durable work is terminal. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-14-01 | Denial of Service | bounded drain timeout | mitigate | Manual deterministic timeout and retained old owner proof. |
| T14-14-02 | Tampering | stale callback / partial cleanup | mitigate | Generation-plus-owner guards and cleanup-before-event barriers. |
| T14-14-03 | Elevation / Tampering | automatic retry or old republish | mitigate | Explicit-only recovery and structural scan. |
| T14-SC | Tampering | package installs | accept | No package installation occurs. |
</threat_model>
<multi_source_coverage_audit>
## Multi-Source Coverage Audit
| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | failed-switch lifecycle | Unavailable failure, old terminal completion, safe recovery | 14-14 | COVERED | After node shim removal. |
| REQ | D-04/D-08/D-09/D-10/D-11/D-12/D-15 | Timeout, cleanup, explicit recovery, attribution | 14-14 | COVERED | Exact RED and GREEN tests. |
| RESEARCH | resolved timeout recovery | Accepted recovery is target-only until real drain | 14-14 | COVERED | Deterministic barrier. |
| CONTEXT | forbidden semantics | No cancellation, latest-wins, queue, retry-only API, automatic retry | NONE | EXCLUDED | Explicitly prohibited. |
</multi_source_coverage_audit>
<verification>Strict XML-attributed RED gates precede GREEN; focused five-case green evidence follows.</verification>
<success_criteria>All D-08/D-09/D-10 recovery semantics hold with deterministic timing and no scope expansion.</success_criteria>
<output>Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-14-SUMMARY.md` when done.</output>
