---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "13"
type: execute
wave: 13
depends_on: [14-12]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py
autonomous: true
requirements: [D-03, D-04, D-11, D-16]
must_haves:
  truths:
    - "Every node API caller has migrated before every temporary node shim is removed."
    - "Final account-bound APIs return typed switching/unavailable outcomes and configured identity is private."
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "Final typed account-bound public surface without node compatibility shims"
      contains: "outcome::result"
  key_links:
    - from: "complete node caller inventory"
      to: "final GeniusNode surface"
      via: "check-all-migrated before shim removal"
      pattern: "PHASE14_TEMP_NODE_LEGACY_SHIM"
---
<objective>Remove the node compatibility seam only after every caller group—production, fixtures, processing/transaction-sync, multi-account, E2E, race, node example, and processing-multi—has migrated.</objective>
<execution_context>@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-06-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-07-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-08-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-09-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-10-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-11-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-12-SUMMARY.md
@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp</context>
<tasks>
<task type="auto">
  <name>Task 1: Prove complete caller migration before the node API cut</name>
  <files>.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py</files>
  <action>Regenerate declaration/caller discovery and fail unless all owned partitions 14-06 through 14-12 are migrated with no unowned expression, stale old-shape expression, or missing target. Persist sorted source and target lists from `--check-all-migrated` for the removal build. Require the persisted lists to contain exactly the newly assigned pairs `example/node_test/NodeExample.cpp` → `node_example` and `test/src/processing_multi/processing_multi_test.cpp` → `processing_multi_test`; their absence returns work to 14-12. This deterministic barrier is mandatory: final node-shim removal may not repair a caller that should have been migrated by an earlier partition.</action>
  <verify><automated>set -euo pipefail; p=.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle; targets="$(mktemp /tmp/phase14-node-targets.XXXXXX)"; sources="$(mktemp /tmp/phase14-node-sources.XXXXXX)"; python3 "$p/14-node-api-caller-audit.py" --check-all-migrated --inventory "$p/14-02-CALLER-INVENTORY.tsv" --compile-commands build/OSX/Release/compile_commands.json --targets-output "$targets" --sources-output "$sources"; test -s "$targets"; test -s "$sources"; sort -cu "$targets"; sort -cu "$sources"; rg -Fx 'example/node_test/NodeExample.cpp' "$sources"; rg -Fx 'test/src/processing_multi/processing_multi_test.cpp' "$sources"; rg -Fx 'node_example' "$targets"; rg -Fx 'processing_multi_test' "$targets"</automated></verify>
  <done>The complete node caller union is exact and fully migrated before compatibility removal begins.</done>
</task>
<task type="auto">
  <name>Task 2: Remove node shims and build the derived full caller union</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv</files>
  <action>Only after Task 1, remove every declaration and definition marked `PHASE14_TEMP_NODE_LEGACY_SHIM`. Per D-03/D-11/D-16, make public `GetAddress` return `outcome::result&lt;std::string&gt;`, make `GetProcessingStatus` return `outcome::result&lt;GenerationProcessingStatus&gt;`, and make every other inventoried account-bound API return or propagate exact `SWITCH_IN_PROGRESS` or `ACCOUNT_UNAVAILABLE` rather than default/sentinel values. Retain configured/bootstrap identity only as a private helper and checked aliases only where they remain unambiguous. Re-run all-migrated discovery; freshly build its full target union and the configured default graph. Do not change timeout/recovery behavior, bridge ownership, trust lifecycle, or repository authority.</action>
  <verify><automated>set -euo pipefail; p=.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle; ! rg -n 'PHASE14_TEMP_NODE_LEGACY_SHIM|Legacy.*ForMigration|UNVAILABLE' src/account/GeniusNode.hpp src/account/GeniusNode.cpp; targets="$(mktemp /tmp/phase14-node-targets.XXXXXX)"; python3 "$p/14-node-api-caller-audit.py" --check-all-migrated --inventory "$p/14-02-CALLER-INVENTORY.tsv" --compile-commands build/OSX/Release/compile_commands.json --targets-output "$targets"; test -s "$targets"; xargs cmake --build build/OSX/Release --parallel 8 --target &lt; "$targets"; cmake --build build/OSX/Release --parallel 8; python3 -c 'import pathlib,re; h=pathlib.Path("src/account/GeniusNode.hpp").read_text(); s=pathlib.Path("src/account/GeniusNode.cpp").read_text(); assert re.search(r"outcome::result&lt;\\s*std::string\\s*&gt;\\s+GetAddress",h); assert re.search(r"outcome::result&lt;\\s*GenerationProcessingStatus\\s*&gt;\\s+GetProcessingStatus",h); public=h[h.find("public:"):h.find("protected:")]; assert "GetConfiguredAccountAddress" not in public and "GetConfiguredAccountAddress" in s'</automated></verify>
  <done>The final typed node surface has no legacy seam and every derived node caller target plus default graph builds freshly.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Temporary node seam → public API | A default-returning shim could keep unavailable state ambiguous. |
| Final header → all node callers | Removal is safe only after every inventory target rebuilds. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-13-01 | Tampering / Information Disclosure | legacy node surface | mitigate | Exact absence scan and typed final signatures. |
| T14-13-02 | Repudiation | caller union | mitigate | Regenerated all-migrated union and fresh default build. |
| T14-SC | Tampering | package installs | accept | No package installation occurs. |
</threat_model>
<multi_source_coverage_audit>
## Multi-Source Coverage Audit
| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | final typed node contract | No partial/unavailable identity leak | 14-13 | COVERED | Cut after all caller groups. |
| REQ | D-03/D-04/D-11/D-16 | Explicit switching, unavailable, status contracts | 14-13 | COVERED | Final API enforcement. |
| RESEARCH | complete caller migration | Full inventory target union precedes cut | 14-13 | COVERED | No caller repair allowed. |
| CONTEXT | Deferred scopes | No bridge/trust/GlobalDB/CRDT redesign | NONE | EXCLUDED | Strict scope fence. |
</multi_source_coverage_audit>
<verification>Full migrated inventory is a blocking precondition; afterward absence scan, derived target union, and default build must pass.</verification>
<success_criteria>Every node caller is migrated, the compatibility seam is absent, and the final public APIs are lifecycle-typed.</success_criteria>
<output>Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-13-SUMMARY.md` when done.</output>
