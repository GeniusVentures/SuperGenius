---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "15"
type: execute
wave: 15
depends_on: [14-14]
files_modified:
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-15-SUMMARY.md
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16]
must_haves:
  truths:
    - "Both caller manifests are independently regenerated, all manager and node target unions rebuild freshly, and the configured default graph rebuilds."
    - "The exact sixteen lifecycle contracts have source/list/XML/JUnit evidence with no disabled, missing, duplicate, extra, skipped, not-run, failing, or stale result."
    - "The post-timeout recovery remains explicit and target-only until true old-generation drain, with no cancellation, automatic retry, or republish escape."
  artifacts:
    - path: ".planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-15-SUMMARY.md"
      provides: "Read-only final caller-union, lifecycle, repetition, and sanitizer evidence"
  key_links:
    - from: "manager and node inventories"
      to: "fresh CMake union target builds"
      via: "independent audit regeneration and sorted union files"
      pattern: "check-all-migrated"
    - from: "fresh lifecycle binaries"
      to: "focused XML and complete CTest JUnit"
      via: "exact runtime-name equality"
      pattern: "ctest_names_exact=PASS"
---
<objective>Run the final, read-only Phase 14 closure gate after all caller partitions, final node shim removal, and timeout/recovery behavior are complete.</objective>
<execution_context>@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/ROADMAP.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-05-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-12-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-13-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-14-SUMMARY.md
@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp
@src/account/TransactionManager.hpp
@src/account/TransactionManager.cpp
@test/src/multiaccount/multi_account_sync.cpp
@test/src/account/account_management_test.cpp</context>
<tasks>
<task type="auto">
  <name>Task 1: Run read-only final surface and caller-union gates</name>
  <files>.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-15-SUMMARY.md</files>
  <action>Do not edit production or test code. Regenerate manager and node declaration/call manifests independently from their audit scripts, then require exact one-row migrated accounting and final public-surface equality. Require the node lists to contain `example/node_test/NodeExample.cpp`, `test/src/processing_multi/processing_multi_test.cpp`, `node_example`, and `processing_multi_test`; omission returns work to 14-12. Persist a Task 1 handoff in the declared `14-15-SUMMARY.md` artifact before Task 2 runs: a JSON-delimited Markdown record containing separate sorted manager/node source and target lists, sorted unique source/target unions, the exact hash-input list (every union caller source plus `GeniusNode.hpp/.cpp`, `TransactionManager.hpp/.cpp`, `multi_account_sync.cpp`, and `account_management_test.cpp`), and the pre-run SHA-256 record for each input. Task 2 must parse this persisted handoff and must not substitute a task-local pre-run hash. Require the manager final surface: no `PHASE14_TEMP_MANAGER_LEGACY_SHIM`, only immutable retired diagnostics, and every live mutation/read is typed or inaccessible. Require the node final surface: no `PHASE14_TEMP_NODE_LEGACY_SHIM`, private configured identity, typed address/processing status, and no sentinel contract. Structurally prove the timeout/failure path has no cancellation, synthetic terminalization, automatic retry, or ready/old-generation republish; `HandleRetiredGenerationDrained` remains the only recovery initializer after true drain. Freshly build every target in the union and the configured default graph; a manifest mismatch returns to its owning plan, not this gate.</action>
  <verify><automated>set -euo pipefail; p=.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle; summary="$p/14-15-SUMMARY.md"; d="$(mktemp -d /tmp/phase14-15-union.XXXXXX)"; python3 "$p/14-public-surface-audit.py" --check-final src/account/TransactionManager.hpp "$p/14-01-PUBLIC-SURFACE.tsv"; python3 "$p/14-manager-caller-audit.py" --check-all-migrated --inventory "$p/14-manager-caller-inventory.tsv" --compile-commands build/OSX/Release/compile_commands.json --targets-output "$d/manager.targets" --sources-output "$d/manager.sources"; python3 "$p/14-node-api-caller-audit.py" --check-all-migrated --inventory "$p/14-02-CALLER-INVENTORY.tsv" --compile-commands build/OSX/Release/compile_commands.json --targets-output "$d/node.targets" --sources-output "$d/node.sources"; for f in "$d/manager.targets" "$d/manager.sources" "$d/node.targets" "$d/node.sources"; do test -s "$f"; sort -cu "$f"; done; rg -Fx 'example/node_test/NodeExample.cpp' "$d/node.sources"; rg -Fx 'test/src/processing_multi/processing_multi_test.cpp' "$d/node.sources"; rg -Fx 'node_example' "$d/node.targets"; rg -Fx 'processing_multi_test' "$d/node.targets"; sort -u "$d/manager.targets" "$d/node.targets" >"$d/all.targets"; sort -u "$d/manager.sources" "$d/node.sources" >"$d/all.sources"; { cat "$d/all.sources"; printf '%s\n' src/account/GeniusNode.hpp src/account/GeniusNode.cpp src/account/TransactionManager.hpp src/account/TransactionManager.cpp test/src/multiaccount/multi_account_sync.cpp test/src/account/account_management_test.cpp; } | sort -u >"$d/hash.inputs"; xargs shasum -a 256 &lt;"$d/hash.inputs" >"$d/pre.sha"; ! rg -n 'PHASE14_TEMP_(MANAGER|NODE)_LEGACY_SHIM|Legacy.*ForMigration|UNVAILABLE' src/account/GeniusNode.hpp src/account/GeniusNode.cpp src/account/TransactionManager.hpp src/account/TransactionManager.cpp; xargs cmake --build build/OSX/Release --parallel 8 --target &lt; "$d/all.targets"; cmake --build build/OSX/Release --parallel 8; python3 -c 'import pathlib; s=pathlib.Path("src/account/GeniusNode.cpp").read_text(); a=s[s.find("HandleAccountSwitchDrainTimeout"):s.find("FailPendingAccountGeneration")+6000]; assert "ScheduleBlockchainRetry" not in a and "ScheduleMigrationRetry" not in a; assert "HandleRetiredGenerationDrained" in s and "FailPendingAccountGeneration" in s; print("phase14_final_surface_and_union=PASS")'; python3 -c 'import json,pathlib,sys; out=pathlib.Path(sys.argv[1]); base=pathlib.Path(sys.argv[2]); names=("manager.targets","manager.sources","node.targets","node.sources","all.targets","all.sources","hash.inputs","pre.sha"); data={n:base.joinpath(n).read_text().splitlines() for n in names}; out.write_text("# Phase 14 final evidence handoff\n\nTask 1 completed successfully. Task 2 must consume this exact pre-run record.\n\n```json\n"+json.dumps(data,indent=2)+"\n```\n")' "$summary" "$d"; test -s "$summary"; rg -F -q 'Task 1 completed successfully' "$summary"</automated></verify>
  <done>Manager/node manifests, final surfaces, sorted unions, union builds, default build, and scope-fence structural checks pass without source repair.</done>
</task>
<task type="auto">
  <name>Task 2: Run exact lifecycle evidence, repetitions, and honest sanitizer reporting</name>
  <files>.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-15-SUMMARY.md</files>
  <action>After Task 1 exits zero, first load and validate its persisted JSON handoff from `14-15-SUMMARY.md`; fail if it is absent, malformed, missing any manager/node union source or target list, or does not supply the pre-run hash-input list and SHA-256 record. Do not regenerate or replace that pre-run record. Then run one uninterrupted fail-fast evidence chain. Parse `TEST`, `TEST_F`, and `TEST_P` source declarations in `multi_account_sync.cpp` and `account_management_test.cpp`, parse both complete `--gtest_list_tests` outputs, and normalize parameterized suite/name expansions before comparing exact unique expected semantic sets. For the four manager, four existing node lifecycle, three timeout/cleanup/late-terminal, and five AccountManagement contracts, reject disabled, missing, extra, duplicate, zero-match, no-run, skipped, error, or failing cases; focused XML must match the same sixteen normalized names. The named stage-cleanup registry must prove exactly eight distinct replacement-stage parameters. Generate CTest JUnit with `--output-junit`, parse it, and require exactly the two selected CTest names with no disabled/skipped/error/failure state. Run the complete deterministic lifecycle subset exactly five consecutive times. Report ThreadSanitizer PASS only if both scoped targets' own test translation units and final executables prove same-tree TSan instrumentation; otherwise record exactly `sanitizer_configured=0 sanitizer_status=NOT_RUN`. Rehash exactly the persisted Task 1 hash inputs after all runs and byte-compare the post-run SHA-256 record to the persisted pre-run record. Only after every parse and hash comparison succeeds, append the exact source declaration names, gtest-list names, focused XML, generated CTest JUnit, five-run result, pre/post hashes, threat evidence, and sanitizer status to `14-15-SUMMARY.md`.</action>
  <verify><automated>set -euo pipefail; p=.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle; summary="$p/14-15-SUMMARY.md"; d="$(mktemp -d /tmp/phase14-15-evidence.XXXXXX)"; multi=build/OSX/Release/test_bin/multi_account_test; acct=build/OSX/Release/test_bin/account_management_test; python3 -c 'import json,pathlib,sys; t=pathlib.Path(sys.argv[1]).read_text(); assert "Task 1 completed successfully" in t; data=json.loads(t.split("```json\\n",1)[1].split("\\n```",1)[0]); required={"manager.targets","manager.sources","node.targets","node.sources","all.targets","all.sources","hash.inputs","pre.sha"}; assert set(data)==required and all(data[x] for x in required); d=pathlib.Path(sys.argv[2]); d.joinpath("hash.inputs").write_text("\\n".join(data["hash.inputs"])+"\\n"); d.joinpath("pre.sha").write_text("\\n".join(data["pre.sha"])+"\\n")' "$summary" "$d"; "$multi" --gtest_list_tests >"$d/multi.list"; "$acct" --gtest_list_tests >"$d/account.list"; filters='MultiAccountTest.AccountGenerationAdmissionBoundaryIsLinearizable:MultiAccountTest.RetiredManagerRejectsEveryMutationEntryPoint:MultiAccountTest.RetiredManagerDiagnosticsAreImmutable:MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration:MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization:MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion:MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle:MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent:MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation:MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration:MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage'; "$multi" --gtest_filter="$filters" --gtest_output="xml:$d/multi.xml"; "$acct" --gtest_filter='AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent:AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap:AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration:AccountManagement.TimedOutSwitchAcceptsOneExplicitRecoveryBehindRetireDrain:AccountManagement.FailedSwitchRequiresExplicitRecovery' --gtest_output="xml:$d/account.xml"; python3 -c 'import collections,pathlib,re,sys,xml.etree.ElementTree as E; expected=set("MultiAccountTest.AccountGenerationAdmissionBoundaryIsLinearizable MultiAccountTest.RetiredManagerRejectsEveryMutationEntryPoint MultiAccountTest.RetiredManagerDiagnosticsAreImmutable MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration AccountManagement.TimedOutSwitchAcceptsOneExplicitRecoveryBehindRetireDrain AccountManagement.FailedSwitchRequiresExplicitRecovery".split()); norm=lambda n:re.sub(r"/[^.]+(?=\\.|$)","",n); source="\\n".join(pathlib.Path(x).read_text() for x in sys.argv[1:3]); declared=[norm(a.strip()+"."+b.strip()) for a,b in re.findall(r"\\bTEST(?:_F|_P)?\\s*\\(\\s*([^,]+)\\s*,\\s*([^)]+)\\)",source)]; assert collections.Counter(x for x in declared if x in expected)==collections.Counter({x:1 for x in expected}); listed=[]; suite=None; [((globals().__setitem__("suite",line.strip()[:-1]) if not line[:1].isspace() and line.rstrip().endswith(".") else listed.append(norm(suite+"."+line.strip().split()[0])) if line[:1].isspace() and line.strip() and suite else None)) for path in sys.argv[3:5] for line in pathlib.Path(path).read_text().splitlines()]; assert collections.Counter(x for x in listed if x in expected)==collections.Counter({x:1 for x in expected}) and not any("DISABLED_" in x for x in listed); cases=[c for path in sys.argv[5:7] for c in E.parse(path).getroot().iter("testcase")]; names=[norm((c.get("classname") or "")+"."+(c.get("name") or "")) for c in cases]; assert len(cases)==16 and len(set(names))==16 and set(names)==expected; assert all(c.get("status","run").lower()=="run" and c.get("result","completed").lower() not in ("skipped","suppressed","notrun","disabled") and not [x for x in c if x.tag.rsplit("}",1)[-1] in ("failure","error","skipped")] for c in cases); stage=pathlib.Path(sys.argv[1]).read_text(); m=re.search(r"kPhase14StageCleanupCases\\s*=\\s*\\{(.*?)\\};",stage,re.S); assert m and len(set(re.findall(r"ReplacementInitializationStage::[A-Za-z_][A-Za-z0-9_]*",m.group(1))))==8; print("phase14_source_list_xml_exact=PASS")' test/src/multiaccount/multi_account_sync.cpp test/src/account/account_management_test.cpp "$d/multi.list" "$d/account.list" "$d/multi.xml" "$d/account.xml"; ctest --test-dir build/OSX/Release -R '^(multi_account_test|account_management_test)$' --output-on-failure --output-junit "$d/ctest.junit.xml"; python3 -c 'import sys,xml.etree.ElementTree as E; root=E.parse(sys.argv[1]).getroot(); cases=list(root.iter("testcase")); names=[c.get("name") for c in cases]; assert len(cases)==2 and len(set(names))==2 and set(names)=={"multi_account_test","account_management_test"}; assert all(not [x for x in c if x.tag.rsplit("}",1)[-1] in ("failure","error","skipped")] for c in cases); print("ctest_junit_names_exact=PASS")' "$d/ctest.junit.xml"; for i in 1 2 3 4 5; do "$multi" --gtest_filter="$filters"; "$acct" --gtest_filter='AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent:AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap:AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration:AccountManagement.TimedOutSwitchAcceptsOneExplicitRecoveryBehindRetireDrain:AccountManagement.FailedSwitchRequiresExplicitRecovery'; done; printf 'sanitizer_configured=0 sanitizer_status=NOT_RUN\n' >"$d/sanitizer.status"; rg -Fx 'sanitizer_configured=0 sanitizer_status=NOT_RUN' "$d/sanitizer.status"; xargs shasum -a 256 &lt;"$d/hash.inputs" >"$d/post.sha"; cmp -s "$d/pre.sha" "$d/post.sha"; python3 -c 'import pathlib,sys; summary=pathlib.Path(sys.argv[1]); paths=sys.argv[2:]; summary.write_text(summary.read_text()+"\n## Task 2 exact evidence\n\n"+"\n\n".join("### "+pathlib.Path(x).name+"\n```text\n"+pathlib.Path(x).read_text()+"\n```" for x in paths)+"\n")' "$summary" "$d/multi.list" "$d/account.list" "$d/multi.xml" "$d/account.xml" "$d/ctest.junit.xml" "$d/pre.sha" "$d/post.sha" "$d/sanitizer.status"; rg -F -q '## Task 2 exact evidence' "$summary"</automated></verify>
  <done>The complete closure gate has exact fresh caller, build, XML, CTest, five-repeat, source-integrity, and honest sanitizer evidence; summary exists only after success.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Audits → build graph | Caller omissions could hide behind a partial target set. |
| Source declaration → runtime XML/JUnit | Renamed, disabled, stale, or substituted tests could falsify evidence. |
| Timeout/recovery code → lifecycle contract | Cancellation/retry/republish escapes violate safety. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-15-01 | Tampering / Repudiation | manifest union provenance | mitigate | Independent regenerated lists, sorted union, fresh build, hashes. |
| T14-15-02 | Repudiation | exact lifecycle evidence | mitigate | Source/list/XML/JUnit name equality and five repeats. |
| T14-15-03 | Denial / Tampering | timeout recovery semantics | mitigate | Structural scope scan and exact recovery tests. |
| T14-15-04 | Repudiation | sanitizer claim | mitigate | PASS requires target-own and executable evidence; otherwise exact NOT_RUN. |
| T14-SC | Tampering | package installs | accept | No package installation occurs. |
</threat_model>
<multi_source_coverage_audit>
## Multi-Source Coverage Audit
| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | complete lifecycle closure | Publication, drain, retirement, unavailable failure | 14-15 | COVERED | Read-only proof after implementation. |
| REQ | D-01..D-16 | All locked lifecycle decisions | 14-01..14-15 | COVERED | Exact final evidence enumerates all 16. |
| RESEARCH | final caller/target union | Independent manager/node audits and fresh union build | 14-15 | COVERED | No final-gate repair. |
| RESEARCH | deterministic validation | XML/JUnit/name equality, repetitions, honest sanitizer | 14-15 | COVERED | Fail-fast chain. |
| CONTEXT | Deferred ideas | No bridge/trust/repository authority redesign | NONE | EXCLUDED | Gate is read-only. |
</multi_source_coverage_audit>
<verification>Task 1 must pass before Task 2; any mismatch invalidates evidence and returns work to its owning plan.</verification>
<success_criteria>Final closure independently regenerates both caller unions, rebuilds their complete union and default graph, and records exact lifecycle evidence without mutating source.</success_criteria>
<output>Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-15-SUMMARY.md` only after both tasks exit zero.</output>
