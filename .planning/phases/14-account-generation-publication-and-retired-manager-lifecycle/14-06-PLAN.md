---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "06"
type: execute
wave: 6
depends_on: [14-05]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/node/node_initialization_progress.cpp
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-11, D-16]
must_haves:
  truths:
    - "Production and startup code use private configured-bootstrap identity before readiness and typed active-ready results after it, per D-03/D-11."
    - "Every migrated production/startup caller is target-resolved and freshly compiled."
  artifacts:
    - path: ".planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv"
      provides: "Complete 14-06 production/startup caller rows"
      contains: "14-06"
  key_links:
    - from: "GeniusNode startup code"
      to: "private configured identity and RequireReadyAccountGeneration"
      via: "classified manifest rows"
      pattern: "configured-bootstrap|active-ready"
---

<objective>
Migrate only production and startup GeniusNode callers after the manager seam is closed, per D-01/D-02/D-03/D-04/D-11/D-16.

Purpose: Preserve valid pre-ready bootstrap identity without exposing it as active state, while eliminating default/sentinel lifecycle interpretation in production code.
Output: Migrated production/startup rows and fresh builds of genius_node, genius_node_test, and node_initialization_progress.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-05-SUMMARY.md
@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp
@test/src/node/node_initialization_progress.cpp
</context>

<tasks>
<task type="auto">
  <name>Task 1: Lock production and startup caller rows to their concrete targets</name>
  <files>.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py</files>
  <action>Rerun complete account-bound declaration/call discovery. Per D-03/D-11, require every call in `src/account/GeniusNode.cpp` and `test/src/node/node_initialization_progress.cpp` to have exactly one `owner_plan=14-06` row and resolve it through compile_commands to `genius_node`, `genius_node_test`, or `node_initialization_progress`. Classify startup port, trust, configuration, and diagnostics as `configured-bootstrap`; classify operations and post-ready observation as `active-ready`; classify selection as `lifecycle-selection`. Exclude comments with the audit parser; do not assign account/blockchain fixtures to this plan.</action>
  <verify><automated>set -euo pipefail; p=.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle; python3 "$p/14-node-api-caller-audit.py" --check-current --inventory "$p/14-02-CALLER-INVENTORY.tsv" --compile-commands build/OSX/Release/compile_commands.json; python3 "$p/14-node-api-caller-audit.py" --check-owner 14-06 --inventory "$p/14-02-CALLER-INVENTORY.tsv" --sources src/account/GeniusNode.cpp,test/src/node/node_initialization_progress.cpp --required-targets genius_node,genius_node_test,node_initialization_progress</automated></verify>
  <done>Every production/startup expression is single-owned, target-resolved, and identity-classified before edits.</done>
</task>
<task type="auto">
  <name>Task 2: Migrate production and startup callers to explicit lifecycle contracts</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/node/node_initialization_progress.cpp, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv</files>
  <action>Consume every 14-06 row. Per D-03/D-11/D-16, route pre-ready `InitNetwork`, port derivation, authorized-node bootstrap, trust-policy construction, and startup diagnostics through the private configured identity; active account operations must call or propagate `RequireReadyAccountGeneration` before validation or side effects and return exact `SWITCH_IN_PROGRESS` or `ACCOUNT_UNAVAILABLE`. Unpack typed address, balance, history, manager, wait, and processing-status results before use. Mark rows migrated. Do not alter TrustStartupController timers, bridge ownership, transaction/GlobalDB/CRDT capabilities, or selection/recovery state-machine behavior reserved for 14-13.</action>
  <verify><automated>set -euo pipefail; p=.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle; python3 "$p/14-node-api-caller-audit.py" --check-migrated --owner 14-06 --inventory "$p/14-02-CALLER-INVENTORY.tsv" --compile-commands build/OSX/Release/compile_commands.json; cmake --build build/OSX/Release --target genius_node genius_node_test node_initialization_progress -j8; python3 -c 'import pathlib; h=pathlib.Path("src/account/GeniusNode.hpp").read_text(); s=pathlib.Path("src/account/GeniusNode.cpp").read_text(); public=h[h.find("public:"):h.find("protected:")]; assert "GetConfiguredAccountAddress" not in public and "GetConfiguredAccountAddress" in s; assert "SWITCH_IN_PROGRESS" in h and "ACCOUNT_UNAVAILABLE" in h'</automated></verify>
  <done>Production/startup callers compile against configured-bootstrap or typed active-ready contracts without exposing unavailable state as active.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Bootstrap configuration → public account API | Configured identity must never be presented as active-ready identity. |
| Production caller → lifecycle result | Default values could hide switching or unavailable state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-06-01 | Spoofing / Information Disclosure | configured identity | mitigate | Private helper plus manifest identity classification. |
| T14-06-02 | Tampering | lifecycle result use | mitigate | Checked-result migration and exact target builds. |
| T14-SC | Tampering | package installs | accept | No package installation occurs. |
</threat_model>

<multi_source_coverage_audit>
## Multi-Source Coverage Audit
| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | node caller migration | No unavailable/partial identity leak | 14-06 | COVERED | Production/startup partition. |
| REQ | D-01/D-02/D-03/D-04/D-11/D-16 | Async lifecycle results and status ownership | 14-06 | COVERED | Exact caller classifications and builds. |
| RESEARCH | configured identity separation | Pre-ready setup is private configured identity | 14-06 | COVERED | Final node cut remains later. |
| CONTEXT | Deferred scopes | No bridge, trust, or repository-authority redesign | NONE | EXCLUDED | Explicit scope fence. |
</multi_source_coverage_audit>
<verification>Current/owner audit precedes edits; migrated audit and fresh exact target builds follow.</verification>
<success_criteria>All production/startup calls use explicit identity and lifecycle semantics, and the three owning targets build freshly.</success_criteria>
<output>Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-06-SUMMARY.md` when done.</output>
