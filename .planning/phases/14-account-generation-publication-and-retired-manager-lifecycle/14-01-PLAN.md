---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/account/TransactionManager.hpp
  - src/account/TransactionManager.cpp
  - test/src/multiaccount/multi_account_sync.cpp
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-PUBLIC-SURFACE.tsv
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-public-surface-audit.py
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-audit.py
autonomous: true
requirements: [D-05, D-06, D-13, D-14, D-15]
must_haves:
  truths:
    - "Admission and closure share one linearization point: work admitted before close can terminalize, while later mutation returns MANAGER_RETIRED without side effects, per D-05/D-06/D-13."
    - "A retiring manager drains only when its durable admitted-operation ledger reaches zero and preserves exactly-once terminal results under its immutable generation, per D-06/D-15."
    - "Retirement freezes value-only diagnostics; temporary compatibility wrappers are explicitly marked PHASE14_TEMP_MANAGER_LEGACY_SHIM and cannot bypass admission."
  artifacts:
    - path: "src/account/TransactionManager.hpp"
      provides: "ManagerLifecycle, MANAGER_RETIRED, checked/internal lifecycle APIs, admitted-operation and immutable retirement contracts"
      contains: "MANAGER_RETIRED"
    - path: "src/account/TransactionManager.cpp"
      provides: "Linearizable admission, terminal ledger, drain notification, generation-frozen completion"
      contains: "TryAdmit"
    - path: ".planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv"
      provides: "Every changed TransactionManager method call expression, source, target, migration owner, and disposition"
      contains: "method\texpression\tsource\ttargets\towner_plan\tdisposition"
  key_links:
    - from: "TransactionManager client mutation entry points"
      to: "TransactionManager::TryAdmit"
      via: "one move-only admission record before first mutation"
      pattern: "TryAdmit"
    - from: "canonical terminal state transition"
      to: "CompleteAdmittedOperation"
      via: "transaction ID and immutable manager generation after state locks release"
      pattern: "CompleteAdmittedOperation"
---

<objective>
Implement the manager lifecycle core for D-05, D-06, D-13, D-14, and D-15 without prematurely privatizing or changing every caller-facing signature.

Purpose: Establish the safety state machine and deterministic evidence first, while a mechanically inventoried, explicitly temporary compatibility seam keeps unmigrated targets compilable until Plans 14-03 through 14-05 consume every caller.
Output: Manager lifecycle core, four deterministic tests, exact public-surface and caller/target inventories, and checked/internal APIs ready for staged migration.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
@src/account/TransactionManager.hpp
@src/account/TransactionManager.cpp
@test/src/multiaccount/multi_account_sync.cpp
@build/OSX/Release/compile_commands.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED manager admission, drain, retirement, and old-generation attribution</name>
  <files>src/account/TransactionManager.hpp, src/account/TransactionManager.cpp, test/src/multiaccount/multi_account_sync.cpp, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-PUBLIC-SURFACE.tsv, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-public-surface-audit.py</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/multiaccount/CMakeLists.txt
  </read_first>
  <action>Add exactly the active tests `AccountGenerationAdmissionBoundaryIsLinearizable`, `RetiredManagerRejectsEveryMutationEntryPoint`, `RetiredManagerDiagnosticsAreImmutable`, and `RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration` with markers `[PHASE14-01-RED-ADMISSION]`, `[PHASE14-01-RED-PUBLIC-SURFACE]`, `[PHASE14-01-RED-DIAGNOSTICS]`, and `[PHASE14-01-RED-ATTRIBUTION]`. Use named condition-variable/future barriers at before-admission, after-admission, before-first-mutation, reservation, enqueue, terminalization, and drain-zero boundaries; no sleeps or callbacks under production locks. Create a comment/string/brace-aware public declaration audit and inventory every current TransactionManager signature exactly once as external mutation, internal continuation/control, active-only read, static utility, or retired-safe snapshot read. Add only the final lifecycle types and hooks needed to compile the RED tests; leave behavior failing at the marked assertions.</action>
  <verify>
    <automated>set -euo pipefail; cmake --build build/OSX/Release --target multi_account_test -j8; red_dir="$(mktemp -d /tmp/phase14-01-red.XXXXXX)"; specs=(MultiAccountTest.AccountGenerationAdmissionBoundaryIsLinearizable MultiAccountTest.RetiredManagerRejectsEveryMutationEntryPoint MultiAccountTest.RetiredManagerDiagnosticsAreImmutable MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration); markers=('[PHASE14-01-RED-ADMISSION]' '[PHASE14-01-RED-PUBLIC-SURFACE]' '[PHASE14-01-RED-DIAGNOSTICS]' '[PHASE14-01-RED-ATTRIBUTION]'); for i in 1 2 3 4; do spec="${specs[$i]}"; marker="${markers[$i]}"; stem="$(printf '%s' "$spec" | tr '.' '_')"; xml="$red_dir/$stem.xml"; out="$red_dir/$stem.out"; set +e; build/OSX/Release/test_bin/multi_account_test --gtest_filter="$spec" --gtest_output="xml:$xml" &gt;"$out" 2&gt;&amp;1; rc=$?; set -e; test "$rc" -eq 1; test -s "$xml"; python3 -c 'import sys,xml.etree.ElementTree as E; path,spec,marker=sys.argv[1:]; root=E.parse(path).getroot(); cases=list(root.iter("testcase")); assert len(cases)==1; case=cases[0]; assert case.get("classname")+"."+case.get("name")==spec; assert case.get("status","run").lower()=="run" and case.get("result","completed").lower() not in ("skipped","suppressed","notrun","disabled"); failures=[c for c in case if c.tag.rsplit("}",1)[-1]=="failure"]; forbidden=[c.tag for c in case if c.tag.rsplit("}",1)[-1] in ("error","skipped")]; assert len(failures)==1 and not forbidden; payload=(failures[0].get("message") or "")+(failures[0].text or ""); assert marker in payload' "$xml" "$spec" "$marker"; rg -F -q "$marker" "$out"; if rg -n 'unknown file: Failure|C\+\+ exception|Segmentation fault|Bus error|Abort trap|AddressSanitizer|ThreadSanitizer|runtime error:|SetUp\(|TearDown\(|[Tt]imed out|No tests ran' "$out"; then exit 1; fi; done; printf 'phase14_01_exact_red_failures=PASS evidence_dir=%s\n' "$red_dir"</automated>
  </verify>
  <acceptance_criteria>
    - Each exact test runs once, exits with the normal GoogleTest failure code, writes nonempty XML with exactly one intended failure and its plan marker, and has no crash/setup/timeout failure.
    - The public-surface inventory has no missing, duplicate, or unclassified signature.
    - Deterministic barriers observe admission, mutation, terminal, and drain boundaries without wall-clock race proof.
  </acceptance_criteria>
  <done>The four RED cases expose the pre-core lifecycle defects and the declaration surface is mechanically fixed for subsequent migration accounting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN the manager lifecycle core and derive every changed caller target</name>
  <files>src/account/TransactionManager.hpp, src/account/TransactionManager.cpp, test/src/multiaccount/multi_account_sync.cpp, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-PUBLIC-SURFACE.tsv, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-public-surface-audit.py, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-audit.py</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - build/OSX/Release/compile_commands.json
  </read_first>
  <behavior>
    - "Admission before close -> durable record may reserve, enqueue, and terminalize; admission after close -> MANAGER_RETIRED and zero side effects."
    - "Last real terminalization -> one immutable retirement snapshot and one drain callback after unlock."
    - "Competing terminal callbacks -> one result tagged with the admitting manager generation."
  </behavior>
  <action>Implement `ManagerLifecycle {ACTIVE,DRAINING,RETIRED}`, `MANAGER_RETIRED`, immutable generation, move-only `AdmittedOperation`, `TryAdmit`, transfer-to-ledger, admitted-continuation validation, idempotent terminalization, `CloseAdmission`, and value-only `RetirementSnapshot` per D-05/D-06/D-13/D-14/D-15. All seven mutation/submission paths (`TransferFunds`, `MintFunds`, `MigrationFunds`, `HoldEscrow`, `PayEscrow`, `AsyncPayEscrow`, `SubmitTransaction`) acquire one admission before first mutation; enqueue transfers that record and never reacquires. New ingress closes; correlated continuations may finish. Never nest the admission mutex with queue/state/wait/account locks or invoke callbacks/posts under it.

Create `14-manager-caller-audit.py` and `14-manager-caller-inventory.tsv` from the current declarations plus comment/string-aware call-expression discovery across `src/`, `test/`, apps/examples, and `compile_commands.json`. Header consumers map to all including `CMakeFiles/&lt;target&gt;.dir` owners. The TSV header is `method\texpression\tsource\ttargets\towner_plan\tdisposition`. It must include the known non-self callers in `src/account/GeniusNode.cpp` (`genius_node`, `genius_node_test`), `src/account/BridgeRelayer.cpp` (`genius_node`, `genius_node_test`), `src/migration/Migration3_6_0To3_7_0.cpp` (`migration`), `burnconfig_policy_e2e_test.cpp`, `transaction_manager_pending_lifecycle_test.cpp`, `consensus_subject_test.cpp`, `multi_account_sync.cpp`, `policy_lifetime_multi_account_test.cpp`, and `bridge_race_fault_rpc_test.cpp`; discovery may add rows but may not omit a resolved compile-time caller. Assign every row to exactly one of Plans 14-03, 14-04, or 14-05.

Preserve only compile-sequencing wrappers needed by those rows and tag every declaration/definition `PHASE14_TEMP_MANAGER_LEGACY_SHIM`; each wrapper delegates to the checked lifecycle boundary and cannot expose new authority. Do not privatize/remove them here. Plans 14-03/04 migrate their owned rows; Plan 14-05 migrates the last rows and removes the seam.</action>
  <verify>
    <automated>set -euo pipefail; cmake --build build/OSX/Release --target multi_account_test -j8; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.AccountGenerationAdmissionBoundaryIsLinearizable:MultiAccountTest.RetiredManagerRejectsEveryMutationEntryPoint:MultiAccountTest.RetiredManagerDiagnosticsAreImmutable:MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration'; python3 .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-public-surface-audit.py --check-core src/account/TransactionManager.hpp .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-PUBLIC-SURFACE.tsv; python3 .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-audit.py --write-inventory .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv --compile-commands build/OSX/Release/compile_commands.json; python3 .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-audit.py --check-assigned --inventory .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv --owners 14-03,14-04,14-05; for fn in TransferFunds MintFunds MigrationFunds HoldEscrow PayEscrow AsyncPayEscrow SubmitTransaction; do rg -U -P -q "TransactionManager::${fn}\\b[\\s\\S]{0,1800}?TryAdmit\\(" src/account/TransactionManager.cpp; done; rg -n 'MANAGER_RETIRED|ManagerLifecycle|RetirementSnapshot|CompleteAdmittedOperation|PHASE14_TEMP_MANAGER_LEGACY_SHIM' src/account/TransactionManager.{hpp,cpp}</automated>
  </verify>
  <acceptance_criteria>
    - All four manager lifecycle cases pass and D-05/D-06/D-13/D-14/D-15 behavior is implemented in the core.
    - Every changed declaration has every compile-time caller and concrete target in the TSV with exactly one migration owner.
    - Temporary wrappers are explicitly marked, lifecycle-gated, and present only for compile sequencing; final public-surface enforcement is deferred to Plan 14-05.
  </acceptance_criteria>
  <done>The manager lifecycle core is deterministic and safe, and every caller that must move before final privatization is mechanically assigned to a build-owning migration plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retained caller → manager | A stale shared owner may attempt mutation after retirement. |
| Async continuation → terminal ledger | Only work admitted before closure may progress to one terminal result. |
| Header contract → build graph | Signature/access changes affect every compile-time caller and target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-MC1 | Tampering / Elevation of Privilege | retained manager APIs | mitigate | One admission gate plus exhaustive public/caller inventories and typed MANAGER_RETIRED tests. |
| T14-MC2 | Repudiation | terminal attribution | mitigate | Immutable manager generation and idempotent terminal ledger. |
| T14-MC3 | Denial of Service | lock/callback boundary | mitigate | Explicit lock order and callbacks after unlock, proven by deterministic barriers. |
| T14-SC | Tampering | package installs | accept | No package or dependency installation occurs. |
</threat_model>

<multi_source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | Phase 14 manager core | Close old admission, preserve admitted work, retire without mutable authority | 14-01 | COVERED | Core half of the phase goal. |
| REQ | D-05/D-06 | Linearizable close and true terminal drain | 14-01 | COVERED | Admission token plus durable ledger. |
| REQ | D-13/D-14/D-15 | Permanent retired denial, immutable diagnostics, old-generation terminal events | 14-01, 14-05 | COVERED | Core behavior here; final public seam removal in 14-05. |
| RESEARCH | Complete public/caller inventories | Classify declarations and map every changed call to concrete CMake targets | 14-01 | COVERED | Two mechanical inventories are blocking. |
| CONTEXT | Deferred bridge ownership / trust timers / repository authority | No redesign | NONE | EXCLUDED | Caller adaptation only; no deferred subsystem ownership changes. |
</multi_source_coverage_audit>

<verification>
Run the exact RED evidence before GREEN, then require all four focused cases, the public core audit, and complete caller/target assignment to pass. No missing caller may be deferred to the final gate.
</verification>

<success_criteria>
- D-05/D-06 manager admission and terminal drain are linearizable and deterministic.
- D-13/D-14/D-15 behavior exists behind checked APIs, while every temporary wrapper is named and inventoried for removal.
- Every affected compile-time caller and concrete build target is assigned to Plans 14-03 through 14-05.
</success_criteria>

<output>
Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md` when done.
</output>
