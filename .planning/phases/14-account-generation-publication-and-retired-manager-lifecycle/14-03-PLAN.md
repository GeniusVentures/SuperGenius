---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "03"
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - src/account/GeniusNode.cpp
  - src/account/BridgeRelayer.cpp
  - src/migration/Migration3_6_0To3_7_0.cpp
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-audit.py
autonomous: true
requirements: [D-05, D-06, D-13, D-14, D-15]
must_haves:
  truths:
    - "Every production TransactionManager caller uses checked mutation/read or private lifecycle capabilities and propagates MANAGER_RETIRED where externally observable."
    - "GeniusNode closes admission before accepted selection and never bypasses admission through direct enqueue or mutable validator ownership."
  artifacts:
    - path: ".planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv"
      provides: "Migrated dispositions and concrete targets for production manager callers"
      contains: "14-03"
  key_links:
    - from: "GeniusNode::SendTransactionAndProof"
      to: "TransactionManager::SubmitTransaction"
      via: "typed checked submission"
      pattern: "SubmitTransaction"
---

<objective>
Migrate the production callers of changed TransactionManager contracts before any legacy entry point is removed.

Purpose: Preserve compile sequencing while making GeniusNode, BridgeRelayer, and the migration engine consume the lifecycle core explicitly.
Output: Production caller rows marked migrated and fresh `genius_node`, `genius_node_test`, and `migration` builds.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv
@src/account/GeniusNode.cpp
@src/account/BridgeRelayer.cpp
@src/migration/Migration3_6_0To3_7_0.cpp
@src/account/TransactionManager.hpp
@build/OSX/Release/compile_commands.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile production caller rows with declarations and owning targets</name>
  <files>.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-audit.py</files>
  <read_first>
    - src/account/TransactionManager.hpp
    - src/account/GeniusNode.cpp
    - src/account/BridgeRelayer.cpp
    - src/migration/Migration3_6_0To3_7_0.cpp
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv
    - build/OSX/Release/compile_commands.json
  </read_first>
  <action>Rerun declaration-driven caller discovery and fail on inventory drift. Require every changed call expression in `GeniusNode.cpp`, `BridgeRelayer.cpp`, and `Migration3_6_0To3_7_0.cpp` to have one `owner_plan=14-03` row. Map `GeniusNode.cpp` and `BridgeRelayer.cpp` to both `genius_node` and `genius_node_test`, and the migration source to `migration`. Preserve rows owned by later plans unchanged; an unassigned or duplicate call is blocking.</action>
  <verify>
    <automated>set -euo pipefail; python3 .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-audit.py --check-current --inventory .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv --compile-commands build/OSX/Release/compile_commands.json; python3 .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-audit.py --check-owner 14-03 --inventory .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv --sources src/account/GeniusNode.cpp,src/account/BridgeRelayer.cpp,src/migration/Migration3_6_0To3_7_0.cpp --required-targets genius_node,genius_node_test,migration</automated>
  </verify>
  <acceptance_criteria>
    - Every production call expression affected by manager access/signature changes has exactly one row and concrete target set.
    - No row assigned to 14-04 or 14-05 is changed or declared migrated.
  </acceptance_criteria>
  <done>The production partition is mechanically complete before edits begin.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate production callers and freshly build their targets</name>
  <files>src/account/GeniusNode.cpp, src/account/BridgeRelayer.cpp, src/migration/Migration3_6_0To3_7_0.cpp, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - src/account/TransactionManager.hpp
    - src/account/GeniusNode.cpp
    - src/account/BridgeRelayer.cpp
    - src/migration/Migration3_6_0To3_7_0.cpp
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv
  </read_first>
  <action>Consume every `14-03` row. In GeniusNode, use private lifecycle capabilities for start/stop/callback registration/validator configuration and checked result APIs for active reads, waits, mint/transfer/escrow, and submission; `SendTransactionAndProof` returns and propagates the checked `SubmitTransaction` result and never calls `EnqueueTransaction`. Bind manager construction and terminal delivery to the immutable account generation, close old admission outside the node mutex before selection acceptance, and do not stop before real drain. In BridgeRelayer, adapt only `MintFunds` result propagation—no provider/relayer ownership redesign. In Migration3_6_0To3_7_0, use its explicit friend/capability for startup controls and checked reads/migration submission/wait/stop. Mark each row migrated, rerun discovery, and freshly build exactly the mapped targets.</action>
  <verify>
    <automated>set -euo pipefail; python3 .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-audit.py --check-migrated --owner 14-03 --inventory .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-manager-caller-inventory.tsv --compile-commands build/OSX/Release/compile_commands.json; cmake --build build/OSX/Release --target genius_node genius_node_test migration -j8; python3 -c 'import pathlib,re; g=pathlib.Path("src/account/GeniusNode.cpp").read_text(); body=re.search(r"GeniusNode::SendTransactionAndProof\b[\s\S]{0,1800}",g).group(); assert "SubmitTransaction" in body and "EnqueueTransaction" not in body; select=re.search(r"GeniusNode::SelectAccount\b[\s\S]{0,8000}",g).group(); assert "CloseAdmission" in select; assert re.search(r"TransactionManager::New\b[\s\S]{0,1400}?generation",g); print("phase14_manager_production_callers=PASS")'</automated>
  </verify>
  <acceptance_criteria>
    - All 14-03 rows are migrated and rediscovery finds no stale production call expression.
    - `genius_node`, `genius_node_test`, and `migration` build freshly after adaptation.
    - D-05/D-15 generation/close wiring is preserved and no deferred subsystem is redesigned.
  </acceptance_criteria>
  <done>All production manager consumers use the checked lifecycle contract and compile in their concrete targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Production caller → manager | Old signatures can bypass lifecycle checks or discard typed retirement. |
| Source → CMake target | A migrated source is incomplete until every owning target recompiles. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-MP1 | Tampering | direct manager control | mitigate | Private capability/checked API migration plus stale-call scan. |
| T14-MP2 | Repudiation | target coverage | mitigate | Compile-command mapping and fresh builds of all three concrete targets. |
| T14-SC | Tampering | package installs | accept | No package installation. |
</threat_model>

<multi_source_coverage_audit>
## Multi-Source Coverage Audit
| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | manager production wiring | No old-generation mutation authority in production | 14-03 | COVERED | All production callers migrated. |
| REQ | D-05/D-06/D-13/D-14/D-15 | Close, terminal drain, retirement and attribution | 14-03 | COVERED | Consumes 14-01 core without weakening it. |
| RESEARCH | Caller/target migration | GeniusNode, BridgeRelayer, migration engine | 14-03 | COVERED | Exact manifest partition and fresh targets. |
| CONTEXT | Deferred bridge ownership | Caller adaptation only | NONE | EXCLUDED | Relayer ownership remains unchanged. |
</multi_source_coverage_audit>

<verification>Both inventory checks and all mapped builds must pass; the final gate is not permitted to repair production callers.</verification>
<success_criteria>Every production manager call is migrated, target-mapped, and freshly compiled with no direct enqueue or premature stop path.</success_criteria>
<output>Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-03-SUMMARY.md` when done.</output>
