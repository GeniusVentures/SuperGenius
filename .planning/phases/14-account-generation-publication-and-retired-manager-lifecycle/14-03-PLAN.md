---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "03"
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/multiaccount/multi_account_sync.cpp
  - test/src/account/account_management_test.cpp
autonomous: true
requirements: [D-04, D-08, D-09, D-10, D-11, D-12, D-15]
must_haves:
  truths:
    - "A deterministic drain timeout terminates only the accepted switch request, publishes ACCOUNT_UNAVAILABLE, emits one generation-tagged failure, starts no replacement initialization, and never claims cancellation of old durable work, per D-08."
    - "The timed-out retiring runtime remains strongly owned until every admitted durable operation really reaches a terminal state; its late terminal event is delivered exactly once with the retired generation, never the failed or later recovery generation, per D-08 and D-15."
    - "While a timed-out retiring generation remains unresolved, account APIs report ACCOUNT_UNAVAILABLE and every explicit or overlapping SelectAccount is rejected with SWITCH_IN_PROGRESS; true drain merely makes recovery safe and never starts replacement work automatically, per D-04, D-08, D-10, and D-11."
    - "Failure at every selected-replacement initialization stage stops and releases all partial pending owners before one ACCOUNT_SWITCH_FAILED event carrying the accepted generation is observable; neither the retired nor failed target generation is republished, per D-09 and D-12."
    - "After timeout retirement has truly drained, or after initialization-failure cleanup has completed, recovery is admitted only by a new explicit SelectAccount and may target the same or a different account; there is no retry-only API, delayed retry, queued target, or implicit old-generation restoration, per D-10."
    - "Late timeout, drain, initialization, cleanup, and failure callbacks validate generation plus expected owner identity and cannot duplicate an event, clean a newer pending generation, or advance a recovery request."
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "Finite account-switch drain timeout configuration, deterministic timeout/stage-failure seam, timeout-versus-drain state, and centralized pending-failure cleanup contract"
      contains: "AccountSwitchDrainTimeout"
    - path: "src/account/GeniusNode.cpp"
      provides: "Bounded drain arbitration, retained-runtime late-drain handling, reverse-order partial-owner cleanup, exactly-once failure publication, and explicit recovery admission"
      contains: "FailPendingAccountGeneration"
    - path: "test/src/multiaccount/multi_account_sync.cpp"
      provides: "Deterministic timeout, late old terminal attribution, and every-stage cleanup-before-event regressions"
      contains: "AccountGenerationDrainTimeoutFailsClosedWithoutCancellation"
    - path: "test/src/account/account_management_test.cpp"
      provides: "Public unavailable, unresolved-retire rejection, no-implicit-retry, and same/different-target explicit recovery regressions"
      contains: "FailedSwitchRequiresExplicitRecovery"
  key_links:
    - from: "GeniusNode::SelectAccount"
      to: "AccountSwitchDrainTimeout scheduler"
      via: "one generation-and-retiring-owner-bound deadline armed for the accepted drain"
      pattern: "SelectAccount.*AccountSwitchDrainTimeout"
    - from: "drain timeout and TransactionManager::DrainCallback"
      to: "retiring generation arbitration"
      via: "one lifecycle-locked WAITING-to-TIMED_OUT or WAITING-to-DRAINED winner guarded by generation and exact manager identity"
      pattern: "HandleAccountSwitchDrainTimeout|HandleRetiredGenerationDrained"
    - from: "selected replacement stage errors"
      to: "GeniusNode::FailPendingAccountGeneration"
      via: "generation-and-owner-checked cleanup of the private pending bundle before unavailable commit and event dispatch"
      pattern: "FailPendingAccountGeneration"
    - from: "ACCOUNT_SWITCH_FAILED observer"
      to: "GeniusNode::SelectAccount"
      via: "failure publication occurs after pending cleanup and leaves no unresolved retire guard, permitting only a new explicit request"
      pattern: "ACCOUNT_SWITCH_FAILED.*SelectAccount"
---

<objective>
Close the account-switch failure state machine with deterministic RED timeout and stage-failure regressions, then GREEN bounded drain arbitration, retained old-runtime completion, cleanup-before-failure publication, exactly-once generation guards, and explicit same/different-target recovery implementing D-04, D-08, D-09, D-10, D-11, D-12, and D-15.

Purpose: A failed switch must become observably unavailable without inventing cancellation, overlapping old and replacement mutation, leaking partial owners, retrying by itself, or losing the retired generation identity of work that was already admitted.
Output: A finite configurable production timeout with a manual deterministic scheduler seam, centralized selected-replacement failure cleanup, five exact active regressions, focused and complete two-binary evidence, and no changes outside the account lifecycle boundary.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PLAN-OUTLINE.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp
@src/account/TransactionManager.hpp
@src/account/TransactionManager.cpp
@test/src/multiaccount/multi_account_sync.cpp
@test/src/account/account_management_test.cpp
@test/src/multiaccount/CMakeLists.txt
@test/src/account/CMakeLists.txt

<interfaces>
Plan 14-01 supplies `TransactionManager::CloseAdmission(DrainCallback)`, an immutable manager generation, a real admitted-operation terminal ledger, generation-bearing `TransactionCompletion`, a frozen `RetirementSnapshot`, and exactly-once drain notification. Timeout handling must keep consuming that same callback; it must not add manager cancellation, synthetic terminalization, or a second drain counter.

Plan 14-02 supplies `AccountLifecycle`, `AccountSwitchAcceptance`, `AccountSwitchEventKind::{ACCOUNT_SWITCH_READY, ACCOUNT_SWITCH_FAILED}`, `AccountSwitchEvent`, `ReadyAccountGeneration`, `RetiringAccountGeneration`, `PendingAccountGeneration`, `ProcessingOwners`, `BeginPendingAccountInitialization`, `PublishReadyAccountGeneration`, `RequireReadyAccountGeneration`, and generation-plus-owner callback guards. Extend those records and the one event path in place; do not create a parallel lifecycle or publication channel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED timeout, late terminal attribution, cleanup ordering, and explicit recovery</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
    - test/src/multiaccount/CMakeLists.txt
    - test/src/account/CMakeLists.txt
  </read_first>
  <action>Add exactly five active regressions: `MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation`, `MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration`, `MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage`, `AccountManagement.TimedOutSwitchRejectsRecoveryUntilRetireDrain`, and `AccountManagement.FailedSwitchRequiresExplicitRecovery`. Extend the existing Plan 14-02 node test seam with default-empty hooks named `capture_drain_timeout`, `before_replacement_stage`, `fail_replacement_stage`, `after_pending_cleanup`, `before_switch_failure_event`, and `after_retired_drain_following_timeout`. Define a test-visible `ReplacementInitializationStage` covering account creation/configuration, blockchain construction/start, transaction-manager construction/start, and processing construction/start, and make the stage-failure test iterate every enumerator. The manual timeout callback and stage failure selector must be injected controls; no sleep, production deadline wait, polling cadence, network accident, or null dependency may prove a failure path. Hooks copy state under the lifecycle lock and run after all node, manager, and service locks are released.

In `AccountGenerationDrainTimeoutFailsClosedWithoutCancellation`, admit and barrier durable work in the retiring manager, accept generation G, manually fire G's captured timeout before terminalization, and assert one G-tagged `ACCOUNT_SWITCH_FAILED`, lifecycle `ACCOUNT_UNAVAILABLE`, exact `ACCOUNT_UNAVAILABLE` from active-account APIs, zero replacement-stage entries/constructions, and no cancellation/terminal callback fabricated by the timeout. Keep weak and strong observations of the retiring owners to prove they remain alive. Release the old operation only after those assertions, then require one real drain and owner release with no initialization, ready event, retry, or old-ready republish. In `AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration`, complete the admitted operation after timeout and after a distinct later generation value is available in the fixture; require one terminal event tagged with the old manager generation and no relabeling, suppression, or duplicate.

In `TimedOutSwitchRejectsRecoveryUntilRetireDrain`, fire the timeout while old work is blocked, require account APIs to stay `ACCOUNT_UNAVAILABLE`, and require same-target, different-target, and overlapping `SelectAccount` calls to return exact `SWITCH_IN_PROGRESS` without allocating a generation or target. After real drain and cleanup, prove no request starts automatically; then leave the explicit post-drain recovery assertion RED for GREEN completion. This implements the supplied D-04/D-08 scope contract: a timed-out request is terminal and publicly unavailable, but unresolved physical retirement still blocks another accepted request.

In `AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage`, use a fresh node/generation for each `ReplacementInitializationStage`, inject failure only after true old drain, retain weak references and cleanup counters for every pending owner created through that stage, and assert that `after_pending_cleanup` observes no pending account/blockchain/manager/processing owner, all stop/unregister/deconfigure actions completed exactly once where applicable, and no ready bundle exists before `before_switch_failure_event` allows delivery. Release the event and require exactly one `ACCOUNT_SWITCH_FAILED` containing the accepted generation and concrete injected error; replay the stage callback and a stale timeout callback and require no second cleanup, event, state mutation, or ready publication. In `FailedSwitchRequiresExplicitRecovery`, prove no timer/retry/post creates another generation after a fully cleaned stage failure, public address remains unavailable, and two independent subcases recover only after explicit `SelectAccount`: one reselects the same failed target and one selects a different stored target. Each accepted recovery gets a new monotonically greater generation and exactly one ready event; the old ready generation is never restored.

Add only the final declarations and hook plumbing needed for all five cases to compile: append `std::chrono::milliseconds AccountSwitchDrainTimeout{60000}` to `GeniusNodeConfig`, declare the private timeout handle/scheduler seam, timeout-versus-drain state, failure-stage enum, and centralized timeout/drain/failure helper signatures consumed by the tests. Keep existing unbounded wait, replacement retry, or premature failure behavior in place during RED so every test fails on its intended assertion. Record each exact failing assertion before GREEN; a missing/disabled test, compile error, crash, or test harness timeout is not an acceptable RED result.</action>
  <verify>
    <automated>set -e; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; for spec in MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage; do if build/OSX/Release/test_bin/multi_account_test --gtest_filter="$spec"; then echo "expected RED failure: $spec"; exit 1; fi; done; for spec in AccountManagement.TimedOutSwitchRejectsRecoveryUntilRetireDrain AccountManagement.FailedSwitchRequiresExplicitRecovery; do if build/OSX/Release/test_bin/account_management_test --gtest_filter="$spec"; then echo "expected RED failure: $spec"; exit 1; fi; done; build/OSX/Release/test_bin/multi_account_test --gtest_list_tests | rg -n 'AccountGenerationDrainTimeoutFailsClosedWithoutCancellation|AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration|AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage'; build/OSX/Release/test_bin/account_management_test --gtest_list_tests | rg -n 'TimedOutSwitchRejectsRecoveryUntilRetireDrain|FailedSwitchRequiresExplicitRecovery'</automated>
  </verify>
  <acceptance_criteria>
    - All five exact tests compile, enumerate once as active tests, and fail only on their intended pre-GREEN timeout, cleanup, retry, attribution, or recovery assertion.
    - D-08 timeout and every replacement-stage failure are driven by manual/injected hooks with no sleep, elapsed-time inference, or incidental dependency failure.
    - The timeout cases separately observe request failure, retained retiring ownership, late real terminalization/drain, and absence of replacement initialization or synthetic cancellation.
    - The stage table covers every owner-acquisition/start boundary and observes cleanup completion before allowing the failure callback to execute.
    - Recovery tests cover same-target and different-target explicit selection, unresolved-retire rejection, no automatic retry, and no old-generation republish.
  </acceptance_criteria>
  <done>The RED suite deterministically exposes unbounded drain, false cancellation, premature replacement work, partial-owner leaks, duplicate/stale failure publication, implicit retry, and unsafe recovery admission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN bounded drain failure while retaining real old-generation completion</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
  </read_first>
  <behavior>
    - "Manual timeout before real drain -> ACCOUNT_UNAVAILABLE plus one failed event for G, no replacement initialization, no cancellation, and retiring G remains owned."
    - "Real drain before timeout -> cancel/retire the deadline and initialize G once; a stale timeout is inert."
    - "Real drain after timeout -> deliver old terminal result under old generation, release retiring runtime, remain unavailable, and initialize nothing automatically."
    - "SelectAccount while timed-out retirement is unresolved -> SWITCH_IN_PROGRESS with no generation allocation; after real drain -> a new explicit request can be admitted."
  </behavior>
  <action>Append `AccountSwitchDrainTimeout` to `GeniusNodeConfig` with the research-backed finite production default of 60 seconds and validate it is strictly positive when constructing the node. Implement a private `AccountSwitchTimeoutScheduler` seam whose production path uses the existing asynchronous scheduler/io context and returns a cancellable per-request handle; `MultiAccountTestAccess` replaces only this seam with a manually fired callback. Store the handle and an explicit `DrainResolution { WAITING, DRAINED, TIMED_OUT }` in the generation-owned retire context, never in process-global state. Arm exactly one deadline for the accepted generation and exact retiring manager without holding `lifecycle_mutex_`; make timeout, cancellation, and callback destruction owner-safe.

Implement `HandleAccountSwitchDrainTimeout(generation, expected_retiring_manager)` and `HandleRetiredGenerationDrained(generation, expected_retiring_manager, RetirementSnapshot)` as the two contenders for one lifecycle-locked transition out of `WAITING`. Both verify accepted generation and exact manager identity. If drain wins, set `DRAINED`, move the retiring runtime out, cancel the timeout after unlock, perform the existing drained retirement release, and begin pending initialization exactly once. If timeout wins, set `TIMED_OUT`, discard only the target-only pending request state, set public lifecycle to `ACCOUNT_UNAVAILABLE`, retain the complete retiring runtime and its real drain callback, and dispatch exactly one generation-tagged `ACCOUNT_SWITCH_FAILED` after unlock. The timeout path must not call `TransactionManager::Stop`, `FinalizeRetirement`, `CompleteAdmittedOperation`, cancellation APIs, account deconfiguration, replacement constructors, `BeginPendingAccountInitialization`, or `PublishReadyAccountGeneration`; it terminates the request, not durable old work, per D-08.

When the real drain callback arrives after timeout, verify the same generation/manager identity and frozen retirement generation, move and release the retiring owners only then, clear the unresolved-retirement guard, and remain `ACCOUNT_UNAVAILABLE`. Do not reconstruct the discarded target, call replacement initialization, emit ready, emit a second failure, or republish the retired ready bundle. Preserve Plan 14-01's immutable completion generation all the way through the node terminal-event bridge: no timeout or later node generation lookup may rewrite it, and duplicate terminal callbacks stay suppressed by the manager ledger. While `retiring_generation_` is still present after timeout, `RequireReadyAccountGeneration` continues returning `ACCOUNT_UNAVAILABLE` and `SelectAccount` returns `SWITCH_IN_PROGRESS` before validation, generation allocation, or side effects. Once true drain releases it, leave the node unavailable with no active request; only a subsequent explicit selection may allocate a fresh generation.

Use generation plus expected owner on timeout, drain, and posted release continuations, and copy callbacks/owners under `lifecycle_mutex_` but cancel timers, stop/release already-drained owners, post work, and invoke events after unlock. Race the manual timeout and drain in both orders in the tests so exactly one continuation wins. Keep initial bootstrap retry behavior unchanged and add no cancellation, rollback, queued-selection, latest-wins, or generic retry abstraction.</action>
  <verify>
    <automated>set -e; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation:MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration:MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration'; build/OSX/Release/test_bin/account_management_test --gtest_filter='AccountManagement.TimedOutSwitchRejectsRecoveryUntilRetireDrain'; python3 -c 'import pathlib,re; h=pathlib.Path("src/account/GeniusNode.hpp").read_text(); s=pathlib.Path("src/account/GeniusNode.cpp").read_text(); extract=lambda name: re.search(r"GeniusNode::"+name+r"\b[\s\S]*?(?=\n\s*(?:[\w:&lt;&gt;,*]+\s+)+GeniusNode::|\Z)",s).group(); assert re.search(r"AccountSwitchDrainTimeout\s*\{\s*(?:std::chrono::milliseconds\s*\(\s*)?60000",h); assert all(x in h+s for x in ["AccountSwitchTimeoutScheduler","DrainResolution","HandleAccountSwitchDrainTimeout","HandleRetiredGenerationDrained"]); timeout=extract("HandleAccountSwitchDrainTimeout"); forbidden=["BeginPendingAccountInitialization","PublishReadyAccountGeneration","CompleteAdmittedOperation","FinalizeRetirement"]; assert not [x for x in forbidden if x in timeout], [x for x in forbidden if x in timeout]; assert not re.search(r"expected_retiring_manager[\s\S]{0,1000}?->Stop\s*\(",timeout); drain=extract("HandleRetiredGenerationDrained"); assert "TIMED_OUT" in drain and "BeginPendingAccountInitialization" in drain; assert re.search(r"TIMED_OUT[\s\S]{0,2500}?ACCOUNT_UNAVAILABLE",drain); print("phase14_timeout_retention_contract=PASS")'</automated>
  </verify>
  <acceptance_criteria>
    - D-08: the configured 60-second production deadline is finite and positive, while tests fire the same callback manually without waiting.
    - D-04/D-11: a timeout makes account APIs unavailable but retains a separate unresolved-retirement guard that rejects every selection with `SWITCH_IN_PROGRESS` until real drain.
    - D-08/D-15: timeout performs no cancellation or synthetic terminalization; late admitted work delivers exactly one terminal result under the old manager generation before real drain releases its runtime.
    - Drain-timeout and real-drain callbacks arbitrate exactly once by generation and manager identity; either drain starts initialization or timeout fails unavailable, never both.
    - A post-timeout real drain releases owners and enables future explicit admission but performs no replacement initialization, retry, ready event, second failure, or retired-generation republish.
  </acceptance_criteria>
  <done>A finite deadline can fail the switch request without lying about cancellation or permitting replacement mutation while the retiring runtime still has durable work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN cleanup-first selected-replacement failure and explicit recovery</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.hpp
    - src/account/TransactionManager.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
  </read_first>
  <behavior>
    - "Failure at each selected-replacement stage -> all partial pending owners stopped/released before exactly one generation-tagged ACCOUNT_SWITCH_FAILED callback."
    - "Late/stale stage, timeout, and cleanup callbacks -> no cleanup of a newer generation, no duplicate failure, and no ready publication."
    - "Fully cleaned failure -> active APIs remain ACCOUNT_UNAVAILABLE and no retry occurs without SelectAccount."
    - "Fresh explicit same-target or different-target SelectAccount after safe cleanup/drain -> new generation accepted and may become ready; old generation is never republished."
  </behavior>
  <action>Create one private `FailPendingAccountGeneration(generation, expected_pending_owners, error)` path for all failures belonging to an accepted replacement. Route synchronous construction/configuration failures and asynchronous blockchain-start, transaction-manager-start/readiness, and processing-start/readiness failures through it. Selected replacements must bypass `ScheduleBlockchainRetry`, migration retry, state re-entry, and any delayed initialization retry; preserve those policies only for initial bootstrap that has no accepted `PendingAccountGeneration`. The injected `ReplacementInitializationStage` hook selects the same production error edges without substituting a second cleanup implementation.

In `FailPendingAccountGeneration`, first validate the active accepted generation, `SWITCHING` lifecycle, current pending generation, and every expected owner identity already created by that continuation. Under `lifecycle_mutex_`, atomically mark the generation terminal so later stage/timeout callbacks are stale, cancel/move its timeout handle, and move the entire pending bundle to a local cleanup value while leaving the ready bundle empty. Do not publish `ACCOUNT_UNAVAILABLE` or copy the failure callback yet. After unlock, clean the moved owners in reverse dependency order: stop processing and release service/queue/core/result storage; unregister callbacks and stop the pending transaction manager; clear blockchain callbacks and stop/release blockchain; deconfigure/release the pending account. Preserve only node-scoped database, trust-policy, configured identity, and other Plan 14-02 non-account owners. Cleanup is idempotent and must not touch a newer pending generation.

After all partial owners and timeout handles are stopped/released, reacquire `lifecycle_mutex_`, revalidate the same terminal generation and absence of a newer request, clear pending/retiring request state, set `AccountLifecycle::ACCOUNT_UNAVAILABLE`, and record that the generation's failure event has not previously been emitted. Copy exactly one `AccountSwitchEvent{ACCOUNT_SWITCH_FAILED, generation, target_address, error}`, release the lock, then dispatch it through Plan 14-02's existing event path. Event observers must see no pending owners or ready generation and may re-enter `SelectAccount` safely. A repeated/stale failure callback returns without cleanup or event. Never assign a retiring or failed bundle to `ready_generation_`, call `PublishReadyAccountGeneration` from failure handling, or derive the event generation from current node state.

Complete `SelectAccount` admission from `ACCOUNT_UNAVAILABLE`: reject while a timed-out `retiring_generation_` remains unresolved, otherwise validate the explicit requested target, allocate a strictly newer generation, create a fresh target-only pending record, set `SWITCHING`, arm a fresh timeout/drain flow as applicable, and follow the normal Plan 14-02 initialization pipeline. Do not retain a failed target as an implicit request and do not add a retry-only API. Prove same-target and different-target recovery in independent test subcases, with no ready/failure event or construction between failure cleanup and the explicit call. Run the timeout, stage-failure, recovery, prior ready-publication, and prior retired-terminal cases together before the complete binaries. Add structural gates scoped to `FailPendingAccountGeneration` and selected replacement error routing that reject `ScheduleBlockchainRetry`, `ScheduleMigrationRetry`, `PublishReadyAccountGeneration`, any assignment from retiring to ready, and any queued/retry target field.</action>
  <verify>
    <automated>set -e; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.AccountGenerationDrainTimeoutFailsClosedWithoutCancellation:MultiAccountTest.AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration:MultiAccountTest.AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage:MultiAccountTest.RetiredManagerDeliversAcceptedTerminalOutcomeWithOldGeneration:MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization:MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion'; build/OSX/Release/test_bin/account_management_test --gtest_filter='AccountManagement.TimedOutSwitchRejectsRecoveryUntilRetireDrain:AccountManagement.FailedSwitchRequiresExplicitRecovery:AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent:AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap:AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration'; build/OSX/Release/test_bin/multi_account_test; build/OSX/Release/test_bin/account_management_test; ctest --test-dir build/OSX/Release -R '^(multi_account_test|account_management_test)$' --output-on-failure; python3 -c 'import pathlib,re; mt=pathlib.Path("test/src/multiaccount/multi_account_sync.cpp").read_text(); at=pathlib.Path("test/src/account/account_management_test.cpp").read_text(); names=["AccountGenerationDrainTimeoutFailsClosedWithoutCancellation","AccountGenerationTimedOutRetireDeliversLateTerminalOutcomeWithOldGeneration","AccountGenerationInitializationFailureCleansBeforeEventAtEveryStage","TimedOutSwitchRejectsRecoveryUntilRetireDrain","FailedSwitchRequiresExplicitRecovery"]; text=mt+"\n"+at; assert all(len(re.findall(r"TEST_F\([^,]+,\s*"+n+r"\s*\)",text))==1 for n in names); assert not any("DISABLED_"+n in text for n in names); assert all(x in mt for x in ["ACCOUNT_CONSTRUCTION","ACCOUNT_CONFIGURATION","BLOCKCHAIN_CONSTRUCTION","BLOCKCHAIN_START","TRANSACTION_MANAGER_CONSTRUCTION","TRANSACTION_MANAGER_START","PROCESSING_CONSTRUCTION","PROCESSING_START"]); h=pathlib.Path("src/account/GeniusNode.hpp").read_text(); s=pathlib.Path("src/account/GeniusNode.cpp").read_text(); extract=lambda name: re.search(r"GeniusNode::"+name+r"\b[\s\S]*?(?=\n\s*(?:[\w:&lt;&gt;,*]+\s+)+GeniusNode::|\Z)",s).group(); assert "FailPendingAccountGeneration" in h and "FailPendingAccountGeneration" in s; failure=extract("FailPendingAccountGeneration"); forbidden=["ScheduleBlockchainRetry","ScheduleMigrationRetry","PublishReadyAccountGeneration"]; assert not [x for x in forbidden if x in failure], [x for x in forbidden if x in failure]; assert not re.search(r"ready_generation_\s*=\s*(?:std::move\s*\()?retiring_generation_",failure); assert re.search(r"pending[\s\S]{0,5000}?(?:StopProcessing|ResetProcessingMembers)[\s\S]{0,5000}?UnregisterStateChangeCallback[\s\S]{0,5000}?Stop\s*\(",failure); assert re.search(r"ACCOUNT_UNAVAILABLE[\s\S]{0,2500}?ACCOUNT_SWITCH_FAILED",failure); selected=extract("BeginPendingAccountInitialization"); assert "FailPendingAccountGeneration" in selected; assert "ScheduleBlockchainRetry" not in selected and "ScheduleMigrationRetry" not in selected; assert not re.search(r"queued_(?:account|target|selection)|retry_(?:account|target|selection)",h+s,re.I); print("phase14_failure_recovery_contract=PASS")'</automated>
  </verify>
  <acceptance_criteria>
    - D-09/D-12: every enumerated selected-replacement stage uses one cleanup path, releases all partial owners before the callback, emits one failure for the accepted generation, and never exposes or republishes a partial/retired bundle.
    - D-10: selected replacements never enter the existing bootstrap retry helpers; no automatic, queued, latest-wins, or retry-only recovery path exists.
    - D-04/D-08: unresolved timed-out retirement blocks all selection; after real drain or completed stage cleanup, only a new explicit same-target or different-target call allocates the next generation.
    - D-11: failure and post-drain idle state keep active address and equivalent APIs at exact `ACCOUNT_UNAVAILABLE` until the explicit recovery generation reaches the one ready commit.
    - D-15: the complete failure/recovery suite preserves exactly-once old-generation terminal attribution while later accepted and ready generations exist.
    - All five new focused cases, relevant prior Plan 14-01/14-02 cases, both complete binaries, scoped CTest, and structural no-retry/no-republish gates pass.
  </acceptance_criteria>
  <done>Every timeout and selected-replacement failure is generation-safe, cleanup-complete, explicitly recoverable, and incapable of cancelling old work, leaking owners, retrying itself, or restoring an obsolete generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Timeout scheduler to node lifecycle | A delayed or manually triggered callback crosses into a generation that may already have drained, failed, or been superseded. |
| Retiring manager to replacement initialization | Durable admitted work controls when old runtime ownership may be released and replacement mutation may begin. |
| Selected replacement stages to pending-owner cleanup | Account, blockchain, manager, and processing failures can occur after different subsets of private owners exist. |
| Cleanup completion to account-switch event observer | Reentrant callers treat failure delivery as evidence that partial replacement ownership is gone and recovery admission is safe. |
| Retired operation completion to node event delivery | A late terminal result must retain the immutable manager generation across timeout and later recovery generations. |

## STRIDE Threat Register — ASVS L1 / HIGH Findings Are Blocking

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan / Blocking Evidence |
|-----------|----------|----------|-----------|-------------|-------------------------------------|
| T14-DOS | Denial of Service | HIGH | Retiring-manager drain | mitigate | A positive configurable 60-second deadline and injected scheduler bound the request; `AccountGenerationDrainTimeoutFailsClosedWithoutCancellation` proves deterministic unavailable termination. |
| T14-FC | Tampering / Repudiation | HIGH | Timeout versus durable old work | mitigate | Timeout never stops, cancels, or synthetic-terminalizes the retiring manager; retained ownership and the late-terminal test prove real completion under the old generation. |
| T14-PM | Tampering / Elevation of Privilege | HIGH | Timeout/drain race into replacement initialization | mitigate | One `DrainResolution` winner plus generation/exact-manager checks prevent timeout and drain from both advancing; both deterministic race orders are blocking. |
| T14-PL | Information Disclosure / Denial of Service | HIGH | Partial pending owner cleanup | mitigate | One reverse-order cleanup path moves all pending owners private, stops/releases them before unavailable/event publication, and the every-stage weak-owner/counter test is blocking. |
| T14-DE | Tampering / Repudiation | HIGH | Duplicate or stale failure event | mitigate | Terminal-generation and expected-owner guards make cleanup/event publication idempotent; replayed stage/timeout callbacks must produce no second event or newer-generation mutation. |
| T14-IR | Tampering / Elevation of Privilege | HIGH | Implicit retry or old-generation republish | mitigate | Selected replacement failures bypass retry helpers, no target is queued, failure never calls the ready publisher, and public same/different-target recovery tests plus structural scans are blocking. |
| T14-SUP | Tampering | LOW | npm/pip/cargo supply chain | accept | This plan installs no package and changes no dependency or lockfile, so the package legitimacy gate is not entered. |
</threat_model>

<multi_source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | Phase 14 failure/recovery slice | Failed or retired generations remain unavailable while admitted old work completes and only complete replacements can publish | 14-03 | COVERED | Tasks 1-3 close the timeout, selected-stage failure, and explicit recovery portion of the outline goal. |
| REQ | D-04 | Reject overlap without cancellation, latest-wins, or queued follow-up | 14-03 | COVERED | Unresolved timed-out retirement returns `SWITCH_IN_PROGRESS`; no target/generation is queued. |
| REQ | D-08 | Bound drain without cancelling old durable work or starting replacement | 14-03 | COVERED | Task 2 implements finite scheduling, one timeout/drain winner, retained runtime, and late real drain. |
| REQ | D-09 | Initialization failure leaves unavailable, emits generation-tagged failure, and exposes no partial/retired generation | 14-03 | COVERED | Tasks 1 and 3 exercise every stage through one guarded cleanup/failure path. |
| REQ | D-10 | Recovery needs a fresh explicit same/different-target selection and never retries automatically | 14-03 | COVERED | Task 3 proves both targets and structurally forbids selected-path retry helpers/queued targets. |
| REQ | D-11 | Active address and equivalent account APIs are explicitly unavailable after failure | 14-03 | COVERED | Timeout and stage-failure public cases consume Plan 14-02's typed ready-only accessors. |
| REQ | D-12 | Every partial replacement owner is stopped/released before failure event | 14-03 | COVERED | Every-stage barriers, weak-owner checks, cleanup counters, and reentrant event recovery prove ordering. |
| REQ | D-15 | Accepted old terminal events survive retirement and keep the old generation | 14-03 | COVERED | The timeout-late-terminal case and prior Plan 14-01 regression run together after recovery exists. |
| RESEARCH | Finite deterministic drain scheduler | 60-second production default with manual test trigger | 14-03 | COVERED | Task 2 implements the recommended `GeniusNodeConfig` value and injectable scheduler seam. |
| RESEARCH | Timeout versus true retirement separation | Timeout ends the request while old runtime remains alive to real drain | 14-03 | COVERED | Task 2 maintains `ACCOUNT_UNAVAILABLE` plus an unresolved-retire guard and never calls cancellation. |
| RESEARCH | Cleanup-before-event | Move pending owners under lock, clean outside lock, then commit unavailable/event | 14-03 | COVERED | Task 3 centralizes reverse-order cleanup and event publication. |
| RESEARCH | Selected replacement retry prohibition | Replacement failures bypass current blockchain retry helper | 14-03 | COVERED | Task 3 separates selected initialization origin from unchanged bootstrap retry policy. |
| VALIDATION | 14-W0-05 | `AccountGenerationDrainTimeoutFailsClosedWithoutCancellation` | 14-03 | COVERED | Exact active deterministic timeout test is added RED before GREEN. |
| VALIDATION | 14-W0-06 | `AccountGenerationInitializationFailureCleansBeforeEvent*` | 14-03 | COVERED | Exact active every-stage test matches the validation wildcard. |
| VALIDATION | 14-W0-07 | `FailedSwitchRequiresExplicitRecovery` | 14-03 | COVERED | Exact public test includes no retry, same-target, and different-target recovery subcases. |
| CONTEXT | D-01/D-02/D-03/D-05/D-07/D-16 | Accepted async switch, event surface, switching errors, close/drain ordering, and processing status | 14-02 | COVERED | Consumed as upstream contracts; Plan 14-03 does not redesign them. |
| CONTEXT | D-06/D-13/D-14 | Accepted-operation terminal ledger, permanent retired mutation denial, immutable diagnostics | 14-01 | COVERED | Consumed unchanged; timeout cannot manufacture cancellation or a second ledger. |
| CONTEXT | Deferred bridge ownership | Provider/relayer/catch-up watcher publication redesign | NONE | EXCLUDED | Existing mechanical cleanup remains outside ready/pending bundle redesign. |
| CONTEXT | Deferred trust refresh | `TrustStartupController` refresh coalescing, retries, and timer lifetime | NONE | EXCLUDED | Account-switch timeout does not alter trust refresh scheduling. |
| CONTEXT | Deferred repository authority | `AtomicTransaction`, GlobalDB, or CRDT capability redesign | NONE | EXCLUDED | Failure cleanup changes account-generation ownership only. |
| CONTEXT | Forbidden generic semantics | Cancellation/rollback abstraction, latest-wins selection, queued follow-up, automatic replacement retry | NONE | EXCLUDED | Locked decisions and structural gates prohibit each behavior. |
</multi_source_coverage_audit>

<verification>
Execute strictly RED before GREEN. Preserve the five recorded RED assertions from Task 1; after Task 2 require timeout, unresolved-retire, and old-generation terminal cases to pass; after Task 3 require all five new cases plus the relevant Plan 14-01/14-02 regressions, both complete binaries, scoped CTest, and structural no-cancellation/no-retry/no-republish scans to exit zero. No sleep-based race proof, disabled/omitted exact case, request-timeout cancellation claim, replacement construction before true drain, event-before-cleanup, stale/current-generation relabeling, automatic retry, or old-ready restoration is acceptable.
</verification>

<success_criteria>
- D-04/D-08: a finite deterministic timeout ends one request unavailable, retains old runtime through true drain, blocks further selection while retirement is unresolved, and never starts replacement mutation or claims cancellation.
- D-09/D-12: every selected replacement stage cleans all private owners before exactly one generation-tagged failure event, with stale callbacks unable to affect later generations.
- D-10/D-11: no automatic retry or obsolete active identity appears; only a new explicit same-target or different-target selection after safe drain/cleanup can recover.
- D-15: durable old work still emits exactly one terminal result under its immutable retired generation after timeout and alongside later recovery generations.
- All five new focused regressions, relevant prior lifecycle regressions, both complete scoped binaries, scoped CTest, and structural scans pass while all exclusions remain untouched.
</success_criteria>

<output>
Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-03-SUMMARY.md` when done.
</output>
