---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "02"
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/multiaccount/multi_account_sync.cpp
  - test/src/account/account_management_test.cpp
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv
  - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07, D-11, D-16]
must_haves:
  truths:
    - "SelectAccount acknowledges one accepted generation promptly; readiness/failure uses one generation-tagged event path, per D-01/D-02."
    - "Ready, retiring, and pending owners are coherent bundles; old drain precedes replacement initialization and one complete ready commit, per D-05/D-07."
    - "Checked account-bound APIs report SWITCH_IN_PROGRESS or ACCOUNT_UNAVAILABLE and processing status is bound to the ready generation, per D-03/D-11/D-16."
    - "Every account-bound GeniusNode declaration and caller/target is inventoried, not only GetAddress and GetProcessingStatus."
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "Account lifecycle, accepted generation, event payload, owner bundles, checked active APIs, configured identity"
      contains: "AccountSwitchAcceptance"
    - path: "src/account/GeniusNode.cpp"
      provides: "Drain-gated pending initialization and sole complete ready publication"
      contains: "PublishReadyAccountGeneration"
    - path: ".planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv"
      provides: "Complete account-bound GeniusNode API declaration/caller/target migration manifest"
      contains: "method\texpression\tcontract\tidentity_kind\tsource\ttargets\towner_plan\tdisposition"
  key_links:
    - from: "SelectAccount"
      to: "TransactionManager::CloseAdmission"
      via: "close before accepted return, drain callback before initialization"
      pattern: "CloseAdmission.*BeginPendingAccountInitialization"
    - from: "pending callbacks"
      to: "PendingAccountGeneration"
      via: "accepted generation plus exact owner identity"
      pattern: "generation.*get\(\)"
---

<objective>
Implement the GeniusNode lifecycle core and derive the complete account-bound API caller/target manifest, without mixing broad caller migration into the core tasks.

Purpose: Make generation ownership and publication correct first, while explicitly marked temporary compatibility wrappers preserve staged compilation until Plans 14-06 through 14-12 migrate every consumer.
Output: Node lifecycle core, seven deterministic regressions, complete API/caller/target inventory, and checked transition APIs.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp
@src/account/TransactionManager.hpp
@test/src/multiaccount/multi_account_sync.cpp
@test/src/account/account_management_test.cpp
@build/OSX/Release/compile_commands.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED accepted switching, coherent publication, stale callbacks, identity, and status</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
  </read_first>
  <action>Add exactly the active cases `SelectAccountReturnsGenerationBeforeReadyEvent`, `SwitchInProgressRejectsAccountCallsAndOverlap`, `ConfiguredIdentityDoesNotPublishUnavailableGeneration`, `AccountGenerationDrainsBeforeReplacementInitialization`, `AccountGenerationRejectsStaleBlockchainCompletion`, `AccountGenerationProcessingStatusTracksLifecycle`, and strengthen `ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent`. Use markers `[PHASE14-02-RED-ACCEPTANCE]`, `[PHASE14-02-RED-SWITCHING]`, `[PHASE14-02-RED-CONFIGURED-ID]`, `[PHASE14-02-RED-DRAIN]`, `[PHASE14-02-RED-STALE]`, `[PHASE14-02-RED-STATUS]`, and `[PHASE14-02-RED-SNAPSHOT]`. Add default-empty barriers around acceptance, old close, replacement start, pending completions, and publication; callbacks run after unlock and no sleep proves races. Add final value types/hooks plus checked transition names needed to compile, while leaving the lifecycle assertions RED.</action>
  <verify>
    <automated>set -euo pipefail; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; red_dir="$(mktemp -d /tmp/phase14-02-red.XXXXXX)"; bins=(build/OSX/Release/test_bin/account_management_test build/OSX/Release/test_bin/account_management_test build/OSX/Release/test_bin/account_management_test build/OSX/Release/test_bin/multi_account_test build/OSX/Release/test_bin/multi_account_test build/OSX/Release/test_bin/multi_account_test build/OSX/Release/test_bin/multi_account_test); specs=(AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent); markers=('[PHASE14-02-RED-ACCEPTANCE]' '[PHASE14-02-RED-SWITCHING]' '[PHASE14-02-RED-CONFIGURED-ID]' '[PHASE14-02-RED-DRAIN]' '[PHASE14-02-RED-STALE]' '[PHASE14-02-RED-STATUS]' '[PHASE14-02-RED-SNAPSHOT]'); for i in 1 2 3 4 5 6 7; do bin="${bins[$i]}"; spec="${specs[$i]}"; marker="${markers[$i]}"; stem="$(printf '%s' "$spec" | tr '.' '_')"; xml="$red_dir/$stem.xml"; out="$red_dir/$stem.out"; set +e; "$bin" --gtest_filter="$spec" --gtest_output="xml:$xml" &gt;"$out" 2&gt;&amp;1; rc=$?; set -e; test "$rc" -eq 1; test -s "$xml"; python3 -c 'import sys,xml.etree.ElementTree as E; path,spec,marker=sys.argv[1:]; root=E.parse(path).getroot(); cases=list(root.iter("testcase")); assert len(cases)==1; case=cases[0]; assert case.get("classname")+"."+case.get("name")==spec; assert case.get("status","run").lower()=="run" and case.get("result","completed").lower() not in ("skipped","suppressed","notrun","disabled"); failures=[c for c in case if c.tag.rsplit("}",1)[-1]=="failure"]; forbidden=[c.tag for c in case if c.tag.rsplit("}",1)[-1] in ("error","skipped")]; assert len(failures)==1 and not forbidden; payload=(failures[0].get("message") or "")+(failures[0].text or ""); assert marker in payload' "$xml" "$spec" "$marker"; rg -F -q "$marker" "$out"; if rg -n 'unknown file: Failure|C\+\+ exception|Segmentation fault|Bus error|Abort trap|AddressSanitizer|ThreadSanitizer|runtime error:|SetUp\(|TearDown\(|[Tt]imed out|No tests ran' "$out"; then exit 1; fi; done; printf 'phase14_02_exact_red_failures=PASS evidence_dir=%s\n' "$red_dir"</automated>
  </verify>
  <acceptance_criteria>
    - All seven exact tests run once and fail only at their marked lifecycle assertion with strict XML/output/normal-failure validation.
    - Snapshot tests reject partial account/manager/processing presence instead of skipping it.
    - Deterministic hooks do not supply production synchronization or run under lifecycle/service locks.
  </acceptance_criteria>
  <done>The RED suite deterministically exposes premature publication, stale advancement, overlap, identity leakage, and retired processing-status defects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN node lifecycle core and inventory the complete account-bound surface</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv, .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
    - build/OSX/Release/compile_commands.json
  </read_first>
  <behavior>
    - "Accepted selection -> generation returned before one later ready event carrying the same generation."
    - "Switching/unavailable -> checked account APIs return exact lifecycle error; ready -> complete owner bundle only."
    - "Old drain -> replacement initialization; stale generation/owner callback -> no transition or publication."
  </behavior>
  <action>Implement `AccountLifecycle`, `AccountSwitchAcceptance`, generation-tagged ready/failure events integrated with `StateTransition`, and ready/retiring/pending owner bundles. Close old admission before acceptance returns, release `lifecycle_mutex_`, wait for the exact old manager's real drain, construct private pending owners, and publish account/blockchain/manager/processing once only after readiness. Every async continuation revalidates generation plus expected owner. Store configured/bootstrap identity privately and keep immutable `NodeTrustSigner` node-scoped. Add checked active address/status and lifecycle mapping used by the core tests; temporary old-shape wrappers, if compilation requires them, are explicitly marked `PHASE14_TEMP_NODE_LEGACY_SHIM` and must delegate without leaking pending/failed identity.

Create a comment/string-aware, declaration-driven `14-node-api-caller-audit.py` and write `14-02-CALLER-INVENTORY.tsv` with header `method\texpression\tcontract\tidentity_kind\tsource\ttargets\towner_plan\tdisposition`. Inventory the complete account-bound public surface, including `SelectAccount`, `TransferAccount`, `DeleteAccount`, `MergeAccount`, `SetPayoutAddress`, both `MintTokens`, `ProcessImage`, all four `GetBalance` overloads, `GetInTransactions`, `GetOutTransactions`, `CountTransactions`, `GetAddress`, `GetMnemonicOfActiveAccount`, `GetProcessingStatus`, both `TransferFunds`, both `PayDev`, `WaitForFinalized`, `IsFinalized`, all three transaction wait methods, `GetTransactionManagerState`, `GetTransactionManager`, `GetTransactionStatus`, `ConfigureRpcEndpoint`, `StartProcessing`, `StopProcessing`, `ResetProcessingMembers`, and protected `SendTransactionAndProof`. Classify each call as active-ready, configured-bootstrap, lifecycle-selection, processing-generation, or internal-control; map source and header consumers to every concrete CMake target from compile commands. Assign every discovered caller expression and each of its resolved target owners exactly once across the complete migration partition: 14-06 production/startup (`GeniusNode.cpp`, `node_initialization_progress.cpp`); 14-07 account/blockchain fixtures; 14-08 processing and transaction-sync fixtures; 14-09 multi-account fixtures; 14-10 bridge E2E fixtures; 14-11 bridge-race translation units and shared fixture (with every shared-header target expanded); and 14-12 only `example/node_test/NodeExample.cpp` → `node_example` plus `test/src/processing_multi/processing_multi_test.cpp` → `processing_multi_test`. The audit must reject an owner outside this set, an unowned expression/target, duplicate expression/target ownership, or a row whose source category disagrees with its owner plan. The known target floor from research must remain present when configured; discovery may add sources or targets but may not silently omit or leave a row unowned. Do not migrate broad callers in this task.</action>
  <verify>
    <automated>set -euo pipefail; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; build/OSX/Release/test_bin/account_management_test --gtest_filter='AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent:AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap:AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration'; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization:MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion:MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle:MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent'; python3 .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py --write-inventory .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv --compile-commands build/OSX/Release/compile_commands.json; python3 .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-node-api-caller-audit.py --check-assigned --inventory .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-CALLER-INVENTORY.tsv --owners 14-06,14-07,14-08,14-09,14-10,14-11,14-12 --require-exact-owner-per-expression-target --check-owner-source-partition; python3 -c 'import pathlib,re; h=pathlib.Path("src/account/GeniusNode.hpp").read_text(); s=pathlib.Path("src/account/GeniusNode.cpp").read_text(); assert all(x in h for x in ["AccountLifecycle","AccountSwitchAcceptance","AccountSwitchEvent","ReadyAccountGeneration","RetiringAccountGeneration","PendingAccountGeneration"]); assert "PublishReadyAccountGeneration" in s and "BeginPendingAccountInitialization" in s and "GetConfiguredAccountAddress" in s; body=re.search(r"GeniusNode::SelectAccount\b[\s\S]{0,8000}",s).group(); assert "CloseAdmission" in body and "SWITCH_IN_PROGRESS" in body; assert re.search(r"generation[\s\S]{0,300}?\.get\(\)",s); print("phase14_node_core=PASS")'</automated>
  </verify>
  <acceptance_criteria>
    - D-01/D-02/D-04/D-05/D-07 core tests pass with one accepted generation, one event path, true drain gating, and no stale callback advance.
    - D-03/D-11/D-16 checked lifecycle contracts exist and core tests consume them without exposing configured/pending/retired identity.
    - Every listed account-bound declaration and every resolved compile-time caller/target has exactly one TSV row and exactly one owner among 14-06, 14-07, 14-08, 14-09, 14-10, 14-11, and 14-12; no broad caller adaptation is hidden in this task.
  </acceptance_criteria>
  <done>The node lifecycle core publishes only complete ready generations and every account-bound API consumer is assigned to an explicit staged migration plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SelectAccount caller → lifecycle | Overlap and account operations cross an unavailable transition. |
| Retiring manager → pending generation | Real drain is the only authorization to initialize replacement mutable owners. |
| Async callback → publication | Stale work must prove generation and exact owner identity. |
| Configured identity → active API | Bootstrap identity must never be presented as active while unavailable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-NC1 | Tampering / Information Disclosure | ready publication | mitigate | One complete bundle commit plus coherent snapshot regression. |
| T14-NC2 | Tampering | stale callbacks | mitigate | Generation and owner revalidation at every continuation. |
| T14-NC3 | Spoofing | configured versus active identity | mitigate | Private configured helper and checked active result. |
| T14-NC4 | Denial of Service | overlap/lock reentry | mitigate | Explicit lifecycle and callback dispatch after unlock. |
| T14-SC | Tampering | package installs | accept | No package or dependency installation occurs. |
</threat_model>

<multi_source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | Phase 14 node core | Complete ready publication and explicit unavailable generations | 14-02 | COVERED | Node half of goal. |
| REQ | D-01/D-02/D-03/D-04 | Accepted asynchronous switch, generation events, switching errors, overlap denial | 14-02 | COVERED | Core lifecycle plus checked API seam. |
| REQ | D-05/D-07 | Close before acceptance and drain before initialization | 14-02 | COVERED | Consumes manager core. |
| REQ | D-11/D-16 | No active identity while unavailable; lifecycle-specific processing status | 14-02, 14-08, 14-12, 14-13 | COVERED | Core checked behavior here; final legacy removal follows every caller partition. |
| RESEARCH | Complete typed migration | Every account-bound declaration/caller/target, not only two getters | 14-02, 14-06, 14-07, 14-08, 14-09, 14-10, 14-11, 14-12 | COVERED | Manifest is generated here and consumed in the complete staged partition. |
| CONTEXT | Deferred bridge ownership / trust timers / repository authority | No redesign | NONE | EXCLUDED | Bridge files receive caller adaptation only. |
</multi_source_coverage_audit>

<verification>
Run exact RED evidence before GREEN. Then pass the seven focused cases and fail unless the declaration-driven manifest covers the complete account-bound surface and assigns every caller/target exactly once across Plans 14-06 through 14-12.
</verification>

<success_criteria>
- D-01/D-02/D-04/D-05/D-07 lifecycle core behavior is green.
- D-03/D-11/D-16 checked APIs are generation-aware and configured identity remains private.
- The complete node API caller/target inventory is exhaustive and partitioned before migration begins.
</success_criteria>

<output>
Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md` when done.
</output>
