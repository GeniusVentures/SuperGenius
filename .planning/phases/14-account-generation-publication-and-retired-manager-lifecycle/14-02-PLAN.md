---
phase: 14-account-generation-publication-and-retired-manager-lifecycle
plan: "02"
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/multiaccount/multi_account_sync.cpp
  - test/src/account/account_management_test.cpp
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07, D-11, D-16]
must_haves:
  truths:
    - "SelectAccount returns an accepted generation without waiting for replacement readiness, and the later ready event carries that exact generation, per D-01 and D-02."
    - "From switch acceptance through the single ready commit, every account-bound public operation and overlapping SelectAccount returns typed SWITCH_IN_PROGRESS, while no account, manager, processing owner, or target identity is publicly visible, per D-03 and D-04."
    - "The retiring manager's admission is closed before accepted selection is observable, and replacement account initialization cannot begin until that exact manager reports true drain, per D-05 and D-07."
    - "Every replacement continuation validates both the accepted generation and the expected pending owner; a stale blockchain, manager, processing, or posted callback cannot advance or publish a newer generation, per D-02."
    - "A generation becomes public at one lifecycle-mutex commit only after its account, blockchain, manager, and processing owners are complete; observers can see either the previous complete ready bundle or no ready bundle, never a partial generation."
    - "Configured/bootstrap identity remains available only to internal startup wiring while public GetAddress reports typed ACCOUNT_UNAVAILABLE until a ready bundle exists and never leaks a pending target, establishing the D-11 contract consumed by 14-03."
    - "GetProcessingStatus reports SWITCH_IN_PROGRESS while switching, ACCOUNT_UNAVAILABLE when no ready generation exists, and a generation-tagged status copied only from the ready replacement processing owner, per D-16."
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "Accepted-switch payload, generation-tagged lifecycle event, explicit lifecycle errors, three-bundle ownership records, configured identity separation, and lifecycle-specific processing status contract"
      contains: "AccountSwitchAcceptance"
    - path: "src/account/GeniusNode.cpp"
      provides: "Admission-close acceptance boundary, drain-gated pending initialization, generation-and-owner callback rejection, and sole atomic ready publication"
      contains: "PublishReadyAccountGeneration"
    - path: "test/src/account/account_management_test.cpp"
      provides: "Public accepted-asynchronous, overlap, switching-error, and configured-identity regressions"
      contains: "SelectAccountReturnsGenerationBeforeReadyEvent"
    - path: "test/src/multiaccount/multi_account_sync.cpp"
      provides: "Deterministic drain-before-init, partial-snapshot, stale-callback, and processing-owner regressions"
      contains: "AccountGenerationDrainsBeforeReplacementInitialization"
  key_links:
    - from: "GeniusNode::SelectAccount"
      to: "TransactionManager::CloseAdmission"
      via: "copied retiring manager closed outside lifecycle_mutex_ before AccountSwitchAcceptance is returned"
      pattern: "SelectAccount.*CloseAdmission"
    - from: "TransactionManager::DrainCallback"
      to: "GeniusNode::BeginPendingAccountInitialization"
      via: "io_context-posted continuation guarded by accepted generation and retiring-manager identity"
      pattern: "CloseAdmission.*BeginPendingAccountInitialization"
    - from: "pending blockchain, manager, and processing callbacks"
      to: "PendingAccountGeneration"
      via: "generation plus expected raw-owner identity revalidation under lifecycle_mutex_"
      pattern: 'generation.*get\(\)'
    - from: "GeniusNode::PublishReadyAccountGeneration"
      to: "SnapshotAccountServices and GetProcessingStatus"
      via: "one complete ready-bundle move and lifecycle READY commit"
      pattern: "PublishReadyAccountGeneration"
---

<objective>
Establish the accepted-asynchronous `GeniusNode` switch and atomic ready-publication contract: deterministic RED public/concurrency regressions first, then GREEN generation-tagged readiness, typed switching denial, private pending/retiring ownership, manager-drain serialization, one complete ready commit, bootstrap identity separation, and lifecycle-specific processing status implementing D-01, D-02, D-03, D-04, D-05, D-07, D-11, and D-16.

Purpose: Account selection must acknowledge a request promptly without exposing either a retiring or half-initialized generation, and every asynchronous callback must prove it still belongs to the generation and owner it was created for.
Output: A generation-aware GeniusNode lifecycle API/state machine, two strengthened phase fixtures with seven exact deterministic regressions, focused and complete two-binary evidence, and the ready/failure interfaces consumed by Plan 14-03 without implementing timeout or initialization-failure recovery here.
</objective>

<execution_context>
@/Users/henriqueklein/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PLAN-OUTLINE.md
@.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp
@src/account/TransactionManager.hpp
@test/src/multiaccount/multi_account_sync.cpp
@test/src/account/account_management_test.cpp
@test/src/multiaccount/CMakeLists.txt
@test/src/account/CMakeLists.txt

<interfaces>
Plan 14-01 supplies `TransactionManager::CloseAdmission(DrainCallback)`, immutable manager generation/lifecycle reads, `RetirementSnapshot`, generation-bearing `TransactionCompletion`, typed `SubmitTransaction`, and permanent `MANAGER_RETIRED` admission rejection. Consume these contracts directly; do not recreate manager drain accounting in GeniusNode.

Current node seams to replace in place are `SelectAccount(std::string_view)`, `AccountServiceSnapshot`, `SnapshotAccountServices`, `ApplyIfCurrentAccountServices`, `StateTransition`, `ShutdownAccountBoundServices`, `TransactionStateChanged`, `GetAddress`, `GetTransactionManager`, and inline `GetProcessingStatus`. Current `account_service_switching_` and independently published `account_`, `transaction_manager_`, and `processing_service_` are not sufficient publication authority.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED accepted switching, atomic publication, stale callbacks, identity separation, and status ownership</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.hpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
    - test/src/multiaccount/CMakeLists.txt
    - test/src/account/CMakeLists.txt
  </read_first>
  <action>Add exactly three active public fixture tests: `AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent`, `AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap`, and `AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration`. Add exactly three active multi-account tests: `MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization`, `MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion`, and `MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle`. Strengthen the existing active `MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent` instead of adding a replacement: remove the partial-snapshot skip and require account/manager/processing presence equality for every observation before comparing addresses, owner identity, and generation.

Extend `MultiAccountTestAccess` and the account fixture's existing friend access with default-empty node lifecycle hooks named `after_switch_acceptance_committed`, `after_old_admission_closed`, `before_replacement_initialization`, `after_pending_blockchain_completion`, `before_posted_transaction_initialization`, `before_ready_publication`, and `after_ready_publication`. Add snapshot inspection for lifecycle, accepted generation, ready/pending/retiring owner identities, processing owner, ready event count/payload, and replacement-initialization count. Hooks must only observe named boundaries, must not supply production synchronization, and must be copied/invoked without holding `lifecycle_mutex_` or any manager/service lock.

Use mutex/condition-variable barriers, promises/futures, and atomics only; no sleeps, polling cadence, or wall-clock timing may prove a race. In the acceptance test, block before replacement initialization, call `SelectAccount`, assert its result contains a nonzero generation and normalized target, assert no ready event ran inline, then release and require one later ready event with the same generation. In the switching test, hold the generation before initialization, require a second `SelectAccount` and every exercised account-bound operation to return exact `GeniusNode::Error::SWITCH_IN_PROGRESS`, and prove no queued selection, alternate target owner, or side effect appears. In the configured-identity test, hold initial startup before ready publication, prove internal bootstrap/authorized-node configuration used the configured identity while public `GetAddress` returns typed `ACCOUNT_UNAVAILABLE`, then allow readiness and require only the ready generation address to become public; never expose the configured accessor publicly or relabel the immutable `NodeTrustSigner`.

In the drain test, admit and barrier an old-manager operation using Plan 14-01's hook, accept selection, require `CloseAdmission` before acceptance returns, hold terminalization/drain-zero, and prove `before_replacement_initialization` remains untouched until the admitted operation reaches a canonical terminal state and the exact retiring manager emits drain. In the stale-callback test, capture generation G and its exact pending `Blockchain` owner callback, complete G, accept G+1 and hold it at the same lifecycle stage, then release G's callback/post; require no G+1 transition, owner change, construction count, or ready event. In the processing-status test, barrier before ready publication and require `SWITCH_IN_PROGRESS`; use the explicit unavailable lifecycle seam that Plan 14-03 will drive and require `ACCOUNT_UNAVAILABLE`; after ready publication require generation G and only G's processing status, then invoke the retired processing callback and prove the current result is unchanged. In the strengthened snapshot test, observe continuously at the deterministic publication barriers and assert all ready owners are simultaneously present or absent; no `continue` may discard a partial observation.

Add only the final public value types, error enumerators, callback signatures, snapshot fields, and default-empty hook plumbing needed for the tests to compile during RED. Mechanically adapt `SelectAccount`, `GetAddress`, `GetProcessingStatus`, and their existing callers in these four files to the final result shapes while preserving the current early publication, successful sentinel/default mapping, and state-only callback sequencing; the contract tests must therefore compile and fail their lifecycle assertions rather than fail on an obsolete signature. Record the failing assertion for all seven cases before continuing; a crash, timeout, missing symbol, or disabled name is not an acceptable RED result.</action>
  <verify>
    <automated>set -e; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; for spec in AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration; do if build/OSX/Release/test_bin/account_management_test --gtest_filter="$spec"; then echo "expected RED failure: $spec"; exit 1; fi; done; for spec in MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent; do if build/OSX/Release/test_bin/multi_account_test --gtest_filter="$spec"; then echo "expected RED failure: $spec"; exit 1; fi; done; build/OSX/Release/test_bin/account_management_test --gtest_list_tests | rg -n 'SelectAccountReturnsGenerationBeforeReadyEvent|SwitchInProgressRejectsAccountCallsAndOverlap|ConfiguredIdentityDoesNotPublishUnavailableGeneration'; build/OSX/Release/test_bin/multi_account_test --gtest_list_tests | rg -n 'AccountGenerationDrainsBeforeReplacementInitialization|AccountGenerationRejectsStaleBlockchainCompletion|AccountGenerationProcessingStatusTracksLifecycle|ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent'</automated>
  </verify>
  <acceptance_criteria>
    - All seven exact tests compile, enumerate as active once each, and fail on their intended pre-GREEN lifecycle assertion.
    - The existing concurrent snapshot regression asserts account/manager/processing presence equality before any content check and contains no partial-owner skip.
    - Acceptance, admission close, drain-zero, replacement initialization, stale callback, ready publication, and processing-owner observations are barrier-controlled values rather than elapsed-time inferences.
    - Public tests distinguish accepted generation from later ready event, exact `SWITCH_IN_PROGRESS` from generic readiness, and configured identity from active ready identity.
    - Test callbacks and hooks are default-empty and execute without `lifecycle_mutex_`, manager lifecycle locks, or service locks held.
  </acceptance_criteria>
  <done>The RED suite deterministically exposes synchronous/partial publication, overlapping selection, premature replacement initialization, stale owner advancement, configured-address leakage, and retired processing ownership before the node lifecycle is rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN accepted generation, drain-gated private initialization, and one ready publication</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-01-SUMMARY.md
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - src/account/TransactionManager.hpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
  </read_first>
  <action>Define `AccountLifecycle { BOOTSTRAPPING, READY, SWITCHING, ACCOUNT_UNAVAILABLE }`, `AccountSwitchAcceptance { generation, target_address }`, `AccountSwitchEventKind { ACCOUNT_SWITCH_READY, ACCOUNT_SWITCH_FAILED }`, and `AccountSwitchEvent { kind, generation, target_address, error }`. Change `SelectAccount` to return `outcome::result&lt;AccountSwitchAcceptance&gt;`. Extend the centralized node-state callback/event path with an `AccountSwitchEventCallback` registration surface; copy the callback and immutable payload under `lifecycle_mutex_`, but post/invoke it only after unlock. This plan emits `ACCOUNT_SWITCH_READY`; define the generation-bearing `ACCOUNT_SWITCH_FAILED` payload and dispatch seam for Plan 14-03 without adding timeout, stage-failure cleanup, retry, or recovery behavior.

Replace `account_service_switching_` as publication authority with lifecycle-mutex-owned `ReadyAccountGeneration`, `RetiringAccountGeneration`, and `PendingAccountGeneration` records. The ready record owns the complete published account, blockchain, manager, and processing set (processing queue/core/result-storage/service grouped as one `ProcessingOwners` value) plus generation and address. The retiring record strongly owns the old account-bound runtime through true manager drain. The pending record owns the accepted generation/target and, only after drain, each replacement account/blockchain/manager/processing owner as it is constructed. Bridge provider/relayer/watcher and trust refresh/timer ownership are expressly not added to these records. Keep compatibility member access only as aliases derived at the ready commit if unavoidable; no independently assigned field may act as public publication authority.

Implement `SelectAccount` as the D-01/D-04/D-05 acceptance boundary. Normalize and validate the stored target without publishing or initializing it. Under `lifecycle_mutex_`, reject `SWITCHING` with exact `Error::SWITCH_IN_PROGRESS`; allocate one monotonically increasing generation; move the complete ready record to retiring; clear the ready record; create a target-only pending record; set `AccountLifecycle::SWITCHING`; and copy the old manager. Release the lifecycle lock, call that exact manager's nonblocking `CloseAdmission` with a generation-and-owner-bound drain callback, and do not return accepted success until `CloseAdmission` has completed. If no old manager exists, schedule the same drained continuation. Never call destructive `Stop`, deconfigure the old account, initialize the target account, or invoke a lifecycle event on the `SelectAccount` caller stack. Queue all post-acceptance work on the existing `io_context`, then return `{generation, normalized_target}` promptly. A second selection while that generation is active returns `SWITCH_IN_PROGRESS`; add no cancellation, latest-wins, or queued-follow-up path.

On the drain callback, require all of: callback generation equals the active accepted generation, lifecycle is `SWITCHING`, pending generation matches, and the retiring manager pointer equals the captured expected owner. Only then move the retired runtime out under `lifecycle_mutex_`; after unlock, unregister callbacks, stop/release the drained old runtime, and post `BeginPendingAccountInitialization(generation, target)`. D-07 is strict: `GeniusAccount::NewFromPublicKey`, `InitMessenger`, database dependency configuration, `Blockchain::New`/`Start`, `TransactionManager::New`/`Start`, and processing construction for the replacement all occur after true drain and final release of old mutable account-bound owners. Do not use count-only or synchronous-method completion as drain evidence.

Build the replacement exclusively inside `PendingAccountGeneration`. Every blockchain completion captures generation plus `weak_ptr`/expected raw identity for the pending blockchain and account; every posted transaction initialization additionally revalidates both before constructing a manager; every manager callback captures generation plus expected manager; every processing callback captures generation plus expected processing service. Under `lifecycle_mutex_`, each continuation must find the same pending generation, `SWITCHING`, and exact owner identities before moving to the next stage. Do not accept state-only checks such as `NodeState::INITIALIZING_BLOCKCHAIN`, because two generations may visit the same state. Copy owners/state under lock, release the lock before `Start`, `Stop`, io posting, logging that can reenter, or callback invocation, and revalidate on return.

Create `PublishReadyAccountGeneration(PendingAccountGeneration)` as the only successful publication point for both initial bootstrap and selected replacements. It must verify the accepted generation and expected account/blockchain/manager/processing owner identities, require manager readiness and complete processing ownership, and under one `lifecycle_mutex_` critical section move the entire pending owner set to ready, set the public generation, clear pending/retiring records, set lifecycle `READY`, update compatibility diagnostics, and copy one `ACCOUNT_SWITCH_READY` payload for accepted selections. Before this commit, `SnapshotAccountServices` returns a jointly empty snapshot; after it, the snapshot returns account, manager, processing service, and generation from the one ready record. Dispatch the ready event after unlock and exactly once. Rework `ApplyIfCurrentAccountServices`, transaction-manager state callbacks, slot-hash/catch-up callbacks touched by this pipeline, and posted initialization to require the snapshot generation and all expected owner identities. Preserve Plan 14-01's old-generation terminal delivery and do not read the node's current generation to label it.</action>
  <verify>
    <automated>set -e; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; build/OSX/Release/test_bin/account_management_test --gtest_filter='AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent'; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization:MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion:MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent'; python3 -c 'import pathlib,re; h=pathlib.Path("src/account/GeniusNode.hpp").read_text(); s=pathlib.Path("src/account/GeniusNode.cpp").read_text(); assert all(x in h for x in ["AccountLifecycle","AccountSwitchAcceptance","AccountSwitchEvent","ReadyAccountGeneration","RetiringAccountGeneration","PendingAccountGeneration"]); body=re.search(r"GeniusNode::SelectAccount\b[\s\S]{0,7000}",s).group(); assert "CloseAdmission" in body and "SWITCH_IN_PROGRESS" in body; assert re.search(r"CloseAdmission[\s\S]{0,5000}?BeginPendingAccountInitialization",s); assert len(re.findall(r"PublishReadyAccountGeneration\s*\(",s)) &gt;= 1; assert "account_service_switching_" not in h; t=pathlib.Path("test/src/multiaccount/multi_account_sync.cpp").read_text(); test=re.search(r"ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent[\s\S]{0,9000}",t).group(); assert re.search(r"bool\s*\(\s*observed\.account\s*\)\s*==\s*bool\s*\(\s*observed\.manager\s*\)",test) or "ASSERT_EQ( static_cast&lt;bool&gt;( observed.account ), static_cast&lt;bool&gt;( observed.manager ) )" in test; assert not re.search(r"if\s*\(\s*!observed\.account\s*\|\|\s*!observed\.manager\s*\)\s*continue",test); print("phase14_ready_publication_source_links=PASS")'</automated>
  </verify>
  <acceptance_criteria>
    - D-01/D-02: selection returns a nonzero accepted generation before any inline ready callback, and exactly one later ready event contains the same generation and normalized target.
    - D-04: overlap is rejected with `SWITCH_IN_PROGRESS`; no target replacement, cancellation, or queued selection exists.
    - D-05: the exact retiring manager is closed before accepted success returns, and no destructive stop occurs before its real drain callback.
    - D-07: replacement account construction and all mutable initialization begin only after the old admitted ledger reaches terminal drain and the retiring mutable runtime is released.
    - Every pending continuation checks accepted generation plus expected owner identity and invokes callbacks/posts only after releasing lifecycle/service locks.
    - The ready bundle is published in one commit only when account, blockchain, manager, and processing ownership are complete; the strengthened snapshot and stale-callback cases pass without a partial-owner skip.
    - No timeout, failure-stage cleanup, automatic retry, explicit-recovery flow, bridge ownership redesign, trust refresh/timer work, or repository-wide storage authority is implemented.
  </acceptance_criteria>
  <done>Accepted selection, true drain, private replacement initialization, stale callback rejection, and one generation-tagged ready publication form one deterministic state machine.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN typed active-account availability, configured identity separation, and lifecycle processing status</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp, test/src/multiaccount/multi_account_sync.cpp, test/src/account/account_management_test.cpp</files>
  <read_first>
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-CONTEXT.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-RESEARCH.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-PATTERNS.md
    - .planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-VALIDATION.md
    - src/account/GeniusNode.hpp
    - src/account/GeniusNode.cpp
    - test/src/multiaccount/multi_account_sync.cpp
    - test/src/account/account_management_test.cpp
  </read_first>
  <action>Complete `GeniusNode::Error::SWITCH_IN_PROGRESS` and `GeniusNode::Error::ACCOUNT_UNAVAILABLE` with stable Outcome category strings. Implement one private `RequireReadyAccountGeneration`/snapshot helper that maps `SWITCHING` to `SWITCH_IN_PROGRESS`, every non-ready/non-switching lifecycle to `ACCOUNT_UNAVAILABLE`, and returns a strong complete ready-generation copy only for `READY`. Complete public `GetAddress` as `outcome::result&lt;std::string&gt;` and remove the `"UNVAILABLE"` sentinel and RED success wrapping. Route existing fallible account operations through this helper before validation or side effects so selection-time calls return the lifecycle error rather than generic `TRANSACTIONS_NOT_READY`. Migrate the account/node/manager consumers exercised by this phase in `GeniusNode.cpp` and the two scoped fixtures; do not broaden the edit into bridge, trust-controller, CRDT, or unrelated test-target API redesign.

For currently sentinel/default-returning account-bound surfaces used by these fixtures—active address, balance overloads, transaction manager/status access, transaction list/count, mnemonic, and wait/status reads—provide checked `outcome::result` forms or lifecycle-checked replacements and update the scoped callers so switching/unavailable cannot be confused with zero balance, empty history, `CREATING`, `INVALID`, or an empty optional. Existing methods already returning `outcome::result` must propagate exact `SWITCH_IN_PROGRESS`/`ACCOUNT_UNAVAILABLE`. Keep ordinary manager readiness errors after a ready generation is published; lifecycle denial takes precedence only while there is no ready bundle. This is a bounded typed migration, not a repository-wide transaction API rewrite.

Store the constructor-selected startup account as a private configured/bootstrap identity distinct from the ready bundle and pending selection target. Add a private `GetConfiguredAccountAddress`/configured-account helper for `InitNetwork`, port derivation, authorized-full-node bootstrap setup, trust-policy construction, and diagnostic labels that must work before first ready publication. Update the two test fixtures to retain/use their known configured authority instead of calling public `GetAddress` before readiness. `NodeTrustSigner` remains the immutable node-scoped authority captured at construction; neither `SelectAccount` nor pending/ready publication mutates it. The configured helper must not be public, must not satisfy account-bound operations, and must never expose a failed or pending selection target. Public `GetAddress` reads only `ReadyAccountGeneration` and therefore establishes D-11 now; Plan 14-03 will drive the same `ACCOUNT_UNAVAILABLE` mapping after real failure cleanup.

Complete `GenerationProcessingStatus { generation, status }` and move `GetProcessingStatus` from the RED-adapted inline dereference to `outcome::result&lt;GenerationProcessingStatus&gt;` implemented in `GeniusNode.cpp`. Under `lifecycle_mutex_`, return `SWITCH_IN_PROGRESS` for `SWITCHING`, `ACCOUNT_UNAVAILABLE` for `BOOTSTRAPPING`/`ACCOUNT_UNAVAILABLE`, or copy generation plus the ready processing service for `READY`; release the lock before calling the service. Revalidate generation and processing owner after the query if the service status can race a switch, and return the lifecycle error rather than the retired value when ownership changed. Processing callbacks must capture generation and expected processing owner, revalidate against the ready record, and perform side effects only after unlock. Never consult `processing_service_` from a retiring or pending record as current status.

Run all exact focused cases, then each complete changed binary directly, then the scoped CTest selection. Add mechanical gates proving exact active declaration count, no `DISABLED_` aliases, no partial-snapshot `continue`, no `UNVAILABLE` sentinel, no inline raw `processing_service_` dereference, typed lifecycle errors, one ready publisher, generation/owner callback comparisons, and configured identity remaining private. Compiler errors in unrelated targets are not a reason to weaken D-03 or D-11; keep any necessary caller migration limited to account/node/manager consumers and record every additional file before touching it.</action>
  <verify>
    <automated>set -e; cmake --build build/OSX/Release --target multi_account_test account_management_test -j8; build/OSX/Release/test_bin/account_management_test --gtest_filter='AccountManagement.SelectAccountReturnsGenerationBeforeReadyEvent:AccountManagement.SwitchInProgressRejectsAccountCallsAndOverlap:AccountManagement.ConfiguredIdentityDoesNotPublishUnavailableGeneration'; build/OSX/Release/test_bin/multi_account_test --gtest_filter='MultiAccountTest.AccountGenerationDrainsBeforeReplacementInitialization:MultiAccountTest.AccountGenerationRejectsStaleBlockchainCompletion:MultiAccountTest.AccountGenerationProcessingStatusTracksLifecycle:MultiAccountTest.ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent'; build/OSX/Release/test_bin/multi_account_test; build/OSX/Release/test_bin/account_management_test; ctest --test-dir build/OSX/Release -R '^(multi_account_test|account_management_test)$' --output-on-failure; python3 -c 'import pathlib,re; files=[pathlib.Path("test/src/account/account_management_test.cpp"),pathlib.Path("test/src/multiaccount/multi_account_sync.cpp")]; text="\n".join(p.read_text() for p in files); names=["SelectAccountReturnsGenerationBeforeReadyEvent","SwitchInProgressRejectsAccountCallsAndOverlap","ConfiguredIdentityDoesNotPublishUnavailableGeneration","AccountGenerationDrainsBeforeReplacementInitialization","AccountGenerationRejectsStaleBlockchainCompletion","AccountGenerationProcessingStatusTracksLifecycle","ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent"]; assert all(len(re.findall(r"TEST_F\([^,]+,\s*"+n+r"\s*\)",text))==1 for n in names); assert not any("DISABLED_"+n in text for n in names); h=pathlib.Path("src/account/GeniusNode.hpp").read_text(); s=pathlib.Path("src/account/GeniusNode.cpp").read_text(); assert "SWITCH_IN_PROGRESS" in h and "ACCOUNT_UNAVAILABLE" in h and "GenerationProcessingStatus" in h; assert "UNVAILABLE" not in s; assert re.search(r"outcome::result&lt;\s*std::string\s*&gt;\s+GetAddress",h); assert re.search(r"outcome::result&lt;\s*GenerationProcessingStatus\s*&gt;\s+GetProcessingStatus",h); assert "GetConfiguredAccountAddress" in s; public=h[h.find("public:"):h.find("protected:")]; assert "GetConfiguredAccountAddress" not in public; assert "PublishReadyAccountGeneration" in s; assert re.search(r"generation[\s\S]{0,300}?\.get\(\)",s); snap=re.search(r"ConcurrentSelectAccountSnapshotsAndCatchupCallbacksStayGenerationConsistent[\s\S]{0,9000}",text).group(); assert not re.search(r"if\s*\(\s*!observed\.account\s*\|\|\s*!observed\.manager\s*\)\s*continue",snap); print("phase14_node_publication_contract=PASS")'</automated>
  </verify>
  <acceptance_criteria>
    - D-03/D-04: all exercised account-bound operations and overlap return exact `SWITCH_IN_PROGRESS` throughout the accepted switch, with no generic readiness/default/sentinel substitute.
    - D-11: public active address is a typed ready-generation result; configured/bootstrap identity remains private and startup remains functional without publishing it.
    - D-16: processing status is generation-tagged and lifecycle-specific, and can never come from a retiring, pending, or stale processing owner.
    - D-01/D-02/D-05/D-07 remain green after typed caller migration: acceptance is asynchronous, ready is generation-tagged, close precedes accepted return, and replacement initialization follows true drain.
    - All seven exact focused regressions, both complete changed binaries, scoped CTest, and mechanical source gates pass.
    - No failure timeout/stage cleanup/recovery behavior from Plan 14-03 and no deferred bridge, trust-refresh, or repository-authority work is present.
  </acceptance_criteria>
  <done>The public node surface exposes only a complete ready generation, reports exact switching/unavailable lifecycle state, and returns processing status solely from the generation that owns it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public caller to `SelectAccount` and account APIs | Untrusted overlap/reentrant requests cross the acceptance boundary while the old generation is retiring and the target is not ready. |
| Retiring manager to pending initialization | A manager drain callback controls whether replacement mutable work may begin and must be bound to the exact accepted generation and manager owner. |
| Async blockchain/manager/processing callbacks to lifecycle publication | Posted work may arrive after a later generation occupies the same node state and must not advance by state alone. |
| Configured/bootstrap identity to public active identity | Startup needs an identity before readiness, but callers must not observe configured, retired, or pending target addresses as active. |
| Ready generation to processing status reader | A status query crosses from lifecycle ownership into a service that can be retired concurrently. |

## STRIDE Threat Register — ASVS L1 / HIGH Findings Are Blocking

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan / Blocking Evidence |
|-----------|----------|----------|-----------|-------------|-------------------------------------|
| T14-PP | Tampering / Information Disclosure | HIGH | Ready account generation publication | mitigate | One lifecycle-mutex `PublishReadyAccountGeneration` commit requires all owners; the strengthened concurrent snapshot test rejects every partial account/manager/processing observation. |
| T14-SC | Tampering / Elevation of Privilege | HIGH | Stale asynchronous callback | mitigate | Every callback checks accepted generation plus exact pending/ready owner identity; `AccountGenerationRejectsStaleBlockchainCompletion` proves an old callback cannot advance G+1. |
| T14-ID | Information Disclosure / Spoofing | HIGH | Active-address versus configured identity | mitigate | Private configured accessor supports bootstrap while public typed `GetAddress` reads only ready ownership; `ConfiguredIdentityDoesNotPublishUnavailableGeneration` blocks configured/pending/retired identity leakage. |
| T14-OR | Denial of Service / Tampering | HIGH | Overlap and callback reentrancy | mitigate | Explicit `SWITCHING`, typed overlap denial, no queue/cancel path, and callbacks dispatched after unlock; public overlap test and source lock/callback gates are blocking. |
| T14-PR | Tampering / Denial of Service | HIGH | Processing status and callbacks | mitigate | Lifecycle snapshot copies a ready generation/owner only, checks ownership after service query, and rejects retired callbacks; deterministic processing-status test proves switching/unavailable/replacement states. |
| T14-DR | Tampering | HIGH | Drain-before-initialization handoff | mitigate | Consume Plan 14-01's exact manager drain callback, revalidate generation plus retiring owner, and gate every replacement constructor behind it; deterministic drain barrier is blocking. |
| T14-SUP | Tampering | LOW | npm/pip/cargo supply chain | accept | This plan installs no package and changes no dependency or lockfile, so the package legitimacy gate is not entered. |
</threat_model>

<multi_source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | Phase 14 node-publication slice | Publish only complete ready account generations while retired work drains and unavailable generations remain hidden | 14-02 | COVERED | Implements the node/publication half of the phase outcome; ROADMAP goal text remains unresolved, so the approved outline/context goal is authoritative for this chunk. |
| REQ | D-01 | Selection success acknowledges acceptance, not readiness | 14-02 | COVERED | Tasks 1-2 require an acceptance generation before any asynchronous ready event. |
| REQ | D-02 | Ready/failure event path carries switch generation | 14-02 | COVERED | Task 2 implements ready emission and defines the failure payload/dispatch seam consumed by 14-03. |
| REQ | D-03 | Account-bound operations return `SWITCH_IN_PROGRESS` | 14-02 | COVERED | Tasks 1 and 3 exercise and implement exact typed denial throughout switching. |
| REQ | D-04 | Overlapping selection is rejected with no queue/cancel/latest-wins | 14-02 | COVERED | Tasks 1-2 test and implement one active accepted generation. |
| REQ | D-05 | Retiring admission closes before accepted return | 14-02 | COVERED | Task 2 consumes Plan 14-01's `CloseAdmission` at the public acceptance boundary. |
| REQ | D-07 | Old generation truly drains before replacement initialization | 14-02 | COVERED | Task 2 binds the exact manager drain callback to generation/owner before any target construction or mutable setup. |
| REQ | D-11 | Public active identity is unavailable when no ready generation exists | 14-02 | COVERED | Task 3 separates private configured/bootstrap identity and typed ready-only `GetAddress`; 14-03 supplies real failure transitions. |
| REQ | D-16 | Processing status is switching, unavailable, or replacement-ready | 14-02 | COVERED | Task 3 returns lifecycle errors or generation-tagged ready-owner status and rejects stale processing callbacks. |
| RESEARCH | Three-bundle node lifecycle | Ready, retiring, and pending owners share one lifecycle synchronization domain | 14-02 | COVERED | Task 2 defines bundle ownership and one publication point. |
| RESEARCH | Generation plus expected owner guards | State-only callback checks are insufficient when generations revisit the same state | 14-02 | COVERED | Task 2 applies owner checks to blockchain, posted init, manager, processing, slot-hash, and catch-up continuations in scope. |
| RESEARCH | Typed lifecycle outcomes | Sentinel/default values cannot distinguish switching from unavailable | 14-02 | COVERED | Task 3 adds node errors and checked scoped consumer migration. |
| RESEARCH | Configured/bootstrap identity separation | D-11 cannot break startup consumers that need identity before ready | 14-02 | COVERED | Task 3 keeps configured identity private and preserves immutable NodeTrustSigner authority. |
| VALIDATION | 14-W0-01 | `SelectAccountReturnsGenerationBeforeReadyEvent` | 14-02 | COVERED | Exact active RED then GREEN public case. |
| VALIDATION | 14-W0-02 | `SwitchInProgressRejectsAccountCallsAndOverlap` | 14-02 | COVERED | Exact active deterministic overlap/operation case. |
| VALIDATION | 14-W0-04 | `AccountGenerationDrainsBeforeReplacementInitialization` | 14-02 | COVERED | Exact manager-terminal/drain barrier case. |
| VALIDATION | 14-W0-07 | `ConfiguredIdentityDoesNotPublishUnavailableGeneration` | 14-02 | COVERED | Covers bootstrap identity separation; failure recovery remains in 14-03. |
| VALIDATION | 14-W0-10 | `AccountGenerationProcessingStatusTracksLifecycle` | 14-02 | COVERED | Exact lifecycle/owner status case. |
| VALIDATION | 14-W0-11 | Strengthened coherent snapshot plus stale blockchain completion | 14-02 | COVERED | Removes the partial-snapshot blind spot and adds the exact stale-callback case. |
| CONTEXT | Deferred bridge ownership | Provider/relayer/catch-up watcher publication redesign | NONE | EXCLUDED | Existing mechanical teardown may remain, but these owners are not added to generation bundles. |
| CONTEXT | Deferred trust refresh | `TrustStartupController` refresh coalescing, retries, and timer lifetime | NONE | EXCLUDED | No trust refresh/timer behavior is changed. |
| CONTEXT | Deferred repository authority | `AtomicTransaction`, GlobalDB, or CRDT capability redesign | NONE | EXCLUDED | Lifecycle publication changes no repository-wide mutation authority. |
| CONTEXT | Plan 14-03 scope | Drain timeout, initialization-stage failure cleanup/event, and explicit recovery | 14-03 | EXCLUDED | This plan defines only the lifecycle/error/event seams required by 14-03 and does not implement those behaviors. |
| CONTEXT | Forbidden switch semantics | Cancellation, latest-wins selection, queued follow-up, and automatic retry | NONE | EXCLUDED | Locked D-04 and phase boundary prohibit these behaviors. |
</multi_source_coverage_audit>

<verification>
Execute strictly RED before GREEN. Preserve the seven intended failing assertions from Task 1, then require the acceptance/drain/publication/stale subset to pass after Task 2, and finally require all seven exact cases, both complete changed binaries, scoped CTest, and mechanical source assertions to exit zero. No sleep-based race proof, disabled/omitted exact case, partial-snapshot skip, state-only callback validation, configured/pending address exposure, generic readiness substitute, callback-under-lock dispatch, or independently published processing owner is acceptable.
</verification>

<success_criteria>
- D-01/D-02: one accepted generation is returned promptly and later appears unchanged in exactly one ready event.
- D-03/D-04: switching is a typed public state that rejects account work and overlap without cancellation or queuing.
- D-05/D-07: the exact retiring manager closes before acceptance and truly drains before any replacement mutable initialization.
- D-11: internal configured identity preserves bootstrap while public identity comes only from a complete ready generation.
- D-16: processing status is lifecycle-specific, generation-tagged when ready, and never borrowed from retired/pending ownership.
- Every async continuation revalidates generation plus expected owner, and one ready commit publishes account, blockchain, manager, and processing ownership together.
- All seven focused regressions, both complete affected binaries, scoped CTest, mechanical source gates, and every HIGH threat's blocking evidence pass while explicit exclusions remain untouched.
</success_criteria>

<output>
Create `.planning/phases/14-account-generation-publication-and-retired-manager-lifecycle/14-02-SUMMARY.md` when done.
</output>
