---
phase: 05.1
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - src/account/BridgeRelayer.hpp
  - src/account/BridgeRelayer.cpp
  - src/account/bridge_chains_config.json
  - test/src/account/bridge_relayer_test.cpp
autonomous: true
requirements: []
tags: [rpc, refactor, bridge, observer]

must_haves:
  truths:
    - "BridgeRelayer implements IBridgeInitObserver and receives chains via OnRpcEndpointsReady"
    - "OnRpcEndpointsReady delegates to the existing Start(vector<ChainContractPair>)"
    - "bridge_chains_config.json has a numeric chain_id field on every chain entry"
    - "BridgeRelayer compiles and its existing tests still pass"
  artifacts:
    - path: "src/account/BridgeRelayer.hpp"
      provides: "BridgeRelayer : public IBridgeInitObserver"
      contains: "IBridgeInitObserver"
    - path: "src/account/bridge_chains_config.json"
      provides: "chain_id on every chain entry"
      contains: "chain_id"
  key_links:
    - from: "src/account/BridgeRelayer.cpp OnRpcEndpointsReady"
      to: "BridgeRelayer::Start"
      via: "direct delegation"
      pattern: "Start\\("
---

<objective>
Make `BridgeRelayer` a first-class consumer of the new observer contract (D-03) and add the `chain_id` field to `bridge_chains_config.json` (D-04). After this plan, `BridgeRelayer` can be subscribed to `ChainRpcEndpointProvider` and will self-start when endpoints are ready — GeniusNode no longer hands chain data to the relayer as a middleman.

Purpose: Complete the consumer side of the observer pattern so Plan 03 (GeniusNode orchestration) only needs to call `provider.AddObserver(*bridge_relayer_)`. Adding the `chain_id` field to the config file now means Plan 01's file parser has valid input to consume.

Output: `BridgeRelayer` implementing `IBridgeInitObserver`; `bridge_chains_config.json` enriched with `chain_id`; updated relayer tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-CONTEXT.md
@.planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-01-SUMMARY.md

@src/account/BridgeRelayer.hpp
@src/account/BridgeRelayer.cpp
@src/account/bridge_chains_config.json
@src/account/ChainRpcEndpointProvider.hpp
@src/account/ChainContractPair.hpp
@test/src/account/bridge_relayer_test.cpp

<interfaces>
<!-- Contract established in Plan 01. BridgeRelayer implements this. -->
From src/account/ChainRpcEndpointProvider.hpp (after Plan 01):
```cpp
class IBridgeInitObserver
{
public:
    virtual void OnRpcEndpointsReady( std::vector<ChainContractPair> chains ) = 0;
    virtual ~IBridgeInitObserver() = default;
};
```

From src/account/ChainContractPair.hpp (NEW in Plan 01 — moved out of BridgeRelayer.hpp):
```cpp
struct ChainContractPair
{
    std::string chain_name;
    std::string contract_address;
    uint64_t    chain_id = 0;
};
```
Plan 01 moved this struct into its own shared header and made BOTH ChainRpcEndpointProvider.hpp
and BridgeRelayer.hpp include it — without either of those headers including the other. That
breaks the would-be include cycle, so in THIS plan BridgeRelayer.hpp may safely include
ChainRpcEndpointProvider.hpp to inherit IBridgeInitObserver (the base class is fully defined).

From src/account/BridgeRelayer.hpp (Start signature — unchanged; OnRpcEndpointsReady delegates here):
```cpp
void Start( std::vector<ChainContractPair> chains );  // existing — OnRpcEndpointsReady delegates here
```

Canonical chain IDs (from GeniusNode.cpp:2034-2043, used to populate bridge_chains_config.json):
- ethereum-mainnet = 1
- ethereum-sepolia = 11155111
- bnb-smart-chain = 56
- bnb-smart-chain-testnet = 97
- polygon-mainnet = 137
- polygon-amoy = 80002
- base-mainnet = 8453
- base-sepolia = 84532
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make BridgeRelayer implement IBridgeInitObserver</name>
  <files>src/account/BridgeRelayer.hpp, src/account/BridgeRelayer.cpp</files>
  <read_first>
    - src/account/BridgeRelayer.hpp (full current state — class declaration, Start signature)
    - src/account/BridgeRelayer.cpp (Start implementation, Create factory, constructor)
    - src/account/ChainRpcEndpointProvider.hpp (IBridgeInitObserver interface from Plan 01)
    - src/account/ChainContractPair.hpp (Plan 01 moved ChainContractPair here — confirm it exists and BridgeRelayer.hpp already includes it)
    - .planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-CONTEXT.md (D-03)
  </read_first>
  <behavior>
    - `BridgeRelayer` publicly inherits `IBridgeInitObserver`.
    - `OnRpcEndpointsReady( std::vector<ChainContractPair> chains )` override calls `Start( std::move(chains) )`.
    - The override is declared `override` (compiler-verified against the interface).
    - No change to `Start()` semantics (best-effort multi-chain per D-21).
  </behavior>
  <action>
    In `src/account/BridgeRelayer.hpp`:
    - Add `#include "account/ChainRpcEndpointProvider.hpp"` at the top (Plan 01 placed `IBridgeInitObserver` there). This include is cycle-free: Plan 01 removed `#include "account/BridgeRelayer.hpp"` from the provider header and moved `ChainContractPair` into the shared `ChainContractPair.hpp`, so the provider header no longer depends on this header. `IBridgeInitObserver` is therefore a COMPLETE type when BridgeRelayer inherits it — no incomplete-type / -Wnon-virtual-dtor issue. (Transitive includes ChainRpcEndpointProvider.hpp pulls — PublicChainInputValidator.hpp, base/logger.hpp — do not include BridgeRelayer.hpp.)
    - Change the class declaration from `class BridgeRelayer : public std::enable_shared_from_this<BridgeRelayer>` to `class BridgeRelayer : public IBridgeInitObserver, public std::enable_shared_from_this<BridgeRelayer>`.
    - Add a public method declaration with Doxygen header: `void OnRpcEndpointsReady( std::vector<ChainContractPair> chains ) override;` documenting that it delegates to `Start()` (D-03: BridgeRelayer self-starts when the provider signals readiness).

    In `src/account/BridgeRelayer.cpp`:
    - Add the implementation `void BridgeRelayer::OnRpcEndpointsReady( std::vector<ChainContractPair> chains ) { Start( std::move( chains ) ); }` with a `logger_->info` line stating observer-driven startup. Place it immediately before or after the existing `Start()` definition.

    Do NOT modify `Start()` body. Do NOT change the constructor or `Create()`. Keep noexcept-by-default conventions.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ninja bridge_relayer_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public IBridgeInitObserver" src/account/BridgeRelayer.hpp` returns 1
    - `grep -c "OnRpcEndpointsReady.*override" src/account/BridgeRelayer.hpp` returns 1
    - `grep -c "BridgeRelayer::OnRpcEndpointsReady" src/account/BridgeRelayer.cpp` returns 1
    - `grep -c "Start( std::move( chains ) )" src/account/BridgeRelayer.cpp` returns at least 1
    - `grep -c "account/ChainRpcEndpointProvider.hpp" src/account/BridgeRelayer.hpp` returns at least 1
    - `ninja bridge_relayer_test` compiles without -Wnon-virtual-dtor / -Werror failures (override is verified by compiler against the interface; IBridgeInitObserver is complete)
  </acceptance_criteria>
  <done>BridgeRelayer publicly implements IBridgeInitObserver; OnRpcEndpointsReady delegates to Start(); override keyword present; relayer builds clean with no include cycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add numeric chain_id field to every bridge_chains_config.json entry</name>
  <files>src/account/bridge_chains_config.json</files>
  <read_first>
    - src/account/bridge_chains_config.json (current 8-entry object — each has only bridge_contract_address)
    - .planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-CONTEXT.md (D-04: chain_id numeric, not hex string; entries lacking chain_id are skipped)
    - src/account/GeniusNode.cpp lines 2034-2043 (the kChainNameToId map being eliminated — these are the authoritative chain_id values)
  </read_first>
  <behavior>
    - Every chain entry in bridge_chains_config.json gains a numeric `chain_id` field matching the value previously hardcoded in `kChainNameToId`.
    - Values are decimal integers (NOT hex strings) per CONTEXT Specifics.
    - JSON remains valid (parseable by boost::json).
    - The 8 existing chains are preserved with their existing `bridge_contract_address` values unchanged.
  </behavior>
  <action>
    Add a `"chain_id": <N>` field to each of the 8 chain entries in `src/account/bridge_chains_config.json`, using these exact values (from the authoritative kChainNameToId):
    - ethereum-mainnet: `"chain_id": 1`
    - ethereum-sepolia: `"chain_id": 11155111`
    - bnb-smart-chain: `"chain_id": 56`
    - bnb-smart-chain-testnet: `"chain_id": 97`
    - polygon-mainnet: `"chain_id": 137`
    - polygon-amoy: `"chain_id": 80002`
    - base-mainnet: `"chain_id": 8453`
    - base-sepolia: `"chain_id": 84532`

    Keep every existing `bridge_contract_address` value exactly as-is. Place `chain_id` as the first field in each entry object (before bridge_contract_address) for readability. Maintain valid JSON (trailing commas forbidden, 4-space indentation consistent with the file). Do NOT add any other fields. Do NOT add a metadata `_comment` entry.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('src/account/bridge_chains_config.json')); assert len(d)==8; assert all('chain_id' in v and 'bridge_contract_address' in v for v in d.values()); assert d['ethereum-mainnet']['chain_id']==1; print('valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('src/account/bridge_chains_config.json'))"` succeeds (valid JSON)
    - The file has exactly 8 entries, each with a numeric `chain_id` and a `bridge_contract_address`
    - `ethereum-mainnet` chain_id is 1; `ethereum-sepolia` chain_id is 11155111; `base-sepolia` chain_id is 84532
    - No `bridge_contract_address` value changed from its original (diff the file — only chain_id fields added)
  </acceptance_criteria>
  <done>bridge_chains_config.json carries a numeric chain_id on all 8 chains matching the former kChainNameToId values; JSON is valid; addresses unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add observer-driven start coverage to bridge_relayer_test.cpp</name>
  <files>test/src/account/bridge_relayer_test.cpp</files>
  <read_first>
    - test/src/account/bridge_relayer_test.cpp (existing tests — understand the test fixture / mocks used for EthWatchService + TransactionManager)
    - src/account/BridgeRelayer.hpp (new IBridgeInitObserver override)
    - src/account/BridgeRelayer.cpp (OnRpcEndpointsReady delegates to Start)
  </read_first>
  <behavior>
    - A new test constructs a BridgeRelayer, calls `OnRpcEndpointsReady` with a small chain list, and asserts that `Start` was invoked (same observable effect as calling Start directly — a watch registered per chain with a valid contract address, D-21 best-effort for invalid ones).
    - The test does NOT sleep (project rule — use the existing wait-condition pattern if async observation is needed; if Start is synchronous, assert directly).
  </behavior>
  <action>
    Read the existing test file to learn how it observes `Start` (likely via `BridgeRelayerTestAccess` friend or a mock `EthWatchService`). Add `TEST( BridgeRelayerTest, OnRpcEndpointsReadyDelegatesToStart )`: build a `std::vector<ChainContractPair>` with one valid chain (e.g. `{"ethereum-sepolia", "0x9af8050220D8C355CA3c6dC00a78B474cd3e3c70", 11155111}`), call `relayer->OnRpcEndpointsReady( chains )`, then assert the observable side-effect of Start that the existing Start test already asserts (e.g. a watch id registered in `chain_watches_` via `BridgeRelayerTestAccess`, or a mock watch-service call). Reuse the same fixture/setup as the existing Start test — do not invent new infrastructure.

    Add a second assertion: calling `OnRpcEndpointsReady` with an empty vector does not register any watch (degenerates to Start({}) which is a no-op per D-21).

    Follow existing test conventions in the file (gtest, no sleep_for).
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ./test_bin/bridge_relayer_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OnRpcEndpointsReady" test/src/account/bridge_relayer_test.cpp` returns at least 2 (call + test name)
    - `grep -c "sleep_for" test/src/account/bridge_relayer_test.cpp` returns 0 (no banned sleep pattern added)
    - `./test_bin/bridge_relayer_test` exits 0
  </acceptance_criteria>
  <done>bridge_relayer_test proves OnRpcEndpointsReady triggers Start's observable effect; no sleeps added; all relayer tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider → relayer (observer callback) | chain list delivered async on io_context; relayer trusts provider-supplied chain_id/contract pairs |
| config file → consensus | chain_id values flow into consensus validator registration; an incorrect chain_id misroutes verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-05 | Tampering | bridge_chains_config.json chain_id values | mitigate | values are operator-controlled local config; cross-checked against known EVM chain IDs at authoring time (this task uses the authoritative kChainNameToId values) |
| T-05.1-06 | Repudiation | observer-driven Start (no explicit GeniusNode call) | accept | startup logging in OnRpcEndpointsReady provides the audit trail; observable behavior identical to direct Start |
| T-05.1-07 | Tampering | IBridgeInitObserver dispatch into BridgeRelayer | mitigate | single-threaded io_context (established pattern); override keyword compiler-verifies the contract; IBridgeInitObserver is a complete type (include cycle broken by Plan 01's shared ChainContractPair.hpp) |
</threat_model>

<verification>
- `ninja bridge_relayer_test` builds clean
- `./test_bin/bridge_relayer_test` exits 0
- `bridge_chains_config.json` is valid JSON with chain_id on all 8 entries
- BridgeRelayer publicly inherits IBridgeInitObserver with an `override` OnRpcEndpointsReady
</verification>

<success_criteria>
- BridgeRelayer self-starts via the observer callback (D-03 consumer side complete)
- Config file carries chain_id (D-04 data side complete), enabling Plan 01's parser
- No regressions in existing bridge_relayer tests
</success_criteria>

<output>
Create `.planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-02-SUMMARY.md` when done
</output>
