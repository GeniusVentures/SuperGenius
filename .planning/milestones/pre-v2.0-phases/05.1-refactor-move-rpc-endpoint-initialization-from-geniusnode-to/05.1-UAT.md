---
status: testing
phase: 05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to
source: 05.1-01-SUMMARY.md, 05.1-02-SUMMARY.md, 05.1-03-SUMMARY.md
started: 2026-06-18T00:00:00Z
updated: 2026-06-18T00:00:00Z
---

## Current Test

number: 1
name: Build and all 47 tests pass
expected: |
  Clean build of all 3 test suites (chain_rpc_endpoint_provider_test, bridge_relayer_test, startup_wiring_test) plus the E2E chainlist test. All 47 tests pass — 8 provider + 19 relayer + 20 startup wiring. 4 chainlist E2E tests also pass (mock transport).
awaiting: user response

## Tests

### 1. Build and all 47 tests pass
expected: Clean build of all 3 test suites (chain_rpc_endpoint_provider_test, bridge_relayer_test, startup_wiring_test) plus the E2E chainlist test. All 47 tests pass — 8 provider + 19 relayer + 20 startup wiring. 4 chainlist E2E tests also pass (mock transport).
result: pending

### 2. GeniusNode is a thin orchestrator — InitializeRpcEndpoints removed
expected: Searching `src/account/GeniusNode.cpp` for `InitializeRpcEndpoints` returns zero occurrences. The ~175-line method, `kChainNameToId` map, and `ChainRpcProviderConfig` struct are all gone. `GeniusNode` now has `ResolveBridgeChainsConfigPath()` + `OnRpcEndpointsReady()` instead.
result: pending

### 3. Observer pattern wires correctly — BridgeRelayer receives chains
expected: `BridgeRelayer` publicly inherits `IBridgeInitObserver`. `GeniusNode` also inherits `IBridgeInitObserver`. Both register via `provider.AddObserver()`. When `Initialize()` succeeds, `OnRpcEndpointsReady` fires on both observers.
result: pending

### 4. Provider Initialize reads bridge_chains_config.json with chain_id
expected: `ChainRpcEndpointProvider::Initialize(path, validator)` opens `bridge_chains_config.json`, requires numeric `chain_id` on each entry (skips entries missing it), calls `IInputValidator::Register` + `SetRpcEndpoints` per chain, and notifies observers when chains are discovered. Returns `true` on success, `false` on failure.
result: pending

### 5. All 8 bridge_chains_config.json entries have chain_id + contract_address
expected: Every entry in `src/account/bridge_chains_config.json` has a numeric `chain_id` field and a `bridge_contract_address` field. The 8 chains are: ethereum-mainnet(1), ethereum-sepolia(11155111), bnb-smart-chain(56), bnb-smart-chain-testnet(97), polygon-mainnet(137), polygon-amoy(80002), base-mainnet(8453), base-sepolia(84532).
result: pending

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

[none yet]
