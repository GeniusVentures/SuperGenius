---
phase: 05.1
plan: 03
type: execute
wave: 3
depends_on: ["05.1-01", "05.1-02"]
files_modified:
  - src/account/GeniusNode.hpp
  - src/account/GeniusNode.cpp
  - test/src/startup/startup_wiring_test.cpp
autonomous: true
requirements: []
tags: [rpc, refactor, bridge, orchestration]

must_haves:
  truths:
    - "GeniusNode resolves the bridge_chains_config.json path (D-01) and passes it to ChainRpcEndpointProvider::Initialize"
    - "GeniusNode no longer parses bridge_chains_config.json, builds ChainRpcProviderConfig, or calls IInputValidator::Register directly"
    - "GeniusNode subscribes BridgeRelayer to the provider via AddObserver before posting Initialize"
    - "The bridge_chains_ member is removed from GeniusNode"
    - "The kChainNameToId map is removed from GeniusNode.cpp"
    - "PerformStartupCatchupScan still receives the chain list (GeniusNode captures chains via the observer callback it also implements)"
    - "Startup ordering preserved: subscribe (sync) → post → Initialize (async) → observer callbacks (async)"
    - "The node boots and bridge wiring compiles without the old ~175-line InitializeRpcEndpoints body"
  artifacts:
    - path: "src/account/GeniusNode.hpp"
      provides: "InitializeAndStartBridge as thin orchestrator; GeniusNode : public IBridgeInitObserver for catch-up; bridge_chains_ removed"
      contains: "IBridgeInitObserver"
    - path: "src/account/GeniusNode.cpp"
      provides: "path resolution + provider.AddObserver + boost::asio::post(Initialize)"
      contains: "AddObserver"
  key_links:
    - from: "src/account/GeniusNode.cpp InitializeAndStartBridge"
      to: "ChainRpcEndpointProvider::AddObserver"
      via: "subscribe relayer + self before post"
      pattern: "AddObserver"
    - from: "src/account/GeniusNode.cpp InitializeAndStartBridge"
      to: "ChainRpcEndpointProvider::Initialize"
      via: "boost::asio::post with resolved config path"
      pattern: "Initialize\\("
---

<objective>
Complete the refactor by reducing `GeniusNode` to a thin orchestrator: resolve the config path (D-01 — stays in GeniusNode because it knows `DevConfig::BaseWritePath` and the binary location), construct the provider, register observers (`BridgeRelayer` and `GeniusNode`-itself for catch-up scan), and post `Initialize()` to the io_context (D-04 ordering preserved). Remove the ~175-line `InitializeRpcEndpoints()` body, the `kChainNameToId` map (D-04), and the `bridge_chains_` member (D-02).

Purpose: This is the capstone that realizes the phase goal — GeniusNode is no longer the middleman parsing config and handing chain data to the relayer. The provider owns parsing; observers receive data directly.

Output: Slimmed `GeniusNode.{hpp,cpp}` with the new orchestration; `startup_wiring_test.cpp` updated to assert the new architecture (no kChainNameToId, path-resolution present, observer wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-CONTEXT.md
@.planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-01-SUMMARY.md
@.planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-02-SUMMARY.md

@src/account/GeniusNode.hpp
@src/account/GeniusNode.cpp
@src/account/ChainRpcEndpointProvider.hpp
@src/account/ChainContractPair.hpp
@src/account/BridgeRelayer.hpp
@test/src/startup/startup_wiring_test.cpp

<interfaces>
<!-- Contracts from Plans 01 & 02 this orchestrator calls. -->

From src/account/ChainRpcEndpointProvider.hpp (after Plan 01):
```cpp
class ChainRpcEndpointProvider
{
public:
    ChainRpcEndpointProvider() = default;   // no chain_id_map ctor
    void AddObserver( IBridgeInitObserver &observer );
    bool Initialize( const std::filesystem::path &bridge_chains_config_path,
                     PublicChainInputValidator   &validator,
                     const base::Logger          &logger );
};

class IBridgeInitObserver
{
public:
    virtual void OnRpcEndpointsReady( std::vector<ChainContractPair> chains ) = 0;
    virtual ~IBridgeInitObserver() = default;
};
```
NOTE (Plan 01): `ChainContractPair` now lives in its own shared header
`src/account/ChainContractPair.hpp`, included transitively by ChainRpcEndpointProvider.hpp
(and BridgeRelayer.hpp). GeniusNode gets it via the provider include — do NOT expect it in
BridgeRelayer.hpp. This shared header is what broke the include cycle between the provider
and the relayer, so this orchestrator compiles cleanly.

From src/account/BridgeRelayer.hpp (after Plan 02):
```cpp
class BridgeRelayer : public IBridgeInitObserver, public std::enable_shared_from_this<BridgeRelayer>
{
    void Start( std::vector<ChainContractPair> chains );
    void OnRpcEndpointsReady( std::vector<ChainContractPair> chains ) override;  // delegates to Start
};
```

GeniusNode members that REMAIN relevant (from GeniusNode.hpp:610-625):
- `transaction_manager_` — provides `GetPublicChainInputValidator()`
- `bridge_relayer_` — shared_ptr, becomes an observer
- `dev_config_` — provides `BaseWritePath` for path resolution (D-01)
- `node_logger_` — passed to Initialize

GeniusNode members being REMOVED (D-02):
- `bridge_chains_` (GeniusNode.hpp:625) — replaced by a member populated via OnRpcEndpointsReady for the catch-up scan
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite GeniusNode as thin orchestrator + IBridgeInitObserver for catch-up</name>
  <files>src/account/GeniusNode.hpp, src/account/GeniusNode.cpp</files>
  <read_first>
    - src/account/GeniusNode.cpp lines 432-455 (the INITIALIZING_TRANSACTIONS post site — where bridge_relayer_ is created and InitializeAndStartBridge is posted)
    - src/account/GeniusNode.cpp lines 1983-2158 (the InitializeRpcEndpoints body being REMOVED)
    - src/account/GeniusNode.cpp lines 2161-2186 (InitializeAndStartBridge — being rewritten)
    - src/account/GeniusNode.cpp lines 2188-2400 (PerformStartupCatchupScan — consumes bridge_chains_, needs the new member)
    - src/account/GeniusNode.cpp lines 2404-2424 (TransactionStateChanged — triggers catch-up post)
    - src/account/GeniusNode.hpp lines 610-630 (members), 730-760 (method declarations)
    - src/account/ChainRpcEndpointProvider.hpp (Plan 01 — Initialize(path), AddObserver, IBridgeInitObserver)
    - src/account/ChainContractPair.hpp (Plan 01 — the shared ChainContractPair struct used by catchup_chains_)
    - .planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-CONTEXT.md (D-01 path stays, D-02 Register + bridge_chains_ removed, D-03 observer, D-04 chain_id from file)
  </read_first>
  <action>
    Header changes (`src/account/GeniusNode.hpp`):
    - Add `#include "account/ChainRpcEndpointProvider.hpp"` (brings IBridgeInitObserver + ChainRpcEndpointProvider; ChainContractPair comes transitively from the shared ChainContractPair.hpp — Plan 01). GeniusNode does NOT include BridgeRelayer.hpp for the struct (it already includes it for bridge_relayer_; the struct itself is sourced from the shared header either way).
    - Make `GeniusNode` implement the observer for the catch-up scan: change the class declaration to add `public IBridgeInitObserver` (in addition to its existing bases). Add a public method `void OnRpcEndpointsReady( std::vector<ChainContractPair> chains ) override;` with a Doxygen header stating it stores the chain list for `PerformStartupCatchupScan`.
    - Add a private member `std::vector<ChainContractPair> catchup_chains_;` to replace `bridge_chains_` (D-02 removes bridge_chains_; the catch-up scan still needs the list, now sourced from the observer callback rather than GeniusNode parsing). Add a brief Doxygen comment noting it is populated by `OnRpcEndpointsReady`.
    - REMOVE the `std::vector<ChainContractPair> bridge_chains_;` member (line 625).
    - REMOVE the `std::vector<ChainContractPair> InitializeRpcEndpoints();` declaration (line 739). Keep `InitializeAndStartBridge()` declaration but update its Doxygen to describe the thin-orchestrator flow (resolve path, construct provider, AddObserver for relayer + self, post Initialize).
    - Add a private member `std::unique_ptr<ChainRpcEndpointProvider> rpc_endpoint_provider_;` to hold the provider across the async Initialize (the provider must outlive the posted task). Add `#include <memory>` if not already present (it is).

    Implementation changes (`src/account/GeniusNode.cpp`):
    - DELETE the entire `InitializeRpcEndpoints()` body (lines 1983-2158) including the `kChainNameToId` map (D-04 — chain IDs now come from the file via Plan 01's parser).
    - Rewrite `InitializeAndStartBridge()` (lines 2161-2186) to:
      1. Resolve the config path using the SAME priority logic that existed (D-01: BaseWritePath → binary-relative via `boost::dll::program_location().parent_path()` → CWD fallback). Extract this resolution into a small private helper `std::filesystem::path ResolveBridgeChainsConfigPath() const` to keep InitializeAndStartBridge readable. Keep the exact path-resolution code (GeniusNode.cpp:1992-2025) verbatim in the helper — this is the one piece that STAYS in GeniusNode per D-01.
      2. Construct the provider: `rpc_endpoint_provider_ = std::make_unique<ChainRpcEndpointProvider>();`.
      3. Subscribe observers synchronously BEFORE the post (D-03 ordering): `rpc_endpoint_provider_->AddObserver( *this );` then `if ( bridge_relayer_ ) { rpc_endpoint_provider_->AddObserver( *bridge_relayer_ ); }`. Use dereference-to-reference (AddObserver takes `IBridgeInitObserver&`).
      4. Post Initialize: `boost::asio::post( *io_, [weak_self = weak_from_this()]() { if ( auto s = weak_self.lock() ) { s->rpc_endpoint_provider_->Initialize( s->resolved_path, s->transaction_manager_->GetPublicChainInputValidator(), s->node_logger_ ); } } );`. Capture the resolved path by copying it into the lambda (resolve before the post, capture by value).
      5. Remove the old direct call to `bridge_relayer_->Start(...)` from InitializeAndStartBridge — the relayer now starts itself via its observer callback. Remove the `bridge_chains_ = bridge_chains;` assignment.
    - Implement `void GeniusNode::OnRpcEndpointsReady( std::vector<ChainContractPair> chains )` to store `catchup_chains_ = std::move( chains );` and log the count. This populates the data `PerformStartupCatchupScan` needs.
    - Update `PerformStartupCatchupScan()`: change the loop over `bridge_chains_` (line 2213) to loop over `catchup_chains_` instead. No other logic change in the scan body — it already uses `chain_entry.chain_id`, `chain_entry.chain_name`, `chain_entry.contract_address` which all still exist on ChainContractPair.
    - Remove now-unused includes that were only needed by InitializeRpcEndpoints (e.g. if `<eth/abi_decoder.hpp>` or `rlp::base::parse::hex_bytes` are no longer referenced in GeniusNode.cpp after deletion — verify with grep before removing; the catch-up scan still uses `eth::abi::event_signature_hash` and `hex_bytes` at lines 2198-2200, so KEEP those includes). Do not remove includes still used by the catch-up scan.

    Constraints: noexcept-by-default where the existing code is noexcept. Allman bracing. No magic numbers (the scan depth `kBridgeCatchupScanDepth` already exists — keep it). Do NOT change the catch-up scan's RPC transport logic or the `NodeState` transitions. Do NOT change the TransactionStateChanged READY-post for the catch-up scan (lines 2413-2423) — it already correctly posts PerformStartupCatchupScan; it will now read catchup_chains_.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ninja genius_node_test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "kChainNameToId" src/account/GeniusNode.cpp` returns 0 (map eliminated per D-04)
    - `grep -c "bridge_chains_" src/account/GeniusNode.cpp` returns 0 (member removed per D-02) — note: must check the catch-up scan loop was updated to catchup_chains_
    - `grep -c "InitializeRpcEndpoints" src/account/GeniusNode.cpp` returns 0 (method body removed; only InitializeAndStartBridge remains)
    - `grep -c "AddObserver" src/account/GeniusNode.cpp` returns at least 2 (self + relayer subscription)
    - `grep -c "ResolveBridgeChainsConfigPath" src/account/GeniusNode.cpp` returns at least 2 (definition + call)
    - `grep -c "OnRpcEndpointsReady" src/account/GeniusNode.cpp` returns at least 1 (GeniusNode's own override) and `grep -c "OnRpcEndpointsReady" src/account/GeniusNode.hpp` returns at least 1
    - `grep -c "catchup_chains_" src/account/GeniusNode.cpp` returns at least 2 (populate in observer + read in scan)
    - `grep -c "IInputValidator::Register" src/account/GeniusNode.cpp` returns 0 (Register moved to provider per D-02)
    - `ninja genius_node_test` compiles clean
  </acceptance_criteria>
  <done>GeniusNode resolves the config path, constructs the provider, subscribes relayer + self, posts Initialize. InitializeRpcEndpoints body, kChainNameToId, bridge_chains_, and the direct IInputValidator::Register loop are all gone. Catch-up scan reads catchup_chains_ populated via the observer callback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update startup_wiring_test.cpp to assert the new architecture</name>
  <files>test/src/startup/startup_wiring_test.cpp</files>
  <read_first>
    - test/src/startup/startup_wiring_test.cpp (full file — identify every assertion referencing kChainNameToId, InitializeRpcEndpoints, bridge_chains_, the old 8-entry map)
    - src/account/GeniusNode.hpp (new IBridgeInitObserver on GeniusNode, no bridge_chains_, no InitializeRpcEndpoints)
    - src/account/ChainRpcEndpointProvider.hpp (Plan 01 — AddObserver, Initialize(path), IBridgeInitObserver)
    - .planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-CONTEXT.md (D-04: chain_id sourced from file)
  </read_first>
  <behavior>
    - No test references the eliminated `kChainNameToId` map or `InitializeRpcEndpoints` as a GeniusNode method.
    - The structural ordering test (BridgeInitializationOrderingIsCorrect) is rewritten to assert the new architecture: provider owns initialization; relayer + GeniusNode subscribe as observers; ordering is subscribe→post→Initialize→callbacks (D-03/D-04).
    - The "without endpoint sources" / graceful-degradation tests (lines ~413-530) are updated to call the new `ChainRpcEndpointProvider::Initialize(path, validator, logger)` with a temp config file rather than the removed `ChainRpcProviderConfig`.
    - Tests do not sleep (project rule).
  </behavior>
  <action>
    Audit `test/src/startup/startup_wiring_test.cpp` line-by-line. For each test:

    - `BridgeInitializationOrderingIsCorrect` (line 222): the hardcoded 8-entry `deployed` map and the comment referencing `kChainNameToId` are OBSOLETE (D-04 eliminated the map). Rewrite this test to assert the new contracts instead: assert `ChainRpcEndpointProvider` exposes `AddObserver` and `Initialize(path, validator, logger)` (compile-time interface check by constructing a provider and an observer stub), and assert `BridgeRelayer` is-an `IBridgeInitObserver` (static_assert / std::is_base_of). Replace the 8-entry map with an assertion that chain IDs now live in `bridge_chains_config.json` (read the bundled file at `src/account/bridge_chains_config.json` and assert each entry has a numeric `chain_id`).

    - Any test calling `provider.Initialize( validator, config, logger )` with a `ChainRpcProviderConfig` (lines ~417-510): replace with a temp `bridge_chains_config.json`-format file (object keyed by chain name with `chain_id` + `bridge_contract_address`) and call `provider.Initialize( tmppath, validator, logger )`. Update the behavioral assertions (no-register-on-failure, graceful degradation) to the new return-bool semantics.

    - Any test referencing `InitializeRpcEndpoints` or `bridge_chains_` as a GeniusNode member: remove or convert to an assertion against the observer-driven flow (e.g. a recording observer receiving chains).

    - The catch-up-scan tests (CatchupScanDefaultDepth, CatchupScanBurnedUtxoSkippedIfConsumed, CatchupScanTopic0HexConversion) at lines ~255-310: these assert pure constants/logic that still hold — keep them, but update any comment referencing `bridge_chains_` to reference `catchup_chains_`.

    Keep `#include` list consistent with the new API (add ChainRpcEndpointProvider.hpp, IBridgeInitObserver usage). Do NOT introduce `std::this_thread::sleep_for`.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ninja startup_wiring_test 2>&1 | tail -20 && ./test_bin/startup_wiring_test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "kChainNameToId" test/src/startup/startup_wiring_test.cpp` returns 0
    - `grep -c "InitializeRpcEndpoints" test/src/startup/startup_wiring_test.cpp` returns 0
    - `grep -c "ChainRpcProviderConfig" test/src/startup/startup_wiring_test.cpp` returns 0
    - `grep -c "IBridgeInitObserver\|AddObserver\|is_base_of" test/src/startup/startup_wiring_test.cpp` returns at least 1
    - `grep -c "sleep_for" test/src/startup/startup_wiring_test.cpp` returns 0
    - `./test_bin/startup_wiring_test` exits 0
  </acceptance_criteria>
  <done>startup_wiring_test asserts the new observer-driven architecture; all references to kChainNameToId, InitializeRpcEndpoints, and ChainRpcProviderConfig are gone; catch-up-scan logic tests preserved; no sleeps; test exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GeniusNode → provider (async post) | path crosses into io_context thread; provider reads file + notifies observers there |
| provider → GeniusNode (observer callback) | chain list delivered to catch-up scan; scan issues RPC eth_getLogs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-08 | Tampering | ResolveBridgeChainsConfigPath (CWD fallback) | accept | path resolution unchanged from pre-refactor GeniusNode.cpp:1992-2025; CWD fallback is the existing last-resort behavior, not newly introduced |
| T-05.1-09 | Denial of Service | async Initialize post + observer fan-out | accept | single-threaded io_context (established pattern); provider catches malformed JSON (Plan 01) |
| T-05.1-10 | Elevation of Privilege | GeniusNode implementing IBridgeInitObserver (new virtual surface) | mitigate | virtual dtor inherited from IBridgeInitObserver (Plan 01); override keyword compiler-verifies; single-threaded dispatch |
| T-05.1-11 | Tampering | catch-up scan reading catchup_chains_ (observer-populated) | mitigate | catchup_chains_ only mutated in OnRpcEndpointsReady before scan runs; scan is a single-shot post-guarded by catchup_scan_done_ (existing guard preserved) |
</threat_model>

<verification>
- `ninja genius_node_test startup_wiring_test` builds clean (no -Wnon-virtual-dtor, no -Werror)
- `./test_bin/startup_wiring_test` exits 0
- Full bridge-related test sweep passes: `./test_bin/chain_rpc_endpoint_provider_test && ./test_bin/bridge_relayer_test && ./test_bin/startup_wiring_test`
- GeniusNode.cpp contains no `kChainNameToId`, no `InitializeRpcEndpoints` body, no `bridge_chains_`, no direct `IInputValidator::Register`
- Path-resolution logic preserved verbatim in `ResolveBridgeChainsConfigPath`
- Startup ordering preserved: observer subscriptions happen synchronously before the `boost::asio::post(Initialize)`
</verification>

<success_criteria>
- GeniusNode is a thin orchestrator: resolve path → construct provider → AddObserver(relayer) + AddObserver(self) → post Initialize
- All endpoint-init logic moved out of GeniusNode (D-01, D-02, D-04 realized)
- BridgeRelayer self-starts via observer; catch-up scan receives chains via the same observer callback on GeniusNode
- No regressions: startup wiring, provider, and relayer test suites all green
- Node build target compiles warning-free
</success_criteria>

<output>
Create `.planning/phases/05.1-refactor-move-rpc-endpoint-initialization-from-geniusnode-to/05.1-03-SUMMARY.md` when done
</output>
