---
phase: 08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-03"]
files_modified:
  - test/src/bridge_race/bridge_race_fault_kill_test.cpp
  - test/src/bridge_race/bridge_race_fault_partition_test.cpp
  - test/src/bridge_race/CMakeLists.txt
autonomous: true
requirements: [D-10, D-11]

master_note: "depends_on 08-03 solely because both plans append to the shared test/src/bridge_race/CMakeLists.txt file — enforces sequential CMake edits, no logical coupling to the RPC fault test itself."

must_haves:
  truths:
    - "Killing a node (object destruction via node.reset()) mid-mint does not prevent the remaining 10 nodes from converging on exactly-once mint"
    - "D-11: The partition is induced by disconnecting/reconnecting peers at the pubsub/libp2p layer"
    - "A pubsub partition that isolates a subset of nodes, followed by a heal, results in CRDT convergence to exactly-once mint across all 11 nodes (no permanent fork, no double-mint post-heal)"
  artifacts:
    - path: "test/src/bridge_race/bridge_race_fault_kill_test.cpp"
      provides: "Node-kill mid-mint fault test using node.reset()"
      min_lines: 35
    - path: "test/src/bridge_race/bridge_race_fault_partition_test.cpp"
      provides: "Pubsub partition + heal fault test using libp2p Host::disconnect()/GossipPubSub::AddPeers()"
      min_lines: 40
  key_links:
    - from: "test/src/bridge_race/bridge_race_fault_partition_test.cpp"
      to: "3rdparty/libp2p/include/libp2p/host/host.hpp"
      via: "GetPubSub()->GetHost()->disconnect(peer_id)"
      pattern: "disconnect\\("
---

<objective>
Add the two remaining fault-injection scenarios: node-kill mid-mint (object-lifecycle destruction,
not process-level crash — that is explicitly deferred per CONTEXT.md) and pubsub partition + heal,
proving CRDT convergence to exactly-once mint survives both.

Purpose: Completes D-08's four fault scenarios (RPC disagreement covered by 08-03; kill and
partition covered here). The partition test is the harder correctness case per 08-RESEARCH.md —
CRDT must reconcile two temporarily-diverged sub-clusters into one consistent exactly-once
outcome after the mesh heals.
Output: `bridge_race_fault_kill_test.cpp`, `bridge_race_fault_partition_test.cpp`, both registered
in `test/src/bridge_race/CMakeLists.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-/08-CONTEXT.md
@.planning/phases/08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-/08-RESEARCH.md
@.planning/phases/08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-/08-PATTERNS.md
@test/src/bridge_race/bridge_race_fixture.hpp
@test/src/bridge_race/CMakeLists.txt
</context>

<interfaces>
`std::shared_ptr<GeniusNode>::reset()` — standard shared_ptr destruction; mirrors
`BridgeAnvilCatchupE2ETest::TearDownTestSuite()`'s per-node `node.reset()` calls (already read this
session), just invoked mid-test on one array element instead of at suite teardown.

`GossipPubSub::AddPeers(const std::vector<std::string> &bootstrapPeers)` (heal/bootstrap — already
used in 08-01's fixture) and `GossipPubSub::GetHost()` returning `libp2p::Host` (both confirmed
present, `3rdparty/ipfs-pubsub/.../gossip_pubsub.hpp`).
`virtual void disconnect(const peer::PeerId &peer_id) = 0;` (`3rdparty/libp2p/include/libp2p/host/host.hpp:171`).

08-RESEARCH.md Open Question 2 (LOW confidence, flagged): the exact `PeerId` extraction API for a
REMOTE node from the local node's perspective is unconfirmed. This plan's Task 2 begins with a
small investigation step to resolve this before writing partition assertions — do not guess the
API; grep `libp2p::Host`'s public interface (`getPeerInfo`, connection manager accessors) and/or
`GossipPubSub::GetLocalAddress()`'s multiaddress format to find the correct extraction path.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Node-kill mid-mint fault test</name>
  <files>test/src/bridge_race/bridge_race_fault_kill_test.cpp</files>
  <read_first>test/src/bridge_race/bridge_race_fixture.hpp, test/src/bridge_e2e/bridge_anvil_catchup_e2e_test.cpp (TearDownTestSuite node.reset() pattern, lines 479-491)</read_first>
  <behavior>
    - `BridgeRaceE2ETest.NodeKillMidMintStillConverges`: seed one burn to a Light destination
      (index != the node to be killed), configure all 11 nodes' RPC endpoints together (D-03
      ordering, as in 08-01), then IMMEDIATELY (before any wait) call `s_nodes[<killed_index>].reset()`
      on one Light node (D-10 — object destruction, not the destination node itself); assert the
      REMAINING 10 nodes still converge to exactly-once mint for the destination within the
      existing timeout, and that the killed node's slot does not need to survive for convergence
      (the destination is a DIFFERENT Light node than the killed one).
  </behavior>
  <action>
    `#include "bridge_race_fixture.hpp"`. Pick `kKilledNodeIndex = 5` (a Light node distinct from
    `kDestinationIndex = 1`). Seed the burn to `DeriveLightDestination(kDestinationIndex)` BEFORE
    `ConfigureRpcEndpoint` (D-03). Build the 3-slot real-Anvil `WeightedRpcEndpoint` vector (as in
    08-01 Task 2, all 3 slots pointed at `s_anvil.RpcUrl()` — this test is about node lifecycle,
    not RPC disagreement, so reuse the simple same-URL 3-slot pattern from the catchup suite, NOT
    08-03's divergent mock configs). Configure all 11 nodes in the tight loop, THEN immediately call
    `s_nodes[kKilledNodeIndex].reset();` (must happen before the `EXPECT_WAIT_FOR_CONDITION` below,
    to genuinely test "mid-mint" rather than post-convergence). Then
    `EXPECT_WAIT_FOR_CONDITION` that every REMAINING node (all indices except
    `kKilledNodeIndex`) reports `GetBalance(dest_addr) == initial + kMintAmount` (use `std::all_of`
    over the surviving subset). Since `s_nodes[kKilledNodeIndex]` was destroyed, do not query it
    after `.reset()` — only assert on the surviving 10.
  </action>
  <acceptance_criteria>
    <source-assertion>Test file contains exactly one `.reset()` call on a `s_nodes[...]` element, positioned before the balance-convergence wait (grep confirms `.reset()` line number precedes the `EXPECT_WAIT_FOR_CONDITION` line number)</source-assertion>
    <source-assertion>No `SIGKILL`, `kill(`, `fork(`, or subprocess spawning appears in this file (confirms D-10's object-destruction-only scope, not process-level crash — explicitly deferred)</source-assertion>
    <test-command>ctest -R bridge_race_fault_kill_test --output-on-failure</test-command>
  </acceptance_criteria>
  <done>Test builds and passes, proving the remaining 10-node cluster converges to exactly-once mint despite one Light node being destroyed mid-mint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pubsub partition + heal fault test</name>
  <files>test/src/bridge_race/bridge_race_fault_partition_test.cpp</files>
  <read_first>test/src/bridge_race/bridge_race_fixture.hpp, 3rdparty/ipfs-pubsub gossip_pubsub.hpp (locate via grep — resolve Open Question 2's PeerId extraction API before writing assertions), 3rdparty/libp2p host.hpp</read_first>
  <behavior>
    - `BridgeRaceE2ETest.PartitionThenHealConvergesExactlyOnce`: split the 11-node cluster into
      two groups (e.g. Full node + 5 Light nodes vs. 5 Light nodes) by disconnecting cross-group
      peers; seed the burn and configure RPC endpoints WHILE partitioned (both groups' watchers can
      still independently reach Anvil RPC — partition is pubsub/CRDT-layer only, not network-layer
      to Anvil); allow time for each sub-group to independently attempt/complete its own mint
      awareness; heal the partition via `AddPeers()`; assert CRDT converges so that ALL 11 nodes
      agree on exactly one mint for the destination (no permanent fork, no double-mint post-heal).
  </behavior>
  <action>
    First, spend a small investigation step: grep `3rdparty/ipfs-pubsub/.../gossip_pubsub.hpp` and
    `3rdparty/libp2p/include/libp2p/host/host.hpp` for the exact API to obtain a REMOTE node's
    `PeerId` from a LOCAL node's perspective (candidates: parsing `GetLocalAddress()`'s multiaddress
    string for the peer id component, or a `Host::getPeerInfo()`/connection-manager accessor) —
    resolve this before writing the disconnect calls; if genuinely no such accessor exists, use the
    multiaddress-parsing fallback (multiaddrs conventionally end in `/p2p/<peer_id>`).
    `#include "bridge_race_fixture.hpp"`. Define `kGroupA = {0,1,2,3,4,5}` (Full node + 5 Lights),
    `kGroupB = {6,7,8,9,10}` (5 Lights). For each cross-group pair, call
    `s_nodes[a]->GetPubSub()->GetHost()->disconnect(PeerIdOf(s_nodes[b]))` and the symmetric call
    (disconnect is likely local-only per-Host, so call it from both sides to fully sever the link —
    verify from the `Host` interface whether one-sided disconnect suffices or both sides are
    needed). Seed the burn to `DeriveLightDestination(<a Group B index>)` before RPC configuration
    (D-03), configure all 11 nodes' RPC endpoints together (real-Anvil 3-slot, as in Task 1), wait
    a bounded interval for each group's watcher poll to fire independently, THEN heal by calling
    `AddPeers()` reconnecting every previously-disconnected cross-group pair, THEN
    `EXPECT_WAIT_FOR_CONDITION` that all 11 nodes converge to `GetBalance(dest_addr) ==
    initial + kMintAmount` within a CRDT-reconciliation timeout (new constant
    `kPartitionHealConvergenceTimeout`, generous — e.g. 60000ms — to allow CRDT merge after heal).
  </action>
  <acceptance_criteria>
    <source-assertion>Test file calls `disconnect(` at least once (partition) and `AddPeers(` at least once after the disconnect calls (heal) — grep confirms line-number ordering: last `disconnect(` occurrence precedes first post-partition `AddPeers(` occurrence</source-assertion>
    <test-command>ctest -R bridge_race_fault_partition_test --output-on-failure</test-command>
  </acceptance_criteria>
  <done>Test builds and passes (or is explicitly marked with a documented fallback if the PeerId extraction API investigation reveals a blocker — report this clearly in the plan SUMMARY rather than silently weakening the assertion), proving CRDT convergence to exactly-once mint survives a partition-then-heal cycle.</done>
</task>

<task type="auto">
  <name>Task 3: CMake registration for both fault tests</name>
  <files>test/src/bridge_race/CMakeLists.txt</files>
  <read_first>test/src/bridge_race/CMakeLists.txt (as written by 08-01 Task 3 and 08-03 Task 3)</read_first>
  <action>
    Append two more `addtest(...)` blocks (`bridge_race_fault_kill_test`,
    `bridge_race_fault_partition_test`), mirroring the exact pattern of the existing entries in
    this file (same include dirs, same `genius_node_test`/`json_secure_storage` link libraries,
    same WHOLEARCHIVE triplet), each followed by
    `set_tests_properties(<name> PROPERTIES TIMEOUT 180)`.
  </action>
  <acceptance_criteria>
    <source-assertion>`test/src/bridge_race/CMakeLists.txt` contains five `addtest(` invocations total (2 from 08-01 + 1 from 08-03 + 2 new)</source-assertion>
    <test-command>ctest -N -R bridge_race</test-command>
  </acceptance_criteria>
  <done>All 5 bridge_race test binaries are registered and listed by `ctest -N -R bridge_race`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node lifecycle (kill) | Test-only object destruction, no external attacker surface |
| Pubsub mesh partition | Simulates network-layer disruption; CRDT convergence is the production mitigation under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Denial of Service | Node destruction mid-mint | mitigate | Test VERIFIES the remaining cluster's watcher/consensus path is resilient to one node's disappearance — no new production mitigation, proves existing resilience |
| T-08-08 | Repudiation/Tampering | CRDT fork during partition | mitigate | Test VERIFIES existing CRDT merge semantics converge to exactly-once post-heal — proves, does not introduce, the convergence guarantee |
| T-08-SC | Tampering | No new package installs | accept | Uses existing libp2p/GossipPubSub APIs only |
</threat_model>

<verification>
`ctest -R "bridge_race_fault_kill_test|bridge_race_fault_partition_test" --output-on-failure`, plus
`ctest -R bridge_race` for the full suite once all 4 plans in this phase are complete.

Execution-time investigation (08-RESEARCH.md Open Question 2, carried forward): before writing
Task 2's partition assertions, confirm the exact remote-`PeerId` extraction API by grepping
`GossipPubSub`/`libp2p::Host`'s public interface (`getPeerInfo`, connection-manager accessors).
Fallback if no dedicated accessor exists: parse the PeerId out of the trailing `/p2p/<peer_id>`
component of `GetPubSub()->GetLocalAddress()`'s multiaddress string. Document which path was used
in the 08-04-SUMMARY.md.
</verification>

<success_criteria>
Both fault tests build, register, and pass — proving exactly-once mint convergence survives (a)
one node's mid-mint destruction and (b) a pubsub partition-then-heal cycle. `ctest -N -R
bridge_race` lists all 5 bridge_race binaries (2 race + 1 RPC-fault + 2 lifecycle/partition-fault).
</success_criteria>

<output>
Create `.planning/phases/08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-/08-04-SUMMARY.md` when done
</output>
