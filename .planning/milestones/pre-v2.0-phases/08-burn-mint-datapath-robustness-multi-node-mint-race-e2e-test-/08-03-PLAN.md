---
phase: 08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - test/src/mock/mock_rpc_config.hpp
  - test/src/mock/mock_rpc_config.cpp
  - test/src/bridge_race/bridge_race_fault_rpc_test.cpp
  - test/src/bridge_race/CMakeLists.txt
autonomous: true
requirements: [D-08, D-09]

must_haves:
  truths:
    - "D-09: RPC faults are delivered via the existing Phase 5 MockRpcTransport injected through PublicChainInputValidator::SetTransportFactory(), extended (not rewritten) for per-slot divergence"
    - "The 3 quorum slots (1 DIRECT + 2 PUBLIC) can be independently configured with divergent MockRpcTransport behaviors, so RPC-endpoint disagreement is genuinely exercised (not all 3 slots silently resolving to the same mock)"
    - "Under RPC endpoint disagreement (one slot returns wrong logs, one times out, one succeeds), the existing >75% weighted quorum still yields the correct mint decision"
  artifacts:
    - path: "test/src/mock/mock_rpc_config.hpp"
      provides: "BuildDivergentSlotConfigs() helper returning 3 MockEndpointConfig with distinct URLs"
      contains: "BuildDivergentSlotConfigs"
    - path: "test/src/bridge_race/bridge_race_fault_rpc_test.cpp"
      provides: "RPC disagreement fault-injection test using the divergent slot configs + TransportFactory dispatch"
      min_lines: 40
  key_links:
    - from: "test/src/bridge_race/bridge_race_fault_rpc_test.cpp"
      to: "src/account/PublicChainInputValidator.hpp"
      via: "SetTransportFactory dispatch lambda keyed on distinct mock:// URLs"
      pattern: "SetTransportFactory"
---

<objective>
Extend the existing Phase 5 Mock RPC Transport with a per-slot divergent-config helper and use it
to prove the >75% weighted RPC quorum (DIRECT weight=100, 2×PUBLIC weight=0, but PUBLIC pairs
must agree per REQ-SLOT-03) still reaches the correct mint decision when the 3 configured RPC
endpoints genuinely disagree (one wrong-logs, one timeout, one success).

Purpose: Phase 5 built the mock transport but never exercised true 3-slot disagreement (all
existing tests use one real Anvil URL for all 3 slots). This plan closes that specific gap
identified in 08-RESEARCH.md ("Fills the gap Phase 5 left").
Output: `BuildDivergentSlotConfigs()` helper in `test/src/mock/mock_rpc_config.{hpp,cpp}`, plus
`bridge_race_fault_rpc_test.cpp` using it against the 11-node race fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-/08-CONTEXT.md
@.planning/phases/08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-/08-RESEARCH.md
@.planning/phases/08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-/08-PATTERNS.md
@test/src/mock/mock_rpc_config.hpp
@test/src/mock/mock_rpc_transport.hpp
@src/account/PublicChainInputValidator.hpp
@test/src/bridge_race/bridge_race_fixture.hpp
</context>

<interfaces>
`sgns::test::MockEndpointConfig` (`test/src/mock/mock_rpc_config.hpp`): `{ std::string url;
MockBehavior behavior = MockBehavior::kSuccess; std::map<std::string, std::vector<std::string>>
responses; }`. `MockBehavior` enum: `kSuccess, kTimeout, kConnectionRefused, kBadJson,
kWrongStatus, kWrongLogs`.

`sgns::test::MockRpcTransport` (`test/src/mock/mock_rpc_transport.hpp`): `final : public
eth::rpc::JsonRpcTransport`; constructed `explicit MockRpcTransport(const MockEndpointConfig
&config)`; `call(const boost::json::object &request) -> std::optional<std::string>`;
`ResetState()`, `SetBehavior(MockBehavior)`, `CallCount() const`.

`PublicChainInputValidator::TransportFactory` (`src/account/PublicChainInputValidator.hpp` lines
59-60, 153-156): `using TransportFactory = std::function<std::unique_ptr<eth::rpc::JsonRpcTransport>(
const std::string &url, std::chrono::seconds timeout)>;` and
`void SetTransportFactory(TransportFactory factory);`.

`sgns::WeightedRpcEndpoint`: fields `url`, `consensus_weight` (int; DIRECT=100, PUBLIC=0 per the
existing catchup-suite convention), `bridge_contract_address`, `accepted_topic0_hashes`.

`GeniusNode::ConfigureRpcEndpoint(chain_id_str, std::vector<WeightedRpcEndpoint>)` — the per-node
wiring point (see 08-01's fixture Task 1 for the exact pattern already used in this phase).

08-PATTERNS.md Pattern 3 (already read this session) gives the exact factory-dispatch lambda shape
to replicate: dispatch on exact URL string match, defaulting to the last config if no match.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BuildDivergentSlotConfigs() helper</name>
  <files>test/src/mock/mock_rpc_config.hpp, test/src/mock/mock_rpc_config.cpp</files>
  <read_first>test/src/mock/mock_rpc_config.hpp, test/src/mock/mock_rpc_transport.hpp</read_first>
  <behavior>
    - `BuildDivergentSlotConfigs()` returns exactly 3 `MockEndpointConfig` with 3 DISTINCT `url`
      values (`"mock://direct"`, `"mock://public1"`, `"mock://public2"`) and 3 DIFFERENT
      `MockBehavior` values by default (`kSuccess`, `kWrongLogs`, `kTimeout`) — matching
      08-RESEARCH.md Pattern 3's exact example.
    - Overload accepting 3 explicit `MockBehavior` values (one per slot) for tests that need a
      different disagreement combination than the default.
  </behavior>
  <action>
    Check if `test/src/mock/mock_rpc_config.cpp` already exists (grep for it); if not, this task
    creates it alongside the header (the current `mock_rpc_config.hpp` only has `LoadMockConfig`
    declared — verify whether its implementation lives in a `.cpp` or is header-only inline, and
    follow the SAME pattern for the new function to keep the file's linkage style consistent).
    Add to `mock_rpc_config.hpp`: `std::array<MockEndpointConfig, 3> BuildDivergentSlotConfigs(
    MockBehavior direct_behavior = MockBehavior::kSuccess, MockBehavior public1_behavior =
    MockBehavior::kWrongLogs, MockBehavior public2_behavior = MockBehavior::kTimeout);` declared in
    `namespace sgns::test`. Implement it (in the `.cpp` or inline, matching the existing
    `LoadMockConfig` linkage style) returning `{ MockEndpointConfig{"mock://direct",
    direct_behavior, {}}, MockEndpointConfig{"mock://public1", public1_behavior, {}},
    MockEndpointConfig{"mock://public2", public2_behavior, {}} }`. This is purely additive — do
    not modify `MockEndpointConfig`, `MockBehavior`, `LoadMockConfig`, or `MockRpcTransport` in any
    way (per 08-RESEARCH.md "Don't Hand-Roll" — extend, do not rewrite).
  </action>
  <acceptance_criteria>
    <source-assertion>`mock_rpc_config.hpp` declares `BuildDivergentSlotConfigs` returning 3 configs with 3 distinct URL string literals containing `"mock://"`</source-assertion>
    <source-assertion>Zero modifications to the existing `MockEndpointConfig` struct fields or `MockBehavior` enum values (grep diff confirms only additive lines)</source-assertion>
  </acceptance_criteria>
  <done>`BuildDivergentSlotConfigs()` compiles and returns 3 configs with distinct URLs and configurable per-slot behaviors, with zero changes to existing mock types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RPC disagreement fault-injection test</name>
  <files>test/src/bridge_race/bridge_race_fault_rpc_test.cpp</files>
  <read_first>
    test/src/bridge_race/bridge_race_fixture.hpp (08-01),
    test/src/mock/mock_rpc_config.hpp (Task 1 additions),
    src/account/PublicChainInputValidator.hpp
  </read_first>
  <behavior>
    - `BridgeRaceE2ETest.RpcDisagreementStillReachesCorrectQuorum`: configure the 3 quorum slots
      via `BuildDivergentSlotConfigs()` (DIRECT succeeds, one PUBLIC returns wrong logs, one PUBLIC
      times out) dispatched through `SetTransportFactory` on node 0 (the Full node, which performs
      RPC verification); seed one burn to a Light destination BEFORE configuring endpoints (D-03
      ordering preserved); assert the mint STILL completes correctly (DIRECT slot alone,
      weight=100, is sufficient per the existing quorum rule — this is what the test verifies
      holds under disagreement, not a new behavior).
    - A second case within the same test or a second `TEST_F`: DIRECT ALSO disagrees (e.g.
      `kWrongLogs`) while both PUBLIC slots report `kSuccess` and agree with each other — assert
      the mint completes via PUBLIC-pair agreement (exercises `WeightedRpcEndpoint`'s dedup-based
      quorum path, not just the DIRECT weight-100 shortcut).
  </behavior>
  <action>
    `#include "bridge_race_fixture.hpp"` and `#include "src/mock/mock_rpc_config.hpp"` and
    `"src/mock/mock_rpc_transport.hpp"`. Since `SetTransportFactory` is a
    `PublicChainInputValidator`-level DI seam (not directly exposed via `GeniusNode`'s public API
    per the interfaces already read), locate how `GeniusNode` exposes access to its
    `PublicChainInputValidator` instance (grep `GeniusNode.hpp` for a validator accessor — if none
    exists, this is a legitimate missing-information gap: flag it in the task's acceptance
    criteria as `MISSING` and use the same mechanism the existing `ConfigureRpcEndpoint`-based
    tests use, since `ConfigureRpcEndpoint` internally must reach the validator already). Build the
    `WeightedRpcEndpoint` vector with `url` values matching `BuildDivergentSlotConfigs()`'s 3
    literal URLs (`"mock://direct"`, `"mock://public1"`, `"mock://public2"`) and appropriate
    `consensus_weight`s (100/0/0), call `node0->ConfigureRpcEndpoint(chain_id, mock_eps)`, then
    install the `TransportFactory` dispatch lambda (per 08-PATTERNS.md Pattern 3) mapping each of
    the 3 URLs to its own `MockRpcTransport` instance built from the corresponding
    `MockEndpointConfig`. Seed the burn via `SendBridgeOutBurn` against the REAL Anvil instance
    (still needed for on-chain log discovery by the watcher) BEFORE this configuration step (D-03
    ordering), then assert the destination balance reaches `kMintAmount` on the Full node within
    the existing timeout constants.
  </action>
  <acceptance_criteria>
    <source-assertion>Test file references all 3 of `BuildDivergentSlotConfigs`'s literal URLs (`mock://direct`, `mock://public1`, `mock://public2`) as distinct strings in the `WeightedRpcEndpoint` vector construction (grep -c confirms 3 distinct mock:// literals, not 1 repeated)</source-assertion>
    <test-command>ctest -R bridge_race_fault_rpc_test --output-on-failure</test-command>
  </acceptance_criteria>
  <done>The RPC disagreement test builds and passes, proving the mint still completes correctly whether quorum is met via the DIRECT weight-100 slot or via PUBLIC-pair agreement, under genuine per-slot behavioral divergence.</done>
</task>

<task type="auto">
  <name>Task 3: CMake registration for the RPC fault test</name>
  <files>test/src/bridge_race/CMakeLists.txt</files>
  <read_first>test/src/bridge_race/CMakeLists.txt (as written by 08-01 Task 3)</read_first>
  <action>
    Append a third `addtest(bridge_race_fault_rpc_test bridge_race_fault_rpc_test.cpp)` block to
    the existing `test/src/bridge_race/CMakeLists.txt`, mirroring the exact
    `target_include_directories`/`target_link_libraries`/WHOLEARCHIVE triplet pattern already
    present for the two 08-01 test binaries, plus `target_link_libraries(bridge_race_fault_rpc_test
    json_secure_storage)` (already covered by the mirrored block) and a link against whichever
    target provides `sgns::test::MockRpcTransport`/`MockEndpointConfig` symbols (check
    `test/src/mock/CMakeLists.txt` for the library/target name that compiles
    `mock_rpc_transport.cpp`/`mock_rpc_config.cpp` and link against it explicitly if it is not
    already transitively pulled in via `genius_node_test`). Add
    `set_tests_properties(bridge_race_fault_rpc_test PROPERTIES TIMEOUT 180)` matching the other
    two entries.
  </action>
  <acceptance_criteria>
    <source-assertion>`test/src/bridge_race/CMakeLists.txt` contains three `addtest(` invocations total (2 from 08-01 + 1 new)</source-assertion>
    <test-command>ctest -N -R bridge_race_fault_rpc_test</test-command>
  </acceptance_criteria>
  <done>`bridge_race_fault_rpc_test` is registered, builds, and links successfully against the mock RPC transport library.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC endpoint responses -> `PublicChainInputValidator` quorum | Disagreeing/malicious endpoints simulated via mock transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering / Spoofing | `WeightedRpcEndpoint` quorum under disagreement | mitigate | This plan's fault test VERIFIES the existing >75%-weight quorum rule holds when RPC endpoints disagree — proves, does not newly introduce, the mitigation |
| T-08-SC | Tampering | No new package installs | accept | Extends existing in-repo mock transport only |
</threat_model>

<verification>
`ctest -R bridge_race_fault_rpc_test --output-on-failure`. Manually confirm (via a temporary
deliberate misconfiguration, e.g. giving all 3 slots the same URL) that the test would then fail to
exercise real disagreement — matching 08-RESEARCH.md Pitfall 3's stated warning sign — before
reverting to the correct distinct-URL configuration.
</verification>

<success_criteria>
`bridge_race_fault_rpc_test` passes, proving the existing weighted-quorum mint logic reaches the
correct decision under two distinct disagreement configurations (DIRECT-succeeds-alone,
PUBLIC-pair-agrees-alone), using genuinely divergent per-slot mock transports (3 distinct URLs).
</success_criteria>

<output>
Create `.planning/phases/08-burn-mint-datapath-robustness-multi-node-mint-race-e2e-test-/08-03-SUMMARY.md` when done
</output>
