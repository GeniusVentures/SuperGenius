---
phase: 04.1-anvil-local-bridge-e2e-test-use-tokencontracts-gnus-ai-env-t
fixed_at: 2026-07-13T00:00:00Z
review_path: .planning/phases/04.1-anvil-local-bridge-e2e-test-use-tokencontracts-gnus-ai-env-t/04.1-REVIEW.md
iteration: 1
findings_in_scope: 10
fixed: 10
skipped: 0
status: resolved
---

# Phase 04.1: Code Review Fix Report (iteration 2 — fresh deep review)

**Fixed at:** 2026-07-13
**Source review:** `.planning/phases/04.1-anvil-local-bridge-e2e-test-use-tokencontracts-gnus-ai-env-t/04.1-REVIEW.md`
**Fix scope:** critical_warning (CR-*, WR-* only; the 5 IN-* items are advisory and out of scope)

**Summary:**
- Findings in scope: 10 (2 Critical + 8 Warning)
- Fixed: 10
- Skipped: 0

All findings were applied by the gsd-code-fixer run that landed after the
review commit (`f159c8f7`). Each row maps a finding to its fix commit.

## Fixed Issues

| Finding | Commit | Subject |
|---|---|---|
| CR-01 | `eb0db9cb` | advance `last_block_per_chain_` to `from_block` not `to_block+1` (don't skip unscanned blocks when `max_chunks` caps the scan) |
| CR-02 | `dc8c69e0` | break scan on failed chunk to retry on next poll (don't advance `from_block` past an RPC-failed chunk — would silently drop burns) |
| WR-01 | `93fffa18` | assert catch-up scan mints all pre-node burns, not a trivially-true `>=` balance check (Test A) |
| WR-02 | `6de7780e` | capture `balance_before` before the poll window for the double-mint assertion (Test C) |
| WR-03 | `cbbc0d18` | validate `anvil_rpc_url` in `FundAccount0WithGnus` before shell interpolation |
| WR-04 | `da2ea34b` | poll for duplicate-mint settlement instead of a trivially-true balance `==` check (`AnvilReplayRejection`) |
| WR-05 | `fe01830b` | bump evmrelay for IPv6 bracketed-host parse in `split_host_port` |
| WR-06 | `5b950d91` | bump evmrelay for CA-load failure diagnostics (no silent TLS breakage) |
| WR-07 | `6fce4c5e` | remove dead `build_facet_map` from `find_contract_creation_blocks.py` |
| WR-08 | `2bf4c4b8` | add post-run summary for chains with `creation_block = 0` (warns when watcher would scan from genesis) |

**Note on WR-05 / WR-06:** `rpc_http_transport.cpp` lives in the `evmrelay`
submodule, so those two were fixed by bumping the submodule to the commit
that adds the IPv6 parse and the CA-load diagnostics respectively, not by an
in-tree edit.

## Verification

- `bridge_anvil_e2e_test` 2/2 and `bridge_anvil_catchup_e2e_test` 3/3 pass on
  POSIX (Test C with `max_chunks=3` exercises the CR-01 capped-scan path).
- The catch-up test's 3/3 result confirms the CR-01/CR-02 cursor + retry logic
  does not regress the max-chunks scenario.

## Advisory items (IN-01 … IN-05) — not in fix scope

The 5 Info findings (retry-backoff shift UB at `attempt >= 64`, `running` bool
data race, `getenv` thread-safety, bare `python3` in CMake, `--rpc` URL
validation in the build script) are pre-existing/style concerns outside the
critical_warning fix scope. They remain as documented in `04.1-REVIEW.md` for a
future hardening pass.

---

_Fixed: 2026-07-13_
_Fixer: Claude (gsd-code-fixer) + verification_
_Iteration: 2_
