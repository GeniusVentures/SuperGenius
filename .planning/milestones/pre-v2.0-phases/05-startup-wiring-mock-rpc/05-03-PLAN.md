---
phase: 05-startup-wiring-mock-rpc
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/account/PublicChainInputValidator.hpp
  - src/account/PublicChainInputValidator.cpp
  - test/src/mock/mock_rpc_config.hpp
autonomous: true
requirements:
  - REQ-MOCK-01
  - REQ-MOCK-03
  - REQ-MOCK-04

must_haves:
  truths:
    - "PublicChainInputValidator uses TransportFactory callable instead of directly constructing RpcHttpTransport per D-07"
    - "Production code defaults to real RpcHttpTransport factory — no behavior change without explicit injection per D-16"
    - "Test code can inject MockRpcTransport factory via SetTransportFactory() per D-14"
    - "VerifyPublicChainSmartContract() constructs transports via factory, not hard RpcHttpTransport construction"
    - "SGNS_E2E_REAL_RPC=1 env var selects real RPC transport at runtime per D-15"
    - "All existing PublicChainInputValidator tests pass unchanged"
  artifacts:
    - path: "src/account/PublicChainInputValidator.hpp"
      provides: "TransportFactory typedef + SetTransportFactory() method"
      contains: "using TransportFactory"
      contains: "SetTransportFactory"
    - path: "src/account/PublicChainInputValidator.cpp"
      provides: "Factory-based transport construction in VerifyPublicChainSmartContract"
      contains: "transport_factory_"
  key_links:
    - from: "src/account/PublicChainInputValidator.cpp"
      to: "evmrelay/include/eth/rpc_receipt_source.hpp"
      via: "factory return type"
      pattern: "JsonRpcTransport"
    - from: "PublicChainInputValidator::SetTransportFactory()"
      to: "GeniusNode::InitializeRpcEndpoints()"
      via: "DI injection point"
      pattern: "SetTransportFactory"
---

<objective>
Replace the hard construction of `RpcHttpTransport` in `PublicChainInputValidator::VerifyPublicChainSmartContract()` with a pluggable `TransportFactory` callable. In production, the factory creates real `RpcHttpTransport` instances (D-16, REQ-MOCK-04). In tests, the factory creates `MockRpcTransport` instances (D-07, D-14, REQ-MOCK-01). The SGNS_E2E_REAL_RPC=1 env var selects real RPC at runtime (D-15, REQ-MOCK-03).

Purpose: Enable mock RPC transport injection without compile-time flags, keeping the same code path for both production and test.

Output: Refactored PublicChainInputValidator with TransportFactory DI, defaulting to real RPC transport.
</objective>

<execution_context>
@/Users/henriqueklein/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-startup-wiring-mock-rpc/05-CONTEXT.md
@.planning/phases/05-startup-wiring-mock-rpc/05-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@src/account/PublicChainInputValidator.hpp
@src/account/PublicChainInputValidator.cpp
@evmrelay/include/eth/rpc_receipt_source.hpp
@evmrelay/include/eth/rpc_http_transport.hpp
@test/src/bridge_e2e/bridge_e2e_test.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TransportFactory type and injection point to PublicChainInputValidator</name>
  <files>src/account/PublicChainInputValidator.hpp</files>
  <read_first>
    - src/account/PublicChainInputValidator.hpp — current class: WeightedRpcEndpoint struct (lines 26-32), PublicChainInputValidator class (lines 37-101), SetRpcEndpoints() (line 45), rpc_endpoints_ member (line 100)
    - evmrelay/include/eth/rpc_receipt_source.hpp — JsonRpcTransport interface: virtual std::optional<std::string> call(const boost::json::object& request) = 0
    - evmrelay/include/eth/rpc_http_transport.hpp — RpcHttpTransport constructor: takes const std::string& url, const RpcHttpTransportOptions& opts
    - .planning/phases/05-startup-wiring-mock-rpc/05-RESEARCH.md — Pattern 1: TransportFactory DI (lines 223-276)
    - .planning/codebase/CONVENTIONS.md — using declarations, PascalCase, std::function for callables
  </read_first>
  <action>
    D-07, D-14, D-16 — Add transport factory injection to PublicChainInputValidator header.

    1. Add `#include <functional>`, `#include <memory>`, `#include <chrono>` to the header (check existing includes first).

    2. Verify include for `eth::rpc::JsonRpcTransport` is present.

    3. Add `TransportFactory` type alias in namespace sgns per RESEARCH.md Pattern 1:
       ```cpp
       using TransportFactory = std::function<std::unique_ptr<eth::rpc::JsonRpcTransport>(
           const std::string& url,
           std::chrono::seconds timeout)>;
       ```

    4. Add `void SetTransportFactory(TransportFactory factory)` public method to PublicChainInputValidator.

    5. Add private member `TransportFactory transport_factory_`.

    6. The default factory (used when none is set) must create RpcHttpTransport: constructs `eth::rpc::RpcHttpTransportOptions opts; opts.timeout = timeout;` and returns `std::make_unique<eth::rpc::RpcHttpTransport>(url, opts)`. This ensures D-16: production uses real RPC only.

    No compile-time #ifdef for mock switching per D-15.
  </action>
  <verify>
    <automated>cmake --build build --target public_chain_input_validator 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: PublicChainInputValidator.hpp contains `using TransportFactory = std::function<...>` with return type `std::unique_ptr<eth::rpc::JsonRpcTransport>`
    - Source assertion: PublicChainInputValidator class has `void SetTransportFactory(TransportFactory factory)` public method
    - Source assertion: Private member `TransportFactory transport_factory_` exists
    - Source assertion: Default factory creates RpcHttpTransport (not mock)
    - Build: compiles without errors
    - No compile-time #ifdef for mock switching (D-15 compliance)
  </acceptance_criteria>
  <done>TransportFactory typedef, SetTransportFactory() injection point, and default real-RPC factory in PublicChainInputValidator header.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hard RpcHttpTransport construction with factory call</name>
  <files>src/account/PublicChainInputValidator.cpp</files>
  <read_first>
    - src/account/PublicChainInputValidator.cpp — lines 160-259: VerifyPublicChainSmartContract() with hard RpcHttpTransport construction at lines 181-183
    - src/account/PublicChainInputValidator.hpp — updated in Task 1: TransportFactory type, SetTransportFactory(), transport_factory_
    - evmrelay/include/eth/rpc_http_transport.hpp — RpcHttpTransportOptions struct, RpcHttpTransport constructor
    - evmrelay/include/eth/rpc_receipt_source.hpp — JsonRpcTransport::call() return type
  </read_first>
  <action>
    D-07, D-14, D-16 — Replace hard RpcHttpTransport construction with factory call in VerifyPublicChainSmartContract.

    Locate lines 181-183: the hard construction of `eth::rpc::RpcHttpTransportOptions opts; opts.timeout = kTimeout; eth::rpc::RpcHttpTransport transport(ep.url, opts);`.

    1. Before the loop, resolve factory with fallback:
       ```cpp
       auto factory = transport_factory_ ? transport_factory_ : [](const std::string& url, std::chrono::seconds timeout) {
           eth::rpc::RpcHttpTransportOptions opts;
           opts.timeout = timeout;
           return std::make_unique<eth::rpc::RpcHttpTransport>(url, opts);
       };
       ```
       This ensures backward compatibility when SetTransportFactory() is never called (production path, D-16).

    2. In the loop body, replace hard construction with `auto transport = factory(ep.url, kTimeout);`.

    3. Update transport usage: factory returns `std::unique_ptr<eth::rpc::JsonRpcTransport>`, so use `transport->call(request)` (arrow operator) instead of `transport.call(request)`.

    4. Verify only `call()` is used — no RpcHttpTransport-specific methods.

    5. Add `#include <memory>` for std::unique_ptr if not present.

    No compile-time flags per D-15.
  </action>
  <verify>
    <automated>ctest --test-dir build -R PublicChainInputValidator -j4</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: VerifyPublicChainSmartContract() does NOT contain `RpcHttpTransport transport(` — no hard construction
    - Source assertion: Transport created via `factory(ep.url, kTimeout)` returning `std::unique_ptr<eth::rpc::JsonRpcTransport>`
    - Source assertion: `transport->call(request)` uses arrow operator
    - Source assertion: Default factory fallback creates RpcHttpTransport when transport_factory_ is not set
    - Source assertion: No #ifdef, #ifndef, or compile-time mock switching
    - Test command: `ctest --test-dir build -R PublicChainInputValidator -j4` — all existing tests pass unchanged
  </acceptance_criteria>
  <done>VerifyPublicChainSmartContract uses TransportFactory — RpcHttpTransport in production, MockRpcTransport injectable in tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add SGNS_E2E_REAL_RPC=1 env var check for test transport selection</name>
  <files>test/src/mock/mock_rpc_config.hpp</files>
  <read_first>
    - .planning/phases/05-startup-wiring-mock-rpc/05-CONTEXT.md — D-15: Real RPC opt-in via env var SGNS_E2E_REAL_RPC=1
    - test/src/bridge_e2e/bridge_e2e_test.cpp — GTEST_SKIP(RUN_E2E_BRIDGE) pattern for env-var-gated tests
    - src/account/PublicChainInputValidator.hpp — updated in Task 1: SetTransportFactory(), TransportFactory type
  </read_first>
  <action>
    D-15 — Implement SGNS_E2E_REAL_RPC=1 env var check that selects between mock and real RPC at test startup.

    Add to mock_rpc_config.hpp (or a new test utility header):
    ```cpp
    inline bool UseRealRpcTransport() {
        static bool checked = false;
        static bool use_real = false;
        if (!checked) {
            const char* env = std::getenv("SGNS_E2E_REAL_RPC");
            use_real = (env && std::string(env) == "1");
            checked = true;
        }
        return use_real;
    }

    inline TransportFactory GetTransportFactory(const MockRpcConfig& mock_config) {
        if (UseRealRpcTransport()) {
            return [](const std::string& url, std::chrono::seconds timeout) {
                eth::rpc::RpcHttpTransportOptions opts;
                opts.timeout = timeout;
                return std::make_unique<eth::rpc::RpcHttpTransport>(url, opts);
            };
        }
        return CreateMockTransportFactory(mock_config);
    }
    ```

    Test fixture SetUp calls `validator->SetTransportFactory(GetTransportFactory(mock_config))`.

    When SGNS_E2E_REAL_RPC=1: real RPC. Absent: mock (default per D-14). No compile flags.
  </action>
  <verify>
    <automated>grep -rn "SGNS_E2E_REAL_RPC" test/src/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: Test utility contains `std::getenv("SGNS_E2E_REAL_RPC")` call checking for value "1"
    - Source assertion: When env=1, real RpcHttpTransport factory injected (not mock)
    - Source assertion: When env absent or != "1", mock transport factory used (default per D-14)
    - Source assertion: No compile-time #ifdef for mock switching (D-15)
  </acceptance_criteria>
  <done>SGNS_E2E_REAL_RPC=1 env var: test setup selects real or mock RPC transport at runtime. Defaults to mock per D-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SetTransportFactory() caller → PublicChainInputValidator | Factory callable crosses from GeniusNode (or test harness) to validator — same process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | SetTransportFactory() | mitigate | Available only to code with access to PublicChainInputValidator instance. Production never calls it per D-16. |
| T-05-07 | Denial of Service | TransportFactory | accept | Null/empty factory handled by inline fallback to real RPC — no crash |
| T-05-SC | Tampering | npm/pip/cargo installs | N/A | No external packages |
</threat_model>

<verification>
Run: `ctest --test-dir build -R PublicChainInputValidator -j4`
All existing tests must pass. Build: `cmake --build build --target supergenius_account`.
Verify: `grep -r "RpcHttpTransport transport(" src/account/PublicChainInputValidator.cpp` returns zero matches.
</verification>

<success_criteria>
- TransportFactory typedef and SetTransportFactory() exist in PublicChainInputValidator
- VerifyPublicChainSmartContract constructs transports via factory, not hard RpcHttpTransport
- Default factory creates real RpcHttpTransport (production path unchanged per D-16)
- SGNS_E2E_REAL_RPC=1 env var selects real transport at runtime per D-15
- All existing tests pass without modification
- No compile-time flags for mock switching
</success_criteria>

<output>
Create `.planning/phases/05-startup-wiring-mock-rpc/05-03-SUMMARY.md` when done
</output>
