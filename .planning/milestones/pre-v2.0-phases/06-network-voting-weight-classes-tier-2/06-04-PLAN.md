---
phase: 06-network-voting-weight-classes-tier-2
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - test/src/blockchain/consensus_slot_quorum_test.cpp
  - test/src/blockchain/validator_registry_promotion_test.cpp
  - test/src/blockchain/CMakeLists.txt
autonomous: false
requirements:
  - REQ-SLOT-01
  - REQ-SLOT-02
  - REQ-SLOT-03
  - REQ-SLOT-04
  - REQ-SLOT-05
  - REQ-SLOT-06
  - REQ-REPUT-01
  - REQ-DETERM-01
decisions_implemented:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
user_setup: []
notes: >
  Replanned 2026-06-19 — test targets updated for actual wiring:
  - Slot hashes populated via ConsensusManager::SlotHashPopulator callback, not GeniusNode::PopulateVoteSlotHashes()
  - Endpoint hashes via PublicChainInputValidator::GetSlotHash(), not HashDirectApiEndpoint()/HashPublicEndpoint()
  - GeniusNode wire test: callback injection sets slot_hash_populator_, CreateVote calls it before signing

must_haves:
  truths:
    - "Every REQ-SLOT-* and REQ-REPUT-01 and REQ-DETERM-01 behavior has at least one failing-without-the-fix, passing-with-the-fix automated test"
    - "The full regression suite (all tests linked to the consensus and account libraries) is green after the slot model lands — CLAUDE.md shared-library mandate"
    - "The CONTEXT.md D-06 arithmetic example is encoded as a golden test: 500 + 50 + 0 = 550, threshold 900, no certificate"
    - "The two tally sites (TallyVotes + HandleVote) produce identical has_quorum for the same bridge-mint vote set — Pitfall 1 regression"
    - "Solo PUBLIC hashes contribute zero; ≥2-validator hash groups contribute 25% — Pitfall/D-03 regression"
    - "Promotion is gated by weight AND penalty — a high-penalty REGULAR node above the weight threshold is NOT promoted"
  artifacts:
    - path: "test/src/blockchain/consensus_slot_quorum_test.cpp"
      provides: "REQ-SLOT-01..06 + REQ-DETERM-01 coverage"
      contains: "TEST"
    - path: "test/src/blockchain/validator_registry_promotion_test.cpp"
      provides: "REQ-REPUT-01 coverage"
      contains: "TEST"
    - path: "test/src/blockchain/CMakeLists.txt"
      provides: "New test targets wired into the build"
      contains: "consensus_slot_quorum_test"
  key_links:
    - from: "test/src/blockchain/consensus_slot_quorum_test.cpp"
      to: "src/blockchain/ValidatorRegistry.cpp EvaluateSlotQuorum"
      via: "calls EvaluateSlotQuorum and asserts on SlotQuorumResult fields"
      pattern: "EvaluateSlotQuorum"
    - from: "test/src/blockchain/consensus_slot_quorum_test.cpp"
      to: "src/blockchain/Consensus.cpp TallyVotes + HandleVote"
      via: "drives both tally sites and asserts agreement"
      pattern: "TallyVotes"
    - from: "test/src/blockchain/validator_registry_promotion_test.cpp"
      to: "src/blockchain/ValidatorRegistry.cpp ApplyVoteEffects"
      via: "invokes ApplyVoteEffects and asserts role transition"
      pattern: "ApplyVoteEffects"
---

<objective>
Provide automated coverage for every Phase 6 requirement and run the full regression suite per the CLAUDE.md shared-library mandate. This plan is verification-only — it adds tests, not production behavior. The tests prove the slot model (Plans 01-02) and the FULL promotion (Plan 03) behave per CONTEXT.md D-01..D-10.

**Replanned 2026-06-19** — test targets updated for actual wiring:
- Slot hashes come from `PublicChainInputValidator::GetSlotHash()` (not `HashDirectApiEndpoint`/`HashPublicEndpoint`)
- Vote population happens via `ConsensusManager::SlotHashPopulator` callback (not `GeniusNode::PopulateVoteSlotHashes`)
- The golden test and tally tests are unchanged (they operate on proto fields regardless of population mechanism)

Purpose: RESEARCH Pitfall 4 ("Forgetting the full regression suite") is the most common failure mode for shared-consensus changes. A green changed-file test is insufficient; `genius_node_test` and all consensus-linked targets must pass. This plan is the gate.

Output:
- `consensus_slot_quorum_test.cpp` — covers REQ-SLOT-01..06, REQ-DETERM-01.
- `validator_registry_promotion_test.cpp` — covers REQ-REPUT-01.
- CMakeLists wiring for both new targets.
- A human-verify checkpoint to confirm the full `ctest -j8` suite is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-network-voting-weight-classes-tier-2/06-CONTEXT.md
@.planning/phases/06-network-voting-weight-classes-tier-2/06-RESEARCH.md
@.planning/phases/06-network-voting-weight-classes-tier-2/06-01-SUMMARY.md
@.planning/phases/06-network-voting-weight-classes-tier-2/06-02-SUMMARY.md
@.planning/phases/06-network-voting-weight-classes-tier-2/06-03-SUMMARY.md
@test/src/blockchain/consensus_certificate_test.cpp
@test/testutil/wait_condition.hpp
@src/blockchain/ValidatorRegistry.hpp
@src/blockchain/Consensus.hpp

<interfaces>
// test/src/blockchain/consensus_certificate_test.cpp — REUSE these helpers:
//   class ConsensusManagerTestAccess  (line 12) — friend access to private tally/proposal state.
//   std::shared_ptr<ValidatorRegistry> MakeRegistry(db, account)  (line 99).
//   std::shared_ptr<ConsensusManager> MakeManager(registry, db, pubs, account)  (line 136).
//   Pattern: StoreGenesisRegistry -> ASSERT_WAIT_FOR_CONDITION(LoadRegistry) -> CreateProposal
//            -> CreateVote -> TallyVotes. See line 431 (TallyVotesWithRegistry).
//
// test/testutil/wait_condition.hpp:
//   ASSERT_WAIT_FOR_CONDITION(condition, timeout, step) — use this, NEVER std::this_thread::sleep_for
//   (CLAUDE.md mandate).
//
// Phase 6 surfaces under test (from Plans 01-03):
//   ConsensusVote::slot_0_hash() / slot_1_hash() / slot_2_hash()  (Plan 01)
//   ConsensusManager::SlotHashPopulator / SetSlotHashPopulator  (Plan 01)
//   PublicChainInputValidator::GetSlotHash(slot_index, chain_id)  (Plan 01)
//   ValidatorRegistry::EvaluateSlotQuorum(votes, registry) -> SlotQuorumResult  (Plan 02)
//   ConsensusManager::EvaluateQuorum / IsBridgeMintSubject  (Plan 02)
//   WeightConfig::full_promotion_weight_ + ApplyVoteEffects promotion branch  (Plan 03)
</interfaces>
</context>

<decisions>
This plan verifies the full locked decision set:
- **D-01:** Tests assert slot hash fields exist and are signed (REQ-SLOT-01).
- **D-02:** Tests assert slot 0 = 50% per qualifying voter (REQ-SLOT-02).
- **D-03:** Tests assert solo PUBLIC hashes contribute zero, ≥2-validator groups contribute 25% (REQ-SLOT-03).
- **D-04:** Tests confirm no hash field on ValidatorEntry (compile-time).
- **D-05:** Tests assert all-abstain voters count toward reputation but not qualified_sum (REQ-SLOT-06).
- **D-06:** Golden test of the CONTEXT.md arithmetic example (REQ-SLOT-04).
- **D-07/D-08:** Promotion tests confirm FULL uses existing weight path (REQ-REPUT-01).
- **D-09:** N/A (proto change verified structurally in Plan 01).
- **D-10:** Tests do NOT modify Tier 1; they assert Tier 1 accessors are unchanged.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create consensus_slot_quorum_test.cpp covering REQ-SLOT-01..06 + REQ-DETERM-01</name>
  <files>test/src/blockchain/consensus_slot_quorum_test.cpp, test/src/blockchain/CMakeLists.txt</files>
  <behavior>
    - REQ-SLOT-01 (proto): A `ConsensusVote` with populated slot hashes produces a `VoteSigningBytes` output different from the same vote with empty slots, proving the hashes are signed.
    - REQ-SLOT-02 (slot 0): A single DIRECT_API voter (weight W, non-empty slot_0_hash) produces `qualified_sum = W/2`.
    - REQ-SLOT-03 (slots 1-2 dedup): Two PUBLIC voters sharing a slot_1_hash each contribute `W*1/4`; a solo PUBLIC voter with a unique hash contributes zero.
    - REQ-SLOT-04 (cumulative >75%): The CONTEXT.md golden example — DIRECT_API rep 1000 fills slot 0, two PUBLIC rep 100 agree on slot 1 — yields `qualified_sum=550`, `threshold=900`, `has_quorum=false`. Add a second case where enough PUBLIC agreement pushes `qualified_sum` over threshold → `has_quorum=true`.
    - REQ-SLOT-05 (both tally sites agree): Drive the same vote set through `TallyVotes` and through the `HandleVote` incremental path (via `ConsensusManagerTestAccess`); assert identical `has_quorum`.
    - REQ-SLOT-06 (abstention): A voter with all three slot hashes empty counts toward `total_voting_reputation` (raising the threshold) but contributes zero to `qualified_sum`.
    - REQ-DETERM-01 (determinism): Run `EvaluateSlotQuorum` on the same vote set in three different insertion orders; assert byte-identical `SlotQuorumResult`.
    - Non-bridge regression: an internal-transfer proposal still uses single-pool `IsQuorum`.
  </behavior>
  <action>
    Create `test/src/blockchain/consensus_slot_quorum_test.cpp` following the established pattern from `consensus_certificate_test.cpp`:

    0. **CRITICAL — In-memory secure storage setup**: Include `"local_secure_storage/impl/MemorySecureStorage.hpp"`. In the test fixture's `SetUp()` (BEFORE any node/account construction), call `GeniusAccount::SetSecureStorageFactory( []( const std::string &id ) -> std::shared_ptr<ISecureStorage> { return std::make_shared<MemorySecureStorage>( id ); } );`. This prevents macOS keychain prompts when tests construct `GeniusNode`/`GeniusAccount` instances. Pattern comes from `test/src/blockchain/blockchain_genesis_test.cpp:44-45`.

    1. Include `ConsensusManagerTestAccess`, the `MakeRegistry` / `MakeManager` helpers.
    2. Use `ASSERT_WAIT_FOR_CONDITION` for any registry-load wait. NEVER `std::this_thread::sleep_for` (CLAUDE.md).
    3. Build `ValidatorEntry` fixtures with explicit `weight`, `role`, `status` via proto setters.
    4. Build `ConsensusVote` fixtures with populated `slot_0_hash` / `slot_1_hash` / `slot_2_hash` (32-byte vectors, e.g. `std::vector<uint8_t>(32, 0xAB)` for a shared hash, distinct values for solo hashes).
    5. For REQ-SLOT-04, encode the exact CONTEXT.md D-06 golden example with named constants (`kDirectApiRep = 1000`, `kPublicRep = 100`, etc.).
    6. For REQ-SLOT-05, use `ConsensusManagerTestAccess` to inject votes and compare `TallyVotes` vs `HandleVote`.
    7. For REQ-DETERM-01, call `EvaluateSlotQuorum` three times with shuffled vote vectors (fixed-seed `std::mt19937`).

    Test names: `ConsensusSlotQuorum.Slot0DirectApi50Pct`, `PublicDedupSoloZero`, `GoldenContextExample`, `BothTallySitesAgree`, `AbstainerRaisesThreshold`, `Deterministic`, `NonBridgeSinglePool`.

    Wire the target into `test/src/blockchain/CMakeLists.txt` following the existing `consensus_certificate_test` pattern.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ninja && ctest -R "ConsensusSlotQuorum" -j8 --output-on-failure</automated>
  </verify>
  <done>
    - `consensus_slot_quorum_test.cpp` exists with tests covering REQ-SLOT-01..06 and REQ-DETERM-01.
    - The golden CONTEXT.md example test asserts `qualified_sum=550`, `threshold=900`, `has_quorum=false`.
    - The determinism test passes with three different vote orderings.
    - `ctest -R ConsensusSlotQuorum` is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create validator_registry_promotion_test.cpp covering REQ-REPUT-01</name>
  <files>test/src/blockchain/validator_registry_promotion_test.cpp, test/src/blockchain/CMakeLists.txt</files>
  <behavior>
    - REQ-REPUT-01 (promotion): A REGULAR entry with `weight >= full_promotion_weight_` and `penalty_score < penalty_threshold_` is promoted to `Role::FULL` after `ApplyVoteEffects` with an approve vote.
    - Promotion does not fire when weight is below threshold.
    - Promotion does not fire when `penalty_score >= penalty_threshold_` even if weight is high.
    - A GENESIS entry is NOT changed to FULL.
    - A SHARDED entry is NOT changed to FULL.
    - An already-FULL entry is NOT re-promoted (idempotent).
    - After promotion, the next `ApplyVoteEffects` approve tick clamps weight against `full_max_weight_`.
  </behavior>
  <action>
    Create `test/src/blockchain/validator_registry_promotion_test.cpp`:

    0. **CRITICAL — In-memory secure storage setup**: Same pattern as Task 1. Include `"local_secure_storage/impl/MemorySecureStorage.hpp"`. In the test fixture's `SetUp()`, call `GeniusAccount::SetSecureStorageFactory(...)`. This is a safety blanket — even if this test only calls `ApplyVoteEffects` on in-memory entries, any indirect path through registry construction that touches account creation will avoid keychain prompts.

    1. Construct `ValidatorRegistry` via `MakeRegistry` with a custom `WeightConfig` (set `full_promotion_weight_` to 10 for fast tests).
    2. Build `ValidatorEntry` fixtures via proto setters.
    3. Build a `registered_votes` map and call `ApplyVoteEffects(entries, registered_votes)`.
    4. Assert the resulting `entry.role()` for each case.

    Test names: `ValidatorRegistryPromotion.RegularBelowWeightStaysRegular`, `RegularAtWeightLowPenaltyPromotes`, `RegularAtWeightHighPenaltyStaysRegular`, `GenesisNotDemoted`, `ShardedNotPromoted`, `FullIdempotent`, `FullUsesFullMaxWeightAfterPromotion`.

    Wire into `test/src/blockchain/CMakeLists.txt` as `validator_registry_promotion_test`.

    No `sleep_for` (CLAUDE.md). Tests are synchronous — `ApplyVoteEffects` is a pure in-memory mutation.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ninja && ctest -R "ValidatorRegistryPromotion" -j8 --output-on-failure</automated>
  </verify>
  <done>
    - `validator_registry_promotion_test.cpp` exists covering REQ-REPUT-01.
    - All seven behavioral cases above pass.
    - `ctest -R ValidatorRegistryPromotion` is green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full regression suite gate (CLAUDE.md shared-library mandate)</name>
  <action>Run the complete ctest -j8 suite to confirm no shared-consensus regression before /gsd:verify-work. See &lt;how-to-verify&gt; for exact commands.</action>
  <what-built>
    Phase 6 Plans 01-03 implemented (slot proto extension, SlotHashPopulator callback injection, slot tally at both consensus sites, Role::FULL promotion) and the two new test files (Plan 04) pass in isolation. The remaining gate is the CLAUDE.md shared-library mandate: a full regression run of ALL tests linked to the consensus and account libraries.
  </what-built>
  <how-to-verify>
    Run the full regression suite from the build directory:

    1. `cd build/OSX/Debug`
    2. `cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=Debug` (reconfigure to pick up new test targets)
    3. `ninja` (full build — must succeed warning-free per CLAUDE.md)
    4. `ctest -j8 --output-on-failure` (the COMPLETE suite)

    Expected outcomes:
    - `ninja` exits 0 with no new warnings.
    - `ctest -j8` exits 0 — ALL tests pass, including `genius_node_test` and every consensus-linked target.
    - Specifically confirm the new tests pass: `ctest -R "ConsensusSlotQuorum|ValidatorRegistryPromotion" -j8 --output-on-failure`.

    If any pre-existing test regresses, that is a Phase 6 bug — do NOT proceed until full suite is green.
  </how-to-verify>
  <resume-signal>Type "approved" if `ninja && ctest -j8` is fully green, or describe the regressing test(s) and the suspected Phase 6 cause.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test execution → CI gate | The full `ctest` run is the trust boundary that prevents a shared-consensus regression from merging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Tampering | Missing test coverage (silent regression) | mitigate | Every REQ-SLOT-* and REQ-REPUT-01 behavior has an explicit test. The golden CONTEXT.md example is encoded. |
| T-06-16 | Denial of Service | Shared-library regression breaks unrelated tests | mitigate | The blocking human-verify checkpoint requires the FULL `ctest -j8` to be green. |
| T-06-17 | Repudiation | Non-deterministic test (flaky) | mitigate | Fixed seed, no wall-clock dependence, `ASSERT_WAIT_FOR_CONDITION` instead of `sleep_for`. |
| T-06-SC | Tampering | No third-party packages | accept | gtest already in thirdparty/. |
</threat_model>

<verification>
- `cd build/OSX/Debug && ninja` — full build, warning-free.
- `ctest -R "ConsensusSlotQuorum|ValidatorRegistryPromotion" -j8 --output-on-failure` — new tests green.
- `ctest -j8 --output-on-failure` — FULL regression suite green (blocking checkpoint).
</verification>

<success_criteria>
- `consensus_slot_quorum_test.cpp` covers REQ-SLOT-01..06 + REQ-DETERM-01, including the CONTEXT.md golden example.
- `validator_registry_promotion_test.cpp` covers REQ-REPUT-01 with all seven promotion edge cases.
- Both targets wired into `test/src/blockchain/CMakeLists.txt`.
- Full `ctest -j8` green — no shared-library regressions.
- Human-verify checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/06-network-voting-weight-classes-tier-2/06-04-SUMMARY.md` when done.
</output>
