---
phase: 05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated
verified: 2026-06-19T01:41:27Z
status: passed
score: 9/9 truths verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: N/A
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 05.2: Bridge V2 — X-only Compressed Encoding — Verification Report

**Phase Goal:** Bridge V2 — X-only compressed encoding: smart contract updated with `bridgeOut(uint256,uint256,uint256,bytes32,bool)` accepting 32-byte X-only compressed SG public key. Event renamed from `BridgeSourceBurned` to `BridgeOutInitiated`. C++ side must decode 32-byte X-only key → decompress to full X+Y → match `GetAddress()`. Versioned catch-up scan handles old topic0 for v1 burns.
**Verified:** 2026-06-19T01:41:27Z
**Status:** passed
**Re-verification:** No — initial verification

## MVP Mode — User-Story Format Guard

The orchestrator flagged `Mode: mvp`. The `verify-mvp-mode` reference requires the goal to be in user-story format ("As a [user role], I want to [capability], so that [outcome]."). This phase's goal (ROADMAP.md:146) is **technical, not user-story form** — it describes an internal protocol/contract upgrade with no end-user-facing capability. Per `verify-mvp-mode.md`, the verifier should "surface this discrepancy and ask the user to run `/gsd mvp-phase`."

**Disposition:** This report proceeds with the standard goal-backward methodology against the technical goal stated in ROADMAP.md. The User Flow Coverage section is omitted because the goal has no user-flow semantics — it is a machine-to-machine protocol change. If the user wishes MVP-mode framing to apply, they should run `/gsd mvp-phase 05.2` to reformat the goal into a user story; the technical truths verified here will remain valid either way.

## Goal Achievement

### Observable Truths

Derived from the phase goal + the 9 requirement IDs (REQ-V2-01 through REQ-V2-09) defined in `05.2-RESEARCH.md:64-74`. These are the **roadmap contract** — ROADMAP.md:161 lists them as the phase requirements.

| #   | Truth (source REQ) | Status | Evidence |
| --- | ------------------ | ------ | -------- |
| 1   | EventRegistry contains `BridgeOutInitiated(...,bytes32,bool)` with `kBytes32` param 5, `kBool` param 6 (REQ-V2-01) | ✓ VERIFIED | `evmrelay/include/eth/eth_watch_cli.hpp:154-162` — `r.register_event("BridgeOutInitiated(address,uint256,uint256,uint256,uint256,bytes32,bool)", {...kBytes32 sgnsDestination..., kBool destinationYOdd})`. EventRegistryTest.RegistryContainsBridgeOutInitiated + BridgeOutInitiatedParam5IsBytes32 + BridgeOutInitiatedParam6IsBool all PASS. |
| 2   | `BridgeSourceBurned` retained in EventRegistry for backward-compat catch-up (REQ-V2-02) | ✓ VERIFIED | `evmrelay/include/eth/eth_watch_cli.hpp:139-145` unchanged v1 registration with `kBytes sgnsDestination`. EventRegistryTest.BridgeSourceBurnedStillRegistered PASS. |
| 3   | BridgeRelayer::Start() registers BOTH v1 and v2 watches per chain (REQ-V2-03) | ✓ VERIFIED | `src/account/BridgeRelayer.cpp:87,95-96,154,172` — declares `event_sig_v1` + `event_sig_v2`, fetches `params_v1`/`params_v2`, calls `watch_service_->watch_event()` twice per chain (best-effort try/catch each). `chain_watches_` now `unordered_map<string, pair<EventWatchId, EventWatchId>>` (`BridgeRelayer.hpp:90`). bridge_relayer_test MultiChainStart + OnRpcEndpointsReadyDelegatesToStart assert both `.first` and `.second` watch IDs non-zero — PASS. |
| 4   | BridgeRelayer::OnWatchEvent() dispatches v2 events with values[5] as Hash256 (REQ-V2-04) | ✓ VERIFIED | `src/account/BridgeRelayer.cpp:264-298` — `holds_alternative<ByteBuffer>` → v1 path; `holds_alternative<Hash256>` → v2 path: extracts 32-byte X via `std::get<Hash256>`, extracts bool parity from `values[6]` with bounds+type guards, calls `eth::DecompressXOnlyPubkey`. OnWatchEventDispatchesV2Event + OnWatchEventV1StillWorks PASS. |
| 5   | Decompress 32-byte X-only key to full X+Y destination (REQ-V2-05) | ✓ VERIFIED | `evmrelay/src/eth/secp256k1_utility.cpp:175-251` — full implementation using libsecp256k1: reject all-zero X → prefix from parity bool → reverse contract bytes to big-endian → `secp256k1_ec_pubkey_parse` → serialize uncompressed → extract Y from indices 33..64 → reverse to contract order → hex concat (128 chars, "0x" stripped). 5 new tests (DecompressEvenYKnownVector, DecompressOddYKnownVector, DecompressInvalidX, DecompressInvalidXNotOnCurve, DecompressRoundTrip) all PASS. |
| 6   | 128-char hex destination matches `GetAddress()` format (REQ-V2-06) | ✓ VERIFIED | `evmrelay/src/eth/secp256k1_utility.cpp:243-249` strips the "0x" prefix from each hex half so output is plain 128-char hex (matching `GeniusAccount::PUBLIC_KEY_HEX_LENGTH=128`). DecompressMatchesKnownVector test asserts `dest->size()==128` and the X half equals the contract-order input X — PASS. |
| 7   | PerformStartupCatchupScan queries BOTH v1 and v2 topic0 hashes (REQ-V2-07) | ✓ VERIFIED | `src/account/GeniusNode.cpp:2096-2102,2352-2416` — v1 topic0 from `BridgeSourceBurned`, v2 topic0 from `BridgeOutInitiated` via `event_signature_hash()`. Two separate `eth_getLogs` RPC calls (v1 id=1, v2 id=2) because EventFilter topics are single-value-per-position. Shared `seen_tx_hashes` set dedupes (D-12). Best-effort per-query error handling. startup_wiring_test BridgeOutInitiatedTopic0IsDeterministic + V1andV2Topic0Differ + V2Topic0IsNonZero PASS. |
| 8   | Catch-up scan decompresses v2 X-only keys before calling MintFunds (REQ-V2-08) | ✓ VERIFIED | `src/account/GeniusNode.cpp:2248-2325` — `is_v2` branch fetches BridgeOutInitiated non-indexed params from `event_registry().params_for()`, calls `decode_log_data()`, validates decoded count >= 6, type-guards Hash256 at index 4 and bool at index 5 via `holds_alternative`, calls `DecompressXOnlyPubkey(x_bytes, destination_y_odd)`, passes resulting 128-char destination to `MintFunds()`. Skips+warns on decode/type/decompression failure (T-05.2-09). |
| 9   | Unit tests for secp256k1 decompression with known vectors (REQ-V2-09) | ✓ VERIFIED | `evmrelay/test/eth/secp256k1_utility_test.cpp:182-257` — 5 new tests with canonical vectors (private-key-1 even-Y, odd-Y variant, all-zero, not-on-curve 0xFF…FF, roundtrip from `secp256k1_ec_pubkey_create`). All 5 PASS; full suite 11/11. |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `evmrelay/include/eth/eth_watch_cli.hpp` | BridgeOutInitiated v2 event registration with kBytes32/kBool + retained v1 | ✓ VERIFIED | Lines 139-162 — both v1 (kBytes) and v2 (kBytes32+kBool) registrations present. |
| `evmrelay/include/eth/secp256k1_utility.hpp` | DecompressXOnlyPubkey free function declaration | ✓ VERIFIED | Lines 35-42 — Doxygen-documented 2-arg declaration (`contract_x_bytes`, `destination_y_odd`), `[[nodiscard]] ... noexcept`. |
| `evmrelay/src/eth/secp256k1_utility.cpp` | DecompressXOnlyPubkey implementation with libsecp256k1 | ✓ VERIFIED | Lines 175-251 — full implementation; uses `secp256k1_ec_pubkey_parse` + `secp256k1_ec_pubkey_serialize`; per-call context create/destroy; guards invalid X. |
| `src/account/BridgeRelayer.cpp` | Dual-watch Start() + variant-dispatch OnWatchEvent() | ✓ VERIFIED | Lines 77-201 (Start: v1+v2 watches best-effort), 210-340 (OnWatchEvent: variant dispatch + decompression + MintFunds). |
| `src/account/BridgeRelayer.hpp` | chain_watches_ updated to pair type | ✓ VERIFIED | Line 90 — `unordered_map<string, pair<EventWatchId, EventWatchId>>`. |
| `src/account/GeniusNode.cpp` | Dual topic0 catch-up scan + v2 decompression before MintFunds | ✓ VERIFIED | Lines 2091-2424 — v1+v2 signatures, two `eth_getLogs` calls, shared dedup, `decode_log_data` + `DecompressXOnlyPubkey` in is_v2 branch, MintFunds with destination. |
| `evmrelay/test/eth/secp256k1_utility_test.cpp` | 5 new decompression tests | ✓ VERIFIED | Lines 182-257 — DecompressEvenYKnownVector, DecompressOddYKnownVector, DecompressInvalidX, DecompressInvalidXNotOnCurve, DecompressRoundTrip. |
| `evmrelay/test/eth/eth_watch_cli_test.cpp` | 4 new EventRegistry v2 tests | ✓ VERIFIED | Lines 232-272 — RegistryContainsBridgeOutInitiated, BridgeOutInitiatedParam5IsBytes32, BridgeOutInitiatedParam6IsBool, BridgeSourceBurnedStillRegistered. |
| `test/src/account/bridge_relayer_test.cpp` | v2 dispatch + dual-watch tests + accessor fix | ✓ VERIFIED | MakeV2BurnNotification helper + OnWatchEventDispatchesV2Event + OnWatchEventV1StillWorks + DecompressMatchesKnownVector + ChainWatches accessor updated to pair map. |
| `test/src/startup/startup_wiring_test.cpp` | 3 v2 topic0 tests | ✓ VERIFIED | Lines 40-41,104-156 — kBridgeEventSignatureV2 + BridgeOutInitiatedTopic0IsDeterministic + V1andV2Topic0Differ + V2Topic0IsNonZero. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `event_registry()` | BridgeOutInitiated signature with AbiParamKind::kBytes32+kBool | `register_event` call | ✓ WIRED | `eth_watch_cli.hpp:154-162` — register_event for BridgeOutInitiated. |
| `DecompressXOnlyPubkey` declaration | libsecp256k1 `secp256k1_ec_pubkey_parse` | 33-byte compressed key [02/03+X] | ✓ WIRED | `secp256k1_utility.cpp:201-216` builds compressed then calls pubkey_parse. |
| `BridgeRelayer::Start()` | BridgeOutInitiated params + watch_event | `event_registry().params_for()` + `watch_service_->watch_event()` | ✓ WIRED | `BridgeRelayer.cpp:95-96,172` — params_v2 fetched and passed to watch_event. |
| `BridgeRelayer::OnWatchEvent()` values[5] | `DecompressXOnlyPubkey()` in secp256k1_utility.hpp | `holds_alternative<Hash256>` check → decompression call | ✓ WIRED | `BridgeRelayer.cpp:269-292` — Hash256 branch extracts X and bool, calls DecompressXOnlyPubkey, moves result into destination. |
| `GeniusNode::PerformStartupCatchupScan` v2 topic0 | `event_signature_hash(BridgeOutInitiated)` | second `eth_getLogs` RPC call | ✓ WIRED | `GeniusNode.cpp:2101-2102,2377-2416` — v2 sig hashed, second filter, second RPC call. |
| Catch-up scan v2 log processing | `DecompressXOnlyPubkey()` in secp256k1_utility.hpp | `decode_log_data` + X-only bytes extraction | ✓ WIRED | `GeniusNode.cpp:2271-2316` — decode_log_data → holds_alternative guards → DecompressXOnlyPubkey → MintFunds with destination. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| BridgeRelayer::OnWatchEvent | `destination` (v2 path) | `DecompressXOnlyPubkey(x_bytes from values[5], destination_y_odd from values[6])` | Yes — libsecp256k1 derives real Y from X+parity | ✓ FLOWING |
| BridgeRelayer::OnWatchEvent | `destination` (v1 path) | `hex_bytes(sgns_dest_bytes from values[5])` — 64-byte ByteBuffer | Yes — full pubkey from event | ✓ FLOWING |
| GeniusNode catch-up scan v2 | `destination` | `DecompressXOnlyPubkey(x_bytes from decoded[4], destination_y_odd from decoded[5])` | Yes — decode_log_data extracts from log.data then decompresses | ✓ FLOWING |
| MintFunds call (both relayer + catch-up) | 5th param `destination` | Set by v1 or v2 dispatch path above | Yes — non-empty for v2 (decompressed 128 chars), empty for v1 catch-up (unchanged pre-existing behavior) | ✓ FLOWING |

### Behavioral Spot-Checks

All test binaries were run directly by the verifier (independent of agent self-report). Test claims in SUMMARY.md were not trusted until re-run.

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| secp256k1 decompression — 5 new tests + 6 pre-existing | `./test_bin/secp256k1_utility_test` (evmrelay build dir) | 11/11 PASS — exit 0 | ✓ PASS |
| EventRegistry v2 registration — 4 new tests | `./test_bin/eth_watch_cli_test --gtest_filter="*BridgeOutInitiated*:*BridgeSourceBurned*"` (evmrelay build dir) | 4/4 PASS — exit 0 | ✓ PASS |
| BridgeRelayer v2 dispatch + dual-watch — 3 new + 19 pre-existing | `./test_bin/bridge_relayer_test` (main build dir) | 22/22 PASS — exit 0 | ✓ PASS |
| Startup wiring v2 topic0 — 3 new + 21 pre-existing | `./test_bin/startup_wiring_test` (main build dir) | 24/24 PASS — exit 0 | ✓ PASS |
| Full genius_node target compiles with v2 changes | `ninja genius_node` | 3/3 steps OK — exit 0, no warnings on BridgeRelayer.cpp/GeniusNode.cpp | ✓ PASS |

**Note:** `startup_wiring_test` initially appeared to fail 2 tests when invoked from a non-build CWD (`ConfigFileHasCorrectChainIds`, `ArchitectureObserverContractsAreInPlace`). The failures were `file.is_open() == false` — the tests read config files via relative paths from the build directory. Re-running from `build/OSX/Debug/` produced 24/24 PASS. This is a CWD-dependent test fixture artifact, **not a 05.2 regression**. The orchestrator's claim of "24/24 ✓" is confirmed correct when run from the build dir.

### Probe Execution

Step 7c: SKIPPED. No `scripts/*/tests/probe-*.sh` exist for this phase; phase is C++ library/test changes with no shell-probe verification model. The validation strategy (`05.2-VALIDATION.md`) uses Google Test, not shell probes.

### Requirements Coverage

The orchestrator prompt requires cross-referencing REQ-V2-01 through REQ-V2-09 against REQUIREMENTS.md. **Note:** these IDs are NOT defined in `.planning/REQUIREMENTS.md` (which contains only PEND-* and TXSTATE-* IDs for a separate pending-proposal feature). The REQ-V2-* IDs are defined canonically in `.planning/phases/05.2-.../05.2-RESEARCH.md:64-74` and referenced from `.planning/ROADMAP.md:161` as this phase's requirements. This is a documentation-location discrepancy but the requirements themselves are well-defined and traceable.

| Requirement | Source Plan | Description (from RESEARCH.md) | Status | Evidence |
| ----------- | ----------- | ------------------------------- | ------ | -------- |
| REQ-V2-01 | 05.2-01 | Register BridgeOutInitiated in EventRegistry | ✓ SATISFIED | eth_watch_cli.hpp:154-162; 3 EventRegistryTest cases PASS |
| REQ-V2-02 | 05.2-01 | Keep BridgeSourceBurned for backward-compat catch-up | ✓ SATISFIED | eth_watch_cli.hpp:139-145; BridgeSourceBurnedStillRegistered PASS |
| REQ-V2-03 | 05.2-02, 05.2-04 | BridgeRelayer::Start() registers both v1+v2 watches | ✓ SATISFIED | BridgeRelayer.cpp:87,95,154,172; MultiChainStart PASS |
| REQ-V2-04 | 05.2-02, 05.2-04 | OnWatchEvent decodes v2 events with values[5] as Hash256 | ✓ SATISFIED | BridgeRelayer.cpp:269-292; OnWatchEventDispatchesV2Event PASS |
| REQ-V2-05 | 05.2-01, 05.2-04 | Decompress 32-byte X-only key to full X+Y destination | ✓ SATISFIED | secp256k1_utility.cpp:175-251; 5 unit tests + DecompressMatchesKnownVector PASS |
| REQ-V2-06 | 05.2-01 | Construct 128-char hex destination matching GetAddress() | ✓ SATISFIED | secp256k1_utility.cpp:243-249 strips "0x" prefix; DecompressMatchesKnownVector asserts size==128 |
| REQ-V2-07 | 05.2-03, 05.2-04 | Catch-up scan queries both v1 and v2 topic0 | ✓ SATISFIED | GeniusNode.cpp:2096-2102,2352-2416; 3 startup_wiring_test v2 topic0 tests PASS |
| REQ-V2-08 | 05.2-03 | Catch-up scan decompresses v2 X-only keys before MintFunds | ✓ SATISFIED | GeniusNode.cpp:2248-2325 — full decode→decompress→MintFunds path |
| REQ-V2-09 | 05.2-01, 05.2-04 | Unit tests for secp256k1 decompression with known vectors | ✓ SATISFIED | secp256k1_utility_test.cpp:182-257 — 5 new tests PASS |

**Orphaned requirements:** None. Every REQ-V2-* ID declared in ROADMAP.md:161 is claimed by at least one PLAN's `requirements:` field and has verifiable codebase evidence. No additional REQ-V2-* IDs appear in REQUIREMENTS.md mapping that would be orphaned.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `src/account/GeniusNode.cpp` | 1252 | `//TODO - Make it async...` | ℹ️ Info (pre-existing) | Pre-existing debt marker unrelated to 05.2 work (HoldEscrow path). Not introduced by this phase — confirmed by commit `3bb4c5bf` scope which only touched catch-up scan region (lines 2078+). |

**05.2-introduced debt markers:** None. Scanned `secp256k1_utility.cpp`, `BridgeRelayer.cpp`, `GeniusNode.cpp`, `eth_watch_cli.hpp`, `secp256k1_utility.hpp`, all test files — no `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/`placeholder`/`coming soon`/`not yet implemented` introduced by this phase.

**Stub patterns:** None. No `return null` / `return {}` / `=> {}` empty implementations in the new code paths. The `DecompressXOnlyPubkey` function returns `std::nullopt` only on legitimate failure modes (all-zero X, parse failure) per design (D-09, T-05.2-02 mitigation) — this is correct error handling, not a stub.

### Human Verification Required

The VALIDATION.md (`05.2-VALIDATION.md:60-61`) lists two manual-only verifications that the planning team deferred. These cannot be verified by grep or test execution:

### 1. Y-parity from bool destinationYOdd for both even-Y and odd-Y real accounts

**Test:** With a deployed v2 bridge contract on testnet, trigger `bridgeOut` for accounts whose SuperGenius public key has even Y AND for accounts whose key has odd Y. Confirm both BridgeRelayer and the catch-up scan produce the correct `GetAddress()` match.
**Expected:** Both parity classes decompress to destinations matching the sender's `GetAddress()`.
**Why human:** Requires a deployed v2 contract and real on-chain transactions. Unit tests cover the deterministic parity→prefix mapping (`DecompressXOnlyPubkey(x, false)` vs `(x, true)` both PASS), but end-to-end against a real contract is out of reach for automated verification. Both parity cases are unit-tested for the decompression primitive itself.

### 2. V1+V2 coexistence on testnet after contract upgrade

**Test:** Deploy v2 contract, trigger `bridgeOut`, confirm both BridgeRelayer dual-watch and catch-up scan process the v2 event correctly while v1 events from prior contract versions still process.
**Expected:** Both event types observed and processed; v1 path unchanged, v2 path produces correct destination.
**Why human:** Requires a live testnet deployment and the contract team's coordination. The dispatch logic is unit-tested for both variant types (OnWatchEventDispatchesV2Event + OnWatchEventV1StillWorks both PASS), but on-chain coexistence is not.

---

These two items are flagged `Needs Human` per the planning team's own VALIDATION.md Manual-Only table. They are deployment-time verifications, not code-completeness gaps — the code paths are fully implemented and unit-tested.

### Gaps Summary

**No gaps.** All 9 truths (one per requirement) VERIFIED. All artifacts exist, are substantive (not stubs), and are wired. Data-flow traces confirm real data flows through all v2 paths to `MintFunds`. All four test suites pass independently when run by the verifier (not just per agent self-report). Build is clean (`ninja genius_node` exits 0).

The two human-verification items above are flagged `human_needed` per VALIDATION.md but they are deployment-time verifications on a live testnet — they do not indicate incomplete code. The orchestrator's critical_context noted 2 code-review blockers (CR-01 amount=0, CR-02 dev_config_.TokenID) that are PRE-EXISTING from Phase 5 (present at commit 848f58e2 before any 05.2 work) and explicitly out of scope for 05.2 verification per the orchestrator's instruction.

The orchestrator's prompt declared `Mode: mvp` — see the MVP Mode section above for the user-story format discrepancy. The technical goal itself is fully achieved; the discrepancy is about how the goal is phrased, not about whether the work is done.

---

_Verified: 2026-06-19T01:41:27Z_
_Verifier: Claude (gsd-verifier)_
