---
phase: 05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated
plan: 04
type: execute
wave: 3
depends_on: ["05.2-02", "05.2-03"]
files_modified:
  - test/src/account/bridge_relayer_test.cpp
  - test/src/startup/startup_wiring_test.cpp
  - evmrelay/test/eth/eth_watch_cli_test.cpp
autonomous: true
requirements:
  - REQ-V2-03
  - REQ-V2-04
  - REQ-V2-07
  - REQ-V2-09
tags: [bridge, v2, tests, event-dispatch, catchup-scan]

must_haves:
  truths:
    - "BridgeRelayer v2 unit tests pass: Start registers both v1 and v2 watches, OnWatchEvent dispatches v1 (ByteBuffer) and v2 (Hash256) correctly"
    - "Decompressed destination from known X verifies against known test vector"
    - "Catch-up scan tests verify dual topic0 computation and v2 signature hash determinism"
    - "EventRegistry tests confirm both BridgeSourceBurned and BridgeOutInitiated are registered with correct param kinds"
  artifacts:
    - path: "test/src/account/bridge_relayer_test.cpp"
      provides: "v2 event dispatch and dual-watch tests"
      contains: "BridgeOutInitiated"
    - path: "test/src/startup/startup_wiring_test.cpp"
      provides: "v2 catch-up scan topic0 tests"
      contains: "BridgeOutInitiated"
    - path: "evmrelay/test/eth/eth_watch_cli_test.cpp"
      provides: "EventRegistry v2 registration verification"
      contains: "BridgeOutInitiated"
  key_links:
    - from: "bridge_relayer_test cases"
      to: "BridgeRelayer::OnWatchEvent via BridgeRelayerTestAccess"
      via: "MakeV2BurnNotification helper → TestAccess::OnWatchEvent"
      pattern: "Hash256.*values"
    - from: "startup_wiring_test cases"
      to: "event_signature_hash(BridgeOutInitiated...)"
      via: "eth::abi::event_signature_hash()"
      pattern: "BridgeOutInitiated.*event_signature_hash"
---

<objective>
Add test coverage for the v2 bridge changes implemented in Plans 01-03. Tests verify: (1) EventRegistry contains both v1 and v2 events with correct param kinds, (2) BridgeRelayer registers dual watches and dispatches v1/v2 events correctly, (3) Catch-up scan computes correct v2 topic0 hash. Plan 01 already covered decompression unit tests; this plan covers the integration points.

Purpose: Ensure the v1+v2 coexistence works correctly end-to-end — event registration, watch registration, event dispatch, and topic0 computation. Catches regressions like wrong param kinds (kBytes vs kBytes32), incorrect variant dispatch, and missing v2 topic0 in catch-up scan.

Output: Passing tests for EventRegistry v2 registration, BridgeRelayer v2 event handling, and catch-up scan v2 topic0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated-/05.2-CONTEXT.md
@.planning/phases/05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated-/05.2-RESEARCH.md
@.planning/phases/05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated-/05.2-01-PLAN.md
@.planning/phases/05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated-/05.2-02-PLAN.md
@.planning/phases/05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated-/05.2-03-PLAN.md

<interfaces>
<!-- Key types and contracts needed by test code. -->

From evmrelay/include/eth/eth_watch_cli.hpp (EventRegistry):
- `eth::cli::event_registry()` — process-wide singleton
- `event_registry().lookup(sig)` returns const vector<AbiParam>* or nullptr
- `event_registry().params_for(sig)` returns vector<AbiParam> (empty if unknown)

From evmrelay/include/eth/abi_decoder.hpp:
- `eth::abi::event_signature_hash(const std::string&)` → Hash256
- `AbiParamKind::kBytes32` vs `AbiParamKind::kBytes`
- `codec::Hash256 = std::array<uint8_t, 32>`
- `codec::ByteBuffer = std::vector<uint8_t>`

From evmrelay/include/eth/secp256k1_utility.hpp (after Plan 01):
```cpp
std::optional<std::string> DecompressXOnlyPubkey(const std::array<uint8_t, 32>&) noexcept;
```

From test/src/account/bridge_relayer_test.cpp (existing test infrastructure):
- `BridgeRelayerTestAccess` — friend accessor for OnWatchEvent and chain_watches_ (line 26-52)
- `MakeBurnNotification(sender, token_id, amount, src_chain, dest_chain, tx_hash, sgns_dest)` — builds v1 notification (lines 57-95)
- `kTestSgnsDestination` — 128-char hex test destination (lines 98-100)
- `CreateForTest(logger)` — factory for BridgeRelayer without TransactionManager (line 44-51)
- ChainWatches returns `const std::unordered_map<std::string, eth::EventWatchId>&` (line 38-42) — NOTE: After Plan 02, this type changed to `std::pair<EventWatchId, EventWatchId>` — accessor must be updated

From test/src/startup/startup_wiring_test.cpp (existing test infrastructure):
- `kBridgeEventSignature` at line 34-35
- `StartupWiringTest` test suite
- Tests for topic0 determinism, config parsing, chain ID validation
- Uses `eth::abi::event_signature_hash()` for topic0 computation

From evmrelay/test/eth/eth_watch_cli_test.cpp (existing test file):
- GTest-based event registry tests
- Tests for registry lookup, param kinds, event registration
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EventRegistry v2 verification test to eth_watch_cli_test.cpp</name>
  <files>evmrelay/test/eth/eth_watch_cli_test.cpp</files>
  <read_first>
    - evmrelay/test/eth/eth_watch_cli_test.cpp (full test file — understand existing registry tests, gtest macros, includes)
    - evmrelay/include/eth/eth_watch_cli.hpp (event_registry() — both v1 and v2 events now registered after Plan 01)
    - evmrelay/include/eth/abi_decoder.hpp (AbiParamKind enum values)
    - .planning/phases/05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated-/05.2-CONTEXT.md (D-04: kBytes32 on param 5, kBool on param 6; D-05: old registration retained)
  </read_first>
  <behavior>
    - A new test (or additions to existing registry tests) verifies that `event_registry()` contains `BridgeOutInitiated` with correct param kinds.
    - Test 1 (RegistryContainsBridgeOutInitiated): `lookup("BridgeOutInitiated(address,uint256,uint256,uint256,uint256,bytes32,bool)")` returns non-null with 7 params.
    - Test 2 (BridgeOutInitiatedParam5IsBytes32): The 6th param (index 5) is `AbiParamKind::kBytes32`, NOT `kBytes`.
    - Test 3 (BridgeOutInitiatedParam6IsBool): The 7th param (index 6) is `AbiParamKind::kBool` — `bool destinationYOdd`.
    - Test 4 (BridgeSourceBurnedStillRegistered): The old v1 event is still in the registry with 6 params (D-05).
    - Tests follow existing file conventions: GTest macros, namespace usage, comments.
  </behavior>
  <action>
    Read the existing `evmrelay/test/eth/eth_watch_cli_test.cpp` to understand test patterns, then add these test cases:

    **Test — RegistryContainsBridgeOutInitiated:**
    ```cpp
    TEST(EventRegistryTest, RegistryContainsBridgeOutInitiated) {
        const auto* params = eth::cli::event_registry().lookup(
            "BridgeOutInitiated(address,uint256,uint256,uint256,uint256,bytes32,bool)");
        ASSERT_NE(params, nullptr);
        EXPECT_EQ(params->size(), 7U);  // 7 params: sender + 5 values + bool
    }
    ```

    **Test — BridgeOutInitiatedParam5IsBytes32:**
    ```cpp
    TEST(EventRegistryTest, BridgeOutInitiatedParam5IsBytes32) {
        const auto params = eth::cli::event_registry().params_for(
            "BridgeOutInitiated(address,uint256,uint256,uint256,uint256,bytes32,bool)");
        ASSERT_GE(params.size(), 7U);
        EXPECT_EQ(params[5].kind, eth::abi::AbiParamKind::kBytes32);
        EXPECT_NE(params[5].kind, eth::abi::AbiParamKind::kBytes);
        EXPECT_FALSE(params[5].indexed);  // sgnsDestination is not indexed
    }
    ```

    **Test — BridgeOutInitiatedParam6IsBool:**
    ```cpp
    TEST(EventRegistryTest, BridgeOutInitiatedParam6IsBool) {
        const auto params = eth::cli::event_registry().params_for(
            "BridgeOutInitiated(address,uint256,uint256,uint256,uint256,bytes32,bool)");
        ASSERT_GE(params.size(), 7U);
        EXPECT_EQ(params[6].kind, eth::abi::AbiParamKind::kBool);
        EXPECT_FALSE(params[6].indexed);  // destinationYOdd is not indexed
        EXPECT_EQ(params[6].name, "destinationYOdd");
    }
    ```

    **Test — BridgeSourceBurnedStillRegistered:**
    ```cpp
    TEST(EventRegistryTest, BridgeSourceBurnedStillRegistered) {
        const auto* params = eth::cli::event_registry().lookup(
            "BridgeSourceBurned(address,uint256,uint256,uint256,uint256,bytes)");
        ASSERT_NE(params, nullptr);
        EXPECT_EQ(params->size(), 6U);
        EXPECT_EQ((*params)[5].kind, eth::abi::AbiParamKind::kBytes);
    }
    ```

    Add `#include "eth/eth_watch_cli.hpp"` if not already included. Match existing test naming conventions. Do not use `sleep_for`.
  </action>
  <verify>
    <automated>cd evmrelay/build/OSX/Debug && ninja eth_watch_cli_test && ./test_bin/eth_watch_cli_test --gtest_filter="*BridgeOutInitiated*:*BridgeSourceBurned*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BridgeOutInitiated" evmrelay/test/eth/eth_watch_cli_test.cpp` returns at least 2 (2 tests reference it)
    - `grep -c "kBytes32" evmrelay/test/eth/eth_watch_cli_test.cpp` returns at least 1 (param kind assertion)
    - `grep -c "kBool" evmrelay/test/eth/eth_watch_cli_test.cpp` returns at least 1 (param 6 bool assertion)
    - `./test_bin/eth_watch_cli_test --gtest_filter="*BridgeOutInitiated*:*BridgeSourceBurned*"` exits 0
  </acceptance_criteria>
  <done>EventRegistry tests confirm BridgeOutInitiated is registered with kBytes32 param 5 and kBool param 6; BridgeSourceBurned retained; all registry tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add BridgeRelayer v2 event dispatch and dual-watch tests to bridge_relayer_test.cpp</name>
  <files>test/src/account/bridge_relayer_test.cpp</files>
  <read_first>
    - test/src/account/bridge_relayer_test.cpp (full 542-line file — understand BridgeRelayerTestAccess, MakeBurnNotification, existing test patterns)
    - src/account/BridgeRelayer.hpp (after Plan 02 — chain_watches_ has changed to pair type)
    - src/account/BridgeRelayer.cpp (after Plan 02 — Start registers both events, OnWatchEvent dispatches v1/v2)
    - evmrelay/include/eth/secp256k1_utility.hpp (DecompressXOnlyPubkey signature)
    - evmrelay/include/eth/abi_decoder.hpp (Hash256 vs ByteBuffer types for variant dispatch)
    - .planning/phases/05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated-/05.2-CONTEXT.md (D-06, D-14, D-15)
  </read_first>
  <behavior>
    - Test 1 (OnWatchEventDispatchesV2Event): Construct a v2 notification with `values[5]` as `Hash256` (32-byte X-only key). Call BridgeRelayerTestAccess::OnWatchEvent. Verify it does not throw (variant dispatch works), logs appropriately, and processes without crash. Since TransactionManager is null, it should log "no TransactionManager" (expected v2 behavior matching v1's null TM handling).
    - Test 2 (OnWatchEventV1StillWorks): The existing v1 test (OnWatchEventHandlesNullTransactionManager at line 249) already tests v1 dispatch. Verify it still passes — no regression from Plan 02 changes.
    - Test 3 (ChainWatchesTypeChanged): After Plan 02, `ChainWatches()` accessor returns `pair<EventWatchId, EventWatchId>` instead of `EventWatchId`. Verify the accessor compiles and the map structure is correct.
    - Test 4 (MakeV2BurnNotificationHelper): A new helper `MakeV2BurnNotification()` that constructs a v2 WatchEventNotification with `values[5]` as `Hash256` (32-byte X from a known test vector). Uses `std::array<uint8_t, 32>` for the X-only key.
  </behavior>
  <action>
    Read the existing `test/src/account/bridge_relayer_test.cpp` fully to understand:
    - The `MakeBurnNotification` helper (lines 57-95) — this is the v1 helper, produces ByteBuffer at values[5]
    - The `BridgeRelayerTestAccess` (lines 26-52) — ChainWatches() returns `const std::unordered_map<std::string, eth::EventWatchId>&` — this needs updating for the new pair type

    **Step 1 — Update BridgeRelayerTestAccess::ChainWatches():**
    After Plan 02, `chain_watches_` changed from `std::unordered_map<std::string, eth::EventWatchId>` to `std::unordered_map<std::string, std::pair<eth::EventWatchId, eth::EventWatchId>>`. Update the `ChainWatches()` accessor return type and the forward declaration / friend relationship accordingly.
    - New return type: `const std::unordered_map<std::string, std::pair<eth::EventWatchId, eth::EventWatchId>>&`
    - Verify the `BridgeRelayerTestAccess` friend declaration in `BridgeRelayer.hpp` still works with the new private member type.

    **Step 2 — Add MakeV2BurnNotification helper:**
    After `MakeBurnNotification` (around line 95), add a new helper `MakeV2BurnNotification`:
    ```cpp
    eth::WatchEventNotification MakeV2BurnNotification(
        const std::string& sender_hex,
        uint64_t token_id_val, uint64_t amount_val,
        uint64_t src_chain_id, uint64_t dest_chain_id,
        const std::string& tx_hash_hex,
        const std::array<uint8_t, 32>& x_only_bytes )
    ```
    This mirrors `MakeBurnNotification` but pushes `Hash256` at `values[5]` instead of `ByteBuffer`. The `x_only_bytes` parameter is a 32-byte array representing the contract-order X coordinate. Use `rlp::base::parse::hex_array(hex, arr)` to parse the hex string test input into the 32-byte array.

    **Step 3 — Add test cases:**

    **Test — OnWatchEventDispatchesV2Event:**
    - Use a known 32-byte X-only key (can use the first 32 bytes of `kTestSgnsDestination`, reversed to contract order).
    - Call `MakeV2BurnNotification(...)` with the X-only bytes.
    - Call `BridgeRelayerTestAccess::OnWatchEvent(relayer, v2_notification)`.
    - Verify no `std::bad_variant_access` is thrown (the `std::holds_alternative<Hash256>` branch is hit).
    - Since TM is null, it logs "no TransactionManager" — verify no crash.

    **Test — OnWatchEventV1StillWorks:** (Regression test)
    - This is the existing test at line 249. Verify it still passes with the updated BridgeRelayer.
    - If this test uses ChainWatches(), update for the new pair type.
    - No new test to add — just verify existing tests compile and pass.

    **Test — DecompressMatchesKnownVector:** (Integration test for REQ-V2-05)
    - Use a known secp256k1 test vector with a verified X and expected Y.
    - Call `eth::DecompressXOnlyPubkey(x_bytes)` directly (no BridgeRelayer needed).
    - Assert the result has length 128 and the first half matches the input X hex.
    - NOTE: This test requires the secp256k1_utility header. Include `eth/secp256k1_utility.hpp`.

    Follow existing test conventions: gtest without sleep_for, Allman bracing, 4-space indent.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ninja bridge_relayer_test && ./test_bin/bridge_relayer_test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MakeV2BurnNotification" test/src/account/bridge_relayer_test.cpp` returns at least 1
    - `grep -c "Hash256" test/src/account/bridge_relayer_test.cpp` returns at least 1 (in v2 helper)
    - `grep -c "DecompressXOnlyPubkey" test/src/account/bridge_relayer_test.cpp` returns at least 1
    - `grep -c "pair.*EventWatchId" test/src/account/bridge_relayer_test.cpp` returns at least 1 (updated ChainWatches)
    - `grep -c "sleep_for" test/src/account/bridge_relayer_test.cpp` returns 0
    - `./test_bin/bridge_relayer_test` exits 0 with all tests passing (existing + new)
  </acceptance_criteria>
  <done>BridgeRelayer tests verify v2 event dispatch, v1 regression safety, and decompression integration; all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add v2 catch-up scan topic0 tests to startup_wiring_test.cpp</name>
  <files>test/src/startup/startup_wiring_test.cpp</files>
  <read_first>
    - test/src/startup/startup_wiring_test.cpp (full file — understand existing topic0 tests at lines 62-94, config parsing tests, gtest patterns)
    - src/account/GeniusNode.cpp (PerformStartupCatchupScan after Plan 03 — v2 event signature string)
    - evmrelay/include/eth/abi_decoder.hpp (event_signature_hash signature)
    - .planning/phases/05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated-/05.2-CONTEXT.md (D-11, D-12)
  </read_first>
  <behavior>
    - Test 1 (BridgeOutInitiatedTopic0IsDeterministic): `event_signature_hash("BridgeOutInitiated(address,uint256,uint256,uint256,uint256,bytes32,bool)")` produces same hash on two calls.
    - Test 2 (V1andV2Topic0Differ): The v1 `BridgeSourceBurned` and v2 `BridgeOutInitiated` topic0 hashes are different (otherwise they'd collide in catch-up scan dedup).
    - Test 3 (V2Topic0IsNonZero): The v2 topic0 hash is not all zeros.
    - Follow existing test patterns in the file (lines 62-94 serve as direct templates).
  </behavior>
  <action>
    In `test/src/startup/startup_wiring_test.cpp`, add these test cases after the existing `DifferentSignaturesProduceDifferentTopic0` test (around line 95):

    **Step 1 — Add v2 event signature constant:**
    After the existing `kBridgeEventSignature` (line 34-35), add:
    ```cpp
    static const std::string kBridgeEventSignatureV2 =
        "BridgeOutInitiated(address,uint256,uint256,uint256,uint256,bytes32,bool)";
    ```

    **Step 2 — Add test cases:**

    **Test — BridgeOutInitiatedTopic0IsDeterministic:**
    - Call `eth::abi::event_signature_hash(kBridgeEventSignatureV2)` twice.
    - `EXPECT_EQ(hash1, hash2)` — same input, same output.
    - Verify hash is not all zeros (loop checking each byte, `EXPECT_FALSE(all_zero)`).

    **Test — V1andV2Topic0Differ:**
    - Compute `event_signature_hash(kBridgeEventSignature)` (v1) and `event_signature_hash(kBridgeEventSignatureV2)` (v2).
    - `EXPECT_NE(v1_hash, v2_hash)` — different event signatures must produce different topic0 hashes.

    **Test — V2Topic0IsNonZero:**
    - Compute `event_signature_hash(kBridgeEventSignatureV2)`.
    - Verify hash is non-zero.
    - Verify hash length is 32 bytes.

    Follow existing test conventions exactly (same TEST naming prefix `StartupWiringTest`, same assertion patterns). Do NOT add sleep_for. Do NOT add new includes — all required headers are already included.
  </action>
  <verify>
    <automated>cd build/OSX/Debug && ninja startup_wiring_test && ./test_bin/startup_wiring_test --gtest_filter="*BridgeOutInitiated*:*V1andV2*:*V2Topic0*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BridgeOutInitiated" test/src/startup/startup_wiring_test.cpp` returns at least 2 (constant + test references)
    - `grep -c "kBridgeEventSignatureV2" test/src/startup/startup_wiring_test.cpp` returns at least 1
    - `grep -c "sleep_for" test/src/startup/startup_wiring_test.cpp` returns 0
    - `./test_bin/startup_wiring_test` exits 0 with all tests passing (existing + 3 new)
  </acceptance_criteria>
  <done>Startup wiring tests verify v2 topic0 hash is deterministic, differs from v1, and is non-zero; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test helpers → BridgeRelayer | Test-constructed ABI values simulate real RPC event data; must match actual production types |
| Test fixtures → catch-up scan topic0 | keccak256 computation tested for both signatures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-10 | Tampering | Test helper constructs wrong ABI variant for v2 values[5] | mitigate | MakeV2BurnNotification uses `std::get<Hash256>` to verify correct variant type; test assertion catches wrong-param-kind registration (REQ-V2-01 test already covers this) |
| T-05.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all test dependencies already present (gtest, evmrelay) |
</threat_model>

<verification>
- `./test_bin/eth_watch_cli_test --gtest_filter="*BridgeOutInitiated*:*BridgeSourceBurned*"` exits 0
- `./test_bin/bridge_relayer_test` exits 0
- `./test_bin/startup_wiring_test --gtest_filter="*BridgeOutInitiated*:*V1andV2*:*V2Topic0*"` exits 0
- `ninja eth_watch_cli_test bridge_relayer_test startup_wiring_test` builds clean
</verification>

<success_criteria>
- EventRegistry tests confirm v2 event registered with kBytes32 and v1 event retained
- BridgeRelayer tests verify v2 variant dispatch works and v1 regression passes
- Catch-up scan tests verify v2 topic0 hash is deterministic and differs from v1
- All test suites build and pass without regressions
</success_criteria>

<output>
Create `.planning/phases/05.2-bridge-v2-x-only-compressed-encoding-smart-contract-updated-/05.2-04-SUMMARY.md` when done
</output>
