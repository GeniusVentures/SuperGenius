# Quick Task 260602-k34: Separate InputValidator Derived Classes

**Description:** Separate InputValidator derived classes into their own headers and sources.

## Tasks

1. Split declarations
   - Keep `IInputValidator` in `src/account/InputValidators.hpp`.
   - Move `GeniusInputValidator` to `src/account/GeniusInputValidator.hpp`.
   - Move `PublicChainInputValidator` and `WeightedRpcEndpoint` to `src/account/PublicChainInputValidator.hpp`.

2. Split implementations
   - Move Genius-specific validation logic to `src/account/GeniusInputValidator.cpp`.
   - Move public-chain validation logic to `src/account/PublicChainInputValidator.cpp`.
   - Leave shared or base-only content in `InputValidators.cpp` if needed.

3. Wire build and includes
   - Update `src/account/CMakeLists.txt`.
   - Update consumers to include the concrete validator headers where concrete types are used.
   - Run a focused build or compile verification when available.
