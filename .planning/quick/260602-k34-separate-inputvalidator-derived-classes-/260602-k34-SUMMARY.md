---
status: complete
quick_id: 260602-k34
date: 2026-06-02
commit: not committed
---

# Quick Task 260602-k34 Summary

## Completed

- Kept `IInputValidator` in `src/account/InputValidators.hpp`.
- Moved native Genius validation into `src/account/GeniusInputValidator.hpp` and `src/account/GeniusInputValidator.cpp`.
- Moved public-chain validation and `WeightedRpcEndpoint` into `src/account/PublicChainInputValidator.hpp` and `src/account/PublicChainInputValidator.cpp`.
- Updated `TransactionManager`, `GeniusNode`, and `ChainRpcEndpointProvider` includes to use the concrete validator headers where concrete types are used.
- Updated `src/account/CMakeLists.txt` so both new validator sources build into `genius_node` and `genius_node_test`.
- Made `ChainRpcEndpointProvider.cpp` endpoint aggregate initialization explicit to avoid missing-field warnings.

## Verification

- `git diff --check`
- `cmake --build build/OSX/Debug --target genius_node_test -j 4`

## Notes

- Code commit was not created because elevated git index write permission was not approved.
